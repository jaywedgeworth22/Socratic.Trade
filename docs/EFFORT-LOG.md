- **[Socratic.Trade][KIMI] Effort Issues Sync hang fix — 30s request timeout + 10min job backstop (branch `kimi/effort-sync-timeout`) — In Progress 2026-07-29.** `sync-effort-issues.py` called `urllib.request.urlopen` with no timeout; stalled connections hung the job 10+ min twice on 2026-07-29, squatting the single socratic-ci runner and stalling the whole PR queue (both runs manually cancelled). Adds per-request `timeout=30` and `timeout-minutes: 10` job backstop. PR open, auto-merge armed.
