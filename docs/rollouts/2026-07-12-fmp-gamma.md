# FMP Gamma Integration

- Summary: Built integration for House of Representatives API, Earnings/Economic Calendars, Earnings Call Transcripts, and FMP Market News in a new file `src/lib/fmp-gamma.ts`.
- Why: Requested by user to isolate FMP API logic and avoid modifying existing providers.
- Files:
  - `src/lib/fmp-gamma.ts`
- Verification:
  - Tested endpoints using an ephemeral test script (deleted after). Note: All tested FMP endpoints currently return 403 "Legacy Endpoint" errors due to the subscription tier limitations of the API key in `.env.local`. Code handles this and falls back to empty arrays to prevent crashes.
  - Ran `npm run lint` (0 related errors) and `npx tsc --noEmit` (clean).
- Follow-ups: The FMP API key must be upgraded to access the new endpoints `/v3/` and `/v4/`.
