# 2026-07-13 — Candlestick "ST" monogram favicon

## Summary

Replaced the favicon / app icons with an interlocking candlestick **"ST"** monogram
that matches the on-page brand logo's candlestick-wordmark style (owner request).
The letters are spelled in candlesticks — thin wicks + varied rounded bodies —
green-biased with reds mixed in, on the existing dark rounded-square background.

## Why / decisions

- Owner wanted the tab icon to be an "ST" spelled with candlesticks like the header
  logo. Several hand-rolled monogram attempts (custom sampler + overlap/stagger) read
  as an abstract candle cluster, not clean letters — owner (rightly) pushed to just
  use the real logo code.
- **Final approach: the EXACT header-logo pipeline, verbatim.** The icon is
  `sampleWordmark("ST")` → `buildTickerUnits()` → `drawTicker` geometry, copied
  byte-for-byte from `app/console/ui/candle-ticker.ts` into the (throwaway) generator.
  So the favicon is the identical candlestick rendering as the on-page wordmark, just
  spelling "ST" — it reads as clean letters, and the candles vary like the logo.
- **Colour = tick 6** of the ticker: green-biased, the tall T stem lands on a green
  candle (no bright-pink column, per owner). Rendered on the existing dark
  rounded-square background; only `y`/geometry differ from a single centered wordmark.
- Owner signed off on this version 2026-07-15 ("make that logo the favicon").

## Files

- `public/icon.svg` — the SVG favicon (browser tabs).
- `public/icons/icon-192.png`, `icon-512.png` — PWA/manifest icons.
- `public/icons/apple-touch-icon-180.png` — iOS home-screen icon.

`app/layout.tsx` already references all four paths (unchanged). The icons were
generated by rasterizing the letters + emitting SVG in headless Chromium (the same
sampling the logo uses at runtime); the generator was a throwaway script, not
committed.

## Verification

- `npx tsc --noEmit` 0 · `npm run build` exit 0 (static-asset swap; layout metadata
  references resolve).
- Visual review at 512 / 192 / 64 / 32 px: reads as a candlestick "ST" at icon and
  PWA sizes; at a 16–32px tab it is a stylized candle cluster (spelling letters in
  candlesticks is inherently dense at that size).

## Follow-ups

- Optional: single-candle T stem (currently ~2 columns) if a crisper T is wanted;
  tune letter spacing.

**2026-08-17 update:** issue #2731 crops this same pipeline mark, raises S above T,
and drops the background.  Website only — see
`docs/rollouts/2026-08-17-favicon-crop-st.md`.
