# 2026-07-19 — MONET cloud-session handoff (PR sweep, junk cleanup, metering fix, bge-m3 recon)

Handoff note for a MONET cloud session. Covers all work done, current state, open
items, and a verified answer (from code recon) to the owner's bge-m3-vs-voyage-finance-2
question. Written on `main` @ `7be71390`.

## 1. Work completed this session (all merged + deployed unless noted)

**PR sweep — owner directive "ensure all open PRs have conflicts + comments resolved, merged, deployed":**
- **#1727** (editable connected-account name + legacy-app retirement) — merged earlier (`b0063a7`), live.
- **#1745** — docs board flip (EFFORT-LOG `#1727` row → Deployed) + deploy-verification note. Resolved
  4 rounds of Codex threads (chronology correction; row relocation; handoff-doc P1 → added rollout
  note + STATUS entry; two **phantom-sha** author nits — shas `3b66da2` / `66721997` that
  `git cat-file -t` cannot resolve, i.e. not real commits — replied non-actionable + resolved,
  no push). Merged.
- **#1736** (model-identity canonicalizer, MONET) — reviews already resolved; merged current main in
  (clean, no conflict) to clear a stale `dirty`; merged.
- **#1735** (SEC RAG migration v52, AG) — merged by owner (had one open money-path "proposal
  attribution" finding at merge; owner's call).
- **#1740** (admin console shell, Codex) — its 2 open findings (mobile wordmark overflow; logout-route
  prefetch) were already fixed in-head; replied + resolved; pushed a clean catch-up merge; merged.
- **#1754** (repo hygiene, MONET) — removed **~9 MB of tracked generated artifacts** #1735 had landed
  on main (`error.log`, `lint-output.txt`, `lint-results.txt/.json`, `lint_results.json`,
  `eslint-report.json`, `eslint_test_results.json`, autogenerated `.codex/environments/environment.toml`)
  + added `.gitignore` patterns. Confirmed unreferenced by any source/script/CI. Merged; verified 8/8
  gone from main.

**Deploy verification:** each merge auto-deployed via Coolify. Production tracked main through
`9a95b22c → 6f835e05 → 5c52b008 → 3cbc00c0 → …`; `/api/health` stayed healthy throughout (`db: ok`,
scheduler ticking, litestream `replicating`/0-degraded, 3 accounts trading / 0 degraded).

**Metering fix — PR #1771 (OPEN, auto-merge enabled):**
`fix(rag-metering): correct SiliconFlow bge-m3 embed price 10x undercount`. Branch
`monet/fix-siliconflow-bge-m3-price`, head `bfba4614`.
- `SILICONFLOW_PRICE_PER_1K_TOKENS["BAAI/bge-m3"].embed` was `0.00001 / 10` (= `0.000001`), 10× below
  its own comment and the parallel confirmed OpenRouter `baai/bge-m3` rate (`0.00001` = $0.01/1M).
  It undercounted SiliconFlow bge-m3 embed spend in `rag_usage.cost_est_usd` + the `$/day` dispatch
  fuse. Removed the `/ 10`.
- Strengthened `test/rag-metering.test.ts` (SiliconFlow embed test now pins the exact cost instead of
  only `> 0`). **Regression guard proven**: reverting to `0.00001 / 10` makes the assertion fail
  (`expected 8e-9 to be close to 8e-8`); restoring passes.
- Local gate green: tsc 0, 11/11 tests, lint 0 errors. No live discrepancy — SiliconFlow is not prod's
  active embed provider (OpenRouter is). Rollout: `docs/rollouts/2026-07-19-siliconflow-bge-m3-embed-price-fix.md`.

## 2. Open items / next actions

- **#1771** — auto-merge (SQUASH) enabled, no Codex findings; will merge when its queued `verify`
  clears on the single-lane Hetzner runner. Nothing to do unless CI goes red (unexpected).
- **`alpha-vantage` dependency** — `/api/health` reported `ok:false` for hours, still down ~2h AFTER
  the 00:00 UTC free-tier reset → so NOT a daily-limit self-heal; likely an **expired/invalid AV key
  or endpoint/config issue**. **Low impact**: market data is fully covered by finnhub/fmp/massive/
  tiingo/twelvedata/yahoo (all `ok`). Worth a glance at the AV key when convenient; not urgent.
  (`voyage` embedding recovered earlier, coinciding with #1766's provider-aware health probe.)
- **CI throughput** — #1739 routed Actions to the single-lane self-hosted Hetzner runner (shared with
  Coolify deploy builds at concurrency 1). CI dispatches fine but runs serially; fresh PRs read
  `unstable`/`blocked` while queued — retry `enable_pr_auto_merge` once `verify` materializes.
- **Recurring Codex false-positive PATTERN — verify every occurrence, never blanket-trust.** The
  codex-connector "wrong author identity" nit has repeatedly cited SHAs that turn out not to exist
  anywhere in this repo's history — confirmed independently three times now: `15910f5e...`
  (rebutted on this PR), and `a14df5f8f813742044fae4fd6a4e5af63c18be9...` (cited again in a later
  round against this very line, re-checked and also does not resolve — `git cat-file -t` errors
  "not a valid object name" against every fetched ref, and a `github-actions[bot]` comment on that
  thread independently confirms the same). **Before dismissing any new instance of this nit, run
  `git cat-file -t <cited-sha>` yourself against a fully-fetched repo** — do not take the citation
  on faith. If a cited SHA ever DOES resolve and the identity is genuinely `Codex <codex@openai.com>`
  (or any non-noreply address), that is a real AGENTS.md git-identity violation: amend/rebase it
  before publishing, exactly like any other bad-identity commit — do not reflexively dismiss it
  just because past instances were phantom. Only reply non-actionable + resolve once you've
  personally verified the SHA is phantom; avoid pushing a no-op commit purely to re-trigger Codex
  (every push starts a fresh review round → churn loop).

## 3. bge-m3 vs voyage-finance-2 — verified answer to the owner's question

Owner asked: "I thought we were moving all to baai/bge-m3 already, thought it had advantages over
voyage-finance-2 and was also cheaper — is that true?" Answered from **code recon on `main` @
`7be71390`** (a verification workflow's 3 code-recon lanes; the model-quality research lane and
adversarial-verify lanes were stopped before completing per the "stop working" instruction, so the
QUALITY-advantage question below remains partly open).

- **"Moving all to bge-m3?" — Partly, and it's LIVE in prod, but not a hard code default.**
  Provider is selected by `resolveActiveRagProvider` (`src/lib/vector-db.ts:154-165`): explicit
  `RAG_EMBED_PROVIDER` pin first, else **key-presence precedence** (OpenRouter key → `openrouter`/
  `baai/bge-m3`; else SiliconFlow key → `siliconflow`/`BAAI/bge-m3`; else `voyage`/`voyage-finance-2`).
  The **code-level default with no alt keys is still voyage-finance-2**, but repo evidence
  (`app/api/health/route.ts:210` "prod flipped to bge-m3 via OpenRouter"; `STATUS.md:24-31`) shows
  **prod is actively running bge-m3 via OpenRouter**. Ingestion and query paths use the same resolver
  (consistent). One env flips BOTH embed and rerank (rerank → `cohere/rerank-v3.5` on OpenRouter vs
  `rerank-2.5` on Voyage). `docs/prod-config-voyage.md` was reconciled this session (banner added,
  content otherwise preserved — see that file) to stop presenting Voyage as the production default;
  it still documents accurate Voyage-tuning/gated-upgrade knowledge for the fallback path.

- **"Cheaper?" — YES, ~12× cheaper per token.** voyage-finance-2 embed = `$0.00012/1K`;
  bge-m3 via OpenRouter = `$0.00001/1K` (confirmed $0.01/1M) → exactly **12×** cheaper.
  (Note: the SiliconFlow table's `0.00001 / 10` bug made *that one row* read as 120×; PR #1771 fixes
  it back to the intended 0.00001 → also 12×.)

- **"Advantages over voyage-finance-2?" — UNPROVEN / nuanced; the switch reads as cost- and
  outage-driven, not quality-proven.** bge-m3 is a strong **general** multilingual model;
  voyage-finance-2 is a **finance-domain-specialized** model. A general-benchmark (MTEB) lead does not
  guarantee better retrieval on SEC-filing/financial text specifically. The code/rollouts frame the
  flip around **cost + avoiding a Voyage outage**, not a measured on-domain retrieval win. **Open
  item:** if retrieval quality matters, A/B bge-m3 vs voyage-finance-2 on real financial queries before
  treating bge-m3 as strictly superior. (The workflow's model-quality research lane was not run to
  completion.)

- **⚠️ MOST IMPORTANT operational finding — corpus RE-EMBED is mandatory and was PENDING.**
  bge-m3 and voyage-finance-2 are **different embedding spaces even at equal 1024 dims** — "cosine
  scores across different embedding models are meaningless even at equal dimensionality"
  (`vector-db.ts:185-186`). The code isolates spaces: a non-Voyage query gets an `embed_model $in`
  Pinecone filter (`vector-db.ts:208-217`, applied at `:5829`), so **bge-m3 queries match ONLY
  bge-stamped vectors and see ZERO of the ~8.5k existing voyage vectors**. Prod flipped to bge-m3 LIVE
  while that bge space started EMPTY, so **dense RAG retrieval degrades to sparse/no-match until the
  corpus is re-embedded**. The full 4-docType backfill is `src/lib/rag/corpus-reembed.ts` via
  `POST /api/admin/reembed` — this is the ONLY path that covers all four docTypes. **CLI
  `scripts/reindex-all.ts` and `app/api/admin/reindex-10k` are SEC 10-K/10-Q ONLY** (both drive
  `refreshFilingBodies`; neither touches `earningscalls-transcripts`, `insider-form4`, or
  `experience-memory` — confirmed by reading both call sites); do not substitute either for the
  full-corpus `/api/admin/reembed` run. (`scripts/reindex-all.ts` is being rewritten to call
  corpus-reembed directly per open PR #1775 — not yet merged as of this writing, so the SEC-only
  scope above is still the current behavior.) All of these paths are
  **operator-triggered, not automatic on flip** (`docs/rollouts/2026-07-18-corpus-reembed.md` opens
  "URGENT — run-me-now"). **ACTION for owner/next agent: confirm the bge-m3 re-embed actually ran to
  completion** (4 docTypes: sec-filings, earningscalls-transcripts, insider-form4, experience-memory —
  exact ids from `CORPUS_REEMBED_DOC_TYPES`; `resolveDocTypes()` silently drops unknown values, so a
  bare `earningscalls` would skip earnings-call re-embedding without an error; 8-K summaries
  repopulate on normal ingest cadence AND have their own admin backfill/re-embed path —
  `POST /api/admin/reindex-8k` calls `reindexEightKDataset`, which re-embeds the ENTIRE persisted
  8-K dataset from `getEightKDataset()`, not just newly-fresh filings (confirmed in `sec8k.ts`); FMP
  transcripts / congress trades / fundamentals repopulate only on normal ingest cadence with no
  admin backfill route). Do NOT
  run `action:"purge-legacy"` (deletes the voyage vectors) until the bge space is independently verified
  full — it's irreversible and would destroy the only fallback.

Key files: `src/lib/vector-db.ts` (`VOYAGE_MODEL` :113, `pinnedEmbedProvider` :131-138,
`resolveActiveRagProvider` :154-165, `activeEmbeddingModel` :177-182, space isolation :203-218,
shared `embedWithRetry` :2076), `src/lib/rag/corpus-reembed.ts`, `src/lib/rag-metering.ts` (price
tables :58-131), `app/api/health/route.ts:119-135,208-217`, `.env.example:324-346` (`RAG_EMBED_PROVIDER`
ships blank), `STATUS.md:24-31`.

## 4. Environment state at handoff

- Prod on current `main`; healthy (`/api/health` db/scheduler/litestream ok; only `alpha-vantage` down).
- Background workflow (RAG recon) and all scheduled self-check-ins were **stopped** on the "stop
  working" instruction. The 3 completed recon lanes are captured in §3 above; the model-quality
  research + adversarial-verify lanes did not run.
- Temp worktrees created this session were cleaned up; the main worktree remains on
  `monet/vigilant-fermi-220244` (stale — pre-dates this session's merged work; not used as a base here,
  all PRs branched off current `main`).

## Verification (this note)

Docs-only. No code changed. Recon findings in §3 are code-grounded (file:line citations from `main` @
`7be71390`); the QUALITY-advantage and the re-embed-completion status are flagged as OPEN (not verified
to completion) rather than asserted.
