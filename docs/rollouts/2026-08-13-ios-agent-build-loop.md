# iOS agent build-loop policy (Socratic.Trade)

Docs/hooks only.  `ios/CLAUDE.md` is the annotated onboarding file.  `xcodebuild` via bash is pre-approved.  Do not stand up Xcode MCP.  Claude PreToolUse hook blocks `.pbxproj` / entitlements / xib writes.  XcodeGen stays `ios/project.yml` → `xcodegen generate`.

Canonical: `/Users/jay/apps/AGENT-SYNC.md` § iOS agent build loop.
