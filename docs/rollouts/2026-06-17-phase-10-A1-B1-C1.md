# 2026-06-17 - phase-10-A1-B1-C1

## Summary
First execution pass of the Phase 10 plan, on branch `phase-10` (off the merged,
pushed `main`). Paper mode unchanged. `tsc` + 136 tests + build green; live scrapes
verified.

- **A1 — positioning sub-score.** New `positioning` `ScoringWeights` factor
  (`positioningScore`): net congressional buying + insider open-market buying +
  squeeze-level short interest lift it, net selling dings it. `scanMarket` recomputes
  `factorBreakdown`/`score` AFTER the web-source overlay and re-sorts, so a
  freshly-disclosed smart-money name ranks up deterministically — closing the
  remaining half of Codex's P2 (scraped data now moves the score, not just the LLM
  prose). Wired through types/defaults (0.8)/normalizeWeights/tuning-LLM-schema/UI.
- **B1 — sector learning dimension.** `recordFillFromProposal` stamps `sector` (from
  the scan quote) into the fill raw; it flows onto `ClosedLot.sector`;
  `getSectorScorecard` (shrunk) feeds the agent `sectorOutcomes`.
- **C1 — SEC 8-K catalyst connector.** New `web-sources/sec8k.ts`: current-8-K atom
  feed + weekly-cached CIK→ticker map → per-symbol "filed an 8-K (material event)"
  bulletins (rolling 4-day window), overlaid into the scan/prompt + web-sources
  status. Live-verified: 94 events. Coarse (per-item 8-K detail is a follow-up).

## Also
- Merged `web-sources` → `main` and pushed `main` + `web-sources` to GitHub.
- Added `docs/phase-11-multi-user.md` (per-user analysis/keys plan; API-keys
  settings section is the first, single-user-now milestone).

## Files
- `src/lib/market.ts` (positioning score + recompute-after-overlay), `src/lib/types.ts`
  + `src/lib/defaults.ts` (ScoringWeights factor), `src/lib/strategy-tuning.ts` (schema),
  `src/lib/performance.ts` (sector on lots + `getSectorScorecard`), `src/lib/strategy.ts`
  (`sectorOutcomes`), `src/lib/web-sources/{sec8k.ts(new),index.ts,types.ts? no}`,
  `app/dashboard-types.ts`, tests (`market`, `performance`, `web-sources-sec8k`).

## Verification
`npx tsc --noEmit` · `npm test` (136) · `npm run build` — all green. Live: FINRA
11,555 / 8-K 94 / congress + insider as before.

## Next (per docs/phase-10-signals-learning-ui-v2.md)
C2 market breadth (computable from the screener rows we already fetch), B2 full
EvidenceDigest for chosen+skipped, D1 adaptive compaction + D2 prompt caching, then
E1 drilldown drawer + E2 learning matrix.
