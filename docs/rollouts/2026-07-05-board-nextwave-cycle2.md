# 2026-07-05 — Board next-wave cycle 2: stale-row corrections + new Planned rows

## Summary

Applied a full stale-row correction pass to both effort boards
(`/Users/jay/apps/TRADING-EFFORT-LOG.md`, the branch-neutral live board, and its repo-tracked
mirror `docs/EFFORT-LOG.md`), sourced from two next-wave audit specs
(`nextwave-socratic-trade.md` and `nextwave-fleet-infra.md`) generated by a fresh cross-agent
sweep of the board against live PR/git state. Also added 11 new Planned rows under a
"2026-07-05 next-wave (cycle 2)" subsection to both boards, and refreshed the top of `STATUS.md`.

No application code changed in this pass — docs/board only.

## Why

Both next-wave specs found the boards had drifted from reality in several ways: rows claiming a
merge that either didn't happen (a phantom PR number), or rows that were correctly described as
merged in their own text but physically still sitting under the "In Progress" heading (which
matters because the GitHub Issues mirror classifies issue state by *section*, not by row text).
The single most important correction is the phantom "PR #808 merged" row: it hid that a real P0
multi-user data race (`checkRegimeFlip`'s `regime:current` read-modify-write with a hardcoded
`"local"` fallback) is still live on `main` today, because the only vehicle carrying that fix is
an open, CONFLICTING PR (#805) that also carries unrelated work from a different lane.

## Corrections applied (both boards, in place, never deleting another agent's row text)

1. **Phantom "PR #808 merged" (CRITICAL).** `gh pr view 808` returns "Could not resolve to a
   PullRequest" — the PR does not exist. The described work (P0 `checkRegimeFlip` RMW fix,
   per-user `regime:current:${userId}` keys, P1 backlog items) is commit `0ce39474` on branch
   `cursor/session-2026-07-05`, confirmed NOT an ancestor of `origin/main`
   (`git merge-base --is-ancestor` fails), sitting inside **open PR #805**
   ("feat(health): Implement connection health routing & headroom monitoring") whose mergeable
   state is **CONFLICTING**. Moved the row from Completed back to In Progress on both boards with
   an honest correction note; the P0 multi-user regime race is still live on `main`.
2. **PR #811 (Console live-data build-out, CODEX)** — confirmed merged
   `2026-07-05T07:37:48Z` via `gh pr view 811`. Moved from In Progress to Completed on both
   boards.
3. **Three rows whose text said COMPLETED/merged but were still physically under the In Progress
   heading:** Pre-policy vetoes advisory-overridable (PR #814, merged `2026-07-05T10:17:03Z`),
   Full-suite test determinism de-flake (PR #812, merged `2026-07-05T08:46:56Z`), Guardrails →
   overridable preferences denylist (PR #799, merged `2026-07-05T06:02:51Z`) — all confirmed
   merged via `gh pr view`, relocated to the Completed section on both boards.
4. **Admin connection health and backend-failure notification pass (AG)** — was marked Completed
   ("ready to land"); actually **open PR #805**, mergeable state **CONFLICTING**, branch also
   carries the unrelated Cursor commit `0ce39474` (see #1). Re-marked In Progress/blocked-on-
   conflict on both boards.
5. **Shared-dep tokenless git-dependency switch** — superseded: `origin/main` already pins
   `@jaywedgeworth22/congress-trading-shared` to `git+https://...#v1.2.0` (landed via PR #444) and
   `scripts/npm-ci-with-shared-deps.sh` is deleted from `main`. Closed as superseded on the live
   board (no matching row existed in the mirror to correct).
6. **`claude/ci-hybrid-runner-verify` (PR #372)** — confirmed `mergeable: UNKNOWN`/observed
   CONFLICTING via `gh pr view`, stalled since 2026-07-04, armed auto-merge cannot fire. Annotated
   on the live board (no matching row in the mirror).
7. **PR #360 (drawdown-advisory-rescope, Monet)** — confirmed merged `2026-07-04T16:15:44Z`.
   Corrected "PR pending" text in the mirror.
8. **PR #437 (w2-episodic-retrieval)** — confirmed merged `2026-07-04T21:05:02Z`. Corrected
   "done, pushed, awaiting the landing train" text on both boards; also fixed a stale "PR #437 in
   flight" mention in the live board's 2026-07-04 landing-train changelog line.
9. **w2-outcome-engine** — confirmed landed via the 2026-07-04 train (its branch is an ancestor of
   the merged Wave-2 lanes). Corrected "Pushed; NO PR" text on both boards.
10. **w2-coaching-durable / w2-reflection-decompose** — confirmed still genuinely unlanded (no PR
    exists for either branch). Flagged explicitly as needing a merge-forward + PR + auto-merge
    landing action (new Planned row, item below).
11. **PR #801 (fourteen logo concept comps)** — confirmed still OPEN via `gh pr view`; superseded
    by merged **PR #809** ("12 logo concepts", merged `2026-07-05T08:52:13Z`) plus the owner's
    same-day final selection (commit `a9cefbf4`, "Dialectic mark + named lockup saved"). No
    existing row tracked #801 on either board, so this was recorded as a standalone changelog/
    correction note rather than edited into a nonexistent row.
12. **Codex global coordination + fleet monitoring setup row** — updated with the current
    Slack relay state: `agent-sync-push` connects to Socket Mode (hello observed) but zero events
    are delivered pending an owner-side Slack Event Subscriptions toggle; `events.jsonl` does not
    exist yet. (The fleet-infra spec's NEW rows, including bootstrapping
    `/Users/jay/apps/FLEET-INFRA-EFFORT-LOG.md`, were NOT added here per the task instructions —
    they already live on that separate board.)

## New Planned rows added (both boards, "### 2026-07-05 next-wave (cycle 2)" subsection)

11 rows: disentangle PR #805; migrate `regime:current` to per-user keys post-P0-fix; owner
ratification of Rule 4's overridability (#814); production release + post-deploy verification of
the 07-05 batch; render new advisory audit kinds in the console; wire `getRedTeamEfficacy` into
the console; headline first-seen timestamps for evidence-age receipts; extend prompt fencing
beyond the money path; open PRs for the two stalled Wave-2 branches; batch typed-confirm for LIVE
approvals; sweep settings-table keys for other shared-row races. Plus one handback note: the five
MONET-tagged risk rows CLAUDE picked up cross-seat return to MONET, with the five empty
`.claude/worktrees/monet-*` worktrees noted as reclaimable.

## Files

- `/Users/jay/apps/TRADING-EFFORT-LOG.md` (live board, outside this repo/worktree)
- `docs/EFFORT-LOG.md` (this repo's tracked mirror)
- `STATUS.md` (new dated section at the top)
- `docs/rollouts/2026-07-05-board-nextwave-cycle2.md` (this file)

## Verification

- Confirmed every PR-state claim above against live GitHub state via `gh pr view <n> --json
  state,mergedAt,mergeable,title` and `git merge-base --is-ancestor 0ce39474 origin/main` before
  writing any correction.
- Diffed `docs/EFFORT-LOG.md` and `STATUS.md` against `origin/main` before landing to confirm the
  change is additive/corrective only (no deletions of other agents' row text, no rewritten history
  sections) — see the PR body for the explicit overlap-review note required by
  `LAND_ALLOW_STALE_OVERLAP=1`.
- Docs-only change; `npm ci` + the full `land.sh` gate (`tsc --noEmit`, `npm test`, `npm run
  build`) run before opening the PR per standard process, expected to pass since no `src/` files
  were touched.

## Follow-ups

- Disentangling PR #805 (splitting the Cursor P0/P1 commit from the AG health-routing work) is the
  single highest-priority follow-up — it is the only vehicle for landing the still-live P0
  multi-user regime race fix.
- `w2-coaching-durable` and `w2-reflection-decompose` need an explicit merge-forward + PR + auto-
  merge landing pass.
- Production release of the 2026-07-05 merge batch (#799 through #820) plus post-deploy money-path
  verification is still outstanding (owner action).
