# Lessons from ZhuLinsen/daily_stock_analysis - gap analysis (2026-08-12)

Owner request: "implement lessons you can learn from https://github.com/ZhuLinsen/daily_stock_analysis" (62k-star LLM stock-analysis system).  Method: 15-agent workflow - 6 parallel deep readers over the external repo, 1 synthesis pass, 8 per-lesson gap analysts over this codebase.  All 8 lessons recommended; 3 implemented in round 1 (rollout: docs/rollouts/2026-08-12-dsa-lessons-round1.md), 5 deferred as Planned board rows.

## Implemented

- **tiered-report-rendering** -> watchlist daily digest + tiered channel rendering
- **news-intel-fanout-quality-bar** -> news entity-relevance gating
- **llm-output-repair-ladder** -> proposal repair-ladder receipts + degraded-data confidence cap

## All lessons with verdicts

### structured-decision-scorecard - PLANNED (deferred, board row exists)
**Validated per-symbol decision-dashboard schema with price levels, attribution weights, and a decision-chain state machine**

The repo forces the LLM's entire per-symbol verdict into one validated JSON 'Dashboard' contract instead of prose plus loose numeric fields: core_conclusion (one-sentence thesis + separate no-position/has-position advice), a data_perspective block (MA alignment, price/MA table, volume), battle_plan.sniper_points (ideal_buy / secondary_buy / stop_loss / take_profit as explicit numeric entry-exit levels), an action_checklist of pass/warn/fail precondition items, signal_attribution (technical/news/fundamentals/market weights auto-normalized to sum to 100 via a validator), and an agent_disagreement_explanation whose Literal-typed adjustment chain must be contiguous from starting action to final action (extra='forbid', and an 'override applied' flag requires a real from/to transition).  Socratic.Trade's TradeProposal has thesis tags and reasoning text but no equivalent renderable scorecard contract; adopting a zod-validated version would give every proposal a daily dashboard report, make red-team overrides auditable as chain steps, and make price levels machine-usable for alerts and grading instead of buried in prose.

External-repo evidence: src/schemas/report_schema.py (Dashboard pydantic schema: sniper_points, signal_attribution model_validator, AgentDisagreementExplanation chain validation); templates/report_markdown.j2 (renders each dashboard block as its own scorecard section); src/utils/sniper_points.py (robust free-text price extraction fallback, e.g. excluding numbers after 'MA'); src/analyzer.py (check_content_integrity defines mandatory vs optional dashboard fields)

Gap verdict: **partial** (effort M, recommended True).  The building blocks of a 'decision scorecard' are scattered and real (thesis tag, rationale, confidence, single-level stop/target prices, red-team verdict, override receipts, a weight-normalization pattern, matured outcome tracking, notify() channels) — but they are NOT unified into one validated, renderable JSON artifact the way the candidate describes. Four concrete gaps: (1) no single `Dashboard`-shaped schema/type that bundles core_conclusion + data_perspective (MA table, volume) + battle_plan + action_checklist + signal_attribution + disagreement chain as one object with build-time invariants — the app's convention is instead many independently-optional fields appended incrementally across the pipeline; (2) sniper_points only has one entry level (referencePrice/limitPrice) plus one stop and one target — no ideal_buy vs secondary_buy distinction; (3) no per-proposal action_checklist of pass/warn/fail precondition items — policy gate results exist as strings/reasons, not a structured checklist array; (4) no technical/news/fundamentals/market signal_attribution weights normalized to sum to 100 and persisted per-proposal (the existing normalizeWeights in market.ts operates on a different, 8-factor scoring-weight space at scan time, and its output isn't attached to the final TradeProposal); (5) the override/disagreement trail (redTeamVerdict.overridden, humanOverrideApplied, finalSizeReview, socraticOverride.applied) is real but is a set of loose flags, not a Literal-typed contiguous adjustment chain validated at construction time (extra='forbid' equivalent, contiguity check).

Implementation sketch:
Do NOT build a Python-pydantic-style monolithic schema authored in one LLM call — that fights this app's actual pattern of incrementally-appended, optional, typed receipts persisted across the strategy loop (see the entryRegimeSeverity precedent explicitly deferring exactly this). Instead:
1. Add a `ProposalScorecard` interface in src/lib/types.ts, additive/optional on TradeProposal (mirrors sizingSnapshot/finalSizeReview pattern): `{ coreConclusion: { thesis: string; noPositionAdvice: string; hasPositionAdvice: string }; dataPerspective: { maAlignment: 'above_both'|'below_both'|'mixed'|'unknown'; priceVsMa: { price: number; sma50?: number; sma200?: number }; volume: { current: number; avg20d?: number } }; sniperPoints: { idealBuy?: number; secondaryBuy?: number; stopLoss?: number; takeProfit?: number }; actionChecklist: { id: string; label: string; status: 'pass'|'warn'|'fail' }[]; signalAttribution: { technical: number; news: number; fundamentals: number; market: number } }`.
2. Populate it deterministically in src/lib/strategy.ts alongside existing proposal construction — maAlignment/priceVsMa/volume come from already-computed indicators.ts sma50/sma200 and MarketQuote.volume (no new data source, no fabricated numbers — omit fields Yahoo/enrichment doesn't actually have, never fake a '0'). sniperPoints.idealBuy/secondaryBuy can reuse referencePrice and a policy-configurable pullback offset (owner-adjustable knob in TradingPolicy, e.g. `secondaryBuyPullbackPct`, default off/undefined — never a silent hard-coded number).
3. signalAttribution: compute from the SAME `factors`/factorBreakdown MarketQuote already carries (map liquidity/momentum→technical, sentiment/newsSent→news, value/quality/fcf/de→fundamentals, positioning/marketBreadthPct→market), normalized to sum to 100 with a small pure function in market.ts modeled directly on the existing `normalizeWeights` (same safeWeight/fallback-to-default pattern) — reuse the pattern, don't invent a new one.
4. actionChecklist: derive from the SAME policy-gate reasons/humanReviewReasons/redTeamVerdict already computed per proposal — map each independent gate (entry-drift, wash-sale, daily cap, red-team availability) to a {id,label,status} row instead of inventing new checks. This makes it a rendering of existing gate state, not a new authority.
5. Disagreement chain: add a small typed union in types.ts, e.g. `type DecisionStep = 'proposed'|'red_team_reject'|'override_requested'|'override_applied'|'human_approved'|'final'` and a `decisionChain: DecisionStep[]` built by appending one entry at each point the strategy loop / approval route already sets redTeamVerdict.overridden, humanOverrideApplied, or socraticOverride.applied — with a small pure validator (contiguity check: each step differs from the last, 'override_applied' only appears if 'override_requested' preceded it) called once at persistence time in db-proposals.ts, throwing/logging rather than silently accepting a malformed chain. Persist via the existing db-proposals.ts / db-socratic.ts modules (JSON column, same as sizingSnapshot).
6. Render as a new con-* component (app/console/components/proposal-scorecard.tsx) using existing con-card/con-chip primitives — a collapsible block inside approval-card.tsx and a read-only view in the decision-case drawer, not a new page.
7. Digest delivery: add an OPT-IN daily notify() message type ('scorecard_digest') gated by a new user-adjustable notification_prefs toggle (off by default, like every other notify() channel) — summarize the day's scorecards through the existing notify() dispatcher (push/email/webhook), no new channel infrastructure.
8. Grading: extend outcome-engine.ts to optionally compare sniperPoints.stopLoss/takeProfit against the matured outcome bars already fetched (OutcomeOHLCFetcher) — a pure additive comparison, not a new fetch pipeline.
Everything above is additive/optional on already-computed values; nothing requires a new provider call, a new LLM round-trip, or fabricated data — if a field (e.g. avg20d volume) isn't actually available, omit it rather than fake it, per the no-mock-data rule.

### llm-output-repair-ladder - IMPLEMENTED
**Post-generation consistency repair with an auditable, named adjustment log (score-action contract, phase-fact checks, per-field fallback cascade)**

Three deterministic post-hoc passes treat the LLM's structured output as untrusted and repair it per-field rather than rejecting or blind-trusting: (1) a canonical score-band-to-action table is injected into the prompt AND re-verified after generation - a neutral action paired with a directional score is overridden to match the score unless the model supplied an explicit guardrail_reason for the divergence; (2) a market-phase guardrail checks the text against ground truth - 'buy now' language during premarket/closed sessions is rewritten to a wait state, postmarket-recap phrasing generated mid-session is replaced, and self-reported 'high' confidence is capped when core data blocks came back degraded; (3) a per-field degradation ladder fills any missing mandatory field first from an independently computed technical result (rule-based trend label, computed support level for stop_loss) and only then from an honest localized 'model did not provide X' placeholder - never a fabricated value, never a silently absent key.  Critically, every override is recorded as a named adjustments entry (e.g. non_intraday_action_adjusted, confidence_capped_core_data_degraded) appended to the delivered report's data_limitations, so corrections are visible, not silent edits.  This is correctness hardening, not paternalism - it never blocks a decision, it makes the numbers and the words agree and shows its work, consistent with ST's no-fake-data rule.

External-repo evidence: src/schemas/decision_scale.py (score-band table injected into prompt and enforced via score_action_conflicts_without_guardrail); src/phase_decision_guardrail.py (temporal-hallucination detection, confidence capping, named adjustments log); src/analyzer.py + src/core/pipeline.py (check_content_integrity, _backfill_agent_dashboard_fields, apply_placeholder_fill three-tier cascade)

Gap verdict: **partial** (effort M, recommended True).  Three sub-pieces, three different states. (1) Score-action contract: N/A by design — ST's schema makes side (buy/sell/short/cover) the action itself, so a 'neutral action with directional score' conflict cannot arise; no gap to close here. (2) Market-phase guardrail: MISSING outright — no deterministic check anywhere in the proposal pipeline compares rationale phrasing against the actual market session (closed/pre/regular/post), and the already-computed DataCompletenessReport / per-symbol EnrichmentSourcedField data (which exist and would be the ideal input for confidence-capping on degraded data) are never read by strategy.ts or strategy-risk.ts — they only feed an admin dashboard. Since the scheduler ticks every 60s across all sessions and the app explicitly supports extended-hours trading, a Bull-step rationale COULD emit session-incongruous language ('buying at the open,' a postmarket recap tone mid-session) with nothing catching it, and a proposal generated on thin/degraded data currently gets exactly the same confidence treatment as one generated on full data. (3) Per-field fallback cascade + named-adjustments log: PARTIAL — a real, independently-computed fallback ladder (ATR-scaled > beta-scaled > flat) already exists but only for bracket stopLoss/takeProfit prices, and its disclosure is a free-text append to `rationale` rather than a structured, queryable list; everywhere else, an incomplete/invalid LLM-repaired proposal is DROPPED wholesale (filterRepairedProposals) rather than field-repaired, and other missing fields get static defaults (timeInForce ?? 'gfd') rather than an independently-computed value. The app does already have the exact STRUCTURAL PATTERN the lesson wants (`preVetoReasons: string[]`, kind-prefixed tags) — it's just wired to allow/block decisions, not to field-level repair receipts.

Implementation sketch:
Scope down to the two real gaps (skip the score-action piece — it doesn't apply to this schema) and reuse existing idioms rather than building parallel infrastructure:

1. Data-degradation confidence cap (src/lib/strategy-risk.ts, alongside the existing calibratedConviction/convictionCapUncorroborated block at ~line 486-507): add a new policy.tuning knob (e.g. `confidenceCapDataDegraded`, default a conservative on-by-default like the existing convictionCapUncorroborated pattern) that reads each candidate's SymbolEnrichment/EnrichmentSourcedField sourcing (src/lib/data-providers.ts) at proposal time — if the core fields the thesis leans on (price, technicals, or whatever tradeThesisTag implies) came back from a degraded/fallback tier rather than a primary source, cap confidenceScore's upside contribution the same way the realized-edge cap does today. This is a real gate on the numbers, not a text check, and it's cheap: the sourcing data is already computed per-run in the market scan, just never consumed for this purpose.

2. Session-text sanity check (new small function, e.g. src/lib/proposal-phase-guard.ts, called from strategy.ts right after the Bull/Green step parses each proposal, before sanitizeProposals): deterministic regex/keyword check comparing the proposal's rationale text against `currentMarketSession(now)` (src/lib/market-hours.ts, already exists) — flag (not silently rewrite) phrasing like "buy now"/"at the open" during a closed/pre session, or postmarket-recap phrasing mid-session. Given ST's "guardrails are adjustable preferences, never a scolding block" philosophy, this must NOT block or rewrite the proposal — it should append a receipt, matching how enrichOpeningProposal already discloses fallbacks via rationale text.

3. Structured adjustments log: extend TradeProposal (src/lib/types.ts, near `preVetoReasons`) with a new optional `dataAdjustments?: string[]` (or reuse/rename preVetoReasons' kind-prefix convention, e.g. `stop_fallback_atr: ...`, `confidence_capped_degraded_data: ...`, `session_phrase_mismatch: ...`) so the existing free-text rationale appends (bracket-stop fallback, marketable-limit conversion, non-bracket-broker disclosure) AND the two new checks above all land in one machine-queryable array — persisted for free since TradeProposal already serializes as JSON in db-proposals.ts. Surface it on the approval card (existing con-* console component that already renders redTeamVerdict/preVetoReasons) as a small receipts list, and optionally roll a count into admin/data-catalog or a new admin panel.

Effort breakdown: knob + cap logic in strategy-risk.ts (~0.5 day, mirrors existing code closely); session-text guard as a new small pure function + call site + tests (~0.5 day); TradeProposal field + db-proposals.ts JSON passthrough + approval-card UI receipt (~0.5-1 day); test coverage across all three (~0.5 day). Total lands in the M band.

### news-intel-fanout-quality-bar - IMPLEMENTED
**Dimensioned news-search fan-out with a rule-based relevance scorer and a provider quality tournament**

Per symbol per run, the pipeline fires 5-6 distinct searches, each an intelligence dimension with its own query template and freshness policy: breaking news (strict recency), analyst/earnings/industry context (180-day lookback), risk_check (insider selling / lawsuits, strict recency), and official announcements (query targets exchange/filing sites), with wholesale query rewrites for ETFs (tracking error / NAV language).  Every raw hit then passes an explainable additive relevance scorer - points for ticker/company-name hits in title vs snippet vs URL, a denylist for ambiguous common-word company names (apple, meta, square, target, gap) that only score when a corroborating event term (earnings/guidance/lawsuit) co-occurs, and a human-readable relevance_reasons list rendered into the LLM prompt so the model sees why each item was kept.  Provider selection is a quality tournament, not first-success failover: a provider's technically successful response is only accepted if it contains at least one direct-company hit; otherwise the best-scoring response across all providers wins.  A spam gate with a hard allowlist for official sources (sec.gov, exchange sites) runs before context assembly.

External-repo evidence: src/search_service.py:3162-3336 (_score_news_relevance additive rubric + ambiguous-name denylist); src/search_service.py:4350-4602 (search_comprehensive_intel per-dimension templates, freshness windows, provider round-robin); src/search_service.py:3993-4283 (quality-bar tournament, _is_better_ranked_news_response); src/search_service.py:3403-3466 (_filter_ranked_news_for_context with official-source allowlist)

Gap verdict: **partial** (effort M, recommended True).  ST already has multi-provider news/sentiment fan-in (Alpha Vantage, Finnhub, Marketaux, Alpaca stream) merged through a priority-ordered cascade, keyword-based sentiment scoring, and prompt-safe headline dedup/compaction — but it has no entity-relevance layer at all: two of its own providers (Alpha Vantage, Marketaux) already return a per-article relevance/match score in their API responses and ST silently discards both, so a headline that only tangentially mentions the ticker (AV relevance_score near 0, or a weak Marketaux match_score) is treated identically to a strong direct hit and can reach the LLM prompt un-flagged. There is no ambiguous-common-word denylist (Apple/Meta/Square/Target/Gap-style tickers are exactly the ones ST's own watchlist can hold — e.g. META, TGT), no explainable relevance-reasons surfaced to the prompt, and the field merge across providers is first-wins-by-priority rather than a best-of comparison. The lesson's core 'dimensioned search fan-out with per-dimension query templates and freshness windows' does NOT map cleanly onto ST's architecture, though, because ST never does free-text web search for news — it uses ticker-parameterized provider APIs where the provider does (partial) entity matching server-side, and the 'official announcements' dimension is already better covered by direct SEC EDGAR 8-K ingestion than a search-engine allowlist gate would be. So the transferable value is narrower than the source lesson: adopt the explainable-relevance-scorer-plus-denylist-plus-quality-comparison pattern, not the search-fan-out mechanism.

Implementation sketch:
1) New leaf module src/lib/news-relevance.ts (mirrors prompt-headlines.ts's no-imports leaf style): scoreHeadlineRelevance(headline, symbol, companyName, opts) -> {score, reasons[]} — additive rubric (ticker token match, company-name-in-title match), plus an owner-editable ambiguous-name denylist entry (e.g. {name:'meta', requiresCorroboration:['earnings','guidance','lawsuit','recall','downgrade','insider']}) that only scores a bare common-word company name when a corroborating event term co-occurs in the same headline; returns human-readable reasons for debuggability (used in logs/ops snapshot, not shown per-headline in the prompt today). Cover with deterministic unit tests.
2) Wire ALREADY-AVAILABLE provider scores first (cheapest slice): in AlphaVantageEnrichmentProvider (data-providers.ts ~4364-4402), read ticker_sentiment[].relevance_score per feed item and only fold ticker_sentiment_score / keep the headline title when relevance_score clears a threshold. In marketaux-provider.ts, add match_score to MarketauxEntity and gate aggregateMarketauxBySymbol's headline collection and sentiment accumulation on it the same way.
3) For providers with no native relevance score (Finnhub company-news, Alpaca/Benzinga stream via news-store.ts), call scoreHeadlineRelevance() against the headline title before it's stored/returned as a SymbolEnrichment.headlines entry.
4) Owner-adjustable settings (db-settings.ts, following the iraWashSaleHandling pattern — default ON but a real off-switch, never a silent hard block): a newsRelevance settings block with enabled (default true), minRelevanceScore (default e.g. 0.35), and an editable ambiguousNameDenylist (JSON array), persisted via db.ts migrate() + CRUD in db-settings.ts. Expose via a small con-* settings panel under /console/connections or a new /console/data-sources section so the owner can tune the threshold/denylist without a redeploy.
5) Cascade merge upgrade (optional follow-up, higher risk): change CascadingEnrichmentProvider's headlines/sentiment resolution from strict first-wins-by-priority to compare each configured provider's average relevance score for the symbol this run and prefer the higher-scoring set — small scoped diff near data-providers.ts ~1550-1700, guarded by its own settings toggle since it changes existing source-attribution semantics (MarketScan.source string).
6) No fake data: a symbol with zero headlines surviving the relevance filter gets {} exactly like today's 'no coverage this cycle' case — never a fabricated backfill. No new scheduler lane or cron needed (filter runs synchronously inside the existing per-provider fetch path); no new notify() channel needed, though a low-relevance-dropped counter could feed the existing api_health_log/ops-snapshot machinery for visibility.
7) Tests: extend marketaux-provider.test.ts / data-providers tests asserting a low-match_score/low-relevance_score entity is excluded from both headlines and sentiment average, plus a new news-relevance.test.ts for the denylist rubric.

### social-sentiment-context - PLANNED (deferred, board row exists)
**Reddit, X, and Polymarket per-ticker sentiment block merged into analysis context**

A SocialSentimentService pulls three per-ticker signals from one aggregator API: a Reddit report (buzz score /100, sentiment score, 7-day mention and subreddit counts, top 5 mentions with per-mention sentiment, 5-day daily history), plus cached (10-min TTL, single-flight deduped) X/Twitter trending and Polymarket prediction-market data (buzz, market sentiment, trade count) filtered to the ticker.  All three render into one formatted text block concatenated onto the news context before it reaches the LLM, gated to US tickers only.  A _coalesce helper explicitly preserves 0 as a valid neutral sentiment rather than treating falsy as missing.  Polymarket implied odds as a sentiment input is the novel piece - a real-money crowd signal, not scraped chatter, which fits ST's real-data-only rule.  ST has congress-trade signals but no retail/social/prediction-market sentiment axis at all.

External-repo evidence: src/services/social_sentiment_service.py:55-343 (three endpoints, TTL cache, _format_social_intel, zero-preserving _coalesce); src/core/pipeline.py:650-661 and 1372-1386 (US-only gate, injection into news_context in both pipeline and agent modes)

Gap verdict: **missing** (effort M, recommended True).  Nothing implementing any piece of this lesson exists. ST's `sentiment` field is news-article tone (Finnhub / Alpaca-Benzinga / Marketaux / Alpha Vantage, all cascaded in data-providers.ts), `insiderSentiment` is SEC Form 4 derived, and congressional `netSentiment` is trade-flow derived — three distinct, already-covered sentiment axes, none of which touch retail social chatter (Reddit/X) or prediction-market odds (Polymarket). The market-wide CFTC futures positioning signal in strategy.ts is the only existing 'real-money crowd positioning' signal, but it's index/macro-level, not per-ticker. There is no SocialSentimentService equivalent, no buzz/mention-count field, no per-mention sentiment breakdown, no Polymarket integration, and no US-ticker gate for any of this because there's nothing to gate. The one complicating factor for implementation: QuiverQuant — the vendor most likely to supply exactly this kind of aggregated WSB/Twitter social sentiment dataset — is under a standing owner ban on direct ST access (src/lib/retired-direct-vendors.ts), so this can't be built by quietly re-adding a Quiver lane; it needs either a genuinely different vendor (the owner would have to supply a new key — no agent may provision one per the 2026-07-20 ruling) or a pass-through via Congress.Trade (App A) if that app ever adds such a dataset.

Implementation sketch:
Follow the MarketauxEnrichmentProvider pattern exactly (src/lib/marketaux-provider.ts is the template): env-key-gated (e.g. `SOCIAL_SENTIMENT_API_KEY`), `resolveXApiKey()` gate function, in-memory TTL cache + negative-cache on failure, `quotaScarce: true`, `suppliesFields` declared, batched/rate-limited fetch, fail-open to `{}` on error (never fabricated). Concretely: (1) new src/lib/social-sentiment-provider.ts with a `SocialSentimentEnrichmentProvider implements MarketEnrichmentProvider`, split into a Reddit sub-fetch, an X/trending sub-fetch, and a Polymarket odds sub-fetch (each independently optional/fail-open so a Polymarket outage doesn't kill Reddit data), all single-flight-deduped per symbol; (2) add `socialSentiment?: number` (0-100, zero-preserving via a `_coalesce`-style helper — `typeof x === 'number'` not truthy checks, matching the existing `insiderSentiment` handling in market.ts:1634-1639) and `predictionMarketSentiment?: number` to SymbolEnrichment/MarketQuote/MarketQuoteSummary in types.ts, wire through `EnrichmentSourcedField` union, `EMPTY_SOURCED`, the `takeScalar(...)` calls in CascadingEnrichmentProvider.enrich, and the merge in market.ts — this is the exact cross-file trap CLAUDE.md calls out for 'per-field enrichment sourcing', miss any one and the field silently never reaches the dashboard; (3) register in src/lib/data-catalog.ts and src/lib/source-capability-matrix.ts under the existing `sentiment_events` category so provenance/Connections-page attribution works for free; (4) add a `sentimentTitle`-style formatter in dashboard-ui.ts for the Market Scan table cell; (5) format a text block (buzz score, mention counts, top mentions, Polymarket implied odds) and append it into the news/evidence context alongside `compactHeadlinesForPrompt` output at the existing injection points in strategy.ts (~4950-4953, 5506-5507, 6266-6278) — this is genuinely a small, additive change since the headline-injection scaffolding already exists; (6) gate to US tickers only using whatever exchange/country signal MarketQuote already carries (check quote.sector/exchange fields — no new gating infrastructure needed, mirror the pattern used for the FMP economic-calendar US-only gate); (7) do NOT touch ScoringWeights/DEFAULT_SCORING_WEIGHTS in defaults.ts for a first pass — inject as LLM context only (like headlines), not a scoring-engine input, keeping this additive and low-risk; a scoring-weight integration (adding a `social` key to ScoringWeights, touching strategy-tuning.ts's Zod schema, market.ts's weight normalization) is a legitimate follow-on but roughly doubles the surface area and should be a separate PR if the owner wants it. No persistence/db-* module needed for a v1 (ephemeral like headlines); if the owner later wants historical backtesting of the signal, add a `db-social-sentiment.ts` module with a table in db.ts's migrate() per the established split. Blocking precondition: the owner must supply a real API key for whichever vendor is chosen (Reddit/X aggregators and Polymarket's own public API are candidates) — no agent may provision one. Also flag honestly: Polymarket has essentially no liquid per-ticker equity markets (it's overwhelmingly political/macro/crypto/sports) — expect this field to be populated for a small handful of mega-cap/news-driven tickers, not the general watchlist, so it should be treated as an occasional-hit context add rather than a reliable per-symbol signal.

### strategy-overlay-library - PLANNED (deferred, board row exists)
**Hot-loadable natural-language strategy template library with regime-tagged auto-routing**

Each of ~15 strategies (golden cross, box oscillation, sector leadership, event-driven, growth quality, etc.) is a YAML file with structured frontmatter (name, market_regimes tags, required_tools, core_rules citations) plus a freeform natural-language instructions block injected verbatim into a sub-agent prompt - no code per strategy.  Users add or override strategies by dropping a file into a directory (env-configurable path, name collision overrides built-ins); no deploy.  A SkillRouter infers a regime tag (trending_up/down, sideways, volatile, sector_hot) from the technical picture and auto-selects the 1-3 strategies whose declared market_regimes match, falling back to a priority-ordered default set.  For ST the right transfer is an advisory overlay layer, not a replacement for computed indicators: owner-editable heuristic templates (sector rotation, event-driven, earnings-season posture) that annotate the deterministic proposal, with a lightweight regime classifier deciding which lenses get emphasized in the prompt per run.  This matches ST's guardrails-as-adjustable-owner-preferences philosophy: strategy emphasis becomes data the owner edits, not code an agent redeploys.

External-repo evidence: strategies/README.md + strategies/*.yaml (frontmatter + freeform instructions format, core_rules numbered-taxonomy citations); src/agent/skills/base.py (SkillManager.load_custom_skills, drop-in override); src/agent/skills/router.py + src/agent/skills/defaults.py (regime inference and market_regimes matching)

Gap verdict: **partial** (effort M, recommended True).  The app has substantial regime-awareness and owner-editable strategy infrastructure, but not the specific mechanism the lesson describes: a hot-loadable library of MULTIPLE named strategy templates with structured frontmatter (market_regimes tags, required_tools, core_rules citations) plus freeform instructions, auto-routed by a regime classifier that picks 1-3 concurrently-emphasized lenses per run with a priority-ordered fallback.

What's missing specifically: (1) no file/DB-backed collection of discrete, independently-taggable strategy templates — strategy_profiles is one full prompt+policy+weights bundle, manually activated, one-at-a-time, not several composable regime-tagged overlays layered onto a single run; (2) no SkillRouter — nothing infers a regime and automatically selects/injects the matching subset of templates into the prompt each run; today regime selection of ANYTHING is a manual owner action (activateStrategyProfile via API/UI) or a fixed always-present block (ownerCoaching, thesis scorecards); (3) no structured per-template frontmatter (regime tags, required tools, rule citations) — strategy_profiles rows are just {name, full prompt text, policy, scoringWeights}; (4) no fallback/priority-ordered default set when nothing matches a regime.

Implementation sketch:
Add a new, purely-additive overlay layer that composes ON TOP of the existing single strategyPrompt/active-profile — do not touch strategy_profiles' semantics.

1. Schema (src/lib/db.ts migrate()): new table `strategy_overlays` (id, user_id, name, market_regimes TEXT [JSON array of MarketRegime enum values, or "any"], instructions TEXT [freeform, owner-authored], priority INTEGER, enabled INTEGER DEFAULT 1, created_at, updated_at). CRUD lives in a new src/lib/db-overlays.ts module per the db-* split convention (listStrategyOverlays/createStrategyOverlay/updateStrategyOverlay/deleteStrategyOverlay), re-exported from db.ts's barrel.

2. Router (new src/lib/overlay-router.ts, dependency-light like market-regime.ts): `selectActiveOverlays(overlays, regime, maxCount)` — filters enabled overlays whose market_regimes includes the current classifyMarketRegime(...).regime or "any", sorts by priority, takes top N (owner-configurable via a new `policy.tuning.maxActiveOverlays`, default e.g. 2), falls back to a priority-ordered default set (or empty) when nothing matches. Pure function, unit-testable in isolation.

3. Wiring (src/lib/strategy.ts, near the existing regimeSeverity computation ~line 5166): when `policy.tuning?.strategyOverlaysEnabled` is true, call the router and append selected overlays' instructions as a new fenced `<strategy_overlays>` block in userContent (and bearUserContent for evidence parity, matching the regimeSeverity precedent). Add the block to the existing DATA-NOT-COMMAND BOUNDARY line in strategy-prompts.ts (overlays are advisory DATA, never able to override risk limits/schema — same trust tier as ownerCoaching/learnedContext). Stamp `TradeProposal.appliedOverlayIds?: string[]` at both proposal-mapping sites (mirroring entryMarketRegime/entryRegimeSeverity) so a future overlay×outcome scorecard has data to read.

4. Gate behind `policy.tuning.strategyOverlaysEnabled?: boolean` (default false/undefined) per COMMON.md rule 4 — ships byte-identical until the owner opts in.

5. Owner UI: a con-* console panel (e.g. under /console/strategy or a new tab beside the existing profile switcher) — CRUD list of overlays with a freeform textarea for instructions, a multi-select over the 5 canonical MarketRegime labels + "any", a priority number, and an enabled toggle. Mirrors the existing strategy_profiles UI pattern closely enough to reuse most of its shape.

6. Tests: overlay-router unit tests (regime matching, priority ordering, fallback-to-default, empty-match, maxActiveOverlays clamp); strategy.ts wiring tests (flag off = byte-identical prompt/proposal, flag on = block present + appliedOverlayIds stamped, router-throw = run still completes with no block, matching the regimeSeverity precedent's try/catch discipline).

7. Docs: docs/rollouts/ note, docs/EFFORT-LOG.md + STATUS.md per Pre-Commit Protocol, and a short addition to docs/strategic-framework.md describing the new advisory layer.

### bayesian-strategy-trust-weights - PLANNED (deferred, board row exists)
**Sample-gated, horizon-bucketed Bayesian shrinkage for per-strategy trust, with a bounded multiplier**

Every strategy opinion is persisted immutably and later graded against realized forward price movement at fixed horizons (1d/3d/5d/10d) into hit/miss/observational/pending/unable outcomes, bucketed strictly by (strategy, horizon, engine_version) - never pooled.  A bucket only influences live weighting once it reaches 30+ evaluated samples; below that it returns a neutral factor of exactly 1.0 rather than an unreliable estimate.  Qualified buckets shrink the raw hit rate through a symmetric Beta(15,15) prior, discount by an unable_rate penalty, and combine into a performance_factor clamped multiplicatively to [1/1.2, 1.2] that scales that strategy's confidence in future aggregation - so no strategy is ever zeroed out or allowed to dominate off a streak.  A companion aggregation rule set forbids reporting 'high consensus' from fewer than 2 valid opinions or zero total confidence.  For ST this is a concrete, auditable calibration mechanism to compare against its existing crossover-learning confidence-size-cap: the sample-floor, symmetric prior, and bounded-ratio clamp are exactly the anti-overfit guards a real-money learning loop needs.

External-repo evidence: docs/multi-strategy-contract.md (grading taxonomy, Beta(15,15) shrinkage, performance_factor clamp, consensus invariants incl. 'zero evidence -> insufficient'); src/agent/skills/aggregator.py (evidence-chain vs diagnostics partition, consensus_level hard rules)

Gap verdict: **partial** (effort M, recommended True).  ST's architecture has no multi-strategy ensemble to weight in the first place: there is one LLM decision engine (Green) plus one adversarial reviewer (Red Team/Bear), not a roster of independently-versioned named strategies each producing an opinion that needs separate trust calibration. So the lesson's core unit of analysis, a (strategy, horizon, engine_version) bucket that is never pooled, has no direct home: ST buckets by thesisTag x regime (strategy-risk.ts), by scan factor (strategy-tuning.ts), or by RAG doc_type x memory_kind (retrieval-usefulness.ts), never by a strategy-engine identity, and never has an engine_version axis at all (strategy-prompt-version.ts stamps a prompt version onto trade_proposals for provenance, but nothing gates historical stats from pooling across prompt-version changes the way the lesson's engine_version dimension would). The one place that DOES persist multiple fixed forward horizons per opinion (db-retrieval-usefulness.ts) collapses them back down to a single best horizon at consumption time (retrieval-usefulness.ts kindStatsFor prefers headline, else largest-sample) rather than keeping all four horizons live and comparable simultaneously, which is precisely the failure mode the lesson calls out (pooled stats hide a strategy that works at 1d but not 10d). Sample floors and shrink priors also vary per module (5 lots here, 20 lots there, no canonical 30-sample/Beta(15,15) constant), so there's no single reusable trust-calibration primitive; the same pattern is reimplemented ad hoc four separate times. Finally there is no consensus-reporting rule at all (2+ valid opinions, nonzero total confidence before declaring high consensus) because there's nothing to form consensus across; the closest analog, Red-Team-unavailable handling, is a mode-aware fallback policy, not a sample-count consensus gate.

Implementation sketch:
Do not port the lesson literally since no multi-strategy roster exists to weight. Instead extract and unify the pattern ST has already proven four times into one reusable primitive, then apply it to the one axis where it is genuinely missing: multi-horizon-simultaneous trust, so a bucket's 1d skill and 10d skill stop being collapsed into one number. Step 1: new src/lib/trust-calibration.ts (pure, unit-testable) exporting shrinkToward(wins, losses, priorMass, priorRate) implementing the symmetric Beta shrinkage already used ad hoc, plus boundedMultiplier(shrunkRate, baseRate, min, max) returning exactly 1.0 below a sample floor and clamped otherwise; this becomes the one function performance.ts, strategy-tuning.ts, retrieval-usefulness.ts, and strategy-risk.ts can all call instead of four independent reimplementations, a real simplification win separate from the horizon gap. Step 2: extend db-retrieval-usefulness.ts's existing per-horizon storage (already correct) with a new query path in a new db-strategy-trust.ts module, following the db-star barrel-split convention (schema/migration added to db.ts's migrate(), CRUD in the new module, re-exported via db.ts), that returns ALL qualified horizon rows for a bucket, not just the best one. Step 3: in retrieval-usefulness.ts's kindStatsFor/usefulnessMultiplier, stop collapsing to one horizon: compute a per-horizon multiplier map and let the caller (experience-memory.ts retrieval) pick the horizon that matches its own use case, falling back to the current single-best behavior when only one horizon has enough samples for backward compatibility. Step 4: extend the same primitive to strategy-risk.ts's thesis x regime bucket so short-horizon and long-horizon thesis performance are surfaced separately in the sizing rationale as an advisory-only receipt first; do not wire it into applyDeterministicSizing's numeric output until proven, per the Phase-0-regression-test discipline this codebase already applies to learned_context. Step 5: add owner-adjustable knobs policy.tuning.trustPriorMass (default 15, replacing today's scattered 5s with the lesson's more conservative default while leaving today's per-module overrides as escape hatches) and policy.tuning.trustSampleFloor (default 30) alongside the existing minClosedLotsForWeightShift so the owner can tune or disable the floor entirely, matching the project's guardrails-are-preferences philosophy. Step 6: surface per-horizon trust as a new con-star console composite on the existing Results/Strategy Tuning page (a small bar or sparkline per horizon per bucket) rather than a new page. Step 7: no new notify() channel needed since this is a passive calibration input, not an alert-worthy event; audit() the shrinkage inputs and outputs the same way sizing_vol_target_applied is audited today for traceability. Use only real, already-collected realized returns and existing outcome-horizon rows; never synthesize a horizon that hasn't matured, since the outcome engine's own unresolvable state already enforces this.

### tiered-report-rendering - IMPLEMENTED
**One typed report context, tiered channel renderers, and an embedded signal-trajectory table**

A single enriched context object (sorted results, localized labels, helper functions) feeds three renderers chosen per channel: full markdown (every dashboard section), a medium chat tier (per-field character caps, top-2 item lists), and a one-line-per-symbol brief (emoji + name + score + 60-char headline).  Adding a schema field flows into all tiers for free.  A static channel-capability table (max chars/bytes, supports image/card, markdown dialect) is consulted by dispatch instead of limits hardcoded in each sender, and chunking for capped channels splits on section separators then headings measured in encoded bytes, never mid-table.  Most transferable: each symbol's section embeds a compare table of the last N prior signal snapshots (timestamp, score, advice, trend) - explicitly excluding the current run's own id - so every report shows how the view on that name changed, not just today's snapshot.  A shared branded poster-image builder backs both a chat image fallback and a user-facing share button from the same structured payload.

External-repo evidence: src/services/report_renderer.py + templates/report_markdown.j2 / report_wechat.j2 / report_brief.j2 (three tiers, one context); src/notification_capabilities.py (frozen ChannelProfile table, reserved renderer presets); src/notification.py + src/services/history_comparison_service.py (_get_history_compare_context, self-exclusion, per-batch caching); src/md2img.py + src/share_image.py (one poster pipeline for push and share)

Gap verdict: **partial** (effort M, recommended True).  Three of the four transferable pieces are genuinely missing, and the fourth has only a weak analog. (1) No single typed report-context object feeds multiple renderers — each NotificationEventType gets exactly one hand-written string builder in directNotificationBody, so there is no 'schema field flows into all tiers for free' property; adding a field means editing prose in one function. (2) No tiered channel renderers (full markdown / medium / one-line brief) — every channel that's enabled gets the identical NotifyMessage body; only SMS has an ad hoc length cap (slice to 1500 chars), and it's baked into that one channel's send() rather than driven by a shared capability table, so Pushover (~1024-char limit) and ntfy (title must be Latin-1, body limits vary by server) have no enforced caps today — a long risk_advisory/budget_alert body can silently get mangled or rejected by those services, an existing latent bug this lesson's dispatch-time capability table would fix as a side effect. (3) No embedded per-symbol signal-trajectory table — trade_proposals holds every past proposal (thesis tag, regime, timestamp, decision) which is exactly the raw data a 'last N snapshots for this symbol, excluding the current run' comparison needs, but nothing queries it by symbol today (symbol is buried in a JSON blob, unindexed), and no report surfaces it — an owner wanting 'how has the model's view of AAPL changed over the last week' currently has to reconstruct it by hand from the Recent Proposals list. (4) No poster/share-image pipeline at all — this app has no image-generation code path outside iOS icon assets, so a shared branded-card builder is a net-new subsystem, not an extension of anything existing. Overall: the codebase has a solid channel-dispatch skeleton (notify.ts) and a periodic-digest precedent (r2-usage) to build on, but the report-context/tiered-rendering/trajectory-table/poster-image system itself does not exist, and — importantly — there is currently no product concept of a periodic 'full watchlist scorecard' notification at all; today's notifications are 100% single-event alerts.

Implementation sketch:
Phase 1 (core value, matches app conventions):
1. `src/lib/db.ts` migration: add a `symbol TEXT` column to `trade_proposals` (backfilled via `json_extract(proposal,'$.symbol')`) plus an index `(symbol, account_number, created_at)` — avoids per-query JSON scans for history lookups.
2. `src/lib/db-proposals.ts`: add `listProposalsBySymbol(symbol, accountNumber, limit, userId)` returning the last N proposals (thesis tag, regime, decision, created_at) for one symbol, self-excluding the current run's own proposal id — the direct analog of `_get_history_compare_context`.
3. New `src/lib/report-context.ts`: `buildWatchlistReportContext(userId, accountNumber)` — one typed object: sorted watchlist symbols with latest signal/score, and per-symbol trailing-N trajectory rows via (2). Pure/typed, no rendering logic, so it's the single source every tier reads from.
4. New `src/lib/report-renderer.ts`: three pure functions over that context — `renderFullMarkdown(ctx)` (full per-symbol sections incl. trajectory table), `renderMedium(ctx)` (per-field char caps, top movers only), `renderBrief(ctx)` (one line/symbol: score + short headline, no fabricated emoji/branding unless the owner asks for that styling). Unit-testable in isolation from notify().
5. `src/lib/notify.ts`: add a static `CHANNEL_CAPABILITIES: Record<NotifyChannelId, {maxBodyChars: number}>` table (sms≈1500 — matches today's ad hoc slice; pushover≈1024; ntfy/webhook/email effectively unbounded) and have `notify()` pick the tier (full/medium/brief) per channel from it instead of sending one body to everyone — this also fixes the latent Pushover/ntfy truncation gap noted above as a byproduct.
6. `src/lib/types.ts`: add `"watchlist_digest"` to `NOTIFICATION_EVENT_TYPES`.
7. `src/lib/scheduler.ts`: add a `journalLane("watchlist-digest", ...)` cadence-gated call (same shape as `runR2UsageDailyDigestIfDue`), gated behind a new owner-adjustable setting (default OFF — e.g. `watchlistDigestEnabled` + cadence in notification_prefs/settings, surfaced in Settings → Delivery on the con-* console design system) so this is opt-in, never forced.
8. Never synthesize trajectory rows: if a symbol has fewer than N prior proposals, render what exists — no padding, matches the repo's 'never label real data mock/fallback' rule.

Phase 2 (optional, materially separate effort — do NOT bundle into Phase 1's estimate): a shared poster/share-image builder (SVG/HTML → PNG via `sharp`, or a headless render) backing a chat-image fallback and an in-app share button. This needs font embedding, layout, and new UI — treat as its own L-effort follow-on, not part of the base digest.

Tests: unit tests for buildWatchlistReportContext (fixture trade_proposals rows), each renderer tier (char-cap and self-exclusion assertions), and notify()'s per-channel tier selection — follow the existing `notify.ts`/`notifications.ts` test patterns (injectable fetchImpl, no real network).

### analysis-stream-budget-contract - PLANNED (deferred, board row exists)
**Named SSE event vocabulary for multi-turn analysis chat, with per-stage budget skip and cross-boundary cancellation**

The analysis chat endpoint streams a fixed event vocabulary the UI renders live: thinking, stage_start/stage_done, tool_start/tool_done (with display names per tool), generating, done, error - plus two budget events.  The orchestrator tracks remaining time against the pipeline deadline and, before each stage after the first, checks a minimum-useful-work threshold (15s): full exhaustion emits pipeline_timeout with a partition fallback, while a merely too-thin remainder emits pipeline_budget_skipped (remaining/minimum/reason) and records a degraded-event instead of starting a stage that would burn an LLM call just to time out; the first stage always runs so tight budgets still produce output.  Cancellation is real, not fetch-abort: each stream is keyed by a client-supplied request_id in a registry (duplicate concurrent id gets 409), a dedicated cancel endpoint fires the event, and on client disconnect the generator shields the in-flight work rather than abandoning owned subprocess/session state mid-execution.  For ST this is the backbone for a symbol-level 'discuss this proposal' chat: the same stage/tool/budget events narrate the proposal pipeline live in the console and iOS app.

External-repo evidence: api/v1/endpoints/agent.py (SSE vocabulary, request_id registry, 409 on duplicate, cancel endpoint, asyncio.shield on disconnect); src/agent/orchestrator.py (_MIN_STAGE_BUDGET_S two-tier policy, pipeline_budget_skipped vs pipeline_timeout, StageFailureReason.BUDGET_SKIP)

Gap verdict: **partial** (effort M, recommended True).  The specific contract in the lesson — a named SSE event vocabulary the UI renders live (thinking/stage_start/stage_done/tool_start/tool_done/generating/done/error plus pipeline_timeout vs pipeline_budget_skipped), a per-stage minimum-useful-work budget check that lets the first stage always run, and a request_id-keyed cancellation registry with real (not fetch-abort) cancellation that shields in-flight work — does not exist anywhere in ST. The chat/"Coach" endpoint (app/api/chat/route.ts -> src/lib/chat/orchestrator.ts) is entirely synchronous: one JSON request in, one JSON reply out, after up to 5 sequential tool-loop steps (src/lib/chat/llm.ts MAX_STEPS) each potentially several seconds of LLM latency. The UI (app/console/assistant/chat.tsx) shows nothing but a spinner for the whole duration and has no way to cancel a turn once sent. All the individual primitives this lesson would be built FROM already exist elsewhere in the codebase (tenant-filtered live SSE bus, soft-timeout/hard-cap "never abandon a paid-for call" pattern, phase-tagged budget-skip audits, singleflight/deadline AbortController pattern, idempotency keys) — but none of them have been assembled into the live, per-turn, cancellable, budget-aware narration this lesson describes. Nothing in docs/EFFORT-LOG.md references a chat-streaming or SSE-narration effort as planned/in-progress.

Implementation sketch:
Reuse the existing SSE bus rather than inventing a second transport: it is already tenant-filtered and always open on every console tab.

1. Event vocabulary — extend `DashboardEventType` in `src/lib/events.ts` with one new member, e.g. `"chat-turn"`, carrying a `detail` shaped like `{ turnKey, stage: "thinking"|"tool_start"|"tool_done"|"stage_skipped"|"generating"|"done"|"error", tool?: string, toolDisplayName?: string, reason?: string, remainingMs?: number, minStageMs?: number }`. Do not add new top-level DashboardEventType values per stage — keep the vocabulary inside `detail.stage` so the SSE route/tenant-filter code in `app/api/events/stream/route.ts` needs zero changes.

2. Emit points — in `src/lib/chat/orchestrator.ts`, thread a `notifyStage` callback (built from `emitDashboardEvent`, scoped with `userId`/`turnKey`) into the tool loop. The natural emit points already exist as comments/audit calls: right before `writeAudit("tool.call", ...)` emit `tool_start` (map tool name -> a display name table, same idea as the lesson's "display names per tool"); right after `executeTool` returns emit `tool_done`. Emit `thinking` once per `model.run` step boundary (llm.ts's `MAX_STEPS` loop) and `generating`/`done` around the final text assembly. Emit `error` from the existing catch block in `app/api/chat/route.ts`.

3. Budget-skip policy — add a small helper (e.g. `src/lib/chat/stage-budget.ts`) mirroring the strategy phase-skip shape (`phase`/`reason`/audit) but keyed to wall-clock remaining-vs-deadline instead of dollar/token ceilings: before starting tool-loop step N>0 in `src/lib/chat/llm.ts`, compute `remainingMs = deadline - Date.now()`; if `remainingMs < minStageBudgetMs`, emit `stage_skipped` (never `pipeline_timeout` unless truly exhausted) and break the loop with whatever partial `text`/`toolCalls` exist so far — never blocking step 0. Record via the existing `writeAudit("chat.stage_skipped", {...})` pattern (matches `chat.evidence_pack`/`chat.tool_evidence_pack` already in orchestrator.ts) so it shows up in the audit trail like everything else in this file.

4. Owner-adjustable knob — add `chatStageMinBudgetMs` (default e.g. 15000, matching the reference repo) to the same settings surface as other advisory guardrails (`app/console/guardrails/field-defs.ts` pattern, or a simpler console/assistant settings panel if chat settings live separately) — resolved via `db-settings.ts` like other per-user policy knobs, never hardcoded, with an honest label ("skip a tool step if too little time remains to be useful — turn off to always attempt every step").

5. Deadline plumbing — give each chat turn an overall wall-clock deadline (new `CHAT_TURN_DEADLINE_MS` env/setting, analogous to `strategyLlmTimeoutMs`/`STRATEGY_LLM_TIMEOUT_MS`) and pass an `AbortSignal` derived from it (same `AbortController`/`withScanDeadline`-style wrapper as `scan-singleflight.ts`) down into `llmFetch`/`llmFetchCapturing` calls inside `src/lib/chat/llm.ts` so a genuinely exhausted turn emits `pipeline_timeout` with whatever partial reply exists, distinctly from an advisory `stage_skipped`.

6. Cancellation registry — a small in-process `Map<turnKey, AbortController>` in a new `src/lib/chat/turn-registry.ts`, following the exact `globalThis` singleton pattern already used in `src/lib/events.ts` (module-instance-splitting safety) and `scan-singleflight.ts`'s `activeScans` Map. `POST /api/chat` registers the turnKey before calling `orchestrate(...)`; a concurrent second POST with the same `clientTurnId` (or an implicit per-user single-active-turn rule) returns 409, matching this app's existing single-active-write-per-user philosophy (`user-write-fence.ts`'s `withUserWriteOperation`/`assertUserOperationClaim`, which orchestrator.ts already uses to prevent overlapping writes — this registry is a natural sibling, not a new concept). Add `POST /api/chat/cancel { turnKey }` that calls `controller.abort()`; on abort, the orchestrator's `assertTurnActive()` checks (already sprinkled throughout as ownership-epoch guards) should also observe the AbortSignal and unwind cleanly, persisting whatever partial assistant turn exists via the existing `persistAssistantTurn()` path with `stage: "cancelled"` rather than silently dropping it — this is the "shield in-flight work" idea, and it can literally reuse `llmFetchCapturing`'s existing don't-abort-the-fetch/report-late-outcome behavior for any tool call already in flight when cancel arrives.

7. Frontend — `app/console/assistant/chat.tsx` already has (via `useConsoleData.tsx`) a live EventSource. Add a listener that filters `chat-turn` events by the in-flight `turnKey`/`clientTurnId` and renders a con-* styled inline stage strip (tool icon + display name + spinner/check, "skipped — low time budget" chip) reusing existing con-* primitives (badges/pills already used elsewhere in console, e.g. `ConAlertFilterPills`/`ConModelAttribution` mentioned in prior sessions) instead of introducing new visual language. Add a Cancel affordance that POSTs `/api/chat/cancel`.

Explicitly NOT in scope for this sized effort: iOS native app parity (the mobile app currently only has `/api/mobile/events` for dashboard-style push, not chat) — track that as a follow-on once the console version is proven, consistent with how SSE parity work has historically landed console-first then iOS (see the 2026-08-04/08-08 iOS SSE indicator entries in docs/EFFORT-LOG.md).




# Round 2 - broad GitHub sweep (same date)

Owner scope expansion: "also other repos you can find in a moderately broad search on github".  Repos read: TauricResearch/TradingAgents (97k), virattt/ai-hedge-fund (62k), freqtrade/freqtrade (53k), microsoft/qlib (47k), microsoft/RD-Agent (14k), pipiku915/FinMem-LLM-StockTrading.  Same 15-agent method.  7 of 8 lessons recommended; 3 selected for round-2 implementation, 4 deferred as Planned board rows, 1 honestly rejected.

## Implemented

- **benchmark-alpha-outcome-grading** -> grade decisions on SPY-relative alpha (owner knob)
- **live-signal-health-monitor** -> rolling RankIC + quantile buckets + churn + drift alarm
- **order-lifecycle-hardening** -> cancel-dust advisory

## All lessons with verdicts

### pit-fundamentals-revision-chain - PLANNED (deferred, board row exists)
**Point-in-time fundamentals with a revision chain; ban latest-wins reads from as-of paths**

Socratic.Trade's fundamentals store (src/lib/db-fundamentals.ts) is a `symbol_field_latest` table with an ON CONFLICT latest-wins upsert — the exact 'latest fact' shape both qlib and ai-hedge-fund identify as the single most common source of fake backtest edge (restated EPS/revenue silently replayed into history). Fix: store fundamentals as (value, fiscal_period, first_published_at/filing_date, superseded_by) rows instead of overwriting; any backtest, replay, learning-loop scoring, or RAG-over-filings query for a historical date must filter first_published_at <= as_of and take the newest surviving revision — filing_date discipline, never report_period. Make the point-in-time contract explicit in the provider-accessor interface and audit every accessor the backtester touches for a latest-only shape (ai-hedge-fund's named get_market_cap trap). Verifiable via a unit test: insert a restatement, assert the pre-restatement as-of query returns the original value.

External-repo evidence: qlib: qlib/data/pit.py (date/period/value/_next revision rows; P operator gates on observe-time, raises on future-period refs); qlib: docs/advanced/PIT.rst, scripts/dump_pit.py; aihedgefund: hedge_fund/data/protocol.py (PIT as load-bearing interface contract), hedge_fund/features/snapshot.py (inline comment banning the latest-only market-cap accessor); Socratic.Trade grounding: src/lib/db-fundamentals.ts lines 77-82 (latest-wins upsert, no revision chain)

Gap verdict: **partial** (effort M, recommended True).  Two things already exist in spirit: (1) `historical_fundamentals` is a genuinely append-only table whose own migration comment says it's "for as-of lookups" — but it has zero readers anywhere in the codebase (confirmed by grep), and its INSERT OR IGNORE on (symbol,field,provider,effective_at) will silently DROP a restatement that lands under the same effective_at instead of superseding it — so even the write path doesn't implement a real revision chain. (2) The team has already built this exact discipline twice elsewhere and could reuse both patterns: `isWithinAsOf` in vector-db.ts is a working, tested, lenient/strict as-of filter for the RAG/filings corpus (acceptance_datetime precedence — literally "filing_date discipline, never report_period"), and `sec_filings` (db-learning.ts/db.ts) already has the exact column shape the lesson wants (report_period, fy, fp, filed_at, accepted_at, superseded_by) — just scoped to filing-document metadata, not to the individual numeric GAAP facts (EPS, revenue, debtToEquity) that `parseCompanyFacts` extracts from those same filings. `parseCompanyFacts` is itself restatement-aware for LIVE use (10-K/A beats 10-K by `filed` date) but has no `as_of` parameter, so it cannot be used safely by any future historical replay. The missing piece is narrow and concrete: a numeric-fact revision-chain table (fiscal_period/first_published_at/superseded_by, matching the codebase's own superseded_by idiom) plus an `as_of` reader, wired at minimum into the SEC-XBRL cascade write path. No current backtest or counterfactual-learning path recomputes signals from fundamentals (both only replay pre-stamped factor scores or price bars), so this is not an active production bug today — but it is a direct, named prerequisite for the already-planned, still-unassigned oss-lessons §6 slice 2 ("TraderHarness PIT masking"), which cannot be implemented correctly on top of a fundamentals store that only remembers the latest value.

Implementation sketch:
Scope to the fields that actually have fiscal-period/filing semantics (SEC-XBRL-derived GAAP facts: debtToEquity, revenueGrowth, and any future EPS/revenue field) — screener-derived continuous metrics (peRatio, sentiment, analystRating, etc.) don't have a "restatement" concept and stay on symbol_field_latest as-is.

1. Schema (db.ts migrate(), new version): add a `fundamental_revisions` table mirroring the proven superseded_by idiom from `sec_filings`/`learned_context`: `symbol, field, fiscal_period_end, value, form, filed_at (=first_published_at), provider, superseded_by TEXT, created_at`, PK on (symbol, field, fiscal_period_end, filed_at) so a genuinely new filed value for the same period always inserts a new row instead of being silently ignored. Index on (symbol, field, filed_at).

2. Writer (src/lib/db-fundamentals.ts, new function `recordFundamentalRevision`): called from the SEC-XBRL branch of CascadingEnrichmentProvider.enrich (data-providers.ts, near the existing recordHistoricalFundamentals call ~line 1777) using the `end`/`filed`/`form` fields `parseCompanyFacts`'s internal Fact type already carries — thread those through `parseCompanyFacts`'s return shape (today it only returns the reduced scalar) so the raw revision rows are available to persist, not just the winning number. On insert of a new (symbol,field,fiscal_period_end) revision, set superseded_by on the prior row for that period (mirrors `db-learning.ts`'s markSuperseded pattern) — receipts over silent edits: the old row stays queryable, just marked.

3. Reader (`getFundamentalAsOf(symbol, field, asOf, { strict = false })` in db-fundamentals.ts, mirroring vector-db.ts's `isWithinAsOf` lenient/strict contract): `SELECT value FROM fundamental_revisions WHERE symbol=? AND field=? AND filed_at <= ? ORDER BY fiscal_period_end DESC, filed_at DESC LIMIT 1`. Lenient (default): no row before asOf → falls back to symbol_field_latest (today's behavior, unchanged) so nothing new can block a live decision — matches the "guardrails are adjustable, fail-open by default" philosophy and the existing VECTOR_ASOF_STRICT precedent. Strict (opt-in via an env/policy knob, e.g. `FUNDAMENTALS_ASOF_STRICT`): no row before asOf → returns undefined instead of guessing, for any future backtest/replay/tuner path that must not leak future data.

4. Interface contract: add a short doc-comment on `getFundamentalAsOf` (and a note in db-fundamentals.ts's file header, which already documents the two-layer design) explicitly banning `getSymbolFieldLatest`/`marketQuoteSummariesFromFieldStore` from any code path that evaluates a historical decision — the ai-hedge-fund-style "banned accessor" contract, enforced by comment + code review today (a lint rule is out of scope for M effort).

5. Verification: one unit test in a new test/fundamental-revisions.test.ts — insert an original 10-K fact, insert a later 10-K/A restatement of the same fiscal_period_end with a later filed_at, then assert `getFundamentalAsOf(symbol, field, asOf=<between the two filed_at dates>)` returns the ORIGINAL value, and `getFundamentalAsOf(..., asOf=<after the restatement>)` returns the restated value. This directly matches the lesson's own verification bar.

6. Leave `historical_fundamentals` alone (still write-only, harmless) rather than repurposing it — its ambiguous effective_at semantics (poll-time, not filing-time, for most non-XBRL fields) make it unsafe to retrofit as the PIT store; a fresh table scoped to the fields that actually have period/filing semantics avoids a false sense of PIT-safety for fields that were never period-anchored to begin with.

No scheduler/notify/con-* console work is needed for the MVP — this is a data-layer correctness fix, not a user-facing feature; the only consumer today is the not-yet-built oss-lessons §6 slice 2 (TraderHarness PIT masking), which this becomes a real prerequisite for.

### truncated-replay-lookahead-harness - PLANNED (deferred, board row exists)
**Empirical lookahead detector: recompute features with history truncated at the decision point and diff**

Port freqtrade's lookahead-analysis mechanism: for a sample of backtest decisions, re-derive the full feature/signal set (technical indicators, congress-signal aggregates, RAG-retrieved filing chunks keyed by date) with the history array truncated to end exactly at that decision's timestamp, then row-diff every feature against the values the full-history run used; any mismatch is mechanical proof of a future-data leak, named by column, regardless of how it was introduced. Include freqtrade's anti-false-positive discipline: skip boundary-forced exits, require a minimum qualifying-signal count before rendering any 'no bias' verdict, and neutralize position-slot starvation. This complements (does not duplicate) the existing off-by-default train/test embargo purge in src/lib/backtest.ts (~line 695) and directly validates whether RAG retrieval is correctly date-bounded per backtest step. Optionally tag each screener/enrichment filter with a backtestSafety classification checked at backtest startup, per freqtrade's taxonomy.

External-repo evidence: freqtrade: freqtrade/optimize/analysis/lookahead.py, lookahead_helpers.py, docs/lookahead-analysis.md; freqtrade: freqtrade/plugins/pairlist/IPairList.py + pairlistmanager.py (supports_backtesting self-declaration enforced at startup); Socratic.Trade grounding: src/lib/backtest.ts lines 143, 695-729 (leakage boundary predicate + off-by-default purge — coarse ordering handled, no differential detector)

Gap verdict: **partial** (effort M, recommended True).  No unified mechanism exists that recomputes the FULL feature/signal set for a sampled past decision with history truncated at that decision's timestamp and row-diffs it against what was actually persisted at decision time. What exists instead is (a) narrow, hand-written point-in-time assertions checked in isolation (isPointInTimeForwardExit for exit bars, pitLabelIsPointInTime for congress labels, asOf/strictAsOf guards threaded through RAG retrieval) that are code-reviewed and unit-tested per function but never empirically cross-checked at production scale, and (b) a persisted-but-off-by-default candidate-pool receipt (ids/scores/asOf/docType/used) that a diff harness could reuse but that nothing currently reads back and compares. Beyond "not yet built," there is a genuine structural obstacle for the FULL freqtrade-fidelity version: this app has no backtestable historical bar/fundamentals database to truncate — runStrategyOnce (src/lib/strategy.ts:417) and the enrichment cascade (src/lib/data-providers.ts) call live provider APIs that return only "as of right now" fundamentals (P/E, EPS, quality, value), with no versioned point-in-time source. Only two subsystems are actually reconstructable point-in-time today: (1) OHLC-bar-derived momentum/liquidity factors, since fetchDailyOHLC returns dated bars that can be truncated by date, and (2) RAG retrieval, since the corpus carries immutable acceptance_datetime stamps and retrieval already accepts an asOf cutoff — this is also exactly the subsystem the lesson's value_hypothesis calls out as highest-value to validate. The other 5-6 fundamentals-derived factors cannot be genuinely truncate-and-diff-checked without a new point-in-time fundamentals data source, which this repo's binding rule ("NEVER create a new provider API key") makes hard to acquire unilaterally. Freqtrade's anti-false-positive discipline (boundary-exit skip, minimum qualifying-signal count, position-slot-starvation neutralization) and its backtestSafety/supports_backtesting-style self-declared capability taxonomy have no analog anywhere in the codebase.

Implementation sketch:
Build a scoped version targeting only the two genuinely reconstructable subsystems, and be honest (not silently optimistic) about the rest:

1. `src/lib/lookahead-audit.ts` (pure/IO split mirroring backtest.ts):
   - IO: `sampleDecisionsForLookaheadAudit(userId, options)` reuses `listSignalSnapshotAuditAfter` (db-learning.ts) to sample N matured decisions (owner-adjustable sample size, default e.g. 25), skipping anything inside the outcome horizon (mirrors the existing embargo concept) so only fully-resolved decisions are audited.
   - For RAG evidence: reconstruct the SAME deterministic query-builder inputs the live run used (symbol/regime/thesis-tag/evidence-summary from the persisted decision — never the literal stored query text, since that's deliberately never persisted), re-invoke retrieval with `asOf` pinned to the decision's snapshot date and `strictAsOf: true`, and diff the returned chunk-id/docType set against the persisted candidate-pool `used:true` rows (requires turning `RAG_PERSIST_CANDIDATE_POOL` on for the audited sample, or extending capture). Use a Jaccard-similarity threshold rather than exact-set equality to absorb benign reranker/embedding nondeterminism — an exact match on "any post-asOf chunk present" is the hard fail; ordering/score drift is not.
   - For momentum/liquidity: fetch full OHLC via `fetchDailyOHLC`, truncate to `bar.date <= decisionDate`, recompute only those two fields, clone the persisted MarketQuote with just those fields replaced, call the existing pure `scoreFactors` (src/lib/market.ts:924), and diff against the persisted subScores.
   - Explicitly tag value/quality/sentiment/positioning/diversification as `backtestSafety: "unverifiable-live-snapshot"` in every finding — store the label so the coverage gap is visible as a receipt, never silently implied clean. This IS the lesson's optional backtestSafety taxonomy, repurposed as an honesty label instead of a startup-enforced capability gate (avoids inventing a new hard rule, matches "guardrails are adjustable preferences").
   - Anti-false-positive discipline: minimum N qualifying observations (owner-adjustable, default e.g. 20) before rendering any "no lookahead bias" verdict; skip decisions still inside the maturation horizon.

2. `src/lib/db-lookahead-audit.ts` (new db-* module per the split convention): `CREATE TABLE lookahead_audit_findings` (added to db.ts's migrate()) storing decisionId, factor/field, persisted value, recomputed value, delta, classification (clean|mismatch|unverifiable), asOf. CRUD lives in the new module; db.ts re-exports.

3. Scheduler: wire a low-frequency (weekly, owner-adjustable cadence) job through the existing durable due-jobs substrate (src/lib/db-jobs.ts's enqueueDueJob, drained the same way outcome-horizons.ts's intraday sampler is) rather than a bespoke cron. A settings toggle (advisory ON by default since it is read-only/non-blocking, with a documented off-switch) controls enable/disable and cadence.

4. notify() (src/lib/notify.ts): new channel/kind e.g. `lookahead_leak_detected`, fired ONLY on a `mismatch` classification above a configurable tolerance — never on `unverifiable`. Purely advisory: informs the owner, blocks nothing, no auto-pause.

5. Console: a new receipts panel (con-* design system) showing per-decision findings — persisted vs recomputed, classification, and the unverifiable-factor labels rendered plainly so the dashboard never shows a false "all clean" when 5 of 8 factors were never actually checked.

Full freqtrade-fidelity (all 8 factors, not just momentum/liquidity/RAG) is not recommended as a near-term target: it is blocked by the lack of a point-in-time fundamentals data source, which this repo cannot self-provision under the "never create a new provider key" rule — flag it to the owner as a data-availability gap rather than attempting to fake coverage.

### benchmark-alpha-outcome-grading - IMPLEMENTED
**Outcome-gated reflection scored on benchmark-relative alpha, not raw return**

The learning loop already matures multi-horizon outcomes (15m/1h/1d/1w side-adjusted returns, per the retrieval-usefulness join) and the repo already has SPY benchmark series code (src/lib/benchmark.ts) — but decision-level outcome grading is raw-return based, so lessons reward market beta, not decision quality. Port tradingagents' two rules: (1) reflection/lesson generation is strictly lazy and outcome-gated — a decision is tagged pending and only reflected on when real forward price data exists, retried on the next scheduler pass if data is not yet available, never forced or faked; (2) the graded quantity is alpha versus a resolved benchmark (SPY or sector ETF) over the same holding window, and the reflection prompt must cite the alpha figure. Verifiable: assert no reflection row exists for an unmatured decision, and that stored lessons reference benchmark-adjusted numbers.

External-repo evidence: tradingagents: tradingagents/graph/trading_graph.py (_resolve_pending_entries, _fetch_returns, _resolve_benchmark), tradingagents/graph/reflection.py, tradingagents/default_config.py (benchmark_map); Socratic.Trade grounding: src/lib/benchmark.ts (SPY equity-curve scoreboard exists, unused for decision grading), src/lib/db-retrieval-usefulness.ts (matured multi-horizon outcomes are side-adjusted raw returns, not benchmark-relative)

Gap verdict: **partial** (effort M, recommended True).  Rule (1) of the candidate lesson — strictly lazy, outcome-gated reflection that never forces/fakes a price and retries on the next scheduler pass — is already fully built in outcome-engine.ts and is not a gap at all. Rule (2) — grade on alpha vs a resolved benchmark and have the reflection prompt cite it — is half-built: outcome-horizons.ts already computes spyExcessPct for the 1d/1w horizons, and that alpha figure is already threaded into the vector-memory doc text and the post-mortem LLM's raw JSON payload. What's missing is that the actual GRADING VERDICT never uses it: the case-level won/lost/flat status (the single field that gates lesson generation, feeds retrieval-usefulness wins/losses, and shows up as 'outcome' everywhere in the UI/prompts) is computed from raw realized P&L or raw side-adjusted returnPct, never spyExcessPct. Three downstream consumers inherit this raw-return bias: (a) retrieval-usefulness.ts's RAG-usefulness ranking nudge, (b) strategy-tuning.ts's decisionMemory summary (which drops the alpha figure even though it's one field away), and (c) performance.ts's thesis/regime/sector scorecards, which have zero SPY join anywhere and are the primary 'what has worked' signal fed to the tuning LLM. Two smaller, harder-to-close gaps: intraday (15m/1h) horizons can never get spyExcessPct (no intraday SPY data source exists in the stack — likely an accepted limitation, not a quick fix), and there is no sector/alternate-benchmark option (SPY only, no benchmark_map equivalent).

Implementation sketch:
Core wiring (the part that actually fixes the "rewards beta not decision quality" problem), in db-*/module-split style with an owner off-switch:

1. `src/lib/outcome-horizons.ts` — add a pure helper `pickHeadlineAlpha(outcomes)` that prefers the longest-resolved horizon with a defined spyExcessPct (mirrors `pickHeadlineRow`'s 1w>1d>1h>15m priority, but only over rows that actually carry spyExcessPct today — i.e., 1d/1w).

2. `src/lib/outcome-engine.ts` measureCase — add a companion status alongside the existing raw one rather than replacing it (never silently redefine what "won" means for existing consumers/tests): extend `SocraticDecisionCase.outcome` with an optional `alphaStatus: "won"|"lost"|"flat"|undefined` and `alphaPct?: number` computed from the headline spyExcessPct when available, else left undefined (never fabricated — mirrors the existing honesty contract). Gate behind a new owner-adjustable policy knob, e.g. `policy.outcomeGradingMode: "raw"|"alpha"` (default "raw" to avoid silently changing existing users' history/behavior — matches the "adjustable preference with an honest default" template like `iraWashSaleHandling`). When `alpha`, the lessonCandidates filter and the LLM prompt instructions additionally require `alphaStatus` to be resolved before treating the case as gradeable (falls back to `raw` gating with a receipt when spyExcessPct is unavailable for that case, e.g. intraday-only horizons — never blocks lesson generation entirely).

3. `src/lib/retrieval-usefulness.ts` runRetrievalUsefulnessJoin — when crediting a horizon row that has spyExcessPct, additionally write an alpha-flavored stat row (e.g. horizon suffix `${row.horizon}:alpha`) using spyExcessPct for win/loss instead of returnPct; `applyRetrievalUsefulnessWeighting` picks the alpha-flavored kind stats when `outcomeGradingMode==="alpha"`, else the existing raw stats — same bounded ±10% RRF nudge, same fail-open contract, just a different input column. `db-retrieval-usefulness.ts` needs no schema change (horizon is already a free-text column).

4. `src/lib/strategy-tuning.ts` line ~529 — add `spyExcessPct` (pulling the headline alpha row) alongside the existing `${status} ${returnPct}%` string in decisionMemory when present — cheap, no schema change, immediately gives the tuning LLM the alpha context it's currently missing.

5. (Stretch, larger lift — flag separately, don't bundle into the same PR) `src/lib/performance.ts` getThesisScorecard/getSectorScorecard/getRegimeScorecard — join each closed lot's entry/exit dates against a cached SPY daily-close series (same fetchDailyOHLC cascade outcome-engine already uses) to add `avgAlphaPct`/`shrunkAvgAlphaPct` fields. This is the highest-value follow-up since these scorecards are the actual "what has worked" text injected into the tuning prompt, but it's a real day of work on its own (needs a SPY-bars cache threaded through pnlForSource's call sites) — better as its own PR.

6. Sector/alternate-benchmark support: skip for v1. SPY-only is a reasonable default for a first cut; add a `benchmarkSymbol` per-sector setting later only if the owner asks — don't build tradingagents' full benchmark_map speculatively.

7. Receipts: emit an `audit("outcome_alpha_grading", {...})` when a case's alphaStatus differs from its raw status (e.g., raw='won' but alphaStatus='lost' because it underperformed SPY) — this is the single most useful observability signal for validating the whole feature actually changes anything, and it's exactly the kind of "receipts over silent edits" the repo conventions require.

8. Tests: extend test/outcome-engine.test.ts and test/retrieval-usefulness*.test.ts to assert (a) a rising-market long that's up 2% but SPY is up 5% gets alphaStatus='lost' while status stays 'won', and (b) with outcomeGradingMode='raw' (default) nothing about existing behavior changes byte-for-byte.

### abstain-not-neutral - REJECTED (no live instance)
**Failed sub-calls vote abstain, never neutral: exclude from numerator AND denominator of any blend**

Adopt ai-hedge-fund's locked failure contract wherever multiple signals, trust weights, or LLM sub-judgments combine into one score: an LLM call that errors/times out/returns unparseable output produces an explicit abstained flag (value present but tagged), and every aggregation site excludes abstained entries from both the numerator and denominator of weighted means — while a genuine 'no edge' neutral (a real opinion) legitimately dilutes the average. Without this, a crashed sub-model's default 0 silently drags blended conviction toward neutral and differentially punishes symbols where more calls happened to fail. This is a semantics contract to apply across the existing (and queued-for-unification) Bayesian trust-weight consumers, congress-score gating, and any Red/Green sub-scoring — not an overlap with the unification work itself, which merges implementations rather than defining failure semantics. Verifiable with a unit test: one abstained + two real votes must equal the two-vote mean exactly.

External-repo evidence: aihedgefund: hedge_fund/signals/llm_agent.py (abstained metadata on LLM failure; data errors still fail loud), hedge_fund/portfolio/construction.py (blend excludes abstained from numerator and denominator); Complements shipped provider retry/backoff hardening: abstain is the contract for what happens after retries are exhausted

Gap verdict: **partial** (effort M, recommended False).  Socratic.Trade has no live multi-LLM-vote weighted blend for this contract to protect: the app deliberately consolidated to one Green proposer call + one Red Team adversary call per opening (docs/single-adversary-consolidation.md), and the one LLM sub-judgment that can fail (Red Team) already implements the exact requested abstain contract (available:false + typed failureKind, consumed as a distinct branch downstream, never defaulted into a numeric average). What's genuinely missing is a *generalized, reusable* abstain/exclude-from-denominator mechanism, and the two places that DO run a fixed-weight multi-component blend today - congress-score.ts's composite score and market.ts's scoreFactors/weightedTotal - are deterministic (not LLM-sub-call) blends where a 'missing' component still consumes its full nominal weight as a literal 0 in the numerator/denominator, which is structurally the same silent-neutral-drag bug the lesson warns about, just triggered by missing upstream DATA rather than a crashed LLM call. congress-score.ts's confidenceCap and congress-score-gate.ts's three-way (PASS/FAIL_SIGNIFICANCE/INSUFFICIENT) verdict are partial, already-shipped mitigations of the same underlying problem at a higher level, but neither actually renormalizes weights over only the components with real evidence.

Implementation sketch:
Two-part, low-risk sketch consistent with app conventions:

1) Shared primitive (future-proofing, cheap): add `abstainableWeightedMean()` to a small new pure module (e.g. src/lib/abstain.ts, no DB/network deps) taking `{ value: number; weight: number; abstained: boolean }[]` and returning the weighted mean computed ONLY over non-abstained entries (both numerator and denominator exclude abstained rows), with a unit test asserting one abstained + two real votes equals the two-vote mean exactly (matches the lesson's stated test). This is the reusable hook any FUTURE multi-model panel/ensemble (should the app ever add one) is required to route through instead of defaulting a failed sub-call to 0.

2) Retrofit congress-score.ts's composite (the one live fixed-weight blend with a real missing-vs-zero distinction today): change each component's provenance from a boolean-ish 'missing' push to also flagging that component's CONGRESS_SCORE_WEIGHTS share as excluded, then renormalize the remaining components' weights to sum to 1 before computing rawScore - i.e. treat 'missing member skill' as abstain rather than 'observed 0 member skill'. Reuse the existing provenance.missing array (already tracked per-component) as the abstain signal so no new field is needed; add an `owner-adjustable` off-switch consistent with the app's guardrail-as-preference convention (e.g. policy.tuning.congressAbstainRenormalization, default off) since this changes the composite's calibration and the cached go-no-go verdict (congress-score-eval.ts) would need re-running against the new score function before flipping the default. Add a unit test in test/congress-score.test.ts proving a fully-populated 3-component signal scores identically whether the other components are 'missing' (abstain-renormalized) vs previously silently zeroed.

Do NOT touch market.ts's scoreFactors - its neutral defaults (e.g. sentiment ?? 50) represent genuine 'no coverage for this ticker' data-absence, diluted across 8 factors each ~12% weight; the lesson's own text says a genuine no-edge neutral legitimately dilutes the average, and there's no LLM sub-call there to abstain from.

### scoped-protection-locks - PLANNED (deferred, board row exists)
**Composable trade-history circuit breakers writing scoped, expiring, reason-carrying locks**

Generalize the existing one-off breakers (src/lib/accuracy-breaker.ts; wash-sale lock provenance in policy.ts) into freqtrade's shared primitive: a TradeLock row (scope: account|symbol, side: long|short|*, until, human-readable reason) written by independently-configurable trigger functions evaluated over a rolling closed-trade window before every proposal — stop-loss-streak guard (with a required_profit filter so profitable trailing-stop exits don't count as failures), rolling-drawdown lock, unconditional per-symbol post-trade cooldown, and per-symbol losing-streak lock. Locks are idempotent to create, self-expire on candle/market boundaries, and a global lock satisfies side-specific queries. Per the owner's 'guardrails are adjustable preferences' philosophy, every trigger's threshold/window is a per-user setting and every lock is advisory-overridable with a logged autonomyOverride — the exact pattern policy.ts already documents for wash-sale blocks. Verifiable: unit-test each trigger's window math and the lock-scoping matrix.

External-repo evidence: freqtrade: freqtrade/plugins/protections/{stoploss_guard,max_drawdown_protection,low_profit_pairs,cooldown_period}.py, protectionmanager.py, freqtrade/persistence/pairlock*.py; Socratic.Trade grounding: src/lib/accuracy-breaker.ts and policy.ts wash-sale lock provenance (existing one-off precedents; policy.ts line 245 documents the advisory-override pattern to reuse)

Gap verdict: **partial** (effort L, recommended True).  The three ingredients the lesson wants generalized all exist independently and are well-precedented (advisory-vs-hard action toggle, owner-adjustable thresholds, reason strings, audit() receipts, quiet-KV persistence) — but they are three bespoke, non-interoperable implementations, not the shared TradeLock primitive the lesson describes. Concretely missing: (1) any persisted row/table with the generic shape {scope: account|symbol, side: long|short|*, until, reason} that multiple independently-configurable triggers can write into and query against — nothing like freqtrade's PairLocks table exists; (2) a PER-SYMBOL stop-loss-streak or losing-streak guard — accuracy-breaker.ts only evaluates at account scope, so one degrading name can hide inside an otherwise-healthy account-wide hit rate; (3) an unconditional per-symbol post-trade cooldown (freqtrade CooldownPeriod) — the only per-symbol cooldown in the app (triggers.ts) gates event-driven run firing, not re-entry after a close; (4) a rolling-drawdown lock at any scope other than the whole account; (5) side-specific (long/short) lock scoping or a "global lock satisfies side-specific query" matching matrix — nothing in the codebase models lock scope/side at all today. The `required_profit` filter concern is effectively already handled by won/lost/flat being P&L-sign-derived rather than exit-mechanism-derived, so that piece of the lesson is not a real gap.

Implementation sketch:
1. New pure-evaluator module `src/lib/trade-locks.ts` mirroring accuracy-breaker.ts's style exactly (pure functions, unit-testable, no DB inside the evaluators): `evaluateSymbolLosingStreak({outcomes, streakLimit, ...})` reusing the same streak-counting logic already proven in evaluateAccuracyBreaker, plus `evaluateSymbolCooldown({lastClosedAt, cooldownMinutes, now})`. Feed both from `getClosedLotsDetailed` (already used by tax.ts for the wash-sale lock — no new data-fetch path needed) filtered per-symbol instead of per-account.
2. New `src/lib/db-trade-locks.ts` (owning module per the db-* split convention) with a `trade_locks` table added to db.ts's `migrate()`: `id, user_id, connected_account_id, scope TEXT ('account'|'symbol'), symbol TEXT NULL, side TEXT ('long'|'short'|'*'), reason TEXT, trigger TEXT, created_at, until TEXT, active INTEGER`. CRUD: `upsertTradeLock` (idempotent — unique on scope+symbol+side+trigger, refreshes `until`/`reason` on re-fire rather than duplicating rows), `getActiveTradeLocks(userId, accountScope, now)` (auto-filters `until < now`, with a global account-scope lock satisfying any symbol/side query — the scoping-matrix logic the lesson calls out), `pruneExpiredTradeLocks`. Re-export via `db.ts`'s `export * from "./db-trade-locks"` barrel.
3. New owner-adjustable `RiskRules` fields, off by default, following the exact accuracyBreaker* naming/doc-comment pattern: `symbolCooldownMinutes`, `symbolLosingStreakLimit` (+ optional window), `symbolLockAction: "advisory" | "close_only"` (reuse the existing two-tier philosophy — no new "halt" tier needed at symbol scope). Surface in `app/console/guardrails/field-defs.ts` and `app/settings-search.ts` exactly like `accuracyBreakerConsecutiveLosses` is today (con-* console design system, ADVISORY_NOTE hint suffix).
4. Wire evaluation into `src/lib/strategy.ts` at the same point the accuracy/drawdown breakers run (top of run loop, per candidate symbol before proposal generation), writing/refreshing rows via `upsertTradeLock` and auditing with `policy_violation_symbol_lock` (matches the existing `policy_violation_accuracy`/`policy_violation_drawdown` naming) plus a `risk_advisory` or `kill_switch` notify() call (both event types already exist — no new NotificationEventType required for MVP).
5. Policy-gate integration: extend `PolicyContext` with `tradeLocks?: Map<string, TradeLockInfo>` (mirroring `washSaleLocks`), add a gate in `policy.ts` alongside the wash-sale gate. Per the documented hard/overridable split (policy.ts ~line 242-260), this is NOT a hard-block pattern like wash-sale's IRA case — it belongs in the overridable-by-default bucket, tagged so an `autonomyOverride` thesis can pass it exactly like the deterministic-bear pre-vetoes, honoring "guardrails are adjustable owner preferences."
6. Tests: `test/trade-locks.test.ts` unit-testing the pure evaluators' window/streak math (mirroring `test/accuracy-breaker.test.ts`) and the scope/side matching matrix (`getActiveTradeLocks` global-vs-symbol-vs-side resolution) in isolation from the DB where possible.
7. Skip for MVP: a dedicated "Active Locks" console panel (freqtrade's PairLocks UI) — the existing audit-log + notify() receipt trail is consistent with how accuracy/drawdown breakers surface today; add a panel only if the owner wants visibility beyond audit records once the primitive is proven.

### order-lifecycle-hardening - IMPLEMENTED
**Dust guard, cancel-race confirmation, timeout escalation, and boot-time broker reconciliation**

Diff src/lib/order-replacement.ts, stale-limit-orders.ts, broker-minimum-guard.ts, and broker-held-orders.ts against four specific freqtrade-hardened behaviors: (a) refuse to cancel a partially-filled order when the filled or remaining stake would strand a position below the broker minimum that no valid exit order can then close — log and leave it resting instead; (b) after issuing any cancel during a replace, re-fetch order status up to ~3 times waiting for a terminal state, and abort the replace entirely if the order filled in the race window (next tick handles it) rather than risking double-fill/double-cancel; (c) count exit-order timeout-cancels per position and escalate to a market emergency exit after N failures instead of resubmitting limit orders forever while price walks away; (d) on every boot/scheduler start, re-fetch every DB row with an open broker-order status and heal drift through the SAME code path live status-polling uses — covering fills that landed while the process was down (deploy, crash, Coolify restart), with very-old not-found orders force-closed via a synthesized cancel through that same path. Each is a small, testable state-machine change on existing modules.

External-repo evidence: freqtrade: freqtrade/freqtradebot.py (manage_open_orders, handle_cancel_enter/exit dust guard, cancel-race retry-then-abort, exit_timeout_count -> emergency_exit, startup_update_open_orders, handle_insufficient_funds); Socratic.Trade grounding: src/lib/order-replacement.ts, stale-limit-orders.ts, broker-minimum-guard.ts, broker-held-orders.ts, db-execution.ts (partial analogs to diff against)

Gap verdict: **partial** (effort S, recommended True).  Of the four freqtrade behaviors named in the lesson, three are already implemented in this codebase, in some cases more conservatively than freqtrade's own design:
(b) cancel-race confirmation — fully implemented (pollCancelSettlement, up to ~5 re-polls over 10s, defer-not-abort semantics).
(c) timeout escalation — implemented via a different, arguably simpler mechanism: stale exits jump straight to a market order after one age threshold rather than counting N failed limit resubmissions (there is no repeated-limit-resubmit loop to escalate away from in the first place).
(d) boot-time reconciliation — implemented: the scheduler's first tick fires immediately (not cadence-gated) and reuses the exact live-polling code path (reconcilePendingFills, flagStalePlacingIntents, the order_replacements pump) to heal drift from fills/placements that happened while the process was down. The one sub-piece missing — force-closing a very-old not-found order via a synthesized cancel — is a DELIBERATE omission: the app escalates to the owner with position evidence instead of fabricating broker state, which is consistent with its explicit 'never fabricate data' convention and an explicit in-code comment in order-replacement.ts explaining why synthesizing a 'canceled' status without broker confirmation is unsafe.

(a) is the real gap. broker-minimum-guard.ts prevents PLACING a new order below the broker's dollar/fractional minimum (and can bump it up), but nothing in the codebase prevents CANCELING the remainder of a partially-filled order when doing so would strand the filled portion below that same minimum, making it impossible to ever place a valid exit for it later. The standalone cancel API (app/api/orders/cancel/route.ts) doesn't even fetch the order's fill state before canceling — by design ('cancel is risk-reducing, always safe' per broker.ts:39), which is true for the REMAINING quantity but ignores what it leaves behind. This is exactly the scenario class that produced broker-minimum-guard.ts in the first place (the Robinhood sub-$1 AAPL trim incident), just at the opposite end of the order lifecycle — the placement-time fix never closed the cancel-time loophole.

Implementation sketch:
Scope strictly to (a); (b)/(c)/(d) need no new code.

1. src/lib/broker-minimum-guard.ts — add `describeCancelDustRisk(order, positionQuantity, activeBroker): string | undefined`, mirroring the existing `describeBrokerMinimumOrderBlock` shape/naming. Inputs: the order being canceled (side, filledQuantity, symbol, averageFillPrice if available), the account's CURRENT position quantity/value for that symbol (fetched via gateway.getEquityPositions, same as order-replacement.ts's exitBackingQuantity pattern), and brokerMinOrderNotional(activeBroker). Logic: only applies to entry sides (buy/short) with filledQuantity > 0 and remaining > 0; compute the filled portion's estimated notional (filledQuantity * averageFillPrice, or filledQuantity * current market price as fallback); if that value is below the broker's floor AND it isn't simply adding to an existing larger position of the same symbol (i.e., positionQuantity - filledQuantity is ~0, meaning this fill IS the entire resulting position), return a warning string. Reuse round6/epsilon helpers already in the file.

2. app/api/orders/cancel/route.ts — before calling cancelEquityOrder, fetch the order via gateway.getEquityOrders (one extra call, already the pattern order-replacement.ts uses), find the target, and if describeCancelDustRisk returns a warning: audit("order_cancel_dust_risk", {...}, userId, policy.connectedAccountId) and include the warning string in the JSON response (e.g. `{ ...result, dustWarning }`) so app/console/orders/cancel-sheet.tsx can surface it inline — but STILL execute the cancel. This must be advisory-only, never a block: per the product-philosophy section (guardrails are owner-adjustable preferences, cancel is the operator's emergency lever per the existing §7 slice 3 doctrine at cancel/route.ts:19-21), a real-money app must never delay or refuse a cancel the owner asked for.

3. Optional owner-facing surfacing: reuse the existing SUB_MINIMUM_ALERT_COOLDOWN pattern (broker-minimum-guard.ts:228-246, shouldAlertBrokerMinimumOrderBlock) to send a proactive sendNotification (notifications.ts channel, mirroring stale-limit-orders.ts:100-114) at most once per (user, account, symbol)/24h if the owner actually goes through with a dust-producing cancel — so it's visible outside the audit log without being spammy.

4. Tests: extend test/broker-minimum-guard.test.ts (or a new test/broker-minimum-guard-cancel-dust.test.ts) covering: partial-fill buy canceled leaves dust below floor -> warning; partial-fill scaling into an existing larger position -> no warning (not the sole source of the position); whole-share orders -> no-op (floor doesn't apply, same isFractionalOrDollarBased gate already in the file); no known broker floor -> no-op.

Effort is small because it's additive glue over an existing, well-tested module (broker-minimum-guard.ts) and an existing single-purpose route (cancel/route.ts) — no new state machine, no schema change, no new scheduler lane.

### memory-decay-and-pruning - PLANNED (deferred, board row exists)
**Time-decayed importance, blended retrieval re-rank, and dual-threshold pruning for the episodic RAG store**

Credit assignment onto cited evidence already exists (the retrieval-usefulness join credits each ragAttributions vector with matured outcomes and applies a bounded ranking nudge) — the missing FinMem half is the lifecycle: nothing in experience-memory.ts / socratic-memory.ts decays or forgets, so ancient never-reinforced cases accumulate forever and dilute retrieval. Add: (1) a scheduled job (not per-query) applying exponential decay to per-doc recency and importance metadata, with different constants per doc_type (lessons/coach-notes decay slower than raw decision cases); (2) at retrieval, re-rank the top-N Pinecone hits with FinMem's capped blend — similarity + recency + min(importance,100)/100 — so outcome-earned importance nudges but can never override semantic relevance; (3) a dual-threshold sweep that archives/soft-deletes docs below a recency floor OR importance floor, bounding index growth. Directly serves the existing Pinecone cost guardrails (pinecone-monthly-pace.ts, pinecone-wu-breaker.ts). Verifiable: decay math and prune thresholds are pure functions.

External-repo evidence: finmem: puppy/memory_functions/decay.py (per-layer ExponentialDecay), puppy/memory_functions/compound_score.py (capped blended score), puppy/memorydb.py (_clean_up dual-threshold OR pruning, decay-before-jump ordering); Socratic.Trade grounding: src/lib/db-retrieval-usefulness.ts (credit assignment already shipped — excluded from this lesson), src/lib/experience-memory.ts / socratic-memory.ts (no decay/recency/importance terms found), pinecone-wu-breaker.ts (cost pressure this bounds)

Gap verdict: **partial** (effort L, recommended True).  The FinMem half explicitly called out as missing in the lesson is confirmed missing, with one nuance the lesson didn't know: the raw ingredient for per-document importance already exists and is silently wasted. Concretely, three gaps: (1) No decay job exists anywhere -- no scheduled or ad-hoc code path mutates a recency/importance value over time for any episodic doc (experience-memory.ts, socratic-memory.ts). The only 'recency' constant in the whole memory subsystem is `memory/salience.ts`'s hardcoded `1.0`, which belongs to the unrelated user-preference-memory system, not the Pinecone episodic store. (2) Retrieval re-ranking today is kind-level only: `applyRetrievalUsefulnessWeighting` blends a bounded win-rate multiplier keyed by (doc_type, memory_kind) into an RRF-positional base -- there is no per-document blended-score (similarity + recency + importance) as FinMem's compound_score.py does, and no per-document recency term at all. The per-doc win/loss rows `creditRetrievalUsefulness` already writes into `retrieval_usefulness_stats` (docId != '') are computed but never read by any consumer in the codebase -- this is the cheapest available wedge for a real per-document importance signal. (3) No pruning/archiving lifecycle exists: every Pinecone-deletion code path found (purgeVectorRecordsByMetadata, purgeVectorRecordIds, purgeManagedVectorsByIds, purgePrivateVectorRecordsForUser, reconcileManagedVectorRecords) is either a GDPR/rights-erasure operation (account deletion, FMP rights revocation) or crash-consistency cleanup of pending-vs-committed commits -- none is age- or importance-triggered, so index growth is genuinely unbounded over time except via the two write-throttle guardrails (pinecone-wu-breaker.ts, pinecone-monthly-pace.ts), which only slow new ingest and never shrink the existing corpus.

Implementation sketch:
Ship in two independently-mergeable layers, following the module-per-concern convention (a new pure math module + a new db-* module + a scheduler hook + a retrieval-path hook), never touching the existing db-retrieval-usefulness.ts split.\n\n1) New table + db-* module (`src/lib/db-memory-lifecycle.ts`, schema added to db.ts migrate() per the split-vs-modified convention): `vector_doc_lifecycle(user_id, vector_id, doc_type, first_seen_at, last_retrieved_at, retrieval_count, archived_at NULL, updated_at)`. Populated at write time by experience-memory.ts/socratic-memory.ts's existing storeContexts call sites (first_seen_at = now), and bumped (last_retrieved_at, retrieval_count++) whenever `retrieveDecisionExperiences`'s `injected` list includes that id -- this is the missing 'was this doc actually used' recency signal FinMem needs, and it's real usage data, not synthesized.\n\n2) Pure decay/blend module (`src/lib/memory-decay.ts`, unit-testable like `usefulnessMultiplier`): `recencyScore(ageDays, halfLifeDays)` = exp(-ln2*ageDays/halfLife), per-doc_type half-life constants (lesson/coach-note slow e.g. 180d, socratic-decision/experience faster e.g. 45d, all env-overridable e.g. RAG_MEMORY_DECAY_HALFLIFE_LESSON_DAYS); `docImportance(statRow)` derived from the ALREADY-COMPUTED per-doc `retrieval_usefulness_stats` row (samples/wins/losses -> 0..100, neutral 50 default for docs with no credited history yet, so new/unproven docs are never penalized); `blendedScore(similarity, recencyScore, importance)` = capped weighted blend, mirroring FinMem's compound_score.py and the lesson's own 'similarity ... can never override semantic relevance' constraint.\n\n3) Scheduler job (`runMemoryDecaySweepIfDue`, wired into scheduler.ts next to `runRetrievalUsefulnessJoinIfDue`, same once-per-UTC-day-per-user watermark pattern via getInternalSetting/setInternalSetting): recomputes blended_score into vector_doc_lifecycle for all live rows -- pure DB read/write, no Pinecone metadata mutation (cheaper, avoids extra write-unit cost against the very quota this is meant to protect), auditable via one `memory_decay_swept` audit row/day with counts.\n\n4) Retrieval-time hook: add `applyMemoryDecayWeighting` in experience-memory.ts's `retrieveDecisionExperiences`, composed AFTER `applyRetrievalUsefulnessWeighting` (same RRF-positional-base, bounded multiplier, fail-open-to-incoming-order pattern) so it never fights the existing nudge -- both are advisory, bounded, and rank-stable when neutral. Off-switch env `RAG_MEMORY_DECAY_WEIGHTING=off`, default ON since it's read-only and harmless like the existing usefulness weighting.\n\n5) Dual-threshold sweep -> SOFT ARCHIVE first, never hard-delete-by-default: a separate, slower (weekly) job flags vector_doc_lifecycle rows for `archived_at` when recencyScore < a floor AND blended_score < an importance floor AND age exceeds a minimum grace period (e.g. never touch anything <90 days old) AND doc_type is NOT lesson/coach-note (owner-curated content is exempt from auto-pruning, always). Archived rows are excluded from retrieval via a join/filter, fully reversible, and receipted with an audit row listing exact ids (mirrors reconcileManagedVectorRecords's promote/delete-id-list receipt style). A SEPARATE, explicitly owner-gated pass (env `RAG_MEMORY_AUTO_PRUNE_HARD_DELETE=off` by default) later reuses the existing `purgeVectorRecordIds` (dry-run-first) to physically remove long-archived ids from Pinecone -- keeping the destructive step opt-in and receipts-driven, consistent with 'guardrails are adjustable, never a cage' but also with never silently destroying data.\n\n6) Owner visibility: extend whatever surface already renders `getAllVectorStoreStats` (con-* console design system, per-doc_type live/archived counts + average blended score + last-swept timestamp) and route threshold-crossing events (e.g. >X% of a doc_type archived in one sweep) through the existing notify()/alertStorageWarning-style channel used by pinecone-wu-breaker.ts.

### live-signal-health-monitor - IMPLEMENTED
**Standing RankIC, bucket-monotonicity, and churn diagnostics on live proposals, with a drift alarm**

backtest.ts already implements tie-corrected cross-sectional Spearman RankIC — but only inside the backtester. Run qlib's signal-quality suite continuously on PRODUCTION decisions, needing only the (symbol, date, conviction score, realized forward return) table that matured outcomes already provide: (1) rolling per-period RankIC between Green's conviction and subsequent N-day return, tracked as a time series — a leading indicator that ranking quality is degrading before P&L shows it; (2) conviction-bucket monotonicity — group proposals into score quantiles and check realized returns actually order by bucket, i.e. 'high conviction' genuinely outperforms 'low'; (3) top-K day-over-day overlap/churn to catch noise-driven conviction flapping that real trading costs would erase — and per qlib's reporting discipline, show headline stats paired gross AND net of costs; (4) a drift alarm on sustained RankIC decline feeding the existing confidence-size-cap machinery to throttle sizing or accelerate recalibration (the cheap distillation of DDG-DA's concept-drift detection). Longitudinal signal health across decisions — distinct from the queued per-proposal scorecard artifact.

External-repo evidence: qlib: qlib/contrib/report/analysis_position/score_ic.py, qlib/contrib/report/analysis_model/analysis_model_performance.py (_group_return, _pred_turnover), qlib/contrib/report/analysis_position/risk_analysis.py (paired with/without-cost stats), qlib/contrib/rolling/ddgda.py (drift-detection principle); Socratic.Trade grounding: src/lib/backtest.ts lines 241-274, 493 (spearmanRankIC exists, backtest-only today); matured multi-horizon outcomes in the retrieval-usefulness join supply the labels; confidence-size-cap machinery per learned-context-safety is the existing throttle to feed

Gap verdict: **partial** (effort M, recommended True).  Three separate pieces of the requested system already exist in production code but none of them is the standing monitor the lesson describes, and they don't talk to each other:

1. RankIC math exists (backtest.ts's spearmanRankIC) but only runs inside the backtester against historical strategy weights — never against live socratic_decisions.
2. A full RankIC + quantile-bucket + placebo + go/no-go harness exists (congress-score-eval.ts / congress-score-gate.ts) — architecturally this is almost exactly what the lesson wants — but it is scoped to the "congress score" sub-signal only, not the LLM's own confidenceScore on real trade proposals, and it runs on-demand via an admin POST route, not a scheduled rolling cadence, and has zero notify() wiring.
3. Conviction-bucket monotonicity for the LLM's own confidenceScore is already enforced (performance.ts getConfidenceCalibration/calibratedConviction, isotonic pool-adjacent-violators over 4 realized win-rate bands) and already throttles sizing through owner-adjustable knobs (policy.tuning.calibrationSizing, convictionCapUncorroborated) in strategy-risk.ts — this is the exact "confidence-size-cap machinery" the lesson wants to feed. But it is a point-in-time recompute from CLOSED lots grouped into 4 discrete bands, not a rolling per-period time series, has no trend/drift concept, no alarm, and is completely invisible to the owner (consumed only inside the LLM prompt payload, never rendered in any console page).

Missing entirely, with no code found anywhere in src/lib or app/:
- A rolling per-period (e.g. daily/weekly) RankIC time series computed continuously against LIVE decisions (as opposed to backtest weights or the congress sub-signal).
- Top-K day-over-day overlap/churn (noise-driven conviction flapping) — no such metric exists for live proposals at all.
- Paired gross-AND-net-of-cost headline reporting for a live signal-health readout (backtest.ts has cost-adjusted equity curves, but only offline/historical).
- A drift alarm on sustained RankIC decline that actually notifies the owner (via notify()) and/or dynamically tightens/loosens the existing convictionCapUncorroborated or calibrationSizing knobs — nothing wires a live rolling metric to either the notify system or the sizer today.
- Any console UI (app/console/*) surfacing any of this — not calibration, not the congress verdict, not RankIC — to the owner.

Implementation sketch:
Reuse rather than rebuild: lift spearmanRankIC out of backtest.ts (or export it) and clone the shape of congress-score-eval.ts's evaluateCongressScore() (rankIC + quantile buckets + topMinusBottom) as the core math for the LLM's own confidenceScore, since that file is a working, tested template for everything except churn and the alarm.

1. `src/lib/db-signal-health.ts` (new db-* module, per the split convention): a `signal_health_snapshot` table (add CREATE TABLE to db.ts's migrate(), CRUD here) storing one row per (userId, periodEnd, horizon): rankIC, tStat, nObservations, quantileBuckets (JSON: avgReturn/hitRate per bucket — the monotonicity check), topKChurnPct (Jaccard/overlap of day N vs day N-1 top-K decision sets by confidenceScore), grossReturn/netOfCostReturn pair, and a rollingRankICSlope for drift detection.

2. `src/lib/signal-health.ts` (new pure-math module): 
   - `computeRankIC(observations)` — call the existing spearmanRankIC per period, mirroring buildCongressScoreObservations's pattern but sourced from decisionCase.confidenceScore + outcome.returnPct (already joined in outcome-engine.ts) instead of the congress score.
   - `computeQuantileBuckets(observations, k)` — same avgReturn/hitRate-per-bucket shape as CongressQuantileStat, direct copy of existing logic.
   - `computeTopKChurn(todayTopK, yesterdayTopK)` — new: 1 - |intersection|/|union| (Jaccard distance) over symbol sets ranked by confidenceScore each day.
   - `pairGrossNet(observations, costBps)` — subtract a round-trip cost estimate (mirror backtest.ts's costRoundTripBps constant/convention) from gross to get net.
   - `detectDrift(rollingSeries, thresholdSlope, minWindows)` — sustained-decline detector over the stored rolling series (simple linear-slope or N-consecutive-declining-windows check — keep it "cheap pure arithmetic" per the lesson's own framing).

3. Scheduler wiring (`src/lib/scheduler.ts`): add a `journalLane("signal-health-refresh", {userId}, () => runSignalHealthRefreshIfDue(userId))` alongside the existing `learning-review`/`retrieval-usefulness-join` daily lanes (same once-per-UTC-day marker pattern already used by retrieval-usefulness.ts's LAST_RUN_KEY_PREFIX). This is what makes it a "rolling" monitor instead of congress-score-gate's manual-refresh pattern.

4. Drift alarm: when `detectDrift` fires, call `notify()` (src/lib/notify.ts) with a `kind` alert (mirrors how block/run_failed alerts are sent) AND write an audit event. Feed-through to the size cap is an OWNER-ADJUSTABLE knob, not automatic enforcement: add `policy.tuning.signalHealthAutoThrottle?: boolean` (default OFF, mirroring `congressGoNoGoGating`'s and `calibrationSizing`'s opt-in pattern) — when on, a confirmed drift alarm tightens `convictionCapUncorroborated` (or forces `calibrationSizing` on) for the account until the next clean window; when off (default), the alarm only notifies/logs — no silent behavior change, consistent with "guardrails are adjustable owner preferences, never a cage."

5. Console UI: a new page under `app/console/results/` (or a new `app/console/signal-health/`) using the con-* design system, rendering the rolling RankIC line, the quantile-bucket bars (reusing the existing ScorecardBars-style component from charts.tsx), churn %, and gross-vs-net stat pair — this is the piece with zero precedent today (calibration/congress-verdict data is currently invisible to the owner).

Tests: mirror test/congress-score.test.ts's structure (synthetic observations with known IC sign/magnitude) plus new churn/drift unit tests with hand-computed Jaccard and slope cases.
