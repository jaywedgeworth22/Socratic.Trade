# Agentic Trading — Audit Work-Split & Kickoff Prompts

**Date:** 2026-07-01 · Companion to `docs/reviews/2026-06-30-improvement-audit.md`.

This splits the audit into independent workstreams. Each section below is a **ready-to-paste kickoff prompt** for a fresh chat. To start one, either paste its section, or open a chat in the relevant repo(s) and say: *"Read `docs/reviews/2026-07-01-audit-work-split.md`, implement workstream <X>."*

## Repo-context split

| Workstream | Needs repos | Farm to a fresh chat? |
|---|---|---|
| A — LLM & prompting | Agentic Trading only | yes (single-repo) |
| B — Learning loop | Agentic Trading only | yes (single-repo) |
| C — RAG / embedding | Agentic Trading only | yes (single-repo) |
| D — Data sources & breadth | Agentic Trading only | yes (single-repo) |
| E — Performance | Agentic Trading only | yes (single-repo) |
| F — UX + aesthetics | Agentic Trading only | yes (single-repo) |
| G — Security / risk / testing / ops | Agentic Trading only | yes (single-repo) |
| C1 — Congress.Trade integration | Agentic Trading **+ congress-trading-shared + Congress.Trade** | keep in a 4-repo chat |
| C2 — API Usage Monitor integration | Agentic Trading **+ congress-trading-shared + API-usage-monitor** | keep in a 4-repo chat |

**Cross-cutting rule:** any "Agentic-only" task that ends up needing a change to the shared `@jaywedgeworth22/congress-trading-shared` contract graduates to a 4-repo chat (bump + publish the package, then bump consumers). None of A–G should need that.

---

### Chat A — LLM & prompting (money-path)
**Repo context:** Agentic Trading only (`/Users/jay/Code/Agentic Trading`). No other repos needed. All work is in `src/lib/*` plus `scripts/eval/` and `test/`.

**Goal:** Bring the autonomous strategy "money-path" (Bull proposer + inline Bear red-team + per-proposal debate) up to the rigor the chat subsystem already has: no fail-open on Bear errors, versioned + eval-covered prompts, prompt caching, cross-provider failover, truncation-aware output caps, strict JSON schemas, and a wired-in rationale-collapse gate. This is a research app defaulting to paper/Test mode — every behavior change must be safe-by-default and, where it changes routing, behind a default-off flag.

**Scope — implement these (verified against current code):**

1. **Fix the inline Bear red-team FAIL-OPEN.** In `src/lib/strategy.ts`, function `proposeTrades` (the inline Bear agent block, roughly lines 2225–2440), there are **three** fail-open exits that silently carry un-critiqued Bull proposals forward: the outer `catch` (`return { proposals: bullProposals, llmSteps }`, ~line 2414), the `bearResult.fallbackToBull` branch (~line 2423), and the `if (!bearKey)` skip (~line 2317). Contrast with `src/lib/red-team.ts` `debateProposal` (already fail-CLOSED: returns `available:false` → `strategy.ts:455` routes to human) — the two paths contradict each other. Make the inline Bear failure **policy-aware**: read `input.policy.strategyAuthority` (already in scope; values include `"propose"` and `"decide"`) and in autonomous (`decide`) mode do NOT auto-carry — either block or force the proposals to human-review status (mirror the existing "Red Team unavailable" routing at `strategy.ts:455–457,698–701`); surface an `available:false`-style signal on the returned step; emit a loud `audit(...)`/`console.warn` and a `sendNotification` (already imported at `strategy.ts:40`). *Acceptance:* a Bear timeout/429/malformed-JSON/missing-key in `decide` mode never yields auto-approved/auto-executed trades — a new `test/` case drives `proposeTrades` with a failing Bear fetch and asserts proposals are routed to human (not carried through). *Note:* the per-proposal `debateProposal` path is already fail-closed and PR #278 clamped its reasoning effort — do not "re-fix" it; the bug is the **inline Bear agent** only.

2. **Version the strategy Bull/Bear prompts + add an offline eval + stamp version on rows.** Today the chat path is versioned (`src/lib/chat/prompt.ts` `PROMPT_VERSION = "agentic-chat@0.7.0"`, stamped via `audit("chat.turn", { prompt_version }, …)` in `src/lib/chat/orchestrator.ts:46,105`) and eval-gated (`scripts/eval/run-offline.ts`, which is **chat-only** — imports `chat/llm`, `chat/prompt`, `chat/tools`, `DATASET`). The strategy Bull/Bear system prompts are large inlined string arrays in `src/lib/strategy.ts` (Bull system ~lines 1920–1990; `bearSystemPrompt` ~lines 2226–2243) with no version and no eval. Extract them into a versioned module (e.g. `src/lib/strategy-prompts.ts` exporting `STRATEGY_PROMPT_VERSION` + `buildBullSystem()`/`buildBearSystem()`), add an offline eval mirroring `run-offline.ts` (new `scripts/eval/run-strategy-offline.ts` + a strategy dataset + deterministic scorers: never propose off-universe, every short carries a stop, reject buys that contradict structured evidence), and stamp the version on each proposal/audit row — add a `prompt_version` column to `trade_proposals` (migration in `db.ts` `migrate()`, wire through `insertProposal` in `src/lib/db-proposals.ts:256+`). *Acceptance:* Bull/Bear prompts live in one versioned module; `npm run <eval-script>` runs the strategy eval green on the deterministic scorers; new proposals persist a non-null `prompt_version`. *Partly-scoped:* the extraction is net-new but must reuse existing `THESIS_PLAYBOOK`/`allowedSides`/schema construction already in `strategy.ts` — don't fork them.

3. **Add Anthropic prompt caching to the strategy/red-team path.** `src/lib/llm-call.ts` `buildLlmRequestBody` (anthropic-messages branch ~line 72) sends `system` as a plain string with no `cache_control`. The working pattern is in `src/lib/chat/llm.ts:363–378` (`{ type: "text", text: SYSTEM_PROMPT, cache_control: { type: "ephemeral" } }`) plus the `"anthropic-beta": "prompt-caching-2024-07-31"` header (`chat/llm.ts:332–333`). Split the strategy system prompt into a stable cacheable prefix (`cache_control: ephemeral`) + dynamic suffix, and ensure the beta header is sent for anthropic-transport strategy calls (`llmAuthHeaders` at `llm-call.ts:49`). *Acceptance:* an anthropic-transport Bull/Bear request body carries a `cache_control` block on the stable prefix and the caching beta header; OpenAI-compatible transport is unchanged.

4. **Add ordered cross-provider failover.** `src/lib/llm-provider.ts` `resolveLlmEndpoint` resolves exactly one endpoint; the Bull call throws on non-200 (`strategy.ts` ~2095–2098) with no retry against another provider. Add an ordered fallback list so that on `429`/`5xx`/timeout (`AbortError` from `llmFetch`, `LLM_TIMEOUT_MS` in `src/lib/llm-request.ts`) the **same** request spec is re-issued against the next endpoint, and record the actually-serving provider (extend the `recordStep`/`recordLlmUsage` metadata already present). Keep this behind a default-off policy flag so single-provider behavior is unchanged by default. *Acceptance:* with the flag on, a mocked 429 from provider A transparently succeeds via provider B and the served-provider is recorded; with the flag off, behavior is byte-identical to today.

5. **Make the Bull output cap truncation-aware.** `src/lib/llm-request.ts` `LLM_OUTPUT_TOKEN_CAPS.strategyProposal` = `LLM_REQUEST_DEFAULTS.maxOutputTokens` = **1500**; a long Bull JSON answer can truncate → `JSON.parse` fails (`strategy.ts` ~2183 already tolerates a malformed response but yields **zero proposals**). No code anywhere inspects `finish_reason`/`stop_reason` (verified: only doc-comment mentions exist). Detect `finish_reason === "length"` (OpenAI) / `stop_reason === "max_tokens"` (Anthropic) on the Bull response and either scale the cap up and retry once or log a distinct truncation reason instead of silently emitting nothing. *Acceptance:* a truncated Bull response is distinguished from a genuine no-op and does not silently produce zero proposals; unit test covers the length-finish branch.

6. **Use strict `json_schema` for the red-team on OpenAI-compatible providers.** `src/lib/red-team.ts:120` sets `openAiJsonObject: true` (bare `json_object`, no schema enforcement); the schema is only enforced as an Anthropic tool, and `debateViaAnthropic` (~line 203) relies on regex/looser extraction. Switch OpenAI/Gemini debate calls to strict `json_schema` (the `{rejected, reason}` shape is already defined at `red-team.ts:26–29`) via `buildLlmRequestBody`. *Acceptance:* the red-team debate request for an OpenAI-compatible provider sends a strict `json_schema` response format; parsing no longer depends on regex.

7. **Wire the rationale-diversity collapse detector into a gate.** `src/lib/rationale-diversity.ts` computes `collapsed` (threshold `DEFAULT_COLLAPSE_THRESHOLD = 0.85`); `strategy.ts:516–524` computes it but only `console.warn`s ("Advisory only — … Never affects proposal generation or selection"). Wire `collapsed:true` into an actual gate: flag-for-review / re-sample / refuse auto-execute — behind a default-off flag so today's advisory-only behavior is the default. *Acceptance:* with the flag on, a collapsed run does not auto-execute (routes to review or re-samples); with the flag off, behavior is unchanged (still advisory).

8. **Delete the dead/unreachable Anthropic branch in `resolveLlmEndpoint`.** `src/lib/llm-provider.ts` (~lines 120–133) has a `/^(claude|anthropic)/i` branch that returns a broken endpoint: `provider: "openai" as any` with `transport: "chat-completions"` pointed at Anthropic's `https://api.anthropic.com/v1/messages` (which is not a chat-completions endpoint — it can never succeed). Remove it (or replace with a correct `anthropic`/`anthropic-messages` resolution if you confirm a real caller needs it — grep first). Optionally emit a `warn` for unpriced LLM models per the audit's S-item. *Acceptance:* the dead branch is gone; `npx tsc --noEmit` and tests stay green; no caller regresses.

**Already-landed — verify first, do NOT redo:** PR #278 (`5f945fa1`) added the interactive-reasoning-effort clamp to the required red-team `debateProposal` (`red-team.ts:126`) and to pending-proposal revalidation, plus timeout/sizing guardrails. Confirm current state before touching those areas; they are out of scope here.

**Out of scope:** Surfacing the Red Team in the dashboard UI (that's a separate workstream); RAG retrieval-quality eval; the `strategy.ts` god-module split (2,975 lines — do not refactor structurally beyond the prompt-module extraction in item 2); chat-path changes; rate-limiting `/api/chat`; broker/execution-layer changes; the synthetic bid/ask limit-price issue.

**Process & constraints:**
- Work in the Agentic Trading repo on a feature branch (per AGENTS.md/CLAUDE.md, land via `bash scripts/land.sh` — never push to `main`; the pre-push hook blocks it). Confirm `git config user.email` is the noreply address before committing.
- Paper/Test mode is the default — **NEVER place real trades or flip `paperMode: false`**. All routing/behavior changes (items 4, 7, and the autonomous-block in 1) go behind **default-off flags** so default behavior is unchanged; only the fail-open safety fix (item 1) changes default behavior, and only in the fail-safe direction (blocking, not enabling, trades).
- Add tests for every item: at minimum a Bear-failure fail-closed test (item 1), a strategy offline-eval with deterministic scorers (item 2), a truncation/`finish_reason=length` unit test (item 5), a failover-on-429 test (item 4), and a collapse-gate test (item 7). Tests use a temp SQLite file (`DATABASE_URL=file:<tmpdir>/...`) — never the dev `data/app.db`.
- Run the verify quartet before claiming done, in order: `npx tsc --noEmit`, `npm run lint`, `npm test`, `npm run build`.
- Follow the handoff protocol: update `STATUS.md`, add `docs/rollouts/YYYY-MM-DD-strategy-llm-money-path.md` (summary/why/files/verification/follow-ups), update `PLAN.md` and `docs/phase-7-strategy.md`, and reference the updated docs in each commit message.
- Open the PR as **READY** (not draft); merge with `gh pr merge <n> --squash --auto` once the required `verify` check is green.

---

### Chat B — Learning loop / auto-tuning

**Repo context:** Agentic Trading only (`/Users/jay/Code/Agentic Trading`). No cross-repo work — everything here is in `src/lib/` and `app/`. (Congress *data* arrives via the existing in-repo `congress-score.ts` / `market.ts` path; you do not touch congress-trading-shared / Congress.Trade.)

**Goal:** Close the learning loops that the audit found "built but never wired into the money path." Today the factor-weight tuner, congress-score validator, counterfactual/missed-opportunity stats, and confidence calibration are all computed but only ever feed a *manual* proposal, an admin read-only route, or a prompt string — none of them actually adjust the scan composite, sizing, or persisted weights autonomously. This workstream wires them in behind default-off flags, statistically gated, clamped, and audited, so the app can learn from its own outcomes without unsafe overfitting.

**Scope — implement these (verified against current code):**

1. **Opt-in autonomous factor-weight tuning (close the loop).** Today `proposeStrategyTuning` (`src/lib/strategy-tuning.ts:152`) is only reachable via `app/api/strategy/tune/route.ts`, and `deriveWeightsFromIC`/`runWalkForwardOOS` only via `app/api/admin/backtest-ic/route.ts` — no scheduler/trigger caller exists. Add a new default-off `policy.tuning.autoApplyWeights` flag to the `TuningSettings` interface (`src/lib/types.ts:153`, alongside knobs like `minClosedLotsForWeightShift`) and a cadence-gated caller (in `src/lib/triggers.ts` near the `runStrategyOnce` call at `:141`, or a new sibling fn) that runs `proposeStrategyTuning` → the existing `applyOosGate` (`strategy-tuning.ts:289`, which already calls `runWalkForwardOOS`) → clamps every weight delta to `MAX_WEIGHT_STEP` (`strategy-tuning.ts:41`, already used at `:580`) → persists via the account-policy `scoringWeights` writer (`src/lib/db-profiles.ts:48,206`) with an `audit(...)` row and a stored previous-weights snapshot enabling revert. *Acceptance:* with `autoApplyWeights` off (default) behavior is byte-identical; with it on, a cadence run that passes the OOS gate persists clamped weights + an audit row, and a revert path restores the prior vector. Note: the `policy.tuning` namespace already exists but has **no** autonomous-apply flag — this flag is new.

2. **Wire congress-score-eval go/no-go into scan/scoring.** `src/lib/congress-score-eval.ts` (placebo-IC, t-stat, marginal IC, quantile spread) has **zero production importers** (verified: repo-wide grep for its exports outside the file and tests is empty). The congress signal *is* used in scan scoring — `outlierInterestScore` (`src/lib/market.ts:44`) and `notableCongressAnalyticsScore`/`scoreCongressSignal` (`market.ts:51,72,332`) — but is never gated by its own validation. Add a cached go/no-go read so a **failing** congress signal (below the eval's t-stat/marginal-IC thresholds) is down-weighted or zeroed in the congress contribution to the composite, behind a default-off flag; surface the current verdict (pass/fail + stats) in the dashboard. *Acceptance:* when the flag is on and the eval verdict is "no-go," the congress term no longer lifts a name into the candidate set; the verdict is visible in the UI; flag off = current behavior.

3. **Feed matured counterfactual/missed-opportunity stats into scan composite / Bull prompt.** `src/lib/counterfactual-learning.ts` + `performance.ts` compute per-factor skipped-winner stats correctly, but `summarizeMissedOpportunities` (`strategy-tuning.ts:108`) is consumed **only** inside the manual tuning proposal. Thread a sample-gated, audited per-factor nudge from these matured stats into either `scoreFactors` weighting (`market.ts:440`) or the Bull-prompt context block (`src/lib/strategy.ts` prompt assembly near `:1883`), behind a default-off flag. *Acceptance:* a factor that recurs across gated missed winners produces a small, clamped, audited nudge (or prompt line) only when the sample gate is met; flag off = no change.

4. **Harden `recurringFactor`: raise threshold + make it benchmark-relative.** `strategy-tuning.ts:134` fires `recurringFactor` at `recurringFactorCount >= 2`, and the winner filter at `:109` selects `returnPct > 0` with **no market adjustment** (confirmed: no SPY/benchmark map anywhere in `strategy-tuning.ts`/`counterfactual-learning.ts`/`performance.ts` today). Raise the count threshold (≥5) and make the "winner" test SPY-relative (return minus SPY over the same horizon), plumbing a SPY return through the missed-opportunity input. *Acceptance:* a name that only beat zero but lagged SPY no longer counts as a missed winner; `recurringFactor` requires ≥5 benchmark-beating misses.

5. **Fix factor attribution defaulting to "momentum."** `performance.ts:708` resolves an unresolvable lot factor to `"momentum"` (`factorByKey.get(key) ?? "momentum"`), silently mislabeling. Either drop lots whose dominant factor can't be resolved, or persist the dominant factor at entry (there's a `dominantFactor` field/helper at `performance.ts:725,803,937`) so it's always available. *Acceptance:* per-factor stats never attribute an unresolved lot to momentum; unresolved lots are excluded or backed by a persisted entry factor. (Also note the 500-row audit cap at `performance.ts:687` can age out entry snapshots — persisting at entry sidesteps that.)

6. **Feed confidence calibration into sizing.** `getConfidenceCalibration` (`performance.ts:833`) is imported into `strategy.ts:44` but only injected into the **Bull prompt** (`strategy.ts:1885,1981`); the sizer still uses the **raw** `confidenceScore` (`strategy.ts:1145`, `rawConviction = confidenceScore/100`). Remap `confidenceScore` through the realized calibration curve before it becomes the conviction sizing multiplier, behind a default-off flag, respecting the existing `convictionCapUncorroborated` cap (`types.ts:173`). *Acceptance:* with the flag on, a poorly-calibrated high-confidence band is sized down toward its realized win rate; flag off = current raw-score sizing.

7. **Per-regime factor weights (stretch — gate carefully).** `policy.scoringWeights` is a single global vector (`types.ts:456`); `deriveWeightsFromICs`/`runWalkForwardOOS` produce one global vector. Extend the policy + OOS harness to support regime-conditioned weight vectors keyed off `determineMarketRegime` (`src/lib/macro.ts:270`), applied in `scoreFactors`, **gated by per-regime sample size**. If per-regime sample sizes are too thin to be safe, implement only the admin-side per-regime IC *report* and leave application off — say so explicitly in the rollout note.

8. **Verify learning integrity: execution-cost not bypassed.** Confirm the net-of-cost model `applyExecutionCost` (`src/lib/execution-cost.ts`, consumed at `performance.ts:183`, default ON for simulated fills) is actually in the code path that produces the win-rate/edge stats feeding the tuner and sizing — i.e. the learner certifies a *cost-aware* edge, not a cost-free one. This is a verification/regression-test item, not necessarily a code change; if you find a bypass, fix it and document it. *Acceptance:* a test asserts the learner's closed-lot returns reflect execution cost when enabled.

**Out of scope:** Bear red-team fail-open, synthetic bid/ask, RAG eval harness, usage-monitor push wiring, dashboard code-splitting, Congress cross-app contract repair, rate-limiting/auth items, and the strategy.ts god-module split. Do **not** enable any new flag by default. Do **not** build a general backtesting UI — reuse the existing OOS/IC machinery.

**Process & constraints:** Work in the Agentic Trading repo on a branch (per AGENTS.md, launch in your own agent worktree on an `agent/*` branch and land via `bash scripts/land.sh`; never push directly to `main`). Paper/Test mode is the default — **never place real trades or flip `paperMode: false`**. Every behavior change goes behind a **default-off** `policy.tuning.*` flag (or equivalent) so the Phase-0 byte-identical invariant holds when flags are off; add explicit tests for both flag states. Add unit tests for: the OOS-gated clamp+persist+revert path (item 1), congress go/no-go gating on/off (item 2), the sample-gated nudge (item 3), the ≥5 + SPY-relative recurring-factor logic (item 4), factor-attribution no-momentum-default (item 5), calibration-remapped sizing (item 6), and execution-cost-in-learner (item 8); use the temp-SQLite `DATABASE_URL=file:<tmpdir>` pattern (never the dev `data/app.db`). Any new `TradeProposal` literals must set `tradeThesisTag` + `entryMarketRegime`. Run the verify quartet before claiming done: `npm run lint`, `npx tsc --noEmit`, `npm test`, `npm run build` (build wipes `.next/`; restart your PM2 preview after). Follow the handoff protocol: update `STATUS.md`, add a `docs/rollouts/YYYY-MM-DD-learning-loop-autotuning.md` note (summary/why/files/verification/follow-ups), update `PLAN.md`, and update the relevant phase doc (`docs/phase-7-strategy.md`). Confirm `git config user.email` is the noreply address before committing. Open the PR as **READY** (not draft).

---

### Chat C — RAG / Embedding / Knowledge Framework

**Repo context:** Agentic Trading only (`/Users/jay/Code/Agentic Trading`). All RAG code lives here — `src/lib/vector-db.ts`, `src/lib/rag/*`, `src/lib/memory/salience.ts`, `src/lib/web-sources/*`. No cross-app work needed. Do NOT modify `congress-trading-shared`, `Congress.Trade`, or `API-usage-monitor`.

**Goal:** Close the highest-leverage gaps in the RAG/embedding pipeline identified by the 2026-06-30 audit (`docs/reviews/2026-06-30-improvement-audit.md` §6.3). The pipeline is mature (voyage-finance-2 → Pinecone → cosine floor → as-of → optional hybrid → optional rerank) but three things undercut it: **no retrieval-quality eval**, the **reranker throws away its own scores** so there's no post-rerank quality gate, and the **corpus is starved** because full-body ingest is flag-gated off. Build the eval harness first (it gates everything else), fix the score-plumbing, and wire/document the corpus enablement.

**Scope — implement these (verified against current code):**

1. **Build a retrieval-quality eval harness (recall@k / MRR).** Verified: `grep -rn "recall@|recallAt|MRR" src scripts test` is **empty**; `scripts/eval/` exists but is chat/congress-only (`dataset.ts`, `run-offline.ts`, `score.ts`, `run-congress-score.ts`). `docs/chat-assistant-rag-learning.md` §5 (lines 122–123) explicitly calls for "a 20–40 query retrieval eval set (recall@k/MRR + faithfulness)". Add ~25–40 golden `(query, expected-chunk-id[])` tuples + a recorded Pinecone-match fixture (JSON, no live Voyage/Pinecone calls) and a vitest that scores recall@k and MRR against `retrieveContext`/`matchToChunk` (`src/lib/vector-db.ts:562,585`). Follow the existing offline-eval shape in `scripts/eval/`. *Acceptance:* `npm test` runs a new `test/rag-retrieval-eval.test.ts` that computes recall@k + MRR over the fixture and asserts a baseline threshold; no network calls.

2. **Capture rerank relevance scores + add a post-rerank relevance floor.** Verified: `rerankMatches` (`src/lib/vector-db.ts:292–320`) reorders matches but **discards `item.relevanceScore`** from the Voyage rerank response; `matchToChunk` (`:562–579`) only reads `match.score` (cosine) — `RetrievedChunk` (`:546–559`) has **no** rerank field; and `minScore` is applied **pre-rerank** at `:706–707` (cosine-only). Attach each reranked item's `relevanceScore` onto its match/metadata, add a `relevanceScore?: number` field to `RetrievedChunk`, surface it (e.g. in citations/provenance), and add an optional **post-rerank** relevance floor (new `RetrieveOptions` field, e.g. `minRelevanceScore`) applied after rerank. Keep it default-off / opt-in so current behavior is byte-for-byte unless a caller sets it. *Acceptance:* a test shows reranked chunks carry a real `relevanceScore` and that a set floor drops below-threshold chunks after rerank (cosine floor still works independently).

3. **Wire + document paid-Voyage corpus enablement (config/cost decision).** Verified: `WEB_SOURCE_SEC8K_FULL_BODY` default OFF (`src/lib/web-sources/sec8k.ts:416–417`, gated at `:398`), `RAG_EMBED_DISCLOSURES` default `"off"` (`src/lib/web-sources/disclosure-rag.ts:18–20`, called from `src/lib/web-sources/index.ts:175`), and the free-tier batch delay `embedBatchDelayMs()` (`src/lib/vector-db.ts:78`). This is primarily a **config/cost** change (needs a paid Voyage key), so **do not flip defaults on**. Instead: (a) confirm the enablement path works end-to-end when the flags are `on` and `embedBatchDelayMs` → 0; (b) add a short operator doc (env vars, cost implications, the paid-key requirement, how to verify corpus growth) in `docs/` and reference it from the rollout note. *Acceptance:* a doc section lists the exact env vars + steps to enable, and a test (or clearly-scoped manual note) confirms the ingest path runs when flags are on. **Flag it: this item is a config/cost decision, so the code deliverable is enablement + docs, not turning it on.**

4. **Evaluate/enable hybrid BM25/RRF — gated on the item-1 eval.** Verified: `hybridRetrievalEnabled()` default `false` (`HYBRID_RETRIEVAL`, `src/lib/vector-db.ts:110–115`), and `src/lib/rag/hybrid.ts:13–18` documents IDF is computed from the ≤50-doc candidate pool (`overFetchK` caps at 50, `:106–108`). Do **not** enable by default. Once item 1 lands, run the eval with hybrid on vs off and record the measured recall@k/MRR delta in the rollout note; only recommend enabling if the eval shows a gain. Note the corpus-pool-IDF recall ceiling (Pinecone sparse-dense is the longer-term fix, out of scope). *Acceptance:* rollout note contains a hybrid-on-vs-off eval-delta table; default stays off unless the delta justifies flipping it.

5. **Align per-chunk char cap with the token chunker.** Verified: `chunk.ts` uses `DEFAULT_MAX_TOKENS = 480` (`:8`) and keeps tables **atomic** (`:119–122`, `is_table`), while `storeContexts` trims every doc at `DEFAULT_CONTEXT_MAX_CHARS = 2400` via `trimContextText` (`src/lib/vector-db.ts:42,225–230,368`) — so a structure-aware/atomic chunk can be silently truncated with `[truncated for vector memory]`. Raise/remove the char cap for already-chunked `storeDocument`/`storeContexts` input, or align it with the token budget so atomic chunks aren't cut. The cap is env-tunable (`VECTOR_CONTEXT_MAX_CHARS`). *Acceptance:* a test proves a max-size structure-aware chunk (≈480 tokens) round-trips through `storeContexts` without `[truncated…]`.

6. **Normalize `doc_type` casing at write time.** Verified: `buildExtraFilters` (`src/lib/vector-db.ts:599–609`) patches casing **only at query time** by expanding lower+upper `$in` variants, because `sec-filings.ts` writes `"10-K"` (upper) and `sec8k.ts` writes `"8-k"` (lower). Normalize `doc_type` to lowercase at the ingest/`storeContexts` boundary, then simplify `buildExtraFilters` toward exact-match (keep a compatibility variant for legacy mixed-case vectors already in Pinecone — call this out). *Acceptance:* new writes store lowercase `doc_type`; a test confirms the write-time normalization and that the query filter still matches legacy mixed-case data.

7. **Replace the regex salience extractor with a structured-output LLM extractor + ticker validation.** Verified: `src/lib/memory/salience.ts:1–4` calls itself a "deterministic rule-based stand-in"; `TICKER_RE = /\b([A-Z]{1,5})\b/` (`:79`) matches any 1–5-char uppercase token ("I", "A", "CEO") in `extractLearnedCandidates` (`:91–125`). Add a structured-output LLM extractor (behind a default-off flag, falling back to the existing regex so offline tests stay deterministic) and **validate extracted tickers against the known universe** (find the securities/universe source already in the repo — e.g. the securities-import / db module — rather than accepting any uppercase token). *Acceptance:* invalid pseudo-tickers ("I"/"CEO") no longer attach as symbols; regex path remains the test fallback; new extractor is off by default.

**Out of scope:** Migrating to a Pinecone sparse-dense index (note as follow-up only); the money-path Bull/Bear prompt eval/versioning (separate workstream); usage-telemetry push wiring; the query-embedding LRU cache and per-day token-budget ceiling (cost-enforcement workstream); any change to auth/middleware; enabling paid features in production by default.

**Process & constraints:**
- Work in the Agentic Trading repo on a feature branch (per AGENTS.md, agents run in their own worktree on an `agent/*` branch — do not edit the `main` integration worktree; if your lane is busy, use a throwaway worktree off `main` with its own PR).
- **Paper mode is default — NEVER place real trades or flip `paperMode: false`.** This is a read/retrieval workstream and must not touch the order path.
- Keep every behavior change behind a **default-off flag** (rerank floor opt-in, hybrid stays off, LLM salience extractor off, corpus flags stay off). Current default retrieval behavior must be byte-for-byte unchanged unless a flag is set.
- Add the new tests named above (`test/rag-retrieval-eval.test.ts` plus focused tests for items 2, 5, 6, 7); reuse fixtures — do NOT call live Voyage/Pinecone in tests.
- Run the verify quartet before claiming done, in order: `npm run lint`, `npx tsc --noEmit`, `npm test`, `npm run build`. (Note: `npm run build` wipes `.next/`; if a dev preview errors afterward, restart the PM2 process.)
- Follow the handoff protocol in AGENTS.md/CLAUDE.md before committing: update `STATUS.md`, add a `docs/rollouts/YYYY-MM-DD-rag-eval-and-rerank.md` note (summary / why / exact touched files / verification commands run / follow-ups), update `PLAN.md` for any scope change, and update the relevant phase doc (`docs/chat-assistant-rag-learning.md`) to match implementation state. Reference the updated docs in each commit message.
- Confirm `git config user.email` resolves to the noreply address (`12656028+jaywedgeworth22@users.noreply.github.com`) before committing.
- Land via `bash scripts/land.sh` (never push to `main` directly) and open the PR as **READY** for review (not draft).

---

### Chat D — Data sources & breadth
**Repo context:** Agentic Trading only (`/Users/jay/Code/Agentic Trading`). No changes needed in `congress-trading-shared`, `Congress.Trade`, or `API-usage-monitor`.

**Goal:** Close the highest-value gaps in the data-source/enrichment cascade — the audit's best-scoring dimension (5/5) but with real, additive holes: no earnings-calendar signal, a synthetic bid/ask masquerading as a real quote that anchors live limit-price math, no institutional ownership despite an already-authenticated Yahoo call, no options/IV despite a connected Robinhood MCP, a inert circuit-breaker, and single-sourced short interest. Land these as small, independently-reviewable, default-safe changes.

**Scope — implement these (verified against current code):**

1. **Add `daysToEarnings` (next-earnings-date signal).** Currently absent everywhere — a repo-wide grep for `daysToEarnings`/`earnings-calendar`/`calendarEvents` returns nothing outside prose in `strategy.ts` prompt strings. Add it via the existing `YahooFinanceEnrichmentProvider.fetchSymbol` authenticated `quoteSummary` call in `src/lib/data-providers.ts` (~line 1474, `modules = "summaryDetail,defaultKeyStatistics,financialData,assetProfile"` — append `calendarEvents`) or `FmpEnrichmentProvider` (`src/lib/data-providers.ts` line 1739, `base = "https://financialmodelingprep.com/stable"`) if Yahoo's `calendarEvents.earnings.earningsDate` proves unreliable. Thread it through the full per-field checklist documented in `CLAUDE.md`: add `daysToEarnings` to `SymbolEnrichment` (interface at `src/lib/data-providers.ts:94`) and `EnrichmentSourcedField` (union at `src/lib/data-providers.ts:133`), add a `takeScalar("daysToEarnings", name, r.daysToEarnings)` call in `CascadingEnrichmentProvider.enrich` (~line 762-804), add it to the `EMPTY_SOURCED` marker map (~line 861), add `daysToEarnings?: number` to `MarketQuote` in `src/lib/types.ts` (interface starts line 641), fold it into `applyEnrichment` in `src/lib/market.ts` (~line 761-798), and surface it in `compactCandidateForPrompt` in `src/lib/strategy.ts` (~line 2495-2539, e.g. `earnIn: quote.daysToEarnings`). Acceptance: a symbol scanned within N days of its next earnings shows a non-`undefined` `daysToEarnings` on its `MarketQuote`, attributed to whichever provider supplied it (never fabricated when unavailable — must be `undefined`, not `0` or a guess).

2. **Fix synthetic bid/ask mislabeled as a real quoted spread.** `src/lib/yahoo-finance.ts` lines 33-34 and 84-85 always synthesize `bid = price * 0.999` / `ask = price * 1.001` when the chart endpoint (which has no real bid/ask) is used. `src/lib/market.ts`'s `toQuoteOnlyMarketQuote` (lines 726-754) tags both as `sources: { ..., bid: "yahoo-finance", ask: "yahoo-finance" }` — indistinguishable from a real quote. This directly feeds live limit-price math: `src/lib/strategy.ts` line 2776 computes the buy/sell limit as `(quote?.ask && quote.ask > 0 ? quote.ask : refPrice) * (1 + buffer)`, and `hasAskData` at line 2476 (`marketScan.topCandidates.some((quote) => quote.ask && quote.ask > 0)`) gates whether the LLM prompt is told ask-relative limits are allowed — both currently treat synthetic ask as real. Fix: either (a) drop `bid`/`ask` entirely from `toQuoteOnlyMarketQuote`'s returned quote/sources, or (b) tag provenance as `yahoo-finance-synthetic` and make `hasAskData` (and any other real-vs-synthetic distinction) exclude that provenance so it degrades to `refPrice`-based limits, matching the "no ask prices available" prompt branch. Acceptance: a quote-only (non-screener) symbol's bid/ask either doesn't appear or is provenance-tagged as synthetic, and `hasAskData`/the limit-price buffer calc no longer treats it as a real ask.

3. **Add institutional/13F ownership.** Currently absent (`institutionOwnership`/`majorHoldersBreakdown` grep empty). `YahooFinanceEnrichmentProvider.fetchSymbol` in `src/lib/data-providers.ts` (~line 1473-1475) already makes an authenticated `quoteSummary` call — add `institutionOwnership` (and/or `majorHoldersBreakdown`) to the `modules` query string and parse the new response section the same way `assetProfile`/`financialData` are parsed today (~lines 1489-1505). Thread new field(s) through the same checklist as item 1 (`SymbolEnrichment`, `EnrichmentSourcedField`, `takeScalar`, `EMPTY_SOURCED`, `MarketQuote`, `applyEnrichment`, prompt compaction). Acceptance: institutional-ownership percentage (or a summarized major-holders breakdown) reaches `MarketQuote` with correct source attribution, at zero additional API cost (same call, wider `modules`).

4. **Wire Robinhood option-chain data into a low-frequency enrichment tier (near-the-money IV, put/call ratio).** `src/lib/robinhood.ts`'s `HttpMcpRobinhoodGateway` currently only calls equity-account MCP tools (`get_accounts`, `get_portfolio`, `get_equity_positions`, `get_equity_orders`, `get_equity_quotes`, `get_equity_tradability`, `review_equity_order`, `place_equity_order`, `cancel_equity_order` — grep of `callTool("` confirms no `get_option_*` calls anywhere in the file). The connected Robinhood MCP exposes `get_option_chains`, `get_option_instruments`, `get_option_historicals`, and related option tools. Add a new low-frequency (longer TTL — this is not a per-scan-cycle concern) enrichment source that calls the option-chain tool(s) for near-the-money strikes on watchlist/position symbols and derives IV and put/call ratio, then thread the new fields through the same enrichment checklist. Gate this behind a default-off env flag (new provider is `costTier: "paid"` or MCP-quota-consuming) since Robinhood MCP calls are rate/session sensitive. Acceptance: with the flag on and Robinhood MCP connected, a watched symbol shows a near-the-money IV and put/call ratio on its `MarketQuote`, provenance-tagged to the new source; with the flag off, behavior is unchanged.

5. **Make the per-provider circuit breaker active, not display-only.** `src/lib/db-health.ts` computes `stoppedWorking`/`stoppedReason` (lines 180-192, exposed via `getServiceHealthSummaries()` at line 128) purely for the health-status UI — `src/lib/data-providers.ts` never reads it (grep of `getServiceHealthSummaries`/`getApiHealth` in that file returns nothing; it only calls `logApiHealth` to write). Have the enrichment-provider resolution path (wherever providers are assembled/dispatched into the `CascadingEnrichmentProvider` cascade, `src/lib/data-providers.ts` ~line 680 onward) consult `getServiceHealthSummaries()` and skip/no-op a lane whose current status is `stoppedWorking` for its own `service` name, with a longer re-probe interval (e.g. re-attempt one call every N minutes to detect recovery) rather than retrying every scan. Gate behind a default-off flag so a bad interaction with a currently-working provider can't silently blackout data. Acceptance: a provider that logged 5 consecutive failures is skipped on the next scan (with a logged/audited skip reason) and is retried again only after the backoff window elapses; existing tests for that provider's happy path still pass unmodified.

6. **Add FMP as a second short-interest source; flag material disagreement.** `shortPercentOfFloat` is currently sourced only from Yahoo (`src/lib/data-providers.ts` line 1499, `YahooFinanceEnrichmentProvider.fetchSymbol` reading `ks.shortPercentOfFloat`). Add an FMP short-interest field to `FmpEnrichmentProvider` (`src/lib/data-providers.ts` line 1739 onward) as a second source in the cascade (first-wins per `takeScalar` semantics still apply for the primary value), and additionally compute/flag when the two sources materially disagree (e.g. >X percentage-point delta) — expose this as a new lightweight field or a warning bulletin rather than silently picking one. Acceptance: with FMP short-interest available, a symbol where Yahoo and FMP disagree beyond the threshold surfaces a disagreement flag/bulletin; `MarketScan.source` correctly includes `fmp` only when FMP's short-interest call actually ran and returned data for at least one symbol this scan.

**Out of scope:**
- Do not touch the Bear-red-team fail-open behavior, RAG eval harness, factor-weight learning loop, fill-history redundant-fetch issue, or any other audit item outside data-source connectivity — those are separate workstreams.
- Do not add new paid API keys/vendors beyond FMP (already integrated) and the existing Yahoo/Robinhood MCP connections.
- Do not change `MarketScan.source` derivation logic beyond what's needed to correctly attribute new providers — it must stay a `+`-joined list of providers that actually ran this scan (never hardcode a name).
- Do not remove or weaken existing tests in `test/data-providers.test.ts`, `test/market.test.ts`, `test/market-custom-symbol.test.ts`, or the `strategy-*.test.ts` files — extend them.
- Do not build a UI surface for these new fields as part of this workstream unless trivially free (dashboard/UI wiring can be a follow-up); focus on getting the data through the enrichment pipeline and into the strategy prompt correctly attributed.

**Process & constraints:** Work in the Agentic Trading repo on a new branch (e.g. `agent/claude` if that's your assigned worktree lane per `AGENTS.md`, otherwise any feature branch). Paper mode is default — NEVER place real trades or flip `paperMode`/`policy.paperMode` while testing; this workstream doesn't need to touch execution at all, only the data/enrichment/prompt layer. New behavior (item 4's options tier, item 5's circuit breaker, and ideally item 6's disagreement flag) must ship behind default-off env flags so existing scans are unaffected until explicitly enabled — follow the existing pattern in `data-providers.ts` (`flagEnabled(process.env.X)`, e.g. `ENRICHMENT_SHORT_CIRCUIT_ENABLED`). Item 1 (earnings) and item 3 (13F) are zero-cost additions to an already-running call and can ship on by default once tested, but confirm they degrade to `undefined`/absent rather than fabricating when the API doesn't return the field (never label real data "mock" or "fallback" per `CLAUDE.md`, and never show a fabricated number in place of missing data). Add/extend unit tests: `test/data-providers.test.ts` for each new field's cascade/attribution behavior and the new circuit-breaker skip logic, `test/market.test.ts`/`test/market-custom-symbol.test.ts` for the bid/ask fix and the `applyEnrichment` merge of new fields, and a `strategy-*.test.ts` (or new file) assertion that `hasAskData`/limit-price computation no longer treats synthetic ask as real. Run the full verify quartet before claiming done: `npm run lint`, `npx tsc --noEmit`, `npm test`, `npm run build`. Follow the handoff protocol in `AGENTS.md`/`CLAUDE.md`: update `STATUS.md`, add a `docs/rollouts/YYYY-MM-DD-<slug>.md` note (summary, why, exact files touched, verification commands run, follow-ups), and update `PLAN.md` if scope shifted. Open a PR as READY for review (not draft) once verification passes, per the repo's PR conventions in `AGENTS.md`.

---

### Chat E — Performance & efficiency
**Repo context:** Agentic Trading only (`/Users/jay/Code/Agentic Trading` on the host; work in your own `agent/claude`-style worktree per `AGENTS.md`/`CLAUDE.md` — or `/workspace` if you're in Cursor Cloud). No changes needed in `congress-trading-shared`, `Congress.Trade`, or `API-usage-monitor`.

**Goal:** Cut redundant DB work and client-bundle weight in the dashboard request path without changing any user-visible numbers or trading behavior. This is a paper-trading research app — every change here must be a pure performance refactor (same outputs, fewer round trips / less shipped JS), never a behavior change.

**Scope — implement these (verified against current code):**

1. **Collapse redundant `listFillEvents` fetch/replay in the dashboard request path.** `getDashboardSnapshot` (`src/lib/dashboard.ts:214`) triggers at least 9 separate `listFillEvents(...)` calls per request, each a 500-row SELECT + `JSON.parse` + O(n) FIFO replay via `calculatePnl`: `getPaperPortfolioProjection` runs twice back-to-back (`dashboard.ts:341` and `:356`, itself calling `listFillEvents` at `performance.ts:265`), `getPerformanceSummary` fetches live+paper fills again (`performance.ts:225-226`), `getThesisScorecard`/`getRegimeScorecard` each refetch+replay (`performance.ts:469`, `:483`), `getTaxSummary` fetches fills a third way plus `getClosedLotsDetailed`/`getOpenLots` (`src/lib/tax.ts:161-163`), and `buildUnifiedFeed` fetches yet again at `dashboard.ts:459`. Fetch live+paper fills once per request in `getDashboardSnapshot`, and thread the parsed `FillEvent[]` arrays through to `getPaperPortfolioProjection`, `getPerformanceSummary`, `getThesisScorecard`, `getRegimeScorecard`, `getTaxSummary`, `getClosedLotsDetailed`, `getOpenLots`, and the `buildUnifiedFeed` call — change these functions to accept optional pre-fetched fills (default to fetching internally when not passed, so other callers like `strategy.ts`, `strategy-tuning.ts`, `robinhood-pnl-crosscheck.ts` keep working unmodified). At minimum, collapse the two identical back-to-back `getPaperPortfolioProjection` calls at `dashboard.ts:341`/`:356` into one. Acceptance: `getDashboardSnapshot` for the common case (`accountNumber` set) results in exactly one `listFillEvents("live", ...)` and one `listFillEvents("paper", ...)` DB call per request (verify via a spy/mock count in a new or existing test), and all existing `test/performance.test.ts` / `test/dashboard-*.test.ts` assertions still pass unchanged (output values must be identical).

2. **Batch proposal point-queries in the audit/unified feed builders.** `buildAuditFeed` (`src/lib/dashboard-feed.ts:73`, proposal lookup at `:84` via `proposalFromPayload`) and `buildUnifiedFeed` (`:567`, lookup at `:612-623`) each call `input.getProposalById(proposalId)` once per audit/fill event — in `getDashboardSnapshot` this is wired directly to `getProposal(proposalId, userId)` (`src/lib/db-proposals.ts:98`, a single-row `SELECT ... WHERE id = ? AND user_id = ?`), so a dashboard with ~100 audit rows and up to 500 fills issues up to ~150 separate prepared-statement round trips. Add a batch lookup — e.g. `getProposalsByIds(ids: string[], userId): Map<string, Proposal>` in `db-proposals.ts` using `WHERE id IN (...)` — and change `dashboard.ts` to collect all distinct `proposalId`s referenced by `audit` + `fills` up front, do one batched query, and pass a `Map`-backed `getProposalById` closure into both `buildAuditFeed` and `buildUnifiedFeed`. Acceptance: one batched `SELECT ... WHERE id IN (...)` query replaces the per-row queries for a given dashboard request; `test/dashboard-feed.test.ts` still passes with identical feed output.

3. **Cap `buildUnifiedFeed` output at the source.** The client already slices to 50 groups (`app/dashboard-client.tsx:3966`, `feed.slice(0, 50)`), but `buildUnifiedFeed` (`src/lib/dashboard-feed.ts:567`) returns every group built from up to 100 audit rows + 500 fills + notifications, uncapped. Note `buildAuditFeed`'s consumer already caps client-side to 100 (`app/dashboard-client.tsx:4272`, `items.slice(0, 100)`) matching its 100-row source (`listAudit(100, ...)` at `dashboard.ts:388`/`:389`) — that one is not a real problem, leave it. Focus only on `buildUnifiedFeed`: sort groups newest-first and cap the returned array at ~60 (slightly above the client's 50, to preserve any client-side re-sorting/filtering headroom) either inside `buildUnifiedFeed` itself or immediately after calling it in `dashboard.ts`. Acceptance: `getDashboardSnapshot(...).unifiedFeed.length` is capped at the new source-level limit; `test/dashboard-feed.test.ts` updated/extended to assert the cap.

4. **Reduce Finnhub's per-symbol REST call volume.** `FinnhubEnrichmentProvider.enrich` (`src/lib/data-providers.ts:1567`, the 5-call fan-out at `:1607-1613`) issues 5 unbatched REST calls per symbol (`company-news`, `quote`, `stock/recommendation`, `stock/profile2`, `stock/metric`) via `Promise.allSettled`, chunked at `CONCURRENCY = 5` (`:237`), gated by `maxSymbols()` (`:304-308`, env-configurable via `FMP_MAX_SYMBOLS`/`MARKET_SCAN_LIMIT`, capped by `MAX_SYMBOLS_CAP`). At the default symbol cap this is up to hundreds of round trips inside the 25s scan timeout. Pick one concrete, low-risk lever (don't do all of them): drop the least-valuable call (e.g. `stock/recommendation` if a cheaper/blended analyst source already covers it elsewhere in the cascade — check `getPerformanceSummary`... i.e. check `CascadingEnrichmentProvider` in the same file to confirm what already backstops it before removing anything), or lower Finnhub's own effective symbol cap independently of the shared `maxSymbols()`, or raise `CONCURRENCY` modestly for this provider only. State clearly in the PR which lever you chose and why. Acceptance: measurably fewer Finnhub HTTP calls per scan (add/adjust a test in `test/data-providers.test.ts` asserting the new call count), with cascade fallback behavior (missing fields degrade to other tiers, never fabricated) unchanged — run the existing Finnhub-related tests in `test/data-providers.test.ts` to confirm no regression.

5. **Code-split `StrategyFlow` and the price-chart route** via `next/dynamic({ ssr: false })`. `app/dashboard-client.tsx:111` statically imports `StrategyFlow` from `./ui/strategy-flow.tsx`, which pulls in `@xyflow/react` (~3.9MB in `node_modules`) plus its CSS; `app/dashboard-client.tsx:116` statically imports `SymbolDrilldown` (`./ui/symbol-drilldown.tsx`), which in turn imports `PriceChart` (`./ui/price-chart.tsx`). Note: `price-chart.tsx` already lazy-loads the `lightweight-charts` JS module at runtime via `await import("lightweight-charts")` inside a `useEffect` (`price-chart.tsx:141`) — that part is already done; the remaining gap is that `StrategyFlow` and the `SymbolDrilldown`/`PriceChart` **component boundary** are still statically bundled into the ~7,000-line `dashboard-client.tsx`, so `@xyflow/react` ships on every first load regardless of which tab is opened. Wrap the `StrategyFlow` import and the `SymbolDrilldown` import in `next/dynamic(() => import(...), { ssr: false })` (add a small loading fallback matching existing skeleton/EmptyState conventions). Acceptance: `npm run build` shows `@xyflow/react` no longer in the initial dashboard route's first-load JS (check `.next` build output / route analysis), the Strategy Flow tab and symbol drilldown still render and function identically in manual verification, and no existing test regresses.

6. **Do NOT implement — track as a follow-up only.** Item: monolithic `snapshot` client state causing whole-tree re-render on every SSE event (`dashboard-client.tsx`, snapshot replaced wholesale on every `load()`, only ~31 `useMemo`s, no `React.memo`/selector split). This is explicitly a larger refactor per the audit (`docs/reviews/2026-06-30-improvement-audit.md` §6.1, "Low / Med / L"). Just note it in the PR description and `STATUS.md`/rollout note as an acknowledged follow-up — do not attempt it in this PR.

7. **Add `better-sqlite3` `cache_size`/`mmap_size` pragmas.** `src/lib/db.ts:21-38` (`getDb()`) currently sets `journal_mode = WAL`, `busy_timeout = 5000`, `synchronous = NORMAL`, `foreign_keys = ON` but no `cache_size` or `mmap_size`. Add `db.pragma("cache_size = -20000")` (20MB page cache, negative = KB) and a reasonable `mmap_size` (e.g. `db.pragma("mmap_size = 268435456")` for 256MB) alongside the existing pragmas, with a one-line comment explaining the choice. Sequence this after item 1 (the redundant-query fix) since that changes the query volume this is meant to help with. Acceptance: pragmas visible via `db.pragma("cache_size")` in a quick manual check or a lightweight test; full test suite still passes (no behavior depends on these).

8. **Add a `.next/cache` restore step to the Playwright CI workflow.** `.github/workflows/e2e.yml` installs Node with `actions/setup-node@v4` (`cache: npm`, only caches `node_modules`) but has no `actions/cache` step for `.next/cache`; `playwright.config.ts:6-7` runs `npm run build && npm run start` as its `webServer` command, so every CI run (including every PR) pays a full cold Next.js build. Add an `actions/cache@v4` step keyed on something like `${{ runner.os }}-nextjs-${{ hashFiles('package-lock.json') }}-${{ hashFiles('src/**', 'app/**') }}` (with a restore-key fallback) caching `.next/cache` before the build step. Acceptance: workflow YAML is valid (`gh workflow view` / a dry run via a test PR), and a second CI run on the same branch shows a faster `next build` step in the Actions log (verify via the Actions UI on the opened PR, or note in the rollout doc that this can only be confirmed after the PR's own CI run since this is the first run establishing the cache).

**Out of scope:**
- Any change to trading logic, sizing, policy, risk gates, or Bear/Red-Team behavior (that's a different workstream from the same audit).
- Wiring the usage-telemetry push client, congress-score-eval, or factor-weight learning loop (separate audit items, not performance).
- The full monolithic-snapshot React state refactor (item 6 above — track only, don't build).
- Changing `MAX_SYMBOLS_CAP`, `CONCURRENCY`, or Finnhub's role in the enrichment cascade beyond the single lever chosen in item 4.
- Any UI/visual redesign — this is a backend/bundle-size workstream; the dashboard must look and behave identically to a user.
- Touching `congress-trading-shared`, `Congress.Trade`, or `API-usage-monitor` repos.

**Process & constraints:**
- Work in the Agentic Trading repo on a dedicated branch (e.g. `agent/claude` per the multi-agent worktree convention in `AGENTS.md`, or any feature branch if you're not in the host's multi-worktree setup — e.g. a single `/workspace` checkout in Cursor Cloud).
- Paper mode is default — **never** place real trades or flip `paperMode: false` while testing.
- Where any change could alter observable numbers (item 1, 2, 4), keep before/after output byte-identical for existing tests; if you must change a function signature (e.g. adding an optional pre-fetched-fills parameter), keep it backward-compatible for all other call sites (`strategy.ts`, `strategy-tuning.ts`, `robinhood-pnl-crosscheck.ts`, `tax.ts` — grep `listFillEvents` usage before touching shared functions).
- Add or extend tests for each item you implement — at minimum: a call-count assertion for item 1 (mock/spy `listFillEvents`), a query-count assertion for item 2, a length-cap assertion for item 3 (`test/dashboard-feed.test.ts`), a Finnhub call-count assertion for item 4 (`test/data-providers.test.ts`), and a manual/build-output check for item 5 (bundle analysis, not necessarily a unit test).
- Run the verify quartet before claiming done, in order: `npm run lint`, `npx tsc --noEmit`, `npm test`, `npm run build`.
- Follow the handoff protocol in `AGENTS.md`/`CLAUDE.md`: update `STATUS.md`, add a `docs/rollouts/YYYY-MM-DD-<slug>.md` note (summary, why, exact files touched, verification commands run, follow-ups — including item 6 as an explicit deferred item), and update `PLAN.md` if scope/approach changed. Update `docs/reviews/2026-06-30-improvement-audit.md` is not required, but if you want to mark these items resolved, do so in your rollout note instead of editing the audit file.
- Open the PR as READY for review (not draft) once the verify quartet is green.

---

### Chat F — UX / information architecture + aesthetics
**Repo context:** Agentic Trading only (`/Users/jay/Code/Agentic Trading`). No changes needed in `congress-trading-shared`, `Congress.Trade`, or `API-usage-monitor`.

**Goal:** Surface the app's core Green/Red (Bull/Bear) decision architecture in the UI instead of burying it in a truncated rationale string, close the audit-log gap on Bear vetoes, give the command palette a discoverable entry point, fix touch-unreachable rationale truncation, correct a stale IA doc, and clean up genuinely drifted aesthetic primitives (unused `.skeleton` CSS, inconsistent icon sizing, ad-hoc blur/shadow scale).

**Scope — implement these (verified against current code):**

1. **Add a first-class `redTeamVerdict` field to `TradeProposal` and render it as its own "Bear Review" block in `DecisionView`.**
   Currently the Bear verdict is appended as plain text onto `proposal.rationale` in `src/lib/strategy.ts` (lines ~457 and ~460, inside the `for (const proposal of sizedProposals)` loop that calls `debateProposal` from `src/lib/red-team.ts`), then rendered with `line-clamp-3` in `app/dashboard-client.tsx:2326` — so the verdict is invisible unless the user reads past a 3-line clamp of the whole rationale.
   - Add `redTeamVerdict?: { rejected: boolean; available: boolean; reason: string }` (mirrors `RedTeamDebateResult` in `src/lib/red-team.ts:12-17`) to the `TradeProposal` interface in `src/lib/types.ts:585-623`.
   - In `strategy.ts`, set `proposal.redTeamVerdict = redTeamResult` instead of (or in addition to, for backward-compat rationale text) appending to `rationale`.
   - In `DecisionView` (`app/dashboard-client.tsx:2241` onward, proposal card starts ~2292), add a distinct "Bear Review" block near the rationale paragraph (~line 2326) that shows the verdict (survived/rejected/unavailable) and reason, styled distinctly from the Bull rationale.
   - Acceptance: a proposal with `redTeamVerdict` set renders a visually separate Bear Review block in the pending-approval card; existing proposals without the field (old data) render unchanged (field is optional).
   - Note: 27 test fixture files under `test/*.ts` construct `TradeProposal` literals (grep `tradeThesisTag`) — since the new field is optional, none should need updates, but scan for any test asserting on `rationale` string content that currently expects the appended "Red Team Debate Survived" / "routed to human approval" text.

2. **Audit Bear rejections.** In `src/lib/strategy.ts` at the `if (redTeamResult.rejected)` branch (~line 451-454), a rejection today only does `console.log` then `continue` — it is never written to the audit log, unlike the sibling `proposal_skipped_negative_ev` (`strategy.ts:431`) and `proposal_skipped_correlation` (`strategy.ts:1106`) patterns which both call `audit(...)`. Add `audit("proposal_rejected_by_red_team", { symbol: proposal.symbol, side: proposal.side, thesisTag: proposal.tradeThesisTag, reason: redTeamResult.reason }, userId)` before the `continue`, following the exact call shape of `audit(kind, payload, userId, connectedAccountId?)` (`src/lib/db.ts:406`).
   - Acceptance: a Bear-rejected proposal produces an `audit_events` row with `kind = "proposal_rejected_by_red_team"` visible in the Activity/Audit feed tab.

3. **Add a persistent command-bar button + visible ⌘K hint.** `app/ui/command-palette.tsx` renders the palette UI itself but has zero trigger affordance in the shell; the only way to open it is the raw `keydown` listener in `app/dashboard-client.tsx:1109-1118` (`(e.metaKey || e.ctrlKey) && e.key === "k"`), with no visible button anywhere. Add a small icon/button (e.g. a `Search`-icon button with a `⌘K` `<kbd>` hint, matching the existing `<kbd>` styling already used inside `command-palette.tsx:99`) to the command bar header in `dashboard-client.tsx` (the `<header>` starting ~line 1619, "Sub-container 2: Action buttons" ~line 1716 is a natural spot) that calls the same `setCmdOpen` setter the listener uses.
   - Acceptance: a visible button in the header opens the command palette on click, in addition to the existing ⌘K/Ctrl-K shortcut; button includes an accessible label and shows the `⌘K` hint.

4. **Update `docs/phase-8-cockpit-ui.md` to match the real IA.** The doc's "User-Facing Tabs" section (lines 63-67) states "Center workspace tabs: `Decision`, `Market Scan`, `Performance`, `Strategy`" (4) and "Bottom drawer tabs: `Activity`, `Runs`, `Notifications`" (3). The actual code has 7 workspace tabs — `dashboard-client.tsx` ~line 1665-1673: `decision`, `assistant`, `market`, `macro`, `performance`, `tax`, `strategy` — and 4 feed tabs — `dashboard-client.tsx:161`: `FeedTab = "activity" | "runs" | "notifications" | "audit"`. Update the doc's tab lists (and any other counts/references in that file that assume the old 4/3 split) to reflect all 7 workspace tabs and all 4 feed tabs.
   - Acceptance: `docs/phase-8-cockpit-ui.md` lists exactly the 7 workspace tabs and 4 feed tabs that exist in code, with no other numeric-tab-count claims left stale.

5. **Demote Tax/Macro to an overflow to reduce the single-screen tab count.** The `Tabs` component call in `dashboard-client.tsx` (~lines 1665-1673) renders all 7 workspace tabs (`decision`, `assistant`, `market`, `macro`, `performance`, `tax`, `strategy`) inline. Move `macro` and `tax` behind an overflow affordance (e.g. a "More" dropdown/menu appended to the `Tabs` row, or folded into the existing `AccountMenu`/settings surface) so the primary tab row shows 5 tabs, while `macro`/`tax` remain reachable (including deep-linkable via the same `workspaceTab` state and `WORKSPACE_TAB_KEY`-style persistence already used for tab memory — check for an analogous stored-tab pattern near `readStoredFeedTab`/`FEED_TAB_KEY` at `dashboard-client.tsx:198,287` and mirror it for workspace tabs if one doesn't already exist).
   - Acceptance: default tab row shows 5 tabs; Macro and Tax are reachable via an overflow control in 1 extra click; existing tab content/behavior for Macro and Tax is unchanged; keyboard/`role="tablist"` accessibility (see `phase-8-cockpit-ui.md:147`) is preserved for the overflow items too.

6. **Add click/tap-to-expand for truncated rationale.** `app/dashboard-client.tsx:2326` uses `line-clamp-3` with a `title={p.proposal.rationale}` tooltip as the only way to see the full text — unreachable on touch devices (no hover). Add a click/tap handler (e.g. local `useState` toggling the clamp class, or an inline "Show more" affordance) so tapping the truncated rationale expands it in place. Also check the other rationale render sites for the same file (`:2480` uses `break-words` with no clamp — no change needed there; `:3848` renders unclamped — no change needed) and only fix the clamped one at `:2326`.
   - Acceptance: on a touch/no-hover viewport, tapping a clamped rationale expands it to full text (and can be collapsed again); desktop hover `title` tooltip behavior can remain as a secondary affordance.

7. **Swap ad-hoc empty/loading states for existing primitives where duplicated.** `EmptyState` (`app/ui/primitives.tsx:387`) is already used 31 times in `dashboard-client.tsx` — it is NOT unused; audit claim is **partly wrong**, correct it in scope. However, `.skeleton` (defined `app/globals.css:201-233`, includes a `prefers-reduced-motion` variant at `:233`) has **zero usages** anywhere under `app/` — confirmed genuinely dead. Separately, several plain-text "no data" states bypass `EmptyState` entirely, e.g. `dashboard-client.tsx:2137` ("No open positions."), `:3400` ("No symbols are currently locked out."), `:3428` ("No harvestable losses right now."), `:3725`, `:4204`, `:4752` — these render bare `<p>`/`<span>` text instead of the `EmptyState` component. Convert the small, clearly-empty-state cases (2137, 3400, 3428 at minimum) to `<EmptyState>`, and add at least one real loading-skeleton use of the `.skeleton` class (e.g. a spot currently doing an ad-hoc "Loading…" text render, such as `:5418`, `:5448`, `:5460`) so the primitive isn't dead code.
   - Acceptance: the 3+ converted empty states render via `<EmptyState>`; at least 1 loading state uses `.skeleton`; no visual regression in already-correct `EmptyState` usages elsewhere.

8. **Define named elevation/blur/spacing tiers.** Confirmed drift: `app/globals.css:30-31,55-56` defines only 2 shadow tiers (`--shadow`, `--shadow-lg`) for light/dark, while `backdrop-blur` usage across `app/*.tsx`/`app/ui/*.tsx` has 7 distinct values in the wild (`backdrop-blur-sm`, `-md`, `-lg`, `-xl`, `-2xl`, plus arbitrary `-[2px]` and `-[3px]` that bypass the Tailwind scale). Define a small named tier system (e.g. 3 elevation levels — surface/raised/overlay — each pairing a specific shadow + blur value) as CSS custom properties or Tailwind theme extensions, and migrate the two arbitrary bracket values (`backdrop-blur-[2px]`, `backdrop-blur-[3px]`) onto the nearest named tier.
   - Acceptance: no more arbitrary bracket blur values (`grep -n 'blur-\['` returns nothing in `app/`); a documented 3-tier elevation scale exists and is used by at least the components that previously used the arbitrary values.

9. **Collapse icon sizing to a 3-step scale.** Confirmed: `grep -ohE 'size=\{?[0-9]+\}?' app/*.tsx app/ui/*.tsx` shows 12 distinct lucide icon `size=` values in use (10, 11, 12, 13, 14, 15, 16, 17, 18, 20; the one `size={1}` hit at `app/ui/strategy-flow.tsx:364` is a react-flow `Background` dot prop, not an icon, and is out of scope). Define 3 named sizes (e.g. sm=14, md=16, lg=20) and migrate the long tail (11/12/13/15/17/18) onto the nearest of the 3, leaving no more than 3 distinct icon sizes across `app/`.
   - Acceptance: `grep -ohE 'size=\{?[0-9]+\}?' app/*.tsx app/ui/*.tsx` (excluding `strategy-flow.tsx`'s `Background` prop) shows at most 3 distinct values.

10. **Add `docs/design/visual-system.md`.** A `docs/design/` directory already exists (currently holding `per-user-broker-token.md`), so this is a new file in an existing directory, not a new directory. Document the color/spacing/elevation/blur/icon-size tokens (including the new tiers from items 8-9), and note the existing WCAG-AA contrast verification referenced in the audit (dimension 6.6, "Aesthetic appeal") if you can locate its source of truth in-repo.
    - Acceptance: `docs/design/visual-system.md` exists and documents the token names/values introduced or consolidated in items 8-9, plus icon-size and elevation scales, in enough detail that a future PR can cite it instead of re-deriving values.

**Out of scope:** Do not touch the Bear LLM's fail-open/fail-closed behavior, prompt content, or `debateProposal` logic itself in `src/lib/red-team.ts` (that's a separate LLM-prompting workstream item, audit item #1 in the Top-10). Do not implement eval coverage/versioning for Bull/Bear prompts. Do not refactor the 7,015-line `dashboard-client.tsx` into smaller components (flagged separately as a large L-effort strategic bet) beyond what's needed for the tab-overflow change in item 5. Do not touch RAG, learning-loop, performance/dashboard-query, or cross-app (Congress.Trade / API-usage-monitor) code — none of that is part of this workstream. Do not change `redTeamProvider()`/model selection in `red-team.ts`.

**Process & constraints:** Work in the Agentic Trading repo (`/Users/jay/Code/Agentic Trading`) on a new branch off `main`. Paper mode is default — never place real trades or flip `paperMode`; this is a UI/docs workstream and shouldn't touch execution paths at all, but if any change is adjacent to the strategy money path (items 1-2 touch `strategy.ts`), double-check no behavioral change to order placement, only to what's recorded/displayed. Keep any behavior change that could alter existing UI expectations (e.g. tab overflow in item 5) straightforward — no new flags are strictly required since this is pure UI/IA/docs work, but if in doubt, default new UI behind an easily-revertible small commit rather than a policy flag. Run the verify quartet before claiming done, in order: `npm run lint`, `npx tsc --noEmit`, `npm test`, `npm run build`. Follow the handoff protocol in `AGENTS.md`/`CLAUDE.md`: update `STATUS.md`, add a `docs/rollouts/YYYY-MM-DD-<slug>.md` note (summary/why/files/verification/follow-ups), and update `PLAN.md` if scope shifts. Open the PR as READY for review (not draft) once `verify` CI is expected to pass. Workstream-specific testing: add/extend a test for the new `redTeamVerdict` field flowing from `strategy.ts` into the persisted proposal (check existing strategy tests for the Red Team debate path to extend rather than duplicate), and add a test asserting the new `audit("proposal_rejected_by_red_team", ...)` call fires on Bear rejection (mirror the existing `proposal_skipped_negative_ev`/`proposal_skipped_correlation` audit tests if they exist — grep `test/*.ts` for those audit kinds first).

---

### Chat G — Security hardening, risk-control wiring audit, money-path testing & cost/ops controls

**Repo context:** Agentic Trading only (`/Users/jay/Code/Agentic Trading`). No cross-repo work — nothing here touches `congress-trading-shared`, `Congress.Trade`, or `API-usage-monitor`. Note the app router lives at repo-root `app/` (NOT `src/app/`); library code is under `src/lib/`.

**Goal:** Close the residual (High→Low, not P0 — the auth IDOR is already fixed and defended in depth per §11/§A of `docs/reviews/2026-06-30-improvement-audit.md`) security gaps, verify the already-built risk kill-switches are correctly wired and durable, add the first end-to-end "money-path" test plus a live-mode pre-flight assertion, and add a hard per-user/day cost ceiling and a query-embedding cache. Several audit items are already partly or fully implemented — verify and only fill the real gaps.

**Scope — implement these (verified against current code):**

1. **Rate-limit `/api/chat` and `/api/scan`.** Both handlers currently call `resolveRequestUserId` but never `enforceRateLimit`. The limiter already exists (`src/lib/rate-limit.ts`, `enforceRateLimit(userId, route, options)`) and is used by order/OAuth/mobile routes. Add a new entry to the `RATE_LIMITS` map (e.g. `chat`, `scan`) and call `enforceRateLimit` at the top of `POST` in `app/api/chat/route.ts` (after `resolveRequestUserId`, before the LLM gate) and `GET` in `app/api/scan/route.ts`. *Acceptance:* a per-user burst past the configured limit returns HTTP 429 with `Retry-After`; a new test asserts this for both routes.

2. **Encrypt Robinhood OAuth tokens at rest.** `setMcpOAuthTokens`/`getStoredMcpOAuthTokens` in `src/lib/mcp-oauth.ts` (lines ~213/234) persist `McpOAuthTokens` as plaintext JSON via `setInternalSetting`/`getInternalSetting`. AES-256-GCM helpers already exist but are **module-private** in `src/lib/db-api-keys.ts` (`encryptValue`/`decryptValue`, using `ENCRYPTION_KEY`). Export them (or add a thin shared `encryptJson`/`decryptJson` in a small helper reused by both call sites) and route the token blob's secret fields (`accessToken`, `refreshToken`) through encrypt-on-write / decrypt-on-read. Preserve the legacy-plaintext fallback that `decryptValue` already has (3-part `iv:tag:ct` check) so existing stored tokens still load. *Acceptance:* a round-trip test stores tokens, asserts the raw settings row is not plaintext, and `getStoredMcpOAuthTokens` returns the original values; legacy plaintext rows still decrypt.

3. **Constant-time admin-token compare + reconsider `allowNonProd`.** Two token comparisons use `===`: `app/api/admin/reindex-10k/route.ts:13` (local `authorized()` helper — the sibling `app/api/admin/reindex-8k/route.ts` likely mirrors it, check it) and `src/lib/auth/admin.ts:63` (`checkAdmin`). Replace with `crypto.timingSafeEqual` over equal-length buffers (guard against length-leak / undefined-env). Also in `checkAdmin`, the `allowNonProd` default is `true` (`src/lib/auth/admin.ts:56,68`) — keep the default but document the risk and add a test that a production-configured admin route denies when neither the email allowlist nor the token match. Prefer migrating the two `reindex-*` routes to use the shared `requireAdmin`/`checkAdmin` gate rather than their own `authorized()` copies. *Acceptance:* token comparison is timing-safe; a test covers match/mismatch/empty-env for both the shared gate and the reindex routes.

4. **Add security response headers.** No CSP / X-Frame-Options / Referrer-Policy exists anywhere today (grep of `middleware.ts`, `next.config.*`, `src`, `app` returns nothing). Add `Content-Security-Policy` (start report-only or a conservative policy behind a default-off env flag so it can't break the dashboard), `X-Frame-Options: DENY`, and `Referrer-Policy: strict-origin-when-cross-origin` — set them in `middleware.ts` on the returned `NextResponse` (the file already builds responses via `NextResponse.next({ request: { headers } })` and returns 401/403 responses) OR via `headers()` in `next.config.ts`, whichever cleanly covers pages + API. *Acceptance:* a response from a normal page/route carries the three headers; CSP defaults to report-only/off-by-default so no console-blocked resources.

5. **Verify (do NOT rebuild) the drawdown kill-switch wiring + restart durability.** This is largely DONE — `src/lib/strategy.ts:244-263` already calls `recordAndEvaluateDrawdownBreaker` on the autonomous path (`!manualRun && policy.systemState === "active"`), flips to `close_only`, persists via `setPolicy`, audits `policy_violation_drawdown`, and sends a `kill_switch` notification; the HWM/start-of-day equity persist across restart in the settings KV (`src/lib/risk-breaker.ts:65-93`). Confirm: (a) the breaker still records/updates HWM even on non-active runs if the audit wants a continuous peak (currently the whole block is gated on `active` — decide if HWM should track regardless, and if you change it, keep it default-safe), and (b) there is a test covering breach→`close_only`. Add only the missing test coverage; report if fully covered. *Acceptance:* a documented finding ("already wired / durable") plus any added regression test — no behavior change unless a real gap is found.

6. **Verify the correlation cluster gate enable path.** Also largely DONE — `applyCorrelationClusterGate` (`src/lib/strategy.ts:1087`) is invoked in the run flow at `src/lib/strategy.ts:509`, keyed on `policy.maxAvgCorrelation` (default off; type at `src/lib/types.ts:491`; validated 0–1 at `app/api/policy/route.ts:125`; UI control at `app/dashboard-client.tsx:3630`). Confirm the gate actually runs before execution and that setting `maxAvgCorrelation` drops an over-correlated opening buy/short (audit `proposal_skipped_correlation`). Add a test if none exists. *Acceptance:* a test proves an opening proposal above the cap is dropped and exits always pass; report "built + wired, opt-in" status.

7. **One end-to-end money-path test + a live-mode pre-flight assertion.** Today the only e2e artifact is a Playwright smoke test (`test/e2e/dashboard-smoke.spec.ts`); `runStrategyOnce` is only touched by `test/persistence-notification.test.ts`. Add ONE vitest test (temp SQLite per the CLAUDE.md convention, `DATABASE_URL=file:<tmpdir>/...`) that drives `runStrategyOnce` in Test/paper mode with a mocked LLM (`MockLLM` from `src/lib/chat/llm.ts`) and a mock/simulated broker, asserting the full proposal→evaluate→execute path books a fill and persists a proposal + fill_event. Separately, add a **pre-flight assertion** helper (unit-tested) that refuses to proceed on the real-broker path unless paper-mode/live-mode invariants hold (e.g. assert `paperMode` unless an explicit live flag is set) — a guard, default-safe. *Acceptance:* the money-path test is green in Test mode; the pre-flight assertion has a unit test proving it blocks a misconfigured live run. NEVER place a real trade or flip `paperMode:false`.

8. **Hard per-user/day token-budget ceiling + query-embedding LRU cache.** (a) `src/lib/triggers.ts:9-10` explicitly defers the "$/token budget ceiling"; a usage ledger already exists (`src/lib/llm-usage.ts` — `recordLlmUsage`, `getLlmUsageSummary` sums `total_tokens`/`cost_usd`, and `estimateLlmCostUsd`). Add a hard ceiling checked at the strategy/trigger entry: before firing `runStrategyOnce` from the trigger/interval path (and optionally at the top of `runStrategyOnce`), sum today's usage for the user via the ledger and skip+audit when over a configurable per-user/day token (or $) limit; default the limit off/very high so existing behavior is unchanged. (b) Add a small in-process LRU cache for **query** embeddings around the single-query embed call in `src/lib/vector-db.ts` (~line 625, `embedWithRetry(voyage, [query], "query")` inside `retrieveContextDetailed`) keyed by normalized query text — do NOT cache document/upsert embeddings. *Acceptance:* over-budget users are skipped with an audit event and the ceiling is default-off; repeated identical retrieval queries hit the cache (unit test with an injected/counting embed fn).

9. **Ops: confirm Litestream restore was actually tested + keep account-deletion table list in sync.** (a) Confirm/record whether a Litestream **restore** (not just replicate) has been exercised — check `docs/litestream.md`, `docs/deployment.md`, `docs/ops-observability-security.md`, and `scripts/`; if untested, add a documented restore-verification step/runbook note (no infra change required in this PR — a script or doc is fine). (b) `src/lib/account-deletion.ts:8-31` hardcodes `DELETE_TABLES_BY_USER_ID`; verify it covers every user-scoped table created by the `db-*.ts` modules and `db.ts` `migrate()`. Add a test that cross-checks the schema's user-scoped tables against this list so a future new `db-*` table can't silently escape deletion. *Acceptance:* a test fails if a user-scoped table is missing from the deletion list; Litestream restore status is documented.

10. **Observability: stamp prompt version + Bear-veto/diversity-collapse events as Langfuse observations.** `withLlmGeneration` (`src/lib/observability.ts:74`) already traces bull/bear/red-team/tuning/revalidation generations with `metadata` and `tags` (e.g. `src/lib/strategy.ts:2139` bull, `:2357` bear; `src/lib/red-team.ts`). Add a `promptVersion` field to the `metadata` of those generation calls (source it from a single constant so it's bumpable), and emit the Bear-veto / diversity-collapse decision points as observations (either as generation `metadata`/`tags` on the bear call, or a lightweight span) so they're queryable in Langfuse. Keep it a no-op when Langfuse isn't configured (`langfuseConfigured()` already gates startup). *Acceptance:* traced generations carry a `promptVersion`; a Bear-veto and a diversity-collapse both appear as a stamped observation/tag; no runtime change when Langfuse env is unset.

**Out of scope:**
- Item 11 from the audit (splitting the ~2,975-line `src/lib/strategy.ts` into proposal-generation/execution/reconciliation/learning modules) — this is a large refactor; leave it as a follow-up PR and note it in PLAN.md, do NOT attempt it here.
- Any change to `paperMode` defaults, real-trade placement, or the already-fixed auth/IDOR layer (`middleware.ts`, `strip-identity`, CSRF) beyond adding the new response headers in item 4.
- Cross-repo work.

**Process & constraints:**
- Work in the Agentic Trading repo on your own agent branch (do NOT edit in the `main` integration worktree; land via `bash scripts/land.sh`, never push to `main`). `git status && git log -3` first — there is an uncommitted `next-env.d.ts` change and a pending rollout note in the tree.
- Paper mode is the default — NEVER place a real trade or flip `paperMode:false`, even in tests.
- Every behavior change (CSP, rate limits, token-budget ceiling, pre-flight guard) must sit behind a default-off / conservative-default flag so nothing breaks the running dashboard.
- Confirm `git config user.email` resolves to `12656028+jaywedgeworth22@users.noreply.github.com` before committing.
- New tests to add (all vitest, temp SQLite per run): chat/scan 429, OAuth-token encryption round-trip + legacy fallback, admin timing-safe compare (match/mismatch/empty), the drawdown-breach and correlation-cap regressions if not already covered, the end-to-end money-path run, the live-mode pre-flight guard, the token-budget skip, the query-embedding cache-hit, and the account-deletion table-coverage cross-check.
- Run the verify quartet before claiming done, in order: `npx tsc --noEmit`, `npm run lint`, `npm test`, `npm run build`. (Ignore the known pre-existing `test/alternative-data.test.ts` `mockFetcher`/`URL | RequestInfo` type error unless you touch that file. `npm run build` wipes `.next/`; restart your PM2 preview after.)
- Follow the AGENTS.md/CLAUDE.md handoff protocol before committing: update `STATUS.md`, add a `docs/rollouts/YYYY-MM-DD-<slug>.md` note (summary/why/files/verification/follow-ups — including the Litestream restore status and the deferred strategy.ts split), and reflect scope in `PLAN.md`; reference the touched docs in the commit message.
- Open the PR as READY for review (not a draft) and merge with `gh pr merge <n> --squash --auto` once the required `verify` check is green.

---

### Cross-repo C1 — Congress.Trade integration repair

**Repo context:** Needs **all three** cross-app repos connected in the chat: **Agentic Trading** = App B (`/Users/jay/Code/Agentic Trading`, the consumer/publisher, Next.js 16 + better-sqlite3), **App A** = `/Users/jay/Code/Congress.Trade` (the Cloudflare Workers/Hono producer that serves `/api/client/v1`), and the **shared package** `/Users/jay/Code/congress-trading-shared` (`@jaywedgeworth22/congress-trading-shared`, currently at `1.1.0`, published to **GitHub Packages** — `publishConfig.registry = https://npm.pkg.github.com`). You will primarily land code in App B and the shared package; App A changes (if you touch its contract) go on their own branch/PR in that repo. Note: `src/lib/congress-shared-aliases.ts` referenced in older notes **no longer exists** in App B (confirmed removed; no code references it) — do not try to edit it.

**Goal:** Repair the drifted Congress.Trade side-channel so its push/real-time half is either actually functional or honestly documented as inert, so no dataset is silently dropped, so the two apps can't quietly diverge on the shared contract, and so ticker aliasing and outbound payload validation are actually enforced. Every behavior change stays behind the existing default-OFF env gates.

**Scope — implement these (verified against current code):**

1. **Resolve the push/SSE + webhook contract drift — pick ONE contract and make both apps honor it.** Today App A's SSE (`Congress.Trade/app/src/delivery/sse.ts:241`) emits `id:<seq>\nevent: trade.new\ndata:<raw Transaction JSON>` and its REST stream (`.../delivery/rest.ts:326-328`) **requires** `?subscription=<id>&token=<secret>`; App B's consumer (`Agentic Trading/src/lib/congress-stream.ts`, `DEFAULT_PATH="/api/stream"`) connects with only a bearer `authorization` header (no `?subscription=`) and parses each message as a `CONGRESS_EVENT_TYPES`-typed JSON envelope in `congress-trade-events.ts` (`applyCongressEvent`). App B's webhook receiver (`congress-webhook-auth.ts`) verifies a single `CONGRESS_WEBHOOK_SECRET` bearer, which does not match App A's per-subscription-secret model. Decide the target contract (recommended: **adapt App B to App A's live subscription model** — create/reuse a subscription, connect with `?subscription=&token=`, and map App A's `trade.new` raw `Transaction` into a `congress.trade` envelope before `applyCongressEvent`; the alternative is adding an envelope-emitting SSE mode to App A). Whichever you pick, both sides must agree. *Acceptance: a documented, mutually-honored contract exists; with the gate on, App B ingests at least one live/synthetic App A event end-to-end (test-covered with a stubbed SSE stream), and until the peer side lands, the inert state is documented in `docs/congress-trade-consume.md` + STATUS.md.*

2. **Stop dropping 4 of 7 outbound datasets.** App B outbound (`src/lib/congress-share.ts`) builds a `SharePayload` with up to 7 dataset arrays (refs, prices, spx, insider, shortVolume, fundamentals, analyst — per `SharePayloadSchema` in the shared pkg), but the receiver side persists only **3**: `src/lib/db-securities-import.ts` (`importSecuritiesBundle`) coerces only `refs`/`prices`/`spx` into `imported_securities_ref`/`imported_price_eod`/`imported_spx_eod`, and App A's import route mirrors that. Pick one: **(a)** trim App B's outbound builder to only emit the 3 persisted datasets (simplest, honest), or **(b)** add `imported_fundamentals`/`imported_analyst` (+ insider/short-volume) tables — the `CREATE TABLE` in `db.ts`'s `migrate()`, CRUD in `db-securities-import.ts`, and confirm App A actually serves/persists them. **Verify first** that App B already *reads* fundamentals/analyst from App A via REST (`getAppAFundamentals`/`getAppAAnalyst` in `congress-trade-client.ts` calling `MARKET_FUNDAMENTALS`/`MARKET_ANALYST`) but has **no insider/short-volume readers** — so option (b) only closes a real gap where the data is round-trippable. *Acceptance: no dataset is silently discarded — either outbound matches what's persisted, or the new tables persist every transmitted dataset; a test asserts round-trip counts match.*

3. **Pin both consumers to the same shared-package tag + make the CI drift check actually catch it.** App B pins `@jaywedgeworth22/congress-trading-shared: "^1.0.0"` (`Agentic Trading/package.json`) while the shared package is at **`1.1.0`** — so B's range doesn't pull the current minor. App A (`Congress.Trade/app`) imports the shared package (`MAX_REFS_BATCH` in `delivery/rest.ts:22`, enrichment types) but **verify whether it has an explicit dependency line** in `Congress.Trade/package.json` / `app/package.json` (grep found none) and add/pin it. **Partly landed:** `shared-package-pin-check.yml` exists in **both** repos (PR #280) — but read it: it only compares **git-SHA pins** against `congress-trading-shared@main` and explicitly **no-ops on semver-range pins** and when `GH_PACKAGES_TOKEN` is unset. So it does **not** currently catch the `^1.0.0`-vs-`1.1.0` drift. Extend it (or pin both repos to an exact version/tag) so a version skew between the two consumers is actually flagged. *Acceptance: both repos reference the same shared-package version, and the CI check fails (or warns loudly) when the two pins diverge — demonstrated by a dry-run.*

4. **Apply the shared ticker-alias map on both ends.** Neither app uses the shared `resolveTickerAlias`/`TICKER_ALIASES` (`congress-trading-shared/src/utils.ts:17`, `src/constants.ts:7`). App B normalizes with `normalizeSymbol` (trim+upper only, from `src/lib/money.ts`) in `congress-share.ts` outbound mappers (lines ~188, 213, 238, 284, 300, 536, 540, 555, 641); App A keeps its **own** copy `TICKER_ALIASES` in `Congress.Trade/app/src/extraction/tickerNormalize.ts` (used by `export/pitScores.ts`). Apply `resolveTickerAlias` after `normalizeSymbol` in App B's outbound mappers, and switch App A to import the shared `TICKER_ALIASES` (retiring its local copy, or re-exporting the shared one) so corporate-action renames don't fragment rows. *Acceptance: a known alias (e.g. from the shared map) resolves to its canonical ticker in App B outbound payloads; App A no longer maintains a divergent local alias table; unit test covers one alias mapping.*

5. **Treat outbound `SharePayloadSchema` validation failure as drop-the-row, not a warning.** `src/lib/congress-share.ts:379-382` runs `SharePayloadSchema.safeParse(payload)` and, on failure, only `console.warn`s the field errors then sends the payload anyway. Change this to drop/omit the invalid rows (prefer using the shared `SecurityRefInput`/typed inputs so invalid entries are filtered before POST), so malformed data never reaches App A's import endpoint (`API_PATHS.ADMIN_SECURITIES_IMPORT`). *Acceptance: an intentionally-invalid row is excluded from the transmitted payload (test asserts it's dropped and the valid rows still send), and no invalid payload is POSTed.*

**Out of scope:** Do not build new App A analytics endpoints or App B analytics consumers; do not add the unused `MARKET_INSIDER`/`MARKET_SHORT_VOLUME` readers on App B unless item 2 option (b) requires them; do not refactor the dashboard, the scan pipeline, or unrelated `db-*` modules; do not touch the API-Usage-Monitor integration; do not remove or weaken any of the ~8 default-OFF env gates. Keep the PR to the Congress.Trade integration seam only.

**Process & constraints:** Work in the **Agentic Trading** repo on a feature branch (App A / shared-package changes each go on their own branch + PR in their respective repos — this is a cross-repo change, so expect up to three coordinated PRs). **Paper mode is the default — NEVER place real trades or flip `paperMode: false`.** All behavior changes stay behind the existing default-OFF gates (`CONGRESS_STREAM_ENABLED`, the outbound/share gates, etc.) — flipping a gate must be an explicit operator action, never the default. **Shared-contract sequencing (critical):** because the shared package now publishes to GitHub Packages with semver, any change to shared types/schemas/constants/utils requires **bump + publish the shared package first, then bump both consumers' pins** — don't edit shared source and expect consumers to pick it up without a publish. Before claiming done, run the verify quartet in App B: `npm run lint`, `npx tsc --noEmit`, `npm test`, `npm run build` (and the equivalent typecheck/tests in App A and the shared package if you touched them). Add tests: an SSE-envelope-mapping/ingest test for item 1, a payload round-trip count test for item 2, an alias-resolution test for item 4, and a drop-invalid-row test for item 5. Follow the handoff protocol in `AGENTS.md`/`CLAUDE.md`: update `STATUS.md`, add a `docs/rollouts/YYYY-MM-DD-congress-integration-repair.md` note (summary/why/files/verification/follow-ups), update `PLAN.md`, and refresh the relevant `docs/congress-trade-*.md` design docs. Open the PR(s) as **READY for review (not draft)**. Confirm `git config user.email` resolves to the noreply address before committing.

---

### Cross-repo C2 — API Usage Monitor integration
**Repo context:** NEEDS ALL FOUR repos connected in this chat: `/Users/jay/Code/Agentic Trading` (App B, the trading app — primary edit target), `/Users/jay/Code/congress-trading-shared` (the shared `@jaywedgeworth22/congress-trading-shared` package — source of the push client), and `/Users/jay/Code/API-usage-monitor` (the monitor — read-only contract reference; only touch it if you extend the budget-status endpoint in item 4). `Congress.Trade` is not needed for this workstream. **Verify all repos are readable before starting** (`ls "/Users/jay/Code/Agentic Trading" /Users/jay/Code/congress-trading-shared /Users/jay/Code/API-usage-monitor`); if any is missing, stop and ask to add it.

**Goal:** Wire App B's existing usage ledgers (`recordLlmUsage`, `recordRagUsage`) and its market-data/broker call paths to push real usage/cost telemetry into the API Usage Monitor via the already-built-but-unused shared client, so the monitor can see providers it cannot poll (Anthropic, Voyage, Robinhood). Then add a cost-aware feedback loop that reads budget status from the monitor and, when over budget, forces cheaper models / skips a cycle — starting with push-only budget alerts into App B's existing notification pipe. Everything new must be behind default-off env flags so paper-mode behavior is unchanged when the monitor isn't configured.

**Scope — implement these (verified against current code):**

1. **Wire the push client into `recordLlmUsage()` and `recordRagUsage()` behind env flags.** The shared push client `createUsageTelemetryClient` already exists at `congress-trading-shared/src/usageTelemetry.ts` (exported via `src/index.ts`) and is schema-validated against `UsageTelemetryEventSchema` / `UsageTelemetryBatchSchema` — but has **ZERO callers** (verified: `grep -rn "usageTelemetry\|createUsageTelemetryClient\|USAGE_MONITOR\|USAGE_INGEST" src/` in App B returns nothing). Add a thin App-B forwarder (e.g. new `src/lib/usage-monitor-push.ts`) that lazily constructs the client from `USAGE_MONITOR_BASE_URL` + `USAGE_INGEST_TOKEN` (no-op when either is unset), and call it from `recordLlmUsage` (`src/lib/llm-usage.ts:105`) and `recordRagUsage` (`src/lib/rag-metering.ts:79`). Must be **fire-and-forget + wrapped in try/catch** (both ledger functions already promise "never throws / never break the caller" — preserve that). Map fields to the monitor contract: `sourceApp` (e.g. `"agentic-trading"`), `environment` (from `NODE_ENV`/an env label), `provider` (`entry.provider`), `keyRef` (`entry.keyRef` for LLM; RAG has none today — label by `provider`+`operation`), `metricType:"cost"` when a cost is known else `"usage"`, `costUsd` (from `estimateLlmCostUsd` / RAG's `estimateRagCost`), `quantity`/`unit:"token"`, `confidence:"estimated"`, and `occurredAt` (ISO) so the shared client's deterministic idempotency key applies. **Verify** the payload against BOTH the ingest route (`API-usage-monitor/src/app/api/ingest/usage/route.ts` — Bearer token or `x-usage-ingest-token`, POSTs to `/api/ingest/usage`, `createMany` into `ExternalUsageEvent`) AND the prisma model (`API-usage-monitor/prisma/schema.prisma` `ExternalUsageEvent`: `sourceApp, environment?, provider, service?, label?, keyRef?, billingMode, metricType, quantity?, unit?, costUsd?, requests?, credits?, limit?, limitWindow?, tier?, confidence, windowStart?, windowEnd?, occurredAt, metadata?`). _Acceptance:_ with the two env vars set, an LLM and a RAG call each result in exactly one accepted `ExternalUsageEvent` (schema-valid, idempotency-keyed); with vars unset, zero network calls and identical prior behavior — proven by a new unit test injecting `fetchImpl`.

2. **Add market-data/broker call-volume telemetry via the same push path.** App B already has a central HTTP wrapper `fetchWithRetry` in `src/lib/data-providers.ts` (~line 311) that takes a `service` label and already feeds `logApiHealth(...)`, and broker selection routes through `src/lib/broker.ts` (`getBrokerGateway` → robinhood/alpaca/test gateways). Emit a `metricType:"usage"`, `unit:"request"`, `requests:1` event per external market-data/broker call (finnhub, yahoo, fmp, robinhood-quotes, alpaca, etc.), keyed off the existing `service`/provider label — reuse the same forwarder from item 1; do NOT add a second client. Keep it fire-and-forget and behind the same flags. _Acceptance:_ a market scan run with the monitor configured produces per-provider `request`-unit events tagged with the provider name; unconfigured = no-op, and `data-providers` real-data behavior (never labeled mock/fallback) is untouched.

3. **Make push-from-app the primary channel for Anthropic / Voyage / Robinhood.** Verified the monitor's poll adapters are blind to these: `adapters/anthropic.ts` ("billing/usage API is behind the Console and not fully public"), `adapters/voyage.ts` ("Voyage AI does not expose remaining credits via API"), `adapters/robinhood.ts` (`emptyResult`, "does not offer a public retail API"). No monitor code change required for this item — it is satisfied by items 1–2 correctly setting `provider` to `anthropic`/`voyage`/`robinhood` so those events land in `ExternalUsageEvent`. Keep the existing poll adapters as-is for providers where polling works (openai, alpaca, finnhub, etc.). _Acceptance:_ document (in the rollout note) which providers are now push-covered vs poll-covered, and confirm via the same test in item 1 that anthropic/voyage/robinhood events serialize correctly.

4. **Cost-aware feedback loop (start push-only alerts).** Add a budget-status **read** endpoint on the monitor (e.g. `GET /api/budget-status` returning per-provider over/under-budget derived from `ProviderPlan` + latest `UsageSnapshot`/`ExternalUsageEvent` — the alert logic already exists in `API-usage-monitor/src/lib/provider-alerts.ts`, reuse `evaluateProvider`-style output; gate reads behind a token). In App B, add a small client that polls it behind `USAGE_MONITOR_BASE_URL`. **Phase 1 (this PR):** on `budget_exceeded`/`budget_warning`, fire an alert through App B's existing notification pipe (`src/lib/notify.ts` `notify(...)` + `src/lib/db-notifications.ts` `insertNotificationEvent`) — do NOT yet change trading/model behavior. **Phase 2 (wire but default-OFF):** behind a separate default-off flag (e.g. `USAGE_BUDGET_ENFORCE=false`), have the strategy loop consult budget status at `resolveLlmEndpoint` (`src/lib/llm-provider.ts`, called from `src/lib/strategy.ts:1829`) to force a cheaper model tier or skip a cycle when over budget. _Acceptance:_ with the flag off (default), no behavior change and only notifications fire; with it on in a test, an over-budget status downgrades the resolved model or short-circuits the run — covered by a unit test with a stubbed budget response. **Flag this as the largest item — if PR scope grows, land items 1–3 first and split item 4 (esp. Phase 2) into a follow-up PR.**

**Cross-repo publish note (blocking for the wire):** App B has `@jaywedgeworth22/congress-trading-shared@^1.0.0` and the **installed 1.0.0 dist does NOT include `usageTelemetry`** (verified: no `usageTelemetry` symbols in `node_modules/@jaywedgeworth22/congress-trading-shared/dist/*.d.ts`), while the shared **source is at 1.1.0** with the client. So you must: (a) `npm run build` + `npm publish` the shared package `1.1.0` (publishConfig registry `https://npm.pkg.github.com`), then (b) bump App B's dependency to `^1.1.0` and `npm install`. If publishing to the private GH registry isn't possible in this session, say so explicitly and fall back to importing the client's logic locally (or a `file:`/workspace link) — but do not silently ship against 1.0.0, which lacks the export.

**Out of scope:**
- No new Prisma migrations beyond the read-only budget-status endpoint in item 4 (`ExternalUsageEvent` already exists — do not alter its schema).
- No changes to the monitor's poll adapters, its cron/`fetch-all` route, or its dashboard UI.
- No changes to how costs are *priced* (`MODEL_PRICE_PER_M` / `estimateRagCost` stay as-is; reuse them).
- No real-trade or autonomy-behavior changes beyond item 4 Phase 2, which must stay default-off.
- Do not remove or bypass the "never throws" contract on the ledger functions.

**Process & constraints:**
- Do all App B work in `/Users/jay/Code/Agentic Trading` on a feature branch (or your agent worktree/branch per AGENTS.md); land via `bash scripts/land.sh`, never push to `main` directly.
- **Paper mode is default — NEVER place real trades or flip `paperMode: false`** while testing item 2/4.
- All new outbound-telemetry and budget-enforcement behavior must be **behind default-off env flags** (`USAGE_MONITOR_BASE_URL`/`USAGE_INGEST_TOKEN` gating the push; `USAGE_BUDGET_ENFORCE` gating Phase 2) so unconfigured runs are byte-for-byte unchanged.
- **Add tests:** a unit test for the App-B forwarder using the shared client's `fetchImpl` injection (assert schema-valid batch, idempotency key present, no-op when unconfigured); a test that `recordLlmUsage`/`recordRagUsage` still never throw when the push fails; and a budget-enforcement test with a stubbed budget-status response.
- Run the verify quartet before claiming done: `npm run lint`, `npx tsc --noEmit`, `npm test`, `npm run build` (in App B; also `npm run typecheck`/build in the shared and monitor repos for any files you touch there).
- Follow the handoff protocol in `AGENTS.md`/`CLAUDE.md`: update `STATUS.md`, add a `docs/rollouts/YYYY-MM-DD-usage-monitor-integration.md` note (summary/why/files/verification/follow-ups — explicitly record the shared-package publish + version bump and which providers are push- vs poll-covered), and update `PLAN.md` for scope changes; reference the updated docs in each commit message.
- Confirm `git config user.email` resolves to the noreply address before committing.
- Open the PR as **READY (not draft)** and merge with `gh pr merge <n> --squash --auto` once the `verify` check is green.

---

