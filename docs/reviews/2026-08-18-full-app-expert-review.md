# Full-App Expert-Panel Review — Desktop Web + Mobile Web + iOS (MONET, 2026-08-18)

Owner-directed top-to-bottom review of Socratic.Trade by a diverse expert panel, covering the desktop website, the phone-width website, and the native iOS app.  Every finding is grounded on current `main` (`c55c2e642` at review start) with a file:line, and every panel finding was independently re-checked by a second, skeptical agent.  Findings that were already fixed, in-flight, out of scope (the PWA), or contrary to owner rulings were meant to be refuted at that stage; the verifiers instead confirmed 307 and downgraded/qualified 29 (see §0 for the honest caveat).

Companion artifacts: the hotfix that came out of the live pass (PR #2851, live), the raw findings JSON (`review-findings.json`, session scratchpad — not committed), and Cursor's 2026-08-17 report-only audits (§10).

> **Peer agents: the board is in this repo, not behind a login.**  The interactive audit board is committed at `docs/reviews/2026-08-18-audit-board.html` (open it directly — self-contained, no server needed), and the same data is machine-readable at `docs/reviews/2026-08-18-work-items.json` (`clusters[]` with `member_uids` + `plan`, `p1_verdicts[]`, `gap_findings[]`, `findings_index{}`).  Claim work by cluster key.  The claude.ai artifact URL is private to the owner's session by design, so it 404s for everyone else — use these files instead.

> **Four owner decisions were taken on 2026-08-19** and are recorded in `docs/reviews/2026-08-19-owner-decisions.md`: the Autopilot typed phrase honors the Typed Confirm preference (`LIVE-17`); the hard-coded 5% opening headroom becomes an editable policy field, 0 = off (`tsx-11`); Stopped allows reject/annotate and QUEUES approvals with an explicit "will not place until started" state (`LIVE-05`); sessions get server-side revocation plus Sign out everywhere (`sec-04`).  Implement to those answers; do not re-litigate them.

> **Superseded in places — read Part II.**  A second, adversarial verification round (2026-08-19, appended below) re-attacked every P1: 27 upheld, 11 narrowed, 2 already fixed.  Two headlines in this Part are WRONG as written and are corrected in Part II §A: `trading-money-path:tsx-01` is **not** "duplicate orders" (reusing the same `client_order_id` is exactly what makes Alpaca refuse the second submission — the harm is misclassification, and the MCP transport is not reachable in production at all, so it is P3), and the oldest-500 fill cap (`perf-01` / `berel-02`) needs >500 fills per (account, source) before it bites, which nobody has confirmed on the production DB — P2.  Part II also adds 50 findings from five surfaces this Part only skimmed, and folds everything into 45 deduped work items.

## 0. How To Read This

- **Method.**  Seventeen expert lanes ran in parallel over the repo and a local dev server, each returning file:line-grounded findings in a fixed schema; one skeptic per lane re-opened every citation on current code and returned CONFIRMED / PARTIAL / REFUTED / UNVERIFIABLE (with corrected severities); one agent cross-checked the result against the three prior full reviews (2026-07-20, 2026-08-04, 2026-08-06); MONET separately walked the live surfaces by hand (desktop 1280px, phone 390px, iPhone 17 Pro simulator in fixture mode) — those are the `LIVE-nn` items in §1.

- **Counts.**  336 panel findings (307 CONFIRMED, 29 PARTIAL, 0 REFUTED, 0 UNVERIFIABLE) + 20 live-pass findings + 50 verifier-added items.  By final severity: P1 40 · P2 148 · P3 148 (no panel P0; the one production-down class item was the live-pass hotfix, LIVE-09).  By kind: 231 needs-fixing (bug/defect/risk) vs 105 could-be-improved (improvement/opportunity).  By surface: Cross-surface 79, Backend 76, Desktop web 64, iOS 59, Mobile web 28, Public site 19, API 11.

- **Honest caveat on verification.**  Zero refutations across 336 items means the verifiers were lenient rather than that every claim is airtight; treat CONFIRMED as "a second reader re-opened the file and agreed", and read the PARTIAL notes as the correction of record.  Severity is the verifier's final severity.

- **Owner rulings were applied as constraints**, not re-litigated: real trading / no paternalism, guardrails advisory with off-switches, PWA out of scope, light default theme, two-space + Title Case copy rules, Central Time timestamps, sole-user (no compat shims).  Where a finding is really an owner-philosophy decision it is labelled as such (e.g. LIVE-17, tsx-11, ios-08).

- **Ids** are `lane:id` (panel) or `LIVE-nn` (live pass) so every line is traceable to its lane's raw output.  Duplicates across lanes were kept (they corroborate each other) and are noted with "same as" where obvious.

## 1. Executive Summary — What Matters Most

Ranked by impact on a real-money trader using this every day.  Each line names the finding id(s) so the detail is one search away.

1. **A production regression shipped and was fixed during the review.**  `/console/connections` (broker + LLM-key management) crashed client-side on `main` HEAD `c55c2e642` (#2848) with `process.uptime is not a function`; hotfix PR #2851 is merged and live (`01f4e73e2`).  The root cause is structural and still open: server DB modules are bundled (stubbed) into the browser via `brokers.tsx → venue-contract → source-settings → db-api-keys → db` — the Connections client bundle is 1.05 MB vs ~476 KB for every other page, and default Turbopack `npm run dev` 500s every route once that page compiles.  → LIVE-01, LIVE-09, `frontend-performance:dweb-01`.  Fix: `import "server-only"` on `src/lib/db.ts`, a client-safe home for `mergeAccountCapabilities`, and make dev use the same bundler as prod.
2. **Money path — ways the app can do something with an order the owner did not intend.**  (a) *[corrected in Part II — P3, not P1]* Alpaca MCP placement falls back to REST after an ambiguous MCP failure and re-submits the same `client_order_id`; Alpaca refuses the duplicate, so the real harm is that a live order is filed as `rejected_by_broker` (no fill row, no lot, daily cap not consumed) — and the MCP transport is unreachable in production today (`trading-money-path:tsx-01`).  (b) The stale-exit remediation cancel-replaces ANY working sell/cover limit or stop-limit older than 15 min with a MARKET order — including activated bracket take-profit legs, bracket stop-limit legs and owner-placed GTC sells (`tsx-02`).  (c) Manually cancelling an app-managed protective stop is silently undone next tick (`tsx-08`); any broker 4xx including 429/408 is filed as a terminal broker rejection (`tsx-04`); approval-time re-anchoring moves EXIT limits with the market (`tsx-09`).
3. **iOS tells the owner an approval succeeded when it did not.**  `proposal.approve` returns `blocked / busy / not_placed / error / rejected_by_broker` as a resolved result, `runCommand` stamps it `succeeded`, and the phone shows a green "Approved — waiting for desk refresh" (`api-contract-web-ios:api-02`, `trading-money-path:tsx-03`).  Related: iOS Guardrails stop-loss rows always show "—" because `FullPolicy` decodes `stopLossPct` at the top level while the server nests it under `riskRules` (`api-01`); the iOS scan timeout (25s) is below the new server budget (35s) so slow scans read as "Check your connection" (`api-03`).
4. **P&L is quietly wrong in four independent ways.**  The fill ledger keeps the OLDEST 500 fills per source (`ORDER BY filled_at ASC LIMIT 500`, sixteen callers) so every P&L / open-lot / tax / model-stats figure freezes once an account passes 500 fills (`performance-accounting:perf-01`, `backend-reliability:berel-02`, prior 2026-07-03 #39); Home Day P&L is zeroed on ordinary trading days because flow inference runs with no fills and reads market P&L as a deposit (`perf-02`); short/cover cash sign is inverted in flow inference (`perf-03`, prior 2026-08-06 C1 root cause); tax "Short-term realized" excludes every covered short (`perf-07`); win rate counts partial closes as round trips (`perf-11`); "Versus the market" TWR silently covers only ~7 trading days (`perf-04`).
5. **The Green-team 400s of the last two days have a concrete cause.**  The Bull strict JSON schema lists `exitPlan` in `properties` but not `required`; OpenAI-family strict mode rejects that with HTTP 400 — misdiagnosed as "terra unservable" (`llm-agent-architecture:llm-01`).  Also: the Red Team reviewer resolves the ACTIVE console account, not the account under review (`llm-02`); delta-only macro pruning strips unchanged FRED fields the prompt tells the model to use (`llm-03`); the daily learning review auto-applies `strategy-directive` rows into the strategy prompt as trusted owner text (`llm-04`); "Retry Red Team" stamps approve-at-half without applying the haircut (`llm-06`); the hard-coded dead-model list drops `claude-fable-5`/`kimi-latest` from rotation (`llm-10`).
6. **Market-data freshness is fabricated in several places.**  Weekend TTL extension fires during Friday's live session so screener/OHLC/enrichment caches freeze until Monday (`market-data-integrity:mdi-01`); daily bars are served "fresh" for 3 calendar days and today's partial bar is persisted as an EOD close (`mdi-02`); the cascade stamps cache-served values with a "now" `asOf`, so 6-hour-old Nasdaq quote/volume wins recency arbitration (`mdi-03`); Yahoo/ROIC/CBOE fallbacks stamp `asOf = now`, defeating the stale-quote gate that converts market orders to limit backups (`mdi-04`); Alpaca "price" is the ASK (`mdi-05`).
7. **Backend: the event-loop pin has a root cause and the broker has no timeout.**  FTS5 idempotency DELETE/COUNT use equality on non-rowid columns = a full-table scan per chunk on a 4.2 GB DB (`backend-reliability:berel-01`); filing-body ingest, `persistLocalComplete` and the summarizer still mirror whole documents into FTS synchronously (`berel-04`); Alpaca SDK/Tradier calls have no HTTP timeout and the stop-monitor lanes only release when the hung promise settles (`berel-03`); the scheduler tick has no re-entrancy guard (`berel-08`); `getEquityOrders` walks the entire order history (up to 50×500) on every snapshot and 20 other call sites (`dweb-02`/`tsx-05`/`berel-09`).
8. **Security: one config slip makes anonymous = owner.**  Production has no `AUTH_SECRET` boot guard; if the secret is missing every anonymous request becomes the primary user (`security-auth:sec-01`, see also LIVE-20 hardcoded owner email as the fallback identity).  Sessions cannot be revoked (30-day stateless JWT, `sec-04`); origin is directly reachable at `<PROD_ORIGIN_IP>:443` bypassing Cloudflare (`sec-02`); `next-auth 5.0.0-beta.31` carries three published advisories (`sec-03`); mobile auth exchange buffers unbounded bodies with no rate limit (`sec-06`); the mobile web-auth handoff mints a session code for any authenticated GET with an arbitrary `code_challenge` (`sec-05`).
9. **iOS: sign-out leaves the previous account's snapshot in UserDefaults** so a cold relaunch boots stale data and mislabels an explicit sign-out as "session expired" (`ios-engineering:ios-01`); guardrail/account edits in the Settings sheet fail silently because `store.error` is never rendered there (`ios-hig-ux:ios-01`); a 3-minute staleness gate disables Approve/Run after any quiet 3 minutes with no polling fallback (`ios-engineering:ios-03`); one malformed list element blanks the whole app (`api-07`); Swift tests never run in CI or before TestFlight and the Swift build is advisory, not a merge gate (`qa-01`, `qa-02`); `PrivacyInfo.xcprivacy` is absent (`ios-engineering:ios-10`); Coach renders markdown as raw text (`ios-hig-ux:ios-02`); "1 Proposals for Review" (LIVE-15); money formatting drops cents (LIVE-14).
10. **Ops: nothing pages the owner when the trader stops trading.**  `schedulerStale`, `tradingLivenessDegraded` and OpenRouter low-balance are computed but never pushed (`ops-observability-admin:sre-02`); there is no at-a-glance "is the trader alive" panel on web/admin/iOS (`sre-01`); litestream kill-switch markers are invisible to `/api/health` and the Backups page (`sre-03`, prior 2026-08-06 A2); three backup layers, zero restore drills (`sre-14`); a broker-held protective stop declined/failed is audit-only (`sre-12`); an overdue next run is never flagged (`sre-11`).
11. **Web console correctness at the top of the funnel.**  Home proposal rows carry a dead/wrong Approve button that POSTs a synthetic id (404) and wrong status tones (`desktop-web-ux:dweb-01`); a bare `r` key fires a strategy run and two mounted RunOnce buttons double-POST (`dweb-02`); "Recent decisions" rows are click-only divs — the Approve flow is unreachable by keyboard (`web-accessibility:a11y-01`); tone-colored chips and the Stop/Start button fail WCAG AA in the default light theme (`a11y-02`, prior #2561); the phone chrome bar squeezes the account scope to ~0–15px since the label became "Stop Agent" (`mobile-web-ux:mweb-01`, LIVE-11); "System" theme never survives reload and ⌘K theme toggle does nothing visible (`mweb-03`, `mweb-04`).
12. **QA gates have holes that let all of the above ship.**  Docs-only CI fast path skips the build for build-time-imported `docs/benchmarks/*.json` (`qa-test-strategy:qa-03`); no API→iOS snapshot contract test (`qa-04`); no global network guard in vitest, so the suite still reaches SEC/Yahoo/SiliconFlow (`qa-05`); 63/122 routes untested including single-proposal approve (`qa-07`); browser coverage is one 18-line desktop smoke, never on PRs, no phone width (`qa-06`); `land.sh` skips lint and never compiles Swift (`qa-11`).
13. **Copy is inconsistent in ways that misinform.**  Macro page says below-median buys are "hard-vetoed" in crisis regimes; the code is advisory tag-not-drop (`ux-copy-editor:copy-03`); iOS sends the owner to a "Strategy page" that does not exist in the app (`copy-04`); "Approvals" vs "Proposals", three names for one run state, four names for the stop control (`copy-05`, `copy-07`); ALL-CAPS "(NOT Real Money)" labels (`copy-11`); a "Mock (offline)" model offered first in the Coach picker (`copy-16`); the two-space rule is unenforced across ~600 strings and double ASCII spaces collapse at render (`copy-01`, `copy-02`); Guardrails header says "hard execution constraints" (LIVE-04).
14. **Public site over-promises and under-discloses.**  Explainer copy promises delegation controls the product does not have (`public-site-growth:dpub-03`); `/framework` states an invariant that is not true (`dpub-04`); Terms were not updated when market-data pooling became a condition of use (`dpub-05`); every Auth.js error lands on `/access-denied` with no way forward (`dpub-02`); session-expired redirect drops the deep-link destination (`dpub-01`).
15. **Still open from prior reviews** (§6): Run once not stamped with account alias + LIVE|PAPER (2026-08-04 R1), iOS one-tap Stop vs web confirm parity (R4), duplicate stale-quote annotations (2026-08-06 C6), single-account Alpaca trade-updates stream (2026-07-20 #43), Coach drafts cannot express short/cover (#45), Anthropic prompt caching dead under OpenRouter (#15).

## 2. Live-Pass Findings (MONET, by hand)

Captured on a fresh `origin/main` worktree: `next dev` (Turbopack, then `--webpack`) at desktop 1280×720 and phone 390×844, plus the iOS app on a dedicated iPhone 17 Pro simulator in `-ASCScreenshots` fixture mode.  These are additive to the panel and are cited from §1.

### LIVE-01 (P1, cross-surface / dev workflow) — `npm run dev` (Turbopack) 500s the whole app after `/console/connections`
- Evidence: fresh worktree at origin/main c55c2e642, `npm run dev -- -p 3005` (Turbopack default in Next 16). `/console` 200 until `/console/connections` compiled → "Module not found: Can't resolve 'fs'" via `./node_modules/better-sqlite3/lib/database.js` from Client Component chain: `app/console/settings/brokers.tsx` ("use client") → `src/lib/venue-contract.ts:22 import { resolveSourceBool } from "./source-settings"` → `src/lib/source-settings.ts:15-16 import { LOCAL_USER } from "./db-api-keys"; import ... from "./db-settings"` → `src/lib/db.ts` → better-sqlite3. After that error every route (`/login`, `/welcome`, `/console/*`, `/admin/*`) returned 500 until restart (verified with curl over all 34 static routes).
- Prod build passes only because `package.json` `"build": "next build --webpack"` and `next.config.mjs:39-66` alias `better-sqlite3`/`fs`/`dns`/... to `false` for client bundles — i.e. server DB code is being bundled (stubbed) into the browser bundle rather than excluded. With `npx next dev --webpack -p 3005` the page is 200 (verified).
- Why: AGENTS.md tells every agent to verify with local `npm run dev`; today that path is broken on a key page and takes the whole app down; also masks a real client/server boundary leak (module bloat + accidental server code on the client).
- Recommendation: (1) add `import "server-only"` at top of `src/lib/db.ts` (and the `db-*.ts` modules) so any client import fails the build loudly; (2) move the pure `mergeAccountCapabilities` (and any other pure venue helpers) out of `src/lib/venue-contract.ts` into a client-safe module (or make `source-settings` not import `db-api-keys` for `LOCAL_USER`); (3) either mirror the webpack aliases into `turbopack.resolveAlias` in `next.config.mjs` or change the `dev` script to `next dev --webpack` so dev == prod bundler; (4) one-line AGENTS.md note.

### LIVE-02 (P2, desktop web) — Bare default 404 page inside the console shell
- `/console/proposals` (a natural guess for the "Proposals" nav label, which actually links to `/console/approvals`) renders Next's stock "404 | This page could not be found." on a gradient — no nav, no wordmark, no link back. No `app/not-found.tsx` / `app/console/not-found.tsx` (verify).
- Rec: add `app/not-found.tsx` (and console-scoped one) with the console chrome + "Back to Home".  Consider `redirect('/console/approvals')` from `/console/proposals` since nav label and iOS both say "Proposals".

### LIVE-03 (P2, copy) — Button/link casing inconsistent in the console top bar + checklist
- Top bar: "Run once" (sentence case) next to "Start Agent" (Title Case); checklist links "Open Connections", "Choose account", "Add API key"; Scan tabs "Market scan" / "Smart money". FLEET-UI-COPY says buttons/nav/tabs Title Case.

### LIVE-04 (P2, copy/philosophy) — Guardrails header says "hard execution constraints"; Autonomy panel says it "does not duplicate those buttons" and then shows a Start Agent button
- `/console/guardrails` header: "authority, caps, and hard execution constraints" vs owner ruling that guardrails are advisory/adjustable.  Autonomy card copy: "Run once and Start/Stop live in the top bar only (one control each — this panel explains state and blockers, it does not duplicate those buttons)" immediately followed by a green "Start Agent" button in the same card.

### LIVE-05 (P2, desktop web) — Proposals page: "The server refuses approving or rejecting proposals while stopped"
- Rejecting a proposal is data (the page itself says "Rejections are data, not failures"), yet the stopped state blocks both approve AND reject.  At minimum reject/annotate should work while Stopped; approve-while-stopped could queue.  Check API: which route enforces this and whether iOS mirrors it.

### LIVE-06 (P3, desktop web) — Terms modal copy: subtitle "Not investment advice. You set authority." reads as two fragments; body single-spaced sentences (two-space rule).

### LIVE-07 (P3, desktop web) — Strategy page shows "Green Team: Not Set. Red Team: Not Set. These selected models do not expose a provider-specific reasoning or thinking control here." even when no model is selected; fallback placeholders "e.g. gpt-5.4-mini, claude-sonnet-5" (stale/odd example ids).

### LIVE-08 (P3, desktop web) — Sidebar at 1280x720: "CONFIGURE" group is clipped at the bottom (Strategy/Guardrails/Connections/Settings need scroll inside sidebar; the Next dev badge overlaps). Check whether the sidebar scrolls independently and whether the active section is kept in view.

### LIVE-09 (P1 → HOTFIXED, PR #2851) — Production `/console/connections` "Dashboard error: process.uptime is not a function"
- Prod `c55c2e642` (#2848, live 00:51Z). `src/lib/db-execution.ts:329` module-scope `process.uptime()`; module reachable from browser bundle via db barrel; Next browser `process` shim has no `uptime`. Reproduced on `next dev --webpack` (same bundler as prod). Fix = lazy guarded accessor + regression test proven to fail on old code. Root cause (server modules in client bundle) remains LIVE-01.

### LIVE-10 (P3, tech debt) — Next 16 deprecation: `middleware.ts` file convention deprecated ("use proxy instead") — dev log prints `The "middleware" file convention is deprecated. Please use "proxy" instead.` on every boot; migrate `middleware.ts` -> `proxy.ts` (`npx @next/codemod@canary middleware-to-proxy .`) before a Next minor removes it. Also `next-env.d.ts` is rewritten by `next dev` (dirty tree noise) — check `.gitignore`.

### LIVE-11 (P2, mobile web) — Top bar at 390px: account switcher collapses to a bare 41x30 chevron (no label, below 44px), Stopped pill 71x24, Run once is icon-only (aria ok) while the checklist copy says "use **Run once** in the top bar" — the label the copy points to is not visible on phones. "Fresh · Deployed today: $0.00" chip still reads like P&L (2026-08-06 C9 rename to "Deployed today" did not fix the ambiguity).
- Evidence: JS probe on http://localhost:3006/console at 390x844: `{"t":"No connected account…","w":41,"h":30}`, `{"t":"Stopped","w":71,"h":24}`, `{"t":"⚡","aria":"Run once","w":44,"h":44}`.

### LIVE-12 (P3, mobile web) — Guardrails Autonomy card renders a literal "#autonomy" anchor label as visible text next to the Start Agent button (both widths).

### LIVE-13 (P2, desktop web) — Settings tab strip (Notifications, Delivery, Sharing, Legal, Learning review, AI budget, Scan shape, Data sources, Confirmation, Boot, Account, Display, Glossary, D…) overflows horizontally at 1280px with no scroll affordance/fade; the notification event list is 24+ unlabeled-group toggles; "Vetoed by Bear risk" uses the retired "Bear" name next to "Red Team override".

### LIVE-14 (P2, iOS) — Money formatting inconsistent: Home "Open P&L $845.4", Assets "$8,962.8", "Avg $198.2", "$3,294" (no cents / trailing zero dropped) vs "$42,150.25" equity. Positions rows show qty + avg cost but NO unrealized P&L / day change per position.
- Fixture mode (`-ASCScreenshots`) screenshots 02-home, 05-assets in scratchpad/ios.

### LIVE-15 (P2, iOS) — Pluralization: "1 Proposals for Review" — `ios/SocraticTrade/ProposalsView.swift:207` `Text("\(snapshot.pendingProposals.count) Proposals for Review")`.

### LIVE-16 (P2, iOS) — Push-permission system prompt fires the moment the Proposals tab is first opened, with no pre-prompt/rationale (HIG: ask in context, explain value first). Verify trigger site in `PushNotifications.swift`.

### LIVE-17 (P2, iOS, owner-philosophy check) — Loosening rituals: `PolicyTightening.swift:51-60` "Ask-First → Autopilot is the loosening. Autopilot is ALWAYS the typed-phrase ritual" (type `AUTOPILOT`), and raising a cap on live requires typing `CONFIRM` when the typed-confirm preference is on. Proposal card shows "Typed confirmation required for this live order" (`ProposalsView.swift:341`). Owner ruling: guardrails are adjustable preferences with an easy override, "never a scolding ritual" — the cap/typed-confirm path has an off-switch (Guardrails "Typed Confirm: yes"), but the Autopilot phrase does not. Owner decision item.

### LIVE-18 (P2, iOS/web parity) — Bottom tabs differ: web-mobile = Home / Proposals / Activity / Orders / More; iOS = Home / Proposals / Assets / Activity / More (pinnable). iOS "More" has no Strategy, Connections, or Settings entry (cannot edit strategy text, models, or keys from the phone app; Guardrails is read + tighten/loosen only). "Open and Recent Broker Orders" subtitle in Title Case (values/subtitles should be sentence case). Proposal "Created Jul 21, 2026 at 12:20 PM" has no timezone label; "Order Limit · 4 shares · limit $468.75" repeats "limit". AAPL logo placeholder while NVDA/MSFT logos render.

### LIVE-19 (P3, iOS) — Assets tab spends its top card on a "Scan Table → Open Scan" redirect stub before Positions; Home shows four stacked full-width agent buttons (Run Once / Stop Agent / Close Only / Wind Down) above the fold — consider a segmented control or a single "Agent" sheet.

### LIVE-20 (P3, web/auth) — Owner identity hardcoded as the default primary/admin user: `middleware.ts:40 const PRIMARY_EMAIL = (process.env.PRIMARY_USER_EMAIL || "mail@jays.services")` and `src/lib/auth/identity.ts:15 const DEFAULT_PRIMARY_EMAIL = "mail@jays.services"`. Any unauthenticated local dev build presents as "Signed in as mail@jays.services" (primary + admin). Sole-user today so low severity, but the fallback should be a neutral placeholder (or required env) so a misconfigured deploy cannot silently make an anonymous session the primary user — verify how `authConfigured=false` + PRIMARY_EMAIL interact in production.

### LIVE-21 (P2, ops — live corroboration of `sre-04` / `berel-01` / `sre-02`) — Paired uptime flaps are origin stalls, not restarts or deploys
- Observed 2026-08-19: UptimeRobot fired PAIRED `socratictrade.com` + `OpenRouter credits low` incidents at 15:37:53Z, 16:41:02Z, 17:38:17Z, 18:42:21Z (each resolved 5-15 min later).
- Receipts: three spaced `/api/health` samples returned 200 in 1.10s / 0.60s / 0.47s with `checks.release.processStartedAt = 2026-08-19T05:05:06.357Z` and `processUptimeSeconds` climbing continuously (54124 → 54133) — i.e. **the process did not restart during any of those windows**, so the flaps are neither crashes nor deploy churn.  Both monitors evidently probe the same origin, so one stalled origin marks both DOWN together.
- Reading: this is the live signature of `ops-observability-admin:sre-04` (Docker `HEALTHCHECK --interval=30s --timeout=5s --retries=3` on the heavy `/api/health`, `Dockerfile:75`) plus the documented loop-pin class (`backend-reliability:berel-01`, `berel-04`).  It is also `sre-02` in action: the owner learns about it from an external monitor, and nothing in-app pages on the underlying degradation.
- Recommendation: split a cheap liveness endpoint (`/api/ready`, already present) for the container HEALTHCHECK and for the uptime monitor, keep `/api/health` as the deep check on a longer interval, and add the in-app push from `sre-02`.  Do not raise the HEALTHCHECK timeout as the only fix — that hides the stall.
- Checked and NOT a problem (verified, so nobody re-raises it): production runs `a8a0a65bc` while `main` is `52add2ae1`.  That is **correct** — the two newer merges (`437f0a4e4`, `52add2ae1`) touch only `ios/**`, `docs/**`, `STATUS.md`, `PLAN.md`, and the Coolify webhook answered the main push `{"status":"failed","message":"Changed files do not match watch paths. Ignoring deployment."}`.  Watch paths are working as designed; this is not the 2026-08-02 "Invalid signature" freeze.

## 3. P1 Findings (40) — Wrong Information Or Broken Core Flow

### Needs Fixing (40)

#### [backend-reliability:berel-01] FTS5 idempotency DELETE/COUNT use equality on non-rowid columns = full-table scan per chunk; this, not tokenization, is the ~165-300ms/row that pins the loop
- **Severity** P1 · **Kind** bug · **Surface** Backend · **Area** rag/fts-mirror · **Effort** M · **Verdict** CONFIRMED
- **Where** `src/lib/db-learning.ts:1671`
- **Evidence** DELETE FROM document_chunks_fts WHERE content_hash = ? AND symbol = ? AND source = ? AND accession = ? (db.ts:2148 fts5(content_hash, symbol, source, accession, text) — no UNINDEXED, no MATCH)
- **Why it matters** FTS5 xBestIndex only indexes MATCH and rowid constraints; a WHERE on ordinary columns is a residual filter evaluated over a full scan of the %_content shadow table. Every mirrored chunk therefore costs one full scan of the entire FTS corpus (4.2 GB DB), and countDocumentChunkFts (:1706), hasLocalFilingCopy/countLocalFtsRows (persist-local-complete.ts:67-72,108-113 with GLOB) do the same. The per-row cost grows with corpus size — matches the receipts (165 ms/row on 08-13, ~300 ms/row on 08-14, 6-13 s ftsMirrorSlice for six 1-row groups on 08-18) and explains why every yield/slice bound has only stretched the pin instead of removing it. Also all four metadata columns are tokenized into the FT…
- **Recommendation** Give each occurrence an indexable identity: either (a) a plain side table document_chunk_fts_keys(symbol,source,accession,content_hash,fts_rowid) with a UNIQUE index, DELETE FROM document_chunks_fts WHERE rowid = ?, and COUNT from the side table; or (b) rewrite the identity predicates as column-filter MATCH ('symbol:"X" AND source:"Y" AND accession:"Z" AND content_hash:"H"'), which uses the FTS index. Declare content_hash/source/accession UNINDEXED if you take (a). Wrap del.run/ins.run in timeSync to prove the split before/after. One-time rebuild migration for the corpus.

#### [backend-reliability:berel-03] Broker calls have no HTTP timeout (Alpaca SDK axios default 0; Tradier fetch has no signal) and the scheduler's stop-monitor / stale-exit lanes are guarded by in-flight sets that only release when the hung promise settles
- **Severity** P1 · **Kind** risk · **Surface** Backend · **Area** scheduler/broker · **Effort** M · **Verdict** CONFIRMED
- **Where** `src/lib/scheduler.ts:914`
- **Evidence** if (protectiveState && !stopMonitorInFlight.has(key)) { stopMonitorInFlight.add(key); void journalLane("synthetic-stop-monitor", ..., () => runSyntheticStopMonitor(...)) ... .finally(() => stopMonitorInFlight.delete(key));
- **Why it matters** safety-maintenance.ts:74-77 wraps the same monitor in withDeadline(15s) but the scheduler lane (the one that actually runs every tick) does not; tradier.ts:339 `await fetch(url, { method, headers, body })` and @alpacahq/alpaca-trade-api (dist/api.js `return axios(req)`, no timeout; alpaca.ts:256-287 trackHealth adds retries, not deadlines) can hang on a half-open socket until TCP retransmit gives up (~15 min on Linux). During that window the account's synthetic-stop monitor and stale-exit replacement are silently skipped every tick, and the tick's serial `await checkBrokerHealth` (:953) blocks every later account in the loop. The 08-18 first-call retry only covers getAccount/getPortfolio/ge…
- **Recommendation** Give every gateway method a hard deadline at the adapter boundary: `AbortSignal.timeout(BROKER_HTTP_TIMEOUT_MS)` in TradierClient.request and PublicBroker fetches; for Alpaca wrap each SDK call in withDeadline (or set axios defaults timeout on the SDK's axios instance) so a hung read rejects in ~20 s. In scheduler.ts wrap the stop-monitor, stale-limit-scan, pending-fill-reconcile and checkBrokerHealth lanes with withDeadline like safety-maintenance does, and clear the in-flight key on deadline so the next tick can retry.

#### [backend-reliability:berel-04] filing-body-ingest, persistLocalComplete and document-summarizer still mirror whole documents into FTS synchronously with no yield and no in-flight-strategy gate — the same pin class the SEC worker was bounded against
- **Severity** P1 · **Kind** risk · **Surface** Backend · **Area** rag/ingest · **Effort** M · **Verdict** CONFIRMED
- **Where** `src/lib/web-sources/sec-filings.ts:736`
- **Evidence** runWithActiveVectorCommitProof(result.managedCommitProof, () => timeSync("filingFtsMirror", ..., () => { for (const chunk of localChunks) { insertDocumentChunkFts(...) } insertIngestedAccession(...)
- **Why it matters** refreshFilingBodies runs daily from scheduler.ts:602 (SEC_FILING_RAG_MAX_PER_RUN default 25) and is not gated by hasInFlightStrategyWork; its FTS mirror is a single synchronous transaction over every chunk of a 10-K (each insertDocumentChunkFts doing the full-scan DELETE from berel-01), so a 900-chunk filing can pin the serving loop for minutes exactly like the 08-13/08-14 incidents. persist-local-complete.ts:48-53 and rag/document-summarizer.ts:521 have the same shape (sync loop, per-row delete+insert, no yieldEventLoop). Only sec-ingest-worker.ts:463-476 got the slice/yield/heartbeat treatment.
- **Recommendation** Route every FTS mirror through insertDocumentChunkFtsBatch (adaptive groups + yields) and the same per-tick slice/resume policy, gate them on hasInFlightStrategyWork like ROIC/SEC worker, and land berel-01 so each row is O(1). Until then, either disable the FTS half of filing-body-ingest or run it only outside market hours.

#### [llm-agent-architecture:llm-01] Bull strict JSON schema lists `exitPlan` in properties but not in `required` — OpenAI-family Green seats 400 since 2026-08-16 (misdiagnosed as 'terra unservable')
- **Severity** P1 · **Kind** bug · **Surface** Backend · **Area** strategy structured output / model failover · **Effort** S · **Verdict** CONFIRMED
- **Where** `src/lib/strategy.ts:5847`
- **Evidence** exitPlan: { type: ["string", "null"], description: "Staged exit plan..." } … required: [...BULL_PROPOSAL_REQUIRED_KEYS] (BULL_PROPOSAL_REQUIRED_KEYS at :6630 has 16 keys, no exitPlan)
- **Why it matters** buildLlmRequestBody sends `json_schema … strict: true` (llm-call.ts:515/529). OpenAI strict mode rejects any schema whose `required` omits a property with HTTP 400 invalid_json_schema; through OpenRouter that surfaces as the fast 'HTTP 400 Provider returned error (881ms)' the 2026-08-18 rollout notes chased for gpt-5.6-terra. #2740 (2026-08-16) added exitPlan to properties only. Every gpt-* rotation seat (terra/sol/luna/5.4-mini/nano/4o) and any gpt primary hits this; #2831 then hardcoded terra as 'unservable' instead of fixing the schema, so runs fail over to the cheapest preferred seats. filterRepairedProposals' projection (:6787) also drops exitPlan on the repair path.
- **Recommendation** Add "exitPlan" to BULL_PROPOSAL_REQUIRED_KEYS (schema + projection use it) and add a unit test that, for every strict schema (Bull items, autonomyOverride, stopPlan, Red verdict, revalidation, tuning), asserts Object.keys(properties) ⊆ required. Re-verify a gpt-* Green run, then remove UNSERVABLE_OPENROUTER_FIRST_PICKS/terra demotion if it was only this. Grep llm_call_latency for status 400 by model since 2026-08-16 to confirm blast radius.

#### [llm-agent-architecture:llm-02] Red Team reviewer resolves the ACTIVE console account, not the account under review (venue/short capability, execution mode, strategy prompt)
- **Severity** P1 · **Kind** bug · **Surface** Backend · **Area** Red Team adversary flow / multi-account correctness · **Effort** S · **Verdict** CONFIRMED
- **Where** `src/lib/red-team.ts:192`
- **Evidence** const activeAccount = getActiveConnectedAccount(userId); const venue = deriveVenueContract(policy, activeAccount); … const basePrompt = getStrategyPrompt(userId);
- **Why it matters** The strategy run passes an account-scoped runPolicy (policy.connectedAccountId) and itself uses getConnectedAccount(connectedAccountId) (strategy.ts:494), but debateProposal re-derives venue/executionState/strategy prompt from whichever account has is_active=1 (the console switcher). With Paper + Roth scheduled concurrently, a Paper short can be refused with 'This <Roth> account cannot short' (:194-201) without any reviewer call, and the reviewer reads the other account's strategy prompt (getStrategyPrompt falls back to the active account, db-profiles.ts:323-325). Cross-account context bleed in a money-path review.
- **Recommendation** Resolve the account from policy.connectedAccountId (getConnectedAccount(id, userId)), fall back to active only when absent; call getStrategyPrompt(userId, policy.connectedAccountId); add a test with two connected accounts where the non-active one is reviewed.

#### [llm-agent-architecture:llm-03] Delta-only macro pruning strips unchanged FRED fields from every run after the first — the stateless model never sees values the prompt tells it to use
- **Severity** P1 · **Kind** defect · **Surface** Backend · **Area** prompt assembly / context quality · **Effort** S · **Verdict** CONFIRMED
- **Where** `src/lib/strategy.ts:4960`
- **Evidence** const { macro: macroForPrompt, omitted: macroOmitted } = pruneMacro(macro, previousMacro); setInternalSetting(macroCacheKey, macro); … ? { ...macroForPrompt, unchangedSinceLastRun: macroOmitted }
- **Why it matters** pruneMacro (macro.ts:594-616) keeps only vix/fedFundsRate/dgs10Treasury/asOf plus fields that changed since `last_macro_sent:${userId}`; every other field (hyCreditSpread, dgs2, CPI, unemployment, usdIndex, wtiOil, corePCE…) is replaced by a bare list of names. LLM calls are stateless and the seat rotates models per run, so 'unchanged since last run' carries no information — yet buildBullSystem (strategy-prompts.ts:185) instructs the model to read hyCreditSpread and the curve together for recession risk. FRED series change daily at most while runs happen many times a day, so almost every run is starved. The key is user-scoped (shared across accounts) and the marker is written before the LLM…
- **Recommendation** Send the full macro block every run (it is ~30 short fields; token savings are negligible next to the 48k-char evidence budget) — delete pruneMacro's delta path or key it by (userId, accountId, model) and include values. Add a prompt-wiring test asserting hyCreditSpread/dgs2Treasury appear on a second consecutive run.

#### [llm-agent-architecture:llm-04] Daily LLM learning review auto-approves strategy-directive pending rows into the strategy prompt as TRUSTED owner text (contract says human approval required)
- **Severity** P1 · **Kind** risk · **Surface** Backend · **Area** learning loop / prompt trust boundary · **Effort** S · **Verdict** CONFIRMED
- **Where** `src/lib/learning-review.ts:660`
- **Evidence** applyApprovedPending(pending, nowIso); action = setPendingLearnedContextStatus(verdict.id, userId, "approved") ? "approved" : null;
- **Why it matters** listPendingLearnedContext(userId, 'pending') (:479) includes riskTier 'strategy-directive' rows; a 'keep' verdict in the default learningReviewMode 'decide' (defaults.ts:90) calls applyApprovedPending, which appends an AI-LEARNED block to the strategy prompt (learned-context/store.ts:399-404). coach-learning.ts:8-9 promises 'human approval required before they touch the strategy prompt', and prompt-safety treats the whole strategy prompt as the ONLY trusted source (prompt-safety.ts:225; strategy.ts:4934) — so an LLM verdict promotes chat-derived text into the one region the data-not-command boundary does not cover, for both Green and Red (red-team.ts:257 sends it raw).
- **Recommendation** Exclude strategy-directive rows from auto-apply (route them to 'defer'/human) or, if the owner wants LLM approval, contain AI-LEARNED blocks as 'learned' (untrusted) rather than owner_strategy. Add a test that a 'keep' verdict on a strategy-directive row does not call setStrategyPrompt.

#### [market-data-integrity:mdi-01] Weekend TTL extension fires during Friday's live session: screener/OHLC/enrichment caches freeze until Monday
- **Severity** P1 · **Kind** bug · **Surface** Backend · **Area** market-data caching / market hours · **Effort** S · **Verdict** CONFIRMED
- **Where** `src/lib/market-hours.ts:288`
- **Evidence** isWeekendOrHolidayClosureAhead: `if (!isEtCalendarTradingDay(today)) return true; return !isEtCalendarTradingDay(addEtCalendarDays(today, 1));` -> true all day Friday; expiresAt = Monday 9:30
- **Why it matters** Verified with tsx: a 5-minute screener write at Fri 10:00 ET expires 2026-08-24T13:30Z (Monday open). Same on the day before any holiday. So every Friday strategy run and Scan refresh reuses the FIRST screener rows of the day (market.ts:1342), OHLC bars (history.ts:243/321), 6h enrichment (data-providers.ts:2083/3881/4098) and macro caches. Ranking, momentum (intradayChangePct), breadth and universe-floor decisions run on frozen data with a fresh generatedAt; only top-candidate prices are rescued by the broker cascade.
- **Recommendation** Only extend when no session remains between now and the naive expiry: gate on `nowMs >= etWallClockToUtcMs(today,16,0)` (or naiveExpiry past today's close) before walking to next open; add tests for Fri 10:00 ET / Wed-before-Thanksgiving 10:00 ET expecting naive TTL.

#### [market-data-integrity:mdi-04] asOf fabricated from the wall clock (Yahoo quote-only fallback, ROIC profile price, CBOE, grouped VWAP) defeats the quote-staleness gate
- **Severity** P1 · **Kind** bug · **Surface** Backend · **Area** quote freshness / staleness gate · **Effort** S · **Verdict** CONFIRMED
- **Where** `src/lib/market.ts:1520`
- **Evidence** toQuoteOnlyMarketQuote: `provider: "yahoo-finance", asOf: new Date().toISOString()` while `quote.asOf` (Yahoo regularMarketTime) is ignored; quotes-cascade.ts:397 ROIC `asOf: new Date().toISOString()`
- **Why it matters** policy.ts:412-413 ages `quote.asOf`; a fabricated 'now' makes a delayed/EOD print look 0s old so the market order is not converted to the limit backup, and quotes-cascade `updateBestQuote` (lines 181-186) prefers the fabricated newer stamp over a real broker timestamp. cboe.ts:47 and mergeGroupedBarData (market.ts:1243, GroupedDailyBar has no `date`) do the same for signals shown with an as-of.
- **Recommendation** Propagate provider timestamps (Yahoo regularMarketTime already parsed at yahoo-finance.ts:76-77/185-186; CBOE last_trade_time; grouped bar date); when the provider gives none leave asOf undefined so isQuoteFresh/quoteAgeSecForStalenessGate treat it as stale.

#### [ops-observability-admin:sre-02] schedulerStale, tradingLivenessDegraded and OpenRouter low-balance never trigger an in-app push; only external keyword monitors can page
- **Severity** P1 · **Kind** risk · **Surface** Backend · **Area** alerting / notifications · **Effort** M · **Verdict** CONFIRMED · **Prior** in-flight PR #2816 (adds JSON flags + UptimeRobot runbook only, no in-app push)
- **Where** `app/api/health/route.ts:128`
- **Evidence** const liveness = getTradingLivenessSummary(); ... if (liveness.degraded) checks.tradingLivenessDegraded = true; // no sendNotification anywhere for this flag
- **Why it matters** Money-critical degradations are computed but only serialized: (a) schedulerStale (route.ts:82-88) has no notify path (Sentry Crons check-in is opt-in on SENTRY_CRONS_ENABLED=1, docs/ops-observability-security.md); (b) tradingLivenessDegraded is consumed only by /api/health and ops-snapshot.ts (rg confirms no other consumer); (c) OpenRouter prepaid balance (route.ts:300-326, src/lib/openrouter-credits.ts) — the single point of failure for every LLM call and all embedding — has zero in-app notification and is not shown anywhere on web/iOS (rg 'remainingUsd|openrouterCredits' in app/, ios/ returns nothing). The owner depends entirely on UptimeRobot→Pushover being configured for exact JSON subs…
- **Recommendation** In the scheduler tick (after the heartbeat write) evaluate the same three signals and fire deduped notifications: `risk_advisory`/new `engine_stalled` when a leader tick sees no completed run for N×cadence during market hours or consecutiveFailedRuns ≥ threshold, and `budget_alert` when credits.ok flips false — with a 'recovered' follow-up. Lower TRADING_LIVENESS_STALE_MINUTES default to max(60, 3×runCadenceMinutes). Surface OpenRouter remaining USD on /console/usage and /admin (admin-gated).

#### [qa-test-strategy:qa-03] Docs-only CI fast path skips the build for build-time-imported docs/benchmarks JSON
- **Severity** P1 · **Kind** risk · **Surface** Backend · **Area** CI classify · **Effort** S · **Verdict** CONFIRMED
- **Where** `.github/workflows/ci.yml:141`
- **Evidence** non_docs="$(printf '%s\n' "$changed_files" | grep -Ev '(^|/)[^/]+\.md$|^docs/' || true)" — but app/api/llm-usage/model-stats/route.ts:9 `import benchmarkJson from "../../../../docs/benchmarks/2026-07-08-llm-model-benchmark.json"`
- **Why it matters** A PR that edits only docs/benchmarks/*.json (or any non-.md under docs/) is classified docs-only, `verify` PASSes without lint/tsc/test/build, auto-merge lands it, and main auto-deploys — a malformed or reshaped JSON breaks the production build or silently changes model-stats numbers shown on /console/usage. Same regex in e2e.yml:77.
- **Recommendation** Tighten both regexes to only .md files: `grep -Ev '\.md$'` (drop the `^docs/` clause), or explicitly exclude `^docs/benchmarks/`. Add a comment pointing at the runtime import so nobody re-widens it.

#### [qa-test-strategy:qa-05] No global network guard in vitest; suite still reaches SEC/Yahoo/SiliconFlow
- **Severity** P1 · **Kind** defect · **Surface** Backend · **Area** Test determinism · **Effort** M · **Verdict** CONFIRMED
- **Where** `vitest.config.ts:31`
- **Evidence** no `setupFiles` in vitest.config.ts; STATUS.md:59 “Full `npm test` still hits leftover SiliconFlow + SEC/Yahoo 404s (untouched)”; 2026-07-05 rollout: “Other test files may still make live provider calls”
- **Why it matters** Any test that reaches src/lib/market.ts, data-providers.ts, or vector-db without a per-file stub makes real HTTP with 6–8s abort timeouts and 429 backoff, so CI is nondeterministic under provider rate limits and each new file can silently regress. This is also the stated reason maxWorkers was pinned to 1.
- **Recommendation** Add test/setup-network-guard.ts registered via `setupFiles` that replaces globalThis.fetch with a stub that throws `Unmocked network call to <host>` for any host other than 127.0.0.1/localhost unless a file opts in with `process.env.ALLOW_TEST_NETWORK=1`; run the suite once, fix the files that fail, then keep the guard on.

#### [security-auth:sec-01] Production boots with no AUTH_SECRET guard: a missing secret turns every anonymous request into the owner
- **Severity** P1 · **Kind** risk · **Surface** Backend · **Area** auth / middleware fail-closed · **Effort** S · **Verdict** CONFIRMED
- **Where** `middleware.ts:385`
- **Evidence** if (!trustedEmail && !isAuthConfigured()) { trustedEmail = PRIMARY_EMAIL; identitySource = ...localFallback; } (isAuthConfigured = !!AUTH_SECRET || CF flag, L165-167)
- **Why it matters** The only thing separating production from the dev fallback is the presence of the AUTH_SECRET env var. instrumentation.ts:47-48 refuses to boot without ENCRYPTION_KEY but has no equivalent for AUTH_SECRET, and request-user.ts:31 maps a header-less request to DEV_USER_ID="local", which is literally the primary owner's account id (identity.ts:56). Infisical keys have been deleted by agents more than once (AGENTS.md); one deleted/renamed AUTH_SECRET and the live real-money console is fully open to the internet as the owner, with no error anywhere. Exploit: attacker GETs /console or POSTs /api/proposals/{id}/approve with no cookie and is the owner.
- **Recommendation** Mirror the ENCRYPTION_KEY guard: in instrumentation.ts throw when NODE_ENV=production (or DB_BOOTSTRAP=live) and neither AUTH_SECRET nor an armed CF Access config is present. In middleware.ts, gate the local fallback additionally on a runtime production marker (DB_BOOTSTRAP !== "live") so the edge path fails closed even if the boot assert is bypassed. Optionally make resolveRequestUser deny (not default to "local") when the middleware email header is absent in production. Add a middleware-auth.test.ts case: AUTH_SECRET unset + DB_BOOTSTRAP=live -> 401.

#### [trading-money-path:tsx-01] Alpaca MCP placement falls back to REST after any MCP error and re-submits the same client_order_id; the duplicate 422 is then filed as rejected_by_broker while the first order lives at the broker
- **Severity** P1 · **Kind** bug · **Surface** Backend · **Area** order placement / idempotency · **Effort** S · **Verdict** CONFIRMED
- **Where** `src/lib/alpaca.ts:346`
- **Evidence** catch (e) { console.warn(`Alpaca MCP tool call "${toolName}" failed, falling back to REST:`, e); return fallbackFn(); } (used by placeEquityOrder at :772; strategy-execution.ts:1279 treats /HTTP 4\d\d/ as terminal)
- **Why it matters** callMcp() falls back on timeout (AbortSignal 8s), MCP 5xx, or JSON parse error even when the MCP server already forwarded the POST to Alpaca. The REST retry reuses input.refId as client_order_id, Alpaca answers 422 'client_order_id must be unique', and both the approval and autonomous lanes short-circuit that 4xx to rejected_by_broker WITHOUT calling reconcilePlacementError — so a real, possibly filled order is recorded as rejected, no fill receipt, no stop plan, no daily-notional accounting. Latent today (both prod accounts are Alpaca REST per STATUS.md) but alpaca-mcp is a supported activeBroker.
- **Recommendation** For place_* tools, do NOT fall back to REST on ambiguous failures (timeout/5xx/undecodable): throw a non-4xx 'uncertain' error so reconcilePlacementError runs the refId lookup. Also treat an Alpaca 422 whose body mentions client_order_id uniqueness as 'placed-elsewhere' and route it to reconcilePlacementError instead of the 4xx terminal branch (both strategy-execution.ts:1279 and strategy.ts:3979).

#### [trading-money-path:tsx-02] Auto stale-exit remediation cancel-replaces ANY working sell/cover limit or stop_limit older than 15 min with a MARKET order — including activated bracket take-profit legs, bracket stop_limit legs and owner-placed GTC sells
- **Severity** P1 · **Kind** bug · **Surface** Backend · **Area** order lifecycle / stale-limit remediation · **Effort** M · **Verdict** CONFIRMED
- **Where** `src/lib/order-replacement.ts:755`
- **Evidence** for (const item of stale) { const side = ...; if (side !== "sell" && side !== "cover") continue; if (... === "held") continue; ... INSERT INTO order_replacements ... 'cancel_requested' (stale = listStaleLimitOrders: LIMIT_ORDER_TYPES = limit|stop_limit)
- **Why it matters** stale-limit-orders.ts:33-49 lists every working limit/stop_limit sell older than staleLimitOrderMinutes (default 15) measured from updatedAt; the only exclusion is Alpaca 'held'. An Alpaca bracket TP leg flips held->new when the entry fills and is exactly this shape 15 min later; the test at test/stale-limit-orders.test.ts:76 even asserts it is listed. autoRemediateStaleExits defaults true (defaults.ts:160); on broker/paper, or on live with requireTypedConfirmation=false, the pump cancels the TP leg (Alpaca cascades the OCO stop leg) and places a market sell of the full remaining qty — liquidating a position 15 min after entry for no thesis reason. Owner GTC take-profit trims and bracket st…
- **Recommendation** Scope remediation to app-originated protective exits only: match clientOrderId prefix sstop-/proposal refIds whose proposal.tradeThesisTag is Risk-Exit/Synthetic Stop, and exclude any order with orderClass bracket/oto/oco/legs and any order not created by the app. Add a regression test that an activated bracket TP leg and a manual GTC sell are never enqueued.

#### [api-contract-web-ios:api-01] iOS Guardrails stop-loss rows always show "—": FullPolicy reads stopLossPct at top level, server nests it under riskRules
- **Severity** P1 · **Kind** bug · **Surface** Cross-surface · **Area** iOS <-> GET /api/policy contract · **Effort** S · **Verdict** CONFIRMED
- **Where** `ios/SocraticTrade/DeskModels.swift:323`
- **Evidence** stopLossPct = try values.decodeIfPresent(Double.self, forKey: .stopLossPct) // vs src/lib/types.ts:1188 `riskRules: RiskRules;` and types.ts:713-731 where stopLossPct/trailingStopPct/shortStopLossPct live inside RiskRules
- **Why it matters** GET /api/policy returns getPolicy(userId) verbatim (app/api/policy/route.ts:42), and TradingPolicy has no top-level stop fields — they are riskRules.stopLossPct etc. (DEFAULT_RISK_RULES stopLossPct: 8, defaults.ts:37). The Swift FullPolicy CodingKeys (DeskModels.swift:329-336) never touch riskRules, so GuardrailsView.swift:100-102 "Stop Loss / Trailing Stop / Short Stop" render "—" for every account since #2704 (2026-08-15). A trader on iPhone concludes no protective stop is configured when an 8% stop is live.
- **Recommendation** Add a nested `riskRules` Decodable (stopLossPct, trailingStopPct, shortStopLossPct, takeProfitPct, maxDrawdownPct) to FullPolicy and read those in GuardrailsView; drop the phantom top-level keys. Add the fixture-backed decode test described in api-06 so the next rename is caught.

#### [api-contract-web-ios:api-02] proposal.approve command reports "succeeded" (green check on iOS) when the broker order was blocked / not placed / errored
- **Severity** P1 · **Kind** bug · **Surface** Cross-surface · **Area** mobile command result contract (money path) · **Effort** M · **Verdict** CONFIRMED
- **Where** `src/lib/mobile-api.ts:965`
- **Evidence** case "proposal.approve": return executeProposal(String(payload.proposalId), command.userId, {...}) // executeProposal returns {status:"blocked"|"error"|"not_placed"|"busy"|"proposed", reasons} WITHOUT throwing (strategy-execution.ts:240-246, 298, 498, 1136, 1394, 1403)
- **Why it matters** runCommand only throws for a handful of pre-checks; every non-placement outcome comes back as a resolved result, so finishCommand stamps status='succeeded' with result.status='blocked'. iOS MobileCommand (MobileModels.swift:507-524) does not decode `result`, so MobileStore.proposalActionFeedback (MobileStore.swift:210-212) returns .succeeded and ProposalsView.swift:480-481 paints a green checkmark "Approved — waiting for desk refresh." on a card that stays pending (or on a broker-rejected order). Only web /api/proposals/:id/approve surfaces reasons. Same pattern for strategy.run_once: only status==='failed' throws (mobile-api.ts:954); skipped_market_closed/skipped_budget arrive as 'succeede…
- **Recommendation** In runCommand treat approve results whose status is not placed/filled as failures (throw new Error(reasons.join(' ')) with result kept for the receipt) or add an explicit `outcome` field and have finishCommand mark 'failed' when result.status ∈ {blocked,error,not_placed,busy,proposed}. On iOS decode `result.status`/`result.reasons`/`result.summary` (lossy `[String: AnyCodable]` is enough) and show them in the proposal banner and Activity rows; do the same for run_once summary and order.cancel dustWarning (mobile-api.ts:1004).

#### [api-contract-web-ios:api-03] iOS Scan times out at 25s while the server scan budget is now 35s (#2830 today) — slow scans surface as "Check your connection"
- **Severity** P1 · **Kind** defect · **Surface** Cross-surface · **Area** iOS <-> GET /api/scan timing contract · **Effort** S · **Verdict** CONFIRMED
- **Where** `ios/SocraticTrade/MobileAPIClient.swift:137`
- **Evidence** /// Interactive market scan — GET `/api/scan`. The server budget is 20s. ... req.timeoutInterval = 25 // vs app/api/scan/route.ts:22 `const INTERACTIVE_SCAN_BUDGET_MS = 35_000;` (was 20_000 until 13b60747a, 2026-08-18)
- **Why it matters** URLRequest.timeoutInterval is an idle timeout and the scan route sends nothing until it finishes, so any scan that legitimately runs 25-35s (Nasdaq 15s timeout + retry + Yahoo whole-set fallback — exactly the path #2830 added) is aborted client-side and shown as a network error ("Check your connection and try again.") while the server keeps working; the next tap coalesces on the single-flight and may time out again. #2830 changed the server without touching the Swift constant or its comment.
- **Recommendation** Raise the iOS scan timeout to server budget + slack (e.g. 45s) and cite the constant; better, have /api/scan return `budgetMs` in the 503/200 body or a response header so the client derives it. Add a vitest that greps MobileAPIClient.swift for the scan timeout and asserts it exceeds INTERACTIVE_SCAN_BUDGET_MS (pattern already exists in test/apns-deep-link-contract.test.ts).

#### [backend-reliability:berel-02] listFillEvents returns the OLDEST 500 fills (ORDER BY filled_at ASC LIMIT 500) so P&L, open lots, tax, model-stats and dashboard silently freeze once an account passes 500 fills
- **Severity** P1 · **Kind** bug · **Surface** Cross-surface · **Area** performance/fills · **Effort** S · **Verdict** CONFIRMED · **Prior** 2026-07-03 #39
- **Where** `src/lib/db-fills.ts:332`
- **Evidence** .prepare("SELECT * FROM fill_events WHERE account_number = ? AND source = ? AND user_id = ? ORDER BY filled_at ASC LIMIT ?").all(accountNumber, source, userId, limit) // limit = 500
- **Why it matters** Sixteen callers (dashboard.ts:675-676, performance.ts:460-493, tax.ts:337, strategy.ts:801, strategy-tuning.ts:335-418, experience-memory.ts:263, model-stats route:58, robinhood-pnl-crosscheck.ts:60) pass 500. Because the sort is ascending, the newest fills are the ones dropped: after fill #500 the equity/P&L replay, closed-lot scorecards, tax lots and 'prior fills' context the LLM sees stop advancing while the broker keeps trading. Prior review 2026-07-03 #39 called for window disclosure but assumed the window was 'last 500'; the direction makes it 'first 500'. A 15-minute-cadence account reaches 500 fills in weeks.
- **Recommendation** Drop the LIMIT for FIFO replay consumers (fill_events per account is a few thousand rows; the query is indexed by idx_fill_events_account) or select the newest N with a subquery and re-sort ascending, then thread the {windowRowsScanned,totalRowsAvailable} receipt through calculatePnl as #39 asked. Add a regression test that inserts 501 fills and asserts the newest is present.

#### [frontend-performance:dweb-02] getEquityOrders pulls the ENTIRE Alpaca order history (up to 50 pages x 500) on every dashboard snapshot and 20 other call sites, then ships all of it to every client each poll
- **Severity** P1 · **Kind** defect · **Surface** Cross-surface · **Area** dashboard server cost / broker chain · **Effort** M · **Verdict** CONFIRMED
- **Where** `src/lib/alpaca.ts:491`
- **Evidence** alpaca.ts:491-506 `async getEquityOrders(...) { ... status: "all", limit: 500 ... for (let guard = 0; guard < 50; guard++) { const page = await this.trackHealth(() => this.alpaca.getOrders({ status: "all", limit: PAGE, direction: "desc", ...until }))`
- **Why it matters** computeDashboardSnapshot (dashboard.ts:485-489) calls it inside the portfolio/positions/orders bundle under a 16 s + 8 s retry deadline; the scheduler tick (scheduler.ts:857,884), protective stops, synthetic stops, order-replacement (6 sites) etc. call it too — 20 call sites, none cached. For an account that has been auto-trading for months this is N sequential Alpaca round-trips per snapshot (<=10 s TTL) at the p95 ~3 s/call STATUS.md documents, a likely contributor to the `getPortfolioBundle 8000+7000ms` timeouts that blank portfolio/positions/orders on the dashboard. The full history array (`orders`, dashboard.ts:1106) is then serialized into the snapshot every 15 s and JSON.parsed on th…
- **Recommendation** For the snapshot and scheduler paths fetch `status: "open"` plus `status: "closed", limit: 100` (or `after: <7d>`), one page each, and cache the closed page per account for the snapshot TTL; keep the full paginated walk only behind an explicit `getEquityOrderHistory()` used by a paged `/api/orders?before=` endpoint the Orders page calls on demand. Log page count per call to confirm the effect on the bundle timeouts in prod.

#### [market-data-integrity:mdi-02] Daily-bar cache served as 'fresh' for 3 calendar days: series lags up to two sessions and today's partial bar is persisted as an EOD close
- **Severity** P1 · **Kind** bug · **Surface** Cross-surface · **Area** price history / benchmark / technicals · **Effort** M · **Verdict** CONFIRMED · **Prior** 2026-08-08 #2557b (symptom gate only)
- **Where** `src/lib/history.ts:246`
- **Evidence** `if (localBars && localBars.length >= 2 && isBarSeriesFresh(localBars, 3, now)) { ... return stampedLocal; }` with isBarSeriesFresh: `ageMs <= maxStalenessDays*24h` on the bar DATE string
- **Why it matters** A Monday 9:35 fetch writes Monday's intraday partial bar into history_cache_eod (line 327 / persistEodBarsToCache) keyed 2026-08-17 (parses to 00:00Z); on Tue and Wed the series is still 'fresh' (<=72h) so no refetch happens until Thursday. Consumers get a stale last 'close' and missing sessions: SPY benchmark (benchmark.ts:345), computed technical signals, /api/history drilldown chart, and Alpaca's after-hours 'last close' quote fallback (alpaca.ts:565-569). The 2026-08-08 #2557b gate only hides >5-day lag.
- **Recommendation** Freshness = last bar date >= previous ET trading day AND (during/after today's session) today's bar refreshed within HISTORY_TTL; never persist the current session's bar as EOD (tag partial and always overwrite it); refetch when local last date < expected last session.

#### [market-data-integrity:mdi-03] Cascade stamps cache-served provider values with a 'now' fetchedAt/asOf; nasdaq-quote price/change/volume are cached 6h and win volume arbitration on fabricated recency
- **Severity** P1 · **Kind** bug · **Surface** Cross-surface · **Area** enrichment cascade provenance · **Effort** M · **Verdict** CONFIRMED
- **Where** `src/lib/data-providers.ts:1571`
- **Evidence** takeScalar: `fetchedAt: supplied?.fetchedAt ?? cascadeFetchedAt` and asOf falls to `cascadeFetchedAt`; nasdaq-quote: `cache.set(\`nasdaq-quote:${symbol}\`, { expiresAt: now + ttlMs(), data })` (ttlMs = NEWS_CACHE_TTL_MS, 6h)
- **Why it matters** nasdaq-quote's lastSalePrice/percentageChange/volume (parseNasdaqQuoteInfo:2521-2539) come back from a 6-hour cache with no timestamp, and the cascade stamps them as observed 'now'. `volume` arbitration is recency-first (evidence-facts.ts:100-107) so the stale cached volume beats Alpaca's real dailyBar.v with a genuine timestamp; when Alpaca snapshot is absent or times out (today's STATUS: 6s aborts), price/intradayChangePct fall to the hours-old cached value. The fake stamps then flow to symbol_field_latest and the 'Received <time>' tooltips (columns.tsx:22-30).
- **Recommendation** Store fieldObservations (or a writtenAt) in the enrichment cache and reuse them on hits; give price-family fields their own short TTL (like ALPACA_SNAPSHOT 30s) instead of NEWS_CACHE_TTL_MS; make recency compare real provider timestamps only (missing => lowest).

#### [ops-observability-admin:sre-01] No at-a-glance 'is the trader alive' panel on web/admin/iOS; run-state chip is intent, not liveness
- **Severity** P1 · **Kind** defect · **Surface** Cross-surface · **Area** admin overview / console header / iOS Home · **Effort** M · **Verdict** CONFIRMED
- **Where** `app/admin/page.tsx:99`
- **Evidence** Promise.allSettled([fetch("/api/admin/connections-health"), fetch("/api/admin/llm-usage?sinceDays=30"), fetch("/api/admin/rag-coverage?sinceDays=30"), fetch("/api/admin/server-metrics"), fetch("/api/chat-history?limit=10"), fetch("/api/admin/r2-usage")]);
- **Why it matters** The Operator Overview never reads /api/health or /api/admin/backup-status, so scheduler heartbeat age, trading-liveness (last completed run / consecutive failures), litestream state, release sha and process uptime are absent from the one page meant to be 'server-wide status'. On the console, StateChip/deriveStateInfo (app/console/lib/derive.ts:132-165, chrome.tsx:290) render 'Running · Autopilot' purely from policy.systemState — a stalled tick, a locked run request (today's 2026-08-18 incident) or five failed runs in a row all still read 'Running'. iOS Home shows the same intent-only pill (HomeView.swift:163). The signals exist server-side (getTradingLivenessSummary, scheduler:lastTick) but…
- **Recommendation** Add a first-row 'Engine Health' card to /admin (fetch /api/health public + /api/admin/backup-status): scheduler heartbeat age with warn >5m, per-account last completed run + consecutive failures, litestream state/age, release sha + uptime, last alert delivered. Feed a compact `engineHealth` {schedulerAgeSeconds, tradingLivenessDegraded, lastCompletedRunAt} into DashboardSnapshot/mobile snapshot so StateChip and the iOS pill can downgrade to 'Running · stalled' (warn tone) when heartbeat >5m or liveness degraded. iOS AdminPortalView (WKWebView of /admin) inherits the admin card for free.

#### [performance-accounting:perf-01] Fill ledger keeps the OLDEST 500 fills per source; newest fills silently vanish from every P&L figure
- **Severity** P1 · **Kind** bug · **Surface** Cross-surface · **Area** performance / fill ledger · **Effort** S · **Verdict** CONFIRMED
- **Where** `src/lib/db-fills.ts:332`
- **Evidence** "SELECT * FROM fill_events WHERE account_number = ? AND source = ? AND user_id = ? ORDER BY filled_at ASC LIMIT ?" (limit 500 everywhere: performance.ts:460-493, dashboard.ts:675, tax.ts:337, outcome-engine.ts:964, strategy.ts:801)
- **Why it matters** listPortfolioSnapshots already had this exact bug fixed ("ASC+LIMIT previously kept the OLDEST slice", db-fills.ts:222) but listFillEvents still returns the first 500 rows chronologically. Once an account passes 500 accounting fills per source (hourly cadence + broker-held stop fills + trims makes this plausible on the paper/Roth books today), every newer exit is dropped: Realized P&L, win rate, avg return, open-lot ledger (Tax), thesis/regime scorecards, decision grading (findRealizedLot), and the strategy's own learning context all freeze at an old state while positions keep closing. Because FIFO needs the full history, a 'newest 500' flip is also wrong.
- **Recommendation** Remove the cap for accounting reads (an account has at most a few thousand rows) or page through all rows; keep a cap only for display lists (performance.fills slice). Add a test that inserts >500 fills and asserts the last sell is realized. Consider a persisted per-symbol lot table so FIFO does not replay history each request.

#### [performance-accounting:perf-03] Short/cover cash sign is inverted in flow inference → phantom deposits/withdrawals on every short trade
- **Severity** P1 · **Kind** bug · **Surface** Cross-surface · **Area** benchmark / TWR cash-flow inference · **Effort** S · **Verdict** CONFIRMED · **Prior** 2026-08-06 C1 (bounded in PR #2589; root cause still open)
- **Where** `src/lib/cash-flows.ts:164`
- **Evidence** tradeCash += f.side === "sell" || f.side === "cover" ? f.notional : -f.notional; // comment: "buys/shorts reduce cash (negative), sells/covers increase it"
- **Why it matters** At Alpaca (and Tradier) a short sale CREDITS proceeds to cash (equity = cash + long_mv + short_mv, short_mv negative); a cover DEBITS cash. The code treats a short like a buy, so a $3k short yields flow = Δcash − tradeCash = +3k − (−3k) = +$6k 'deposit' and a $3k cover a −$6k 'withdrawal'. On a quiet day the #2557 sanity bound demotes it to 'inferred — unverified' (still shown as a bogus transfer row on Results); on a day the book moved ≥ 2% of the notional it is VERIFIED and TWR is computed against a fake denominator. The owner runs shorts (PG/T on 8/6), so this fires in production. prior_ref 2026-08-06 C1: the phantom-$36.5k mechanism was bounded (#2589) but this sign bug is a live source…
- **Recommendation** tradeCash += (side === 'sell' || side === 'short') ? +notional : −notional (buys and covers consume cash). Add tests: short open / cover with flat equity must infer zero flow.

#### [qa-test-strategy:qa-04] No API-to-iOS snapshot contract test; a server rename silently blanks phone lists
- **Severity** P1 · **Kind** risk · **Surface** Cross-surface · **Area** Contract tests (mobile snapshot) · **Effort** M · **Verdict** CONFIRMED
- **Where** `ios/SocraticTrade/MobileModels.swift:60`
- **Evidence** pendingProposals = try values.decodeIfPresent([PendingProposal].self, forKey: .pendingProposals) ?? [] ; positions … ?? [] ; iOS test decodes a hand-typed string `private let fullSnapshotJSON` (MobileModelsTests.swift:371); app/api/mobile/snapshot/route.ts GET has zero TS tests
- **Why it matters** The snapshot shape is an inline object literal in the route with no shared type; the only decoder test uses JSON written by hand, so a rename of `pendingProposals`, `recentCommands`, or a nested Proposal field on the server passes every TS and Swift test while the iPhone shows empty proposals/positions (wrong information, not an error).
- **Recommendation** Add test/mobile-snapshot-contract.test.ts that seeds a temp DB + `broker:"test"` account, calls the route GET, asserts the exact top-level key set, and writes/compares a committed fixture test/fixtures/mobile-snapshot.json; add that JSON as a bundle resource of SocraticTradeTests and decode it in MobileModelsTests (replacing the inline string). Reuse the apns-deep-link-contract.test.ts pattern of reading Swift source for CodingKeys.

#### [web-accessibility:a11y-02] Light theme: tone-colored text on soft fills and the Stop/Start button fail WCAG AA (3.0–4.3:1); --con-info fails on every surface
- **Severity** P1 · **Kind** defect · **Surface** Cross-surface · **Area** console/tokens · **Effort** M · **Verdict** PARTIAL — Contrast math independently reproduced and confirmed exact: chip-pos #157f4e on soft #e5f1ec = 4.34:1, con-info #0891b2 on white = 3.68:1 (matches expert's numbers precisely via WCAG relative-luminance formula). Real, current-main P1 bug. BUT already fixed on… · **Prior** 2026-08-06 #2561 (1) — still open on main
- **Where** `app/console/console.css:559`
- **Evidence** .con-chip-pos { color: var(--con-pos); background: var(--con-pos-soft); } … .con-chip-info { color: var(--con-info) … } (tokens :40-50: --con-pos #157f4e, --con-neg #d1332e, --con-warn #9a6209, --con-info #0891b2, --con-none #64748b)
- **Why it matters** Computed (WCAG relative luminance, soft = token mixed over #fff): chip pos #157f4e on #e5f1ec = 4.34, neg #d1332e on #faebea = 4.29, warn #9a6209 on #f3ece1 = 4.34, none #64748b on #f0f1f3 = 4.21, info #0891b2 on #e1f2f6 = 3.19 — all at 11px/600 (needs 4.5). Over --con-surface-2 they drop further (3.9–4.0; info 2.97). .con-stop-btn text (neg on neg-soft, :486-490) = 4.29 and .con-start-btn (:494-498) = 4.34 at 12.5px/700 — the money-critical chrome control. --con-info #0891b2 fails even on plain white (3.68) and --con-info-contrast white-on-#0891b2 = 3.68 (:46-47). Reality banner 'NO ACCOUNT' word (--con-none on none-12% wash, :597/:607) = 3.74. Header comment (:19-21) promises AA. Dark pal…
- **Recommendation** Darken the light tone tokens one step and validate against the SOFT fill, e.g. --con-pos #0f6b41, --con-neg #b8281f, --con-warn #7f5008, --con-info #036d8a (also fixes info-contrast), --con-none #56657c; re-run the ratio script against soft@white and soft@surface-2 and against .con-stop-btn/.con-start-btn. Add a unit test that computes these pairs from console.css so regressions fail CI.

#### [desktop-web-ux:dweb-01] Home proposal rows: dead/wrong Approve button, wrong status tones, auto-firing live confirm
- **Severity** P1 · **Kind** bug · **Surface** Desktop web · **Area** Home dashboard / proposal lifecycle · **Effort** M · **Verdict** CONFIRMED
- **Where** `app/console/page.tsx:79`
- **Evidence** decisionFromProposal(`${latest?.runId}-${item.proposal.symbol}-${item.status}`, …) … const isPendingOrFailed = row.status === "pending" || row.status === "failed" || row.status === "blocked";
- **Why it matters** Latest-run rows get a synthetic id, yet the sheet renders a primary 'Approve Proposal' for blocked rows and POSTs /api/proposals/<runId>-<sym>-blocked/approve (404). Real run statuses are proposed/blocked/error (strategy.ts results.push), so chipTone (line 1025) paints 'error' (label 'Failed') and 'proposed' GREEN like filled; the typed live-confirm input auto-submits on onChange match (1080-1084) with no Reject, no LiveTag/BROKER stamp — inconsistent with the Approvals card contract. Also `latest.proposals.slice(0,5).map(...) ?? pending…` (77-80) never falls back when the array is [], so older pending proposals vanish from Home.
- **Recommendation** Make Home rows read-only summaries: drop the Approve path (or route real pending ids to the ApprovalCard/LiveApproveSheet), map tone by isNotPlacedStatus/'error'/'proposed', link to /console/decisions/[id] via row.href, and use `.length ? … : pending` for the fallback.

#### [frontend-performance:dweb-01] /console/connections client bundle ships the server DB layer (SQL schema, migrations, key-store logic): 1.05 MB gz vs ~476 KB for every other console page
- **Severity** P1 · **Kind** defect · **Surface** Desktop web · **Area** bundle / server-client boundary · **Effort** S · **Verdict** CONFIRMED
- **Where** `app/console/settings/brokers.tsx:12`
- **Evidence** brokers.tsx:12 `import { mergeAccountCapabilities } from "@/lib/venue-contract"` -> venue-contract.ts:22 `import { resolveSourceBool } from "./source-settings"` -> source-settings.ts:15-16 `import { LOCAL_USER } from "./db-api-keys"; import { getUserSetting, setUserSetting } from "./db-settings"` (db barrel)
- **Why it matters** Measured on the 2026-08-16 production build in a sibling worktree: `app/console/connections/page-*.js` = 1,063,218 B raw / 285,282 B gz and contains 120 `CREATE TABLE`, 440 `SELECT `, 363 `getDb`, 11 `ENCRYPTION_KEY`, 31 `process.env` references; it also drags in a zod+drizzle+cheerio chunk (729 KB raw / 235 KB gz) and a crypto-browserify polyfill chunk (326 KB raw / 100 KB gz) that no other route loads. Total JS for /console/connections is 1,052,272 B gz vs 475,762 B gz for /console/approvals. The webpack `better-sqlite3: false` alias only stubs the native binding, so all the JS around it (schema, migrations, API-key store, apns) is compiled into the browser bundle: slow page on phone, and…
- **Recommendation** Make `mergeAccountCapabilities`/`knownBrokerLimits` a pure module (move the `publicParked` check in venue-contract.ts:250 behind an injected flag or a separate server-only helper), so brokers.tsx never reaches source-settings/db. Then guard the barrel: add `import "server-only"` to src/lib/db.ts, db-settings.ts, db-api-keys.ts and an eslint `no-restricted-imports` rule for app/console/** -> `@/lib/db*`, `@/lib/source-settings`, `@/lib/server-knobs`. Verify by grepping `.next/static/chunks/app/console/**` for `getDb`/`SELECT ` after `next build`.

#### [ops-observability-admin:sre-03] Backup status can lie: litestream kill-switch markers are invisible to /api/health and Backup Status, and the R2 'writes paused' card ignores the B2 cutover
- **Severity** P1 · **Kind** bug · **Surface** Desktop web · **Area** backups / admin overview · **Effort** M · **Verdict** CONFIRMED · **Prior** 2026-08-06 A2
- **Where** `scripts/coolify-prod-start.sh:188`
- **Evidence** LITESTREAM_DISABLE_MARKER="${DATA_DIR}/.litestream-disabled" if [ -f "$LITESTREAM_DISABLE_MARKER" ]; then log "Generic kill-switch marker present ... starting WITHOUT litestream replication"
- **Why it matters** Two markers make the boot script start without litestream (generic .litestream-disabled, owner override 2026-08-13, lines 188-192; R2 .litestream-r2-disabled only when the endpoint is Cloudflare R2, lines 200-212). Neither marker is modeled anywhere in src/ (rg 'litestream-disabled' in src/app returns nothing): /api/health reports litestreamState 'unknown' and pages 'Litestream runtime status is unavailable — replication freshness cannot be confirmed' (route.ts:459-460) every 12h, and /admin/backups shows an unexplained 'unknown' — the true state 'backups deliberately OFF since <date>' is never said. Conversely app/api/admin/r2-usage/route.ts:29 returns replicationDisabled from raw marker e…
- **Recommendation** Add `litestreamPausedBy: 'generic-marker'|'r2-marker'|null` (read the same two paths + endpoint check as the boot script) to runtime-health and surface it in /api/health.checks.storage, /api/admin/backup-status and the Backups page as an explicit 'Replication intentionally off since …' state with its own alert key (daily reminder, not the 'unavailable' text). Make replicationDisabled in r2-usage route = marker && isLitestreamReplicaCloudflareR2(); drop or reword the overview card copy now that the replica is B2.

#### [performance-accounting:perf-02] Home Day P&L is zeroed on ordinary trading days: flow inference runs with NO fills and reads market P&L as a deposit/withdrawal
- **Severity** P1 · **Kind** bug · **Surface** Desktop web · **Area** dashboard Day P&L · **Effort** S · **Verdict** CONFIRMED
- **Where** `app/console/lib/derive.ts:443`
- **Evidence** const flowMap = inferExternalCashFlows([baseline, fakeCurrent], []); ... const pnl = currentEquity - baseline.equity - flow; — cash-flows.ts:204-216: residualIsTransfer = |Δequity| >= max(threshold, 0.25*swapped) → flow = deltaEquity
- **Why it matters** With an empty fill list, tradeCash is 0, so any day with a trade lands in the 'cash and positions moved opposite' branch. Example on a $100k book: buy $2k, market +$1k → Δcash −2k, Δpos +3k, Δequity +1k; swapped=2k, 0.25×2k=500 ≤ 1k → flow=+$1,000 'deposit'; sanity bound (5×|Δequity|=5k) does not catch it → Day P&L renders $0.00 (0.00%) with cashFlowAdjusted=true. Any day where |Δequity| ≥ 0.5% of equity and ≥ 25% of traded notional prints the wrong headline number. The benchmark path passes fills (dashboard.ts:718) so it is mostly immune; deriveDayPnl is not.
- **Recommendation** Pass the snapshot's fills (snapshot.performance.fills — filter to accounting statuses, or extend the summary with the current-day fills) into inferExternalCashFlows in deriveDayPnl; better, use the broker's own previous-close equity (Alpaca account.last_equity) as the Day P&L baseline and drop inference for the day figure. Add a unit test for the buy-$2k/+$1k case.

#### [ux-copy-editor:copy-03] Macro page says below-median buys are "hard-vetoed" in risk-off/crisis regimes; the code is advisory tag-not-drop
- **Severity** P1 · **Kind** bug · **Surface** Desktop web · **Area** copy accuracy / Macro · **Effort** S · **Verdict** CONFIRMED
- **Where** `app/console/macro/indicators.ts:698`
- **Evidence** "VIX above 30 — panic-level volatility. Buy ideas scoring below the scan median are hard-vetoed, ..." (also :706); src/lib/strategy-risk.ts:187-197 tags preVetoReasons and keeps the proposal
- **Why it matters** Wrong information about what the system will do with the owner's money in a crisis regime — the regime rule was converted to an overridable advisory pre-veto (strategy-risk.ts comment: "advisory pre-veto (tag-not-drop). Was a hard `continue` drop"). Also contradicts the owner's "nothing hard but the account boundary" ruling in user-facing copy.
- **Recommendation** Reword both regime hints: "Buy ideas scoring below the scan median are flagged with an advisory veto the agent must override with a thesis (Socratic override mode); the optional crisis exposure cap can shrink every newly opened position." Grep the rest of macro/indicators.ts and settings/help.tsx for 'hard' and align with the advisory reality.

#### [web-accessibility:a11y-01] Home 'Recent decisions' rows are click-only <div>s — details + Approve flow unreachable by keyboard
- **Severity** P1 · **Kind** bug · **Surface** Desktop web · **Area** console/dashboard · **Effort** S · **Verdict** CONFIRMED
- **Where** `app/console/page.tsx:1029`
- **Evidence** <div className="cursor-pointer hover:bg-… flex items-center justify-between gap-2 …" onClick={() => setOpen(true)}> … View details <ArrowRight size={12} />
- **Why it matters** ProposalRow (page.tsx:968) opens the Proposal Details sheet — which contains the Approve/typed-confirm path (page.tsx:1075+) — from a plain div with no role, tabIndex, or key handler. Keyboard and switch users cannot open it at all; screen readers see static text 'View details'. The row nests a SymbolButton, so the outer element cannot simply become a button.
- **Recommendation** Make 'View details' a real <button type="button"> (or <Btn variant="ghost" size="sm">) inside the row that calls setOpen(true), keep the row-level onClick as a pointer convenience, and give the sheet an aria-labelledby title (already passed).

#### [ios-engineering:ios-01] Sign-out / expiry / account deletion never clears the UserDefaults snapshot cache — next launch boots the previous account's data and mislabels an explicit sign-out as "session expired"
- **Severity** P1 · **Kind** bug · **Surface** iOS · **Area** session/storage · **Effort** S · **Verdict** CONFIRMED
- **Where** `ios/SocraticTrade/MobileStore.swift:149`
- **Evidence** init: `let cached = previewSnapshot ?? Self.loadCachedSnapshot(); snapshot = cached; isAuthenticated = cached != nil` — and clearLocalSession() (628-650) sets snapshot=nil but never `UserDefaults.standard.removeObject(forKey: cacheKey)`
- **Why it matters** After `signOut()` (or `confirmAccountDeletion`, or a 401 wipe) the cache row survives, so a cold relaunch derives `isAuthenticated = true` from a stale blob: online, `load()` 401s and shows "Your session expired. Sign in again." to a user who deliberately signed out (shouldReportSessionExpiry is true because isAuthenticated was true); offline, `hasInitialized` flips and MobileControlView renders the previous account's holdings, orders, proposals and email. Same failure after account deletion — the deleted account's portfolio remains on disk.
- **Recommendation** In `clearLocalSession()` (and `signOut`/deletion) `UserDefaults.standard.removeObject(forKey: Self.cacheKey)`; do not infer authentication from the cache alone — persist an explicit signed-in flag or check `HTTPCookieStorage.shared` for the session cookie before treating the cache as authenticated; treat a cache-only launch as `isAuthenticated=false` + splash until the first successful load.

#### [ios-hig-ux:ios-01] Guardrail/account edits inside Account & Settings fail silently — store.error is never rendered in the sheet, and the scaffold banner is off-screen or auto-cleared elsewhere
- **Severity** P1 · **Kind** defect · **Surface** iOS · **Area** iOS error surfacing (Settings sheet, Guardrails, Home) · **Effort** M · **Verdict** CONFIRMED
- **Where** `ios/SocraticTrade/HomeView.swift:766`
- **Evidence** HomeView.swift:766 `Form { identitySection ... GuardrailTighteningSection() ... }` (no store.error view); PolicyTightening.swift:623 `store.error = "Type \(pending.phrase) to confirm."`; MobileStore.swift:292 `error = nil`
- **Why it matters** Every failure from account.activate, policy.patch (cap raise/lower, Ask-First↔Autopilot), the typed-phrase mismatch, and custom-cap validation is written to `store.error`, but AccountSettingsView (a sheet) contains no InlineErrorBanner — the only renderer is SnapshotScaffold (AppComponents.swift:580) which sits at the TOP of the underlying tab's scroll view. On the Guardrails tab the edit card is the LAST card, so the user is scrolled to the bottom when the banner appears at the top. Worse, `load()` sets `error = nil` on every successful refresh and SSE events trigger `scheduleReload()`, so a banner can vanish within seconds. Net effect: the owner taps a money-adjacent change, sees nothing,…
- **Recommendation** Render `store.error` inside AccountSettingsView (a top Section or `.alert(item:)`), and change SnapshotScaffold to surface action errors as an overlay/toast anchored to the safe area (or `.alert`) instead of a card at scroll offset 0. Do not clear an action error in `load()`; clear it only on dismiss/timeout or when the same operation succeeds. Add an XCTest that a failed `policy.patch` leaves a visible error state.

#### [qa-test-strategy:qa-01] iOS unit tests never execute in CI or before TestFlight ship
- **Severity** P1 · **Kind** risk · **Surface** iOS · **Area** CI / iOS test target · **Effort** S · **Verdict** CONFIRMED
- **Where** `.github/workflows/ios-build.yml:73`
- **Evidence** xcodebuild build \ -project 'ios/Socratic Trade.xcodeproj' -scheme SocraticTrade -destination 'generic/platform=iOS' — no `xcodebuild test` anywhere in .github/ or scripts/
- **Why it matters** 15 XCTest files (2,416 lines: MobileModels decode, PolicyTightening, OrderCancel, PushNotification, DeepLink) only run when a human remembers; ios-ship.yml archives every 30 min from main without them, so a decode/contract regression ships to the phone.
- **Recommendation** Add a second step to ios-build.yml: `xcodebuild test -project 'ios/Socratic Trade.xcodeproj' -scheme SocraticTrade -destination 'platform=iOS Simulator,name=iPhone 17 Pro' CODE_SIGNING_ALLOWED=NO` (the Mac runner already has simulators), and have scripts/ios-scheduled-ship-gate.sh refuse to archive when the last ios-build test run on that sha is not green.

#### [qa-test-strategy:qa-02] Swift build result is not part of the required merge gate
- **Severity** P1 · **Kind** risk · **Surface** iOS · **Area** CI ruleset · **Effort** M · **Verdict** CONFIRMED
- **Where** `.github/workflows/ci.yml:305`
- **Evidence** ruleset main-protection: required_status_checks:[{context:"verify"}]; verify job needs:[classify, verify-hosted] only — ios-build.yml runs separately and is advisory
- **Why it matters** AGENTS.md documents a merge that produced a duplicate `@Binding pendingDeepLink` init and did not compile while the full green gate passed (2026-08-13). Today the same class merges again because a red `xcodebuild (unsigned)` cannot block.
- **Recommendation** Add `ios-changed` to the classify job outputs (git diff --name-only ... | grep '^ios/'), move the xcodebuild job into ci.yml gated on it, and make the `verify` gate step read its result: skipped-because-unchanged = pass, failure = fail. Keeps non-iOS PRs off the Mac runner while making iOS breakage un-mergeable.

#### [trading-money-path:tsx-03] iOS shows 'Approved — waiting for desk refresh' (green check) for approvals that returned busy/blocked/rejected_by_broker/not_placed/error
- **Severity** P1 · **Kind** bug · **Surface** iOS · **Area** approval flow feedback · **Effort** S · **Verdict** CONFIRMED
- **Where** `src/lib/mobile-api.ts:966`
- **Evidence** case "proposal.approve": return executeProposal(String(payload.proposalId), command.userId, {...}); … return toPublicMobileCommand(finishCommand(command, "succeeded", result)); (:1080)
- **Why it matters** executeProposal RETURNS (does not throw) objects like {status:'busy'} (strategy lock held by a running cycle), {status:'blocked'}, {status:'error'} (broker declined), {status:'not_placed'}. runCommand passes them straight to finishCommand('succeeded'), so ios/SocraticTrade/MobileStore.swift:214 maps the command to .succeeded and ProposalsView.swift:481 renders 'Approved — waiting for desk refresh.' The phone user believes a live order went in when nothing was placed; the only truth is buried in Activity. Console handles the same statuses correctly (approval-card.tsx:347-366).
- **Recommendation** In runCommand('proposal.approve'), inspect result.status: 'placed'/'filled'/'paper'/'proposed' → succeeded; 'busy'/'blocked'/'error'/'not_placed'/'rejected_by_broker' → finishCommand('failed', result, reasons.join(' ')). Mirror the console's finish() wording in the failed message. Add a mobile-api test for the busy case.

#### [ux-copy-editor:copy-04] iOS tells the user to edit the universe "on the Strategy page" — the app has no Strategy screen and on the web the universe editor lives under Guardrails
- **Severity** P1 · **Kind** bug · **Surface** iOS · **Area** copy accuracy / first-run setup · **Effort** S · **Verdict** CONFIRMED
- **Where** `ios/SocraticTrade/HomeView.swift:94`
- **Evidence** ? "Include an index or symbols on the Strategy page." (also HomeView.swift:109, GuardrailsView.swift:86 "Universe edits stay on the Strategy page."); web editor: app/console/guardrails/page.tsx:425 <AdvancedGroup title="Universe">, derive.ts:707 ctaLabel "Open Guardrails · Universe"
- **Why it matters** The iOS setup checklist and Guardrails screen misdirect a first-run user to a screen that does not exist in the app and to the wrong page on the website (Strategy has models/prompts/presets; the universe is on Guardrails → Advanced rulebook → Universe). Wayfinding is broken exactly at the step that blocks the first run.
- **Recommendation** Change all three strings to name the real location and surface: "Include an index or symbols on the website under Guardrails → Universe (socratictrade.com/console/guardrails)." Consider a deep link/button that opens that URL in Safari or the in-app web view.

#### [mobile-web-ux:mweb-01] Phone chrome bar over-budget since run-state label became "Stop Agent"/"Resume Agent" — account scope squeezed to ~0-15px at 360-390px
- **Severity** P1 · **Kind** defect · **Surface** Mobile web · **Area** console chrome bar (shell.tsx ChromeBar / chrome.tsx ScopeSelector, StateChip, RunStateButton) · **Effort** S · **Verdict** CONFIRMED
- **Where** `app/console/components/chrome.tsx:326`
- **Evidence** const label = state === "halted" ? "Start Agent" : state === "close_only" ? "Resume Agent" : "Stop Agent"; // chrome.tsx:203 still says "at 360px the leftover is ~73px"
- **Why it matters** The phone bar was width-tuned when the run-state control was a short STOP/Start (#1708 comment at chrome.tsx:199-205: scope is min-w-0 flex-1 and takes whatever is left). #2757 (2026-08-16) lengthened the label to "Stop Agent"/"Resume Agent" (~125-140px incl. icon+padding, flex-shrink:0 in .con-stop-btn), while the coarse-pointer rule gives Run-once 44x44 and StateChip stacks "Paused"/"market closed" (~90px), plus 32px cmdK + 32px avatar + gaps/padding (~54px). Fixed widths sum to ~375px, so at 360px the bar overflows and at 390px (iPhone 14/15) the account scope — the ONE control that says which brokerage account is loaded — gets ~13px and truncates to nothing. For a multi-account trader t…
- **Recommendation** On phones drop the noun: render `Stop`/`Start`/`Resume` below sm (`<span className="hidden sm:inline"> Agent</span>`) or icon-only with aria-label, and give ScopeSelector a small min-w floor (e.g. 84px) so the account label always shows at least a broker name + last-4. Add a 360px/390px width check to the phone-chrome test.

### Could Be Improved (0)

## 4. P2 Findings (148) — Degraded UX / Quality

### Needs Fixing (110)

- **[ops-observability-admin:sre-05] /api/health cannot represent a broker outage when Alpaca runs on user-lane keys (the production case)** — `app/api/health/route.ts:235` · defect · API · effort S · CONFIRMED
  - Why: The dependencies loop skips every keySource==='user' lane, so a hard-stopped user-lane alpaca-broker (expired/rotated credentials, 5 consecutive failures) produces no `dependencies.alpaca-broker` entry at all — not ok:false, not degraded — while the header comment (line 205) says Alpaca is one of only two critical liveness deps. The route itself documents that 'prod uses Conne…
  - Fix: Include user-lane summaries in the projection as `dependencies.<service>` with ok/degraded booleans (no identity), i.e. treat 'no healthy configured lane of any keySource' as ok:false + degraded:true for broker services (still never 503 — a restart cannot fix credentials). Add a `brokerOrderPathHealthy` boolean derived from the broker-health pause marker.

- **[qa-test-strategy:qa-07] Single-proposal approve, replace-market, pause/enable routes have no route-level tests (63/122 routes untested)** — `app/api/proposals/[id]/approve/route.ts:11` · risk · API · effort M · CONFIRMED
  - Why: These are the buttons the owner presses daily. app/console/lib/api.ts:335 and app/console/orders/api.ts:122 branch on the exact strings `LIVE_CONFIRMATION_REQUIRED` vs `live_confirmation_required`; nothing pins those payloads or the 404-plain-text vs 400 mapping, so a refactor can break the live-confirmation dialog while all lib-level tests stay green.
  - Fix: Add test/proposal-approve-route.test.ts, test/replace-market-route.test.ts, test/strategy-pause-enable-route.test.ts using `await import("../app/api/.../route")` + `new Request(...)` like order-confirmation-status.test.ts:254, asserting status codes and the exact 409 JSON keys the console/iOS clients read; add a filesystem-enumerating test that lists app/api/**/route.ts and fa…

- **[backend-reliability:berel-08] Scheduler tick has no re-entrancy guard: setInterval fires every 60 s while a tick can await serial per-account broker health calls and then all strategy runs, so ticks overlap and unguarded per-tick lanes stack under exactly the slow-broker conditions of 08-18** — `src/lib/scheduler.ts:411` · defect · Backend · effort S · CONFIRMED
  - Why: checkBrokerHealth (:953) does getAccounts+getPortfolio per account with a 16+8 s worst case each, serially inside the loop; with 3 accounts that alone can exceed 60 s, and the tick then awaits every strategy run (:1080, minutes). The next interval starts a fresh tick that re-runs stale sweeps, material-event drain, and per-account proposal-expiry / pending-fill-reconcile (:942…
  - Fix: Add a globalThis-pinned `tickInFlight` flag: skip (and journal 'skipped: previous tick still running' with its age) instead of overlapping; wrap checkBrokerHealth in withDeadline(15s) and run the per-account health probes with Promise.allSettled instead of serially; move the strategy-run await off the tick critical path (fire-and-forget with the existing lock).

- **[backend-reliability:berel-10] Each embed_queued resume slice re-reads the raw filing, JSON.parses sections and chunks artifacts and runs two full-scan FTS COUNTs before writing 6 chunks — fixed sync overhead repeated ~150 times per large filing** — `src/lib/rag/sec-ingest-worker.ts:353` · defect · Backend · effort S · CONFIRMED
  - Why: With FTS_MIRROR_MAX_CHUNKS_PER_TICK = 6 a 933-chunk filing needs ~156 slices; every slice re-does filingTextFromParsedSections(sections, rawContent) (:381), parses chunks.json (:444-447) and two COUNT(*) scans of the FTS table (:355, :457-461), then writes an attempt row (db-rag-ingest.ts:655) and releases the lease. The bounded write is now dwarfed by unbounded, un-yielded pr…
  - Fix: Skip the raw/sections parse on the FTS-only resume path (storeAlreadyDone) — only chunks.json is needed; keep a small in-process LRU of parsed ftsRows keyed by taskId across slices; persist the resume offset in the task row (payload receipt already carries ftsMirrorOffset) instead of COUNT; and once berel-01 lands raise the per-tick chunk cap back toward 20.

- **[frontend-performance:dweb-04] `market_scan` audit still stores the full scan on every /console/scan mount (even cache-served scans) — the 2026-08-02 audit hygiene only bounded strategy_run** — `app/api/scan/route.ts:116` · defect · Backend · effort S · CONFIRMED · prior docs/rollouts/2026-08-02-audit-write-hygiene.md
  - Why: `useLiveScan` fetches /api/scan every time the Scan tab mounts; the route audits the full MarketScan (topCandidates with 89-field MarketQuote objects + quotesBySymbol for the whole ranked set) even when `scanMarket` served it from its 5-minute cache (`DEFAULT_CACHE_TTL_MS`, market.ts:102) — so tabbing away and back writes another ~600 KB-2.7 MB row into audit_events (the table…
  - Fix: Audit `summarizeMarketScanForAudit(scan)` (audit-bounded-run.ts) for `market_scan` and persist the full scan once in a dedicated single-row-per-scope table (or reuse `symbol_field_latest`), keyed by scan.generatedAt; skip the audit write entirely when `scan.cached` is true and generatedAt matches the last stored one.

- **[llm-agent-architecture:llm-05] Approved coach directives are appended to the ACTIVE account's strategy prompt (and user/profile prompt) regardless of the pending row's account** — `src/lib/learned-context/store.ts:401` · bug · Backend · effort S · CONFIRMED
  - Why: pending.connectedAccountId is ignored; getStrategyPrompt/setStrategyPrompt without an account resolve the is_active account (db-profiles.ts:323-325, 611-618) and also overwrite the user-level setting and active profile with that account's prompt + block. A directive spoken while Roth was active lands on Paper if Paper is active at approval time (human route or the daily review…
  - Fix: Thread pending.connectedAccountId into getStrategyPrompt/setStrategyPrompt (and show the target account on the approvals card); add a two-account test.

- **[llm-agent-architecture:llm-08] Strict-parse path trusts provider schema enforcement; json_object transports (DeepSeek, Gemini fallback) can deliver off-playbook tags / non-venue types / null enums that sanitizeProposals silently normalizes** — `src/lib/strategy.ts:6115` · defect · Backend · effort S · CONFIRMED
  - Why: All the type/enum/range gates (playbook membership, venue.orderTypes, allowedSides, tif/marketHours enums, confidence 1-100, stopPlan shape, projection to schema keys) live only in filterRepairedProposals on the jsonrepair branch (:6149-6155). llm-call.ts:511/517 downgrades DeepSeek and untranslatable Gemini schemas to json_object with no schema, so a valid-JSON reply with tra…
  - Fix: Run filterRepairedProposals (validation + projection) on the strict path too, auditing drops as strategy_bull_schema_invalid_dropped; keep the repair path as-is.

- **[llm-agent-architecture:llm-10] Hardcoded 'dead' OpenRouter model list drops claude-fable-5 and kimi-latest from rotation although the live catalog serves both slugs today** — `src/lib/model-rotation.ts:73` · bug · Backend · effort S · CONFIRMED
  - Why: normalizeOpenRouterModelId maps claude-fable-5 → anthropic/claude-fable-5 and kimi-latest → ~moonshotai/kimi-latest; both are present in OpenRouter's /models today (checked via the read-only catalog: anthropic/claude-fable-5, ~moonshotai/kimi-latest alias → kimi-k3). The comment cites the untilded anthropic/claude-fable-latest 404, which is not the slug the code sends. applyRo…
  - Fix: Delete the hardcoded dead list; rely on the live allowlist with a cached TTL and a per-slug cooldown after a 404. Make learningReviewModel explicit-only (blank → skip with an honest audit).

- **[llm-agent-architecture:llm-13] Learned-context semantic gate resolves its LLM via env CHAT_LLM (getLLM) — MockLLM in production, so the 'on by default' second-layer risk classifier is silently inert** — `src/lib/learned-context/semantic-gate.ts:124` · defect · Backend · effort S · CONFIRMED
  - Why: getLLM (chat/llm.ts:741-769) needs CHAT_LLM + CHAT_LLM_MODEL env, otherwise returns MockLLM; owner rules put LLM keys on Connections, not env, so in prod the gate calls the mock, gets 'Mock Response: …', parseGateTier → null → 'fact'. The file header claims the gate is ON by default and can only upgrade fact→risk; in practice every keyword-'fact' candidate skips the semantic c…
  - Fix: Resolve via resolveLlmEndpoint like salience-llm, and audit 'learned_context_semantic_gate_skipped' when no real model resolves; add a test that MockLLM never counts as a gate run.

- **[market-data-integrity:mdi-08] Dynamic universes (NYSE/Nasdaq Composite, FT Wilshire 5000) include preferreds, baby bonds and units — ~20% of the NYSE screener** — `src/lib/market.ts:1360` · risk · Backend · effort S · CONFIRMED
  - Why: Symbols like 'AGM^D' or 'BRKRP' rank as candidates (marketCap is the parent's), every enrichment provider is called on symbol forms Alpaca/Yahoo reject (wasted quota, provider_failures noise), and a proposal on such a symbol would 422 at the broker. The universe floor only filters price/dollar-volume.
  - Fix: In toMarketQuote (or applyUniverseFloor) drop rows with '^' in symbol or names matching /Preferred|Depositary|Notes?|Debentures|\bUnits?\b|Warrants?|Rights?/i, and add a `commonStockOnly` toggle defaulting on.

- **[market-data-integrity:mdi-10] No early-close (1:00 PM ET) calendar: sessions read 'regular' until 4 PM on half-days** — `src/lib/market-hours.ts:35` · risk · Backend · effort S · CONFIRMED
  - Why: Day after Thanksgiving, Christmas Eve and July 3 (when a weekday) close at 13:00 ET; the scheduler will keep running 'regular-hours' cycles and any market/limit orders placed 1-4 PM queue to the next open (surprise fills at Monday's open); cache TTL extension and 'next open' hints are wrong for those afternoons.
  - Fix: Add getEarlyCloses(year) (NYSE published list) and make CLOSE/POST_CLOSE date-aware; treat 13:00-16:00 on those days as 'closed'.

- **[market-data-integrity:mdi-12] Yahoo v7 batch quote runs without the crumb session the repo says v7 now requires; failure is silent and the fallback fans out unbounded per-symbol chart calls** — `src/lib/yahoo-finance.ts:147` · risk · Backend · effort S · CONFIRMED
  - Why: If v7 401s, cascade Level 3 (quotes-cascade.ts:309) and the empty-screener whole-universe fallback (market.ts:1460) silently return nothing and degrade to fetchQuoteOnlyMarketQuotes = `Promise.all(requested.map(...fetchYahooFinanceQuote))` (market.ts:1484) — hundreds of concurrent chart calls for a 500-name universe, which Yahoo 429s, ending in ScanQuotesUnavailableError. (Liv…
  - Fix: Reuse the YahooFinanceEnrichmentProvider crumb/cookie for v7 (or remove Level 3), record non-ok statuses via db-health, and bound single-chart concurrency (e.g. 4-8) with the existing politeFetch/backoff helper.

- **[market-data-integrity:mdi-13] Snapshot/benchmark day keys use the UTC date: evening (post-market) snapshots align to the NEXT session's SPY close** — `src/lib/cash-flows.ts:33` · defect · Backend · effort S · CONFIRMED
  - Why: A strategy run/snapshot after 7 PM CDT (6 PM CST) is keyed to tomorrow's date, so `closeOnOrBefore(date)` (benchmark.ts:75-95) pairs Monday-evening equity with Tuesday's SPY close (off by one trading day) and two same-session snapshots become two 'dates'. economic-calendar.ts:51-57 (utcDay) and cboe.ts:47 use the same UTC-day convention.
  - Fix: Key by America/New_York calendar day using the existing etDateParts helper (market-hours.ts:211) for snapshots, bars and calendar 'today'.

- **[ops-observability-admin:sre-04] Docker HEALTHCHECK gives the heavy /api/health only 5s; documented 6–13s event-loop stalls can flip the container unhealthy and fail deploy swaps** — `Dockerfile:75` · risk · Backend · effort S · CONFIRMED
  - Why: The probe the container's health depends on does per-lane DB aggregates (getServiceHealthSummaries: 4 queries per lane), a litestream IPC socket read, a 256KB log-tail scan (route.ts:398), a trading-liveness scan over up to 200 runs per account (route.ts:128), and an outbound OpenRouter credits fetch of up to 1.5s (route.ts:305). STATUS.md (2026-08-18) records '/api/health 200…
  - Fix: Add a minimal `/api/health/live` (DB ping + release identity, no network, no file scan, ≤50ms) and point the Dockerfile HEALTHCHECK at it; keep the rich /api/health for UptimeRobot/deploy-verify. Alternatively raise --timeout to 15s and move the OpenRouter credits read fully out of the request path (scheduler-cached only).

- **[ops-observability-admin:sre-07] Deploy drift is still only detectable by manually running verify-deploy-sha.sh** — `scripts/verify-deploy-sha.sh:6` · risk · Backend · effort S · CONFIRMED · prior 2026-08-06 A1 follow-up; in-flight PR #2796
  - Why: No workflow under .github/workflows references verify-deploy-sha (rg over .github returns nothing), so the 2026-08-06 all-day deploy freeze class (main ahead of prod for 14h) would again be found only by a human running the script. Merge==live is the standing protocol, but nothing asserts it.
  - Fix: Land the scheduled freshness check (a 20-min GitHub cron that fails when the oldest undeployed main commit is >1h and pages via Sentry/Slack) — this is exactly PR #2796; merge it and add its status to the admin Engine Health card (release sha vs origin/main age).

- **[ops-observability-admin:sre-08] All app + litestream stdout is appended forever to a log file on the persistent data volume with no rotation** — `scripts/coolify-prod-start.sh:223` · risk · Backend · effort S · CONFIRMED
  - Why: Because `next start` is litestream's -exec child it inherits stdout/stderr, so every console.log from the app (scheduler lines each tick, provider/RAG chatter — 56 console calls in scheduler.ts alone) is teed with -a into $DATA_DIR/litestream-runtime.log (line 49) on the same volume as app.db. Nothing truncates or rotates it (rg 'logrotate|truncate' in the script returns nothi…
  - Fix: At boot, keep only the last N MB (`tail -c 20M` into a temp and mv back) or pipe through `rotatelogs`/`multilog`; better, tee only litestream's own stderr (run next start with its own stdout) so the file stays a litestream log. Add the file size to /api/admin/backup-status.

- **[ops-observability-admin:sre-14] Three backup layers, zero restore drills — nothing ever proves the B2 replica or R2 cold snapshot restores** — `src/lib/r2-cold-snapshot.ts:1` · risk · Backend · effort M · CONFIRMED
  - Why: Monitoring covers freshness of LTX levels and log markers, but a replica that is fresh yet unrestorable (corrupt LTX chain, wrong bucket creds at restore time, WAL mismatch) would pass every check. The documented rollback path is 'restore from the replica'; the 2026-08-11/12 wedges show the chain can go bad silently.
  - Fix: Add a weekly due-job that runs `litestream restore -o /tmp/drill.db` from the live replica config with a timeout, opens it read-only, runs `PRAGMA integrity_check` + a row count on strategy_runs, records {ok, durationMs, restoredAsOf} in an internal setting, surfaces it on /admin/backups ('Last verified restore: …') and pages via alertStorageWarning('litestream_restore_drill_f…

- **[performance-accounting:perf-08] Drawdown breaker HWM is not flow-adjusted and its 'day' is UTC — withdrawals/paper resets read as drawdown, daily-loss window resets at 7pm CT** — `src/lib/risk-breaker.ts:86` · defect · Backend · effort S · CONFIRMED
  - Why: A $20k withdrawal (or Alpaca paper reset) drops equity below the persisted HWM forever, so the advisory/kill-switch reports 'Trailing drawdown 20%' on no market loss and, with drawdownBreakerAction set, flips to close_only. Start-of-day equity keyed on the UTC date makes 'today's loss' span 7pm CT→7pm CT, unlike every other daily boundary in the app (Central).
  - Fix: Adjust HWM by detected external flows (or subtract flows from equity before comparing), expose a 'reset high-water mark' control on Guardrails, and key sod on the Central/market day like dailyExecutionStats.

- **[performance-accounting:perf-09] Decision outcome grading uses only the FIRST partial exit of a lot (won/lost/pnlUsd wrong for scaled-out positions)** — `src/lib/outcome-engine.ts:966` · bug · Backend · effort S · CONFIRMED
  - Why: calculatePnl emits one ClosedLot per (lot, closing fill) pair; a take-profit trim at +2% followed by the remainder stopped out at −8% grades the decision 'won' with the trim's P&L, and terminates the case early while most of the position is still open. Feeds lessons and Decisions pages with wrong verdicts.
  - Fix: Aggregate all closed lots with the same entryAt (sum pnl, capital-weighted returnPct) and only mark terminal when the entry lot is fully closed (compare closed qty to entry fill qty).

- **[performance-accounting:perf-10] Synthetic 20 bps 'paper' execution-cost haircut is applied to REAL Alpaca-paper fill prices, but only when the price is known at booking — same account mixes shaded and raw fills** — `src/lib/performance.ts:244` · defect · Backend · effort S · CONFIRMED
  - Why: Since the local simulator was removed, source 'paper' means a real broker paper account (owner: an account is an account). Fills that return an averagePrice immediately are booked net of a modeled cost; fills reconciled later are overwritten with the raw broker truth. Realized P&L on the paper book therefore neither equals the broker's nor is uniformly net-of-model, and Result…
  - Fix: Book price = broker price always; keep the modeled cost in a separate FillEvent field (modeledCostBps) used only by the learning loop's net-of-cost stats, or disable PAPER_EXECUTION_COST_MODEL by default now that paper = broker.

- **[qa-test-strategy:qa-08] TestBrokerGateway always fills instantly, so integration tests can only see the happy path** — `src/lib/robinhood.ts:920` · risk · Backend · effort M · CONFIRMED
  - Why: 38 test files (incl. test/e2e-money-path.test.ts “runs strategy through to execution”) drive strategy → executeProposal → fills through this gateway, so accepted→partial→filled, rejected, cancel_requested and stale-limit paths in strategy-execution.ts (reconcilePendingFills, flagStalePlacingIntents) are only covered by bespoke per-file gateway mocks; the end-to-end test would…
  - Fix: Give TestBrokerGateway a scriptable lifecycle (`scriptOrder(refId, ["accepted","partially_filled","filled"])`, `getEquityOrders` returning scripted resting orders) and extend e2e-money-path.test.ts with a resting-then-filled and a rejected scenario asserting fill_events/positions/dailyStats end state.

- **[security-auth:sec-02] Origin is directly reachable at <PROD_ORIGIN_IP>:443 with Host: socratictrade.com — every Cloudflare layer (WAF, bot/UA rules, upload cap, cf-connecting-ip trust) is bypassable** — `middleware.ts:173` · risk · Backend · effort M · CONFIRMED
  - Why: App-level auth still holds (good), but the deliberate framework anti-scrape stack (Cloudflare WAF UA ruleset, ai_bots_protection, the ua-gate in app/api/framework/content/route.ts:19-27 which any client defeats by sending a browser UA plus x-framework-viewer: 1) is moot for a scraper that hits the IP. Rate limiters that key on cf-connecting-ip (app/api/auth/robinhood/callback/…
  - Fix: Add a Hetzner Cloud Firewall (or ufw) rule allowing 80/443 only from Cloudflare's published IPv4/IPv6 ranges (plus Tailscale 100.64/10 for ops), or move to a Cloudflare Tunnel; enable Authenticated Origin Pulls and switch the zone to Full (strict). Document it in the hetzner-cutover rollout note. Until then, treat cf-connecting-ip as untrusted unless the request came through C…

- **[security-auth:sec-03] next-auth 5.0.0-beta.31 / @auth/core 0.41.2 carries three published advisories (one critical) with a fix one beta ahead** — `package.json:56` · risk · Backend · effort S · CONFIRMED
  - Why: This is the login library for a real-money console with three OAuth providers (Google/GitHub/Apple) — exactly the multi-provider shape the state/nonce/PKCE-binding advisory targets. The email allowlist limits blast radius, but the fix (next-auth 5.0.0-beta.32 / @auth/core 0.41.3, both published) is a one-line bump. npm audit --omit=dev reports 8 findings (2 critical, 6 high);…
  - Fix: Bump next-auth to ^5.0.0-beta.32 (pulls @auth/core 0.41.3), run the auth test files (middleware-auth, apple-web-auth, mobile-auth-exchange-route) and a manual Google+Apple web login before merge. Add `npm audit --omit=dev --audit-level=critical` as a non-blocking CI step so auth-library advisories surface within a day instead of at review time.

- **[security-auth:sec-06] Unauthenticated /api/mobile/auth/exchange buffers an unbounded JSON body (and /api/mobile/auth/apple has no rate limit) on the single process that also runs trading** — `app/api/mobile/auth/exchange/route.ts:8` · defect · Backend · effort S · CONFIRMED
  - Why: The sibling public routes were already fixed for exactly this (csp-report readCappedBody, apple route readJsonWithLimit(APPLE_AUTH_MAX_BYTES), webhooks readBodyWithLimit); exchange was missed. Because it is public (middleware.ts:303,338-341) an anonymous caller can POST large bodies repeatedly; with the origin directly reachable (sec-02) even Cloudflare's 100 MB cap does not a…
  - Fix: Use readJsonWithLimit(request, APPLE_AUTH_MAX_BYTES) in exchange and return 413/400 like the apple route; add enforceRateLimit keyed on cf-connecting-ip (with the 'unknown-ip' shared bucket pattern from robinhood/callback) to both exchange and apple. Extend public-auth-rate-limit-hardening.test.ts.

- **[trading-money-path:tsx-04] Any HTTP 4xx from the broker — including 429 rate-limit and 408 — is filed as a definitive terminal rejected_by_broker without a refId check** — `src/lib/strategy-execution.ts:1279` · defect · Backend · effort S · CONFIRMED
  - Why: formatAlpacaOrderError prefixes `HTTP ${status}`; Alpaca returns 429 when the 200 req/min budget is exhausted (see tsx-05 for why that is realistic) and the SDK does not retry. The proposal is retired as 'Rejected by broker' — wrong information to the trader, and unlike not_placed it never says 'safe to retry'. Same regex at strategy.ts:3979.
  - Fix: Narrow the terminal branch to 400/403/422 (and 404 on cancel), route 408/425/429/409 through reconcilePlacementError (or straight to not_placed with the retry hint), and put the status code in the audit payload.

- **[trading-money-path:tsx-05] Alpaca getEquityOrders walks the ENTIRE order history (up to 50×500 pages) on every call, and 3+ scheduler lanes call it every 60s per account** — `src/lib/alpaca.ts:500` · risk · Backend · effort M · CONFIRMED
  - Why: stale-limit-scan (scheduler.ts:884), synthetic-stop monitor (synthetic-stops.ts:287), pending-fill reconcile, executeProposal (:308), cancel lookup, replace polling all call it. Daily protective-stop replacement grows history without bound, so cost is O(history) round-trips per call — 6+ full walks/minute across Roth+Paper. This is consistent with the STATUS.md 6-8s getAccount…
  - Fix: Add getOpenEquityOrders() (status open, one page) for coverage/stale checks, and bound the 'all' walk with `after` = oldest pending fill/placing intent created_at minus a day; cache the list per tick across lanes (the tick already holds a per-account lease). Keep the full walk only for explicit forensics.

- **[trading-money-path:tsx-06] Alpaca getEquityTradability is a stub (tradable:true, fractional:true for every symbol) — shortable/fractionable/easy_to_borrow are never checked pre-flight** — `src/lib/alpaca.ts:582` · defect · Backend · effort S · CONFIRMED
  - Why: executeProposal (:483) and the 'not tradable' hard gate rely on this. On Alpaca a short on a non-shortable/HTB name, a dollar order on a non-fractionable asset, or a halted/inactive asset only fails at the broker (403/422 → rejected_by_broker) after the owner already typed the live confirmation, and the LLM keeps proposing the same names.
  - Fix: Call GET /v2/assets/{symbol} (cache 1h): map tradable, fractionable, shortable, easy_to_borrow, status==='active'; return fractional=false so the notional→qty path floors correctly, and reason strings for the block. Feed shortable into the strategy prompt's short candidates.

- **[trading-money-path:tsx-09] Approval-time ratio re-anchoring moves EXIT limits with the market — a held take-profit sell at $110 (ref $100) is placed at $99 if the quote fell to $90; entries chase upward inside the drift cap** — `src/lib/approval-reprice.ts:100` · risk · Backend · effort S · PARTIAL — Behaviour confirmed: approval-reprice.ts:99-103 ratio-re-anchors any-side limits including sell/cover, and strategy-execution.ts:434-449 only routes drift back to the human for live+typed material drift or opening-side entry-drift-cap breach — so on paper (or…
  - Why: A profit-taking or manual sell limit is a price LEVEL, not an aggressiveness ratio. On broker/paper, or live with requireTypedConfirmation=false, drift is not routed back to the human (strategy-execution.ts:434-472 gates only live+typed and opening-side drift), so the order placed is materially different from the one reviewed and the sell can execute below the intended target.…
  - Fix: Exempt sell/cover limits from ratio re-anchoring unless the proposal is a protective exit (already owned by protective-exit-routing) or the owner opts in; for opening limits keep it but treat 'material' drift as re-approval on every mode when the card is human-approved (owner is clicking anyway).

- **[trading-money-path:tsx-11] Hard-coded 5% opening-order headroom gate shrinks the owner's own per-order cap with no policy toggle — a human-approved $490 order against a $500 maxOrderNotional is blocked at approval** — `src/lib/policy.ts:567` · defect · Backend · effort S · CONFIRMED
  - Why: Owner ruling: guardrails are adjustable preferences with an easy off-switch. This one is a compile-time constant that turns the configured cap into 95% of itself; the LLM is told about it (strategy-prompts.ts:178) but chat/from-draft/manual proposals and broker-minimum bumps hit it as a surprise block, and the only 'override' is to raise the cap the owner deliberately set.
  - Fix: Make it policy.openingOrderHeadroomPct (default 5, 0 = off) exposed on Guardrails, and treat it as advisory (escalatable) rather than a block on the human-approval path.

- **[api-contract-web-ios:api-04] strategy.run_once stores the full StrategyResult (marketScan.quotesBySymbol, topCandidates, llmSteps) in mobile_commands.result and ships it in every /api/mobile/snapshot recentCommands[]** — `src/lib/mobile-api.ts:955` · defect · Cross-surface · effort M · CONFIRMED
  - Why: The strategy_run audit deliberately omits the full MarketScan (scan/route.ts comment) because it is large, yet the mobile command row persists it whole and the phone downloads up to 30 such blobs on every SSE-triggered reload, then writes the whole snapshot into UserDefaults (MobileStore.swift:143 saveCachedSnapshot). iOS never decodes `result`. performance.fills also carries…
  - Fix: Persist a compact result for run_once (runId, status, summary, proposal count/ids) — reuse summarizeResult-style shaping before finishCommand — or strip marketScan/llmSteps in toPublicMobileCommand; omit `result` from snapshot recentCommands (keep it on GET /api/mobile/commands/:id); drop `raw` from fills in the mobile projection.

- **[api-contract-web-ios:api-06] No generated contract fixtures between server routes and Swift decoders — hand-mirrored enums/constants/JSON drift silently** — `ios/SocraticTradeTests/MobileModelsTests.swift:6` · risk · Cross-surface · effort M · CONFIRMED
  - Why: api-01 and api-03 are both drift the compiler cannot see. Other hand mirrors: OrderCancel.swift:29-49 vs broker-held-orders.ts:9-25,54 (order states); PolicyTightening.swift:24 `maxOrderNotionalCeiling = 100_000` vs mobile-api.ts:335; DeskModels.swift:176-184 coach model ids vs app/ui/llm-model-catalog.ts; DeskModels.swift:482 group keys vs source-settings-catalog.ts. The repo…
  - Fix: Add a vitest that boots the routes against a seeded temp DB and writes golden JSON (snapshot, policy, scan, quote, symbol-desk, chat-history, llm-budget, source-features) into ios/SocraticTradeTests/Fixtures/, and Swift tests that decode those files; extend apns-deep-link-contract.test.ts to assert the mirrored sets/constants match their TS sources.

- **[frontend-performance:dweb-05] After acknowledge/cancel/run-once, `await refresh()` is served from the 10 s snapshot cache, so the acked alert / cancelled order stays on screen** — `app/console/components/alert-center.tsx:298` · bug · Cross-surface · effort S · CONFIRMED · prior docs/rollouts/2026-08-04-ux-wave-c-speed.md C1
  - Why: getDashboardSnapshot (dashboard.ts:340) returns the cached entry for `DASHBOARD_SNAPSHOT_TTL_MS = 10_000` (dashboard-snapshot-cache.ts:13). Foreground `refresh()` (useConsoleData.tsx:203-207) calls the same GET with no bypass, so a user-initiated mutation whose route does not invalidate (notifications ack, order cancel/replace-market, strategy run request, account activate if…
  - Fix: Either (a) call `invalidateDashboardSnapshotCache(userId)` in every mutating route under app/api that touches snapshot material (notifications/ack, orders/cancel, orders/replace-market, strategy/run|pause|enable, connected-accounts/*/activate), or (b) make foreground `refresh()` send `?fresh=1` and have /api/dashboard invalidate the key before computing. Add a test asserting t…

- **[frontend-performance:dweb-07] TickerLogo starts as theme=dark and flips to light after mount, so every logo in the light-default console requests the dark variant first (double fetch + wrong-variant flash), plus a MutationObserver/matchMedia listener per instance** — `app/console/ui/ticker-logo.tsx:21` · bug · Cross-surface · effort S · CONFIRMED
  - Why: Owner ruling: default theme is light. Every `<TickerLogo>` (used by SymbolButton in positions/orders/activity/scan rows and drawers) commits an `<img>` with `theme=dark`, then the effect reads `.console-root[data-theme="light"]` and re-commits `theme=light`: two image URLs, two server hits, two logo.dev fetches on a cold disk cache, and a dark tile that flashes to light — repe…
  - Fix: Initialise theme from the nearest `.console-root[data-theme]` synchronously (`useSyncExternalStore` or a tiny ThemeContext provided by ShellFrame that already knows `dataTheme`), default `"light"` when unknown, and hoist the observer into that single provider. Also add `sizes`/explicit width+height so lazy logos do not shift rows.

- **[frontend-performance:dweb-08] /console/watchlist and /console/lessons fetch their own data but still wait behind the full-screen snapshot loader (up to ~24 s worst case)** — `app/console/components/shell.tsx:67` · defect · Cross-surface · effort S · CONFIRMED
  - Why: The shell's own comment says routes that consume zero snapshot fields should be allow-listed so they paint immediately, but only /console/usage is. Watchlist (own /api/watchlist + /api/alerts poll, 30 s) and Lessons (own /api/learned-context poll, 60 s) render nothing until /api/dashboard returns — on a cold phone load that is the whole broker chain (16 s + 8 s retry bundle pe…
  - Fix: Add "/console/watchlist" and "/console/lessons" to SNAPSHOT_INDEPENDENT_ROUTES (learned-context's `refresh` is safe pre-snapshot); consider SELF_SKELETON for /console/scan (own live scan) and /console/assistant, and prefer a route-level `snapshotRequired` export over a hard-coded Set so new pages opt in explicitly.

- **[llm-agent-architecture:llm-06] 'Retry Red Team' stamps approve-at-half / reject verdicts without applying the haircut or hold — approving the card places FULL size** — `src/lib/retry-red-team.ts:47` · bug · Cross-surface · effort M · CONFIRMED
  - Why: The run-loop path (strategy.ts:2659-2697) applies applyRedTeamHalfSize or holds for human when half is unplaceable, and rejects drop the proposal; the retry path (console card + iOS via mobile-api.ts:39) only stamps the verdict. The card then shows 'AI critic: half size' (approval-card.tsx:201-206) beside the unchanged full notional, and executeProposal only re-reviews/haircut…
  - Fix: On retry: pass {context, sizing} like the run loop, apply applyRedTeamHalfSize on approve-at-half (or requireHumanReview with the half-unplaceable note), and on reject set the same rejected reasons/decision the loop uses; surface the size delta on the card.

- **[llm-agent-architecture:llm-11] Under rotation the approval card asserts 'this run's rotation pick, not a failover' even when an implicit fallback (the cheapest seats) actually served the proposal** — `app/console/components/approval-card.tsx:127` · defect · Cross-surface · effort S · CONFIRMED
  - Why: Issue #2577 added implicit failovers for rotating Green seats; implicitGreenRotationFallbacks prefers gemini-flash/mistral-medium/flash-lite/mistral-small (model-rotation.ts:90-95, 184-197). When the pick 400s/times out the run is served by one of those, but only bullFailoverNote/strategy_llm_failover (run record + audit) know it — the proposal carries just proposedByModel, so…
  - Fix: Persist greenServedByFallback {fromModel, reason} on the proposal at strategy.ts:6212-6230 and render 'rotation pick X failed (HTTP 400); served by fallback Y' on the card + iOS ProposalsView.

- **[market-data-integrity:mdi-05] Alpaca broker quote 'price' is the ASK; it becomes the displayed Price, watchlist price, alert trigger and stop/take-profit mark, while Chg% stays the screener's** — `src/lib/alpaca.ts:551` · defect · Cross-surface · effort M · CONFIRMED
  - Why: mergeQuoteData (app/api/scan/route.ts:102, strategy.ts:771) replaces `price` with the ask but keeps intradayChangePct (market.ts:1129-1141), so a row shows an ask-priced 'Price' next to a change computed from a different source/time; the watchlist (api/watchlist/route.ts:20 -> page.tsx:253) and price alerts show/evaluate the ask; currentPricesFromScan (strategy.ts:782 -> gener…
  - Fix: Use mid (or latest trade from the snapshot endpoint) as `price`, keep bid/ask separately, recompute intradayChangePct from prevClose when price is replaced (or clear/relabel it), and show 'ask'/'mid' in the tooltip.

- **[market-data-integrity:mdi-09] 'volume' mixes session, 10-day average and IEX-only volume across providers, feeding liquidity score, dollar-volume floor and outlier detection** — `src/lib/data-providers.ts:2143` · defect · Cross-surface · effort M · CONFIRMED · prior 2026-07-20 #48
  - Why: Robinhood prefers AVERAGE volume even during the session; Alpaca's default IEX feed reports only IEX-printed volume (a small fraction of consolidated); all land in MarketQuote.volume used by liquidityScore (market.ts:1701-1702), passesUniverseFloor dollar volume (145-147), the mean+2sigma volCut (435-441) and ADV caps — apples vs oranges across symbols in one ranking. Prior 20…
  - Fix: Add `averageVolume` as its own field; only session volume may populate `volume`; label IEX-only volume in provenance and prefer consolidated (Nasdaq quote / Yahoo) for floor and ADV.

- **[market-data-integrity:mdi-11] History cascade merges split-only, split+dividend and IEX-only bars into one series/cache without tagging the adjustment basis** — `src/lib/history.ts:611` · defect · Cross-surface · effort M · CONFIRMED
  - Why: Whichever tier answers determines whether closes are total-return or price-return; mergeOHLCBars (131-160) and upsertHistoryCacheEod then splice bars of different bases by date, creating discontinuities at ex-dividend dates. The SPY benchmark (benchmark.ts) therefore compares account equity to SPY total return on some days and price return on others, and indicators computed ov…
  - Fix: Tag OHLCBar.adjustment ('split'|'total'), choose one basis per consumer (split-only for charts/indicators; total-return for benchmark), request it explicitly from each provider, and never merge/persist bars of a different basis into an existing series.

- **[mobile-web-ux:mweb-03] Choosing "System" theme never survives a reload — it is stored by deleting the key, and a missing key reads back as Light** — `app/console/lib/useConsoleTheme.ts:37` · bug · Cross-surface · effort S · CONFIRMED
  - Why: The default-light change (#2628) made "missing" mean Light, but `set("system")` still encodes System as "missing". So the System option in the phone profile menu (chrome.tsx:928-949) appears to work until the next page load, when it silently reverts to Light — a control that lies.
  - Fix: Persist the literal string "system" (`localStorage.setItem(STORAGE_KEY, next)` for all three values) and treat only a truly absent key as Light. Add a unit test for the round trip.

- **[mobile-web-ux:mweb-04] ⌘K "Toggle theme" writes localStorage but does not change the visible theme (separate hook instance from the shell)** — `app/console/components/command-palette.tsx:68` · bug · Cross-surface · effort S · CONFIRMED
  - Why: useConsoleTheme keeps state in a per-instance useState; the shell's data-theme (shell.tsx:84) is a different instance, so cycling from the palette only mutates the palette's private state + storage. Nothing visible happens until reload — and because of mweb-03 the "system" step then reads back as light. The palette trigger is deliberately always visible on phones (shell.tsx:40…
  - Fix: Lift theme state into a context provided by ShellFrame (or broadcast a `console:theme` event / `storage`-style subscription) so all consumers share one source; have the palette call the shell's `set`.

- **[ops-observability-admin:sre-11] An overdue next run is never flagged: web says 'Next run now' forever, iOS says 'Next Run 12 minutes ago'** — `app/console/lib/format.ts:123` · defect · Cross-surface · effort S · CONFIRMED
  - Why: scheduler.nextRunAt is an in-memory schedule (scheduler.ts:378-386); when the tick stalls or a run request is locked (2026-08-18 STATUS: request stuck 'running', no strategy_run created for 17m+), the timestamp passes and the console footer (chrome.tsx:1099) and dashboard card (page.tsx:439) keep printing 'Next run now', while iOS HomeView.swift:666-667 prints a relative past…
  - Fix: In timeUntil/next-run rendering, when now − nextRunAt > max(2 min, cadence/2) render 'overdue by 12m' in warn tone with a tooltip pointing to Activity; on iOS compute the same and swap the LabeledContent value color; both should link to the run-lock/heartbeat detail once sre-01 lands.

- **[performance-accounting:perf-04] 'Versus the market' TWR silently covers only the last ~100 snapshots (≈7 trading days) but is framed as account performance** — `src/lib/performance.ts:497` · defect · Cross-surface · effort M · PARTIAL — True mechanism: performance.ts:497-498 `listPortfolioSnapshots(..., 100)`, two snapshots per run (strategy.ts:814 pre-run, 4334 post-run) at 60-min default cadence (defaults.ts:164) ⇒ ~7 sessions after per-date collapse. Overstated on labeling: results/page.t…
  - Why: The equity curve behind Results 'Your account / SPY / vs SPY' and iOS 'vs SPY' is a rolling ~1–2 week window whose start date drifts daily (the 8/6 review saw 7/29→8/6). Nothing on the page says 'trailing 7 days'; the tooltip describes a since-inception TWR. A GIPS-minded reader would call this an unlabeled trailing window with a moving start. Also affects the Results BucketCa…
  - Fix: Store/collapse one EOD snapshot per date (SQL: latest row per date) with a large limit so the curve is since-inception, and offer explicit windows (1W / 1M / 3M / YTD / All) with the window named in the label. Until then, put 'trailing N sessions (since <date>)' in the Stat sub-label instead of only the raw dates.

- **[performance-accounting:perf-05] 'Unrealized P&L' / 'Realized P&L' mean different things on Results (lot ledger), Home (broker cost) and iOS (broker positions) — none labels its basis** — `app/console/results/page.tsx:962` · defect · Cross-surface · effort S · CONFIRMED · prior 2026-08-06 C3 (ledger mismatch now flagged for Tax only, PR #2589)
  - Why: For an account with pre-app holdings or a drifted ledger (2026-08-06 C3), the three surfaces show three different 'Unrealized P&L' numbers for the same account and none says 'app-booked lots only' vs 'broker cost basis'. Realized P&L on Results is all-time and app-booked (excludes exits of positions the app never opened), while the Tax card is tax-year and the SPY card is ~7 d…
  - Fix: Pick one definition per surface and label it: 'Unrealized (broker cost basis)' everywhere for the account figure; keep the lot-ledger figure only in the tax/lots section labeled 'app-booked lots'. Add 'all-time, app-booked trades' under Realized P&L and the window under each card title.

- **[performance-accounting:perf-06] Win rate '0%' and Avg return '+0.00%' render for accounts with zero closed lots (web + iOS)** — `app/console/results/page.tsx:957` · defect · Cross-surface · effort S · CONFIRMED
  - Why: Any active account has snapshots, so hasAny is effectively always true; winRate() returns 0 for an empty lot list (performance.ts:1708) and iOS has no gate at all, so a fresh or all-open book shows a fabricated 0% win rate next to '—' policy elsewhere ('No closed trades to score yet').
  - Fix: Carry closedLotCount in PerformanceSummary and render '—' (web) / '—' (iOS) until n ≥ 1; show 'n=…' beside win rate.

- **[performance-accounting:perf-11] Win rate and 'n' count partial-close lots, not round trips — trims systematically inflate wins and sample counts** — `src/lib/performance.ts:1707` · defect · Cross-surface · effort M · CONFIRMED
  - Why: One position trimmed twice at a profit then stopped out counts as 2 wins / 1 loss (67%) and n=3 on Results 'By thesis' and Home 'Closed thesis samples', while the round trip lost money. Shrinkage priors do not fix a biased unit of account.
  - Fix: Report per round-trip (group ClosedLots by entryRunId+symbol or by position episode) for win rate/n, and label the tile 'closed lots' if lots are kept.

- **[performance-accounting:perf-12] Closing fills with no matching app lot are silently dropped — exits of pre-app/manual positions never realize P&L, and nothing surfaces the count** — `src/lib/performance.ts:599` · risk · Cross-surface · effort M · CONFIRMED · prior 2026-08-06 C3 (flagging only, PR #2589)
  - Why: The Roth account holds positions the app did not open; when the strategy sells them the sell is booked but produces $0 realized and disappears from win rate/avg return, and the lot ledger drifts (C3 symptoms: AXP lot with no position). Tax now flags mismatched symbols but Realized P&L / win rate carry no indicator.
  - Fix: Seed opening-balance lots from broker positions (avg_entry_price, qty) at connect time, labeled 'broker basis'; count and display 'unmatched closing fills: N' on Results when any sell/cover found no lot.

- **[public-site-growth:dpub-01] Session-expired redirect drops the destination: deep links (push/email → /console/approvals?proposal=…) land on the dashboard after sign-in** — `middleware.ts:417` · bug · Cross-surface · effort M · CONFIRMED
  - Why: Every notification deep link (approvals, fills, price alerts, run_failed) points at a /console path. When the 30-day JWT has expired on the phone browser or desktop, the owner is bounced to /login and, after OAuth, to `/` → /console — the proposal/order they tapped is gone and they must re-find it. Neither the middleware nor the login server actions carry the original path.
  - Fix: In middleware append `?callbackUrl=<pathname+search>` (same-origin relative only) on the /login redirect; in app/login/page.tsx read `searchParams.callbackUrl`, validate it starts with `/` and not `//`, and pass it as `redirectTo` to each `signIn(...)` call (default `/console`). Add a middleware-auth test asserting the redirect preserves `/console/approvals?proposal=x`.

- **[qa-test-strategy:qa-06] Browser coverage is one 18-line desktop smoke on `/`, not run on PRs, no phone width** — `test/e2e/dashboard-smoke.spec.ts:3` · defect · Cross-surface · effort L · CONFIRMED
  - Why: The owner uses the console daily at desktop AND phone widths, yet no automated run ever loads /console/approvals, /console/orders, /console/strategy (1,522 lines), /console/results or any /admin page in a browser, and none at 390px. A client-side crash on a subroute reaches production before anyone sees it; nightly smoke only says the home shell painted.
  - Fix: Add a `phone` Playwright project (devices["iPhone 15"]), a route-crawl spec that visits every app/console/**/page.tsx and app/admin/** and fails on `Application error`/pageerror/console.error, and one interaction spec (seed a `broker:"test"` account through the API, open Approvals, approve, assert the placed state). Run e2e on PRs whose diff touches app/console/** or app/admin…

- **[qa-test-strategy:qa-11] land.sh skips lint and never compiles Swift for ios/** diffs** — `scripts/land.sh:156` · defect · Cross-surface · effort S · CONFIRMED
  - Why: A lint error (the only rule class CI fails on) is discovered only after push, costing a full ~20-minute CI cycle; an ios/** change is first compiled in CI (AGENTS.md caution) even though the landing machine is the Mac with Xcode.
  - Fix: Add `npm run lint` as step 0 in land.sh, and when `git diff --name-only origin/main...HEAD | grep -q '^ios/'` run the AGENTS.md xcodebuild build (and `xcodebuild test`) before pushing.

- **[security-auth:sec-04] Sessions cannot be revoked: 30-day stateless JWT, sign-out is cookie-delete only, no 'sign out everywhere', no absolute lifetime** — `src/lib/auth/session-token.ts:6` · risk · Cross-surface · effort M · CONFIRMED
  - Why: A stolen/leaked cookie (lost phone, laptop left unlocked, WKWebView copy, log capture) stays valid for up to 30 days and can approve trades, change guardrails, and read broker positions; the only remediation is rotating AUTH_SECRET, which logs out every device. Auth.js rolling refresh means an active session never actually expires. The plumbing for a fix already exists: middle…
  - Fix: Store a per-user `sessions_valid_after` timestamp (settings row); in resolveRequestUser reject sessions whose loginAt (or iat for legacy cookies) predates it. Expose 'Sign out of all devices' on /console/settings and in the iOS Settings sheet (also called by account deletion). Add an absolute cap (e.g. force re-login after 30 days from loginAt regardless of refresh) — a prefer…

- **[trading-money-path:tsx-08] Manually cancelling an app-managed protective stop from the console/iOS is silently undone next tick — the reconciler deletes the row and places a fresh stop; the cancel sheet promises 'No new order is placed'** — `src/lib/order-cancel.ts:198` · defect · Cross-surface · effort M · CONFIRMED
  - Why: cancelWorkingOrder does not know the order is a protstop-… row. broker-protective-stops.ts:1104 sees the tracked order canceled ('stale_resting_row'), deletes the local row, and the placement section re-arms the position within a tick. app/console/orders/cancel-sheet.tsx:86 states 'No new order is placed — the unfilled shares simply stop working', which is false for these orde…
  - Fix: In cancelWorkingOrder, detect app-managed stops (broker_protective_stops row / clientOrderId prefix protstop-/sstop-) and either (a) write a per-symbol 'owner-cancelled, do not re-place until <policy change or next session>' marker honored by the reconciler, or (b) return a dustWarning-style notice ('This is a managed protective stop; it will be re-placed next minute unless yo…

- **[trading-money-path:tsx-14] Stale-exit remediation on live spams 'stale_exit_auto_remediation_deferred' every 60s per resting sell limit and pushes a misleading 'limit order still working — cancel/reprice before replacing with market' alert for bracket take-profit legs** — `src/lib/order-replacement.ts:763` · defect · Cross-surface · effort S · CONFIRMED
  - Why: Same root as tsx-02: activated bracket TP legs and GTC sells are 'stale' forever, so on the Roth every such order writes ~1,440 audit rows/day (prior review C7/C8 already flagged Activity flooding) and stale-limit-orders.ts:74-78 sends one push per order telling the owner to cancel a take-profit that is resting by design.
  - Fix: Once tsx-02 narrows the stale set, also route the deferred audit through auditDeduped and drop the alert for bracket legs / non-app orders (or reword to 'take-profit resting N days').

- **[ux-copy-editor:copy-02] Double ASCII spaces typed into JSX text collapse to one space at render — looks compliant in source, isn't on screen** — `app/console/components/consent-gate.tsx:107` · defect · Cross-surface · effort S · CONFIRMED
  - Why: At least 19 JSX text sites use two literal spaces instead of NBSP+space: consent-gate.tsx:107/117/121 (the first-run consent screen), settings/page.tsx:785-786, settings/llm-budget.tsx:97-98, settings/sharing.tsx:119/126, settings/legal.tsx:53, shell.tsx:136, strategy/overlays-panel.tsx:158-159/214, scan/smart-money.tsx:247/279, ui/drilldown-sections.tsx:195. Same trap for LEG…
  - Fix: Replace the literal double spaces in JSX text with {SENTENCE_GAP}; for shared string constants rendered as text nodes, either use " " in the constant or wrap the render in a `white-space: pre-line`-safe helper. Add the same lint check as copy-01 for `\. [A-Z]` inside JSX text nodes.

- **[ux-copy-editor:copy-05] "Approvals" vs "Proposals": nav/H1/iOS tab say Proposals, but a dozen copy strings send users to "Approvals"** — `app/console/assistant/draft-card.tsx:343` · defect · Cross-surface · effort S · CONFIRMED
  - Why: The route is /console/approvals but the destination is labeled Proposals everywhere the user can click; copy that says "Approvals" (draft-card.tsx:289/335/336/343/349/352, chat.tsx:93/385/497, assistant/page.tsx:18, settings/help.tsx:124, settings/danger.tsx:204, ui/drilldown-sections.tsx:331, guardrails/stop-flow.tsx:188, iOS InsightsView.swift:256 "Approvals here are real")…
  - Fix: Pick one noun (Proposals matches nav, H1, iOS, badge) and replace "Approvals" in user copy with "Proposals" ("Review in Proposals", "Open the Proposals screen"); keep "approve/reject" as verbs. Reserve "idea" for pre-proposal candidates or drop it. Optionally add a redirect alias so the URL can move to /console/proposals later.

- **[ux-copy-editor:copy-07] One run state, three names (Exit-only / Close-only / Close Only) and one stop control, four names (Stop Agent / STOP everything / system stopped / strategy stopped) across web and iOS** — `app/console/lib/derive.ts:171` · defect · Cross-surface · effort M · CONFIRMED
  - Why: The same close_only state is labeled Exit-only on the web run-state chip/help glossary and iOS DeskModels, Close-only on the web control sheet, error copy and event-run field, and Close Only on the iOS Home button/action feed. The stop control is "Stop Agent" (chrome.tsx:325) but the glossary term is "STOP everything" (help.tsx:81) with an "Arming" entry (help.tsx:67) nobody s…
  - Fix: Standardize: state words Running / Paused · market closed / Exit-only / Winding down / Stopped (already in derive.ts); actions Start Agent / Resume Agent / Stop Agent / Exit-only / Wind Down on both surfaces; "the agent" as the subject (not system/strategy); "run" not "cycle". Update help.tsx terms to the on-screen labels ("Stop Agent", drop "Arming") and fix approvals/page.ts…

- **[ux-copy-editor:copy-11] ALL-CAPS "(NOT Real Money)" / "(LIVE — Real Money)" account labels and shouting PAPER chips contradict `Alpaca (paper)` and the no-ceremony ruling** — `app/console/settings/brokers.tsx:626` · defect · Cross-surface · effort S · CONFIRMED
  - Why: FLEET-UI-COPY special case: paper reads `Alpaca (paper)` lowercase p, no Live pills; owner ruling: an account is an account, no are-you-sure-it's-real-money ceremony. Connect sheets, the connected-accounts list ("Load PAPER"), the glossary term "Alpaca PAPER Account (NOT Real Money)", the web reality banner word "PAPER" (derive.ts:54) and the iOS Home "PAPER" pill (HomeView.sw…
  - Fix: Use `Alpaca (paper)`, `Tradier (sandbox)`, `Tradier`; drop "(NOT Real Money)"/"(LIVE — Real Money)"; render the paper chip as "paper" (lowercase, muted) on both surfaces; rewrite help.tsx:31-40 to "Paper account — the broker's practice endpoints; orders route the same way, dollars are the broker's practice balance."

- **[ux-copy-editor:copy-14] Setup copy says "Connect Alpaca or Robinhood" while Connections offers Alpaca, Robinhood, Tradier, Public and eToro** — `app/console/lib/derive.ts:534` · defect · Cross-surface · effort S · CONFIRMED
  - Why: Stale enumeration in the two places a new user reads first (Needs-attention item and readiness checklist on web; iOS Home checklist). A Tradier/Public user is told their broker is not an option.
  - Fix: "Connect a broker account (Alpaca, Robinhood, Tradier, Public, or eToro) …" or simply "Connect a broker account" with the list living on Connections; update ios/HomeView.swift:87 to "Connect a broker in Account & Settings, then select it here."

- **[ux-copy-editor:copy-16] "Mock (offline)" model is offered as the first group in the Coach model picker (web + iOS) and the server silently answers with a mock when no key resolves** — `app/ui/llm-model-catalog.ts:124` · defect · Cross-surface · effort M · CONFIRMED
  - Why: Owner rulings: no test/paper/simulation mode framing anywhere in the product; TestBrokerGateway is kept as test infrastructure only. The Coach ships a product-facing "Mock" model in the picker (chat.tsx:432 tooltip explains it), and when a provider key is missing the request degrades to MockLLM output prefixed "Mock Response:" instead of a clear "no key" state — a trader can e…
  - Fix: Remove the Offline/Mock group from CHAT_MODEL_GROUPS and iOS DeskModels model options (keep MockLLM for vitest via env/test wiring only); when no key resolves, return the existing 412-style "No LLM key — open Connections" error rather than a mock reply.

- **[web-accessibility:a11y-03] Sheet focus sentry and Escape are not stack-aware — Escape on a drawer/palette above a Sheet also closes the Sheet** — `app/console/ui/sheet.tsx:116` · defect · Cross-surface · effort M · PARTIAL — Confirmed: sheet.tsx's own unconditional Escape handler (e.key === 'Escape' → onCloseRef.current()) and focusin sentry are exactly as described; focus-trap.ts:11-38 explicitly documents this as a known gap in its own doc comment. Real, current bug. Already ad… · prior 2026-08-06 #2561 (2) — still open on main
  - Why: focus-trap.ts:39-43 documents this exact follow-up ('Sheet and nav's TabsSheet should adopt this hook too … today Escape on a stacked surface also closes the sheet beneath it'). Symbol drawer (z 111) and Cmd+K palette (z 210) provably open on top of Sheets (EvidenceCard SymbolButtons inside Proposal Details / Review changes). Keyboard users lose the underlying dialog state on…
  - Fix: Migrate Sheet and TabsSheet onto useFocusTrap(ref, open, { onEscape }) so the module-level trapStack decides who owns Tab/Escape; delete the private focusin sentry and nextSheetFocusTarget.

- **[web-accessibility:a11y-04] Every console route has the same document title 'Socratic Trade' (WCAG 2.4.2)** — `app/console/layout.tsx:5` · defect · Cross-surface · effort S · CONFIRMED
  - Why: Console pages are client components and export no title, so tabs, history, and screen-reader page announcements are indistinguishable across Approvals / Guardrails / Orders / Scan. Public pages do it right (how-it-works → 'Decision framework · Socratic Trade'). The owner runs several console tabs at once on desktop.
  - Fix: Give each route a title via a tiny server wrapper (export const metadata = { title: 'Approvals' } using the root '%s · Socratic Trade' template) or, in the shell, set document.title from DESTINATIONS[destinationLabel(pathname)] in a useEffect; include the pending count for /console/approvals (e.g. '(3) Approvals').

- **[web-accessibility:a11y-06] Error toasts are polite + auto-dismiss in 9s with no hover/focus pause; typed-confirm server refusals are never announced** — `app/console/ui/toast.tsx:38` · defect · Cross-surface · effort S · CONFIRMED
  - Why: 'Approval failed' / 'Could not load account' are money-path outcomes but share a single polite region with 'Saved' and vanish before a screen-reader user finishes hearing them; no pause on mouseover/focus (WCAG 2.2.1). In LiveApproveSheet the 'The server refused the confirmation' block (approval-card.tsx:1080-1089) is inserted with no role=alert/aria-live, so a screen-reader u…
  - Fix: Render neg/warn toasts in a separate role="alert" (aria-live="assertive") container, keep neg toasts until dismissed (or ≥20s) and pause the timer on hover/focus; add role="alert" to the serverReasons block and aria-describedby from the typed input to the 'Type exactly' label + hint.

- **[web-accessibility:a11y-08] Approval cards have no heading or accessible name; 'Reject' is a bare repeated name across N cards** — `app/console/components/approval-card.tsx:455` · defect · Cross-surface · effort S · CONFIRMED
  - Why: The approvals page (h1 at approvals/page.tsx:262) renders one <article> per proposal with 'BUY AAPL' as a <span>, so screen-reader users cannot jump between proposals by heading or landmark name; the Approve button carries side+symbol in aria-label (:965-971) but Reject (:957) does not, so a rotor list reads 'Reject, Reject, Reject'. Section labels inside the receipt are <div…
  - Fix: Make the header 'BUY AAPL' an <h2 id> and set aria-labelledby on the <article>; give Reject an aria-label mirroring Approve; render receipt section titles as <h3 className="con-card-title">.

- **[web-accessibility:a11y-09] Account-scope menu uses role=menu without arrow keys/focus move and puts an invisible full-screen focusable button in the tab order; UserMenu/theme buttons lack state** — `app/console/components/chrome.tsx:235` · defect · Cross-surface · effort M · CONFIRMED
  - Why: ScopeSelector announces a menu (aria-haspopup=menu at :211) but on open focus stays on the trigger, no ArrowUp/Down/Home/End handling exists (only Escape at :104-115), and the first Tab lands on an opacity-0 full-viewport 'Close account menu' button with no visible focus indicator (2.4.7). UserMenu (:892-899) declares aria-haspopup="menu" but the panel (:910) has no role=menu;…
  - Fix: Either implement APG menu-button behavior (focus first item on open, arrow-key roving, Tab closes) or drop role=menu/menuitemradio for a plain listbox/radiogroup and set aria-haspopup="dialog"/"listbox". Make click-away backdrops aria-hidden <div>s (like UserMenu :909) and add aria-pressed to the theme buttons.

- **[desktop-web-ux:dweb-02] Bare 'r' key fires a strategy run; two mounted RunOnce buttons double-POST** — `app/console/components/command-palette.tsx:128` · defect · Desktop web · effort S · CONFIRMED
  - Why: Any unmodified 'r' outside an input starts an LLM-spending run with no confirmation and no on-screen hint; 'a' and '1'-'6' also navigate silently. shell.tsx 406-417 mounts BOTH the desktop and icon-only RunOnceButton (CSS-hidden, not unmounted), each with a console:run-once listener (chrome.tsx 759-765), so one keypress/palette command sends two POST /api/strategy/run and two…
  - Fix: Require the palette (or a modifier) for Run once, render one RunOnceButton with responsive styling, and show hotkeys in the palette footer/help.

- **[desktop-web-ux:dweb-04] Desktop freshness/'refresh failing' strip and market session are only at document end; rail not sticky** — `app/console/components/shell.tsx:211` · defect · Desktop web · effort S · CONFIRMED
  - Why: On long pages (Home, Guardrails, Results, Settings) the only desktop signal that data is stale ('refresh failing — showing last good data', chrome.tsx 1106-1109), the stream status and 'Market: closed' are off-screen; the mobile equivalent is deliberately in the sticky top chrome. The DesktopRail (nav.tsx 112) also scrolls away with the page.
  - Fix: Make FreshnessStrip position: sticky bottom on lg (or fold freshness + market session into the sticky top bar) and make the rail sticky top-[topbar] h-[calc(100dvh-topbar)].

- **[desktop-web-ux:dweb-06] Lessons page uses an undefined width token — page width/padding jump on that tab** — `app/console/lessons/page.tsx:5` · defect · Desktop web · effort S · CONFIRMED
  - Why: --con-page-w is defined nowhere (grep of app/**.css/tsx), so max-width resolves to none: Lessons stretches to the 1400px frame and adds its own px-4/pt-6 on top of main's padding, so the h1 shifts and the column widens on every visit — exactly the nav-click width jump page-width.ts standardized CONSOLE_PAGE_WIDTH to prevent.
  - Fix: Use `${CONSOLE_PAGE_WIDTH} flex flex-col gap-4` like every other page and drop the extra padding.

- **[desktop-web-ux:dweb-07] Home Coach card shows three dead 'quick action' chips and attaches notes to an unnamed decision** — `app/console/page.tsx:382` · defect · Desktop web · effort S · CONFIRMED
  - Why: Chip is a plain <span> (primitives.tsx 160), so the three items look like coaching presets but do nothing; on an empty DB the card is prose + dead chips (CoachNoteForm returns null without a decision, 1143). When a decision exists the form never says which decision (symbol/side/date) the note will attach to.
  - Fix: Make them buttons that prefill the textarea (or route to /console/assistant with the intent), and caption the form 'Attaches to BUY NVDA · 2h ago'.

- **[desktop-web-ux:dweb-18] Home proposal rows are click-only divs (no keyboard access); sheet Escape stacking in flight** — `app/console/page.tsx:1029` · defect · Desktop web · effort S · CONFIRMED · prior 2026-08-06 H #2561; in-flight PR #2795
  - Why: The primary row on Home has no role/tabIndex/keydown, so it is unreachable by Tab and unannounced; the same rows contain a SymbolButton so focus order skips straight into the drilldown. Sheet Escape stacking (#2561) is being fixed in PR #2795.
  - Fix: Render the row as <button> (or add role=button, tabIndex=0, Enter/Space) with an aria-label naming symbol + status.

- **[llm-agent-architecture:llm-09] Late (post-soft-timeout) LLM responses are audited but never metered; Red Team late responses are not captured at all — the Usage page and budget ledger miss the most expensive calls** — `src/lib/strategy.ts:6594` · defect · Desktop web · effort S · CONFIRMED · prior 2026-07-20 grok #19
  - Why: llmFetchCapturing lets the request run to hardCap (up to 300s, llm-request.ts:458) after the soft timeout; the provider bills the full reasoning completion, then the fallback is metered on top. Only the fallback's spend reaches llm_usage/the daily budget, so timeouts on frontier reasoning seats (exactly the costly case) are invisible in /console/usage and to checkLlmDailyBudge…
  - Fix: In recordLlmOutcome's late branch call recordLlmUsage(context 'strategy-late') with the extracted usage; give the Red Team fetch an onOutcome that does the same. Show 'late/abandoned' rows on the Usage page.

- **[llm-agent-architecture:llm-12] Coach/Assistant routes models to NATIVE providers while the engine, rotation and the policy save-gate assume universal OpenRouter — an OpenRouter-only owner sees 'no key' for every Coach model and llama routes to OpenAI** — `src/lib/chat/llm.ts:655` · defect · Desktop web · effort M · CONFIRMED
  - Why: resolveLlmEndpoint (llm-provider.ts:295-310) serves EVERY strategy model through the OpenRouter key first and modelCredentialService returns 'openrouter' in prod ('all models are served through OpenRouter', app/api/policy/route.ts:291). The Coach instead resolves the model's native family key (llmForModel :721-723), /api/chat/providers (route.ts:16) lists only native services,…
  - Fix: Route Coach through resolveLlmEndpoint-style precedence (OpenRouter key first, native second) and report 'openrouter' in /api/chat/providers; add llama→meta to chatProviderForModel or reuse llmModelFamily.

- **[llm-agent-architecture:llm-14] Strategy page Red Team hint describes the retired two-pass design ('deeper adversarial debate on high-conviction … ideas'); fallback hint hides the implicit rotation fallback** — `app/console/strategy/page.tsx:722` · defect · Desktop web · effort S · CONFIRMED
  - Why: Since the single-adversary consolidation (O2: conviction gate removed) there is exactly one Red Team review per risk-adding opening and exits are never reviewed; the copy tells the owner a tier of review exists that does not. The Green fallback hint (:779) says fallbacks are only what the owner lists, but under rotation with an empty list the run appends two implicit pool fall…
  - Fix: Rewrite: 'Reviews every risk-adding opening (buy/short) after sizing; exits are never reviewed. Blank = fail closed → human review.' Add to the fallback hint: 'When the Green seat rotates and this is blank, the run tries up to 2 other eligible pool models.'

- **[market-data-integrity:mdi-06] Interactive Scan VWAP is yesterday's grouped-daily VWAP stamped with fetch time and described as today's session** — `src/lib/market.ts:1241` · defect · Desktop web · effort S · CONFIRMED
  - Why: fetchRecentGroupedBarsRest returns the newest PRIOR day's EOD bars but only `grouped.bars` is passed (api/scan/route.ts:110), so the drilldown tooltip 'Volume-weighted average price of the latest session. Price above VWAP = buyers paid up today' (drilldown-sections.tsx:699) compares today's price with yesterday's VWAP and stamps it as received now; it also overwrites any real…
  - Fix: Pass `grouped.date` into mergeGroupedBarData, stamp asOf = bar date, label 'prior-session VWAP (Aug 15)', and skip when the quote already carries a same-day vwap (Alpaca dailyBar.vw).

- **[market-data-integrity:mdi-07] Nasdaq screener asOf is a sentence ('Last price as of Aug 18, 2026'); it is stored as MarketQuote.asOf and renders 'Invalid Date' in the drilldown** — `app/console/ui/symbol-drilldown.tsx:511` · defect · Desktop web · effort S · CONFIRMED
  - Why: Rows that keep the screener asOf (no connected account, gateway failure, non-top quotesBySymbol names) show 'Quote data from the scan currently on screen (Invalid Date).' with no timezone label; policy/quotes-cascade parse it as NaN and treat it as missing; the sentence is also persisted to the field store as an as_of.
  - Fix: Parse the screener date into a real ISO (or leave asOf undefined) and keep the human sentence in a separate `asOfLabel`; guard the drilldown formatter with Date.parse and append ' CT'.

- **[ops-observability-admin:sre-06] Admin and console have no route-level error boundaries; a render crash drops to the public-site-styled root boundary and loses the admin shell** — `app/error.tsx:14` · defect · Desktop web · effort S · CONFIRMED
  - Why: `find app -name error.tsx` returns only app/error.tsx and app/global-error.tsx. Any thrown render error in app/admin/** or app/console/** unmounts the AdminLayout/console shell and shows a legacy glass-token page titled 'Dashboard error' with no nav, no theme tokens (con-*), and no link back. app/global-error.tsx:14-18 also carries a stale comment 'Dark is the app default' and…
  - Fix: Add app/admin/error.tsx and app/console/error.tsx that render inside the shell using con-* primitives (title 'This Page Failed to Load', digest, Retry, and links to /admin and /console); update global-error.tsx comment/palette to light-default. Reset via `reset()` and re-fetch.

- **[ops-observability-admin:sre-09] The LLM/OpenRouter chat lane has no Connections health lane; Admin Connections lists openai/anthropic/gemini/xai user lanes that never receive traffic under universal routing** — `app/api/admin/connections-health/route.ts:26` · defect · Desktop web · effort M · CONFIRMED
  - Why: llm-provider.ts:45 routes every model to OpenRouter outside tests, and none of llm-call.ts / llm-provider.ts / chat/llm.ts / red-team.ts import logApiHealth (rg confirms), so the page that promises 'per-provider call health: last success/failure, call volume, error patterns' has no row for the one provider every decision depends on, while showing four expected lanes that stay…
  - Fix: Log every OpenRouter chat/completions call via logApiHealth({service:'openrouter-llm', keySource:'user'|'env', ok, latencyMs, errorText scrubbed}) in llm-call.ts, add it (and rag-embed's actual provider) to EXPECTED_BACKEND_LANES, and drop/relabel the direct-provider user lanes as 'not routed (via OpenRouter)'.

- **[ops-observability-admin:sre-13] Admin overview fabricates 'CPU load 0.0%' and '0 Running' when the series/probe is empty or failed** — `app/admin/page.tsx:448` · bug · Desktop web · effort S · CONFIRMED · prior 2026-08-06 D6
  - Why: emptyMetrics() (server-metrics/route.ts:694-702) returns cpu: [] whenever Hetzner is unconfigured or its read failed, and [] is truthy, so currentCpu = slice(-1)[0]?.value ?? 0 (line 224) renders a confident '0.0%' with a meter; the containers line renders '0 Running' when the probe 403'd/failed or Coolify was not queried (resourcesObservation.state === 'unavailable'). The ful…
  - Fix: Treat cpu.length === 0 as 'Unavailable' (no meter); when resourcesObservation.state !== 'known' print '—' with the observation reason instead of a count; reuse the same asOf/stale chip the server page shows.

- **[performance-accounting:perf-07] Tax 'Short-term realized' silently excludes every covered short (short-side lots skipped)** — `src/lib/tax.ts:352` · bug · Desktop web · effort S · CONFIRMED
  - Why: A closed short is a realized (always short-term) gain/loss; the Tax card's ST realized, estimated liability, and wash-sale figures omit them entirely, so a shorting account under-reports taxable results. Estimates-only label does not excuse a systematic omission.
  - Fix: Include side==='short' lots as short-term realized (holding period does not matter for shorts); apply the same loss/wash logic to short covers at a loss.

- **[public-site-growth:dpub-02] Every Auth.js error (OAuth failure, Apple re-auth without email, config error) is routed to /access-denied, whose only copy is “ask the owner to add your email to the allowlist” and which offers no way out** — `src/lib/auth/auth.ts:197` · defect · Desktop web · effort S · CONFIRMED
  - Why: A transient Google/GitHub callback error, a GitHub account with no verified email, or the documented Apple second-authorization case (auth.ts:14-18) all show the allowlist message — wrong diagnosis for the owner (who IS the allowlist) and misleading for a friend/family user. The page is also a dead end: no “Sign out and try another account” (`/logout`), no “Sign in”, no “Reque…
  - Fix: Read `searchParams.error` on /access-denied (or point `pages.error` at /login and render an inline error there): map `AccessDenied` → allowlist copy + Request access mailto; `OAuthCallbackError`/`Configuration`/`Verification` → “Sign-in failed, try again”; Apple-no-email → the fallback-provider hint already written at app/login/page.tsx:100-102. Add links to `/logout` (“Sign o…

- **[ux-copy-editor:copy-01] Two-space sentence rule is unenforced across ~600 web copy strings; SENTENCE_GAP used in only 9 files** — `app/console/page.tsx:265` · defect · Desktop web · effort L · CONFIRMED
  - Why: FLEET-UI-COPY: two spaces between sentences everywhere humans read prose (NBSP+space or SENTENCE_GAP in JSX). rg finds 596 same-line single-space sentence gaps in user-facing web strings (481 in app/console, 17 in app/admin, ~98 on public pages; 168 of them tooltips/hints), versus only 35 SENTENCE_GAP/NBSP uses in 9 files. Worst files: guardrails/field-defs.ts (42), framework/…
  - Fix: Sweep the console prose through SENTENCE_GAP (JSX text) or literal " " (string literals/hints/tooltips), starting with the always-visible surfaces (chrome.tsx account/run-state copy, dashboard page.tsx, approval-card, guardrails field hints, settings/help glossary), then add an eslint no-restricted-syntax/regex check for `[a-z)]\. [A-Z]` inside JSX text and `hint:`/`title=` st…

- **[ux-copy-editor:copy-08] Web console headings and buttons are sentence case ("Run once", "Commit changes", "Win rate") against the Title Case rule and against iOS ("Run Once", "Win Rate")** — `app/console/components/chrome.tsx:789` · defect · Desktop web · effort L · CONFIRMED
  - Why: FLEET-UI-COPY: Title Case for headings/titles/buttons (`Run Once`, `Win Rate`, `Price Alerts`, `Delete Account`). Console card titles are overwhelmingly sentence case — "Signal health", "Run cadence", "Protective stops", "Price alerts", "Broker connections", "Advanced rulebook", "Data sources", "Event notifications", con-card-title "Win rate" — while iOS uses "Win Rate", "Pric…
  - Fix: Decide once with the owner (the rule as written says Title Case) and apply mechanically: Card title=, con-card-title, AdvancedGroup title, Sheet title, Btn labels, settings TOC — leaving values/statuses/subtitles sentence case. Fix "Options And Event Contracts" → "Options and Event Contracts" either way. Add a labels lint or a checklist item to the console README.

- **[ux-copy-editor:copy-13] Scolding + inaccurate breaker copy: "a hard limit did its job. Review what fired before loosening anything." while breakers are advisory by default** — `app/console/components/chrome.tsx:662` · defect · Desktop web · effort S · CONFIRMED
  - Why: Owner ruling: guardrails are adjustable preferences, never a scolding ritual; and the copy is factually off — the daily-loss/drawdown/accuracy breakers default to advisory (field-defs.ts:28-30 say so). Same pattern in settings/help.tsx:142 ("per-account hard limits the policy gate enforces … loosening one on a brokerage account costs a typed word" — the typed word is off-switc…
  - Fix: chrome.tsx:662 → "A breaker fired on this account. What tripped it, and its current response, is on Guardrails." help.tsx:142 → "The per-account caps the policy gate applies … Editable on Guardrails; typed confirmation for loosening is optional (Settings → Advanced action confirmation)." help.tsx:88 → describe both settings neutrally without "recommended"/"safety net".

- **[web-accessibility:a11y-10] Scan 'Columns' popover: no aria-expanded/controls, no Escape, focus not moved in** — `app/console/scan/scan-table.tsx:356` · defect · Desktop web · effort S · PARTIAL — Confirmed on inspection: scan-table.tsx Columns trigger button has no aria-expanded/aria-controls, and grep for Escape/useFocusTrap/aria-expanded/aria-controls in the file returns nothing — mouse-only overlay. Already addressed by open PR #2795 ('Scan Columns… · prior 2026-08-06 #2561 (4) — still open on main
  - Why: The popover holds checkboxes and Move earlier/later buttons but is not announced as a disclosure (no aria-expanded/aria-controls), Escape does nothing, and focus stays on the trigger — the reordering UI is effectively mouse-only for AT users while every other overlay in the console uses useFocusTrap.
  - Fix: Add aria-expanded={columnsOpen} aria-controls={id} to the trigger, wrap the panel with useFocusTrap(ref, columnsOpen, { onEscape }) as a role="dialog" aria-label="Scan columns", and drop the fixed backdrop <button> in favor of an aria-hidden div.

- **[api-contract-web-ios:api-07] Snapshot decode is all-or-nothing: one malformed element in any list blanks the whole iOS app** — `ios/SocraticTrade/MobileModels.swift:58` · risk · iOS · effort M · CONFIRMED
  - Why: decodeIfPresent throws (not nil) on a wrong-shaped element, so a single NaN→null number or missing timestamp anywhere in positions/orders/fills/alerts/recentCommands turns into MobileAPIError.decoding → "Couldn't load your workspace" with no way to see the desk. The comment on `catalog` shows the team already hit this class once.
  - Fix: Decode list sections with a lossy array wrapper (skip and log bad elements) and wrap `performance`/`benchmark` in try?; keep `readiness`/`policy` strict. Pair with a server-side schema assertion (api-06).

- **[ios-engineering:ios-03] 3-minute staleness gate disables Approve/Run/Watchlist/Policy after any quiet 3 minutes because there is no polling fallback and SSE only fires on demand-driven events** — `ios/SocraticTrade/MobileStore.swift:223` · defect · iOS · effort M · CONFIRMED
  - Why: `lastUpdatedAt` only advances on `load()`, which runs on scene-active, after a command, or on an SSE payload event; the events endpoint emits nothing on an idle desk (`: ping` heartbeats only — app/api/mobile/events/route.ts:41). Reading rationales for 3 minutes on a quiet evening then tapping Approve or Add to Watchlist is refused and the pill turns "Stale". Owner ruling: gua…
  - Fix: Refresh-then-act: when `canSubmit` fails only on staleness, `await load()` and re-check before submitting instead of refusing; add a light foreground poll (every 60–120 s while `.active` and no SSE payload in the last N s) so `lastUpdatedAt` stays honest; keep the pill.

- **[ios-engineering:ios-05] Cold-start push tap can be dropped: `route` is nil until ContentView's `.task` runs `configure(route:)`, and `didReceive` calls `self.route?(destination)` with no buffering** — `ios/SocraticTrade/PushNotifications.swift:441` · risk · iOS · effort S · CONFIRMED
  - Why: For a launch-from-notification tap, UNUserNotificationCenter delivers `didReceive` right after `didFinishLaunching`; the SwiftUI `.task` that installs the router runs after first render. Both land on the main actor with no ordering guarantee, so the destination can be silently discarded and the user lands on Home instead of the proposal — the exact scenario the PushAppDelegate…
  - Fix: Store a `pendingDestination` in the coordinator when `route == nil` and flush it inside `configure(route:)`; add a unit test that calls `didReceive` before `configure` and asserts the destination is delivered.

- **[ios-engineering:ios-09] No CI job runs the Swift test suite — ios-build.yml compiles only, the ship script archives only; 15 XCTest files (~2.4k lines) never execute anywhere but a developer's Mac** — `.github/workflows/ios-build.yml:72` · defect · iOS · effort S · CONFIRMED
  - Why: DeepLinkTests, PushNotificationTests (the deep-link contract the server test pins against), OrderCancelTests, PolicyTighteningTests and MobileModelsTests guard money-adjacent behavior, yet the required `verify` gate is TS-only and the iOS gate is compile-only; a regression in the Swift routing/decoding tables merges green. ios/CLAUDE.md already documents the simulator test com…
  - Fix: Add a `test` step to ios-build.yml (or a sibling job on the xcode26 Mac runner): `xcodebuild test -project 'ios/Socratic Trade.xcodeproj' -scheme SocraticTrade -destination 'platform=iOS Simulator,name=<available iPhone>' CODE_SIGNING_ALLOWED=NO -resultBundlePath …`, discovering the simulator name from `xcrun simctl list`.

- **[ios-engineering:ios-10] PrivacyInfo.xcprivacy still absent although the app uses UserDefaults (required-reason API) — ITMS-91053 on upload / future App Store rejection** — `ios/project.yml:39` · defect · iOS · effort S · PARTIAL — True on main (`find ios -name '*.xcprivacy'` -> none; UserDefaults used in MobileStore.swift:139/144, CoachView.swift:10, MobileControlView.swift:96). Already addressed by in-flight PR #2794 (cursor/ios-release-readiness-2560-b532: adds ios/SocraticTrade/Priv… · prior 2026-08-06 H #2560 (still open)
  - Why: Apple has required a privacy manifest declaring `NSPrivacyAccessedAPICategoryUserDefaults` (reason CA92.1) since 2024; uploads warn today and are slated to be rejected. Flagged 2026-08-06 (#2560) and still open — ITSAppUsesNonExemptEncryption from the same batch was fixed, this half was not.
  - Fix: Add `ios/SocraticTrade/PrivacyInfo.xcprivacy` (NSPrivacyAccessedAPITypes: UserDefaults CA92.1; NSPrivacyCollectedDataTypes: email/financial info, linked, not tracking; NSPrivacyTracking false) and ensure XcodeGen picks it up as a resource; verify with `xcodebuild -exportArchive` output.

- **[ios-hig-ux:ios-02] Coach replies render markdown as raw text (`**bold**`, `## headings`, `- bullets`, tables) — web renders GFM** — `ios/SocraticTrade/CoachView.swift:272` · bug · iOS · effort M · CONFIRMED
  - Why: `Text(String)` is verbatim in SwiftUI (only string literals parse markdown), so the same assistant answer that reads cleanly on the web shows literal asterisks, pound signs and pipe tables on the phone. Coach is the owner's 'ask the desk' surface; unreadable replies undercut it.
  - Fix: Render assistant turns with `Text(AttributedString(markdown: text, options: .init(interpretedSyntax: .full)))` (or a small block renderer for headings/lists/tables since AttributedString flattens block structure), keep user turns plain, and add `.textSelection(.enabled)` so answers can be copied.

- **[ios-hig-ux:ios-03] Coach 'Suggested Draft' cannot be staged from iOS, yet the copy tells the owner to approve it in Proposals** — `ios/SocraticTrade/CoachView.swift:323` · defect · iOS · effort M · CONFIRMED
  - Why: The web draft card has a Stage action that creates a proposal via /api/proposals/from-draft; iOS CoachDraftCard is display-only (no button, no client call — `grep from-draft ios/` is empty). The card's own copy implies the draft is already waiting in Proposals, so the owner will open Proposals, find nothing, and conclude the app lost it.
  - Fix: Add a 'Stage for Approval' button on CoachDraftCard that POSTs /api/proposals/from-draft (mobile-side wrapper in MobileAPIClient), shows the staged/deduped result, and bumps the Proposals badge via `store.load()`. Until then, change the copy to say the draft is not yet a proposal and must be staged on the web.

- **[ios-hig-ux:ios-04] Scan table header advertises three side-by-side columns (Score · Px · Chg) but every row stacks those values vertically — header and rows do not align** — `ios/SocraticTrade/ScanView.swift:158` · defect · iOS · effort S · CONFIRMED
  - Why: The header row is a fixed-width three-column HStack while ScanRow renders score/price/change as one trailing VStack, so the labels float over nothing and 'Px' can sit above a change percentage. It reads as a broken table on the one iOS screen that is explicitly 'the interactive scan table'.
  - Fix: Either drop ScanTableHeader and label values inline (e.g. '82 score · $184.20 · +1.3%'), or make ScanRow a real three-column HStack using the same fixed widths (and drop the fixed widths in favor of `Grid`/`LazyVGrid` so Dynamic Type does not truncate). Add sort control (score/change) via a toolbar Menu to match the web scan.

- **[ios-hig-ux:ios-05] Insights hardcodes 'vs SPY' / 'SPY' while the snapshot carries `benchmark.benchmarkSymbol` (Home/Results use it) — wrong label when the benchmark is ^GSPC or a sector index** — `ios/SocraticTrade/InsightsView.swift:74` · bug · iOS · effort S · CONFIRMED
  - Why: Owner ruling (2026-08-12) prefers real index data (^GSPC, GICS sector indices) with ETF fallback, so the benchmark symbol is not always SPY. Insights would then attribute the comparison to the wrong instrument — misinformation on a P&L-adjacent card.
  - Fix: Use `benchmark.benchmarkSymbol` in both title and detail (as Home and Results already do); ideally share one BenchmarkLine view across the three screens.

- **[ios-hig-ux:ios-06] Activity tab is described as 'Everything the agent did, newest first' but shows only fills plus the phone's own commands — no strategy-run/decision journal, so a run_failed push lands on a screen that cannot show the failure** — `ios/SocraticTrade/MobileControlView.swift:64` · defect · iOS · effort M · CONFIRMED · prior in-flight PR #2804 (2026-08-17 audit) / #2841
  - Why: The web Activity page is the decision journal (runs, decisions, alert center). iOS Activity renders Today tiles, scheduler, 20 fills, and `recentCommands` (mobile commands only). After 'Run Once' on the phone the owner has no way to see the run outcome (no proposals? why? which model failed?) — the most common daily question. The 2026-08-17 audit (in-flight PR #2804) flagged t…
  - Fix: Short term: change the More detail to 'Fills and the actions you took on this phone.' Medium term: extend /api/mobile/snapshot with the last N strategy runs (status, outcome word, proposal count, failure reason) and render a 'Runs' section at the top of Activity that the run_failed/run_completed deep link scrolls to. Coordinate with in-flight #2841 rather than duplicating.

- **[ios-hig-ux:ios-07] Positive/warning tones use system Color.green/Color.orange — P&L values, pills, and metric tiles fail WCAG AA contrast on the light default theme; web already moved to darker brand pos/neg/warn hexes** — `ios/SocraticTrade/AppComponents.swift:13` · defect · iOS · effort S · CONFIRMED · prior 2026-08-04 R12; 2026-08-06 #2561 (web twin)
  - Why: System green (#34C759) on white/grouped background is ~2.2:1 and system orange (#FF9500) ~2.0:1 — below the 3:1 large-text floor, and these are exactly the Open P&L / Realized P&L numbers and 'Stale' pills the owner glances at outdoors. The web console fixed the same class of light-theme chip contrast (2026-08-06 #2561) by adopting #157f4e/#d1332e/#9a6209; iOS still ships the…
  - Fix: Define AppPalette.positive/negative/warning as trait-aware UIColors mirroring the console tokens (#157f4e / #d1332e / #9a6209 light; the dark twins from console.css), keep soft backgrounds at 0.09-0.12 opacity, and add an XCTest asserting each light foreground vs `.systemGroupedBackground` ≥ 4.5:1.

- **[ios-hig-ux:ios-08] Switching to a non-paper account triggers a destructive-role 'Use Brokerage Account?' alert while paper switches instantly — paper-as-safe-default ceremony the owner has ruled out** — `ios/SocraticTrade/HomeView.swift:1049` · defect · iOS · effort S · CONFIRMED
  - Why: Owner ruling: 'An account is an account… don't treat paper as a safe home base, and don't add are-you-sure-it's-real-money ceremony.' Selecting which account the desk points at is not an order; the web Connections page switches with one click for every environment. The asymmetric alert (destructive red button) re-imposes exactly the paternalism the rulings forbid, and adds a t…
  - Fix: Remove `confirmingLiveActivation`; call `activate()` for every environment. If any acknowledgement is wanted, make it symmetric and non-modal (haptic + 'Switched to X' toast).

- **[performance-accounting:perf-13] iOS drops benchmarkUnavailable / unverifiedFlows / subPeriods and always claims 'cash flows neutralized'** — `ios/SocraticTrade/MobileModels.swift:458` · defect · iOS · effort S · CONFIRMED
  - Why: When the SPY feed is dead the web shows a first-class 'benchmark unavailable' state; iOS just hides the card. When flows were 'inferred — unverified' and excluded, or no flows were detected, iOS still asserts neutralization. Same number, less honest framing on the phone.
  - Fix: Decode benchmarkUnavailable and unverifiedFlows; render the unavailable reason; make the caption conditional on cashFlowAdjusted and mention unverified count.

- **[security-auth:sec-05] Mobile web-auth handoff mints a session code for ANY authenticated GET with an arbitrary code_challenge — a scheme-squatting app plus one tapped link steals a 30-day session** — `app/api/mobile/auth-redirect/route.ts:24` · risk · iOS · effort M · CONFIRMED
  - Why: The server never saw the challenge at flow start, so it cannot tell an app-initiated callback from a link an attacker crafted with THEIR challenge. Chain: victim is logged into socratictrade.com in Safari and has any app that registers the socratictrade:// scheme (DeepLink.swift:31-33 itself notes 'any app on the device can register the same scheme'); attacker sends https://so…
  - Fix: Bind the challenge at flow start: add POST /api/mobile/auth/start that stores {code_challenge, exp} in a short-lived HttpOnly, SameSite=Lax cookie (or server-side nonce) that auth-redirect must present and match; and/or move the callback to a universal link (ASWebAuthenticationSession .https(host:path:) on iOS 17.4+ with the AASA already served) so only the signed app can rece…

- **[ux-copy-editor:copy-12] iOS proposal cards show a red "Live" pill on every live proposal — the rule and the iOS Home hero both say no Live callouts** — `ios/SocraticTrade/ProposalsView.swift:366` · defect · iOS · effort S · CONFIRMED
  - Why: FLEET-UI-COPY: prefer not saying Live; no Live dots/pills. The web equivalent already renders BROKER (ui/primitives.tsx:177 LiveTag) and the iOS Home hero deliberately shows only a paper badge, so the Proposals tab is the one surface still shouting Live in red on the primary money-path card.
  - Fix: Drop the execution-mode pill for live proposals (keep a muted "paper" pill only), or render "Broker" in accent color to match the web LiveTag; keep the typed-confirm sheet wording ("Confirm Order for AAPL") without "Live".

- **[mobile-web-ux:mweb-02] iOS Safari status/toolbar tint follows OS scheme, not the console's actual (default LIGHT) theme** — `app/console/layout.tsx:15` · defect · Mobile web · effort S · CONFIRMED
  - Why: Owner ruling: console defaults to light regardless of OS (useConsoleTheme.ts:19 "Missing / unknown → light"), and the user can also pick dark while the OS is light. theme-color is what iOS Safari 15+ / Chrome Android paint behind the URL bar and toolbar, so an OS-dark owner gets a near-black browser chrome over a light page (and vice-versa after choosing Dark). Same mismatch f…
  - Fix: In useConsoleTheme (or a shell effect) write the resolved --con-bg into `<meta name="theme-color">` and set body background/color-scheme to match the console's data-theme; keep the static media-query pair only as SSR fallback.

- **[mobile-web-ux:mweb-05] iOS Safari auto-zooms on focus: ⌘K search input is 15px and Coach model/reasoning pickers + custom-model field force 11px inline** — `app/console/console.css:1251` · defect · Mobile web · effort S · CONFIRMED
  - Why: The project already knows iOS zooms any focused control under 16px (globals.css:225-231, console.css:654-660), but two paths bypass that floor: the palette input is a more-specific unlayered selector at 15px, and the Coach selects/TextInput set fontSize inline (inline beats everything). Opening ⌘K or tapping the model picker on an iPhone zooms the whole page and leaves it zoom…
  - Fix: Add `.con-cmdk-search input` to the ≤640px 16px block; replace the inline fontSize on chat.tsx selects/input with a class that yields to the phone floor (or `text-[length:var(--con-fs-xs)] max-sm:text-[16px]`).

- **[mobile-web-ux:mweb-06] Three different hard-coded bottom offsets vs the real tab-bar height — save bar and toasts collide with the tab bar when the home-indicator inset appears** — `app/console/components/policy-form.tsx:358` · defect · Mobile web · effort S · CONFIRMED
  - Why: The bar is ~59px of content (9+28+2+15+4 padding/icon/gap/label + 1px border) plus env(safe-area-inset-bottom), which on iOS Safari becomes non-zero as soon as the toolbar collapses on scroll. `bottom-16` (64px, z-40 < bar z-50) puts the Guardrails "Review & save" bar partly under the tab bar; toasts at 76px overlap tab labels; .ac-actions at 3.5rem sits ~3px under the bar eve…
  - Fix: Publish the measured bar height as a CSS variable on .console-root (`--con-tabbar-h`) from MobileTabBar's ResizeObserver, and use `calc(var(--con-tabbar-h) + 8px)` for .ac-actions, .con-toasts and the policy save bar; give the save bar z-index above the bar or the same clearance rule.

- **[mobile-web-ux:mweb-07] Bottom sheets / More sheet / palette do not lock or contain scroll — page behind scrolls on iOS while a modal is open** — `app/console/console.css:858` · defect · Mobile web · effort S · CONFIRMED
  - Why: Only .con-drawer-body sets `overscroll-behavior: contain` (console.css:975). Scrolling a bottom sheet past its end, or dragging on the 12dvh scrim above it, scroll-chains to the underlying page on iOS Safari; the fixed sheet stays put while content behind moves and Safari's toolbar shows/hides, shifting env() insets under the sheet. On money sheets (STOP, Approve live typed-co…
  - Fix: Add `overscroll-behavior: contain` to .con-sheet, .con-cmdk-list and the TabsSheet scroller, and lock body scroll while any modal is open (set `overflow:hidden` on html/body or `position:fixed; top:-scrollY` pattern, restored on close) via a tiny useScrollLock hook shared by Sheet/TabsSheet/SymbolDrawer/CommandPalette.

- **[mobile-web-ux:mweb-08] Full-screen overlays (symbol drawer, sheets, More sheet) ignore browser history — Android back / iOS edge-swipe leaves the console instead of closing the overlay** — `app/console/ui/symbol-drawer.tsx:63` · risk · Mobile web · effort M · CONFIRMED
  - Why: On phones the drawer covers the entire viewport and reads as a new screen; the reflexive dismiss is the back gesture. Today that navigates away from /console/scan or /console/approvals (losing scroll/filter state), and the drawer/sheet reopens on forward. Escape (the only non-tap close) does not exist on a phone keyboard.
  - Fix: On open, `history.pushState({overlay:id})`; close on `popstate`; on programmatic close, `history.back()` if our state is on top. Wrap in the same hook as the scroll lock (mweb-07). Also make the drawer header sticky-safe: it already is (flex column), fine.

- **[mobile-web-ux:mweb-09] Bottom-anchored sheets with typed-confirm inputs vs the on-screen keyboard — no interactiveWidget or visualViewport handling** — `app/console/layout.tsx:9` · defect · Mobile web · effort M · CONFIRMED
  - Why: Chrome Android ≥108 defaults to resizes-visual, and iOS never resizes the layout viewport, so a `position:fixed; bottom:0` sheet stays anchored to the layout-viewport bottom while the keyboard covers its lower ~40%: the typed-confirm field is auto-scrolled into view but the "Place broker order" / Cancel buttons (approval-card.tsx:1109-1120) can end up beneath the keyboard, and…
  - Fix: Add `interactiveWidget: "resizes-content"` to the console viewport export (fixes Android), and on iOS listen to `visualViewport` resize/scroll to set the sheet's `bottom`/`max-height` from `visualViewport.height` (or render sheets `top:0`-anchored when an input inside is focused). Keep the primary button visible above the keyboard.

- **[mobile-web-ux:mweb-10] Toggle switch is a 36x20 target with no coarse-pointer bump and no clickable label** — `app/console/console.css:671` · defect · Mobile web · effort S · CONFIRMED
  - Why: Every other pressable primitive got the `@media (pointer: coarse)` 44px floor (console.css:458-465, 538-542) but the switch did not, and its label text is not associated, so on a phone the only hit area for e.g. "Auto-resume on boot", the typed-confirm switch, and data-source toggles is 20px tall — a mis-tap on a boot-safety switch is a correctness issue.
  - Fix: Add `.con-toggle` to the coarse-pointer block via a transparent ::before hit-area (min 44x44 without changing visuals), and give Toggle an optional `id` so callers wrap the row in `<label htmlFor>` (or make the whole row clickable).

- **[web-accessibility:a11y-07] ~147 explanations live only in native title= on non-interactive elements; Tooltip triggers on Chip/Stat/Ago are not focusable nor aria-described** — `app/console/ui/primitives.tsx:485` · defect · Mobile web · effort M · PARTIAL — Core claim confirmed: Tooltip (primitives.tsx:483-510) sets no tabIndex on the wrapping span for non-interactive children and doesn't wire aria-describedby. Counts are roughly right (I independently found 148 title= on div/span/td/th/p, 66 Chip-title occurren… · prior 2026-08-06 #2561 (3) — still open on main
  - Why: rg counts 147 title= on div/span/td/th/p in app/console (e.g. scan-table.tsx:463 cell titles, chrome.tsx:1044 error text behind 'delayed', positions.tsx:89-95 column definitions) plus 66 <Chip title>. On the owner's phone browser (a primary client) and for keyboard users none of that is reachable; the Tooltip wrapper has no tabIndex for non-interactive children and never links…
  - Fix: In Tooltip: add tabIndex={0} + role="button"/aria-expanded when the child is not interactive, wire useId + aria-describedby to the tooltip node, and keep it mounted while focused. Replace load-bearing td/span titles (freshness error, scan cells) with tap-to-open Tooltip or an inline detail line; keep native title only as a redundant desktop hint.

- **[public-site-growth:dpub-03] Public explainer copy promises delegation controls that do not exist in the product (“Evidence threshold: minimum source count, contradiction tolerance, freshness window”; “Review mode: notify before/after action, alert only when blocked”)** — `app/how-it-works/page.tsx:94` · defect · Public site · effort S · CONFIRMED
  - Why: The “Authority and overrides” section is written as what the owner can configure (“The interface separates confidence from permission… delegated scope is”), but neither an evidence-threshold knob nor a before/after review-mode selector exists in TradingPolicy or the Guardrails UI (authority is `propose | decide`, types.ts:24). On a marketing page for a trading product, describ…
  - Fix: Rewrite AUTHORITY_ITEMS to the real surface: Ask-First vs Autopilot (`propose`/`decide`), per-account authority, close-only/halted system states, notional / daily-loss / concurrency / concentration caps, Socratic override mode + max % NAV, notification channels. Delete the two invented bullets or move them to a clearly labeled “planned” list.

- **[public-site-growth:dpub-04] /framework states an invariant (“Sizing is deterministic and code-side; advisory context cannot reach it… dissent can shrink risk, never enlarge it”) that the Socratic override sizing path contradicts — the model can raise notional above the deterministic size** — `app/framework/content.ts:226` · defect · Public site · effort S · CONFIRMED
  - Why: `applySocraticOverrideSizing` lets an LLM-authored `autonomyOverride.cashDeploymentPct` raise the sized notional (capped only by `socraticOverrideMaxPctOfNav`, which is `Infinity` when unset, and downstream policy caps) and it runs regardless of `socraticOverrideMode`. The owner chose this (2026-07-03 autonomy rollout), which is fine — but the public framework page, whose whol…
  - Fix: Amend the invariant and the intro (content.ts:42 “deterministic code — never a model — controls sizing”) to say: deterministic sizing by default; an on-the-record Socratic override may request a larger deployment, bounded by the owner's `socraticOverrideMaxPctOfNav` and the policy gate. Mirror in docs/trading-framework.md. Optionally gate `applySocraticOverrideSizing` on `socr…

- **[public-site-growth:dpub-05] Terms and Conditions were not updated when market-data pooling became a condition of use (#2815): Privacy §8 says “Using the app requires contributing…”, Terms are silent, effective date still July 10** — `app/terms-and-conditions/page.tsx:110` · defect · Public site · effort S · CONFIRMED
  - Why: The versioned clickwrap (src/lib/legal-notice.ts LEGAL_NOTICE_VERSION=1) binds users to the Terms, but the contractual obligation actually being imposed (mandatory pooling of quotes/fundamentals/news pulled through the user's own keys, and the shared research corpus) lives only in the Privacy Policy. Terms also lack governing law/venue and any mention of LLM/market-data provid…
  - Fix: Add a Terms section “Shared market-data pool” mirroring privacy §8 (what is pooled, what is never pooled, that it is required to use the service), a governing-law clause (owner's state), and a sentence on third-party processors; bump the effective date and `LEGAL_NOTICE_VERSION` so the clickwrap re-prompts once. Keep the two docs cross-linked as today.

- **[public-site-growth:dpub-06] Two-space sentence rule is not implemented on any public page: zero NBSP+space gaps, ~40 single-spaced sentence breaks and ~27 raw double ASCII spaces (collapsed by the browser)** — `app/welcome/page.tsx:230` · defect · Public site · effort M · CONFIRMED
  - Why: docs/FLEET-UI-COPY.md §Two spaces: “HTML/JSX… that collapses spaces: use NBSP+space… or `SENTENCE_GAP`”. The public pages predate the helper and mix single-spaced prose with double-ASCII spaces that render as one; the legal pages (the longest prose on the site) are entirely single-spaced. Owner has reaffirmed this rule repeatedly (2026-08-08/10).
  - Fix: One sweep: import `SENTENCE_GAP` from app/console/lib/format.ts (or add a shared `src/lib/sentence-gap.ts` so public pages don't import console code) and join sentences in FEATURES/STEPS/CORE_LOOP/DECISION_FILE/SECTIONS arrays and JSX text with it; for the string arrays, store ` ` literally. Add a vitest that renders each public page and asserts no `[.!?] [A-Z]` single-space b…

### Could Be Improved (38)

- **[api-contract-web-ios:api-11] zod is a dependency but unused by all 122 routes; four different error envelopes plus plain-text 401/403 from middleware** — `middleware.ts:416` · improvement · API · effort L · CONFIRMED
  - Why: Clients must guess: iOS serverMessage tries `message` then `error` then raw text (MobileAPIClient.swift:437-451); browser code calling res.json() on a text 401 throws. Bodies are parsed with `request.json().catch(() => ({}))` and hand-rolled type checks (mobile/commands/route.ts:30-35, chat/route.ts:83-89), so limits like GET /api/mobile/commands?limit=abc become NaN → SQL LIM…
  - Fix: Introduce one `apiError(status, code, message)` helper returning `{ ok:false, error: code, message }` and use it in middleware and routes; adopt zod schemas at least for /api/mobile/commands payloads, /api/policy PATCH and /api/chat (the dependency is already there).

- **[qa-test-strategy:qa-17] No invariant test that every API route resolves the request user or is explicitly public** — `test/route-ownership.test.ts:18` · improvement · API · effort S · CONFIRMED
  - Why: Owner ruling: keep security isolation. Today a new route that forgets `resolveRequestUserId` is caught only by middleware, and nothing lists which routes are intentionally public, so the allowlist drifts silently.
  - Fix: Add test/api-route-auth-invariant.test.ts that enumerates app/api/**/route.ts and asserts each file either references resolveRequestUser/resolveRequestUserId/requireAdmin or is in an explicit PUBLIC_ROUTES list with a one-line reason.

- **[backend-reliability:berel-06] No event-loop lag instrumentation anywhere — every loop-pin incident was reconstructed from broker latency and Uptime Robot; retries under a pin (awaitWithFirstCallRetry) just double broker load** — `src/lib/inflight-deadline.ts:128` · improvement · Backend · effort S · CONFIRMED
  - Why: The last week of receipts (alpaca-broker avg 3 s / max 14 s, ftsMirrorSlice 6-13 s, /api/health 4.2 s) are all symptoms of the same variable — event-loop delay — which is never measured. timeSync only covers hand-picked sync calls. Because logApiHealth latency includes queueing behind a pinned loop, broker 'slowness' is misattributed to Alpaca and the 16 s first-wait + second…
  - Fix: In instrumentation.ts start `perf_hooks.monitorEventLoopDelay({resolution: 20})`, expose p50/p95/p99 + max on /api/health and the ops snapshot, log `[slow-loop]` when p99 > 500 ms, and count it in journalLane records. Make the first-call retry conditional on loop p99 being low (a slow loop means 'wait', not 'retry'). Feed the number to the admin Operations panel so pins are se…

- **[backend-reliability:berel-07] All CPU-heavy RAG work (HTML extraction, chunking, FTS5 writes, JSON.parse of MB artifacts) runs on the single serving+trading event loop; no worker_threads or sidecar exists** — `src/lib/rag/sec-ingest-worker.ts:463` · opportunity · Backend · effort L · CONFIRMED · prior in-flight PR #2807 F4
  - Why: Four rounds of bounds (40-row groups, 250 ms adaptive stretch, 20/6 s slices, 6/2 s slices, pause-on-strategy) have each traded ingest throughput for a still-nonzero pin on the same loop that places orders and answers Docker health. Yielding cannot make a 300 ms synchronous SQLite call fair to HTTP. In-flight PR #2807 (F4) already names a sidecar/worker as the durable fix; not…
  - Fix: Move parse+chunk+FTS mirror into a `worker_threads` Worker that opens its own better-sqlite3 connection (WAL allows a second writer with busy_timeout) and receives {taskId, artifacts} messages; the main thread only claims tasks, heartbeats the lease and advances checkpoints. Land berel-01 first so the worker's per-row cost is O(1); then the SEC_INGEST_WORKER_ENABLED knob can s…

- **[backend-reliability:berel-09] Alpaca getEquityOrders walks the ENTIRE order history (status:all, up to 50×500) on every call, and it is called every 60 s by the stale-limit lane and again by pending-fill reconcile and the dashboard** — `src/lib/alpaca.ts:491` · improvement · Backend · effort S · CONFIRMED
  - Why: scheduler.ts:884 (`gw.getEquityOrders` in stale-limit-scan) and strategy-execution.ts:1543 fetch it per tick; an autonomy account with a few thousand historical orders pays N sequential REST round trips per minute, so broker latency and 200 req/min budget degrade linearly with account age — plausibly part of the 6-14 s alpaca-broker latencies now blamed on the loop.
  - Fix: Add an `after` window (e.g., 30 days, or the oldest still-pending fill/proposal timestamp) to the default walk, expose a separate `getOpenEquityOrders` for the stale-exit and reconcile lanes, and memoize the per-tick result across lanes for the same account. Keep the unbounded walk only for the explicit history/backfill path.

- **[llm-agent-architecture:llm-07] Every Red Team review re-sends the ENTIRE Green user content (all candidates, RAG, learned, reflection, scorecards) — RedTeamReviewContext documents a subset the code ignores** — `src/lib/strategy.ts:6318` · improvement · Backend · effort S · CONFIRMED · prior 2026-08-06 D5
  - Why: red-team.ts:77-94 declares a curated context (macro, limits, portfolio, scorecards, analogs, candidatesUnderReview) and the approval-time re-review builds exactly that (strategy-execution.ts:580-593), but the run-time review spreads the full 48k-char evidence budget + full marketScan.topCandidates + retrievedFinancialContext + learnedContext + reflectionSummary, per opening, ×…
  - Fix: Project adversaryContext onto the interface keys + candidatesUnderReview + only the RAG snippets that mention the reviewed symbol; extend the Red data-not-command clause to whatever untrusted blocks remain. Expect a 3-5x Red-lane cost cut.

- **[llm-agent-architecture:llm-16] OpenRouter's billed `usage.cost` is never requested or stored; all cost figures come from a hand-maintained price table** — `src/lib/llm-usage.ts:180` · improvement · Backend · effort S · CONFIRMED · prior 2026-07-20 grok #18
  - Why: Every strategy/Red/tuning call goes through OpenRouter (llm-provider.ts:295-310) and OpenRouter returns exact billed credits when the body includes usage:{include:true}; instead estimateLlmCostUsd prices from MODEL_PRICE_PER_M (:59) plus a $15/M default for unknown ids, so /console/usage, the daily budget ceiling and usage-budget downgrades act on estimates that drift from the…
  - Fix: Add usage:{include:true} in buildLlmRequestBody for provider openrouter, persist usage.cost as cost_usd when present (fall back to the table otherwise) and flag rows as 'billed' vs 'estimated' on the Usage page.

- **[ops-observability-admin:sre-12] Broker-held protective stop declined/failed is audit-only; no owner push** — `src/lib/broker-protective-stops.ts:1542` · improvement · Backend · effort S · CONFIRMED · prior 2026-08-06 C4/C11
  - Why: All three failure branches (1542, 1551, 1588) write an audit row and queue a retry marker but call no sendNotification; the synthetic lane only pages after its own persistent failure (`protective_exit_failing`, synthetic-stops.ts:121). A trader who chose broker-held stops learns a stop is not resting only by scrolling Activity, and the 2026-08-06 review found positions with no…
  - Fix: Coalesce broker_protective_stop_error into the existing `protective_exit_failing` event (per symbol+fingerprint, one alert per streak, with 'synthetic fallback armed: yes/no' in the body) and show a 'Protection' badge on the position row when neither broker nor synthetic coverage is resting.

- **[performance-accounting:perf-14] Broker-authoritative equity history and cash-activity ledger are already implemented but unwired — inference could be replaced by facts** — `src/lib/alpaca-account-insights.ts:167` · opportunity · Backend · effort M · CONFIRMED
  - Why: Alpaca /v2/account/activities gives real CSD/CSW/ACATS transfers, DIV and FEE rows; /portfolio/history gives daily EOD equity and profit_loss; account.last_equity is the true prior-close baseline. Using them removes the entire class of phantom-flow bugs (perf-02/03, C1), fixes the 7-day window (perf-04), and gives a dividend/fee-aware P&L. The 'built-but-unwired rigor' theme f…
  - Fix: Per connected account: use activities (TRANS/CSD/CSW) as the flow ledger when available (fall back to inference otherwise, and label which), use portfolio/history for the daily equity curve, and last_equity for Day P&L. Needs per-account credential plumbing (resolvePrivateAlpacaAccount currently picks a ranked account).

- **[qa-test-strategy:qa-10] maxWorkers:1 serializes ~7,000 tests: `npm test` is ~14 min of every CI run** — `vitest.config.ts:31` · improvement · Backend · effort S · PARTIAL — Core technical claim confirmed: vitest.config.ts:31 area has maxWorkers:1, testTimeout:60_000; land.sh sets VITEST_MAX_THREADS which vitest.config.ts never reads (grepped — zero references), so it is genuinely inert. However the '#1493 reduced from 4' attribu…
  - Why: With forks isolation each file already gets its own process, so the flakiness that motivated 1 worker was real network + import cost (both since fixed per the 2026-07-05 note). Every PR pays ~10 extra minutes and land.sh locally too; the inert env var in land.sh gives a false sense of parallelism.
  - Fix: After qa-05's network guard lands, set maxWorkers to 3 (ubuntu-latest has 4 vCPU) and lower testTimeout to 20s with per-file overrides for the known slow importers; replace `VITEST_MAX_THREADS` in land.sh with `--maxWorkers`. Track a flake list by file rather than throttling the whole suite.

- **[api-contract-web-ios:api-05] proposal.approve / proposal.reject are queued FIFO behind a running strategy.run_once (up to 30 min) with no ETA, while the web approve path is synchronous** — `src/lib/mobile-api.ts:88` · improvement · Cross-surface · effort M · CONFIRMED
  - Why: An approve tapped on the phone during a run sits 'queued' until the run drains; the phone shows 'pending' with no queue position, and when it finally executes the price may have drifted enough that executeProposal returns 'proposed' (which api-02 then paints green). proposal.reject is a pure DB write and has no reason to wait. The console's approve returns 'busy' within a boun…
  - Fix: Make proposal.reject immediate; run proposal.approve immediately with the existing mutation-lease bounded wait so the phone gets 'busy'/'placed'/'blocked' in the same request (or return queue position + oldest-running startedAt in readiness.commandBacklog so the UI can say 'behind a run started 12m ago').

- **[frontend-performance:dweb-03] Dashboard snapshot is a 'load the universe' payload polled every 15 s: unused raw `audit[]` (incl. unbounded market_scan payloads), full 500-symbol `latestScan.quotesBySymbol`, full order history, duplicated profiles** — `src/lib/dashboard.ts:1108` · improvement · Cross-surface · effort M · CONFIRMED · prior 2026-07-20 #60
  - Why: `snapshot.audit` (100 raw audit rows with full payloads) has zero consumers in app/console, app/admin, or ios/ (grep) — the client uses `auditFeed`/`unifiedFeed`. When a `market_scan` audit is among the last 100 rows its whole scan (~600 KB avg, 2.7 MB p90 per the 2026-08-02 audit investigation) is sent as `audit[]` payload AND again as `latestScan`. `latestScan.quotesBySymbol…
  - Fix: Drop `audit` from the response (keep `auditFeed`/`unifiedFeed`); trim `latestScan` to `topCandidates` + `quotesBySymbol` filtered to symbols referenced by positions/orders/pending proposals (the client fallbacks in orders/page.tsx:278 and derive.ts only key by those); send `activeProfileId` instead of a second copy of `activeProfile`. Longer term split the snapshot into ETag'd…

- **[frontend-performance:dweb-09] Sentry Session Replay bundle (~38 KB gz) ships on every page although replay is env-disabled; Sentry core is ~160 KB gz of every console load** — `instrumentation-client.ts:31` · improvement · Cross-surface · effort S · CONFIRMED
  - Why: Statically referencing `Sentry.replayIntegration` keeps the replay module in the graph; the production build has chunk c9eecfba (120,992 B raw / 38,094 B gz, 68 `replay` refs) plus 8863 (509,252 B raw / 159,661 B gz Sentry core) loaded by every route in the approvals/scan/settings chunk sets. That is ~200 KB gz — about 40% of a console page's JS — for a feature that is off, an…
  - Fix: Use `Sentry.lazyLoadIntegration("replayIntegration")` inside the `replayEnabled` branch, and set `webpack.treeshake: { removeDebugLogging: true, excludeReplayIframe: true, excludeReplayShadowDom: true, excludeReplayWorker: true }` in next.config.mjs (options exist in @sentry/nextjs 10.70 types). Consider `removeTracing` if browser tracing is not used.

- **[frontend-performance:dweb-10] `motion` (framer) shared chunk (~40 KB gz / 124 KB raw) is loaded on every console page for one tooltip fade** — `app/console/ui/primitives.tsx:7` · improvement · Cross-surface · effort S · CONFIRMED
  - Why: primitives.tsx is imported by every console page, so chunk 9249 (124,371 B raw / 40,728 B gz, 53 `projection`/18 `layoutId` refs = full motion runtime) is in the critical JS of every route for a 150 ms opacity/translate on the Tooltip. That is roughly the size of the whole approvals page chunk.
  - Fix: Replace with a CSS transition (`transition: opacity .15s, transform .15s` + `@starting-style` or a data-open class) or, if exit animation matters, `LazyMotion features={domAnimation}` + `m.div` to load ~1/3 of the runtime lazily. Remove `motion` from dependencies if nothing else needs it.

- **[frontend-performance:dweb-11] HTML -> JS -> /api/dashboard is a strict waterfall: the (up to ~24 s) snapshot request cannot start until ~1.5 MB raw JS is parsed and hydrated** — `app/console/layout.tsx:22` · opportunity · Cross-surface · effort S · CONFIRMED
  - Why: Every console route is client-only behind the loader, so the first meaningful data request starts after download+parse of ~476 KB gz JS (1.47 MB raw for /console/approvals) and React mount — on a phone that is ~0.5-1 s of pure dead time added in front of a broker chain that is already the slowest thing in the product. The layout is a server component and could emit the hint fo…
  - Fix: In app/console/layout.tsx render `<link rel="preload" as="fetch" href="/api/dashboard" crossorigin="use-credentials">` (or an inline `<script>` that starts `fetch("/api/dashboard")` and stashes the promise on `window.__stSnapshot` for useConsoleData to adopt), and open the EventSource in the same early script. Measure with the browser Performance panel: first `/api/dashboard`…

- **[ops-observability-admin:sre-10] Provider/storage alerts re-fire on fixed cooldowns with no 'recovered' event and no 'since/nth reminder', and storage alerting runs only as a side effect of GET /api/health** — `src/lib/db-health.ts:574` · improvement · Cross-surface · effort M · CONFIRMED · prior 2026-08-06 D3 (mutes landed; recovery/closure not)
  - Why: alertConnectionFailure and alertStorageWarning (db-health.ts:775-843) send the same title every 6h/12h while a condition persists and never send closure (rg 'recover' in db-health.ts finds only a comment), so on the phone the owner cannot tell 'still broken since Monday' from 'broke again' and never learns it healed. All litestream/tier alerts are evaluated exclusively inside…
  - Fix: Track first-seen per alert key and include 'since <CT time> · reminder N' in the body; emit a one-shot '<service> recovered' / 'Backups healthy again' when the condition clears (clear the cooldown key on success). Move the storage/tier evaluation into a scheduler lane (every 5 min) with the same alertStorageWarning keys; keep /api/health read-only.

- **[trading-money-path:tsx-10] Approve is starved for the entire duration of an autonomous run (which can be 15-20 min per STATUS.md) because executeProposal contends for the whole-run strategy lock; console gives up after ~2 min, iOS shows 'Approved' (tsx-03)** — `src/lib/strategy-execution.ts:297` · improvement · Cross-surface · effort M · CONFIRMED
  - Why: The lock exists for a daily-cap TOCTOU that the owner has ruled advisory anyway, and the placement window is already serialized by withAccountMutation. Risk-reducing exit approvals (Risk-Exit sells) are refused while an LLM cycle owns the loop — the exact moment the owner most wants to act. app/console/lib/api.ts:306-309 retries busy for ~2 min then surfaces 'Approval is still…
  - Fix: Let executeProposal proceed on the mutation lease alone (re-reading dailyExecutionStats inside the lease), or at least never block sell/cover approvals on the run lock; keep the run lock only for run-vs-run exclusivity.

- **[trading-money-path:tsx-12] 'Not placed — safe to retry' has no retry path: not_placed is terminal and executeProposal refuses anything but status=proposed** — `src/lib/dashboard-ui.ts:326` · improvement · Cross-surface · effort S · CONFIRMED
  - Why: The lease-lost / broker-reachable-but-absent branches (strategy-execution.ts:1332,1388; strategy.ts:4011,4074) tell the owner it is safe to retry, and app/console/lib/thesis.ts:91 says 'can be reviewed and retried', but there is no button, endpoint, or status transition to re-queue the card. The owner must wait for the next run to re-propose (which may not) or hand-type a chat…
  - Fix: Add POST /api/proposals/[id]/requeue (CAS not_placed→proposed with a fresh refId, audit) and a Retry action on the console card + iOS; or auto-requeue once when the cause was lease-lost.

- **[desktop-web-ux:dweb-03] Home hierarchy: no attention/equity hero, account stats mislabeled, duplicated cadence/spend blocks** — `app/console/page.tsx:276` · improvement · Desktop web · effort M · CONFIRMED · prior 2026-08-04 R6
  - Why: A daily trader's first glance has no 'what needs me now' (NeedsAttention in components/needs-attention.tsx is unmounted; pending count only lives in the rail badge) and no equity headline; Portfolio Value/Day P&L sit under a learning-loop title. State chip + last-run repeat in the strategy bar (146-174) and the 'Run cadence' card (419-451); daily spend appears three times (str…
  - Fix: Top row: equity + day P&L hero, pending-proposals CTA, run state/next run; rename the stats card 'Account'; delete the Run cadence card; single spend indicator.

- **[desktop-web-ux:dweb-05] Decisions route is orphaned from nav; proposal lifecycle spread over five surfaces** — `app/console/decisions/page.tsx:1` · improvement · Desktop web · effort M · CONFIRMED · prior 2026-08-06 H #2556
  - Why: Decision traces are the product's evidence spine yet /console/decisions has no rail item (nothing is highlighted while on it), is reachable only via Home 'All Decisions', and Home rows/Approval cards/Activity runs never deep-link into a trace. Meanwhile 'Proposals', 'Decisions', Activity→Runs, Home Latest run/Previous Trades and Results→By thesis all present the same lifecycle…
  - Fix: Add Decisions to the Review group (or fold the index into Activity→Runs), link every proposal/decision row to its trace, and publish one status→label→tone map used by all five surfaces.

- **[desktop-web-ux:dweb-08] Positions blotter lives only on Home and lacks last price, day change, sort and any exit action** — `app/console/components/positions.tsx:89` · opportunity · Desktop web · effort M · CONFIRMED
  - Why: There is no Portfolio/Positions destination — a trader must scroll Home to see holdings, and cannot sort by P&L/weight, see today's move per name, or act (the only exit path is asking Coach to draft, then Approvals). The from-draft→approve rail already exists (app/api/proposals/from-draft), so a per-row 'Draft exit' would keep the no-manual-order-entry design.
  - Fix: Add a Portfolio destination (Monitor group) with sortable columns incl. Last/Day chg/Day P&L, and a 'Draft exit…' row action that posts a from-draft proposal for the owner to approve.

- **[desktop-web-ux:dweb-09] Three different save models across the Configure pages (and mixed within Guardrails)** — `app/console/guardrails/page.tsx:335` · improvement · Desktop web · effort M · CONFIRMED · prior 2026-07-20 UI #6/#12
  - Why: Guardrails = draft + 'Review changes' sheet, Strategy = blur autosave, Settings = per-control autosave with SaveStatus; the Tax card on Guardrails autosaves while its neighbours need Save. The owner cannot predict whether an edit is live yet, which matters when a cap or model choice affects the next run.
  - Fix: Pick one model per page class (money-affecting policy → draft+Review; preferences → autosave) and label the pattern in the card header ('Saves on Review' / 'Saves instantly').

- **[desktop-web-ux:dweb-10] Activity journal and Orders history cannot be searched, filtered or exported; hard caps** — `app/console/activity/page.tsx:216` · improvement · Desktop web · effort M · CONFIRMED
  - Why: 'Decision journal: everything the agent did' offers four tabs but no symbol/date/status filter, no search, no load-more beyond 60/40/100 rows; Orders shows the last 20 finished orders with no date range and no CSV. A daily trader reviewing a week, a ticker, or tax lots has to leave the app.
  - Fix: Add symbol/date/status filters + load-more to Activity, a date-range + 'Export CSV' on Orders/Fills (reuse /api/history), and link rows to decision traces.

- **[desktop-web-ux:dweb-11] Command palette lacks ticker lookup, STOP/Start, and account switch verbs** — `app/console/components/command-palette.tsx:160` · improvement · Desktop web · effort M · CONFIRMED · prior 2026-08-04 R7
  - Why: ⌘K is the desk's fastest surface but typing 'NVDA' does nothing (SymbolDrawer exists), and the money-critical verbs from the 2026-08-04 R7 backlog (STOP, Start, switch account) are missing while Run once is present — the one verb that spends. Settings hits landed (#2558) — good.
  - Fix: Add symbol search → open drawer, 'Stop agent'/'Start agent' → ControlSheet, 'Switch to <account>' rows, and 'Open proposal <SYM>'.

- **[desktop-web-ux:dweb-12] Multi-account desk: no cross-account overview; switching reloads the whole console** — `app/console/components/chrome.tsx:127` · opportunity · Desktop web · effort M · CONFIRMED
  - Why: With Paper + Roth live (STATUS.md), the rail badge, Home, and Orders show the active account only; the only cross-account glance is the state chip per row inside the scope menu (163-167). A pending Roth proposal is invisible while Paper is loaded, and each switch is a full reload with intro splash.
  - Fix: Add an 'All accounts' strip on Home (equity, day P&L, pending, run state per account, click to switch) and make the rail badge total pending across accounts with a per-account tooltip.

- **[frontend-performance:dweb-06] Single console context re-renders every page 3x per poll and once per SSE event because `fetching` and `stream` health live in the same context value as `snapshot`** — `app/console/lib/useConsoleData.tsx:341` · improvement · Desktop web · effort S · CONFIRMED
  - Why: 40 call sites read `useConsoleData()`, including every page root and the shell; only FreshnessStrip/MobileFreshnessBar (shell.tsx:202,211) read `stream`, and `retrying` is only meaningful pre-snapshot. Each poll produces three distinct context values (fetching=true, new snapshot, fetching=false) and every SSE event (`ready`, `market-data`, `order`, `proposal`, `dirty` — market…
  - Fix: Split into two contexts (`ConsoleSnapshotContext` = {snapshot, fetchedAt, error, refresh}; `ConsoleStatusContext` = {stream, retrying, slowFirstLoad, loading}); keep `lastEventAt/lastEventType` in a ref and only publish `status` transitions; only flip `fetching` state while `snapshot === null`. Wrap the freshness strips in the status context.

- **[llm-agent-architecture:llm-15] /console/usage still shows no embedding/rerank spend — RAG cost is only on the admin RAG-coverage page** — `app/console/usage/page.tsx:10` · improvement · Desktop web · effort S · CONFIRMED · prior 2026-08-06 D4
  - Why: meterEmbed/meterRerank (vector-db.ts:6777, rag-metering.ts) book the 10-K backfill and rerank calls that drove the 'OpenRouter credits low' alerts, but the owner-facing Usage page only lists chat/strategy LLM contexts, so the spend class most likely to drain prepaid credits is invisible where the owner looks.
  - Fix: Add an 'Embeddings / RAG' context row (embed tokens, rerank calls, estimated $) to LlmUsageClient from getRagUsageSummary, per account and per day.

- **[qa-test-strategy:qa-12] ESLint warn backlog has no ratchet: 49 console warnings (26 set-state-in-effect), 346 in src/api/admin** — `eslint.config.mjs:52` · improvement · Desktop web · effort M · CONFIRMED
  - Why: Because CI only fails on errors, the count can grow unbounded; `react-hooks/set-state-in-effect` in console pages is a real double-render/effect-cascade source on the phone-width site, and 162 unused vars is dead code the next agent will misread.
  - Fix: Add scripts/eslint-ratchet.mjs that stores the per-rule warning baseline in a JSON file and fails `verify` when a count rises; promote `@typescript-eslint/no-unused-vars` to error with `argsIgnorePattern: "^_"` after a one-time burn-down; burn the 26 console set-state-in-effect sites down first.

- **[web-accessibility:a11y-05] No skip link — ~22 focus stops (chrome bar + 15-item rail) precede <main> on every console page** — `app/console/components/shell.tsx:180` · improvement · Desktop web · effort S · CONFIRMED
  - Why: ScopeSelector, StateChip, Admin, Cmd+K, avatar, Run once, Stop Agent, then 15 rail links (nav.tsx:112-135) all sit before main content; there is no 'Skip to content' link anywhere (grep 'skip to' → none). WCAG 2.4.1 bypass blocks; a keyboard trader tabs through the whole chrome on every navigation.
  - Fix: Add a visually-hidden-until-focused <a href="#con-main" className="sr-only focus:not-sr-only …">Skip to content</a> as the first child of .console-root and id="con-main" tabIndex={-1} on <main>. Also move focus to the page h1 on client-side route change.

- **[ios-hig-ux:ios-09] First-run on iOS is a dead end: the checklist sends the owner to 'Account & Settings' to connect Alpaca/Robinhood, but that sheet can only select accounts; the in-flight Safari handoff loses the session — reuse the AdminPortalView cookie-sharing web view instead** — `ios/SocraticTrade/HomeView.swift:87` · improvement · iOS · effort M · CONFIRMED · prior in-flight PR #2794
  - Why: On an empty account the Home hero says 'Finish setup to trade' and points at a place that cannot connect a broker; universe copy (`:109`) points at 'the Strategy page' that does not exist on the phone. PR #2794 (open) adds 'Open Connections' / 'Open Strategy' links that open Safari — but Safari has no session cookie, so the owner must sign in again there. AdminPortalView alrea…
  - Fix: Generalize AdminPortalView into a `ConsoleWebSheet(path:)` fenced to /console/connections and /console/strategy, present it from the checklist CTA and the empty-accounts row, and call `store.load()` on dismiss so the new account/universe appears without a manual pull. Neutralize broker names in the copy ('Connect a broker account'). Coordinate with #2794 so the two do not both…

- **[ios-hig-ux:ios-10] Positions rows show market value and average cost but no per-position P&L, last price, or day change — the trader has to open every sheet to see what is winning** — `ios/SocraticTrade/MarketsView.swift:140` · improvement · iOS · effort S · CONFIRMED
  - Why: The unrealized P&L is already computable from the snapshot (PositionDetailCard does it) but the list hides it; a holdings list without P&L is the single most-missed glance on a phone. Rows also do not sort (biggest positions/losers first) and there is no per-position mark price.
  - Fix: Add a tinted P&L line (`$ and %`, pos/neg color) to PositionRow using the same formula, and a `sort` Menu (market value / P&L / symbol). Consider adding `lastPrice` and `dayChangePct` to the mobile snapshot's Position payload so the row can show '$184.20 · +1.3% today'.

- **[ios-hig-ux:ios-11] No success acknowledgement or haptics for money actions — Run Once, Approve, Cancel Order, Add to Watchlist, Create Alert just stop spinning** — `ios/SocraticTrade/AppComponents.swift:894` · improvement · iOS · effort M · CONFIRMED · prior 2026-08-04 R10
  - Why: After tapping Run Once the owner sees the spinner clear and no confirmation that a run was queued; approve/reject show an on-card strip but watchlist/alert/cancel/policy edits show nothing on success (and errors are hidden per ios-01). HIG expects a haptic and a transient confirmation for consequential actions; the 2026-08-04 panel already asked for an iOS command-outcome bann…
  - Fix: Add `.sensoryFeedback(.success/.error, trigger:)` on CommandButton outcomes and a lightweight toast overlay in the tab shell (‘Run once queued · Roth’, ‘Order cancel sent’, ‘NVDA added to watchlist’) driven by a `store.notice` published value; reuse the same overlay for ios-01 errors.

- **[qa-test-strategy:qa-09] MobileStore.submit/load are untested despite an existing URLProtocol harness** — `ios/SocraticTrade/MobileStore.swift:358` · improvement · iOS · effort M · CONFIRMED
  - Why: submit() is the path every approve/reject/stop/activate tap takes (idempotency key, busy dedupe, pendingAccountId, proposal feedback) and load() decides stale-snapshot fallback and 401 handling; none of that has a test, so a regression shows up as double-submitted commands or a blank Home on the phone.
  - Fix: Add ios/SocraticTradeTests/MobileStoreTests.swift using the existing MobileTestURLProtocol: (1) submit sends the Idempotency-Key header and refuses a second submit while busy; (2) submit with an unadvertised command sets `error` and does not hit the network; (3) load() on a 500 keeps the cached snapshot and sets snapshotLoadFailed; (4) load() on 401 after init routes through a…

- **[mobile-web-ux:mweb-11] No phone type-scale bump: 412 uses of 11px and 185 of 12.5px text render at desktop sizes on a 6-inch screen** — `app/console/console.css:90` · improvement · Mobile web · effort S · CONFIRMED
  - Why: Body copy on proposals, hints, table cells and chips is overwhelmingly `sm`/`xs`; on a phone held at arm's length 11-12.5px is below iOS/Android body defaults (17pt/16sp) and forces pinch-zoom on the very screens the owner triages from a phone. Because everything is tokenized, this is a one-block fix with no per-component churn.
  - Fix: Add `@media (max-width: 640px) { .console-root { --con-fs-xs: 12px; --con-fs-sm: 14px; --con-fs-base: 15px; --con-fs-md: 16px; } }` (and let 2xs stay for captions), then spot-check the chrome bar/tab bar which use literal 10px.

- **[mobile-web-ux:mweb-12] Phone chrome-bar controls are 32px (avatar, ⌘K trigger, run-state chip) — known follow-up still open** — `app/console/components/chrome.tsx:900` · improvement · Mobile web · effort S · CONFIRMED · prior 2026-08-04 P2 (32px avatar)
  - Why: The three secondary bar controls sit at 32px next to a 44px Stop button; the StateChip (chrome.tsx:300-315, py-1 with 11px/10px stacked text) is ~30px tall and opens the money-relevant ControlSheet. The 32px avatar was already flagged (2026-08-04 P2 "Settings touch targets (32px avatar)") and remains.
  - Fix: Give these a `.con-bar-ctl` hook and include it in the `(pointer: coarse)` 44px block (visual size can stay 32 via inner element); fix the stale 44px comment.

- **[mobile-web-ux:mweb-13] Overlay close/pin/row targets under 44px: sheet X is 18px, drawer X 32px, More-sheet pins 36px and rows ~35px, scan star 28px, expand toggle text-only** — `app/console/ui/sheet.tsx:157` · improvement · Mobile web · effort S · CONFIRMED
  - Why: On phones these are the primary ways out of a modal (no Escape key) and the primary way to pin tabs / watch a symbol / expand a proposal's full reasoning; an 18px X in the corner of a bottom sheet is easy to miss and mis-taps land on the header.
  - Fix: Standardize an `IconTarget` wrapper (44x44 hit area, 18px glyph) used by Sheet, TabsSheet, SymbolDrawer; add `@media (pointer:coarse){ .con-nav-item{min-height:44px} .ac-expand-toggle{min-height:44px} }` and bump WatchButton to h-9/w-9 with a coarse floor.

- **[mobile-web-ux:mweb-14] Watchlist has no phone card variant — a 5-column table with row actions in the last column scrolls its Alert/Delete buttons off-screen** — `app/console/watchlist/page.tsx:227` · improvement · Mobile web · effort M · CONFIRMED
  - Why: Orders (orders/page.tsx:402), Positions (positions.tsx:158) and Scan (scan-table.tsx:505) all switch to stacked cards below lg; Watchlist does not, so at 360-430px the ghost `Alert`/trash buttons (watchlist/page.tsx:262-286) sit past the right edge with no scroll affordance and 28px-tall `size="sm"` targets. Watchlist is a natural phone screen (glance at prices, arm an alert).
  - Fix: Add the same `lg:hidden` card list (symbol + price + armed count + Alert/Remove buttons at ≥40px) and hide the table below lg; while at it add a subtle right-edge fade to `.overflow-x-auto` tables that still exist on Results.

- **[mobile-web-ux:mweb-15] Home on a phone buries portfolio value / day P&L under the readiness hero, strategy bar and "Latest strategy run" list** — `app/console/page.tsx:217` · improvement · Mobile web · effort M · CONFIRMED · prior 2026-08-04 R6
  - Why: On phone the grid collapses to one column so the first thing after the strategy bar is a list of last-run proposals; equity, day P&L, cash and next-run appear only after scrolling past them. A trader opening the site on a phone wants the number first. Prior review R6 ("Ready-state Home density: equity/P&L hero") is still open.
  - Fix: Below xl, render a compact 2x2 stat strip (Portfolio value, Day P&L, Cash/BP, Deployed today) directly under the strategy bar (reuse the existing Stat components), and collapse Coach/Framework cards by default on phones.

## 5. P3 Findings (148) — Polish

### Needs Fixing (81)

| id | title | where | kind | surface | effort | verdict |
|---|---|---|---|---|---|---|
| api-contract-web-ios:api-10 | Mobile command backlog count and worker queue are global across users (not scoped to the requester) | mobile-api.ts:1158 | risk | API | S | CONFIRMED |
| api-contract-web-ios:api-14 | docs/mobile-api-and-clients.md is stale and omits most of the routes iOS actually depends on; /api/mobile/bootstrap has no caller | mobile-api-and-clients.md:34 | defect | API | S | CONFIRMED |
| api-contract-web-ios:api-17 | POST /api/mobile/commands maps any non-validation throw (DB, JSON) to HTTP 400 with the raw message; worker kick swallows failures | route.ts:65 | defect | API | S | CONFIRMED |
| ops-observability-admin:sre-17 | Server-knob writes are audited as user 'local' regardless of which admin flipped them | route.ts:80 | defect | API | S | CONFIRMED |
| performance-accounting:perf-19 | OOS backtest annualizes compounded overlapping-horizon returns by calendar days (overstates annualized return; Sharpe scaling inconsistent) | backtest.ts:1207 | defect | API | S | CONFIRMED |
| security-auth:sec-11 | /api/ops/* falls back to ADMIN_REINDEX_TOKEN when OPS_DIAGNOSTIC_TOKEN is unset — one token spans two trust levels | ops-auth.ts:21 | risk | API | S | PARTIAL (prior 2026-07-20 #52) |
| security-auth:sec-12 | x-admin-token path has no rate limit and no failure audit; middleware lets any bearer/x-admin-token request straight to admin handlers | admin.ts:88 | risk | API | S | CONFIRMED |
| backend-reliability:berel-05 | Docker HEALTHCHECK (5 s timeout) probes the heavy /api/health — measured 4.2 s under loop load — so a modest pin flips the container unhealthy and a… | Dockerfile:75 | risk | Backend | S | PARTIAL |
| backend-reliability:berel-11 | busy_timeout = 60000 on a synchronous single-connection means any external writer contention (rolling-deploy sibling container, litestream checkpoint… | db.ts:99 | risk | Backend | S | PARTIAL |
| backend-reliability:berel-12 | hasInFlightStrategyWork / markStaleRunningRuns scan strategy_runs by status with no status-leading index; the check runs every 5 s in the SEC worker,… | db-execution.ts:311 | defect | Backend | S | CONFIRMED |
| backend-reliability:berel-13 | litestream-runtime.log is appended forever with tee -a on the persistent volume; nothing rotates or truncates it | coolify-prod-start.sh:223 | risk | Backend | S | CONFIRMED |
| backend-reliability:berel-14 | Several always-growing tables have no retention: llm_usage, notification_events, portfolio_snapshots (positions JSON per run), sec_ingest_task_attemp… | db-fills.ts:118 | risk | Backend | M | CONFIRMED |
| backend-reliability:berel-15 | Alpaca trade-updates stream captures credentials once at start and has no idle/ping watchdog, so a rotated key or a half-open socket silently stops s… | alpaca-trade-updates-stream.ts:50 | defect | Backend | S | CONFIRMED |
| llm-agent-architecture:llm-18 | Pending-proposal revalidation feeds raw rationale (persisted model + Red text) to a withdraw/reaffirm reviewer with no containment and no data-not-co… | proposal-revalidation.ts:203 | risk | Backend | S | CONFIRMED |
| market-data-integrity:mdi-14 | Dead mock enrichment tier still exported (hash-generated PE/EPS/dividends/headlines) despite AGENTS.md saying it was removed | data-providers.ts:1255 | risk | Backend | S | CONFIRMED |
| market-data-integrity:mdi-18 | Alpaca after-hours close fallback stamps asOf as String(bar.time) and labels any history tier as 'yahoo-finance-delayed' | alpaca.ts:569 | defect | Backend | S | CONFIRMED |
| ops-observability-admin:sre-16 | Ops runbook docs are stale (PM2 litestream→R2, self-hosted gitleaks, 'restart pm2 trading') and there is no current 'what pages me and why' page | ops-observability-security.md:52 | defect | Backend | S | CONFIRMED (prior in-flight PR #2816 adds docs/runbooks/u…) |
| performance-accounting:perf-17 | SPY benchmark is total-return or price-return depending on which history provider answered; intraday tip vs EOD close misaligned | history.ts:518 | defect | Backend | S | CONFIRMED |
| qa-test-strategy:qa-16 | codex-autofix workflow fails on every review event, training everyone to ignore red checks | codex-autofix.yml:21 | defect | Backend | S | CONFIRMED |
| security-auth:sec-07 | Middleware matcher skips any path ending in .png/.svg/... including /api/* — identity-header stripping, CSRF, and auth do not run there | middleware.ts:435 | risk | Backend | S | PARTIAL |
| security-auth:sec-08 | 228 lib/db functions default userId to "local" (the owner's account) — a forgotten argument silently reads or writes the owner's data | db-learning.ts:12 | risk | Backend | L | CONFIRMED |
| trading-money-path:tsx-07 | Alpaca extended-hours LIMIT entries keep time_in_force=gtc; Alpaca requires DAY for extended_hours=true, so a whole-share ext-hours entry the owner a… | alpaca.ts:157 | bug | Backend | S | PARTIAL |
| trading-money-path:tsx-13 | Robinhood adapter silently converts a fractional/dollar BUY LIMIT (or ext-hours entry) into a regular-hours MARKET order with no audit; the persisted… | robinhood.ts:972 | defect | Backend | S | PARTIAL |
| trading-money-path:tsx-18 | Tradier single-leg path sends the raw (unrounded) limit price and always attaches `stop` even on limit/market types, unlike its own bracket path | tradier.ts:789 | defect | Backend | S | CONFIRMED |
| trading-money-path:tsx-19 | Alpaca MCP placement path never forwards extended_hours, so an approved ext-hours limit entry over alpaca-mcp is silently placed as a regular-hours o… | alpaca.ts:741 | defect | Backend | S | CONFIRMED |
| api-contract-web-ios:api-08 | iOS Coach request timeout (90s) is shorter than the server chat deadline (120s default); retry then hits 409 with no human message | MobileAPIClient.swift:117 | defect | Cross-surface | S | PARTIAL |
| api-contract-web-ios:api-12 | iOS maps every 401/403 to "Your session expired. Sign in again." and wipes cookies, discarding the server's real reason | MobileAPIClient.swift:427 | defect | Cross-surface | S | CONFIRMED |
| api-contract-web-ios:api-19 | With no provider key, iOS Coach silently selects the offline "Mock" model and chats with MockLLM instead of pointing at Connections | DeskModels.swift:198 | defect | Cross-surface | S | CONFIRMED |
| frontend-performance:dweb-13 | 1 MB 1024x1024 PNG is declared as the primary favicon (no `sizes`) on every page and in the manifest | layout.tsx:25 | defect | Cross-surface | S | CONFIRMED |
| frontend-performance:dweb-17 | Root `<Toaster theme="system">` renders sonner toasts in OS-dark while the console defaults to light | layout.tsx:80 | defect | Cross-surface | S | CONFIRMED |
| ops-observability-admin:sre-18 | Copy nits on ops surfaces: 'Data catalog' nav label, iOS 'Next run' vs 'Next Run', unlabeled CT timestamps on overview | layout.tsx:77 | defect | Cross-surface | S | CONFIRMED |
| public-site-growth:dpub-15 | Global toast host uses `theme="system"`, so on a dark-mode Mac/iPhone the light-default app shows dark toasts on public and console pages | layout.tsx:80 | defect | Cross-surface | S | CONFIRMED |
| trading-money-path:tsx-16 | Policy hard-gate copy is broker-specific: 'No Robinhood account is selected.' fires for Alpaca/Tradier accounts | policy.ts:326 | defect | Cross-surface | S | CONFIRMED |
| ux-copy-editor:copy-10 | "Green team"/"Red team" lowercase in 18 places vs "Green Team"/"Red Team" product names in 103 | help.tsx:103 | defect | Cross-surface | S | CONFIRMED |
| ux-copy-editor:copy-21 | "P/L" on Orders and proposal cards vs "P&L" everywhere else (web and iOS) | page.tsx:376 | defect | Cross-surface | S | CONFIRMED |
| web-accessibility:a11y-12 | Meter progressbar has no accessible name at any of its call sites | primitives.tsx:201 | defect | Cross-surface | S | PARTIAL (prior 2026-08-06 #2561 (5) — still open on ma…) |
| web-accessibility:a11y-14 | Readiness checklist conveys complete/next state by icon and border color only | readiness-checklist.tsx:27 | defect | Cross-surface | S | CONFIRMED |
| web-accessibility:a11y-15 | Text-input boundaries are 1.5:1 against their background (WCAG 1.4.11 non-text contrast) | console.css:612 | defect | Cross-surface | S | CONFIRMED |
| desktop-web-ux:dweb-13 | Theme 'System' choice never persists — reload snaps back to Light | useConsoleTheme.ts:38 | defect | Desktop web | S | CONFIRMED |
| desktop-web-ux:dweb-14 | Title Case rule not applied consistently to card titles, buttons and admin nav | page.tsx:366 | defect | Desktop web | S | CONFIRMED |
| desktop-web-ux:dweb-15 | Two-space sentence rule effectively unimplemented across the console | format.ts:54 | defect | Desktop web | M | CONFIRMED |
| desktop-web-ux:dweb-16 | 'You're set' readiness card returns on every visit — Dismiss is not persisted | readiness-checklist.tsx:79 | defect | Desktop web | S | CONFIRMED |
| desktop-web-ux:dweb-17 | Orders stale-limit banner points to Settings for a field that lives on Guardrails | page.tsx:322 | defect | Desktop web | S | CONFIRMED |
| llm-agent-architecture:llm-19 | 'Mock - deterministic, no key' is a product-facing model in the Coach picker (a test/simulation mode in the console) | llm-model-catalog.ts:124 | defect | Desktop web | S | CONFIRMED |
| performance-accounting:perf-15 | Home mark-to-market % uses NET signed cost basis — shorts shrink the denominator and inflate the % (rows use \|basis\|) | derive.ts:804 | defect | Desktop web | S | CONFIRMED |
| performance-accounting:perf-16 | Capital-regime and flow dates are UTC calendar days; Day P&L uses Central — snapshots after 7pm CT land on the next day's row | cash-flows.ts:40 | defect | Desktop web | S | CONFIRMED |
| performance-accounting:perf-18 | Synthetic paper curve fabricates a $100 equity base rendered as money | performance.ts:1664 | defect | Desktop web | S | CONFIRMED |
| performance-accounting:perf-21 | Wash-sale estimate disallows the whole lot loss regardless of replacement quantity and never carries basis forward | tax.ts:312 | defect | Desktop web | S | CONFIRMED |
| public-site-growth:dpub-16 | Web login value bullets drifted from iOS despite the “keep in sync” comment, and the third web bullet is device-centric copy on a desktop page | page.tsx:25 | defect | Desktop web | S | CONFIRMED |
| trading-money-path:tsx-15 | Console decision-sheet Approve toasts positive 'Approved · Order status: busy/blocked/error' for non-placed outcomes | page.tsx:997 | defect | Desktop web | S | CONFIRMED |
| ux-copy-editor:copy-06 | Coach page still calls itself "the assistant" in six user-facing strings | chat.tsx:385 | defect | Desktop web | S | CONFIRMED (prior 2026-08-06 P2/P3 polish batch #2562) |
| ux-copy-editor:copy-15 | Web compact money renders uppercase suffixes ("$3.4T", "$850B", "$1K–$15K") via Intl compact; rule and iOS use lowercase k/m/b | drilldown-data.ts:674 | defect | Desktop web | S | CONFIRMED |
| ux-copy-editor:copy-22 | Microcopy nits: release-note voice ("Bulk approve now supports LIVE proposals"), brand spelled two ways, one browser-timezone timestamp | page.tsx:466 | defect | Desktop web | S | CONFIRMED |
| api-contract-web-ios:api-18 | Login/handoff URLs hardcode https://socratictrade.com in LoginView despite MobileAPIClient.productionBaseURL existing to prevent that drift | LoginView.swift:265 | defect | iOS | S | CONFIRMED |
| ios-engineering:ios-02 | Session hard-expires after 30 days with no refresh — iOS never touches Auth.js's session endpoint, so a daily user is forced to re-login monthly with… | MobileAPIClient.swift:14 | defect | iOS | M | PARTIAL |
| ios-engineering:ios-04 | Cached snapshot is stamped `lastUpdatedAt = Date()` at init, so a days-old cache shows a green "Updated · now" pill and passes the staleness gate for… | MobileStore.swift:153 | defect | iOS | S | PARTIAL |
| ios-engineering:ios-06 | Push registration has no automatic retry — a 5xx during a deploy swap at cold start leaves alerts `.failed` until the next cold launch, and `.awaitin… | PushNotifications.swift:360 | defect | iOS | S | PARTIAL |
| ios-engineering:ios-07 | Login failures surface as "Apple Sign-In failed: Your session expired. Sign in again." — 401/403 bodies from the auth routes are dropped by the unaut… | MobileAPIClient.swift:427 | defect | iOS | S | PARTIAL |
| ios-engineering:ios-08 | Required numeric/string fields inside array elements fail the whole snapshot decode on a single null — one bad position/fill/alert row blanks the app | MobileModels.swift:240 | risk | iOS | M | PARTIAL |
| ios-engineering:ios-14 | Session cookie and full portfolio snapshot live in backed-up, non-Keychain stores; the snapshot blob is rewritten to UserDefaults on every reload | MobileStore.swift:144 | risk | iOS | M | CONFIRMED |
| ios-engineering:ios-21 | `Dictionary(uniqueKeysWithValues:)` in CommandAttemptTracker.reconcile traps on duplicate command ids | MobileStore.swift:46 | risk | iOS | S | CONFIRMED |
| ios-hig-ux:ios-12 | Proposal card still marks 'Live' in negative red and shows a '?' icon for paper — contradicts the Home hero rule ('owner does not want Live called ou… | ProposalsView.swift:366 | defect | iOS | S | CONFIRMED (prior 2026-08-06 #2562; 2026-08-04 R12) |
| ios-hig-ux:ios-13 | Dynamic Type breaks in fixed-width layouts: proposal detail labels are clamped to 58pt, scan header columns to 52/64/56pt, and nav/tab bar fonts are… | ProposalsView.swift:572 | defect | iOS | S | CONFIRMED |
| ios-hig-ux:ios-14 | Sub-44pt tap targets remain on high-frequency controls: scan watchlist star (32×32), alert trash button (icon-only), symbol tap buttons at 18–26pt lo… | ScanView.swift:199 | defect | iOS | S | CONFIRMED (prior in-flight PR #2804 (2026-08-17 audit, s…) |
| ios-hig-ux:ios-17 | Copy consistency drift: subtitles/tiles mix Title Case and sentence case, cadence says 'Manual' vs 'manual', alert rows show raw '>' / '<', and Scan… | MarketsView.swift:170 | defect | iOS | S | CONFIRMED |
| ios-hig-ux:ios-18 | 47 call sites request `.weight(.medium/.semibold)` on Lato faces the bundle does not ship (Regular/Bold/Black/Italic only) — weight may silently fall… | AppTypography.swift:52 | risk | iOS | S | PARTIAL |
| security-auth:sec-09 | Native Sign in with Apple does not use a nonce, so a captured identity token is replayable for its lifetime | route.ts:23 | risk | iOS | S | CONFIRMED |
| security-auth:sec-13 | POST /api/mobile/push/register reassigns a device token owned by another user to the caller | route.ts:84 | risk | iOS | S | CONFIRMED |
| ux-copy-editor:copy-09 | iOS mixes Title Case and sentence case inside the same tile rows, empty states, insight titles, and section subtitles; "Ask-First" vs "Ask-first" | ActivityView.swift:28 | defect | iOS | S | PARTIAL |
| mobile-web-ux:mweb-18 | Consent gate card has no max-height/scroll — Accept & Continue can be unreachable on short/landscape phones or with large text | consent-gate.tsx:87 | defect | Mobile web | S | CONFIRMED |
| web-accessibility:a11y-16 | Mobile 'More' tab has no aria-expanded/aria-controls; approvals badge count is an unlabeled number inside the link name | nav.tsx:442 | defect | Mobile web | S | CONFIRMED |
| web-accessibility:a11y-17 | Interactive text below the app's own 10px micro-tier floor: BROKER tag inside Approve at 9.5px, tab-bar labels at 10px | console.css:578 | defect | Mobile web | S | CONFIRMED |
| ops-observability-admin:sre-19 | admin.socratictrade.com sub-route redirect list omits the newer admin pages | middleware.ts:276 | defect | Public site | S | CONFIRMED |
| public-site-growth:dpub-08 | Sitemap, robots allow-list, and middleware PUBLIC_PREFIXES still advertise the deleted /design/socratic-trade route (404 on prod, listed in the live… | sitemap.ts:15 | defect | Public site | S | CONFIRMED |
| public-site-growth:dpub-09 | Production robots.txt is being rewritten by Cloudflare's managed “Content Signals” robots, which prepends `User-agent: * … Allow: /` ahead of the app… | robots.ts:47 | risk | Public site | S | CONFIRMED |
| public-site-growth:dpub-11 | Headings, buttons and nav on all public pages use sentence case, contrary to the Title Case rule (“Request access”, “Decision framework”, “What it do… | page.tsx:117 | defect | Public site | S | CONFIRMED |
| public-site-growth:dpub-12 | Landing illustration shows a specific real security as a BUY (“BUY NVDA ~$4,200 · 82/100 confidence”) beneath a disclosure that says nothing here is… | decision-trace-illustration.tsx:16 | risk | Public site | S | CONFIRMED |
| public-site-growth:dpub-14 | Root-layout `alternates.canonical: "/"` is inherited by every page that does not override it — /login, /framework, /access-denied and all console pag… | layout.tsx:41 | defect | Public site | S | CONFIRMED |
| public-site-growth:dpub-18 | docs/go-to-market.md contradicts current product philosophy and shipped defaults (paper/local-sim positioning, “Test — Local Sim”, landing “default o… | go-to-market.md:13 | defect | Public site | S | CONFIRMED |
| ux-copy-editor:copy-17 | Public pages, Terms and Privacy use sentence-case section headings and have zero NBSP/SENTENCE_GAP (all single-space sentences) | page.tsx:146 | defect | Public site | M | CONFIRMED |
| web-accessibility:a11y-19 | Login page has no heading and its Apple-only warning is 3.2:1 amber | page.tsx:99 | defect | Public site | S | PARTIAL |

### Could Be Improved (67)

| id | title | where | kind | surface | effort | verdict |
|---|---|---|---|---|---|---|
| backend-reliability:berel-16 | /api/health flags that matter for a trader (schedulerStale, tradingLivenessDegraded, storage/litestream degraded) are informational JSON only; the pu… | route.ts:88 | improvement | Backend | S | PARTIAL (prior in-flight PR #2816) |
| llm-agent-architecture:llm-20 | Prompt versions bump weekly (2.8→2.12 in 10 days) with no behavioral eval — the 'offline strategy eval' scores fixtures, not model output; no recorde… | strategy-prompts.ts:20 | opportunity | Backend | M | CONFIRMED |
| market-data-integrity:mdi-15 | Index membership is a static Wikipedia snapshot from 2026-06-14; the live S&P 500 constituents module has no consumer | sp500.ts:1 | improvement | Backend | S | CONFIRMED |
| ops-observability-admin:sre-15 | Sentry events carry no release; Langfuse release reads a Vercel var — neither uses the app's own release identity | sentry.server.config.ts:5 | improvement | Backend | S | CONFIRMED |
| performance-accounting:perf-20 | FillEvent carries no fee/commission or dividend fields — all P&L is gross of costs and cash income | types.ts:2742 | improvement | Backend | M | CONFIRMED |
| performance-accounting:perf-22 | TWR flow convention assumes the transfer arrived at the START of the sub-period though it is detected at the END snapshot | benchmark.ts:137 | improvement | Backend | S | CONFIRMED |
| qa-test-strategy:qa-13 | No coverage tooling, so thin spots in policy/performance are invisible | package.json:86 | improvement | Backend | S | CONFIRMED |
| qa-test-strategy:qa-15 | Flat 614-file test dir with PR/agent-named files and two live-network scratch scripts | alpaca-ping.ts:4 | improvement | Backend | S | CONFIRMED |
| api-contract-web-ios:api-09 | Every SSE payload frame (ready, dashboard.chat-turn stages, shared market-data/dirty broadcasts) triggers a full snapshot reload on iOS | MobileAPIClient.swift:310 | improvement | Cross-surface | S | PARTIAL |
| api-contract-web-ios:api-13 | No client-version negotiation: catalog.version exists but GETs carry no build header and the snapshot has no minimum-build/upgrade signal | mobile-api.ts:1290 | improvement | Cross-surface | S | PARTIAL |
| api-contract-web-ios:api-15 | /api/scan returns the entire MarketScan (quotesBySymbol for the whole universe, full MarketQuote per candidate) to a phone that decodes 9 fields | route.ts:118 | opportunity | Cross-surface | M | CONFIRMED |
| api-contract-web-ios:api-16 | iOS Symbol sheet cannot age-tag other-account positions: server sends recordedAt, Swift PeerAccount drops it | MobileModels.swift:557 | improvement | Cross-surface | S | CONFIRMED |
| frontend-performance:dweb-15 | Symbol drawer endpoints (/api/history, /api/quote, /api/symbol-desk) send no Cache-Control, so every drawer open re-fetches EOD history that the serv… | symbol-drilldown.tsx:69 | improvement | Cross-surface | S | CONFIRMED |
| market-data-integrity:mdi-16 | Delayed/cached provenance not surfaced: iOS scan says 'as of <generatedAt>', web chip says 'fresh', dashboard tile shows no age | ScanView.swift:59 | improvement | Cross-surface | S | CONFIRMED |
| qa-test-strategy:qa-18 | Console and iOS run-state derivations are tested from separate hand-typed tables | RunStateDerivationTests.swift:4 | opportunity | Cross-surface | S | CONFIRMED |
| trading-money-path:tsx-17 | Daily notional/order-count budget resets at America/New_York midnight while every owner-facing timestamp is Central — the 'Today' budget chip rolls o… | db-execution.ts:15 | improvement | Cross-surface | S | CONFIRMED |
| ux-copy-editor:copy-19 | Same guardrail fields carry different labels on web vs iOS (Max per order / Max Order, Max spend per day / Daily Cap, Run every / Cadence, Base indic… | GuardrailsView.swift:69 | improvement | Cross-surface | M | CONFIRMED |
| web-accessibility:a11y-18 | Heading structure gaps: 68 heading-styled <div class="con-card-title"> vs 16 real h2/h3; collapsible Card demotes its h2 to <span> and nests a Link i… | primitives.tsx:35 | improvement | Cross-surface | S | CONFIRMED |
| web-accessibility:a11y-20 | Live updates (new proposals via SSE, freshness going 'delayed') are silent to assistive tech | chrome.tsx:1043 | opportunity | Cross-surface | S | CONFIRMED |
| desktop-web-ux:dweb-19 | Approvals: 'Live' tile/chip wording vs copy canon, plus a reality filter that can never be changed | page.tsx:57 | improvement | Desktop web | S | CONFIRMED |
| desktop-web-ux:dweb-20 | Watchlist table is thin for a monitor screen (Symbol/Last/Alerts/Added only) | page.tsx:231 | opportunity | Desktop web | M | CONFIRMED |
| frontend-performance:dweb-12 | Three heavy client libraries are declared but never imported (@xyflow/react, lightweight-charts, react-resizable-panels) | package.json:45 | improvement | Desktop web | S | CONFIRMED |
| llm-agent-architecture:llm-17 | No Settings nudge when Green and Red are the same model (the owner-approved replacement for the dropped hard independence rule) | page.tsx:716 | improvement | Desktop web | S | CONFIRMED |
| market-data-integrity:mdi-17 | Watchlist shows a bare price: no age, no change, raw provider id, and the price is the broker ask | page.tsx:253 | improvement | Desktop web | S | CONFIRMED |
| ops-observability-admin:sre-20 | Overview R2 card tells the owner to run a curl (POST /api/admin/r2-usage/resume) instead of offering the action | page.tsx:501 | improvement | Desktop web | S | CONFIRMED (prior in-flight PR #2793 (same class, other r…) |
| performance-accounting:perf-23 | Results has no drawdown, profit factor, expectancy, or selectable window; downside deviation is computed but never shown | page.tsx:230 | opportunity | Desktop web | M | CONFIRMED |
| public-site-growth:dpub-17 | /login renders the sign-in form even for an already-authenticated user (public path bypasses identity), instead of bouncing to /console | middleware.ts:306 | improvement | Desktop web | S | CONFIRMED |
| qa-test-strategy:qa-14 | Owner two-space sentence rule has no automated guard on the web (iOS has one) | page.tsx:111 | opportunity | Desktop web | S | CONFIRMED |
| security-auth:sec-10 | CSP has been report-only since 2026-07-01 with unsafe-inline/unsafe-eval; nothing is enforced and reports only go to console.log | middleware.ts:126 | improvement | Desktop web | M | CONFIRMED |
| ux-copy-editor:copy-18 | First-run/empty and loading copy leaks engineering vocabulary ("snapshot", "persisted decision trace", "dashboard API", "autonomy desk") | page.tsx:443 | improvement | Desktop web | S | CONFIRMED (prior 2026-08-06 C12) |
| ux-copy-editor:copy-20 | Settings jump-list labels do not match the card titles they scroll to (Display→Appearance, Boot→After a restart, AI budget→Daily AI Budget…) | page.tsx:63 | improvement | Desktop web | S | CONFIRMED |
| web-accessibility:a11y-11 | None of the 23 console tables has a caption/aria-label; 105/106 <th> lack scope | positions.tsx:89 | improvement | Desktop web | S | PARTIAL |
| ios-engineering:ios-11 | MobileStore and PushNotificationCoordinator are `ObservableObject` (repo rule: @Observable, never ObservableObject) and re-publish `isStreamConnected… | MobileStore.swift:106 | improvement | iOS | M | PARTIAL |
| ios-engineering:ios-12 | No retry on 5xx for idempotent GETs (snapshot has no retry at all) and Cloudflare mapping stops at 523 — deploy-swap 502/503s and 524s reach the user… | MobileAPIClient.swift:373 | improvement | iOS | S | CONFIRMED |
| ios-engineering:ios-13 | SSE reconnect is a fixed 5 s loop with no backoff, and every scene `.active` tears down a healthy stream and fires a second `load()` concurrent with… | MobileStore.swift:343 | improvement | iOS | S | CONFIRMED |
| ios-engineering:ios-15 | 401 handling is inconsistent across passthroughs — Coach/Scan/Symbol/DataSources/Guardrails show text but never clear the session, so the app can sta… | MobileStore.swift:463 | improvement | iOS | S | CONFIRMED |
| ios-engineering:ios-16 | Fill and price-alert push links carry `?symbol=` but the router drops it — taps land on the Assets tab instead of the ticker sheet | DeepLink.swift:75 | improvement | iOS | S | CONFIRMED |
| ios-engineering:ios-17 | Delivered notifications are never removed once a proposal is handled, and `.badge` is requested but never managed | PushNotifications.swift:273 | improvement | iOS | S | CONFIRMED |
| ios-engineering:ios-18 | No optional biometric app lock for a device that holds a 30-day trading session | SocraticTradeApp.swift:34 | opportunity | iOS | M | CONFIRMED |
| ios-engineering:ios-19 | Production origin is hardcoded in LoginView (web-auth start + callback), AdminPortalView and DeepLink with no debug override — every simulator run hi… | LoginView.swift:265 | improvement | iOS | S | CONFIRMED |
| ios-engineering:ios-20 | Project builds in Swift 5.9 language mode with no strict-concurrency checking; actor-crossing closures and mutable captures are unchecked | project.yml:18 | improvement | iOS | M | CONFIRMED |
| ios-engineering:ios-22 | Store/client behavior (load 401 → sign-out, submit idempotency header, SSE reconnect, sign-out cache) is untested although a URLProtocol harness alre… | MobileModelsTests.swift:416 | improvement | iOS | M | CONFIRMED |
| ios-hig-ux:ios-15 | Account & Settings (sign out, switch account, push toggle, guardrail edits, admin portal) is reachable only from the Home tab's toolbar avatar — buri… | HomeView.swift:31 | improvement | iOS | S | CONFIRMED |
| ios-hig-ux:ios-16 | Notifications are non-actionable and unbadged: server payload sends no `badge`, `category`, or `thread-id`; the app never sets the icon badge though… | PushNotifications.swift:263 | opportunity | iOS | M | CONFIRMED |
| ios-hig-ux:ios-19 | Symbol sheet is read-only research: no Add to Watchlist / Create Alert / Cancel-order actions, no medium detent, no price sparkline — the web drilldo… | SymbolInfoSheet.swift:46 | improvement | iOS | M | CONFIRMED |
| ios-hig-ux:ios-20 | Results has no equity curve — the web Results page's headline visual — only four tiles and a fill list | ResultsView.swift:57 | opportunity | iOS | M | CONFIRMED |
| ios-hig-ux:ios-21 | Proposal triage is one-at-a-time; web has select + bulk approve/reject with the same live-confirmation rules | ProposalsView.swift:26 | improvement | iOS | M | CONFIRMED |
| ios-hig-ux:ios-22 | Typed live-approval alert omits the account and notional that the web confirmation shows | ProposalsView.swift:103 | improvement | iOS | S | CONFIRMED |
| performance-accounting:perf-24 | iOS Home shows Equity + Open P&L only — no Day P&L or equity sparkline (web Home has both) | HomeView.swift:207 | opportunity | iOS | S | CONFIRMED |
| security-auth:sec-14 | No local app lock on iOS: the 30-day session cookie is available to anyone holding the unlocked phone | MobileStore.swift:633 | opportunity | iOS | M | CONFIRMED |
| frontend-performance:dweb-14 | Intro canvas forces layout every animation frame (getBoundingClientRect + querySelectorAll per frame, up to END + 45 s) exactly while the first snaps… | intro-canvas.tsx:427 | improvement | Mobile web | S | CONFIRMED |
| frontend-performance:dweb-16 | Desktop and mobile trees are both mounted (hidden by CSS) in scan-table, positions and orders, doubling row mounts; the phone card list is not virtua… | scan-table.tsx:505 | improvement | Mobile web | S | CONFIRMED |
| mobile-web-ux:mweb-16 | Sticky top chrome eats ~110px on phones and the healthy freshness line hides the as-of clock in a hover-only title | chrome.tsx:1022 | improvement | Mobile web | S | CONFIRMED |
| mobile-web-ux:mweb-17 | Scan on phones is an unsorted, unfilterable, unvirtualized card wall — sort/columns/search live only in the lg table header | scan-table.tsx:505 | improvement | Mobile web | M | CONFIRMED |
| mobile-web-ux:mweb-19 | Tap-opened tooltips are viewport-unaware (w-max max-w-xs, centered) — right-edge chips overflow the screen and cause horizontal scroll leakage | primitives.tsx:504 | improvement | Mobile web | S | CONFIRMED |
| mobile-web-ux:mweb-20 | Two full-viewport animated gradient orbs keep animating under the opaque console — GPU/battery cost on phones for invisible pixels | globals.css:155 | improvement | Mobile web | S | CONFIRMED |
| mobile-web-ux:mweb-21 | Command palette sizes with vh (not dvh/visualViewport) — on phones the auto-focused search opens the keyboard over most of the result list | console.css:1220 | improvement | Mobile web | S | CONFIRMED |
| mobile-web-ux:mweb-22 | Equity chart is a fixed 640x140 viewBox that scales to ~70px tall on a phone, with no touch inspection | equity-chart.tsx:11 | improvement | Mobile web | S | CONFIRMED |
| mobile-web-ux:mweb-23 | Coach composer: Enter always sends — on a phone keyboard there is no Shift, so a newline is impossible; no enterKeyHint | chat.tsx:612 | improvement | Mobile web | S | CONFIRMED |
| mobile-web-ux:mweb-25 | LIVE tag inside the Approve button is 9.5px — money-critical label below the sanctioned micro tier; prior P2 still open | console.css:578 | improvement | Mobile web | S | CONFIRMED (prior 2026-08-04 P2 (LiveTag min type size)) |
| web-accessibility:a11y-13 | Coarse-pointer 44px floor covers only .con-btn; sheet/drawer close X (18px), toast ×, scope/state/avatar triggers and Watch stars are excluded | console.css:458 | improvement | Mobile web | S | CONFIRMED |
| mobile-web-ux:mweb-24 | /welcome offers no Sign in link — a returning owner on a phone has only a mailto "Request access" | page.tsx:114 | improvement | Public site | S | CONFIRMED |
| public-site-growth:dpub-07 | Anonymous visitors never see the marketing page and the login page never explains the product; /welcome has no “Sign in” and /login has no “How it wo… | page.tsx:14 | opportunity | Public site | S | CONFIRMED (prior 2026-08-06 D7) |
| public-site-growth:dpub-10 | Two overlapping public explainers with divergent narratives: /how-it-works (“Decision framework”, aspirational) is the only one linked; /framework (“… | page.tsx:137 | improvement | Public site | S | CONFIRMED |
| public-site-growth:dpub-13 | Social previews have no image: layout and welcome declare `twitter.card: summary_large_image` and OpenGraph blocks but no `openGraph.images` / opengr… | layout.tsx:52 | opportunity | Public site | S | CONFIRMED |
| public-site-growth:dpub-19 | Footer legal tagline is inconsistent across public pages (“Not investment advice. You set authority.” vs “Not investment advice. Trading involves ris… | page.tsx:62 | improvement | Public site | S | CONFIRMED |
| public-site-growth:dpub-20 | Hero headline leans on internal jargon (“autonomy desk for market decisions”) — clear to the owner, opaque to a first-time reader; the plain-English… | page.tsx:126 | improvement | Public site | S | CONFIRMED |

## 6. Surface-By-Surface Digest

### Desktop Web
- Home: dead Approve on latest-run rows + wrong tones (`dweb-01`), click-only rows (`a11y-01`, `dweb-18`), no attention/equity hero and duplicated cadence/spend blocks (`dweb-03`), Coach card with dead quick-action chips (`dweb-07`), positions blotter without last price / day change / exit action (`dweb-08`).
- Navigation and IA: Decisions route orphaned from nav while the proposal lifecycle is spread over five surfaces (`dweb-05`); default 404 inside the console (LIVE-02); command palette lacks ticker lookup / STOP / account switch (`dweb-11`); bare `r` fires a run (`dweb-02`); freshness strip only at document end (`dweb-04`).
- Configure pages: three save models across Strategy / Guardrails / Connections (`dweb-09`); Settings tab strip overflows with no affordance (LIVE-13); Guardrails header says "hard execution constraints" and the Autonomy card contradicts itself (LIVE-04); Proposals page refuses reject while stopped (LIVE-05).
- Accessibility: contrast failures on tone chips + Stop/Start (`a11y-02`), no skip link (`a11y-05`), same document title on every route (`a11y-04`), sheet Escape not stack-aware (`a11y-03`), toasts auto-dismiss without pause (`a11y-06`), ~147 explanations only in `title=` (`a11y-07`).
- Performance: Connections bundle 1.05 MB (`dweb-01` perf), dashboard snapshot is a "load the universe" payload every 15 s (`dweb-03` perf), 3× re-render per poll (`dweb-06` perf), Sentry replay + `motion` shipped everywhere (`dweb-09`, `dweb-10`), HTML→JS→/api/dashboard strict waterfall (`dweb-11` perf), TickerLogo requests dark then light (`dweb-07` perf).

### Mobile Web (phone-width site)
- Chrome bar over budget since "Stop Agent" (`mweb-01`, LIVE-11); iOS Safari tint follows OS not the light theme (`mweb-02`); auto-zoom on 11–15px inputs (`mweb-05`); hard-coded bottom offsets collide with the tab bar (`mweb-06`); sheets do not lock scroll (`mweb-07`) and ignore browser history (`mweb-08`); typed-confirm inputs vs the keyboard (`mweb-09`); 36×20 toggles (`mweb-10`); no phone type-scale bump (`mweb-11`); many sub-44px targets (`mweb-12`, `mweb-13`); Watchlist has no card variant (`mweb-14`); Home buries portfolio value / day P&L (`mweb-15`); "System" theme lost on reload (`mweb-03`); ⌘K theme toggle inert (`mweb-04`); "#autonomy" literal (LIVE-12).

### iOS
- Correctness: sign-out leaves cached snapshot (`ios-engineering:ios-01`); silent errors in Settings sheet (`ios-hig-ux:ios-01`); approve "succeeded" on blocked (`api-02`/`tsx-03`); stop-loss "—" (`api-01`); scan timeout below server budget (`api-03`); 3-minute staleness gate (`ios-engineering:ios-03`); cold-start push tap dropped (`ios-05`); all-or-nothing decode (`api-07`); iOS drops `benchmarkUnavailable`/`unverifiedFlows` and always claims cash flows neutralized (`perf-13`); Insights hardcodes SPY (`ios-hig-ux:ios-05`); "1 Proposals" (LIVE-15).
- Product / HIG: Coach markdown raw (`ios-02`); Suggested Draft cannot be staged (`ios-03`); Scan header vs stacked rows (`ios-04`); Activity is fills + own commands only (`ios-06`); system green/orange fail contrast (`ios-07`); destructive-role alert on brokerage switch (`ios-08` — owner-philosophy); first-run dead end (`ios-09`); positions without P&L (`ios-10`, LIVE-14); no success acknowledgement / haptics for money actions (`ios-11`); push permission prompt without pre-prompt (LIVE-16); loosening rituals (LIVE-17 — owner decision); tab/parity mismatch with web and no Strategy/Connections/Settings on the phone (LIVE-18); "Live" pill on every live proposal (`copy-12`); universe copy points at a non-existent Strategy page (`copy-04`).
- Engineering / ship: Swift tests never run in CI or before TestFlight (`qa-01`, `ios-09` eng), Swift build not a merge gate (`qa-02`), `PrivacyInfo.xcprivacy` missing (`ios-10` eng), MobileStore submit/load untested (`qa-09`), verifier-added: `.serverError` releases the command attempt on 502/503/504 (possible double submit) and a single failed background reload trips `snapshotLoadFailed`.

### Public Site
- Over-promised controls (`dpub-03`), untrue `/framework` invariant (`dpub-04`), Terms not updated for data pooling (`dpub-05`), `/access-denied` dead end for every Auth.js error (`dpub-02`), deep-link lost on session expiry (`dpub-01`), no two-space gaps anywhere (`dpub-06`), sentence-case CTAs "Request access" / "Decision framework" (LIVE), no mention of the iOS app on any growth page (verifier-added), login page prints env-var names when auth is unconfigured (LIVE).

### API + Backend
- Money path: `tsx-01`, `tsx-02`, `tsx-04`, `tsx-05`, `tsx-06` (tradability stub), `tsx-08`, `tsx-09`, `tsx-10` (approve starved by autonomous run), `tsx-11` (hard-coded 5% headroom gate with no policy toggle — owner-philosophy), `tsx-12` ("safe to retry" with no retry path), `tsx-14` (remediation-deferred spam).
- Accounting: `perf-01`…`perf-14` (see §1 item 4; plus HWM not flow-adjusted / UTC day `perf-08`, first-partial-exit grading `perf-09`, 20 bps haircut on real Alpaca-paper fills `perf-10`, closing fills without app lot dropped `perf-12`, broker-authoritative equity/cash ledger implemented but unwired `perf-14`).
- Reliability: `berel-01`…`berel-10` (FTS full scan, sync FTS mirrors, no broker timeout, no tick re-entrancy guard, no event-loop-lag instrumentation, all CPU-heavy RAG on the serving thread, resume slices re-parse artifacts).
- LLM: `llm-01`…`llm-16` (strict schema, wrong account in Red Team, macro pruning, auto-applied directives, half-approve without haircut, full Green content re-sent to Red, json_object transports off-schema, dead-model list, late responses unmetered, native-provider Coach routing, `MockLLM` semantic gate in production, no `usage.cost` capture).
- Data: `mdi-01`…`mdi-13` (Friday freeze, 3-day bars, fabricated asOf ×2, ask-as-price, VWAP yesterday, screener asOf sentence, preferreds/units in dynamic universes, mixed volume semantics, no early-close calendar, unlabeled adjustment mixing, Yahoo v7 crumb, UTC day keys).
- API contract: `api-01`…`api-11` (nested `riskRules`, approve success lie, scan timeout, full StrategyResult stored in `mobile_commands`, FIFO behind run_once, no generated fixtures, all-or-nothing decode, zod unused / four error envelopes).
- Security: `sec-01`…`sec-06`.  Ops: `sre-01`…`sre-14`.  QA: `qa-01`…`qa-17`.

### Cross-Surface Parity
- One run state with three names and one stop control with four (`copy-07`); "Approvals" vs "Proposals" (`copy-05`); "Realized/Unrealized P&L" mean three different things on Results, Home and iOS (`perf-05`); win rate "0%" for zero closed lots on web + iOS (`perf-06`); overdue next run never flagged on either surface (`sre-11`); tabs differ web-mobile vs iOS (LIVE-18); Run once not stamped with account + LIVE|PAPER on either (2026-08-04 R1, still open).

## 7. Prior Reviews — Repeats, Still Open, Resolved

Cross-checked against `docs/reviews/2026-07-20-grok4-multi-expert-full-app-review.md`, `2026-08-04-expert-panel-web-ios-ux.md`, `2026-08-06-claude-full-product-review.md`.  50 of today's findings are repeats of a prior item; 15 prior items are still open but were not re-caught by today's lanes; 53 prior items were verified as resolved.

### Still Open From Prior Reviews (Not Re-Caught Today)

- **2026-08-04 R1 (P0 row)** [P1] Run once is not stamped with target account alias + LIVE|PAPER on web or iOS — app/console/components/chrome.tsx:779-789 button text/aria-label is bare 'Run once' (title is a generic 'Manual runs always ask first…'); success toasts at chrome.tsx ~744-746 say 'Run started'/'Run complete' with no account; ios/SocraticTrade/HomeView.swift:242,416 'Run Once' with no alias/tier. r…
- **2026-08-04 R4 (P0 row)** [P2] STOP confirm parity still missing: iOS one-tap Stop Agent vs web Confirm: STOP sheet — ios/SocraticTrade/HomeView.swift:436-449 CommandButton 'Stop Agent' submits strategy.stop directly (no confirmationDialog/alert; only alert in file is 'Use Brokerage Account?' at :1065); web app/console/components/chrome.tsx:417,484 opens 'STOP everything' sheet with 'Confirm: STOP'.
- **2026-08-06 C6** [P2] Proposal rationale still accumulates duplicate '[Stale quote backup: …]' annotations on re-evaluation — src/lib/policy.ts:461-468 appends the annotation unconditionally (no dedup/replace); src/lib/strategy.ts:3076 (planning gate) and :3424 (final gate) both call evaluateTradeProposal on the same proposal object, producing the '159s'/'160s' pair seen live. Only app/mobile/mobile-pwa-client.tsx:322 str…
- **2026-07-20 P1 #43** [P2] Alpaca trade_updates stream still single-operator, single-account ('local', first-ranked Alpaca account only) — src/lib/streams/alpaca-trade-updates-stream.ts:50 resolveAlpacaStreamAccount("local"); src/lib/db-api-keys.ts:872-886 returns the first ranked Alpaca account with a key — a second connected Alpaca account (e.g. paper + live) never gets streamed fills and relies on polling only. Today's berel-15 cov…
- **2026-07-20 P1 #45** [P2] Chat/Coach drafts still cannot express short/cover — promote-draft rejects them — src/lib/chat/promote-draft.ts:17 `if (draft.side !== "buy" && draft.side !== "sell") return { ok:false, error: UNSUPPORTED_SIDE }`; src/lib/chat/types.ts:48 side is an untyped string. Shorting is a shipped capability (Alpaca short buy-stops #2707) so drafts lag the engine.
- **2026-07-20 P1 #15** [P2] Anthropic prompt caching still dead on the strategy path under universal OpenRouter routing — src/lib/llm-call.ts:243-338 sets cache_control only when endpoint.provider === 'anthropic' (native); the openrouter branch at :247-253 sends plain chat-completions with no cache_control on system content. rg cache_control src/lib -> only llm-call.ts (native branch) and src/lib/chat/llm.ts (native C…
- **2026-07-20 R3 (RAG ops P0)** [P2] Corpus re-embed still limited to 4 docTypes; 8-K / fundamentals / congress excluded — src/lib/rag/corpus-reembed.ts:85-88 CORPUS_REEMBED_DOC_TYPES = sec-filings, earningscalls-transcripts, experience-memory, insider-form4 (and :103-105 a 3-type subset). No 8-K/fundamentals/congress entries.
- **2026-07-20 P1 #28 / #29** [P2] Hybrid BM25/RRF retrieval and strict as-of mode still default OFF in code — src/lib/vector-db.ts:884-888 hybridRetrievalEnabled() = envFlagOn('HYBRID_RETRIEVAL', false); :898+ 'R1 strict as-of mode … OFF by default'. May be flipped via Infisical/Settings knob — code default unchanged since the review.
- **2026-07-20 P1 #14** [P3] OpenRouter credit probe still reads only the 'local' user's key — src/lib/openrouter-credits.ts:69 resolveLlmCredential("openrouter", "local") ; app/api/health/route.ts:187 resolveApiKeyWithSource(ragEmbedProvider, "local"). Low impact while the owner is the sole user.
- **2026-07-20 P1 #47** [P3] maxFundamentalsAgeSec still gates on scan generatedAt, not per-field age (now annotate-only) — src/lib/policy.ts:476-486 computes scanAgeSec from context.marketScan.generatedAt and annotates the rationale; no field-level fetchedAt/asOf is consulted (today's mdi-03 shows fetchedAt is itself fabricated on cache hits). Downgraded to P3 because staleness is advisory-only per owner 2026-08-04.
- **2026-07-20 P1 #44** [P3] 'placing'/partial proposals still lock daily/hourly caps at full estimated notional — src/lib/db-execution.ts:60,94 status IN ('placed','filled','paper','placing') summed by estimated_notional regardless of filled quantity.
- **2026-07-20 P2 'OpenRouter credit flap near $10 threshold'** [P3] Low-credit signal still has a single threshold with no hysteresis/recovered band — src/lib/openrouter-credits.ts:43-44,77 single OPENROUTER_LOW_CREDIT_USD threshold, ok flips per read; no recover margin. Interacts with today's sre-10 (no 'recovered' events).
- **2026-07-20 P1 #27** [P3] Infisical Cloud remains a hard boot SPOF — no last-known-good secrets fallback — scripts/coolify-prod-start.sh:112-113 exec node scripts/infisical-run.mjs; scripts/infisical-run.mjs:28-33 any InfisicalBootstrapError -> process.exit(2), :142-146 CLI probe failure exits; no cached snapshot path.
- **2026-08-04 P2 'palette dirty-guard'** [P3] Command-palette navigation still bypasses in-console unsaved-draft interception — app/console/components/command-palette.tsx:8-9 comment: 'in-console unsaved-draft interception on palette jumps is a deliberate v1 gap'; only beforeunload guard exists.
- **2026-08-06 C12** [P3] Guardrails cadence copy 'next run time not in this snapshot' still shipped (agent-speak in owner copy) — app/console/guardrails/page.tsx:627 "Running; next run time not in this snapshot." (other C12 nits — 'Broker reported Expired: Expired' — fixed at src/lib/dashboard-feed.ts:810). Overlaps today's sre-11 (overdue next run never flagged).

### Today's Findings That Repeat A Prior Item

- `perf-02` ← 2026-08-06 C1 (+H #2557): Same class: flow inference misreads equity moves as deposits/withdrawals. C1's specific $36.5k phantom was sanity-bounded in #2589 (be45dc689); today shows the class persists (no-fill days read as fl…
- `perf-03` ← 2026-08-06 C1 (+H #2557): Same class as C1 (phantom transfers from flow inference), narrowed to the short/cover cash-sign inversion.
- `perf-01` ← 2026-08-06 C3: Plausible root cause of C3 (open-lots ledger contradicting live positions): the oldest-500 fill cap drops the newest closes. #2589 added lot-ledger reconciliation but did not touch the cap. Not prove…
- `berel-02` ← 2026-08-06 C3: Duplicate of perf-01; same plausible-root-cause mapping to C3.
- `perf-13` ← 2026-08-06 C2: Partial: C2 asked for an explicit 'benchmark unavailable' state; web got it in #2589, iOS still drops benchmarkUnavailable and claims flows neutralized.
- `llm-15` ← 2026-08-06 D4: Exact repeat: /console/usage still has no embedding/rerank spend row.
- `llm-07` ← 2026-08-06 D5: Exact repeat: Red Team reviewer re-sends the entire Green payload (D5 asked for compaction).
- `sre-13` ← 2026-08-06 D6: Exact repeat: admin overview still fabricates CPU 0.0% / 0 Running placeholders.
- `dpub-07` ← 2026-08-06 D7: Finding text already cites D7: unauth apex -> /welcome, marketing page unreachable, funnel halves unconnected.
- `mweb-24` ← 2026-08-06 D7: D7's 'Request access mailto -> form' + no Sign in on /welcome; app/welcome/page.tsx:32,242 still mailto.
- `sre-07` ← 2026-08-06 A1 (follow-up): A1's follow-up asked for an alert on 'main ahead of prod sha >1h' / a verify-deploy-sha cron; still manual only.
- `sre-03` ← 2026-08-06 A2: Partial: A2 noted /api/health litestreamState 'unknown' while the R2 kill-switch was engaged; today's finding is the same visibility gap (markers invisible to health/Backup Status), now also ignoring…
- `copy-07` ← 2026-08-06 D2: Same class (cross-surface run-state vocabulary drift). D2 was PWA-vs-console 'Running' vs 'Paused · market closed'; today's is web+iOS Exit-only/Close-only/Close Only and four names for STOP.
- `a11y-02` ← 2026-08-06 H #2561: Exact repeat: light-theme tonal chip text fails WCAG AA on soft fills.
- `a11y-03` ← 2026-08-06 H #2561: Exact repeat: Sheet Escape handler not stack-aware (closes sheet under drawer/palette).
- `dweb-18` ← 2026-08-06 H #2561: Second half of dweb-18 (sheet Escape stacking) is the #2561 item; the click-only proposal rows half is new (= a11y-01).
- `a11y-07` ← 2026-08-06 H #2561: Exact repeat: tooltip content keyboard-unreachable / unannounced.
- `a11y-10` ← 2026-08-06 H #2561: Exact repeat: scan Columns popover missing aria-expanded/Escape/focus move.
- `copy-06` ← 2026-08-06 H #2562: Coach/Assistant label drift item from the #2562 polish batch, still open.
- `ios-12` ← 2026-08-06 H #2562 / 2026-08-04 R12 + shipped 'word-first LIVE/PAPER pill (not red-as-live)': iOS paper '?' icon (#2562) and red Live pill (08-04 hero rule was applied to Home hero only, not ProposalsView).
- `copy-12` ← 2026-08-04 R12 / R5: Same as ios-12: red 'Live' pill on iOS proposal cards contradicts the 08-04 'Live != system red' rule.
- `ios-13` ← 2026-08-06 H #2562: iOS Dynamic Type fixed widths item from #2562, still open.
- `api-18` ← 2026-08-06 H #2562: iOS URL literals item from #2562 (LoginView hardcodes https://socratictrade.com); still open.
- `ios-19` ← 2026-08-06 H #2562: (second iOS list) Same URL-literal item as api-18; #2562.
- `ios-10` ← 2026-08-06 H #2560: (second iOS list) Privacy manifest still absent; the other #2560 halves (close-only/wind-down #2757, APNs wiring, ITSAppUsesNonExemptEncryption project.yml:76) landed.
- `ios-07` ← 2026-08-04 R12: Exact repeat: iOS still uses system Color.green/orange instead of brand pos/neg hexes.
- `ios-08` ← 2026-08-04 R5 / 2026-07-20 P1 #5: Partial: 'live quiet, don't re-paternalize' theme; iOS destructive-role 'Use Brokerage Account?' alert on non-paper switch is the same failure.
- `dweb-03` ← 2026-08-04 R6: Exact repeat: ready-state Home density — no equity/P&L hero, coach/framework not collapsed.
- `mweb-15` ← 2026-08-04 R6: Phone variant of R6: portfolio value/day P&L buried under readiness hero + strategy bar.
- `dweb-11` ← 2026-08-04 R7: Exact repeat: command palette lacks Run once / STOP / switch-account verbs (settings-search was wired in #2791 but ops verbs were not).
- `dweb-02` ← 2026-08-04 (shipped fix 'ux-run-once-single-primary' + R8): Residual of the dup-Run-once fix: desktop and phone RunOnce buttons both stay mounted (CSS-hidden) so the R8-style bare 'r' hotkey double-POSTs. R8's a/r landed (command-palette.tsx:119,128) without…
- `ios-01` ← 2026-08-04 R10: Partial: R10 asked for an iOS command-outcome banner under Agent controls; today's guardrail/account edit errors never render (store.error unrendered in sheet).
- `ios-11` ← 2026-08-04 R10: Partial: no success acknowledgement for money actions is the positive half of the R10 outcome banner.
- `mweb-12` ← 2026-08-04 P2 'Settings touch targets (32px avatar)': Finding text already says 'known follow-up still open'.
- `mweb-25` ← 2026-08-04 P2 'LiveTag min type size': Finding text already says 'prior P2 still open' (9.5px LIVE tag in Approve).
- `a11y-17` ← 2026-08-04 P2 'LiveTag min type size': Same 9.5px LIVE/BROKER tag inside Approve.
- `copy-22` ← 2026-08-04 P2 'brand name Socratic.Trade consistency': 'brand spelled two ways' is the same item.
- `llm-16` ← 2026-07-20 P1 #18: Exact repeat: hand-maintained price table instead of OpenRouter billed usage.cost.
- `llm-09` ← 2026-07-20 P1 #19: Partial: #19 was failed OpenRouter attempts unmetered; today's is late (post-soft-timeout) responses unmetered. Same gap class (calls that cost money but never reach the ledger).
- `sre-14` ← 2026-07-20 P1 #23: Exact repeat: no restore drill ever recorded (now three backup layers, still zero drills).
- `berel-16` ← 2026-07-20 P1 #26: Exact repeat: /api/health degrade flags are informational, probe stays 200.
- `sre-02` ← 2026-07-20 P1 #26: Same as berel-16 from the alerting side; only openrouterCredits is externally keyword-monitored (#20 landed).
- `mdi-09` ← 2026-07-20 P1 #48: Exact repeat: average/IEX-only volume stamped as session volume across providers.
- `sec-11` ← 2026-07-20 P1 #52: Exact repeat: /api/ops/* falls back to ADMIN_REINDEX_TOKEN.
- `dweb-03` ← 2026-07-20 P1 #60: (second desktop list, dashboard snapshot 'load the universe') exact repeat of the BFF-polled-for-every-tab finding.
- `api-12` ← 2026-07-20 P2 'iOS false logout on any non-2xx': Partial: now scoped to 401/403 but still discards the server reason and wipes cookies.
- `ios-07` ← 2026-07-20 P2 'iOS false logout on any non-2xx': (second iOS list) same as api-12; auth-route 401 bodies dropped by the unauthorized mapping.
- `ios-03` ← 2026-07-20 P2 'iOS SSE no poll fallback': (second iOS list) exact repeat: staleness gate + no polling fallback.
- `mdi-14` ← 2026-07-20 P2 'Mock enrichment still exported': Exact repeat: dead mock enrichment tier still exported (data-providers.ts:1255).
- `sre-20` ← 2026-08-06 H #2563: Same class: curl-only capabilities the UI should offer as actions (R2 resume POST).

### Prior Items Verified Resolved Since

- 2026-07-20 T1/T2/#8 skips-as-completed + silent budget skip -> distinct skipped_budget/skipped_market_closed/skipped_broker_unhealthy statuses (src/lib/strategy.ts:521,632,685,744,1064; types.ts:2580) — 40509feb9 (Wave A) + c4d6055fa #2418…
- 2026-07-20 T3 Usage-Monitor budget keyed on vendor family -> prefers 'openrouter' provider (src/lib/usage-budget.ts:482-499) — 40509feb9
- 2026-07-20 #21 early budget admission before scan -> src/lib/strategy.ts:667 'Early LLM budget admission (BEFORE market scan)' — 40509feb9
- 2026-07-20 T5 SiliconFlow bge-m3 10x undercount -> f5f5e4173 #1771 (rag-metering.ts:60-66)
- 2026-07-20 M1 no broker-held short cover stops -> broker-held buy-stops for shorts, default ON when shorting on (src/lib/broker-protective-stops.ts:164-169) — 141a72a7f #2707
- 2026-07-20 M2 add-to-loser blind for market/dollar entries -> markForAddToLoser prefers live scan quote / position MV (src/lib/policy.ts:1072-1094) — 40509feb9
- 2026-07-20 M3 RH stops poll-only honesty -> stop-flow lane diagram surfaces broker-held vs app-monitored per broker (app/console/guardrails/stop-flow.tsx:50-57); default remains opt-in by design
- 2026-07-20 #41 bracket permission ignores shortStopLossPct -> src/lib/policy.ts:350-358 accepts stopLossPct OR shortStopLossPct
- 2026-07-20 I1 SIWA audience mismatch -> NATIVE_APPLE_CLIENT_ID = trade.socratic.app (src/lib/auth/apple-client-id.ts:4) matches ios/project.yml:50
- 2026-07-20 I2 iOS live approve without typed confirm -> liveConfirmation payload (ios/SocraticTrade/ProposalsView.swift:159) — 5f6c7f087 #1859 (today's ios-22 is only about omitted account/notional in the alert)
- 2026-07-20 I3 account-deletion decode -> AccountDeletionResult decodes ok+logoutUrl and MobileStore signs out (MobileModels.swift:626-636, MobileStore.swift:556)
- 2026-07-20 I4 not a buildable target -> XcodeGen ios/project.yml, tests target, ios-build.yml (today's qa-01/ios-09 concern test execution, not buildability)
- 2026-07-20 S1 CF Access header trusted without JWT -> Cf-Access-Jwt-Assertion verified against team JWKS with aud check (middleware.ts:174-214) — 0dff20618 #1787
- 2026-07-20 S2 SSRF (Alpaca baseUrl + webhooks) -> src/lib/egress-guard.ts validateBrokerBaseUrl/validateWebhookUrl wired in connected-accounts route + notify.ts — 19bb04cad #1791
- 2026-07-20 S3 wrong ENCRYPTION_KEY fails open -> prod guard + versioned envelope — 0dff20618 #1787
- 2026-07-20 P1 #1 single live-approve sheet lacked tone=live -> app/console/components/approval-card.tsx:1065 tone="live"
- 2026-07-20 P1 #2 approval CTA not sticky on phone -> sticky actions above tab bar (approval-card.tsx:955) — 0efc4e04a #2414 (PR-A2)
- 2026-07-20 P1 #6 Guardrails Advanced defaultOpen -> app/console/guardrails/page.tsx:341 collapsible defaultOpen={false}
- 2026-07-20 P1 #7 Settings long dump -> Settings TOC/jump list — 63165e6e4 #2425 (today's copy-20 is a label-mismatch nit on that list)
- 2026-07-20 P1 #9 Activity 'simulated' wording -> no 'simulated' strings in app/console — 40509feb9
- 2026-07-20 P1 #10 no first-run checklist -> app/console/components/readiness-checklist.tsx
- 2026-07-20 P1 #12 autonomy split across chrome/Guardrails/Strategy -> single AutonomyCard surface (guardrails/page.tsx:252-254) — 63165e6e4 #2425
- 2026-07-20 P1 #13 Cmd+K undiscoverable on phone -> chrome ⌘K trigger button on phone (today's mweb-12 sizes it)
- 2026-07-20 P1 #16 chat usage drops cache token fields -> cachedPromptTokens/cacheCreationTokens read for OpenAI/DeepSeek/Anthropic (src/lib/llm-usage.ts:185-193)
- 2026-07-20 P1 #20 no external UptimeRobot credit monitor -> exists (today's sre-02: 'only openrouterCredits is keyword-monitored')
- 2026-07-20 P1 #25 CI + prod share one box -> fleet CI GitHub-hosted only since 2026-07-29 (AGENTS.md)
- 2026-07-20 P1 #33/#34 coach notes slice(-20) no archive; lesson vectors never written -> socratic_coach_note_archive + coach-note/lesson episodic doc types (src/lib/db-socratic.ts:160-201, experience-memory.ts:45, chat/coach-learning.ts:65…
- 2026-07-20 P1 #40 dashboard collapses short/cover to 'Trade' -> sideLabel renders Short/Cover (src/lib/dashboard-feed.ts:544-549)
- 2026-07-20 P1 #51 MarketQuote.stale never cleared -> field removed from types (rg 'stale: true' src/lib -> only congress-score-gate)
- 2026-07-20 P1 #53 TradingView webhook fans out to all users -> per-account triggerSettings.enabled opt-out (src/lib/triggers.ts:286-295, 2026-07-28); still global-by-default by design
- 2026-07-20 P1 #54 congress webhook weak body bounds -> readBodyWithLimit + CONGRESS_WEBHOOK_MAX_BYTES (app/api/webhooks/congress/route.ts:7,43) — #1791; bearer fallback retained as documented legacy
- 2026-07-20 P1 #56 evidence bulletins only first 3 sanitized -> only first 3 are sent to the prompt at all (src/lib/strategy.ts:5692, 6530), all routed through UntrustedPromptField
- 2026-07-20 P1 #57 SSE global events without userId -> tenant filter drops other users' tagged events (app/api/events/stream/route.ts:34-36)
- 2026-07-20 P2 iOS idempotency UUID per tap -> CommandAttemptTracker reuses the pending key (ios/SocraticTrade/MobileStore.swift:19-33, MobileAPIClient.swift:86-90)
- 2026-07-20 P2 iOS no push/deep links -> PushNotifications.swift + DeepLink.swift shipped (today's ios-05/16/17 are quality gaps on top)
- 2026-08-06 B1 shared-package lockfile drift -> package.json and package-lock.json both resolve congress-trading-shared v2.5.2 (b2847eb9)
- 2026-08-06 C1 (specific $36.5k phantom withdrawal) -> sanity-bounded inferred transfers + unverifiedFlows — be45dc689 #2589 (class persists per today's perf-02/03)
- 2026-08-06 C2 SPY 0.00% everywhere -> explicit benchmarkUnavailable state (app/console/results/page.tsx:323-337) — be45dc689 #2589
- 2026-08-06 C3 open-lots ledger vs positions -> lot-ledger reconciliation — be45dc689 #2589 (today's berel-02 500-fill cap remains a residual cause)
- 2026-08-06 C4 unmanaged shorts silently '—' -> unmanagedShortNotice attention banner (app/console/lib/derive.ts:357-395) — d4e8ebf37 #2588
- 2026-08-06 C5 Red Team failed chip without cause -> 'AI critic failed — <cause>' + CriticFailureStat rate (approval-card.tsx:179-223, results/page.tsx:720) — bfb558f60 #2590
- 2026-08-06 C7 activity BUY/TRADE duplicates + ingest flooding -> ingest/embed kinds routed to OPS_AUDIT_KINDS System group + pre-insert receipt fold (src/lib/dashboard-feed.ts:650-675, 1112) — bfb558f60 #2590
- 2026-08-06 C8 no-op disclosure-embed audits every minute -> recordEmbedAudit isNoop rollup (src/lib/web-sources/disclosure-rag.ts:168-174) — bfb558f60 #2590
- 2026-08-06 C9 'Today: $0.00' budget chip read as P&L -> 'Deployed today:' + meter (app/console/components/chrome.tsx:1031,1041,1102) — d4e8ebf37 #2588
- 2026-08-06 C10 scan chip '1h ago old' -> fixed (app/console/scan/page.tsx:114 comment) — d4e8ebf37 #2588
- 2026-08-06 C12 'Broker reported Expired: Expired' -> de-duplicated (src/lib/dashboard-feed.ts:810)
- 2026-08-06 D3 alert per-condition mute/snooze -> alert-mutes (#2555) wired in alert-center.tsx:5,169 — bfb558f60 #2590
- 2026-08-06 H #2556 Home 'All Decisions' -> 404 -> real /console/decisions index — d4e8ebf37 #2588 (today's dweb-05: page exists but is orphaned from nav)
- 2026-08-06 H #2558 settings-search catalog wired to nothing -> settingsPaletteHits in command palette — 5f9b4aaf8 #2791
- 2026-08-06 H #2559 iOS SSE indicator false on idle stream -> heartbeat comments count as connected (ios/SocraticTrade/MobileStore.swift:314, MobileAPIClient.swift:289)
- 2026-08-06 H #2560 (partial) iOS Close-only/Wind-down -> 82e74746c #2757 (HomeView.swift:383-388); APNs wired; ITSAppUsesNonExemptEncryption set (ios/project.yml:76) — privacy manifest still missing (today's ios-10)
- 2026-08-06 H #2562 (partial) retired-FMP mentions, 'Paper mode' preflight copy, dead market-data-filled event, intro-canvas dark-palette candles (#2667) — resolved; Coach/Assistant, iOS '?' icon, URL literals, Dynamic Type still open (toda…
- 2026-08-04 shipped set (single Run once primary web+iOS, Zap icon, Approve live + LiveTag, focus ring, Proposals empty state) — landed on grok/ux-expert-review-dup-run-once; R2 skip honesty via c4d6055fa #2418; R9 iOS close-only via #2757;…

## 8. Verifier-Added Items (50, single-verifier confidence)

Issues the skeptic found in a lane's scope that the lane's expert had missed.  These were verified by one reader only.

- [ios-hig-ux] ios-missed-1 (P3/defect/ios): CoachView.swift:132-137 — the Coach composer's Send button is an Image(systemName: "arrow.up.circle.fill") at font size 32 with no explicit `.frame(width:height:)` or `.contentShape`, giving it a tap target well under the 44x44 HIG minimum on the one screen where every message requires tapping it.
- [ios-hig-ux] ios-missed-2 (P3/consistency/ios): GuardrailsView.swift:82 ('Universe edits stay on the Strategy page. This screen edits authority and caps.') independently corroborates ios-09/ios-17's point that iOS repeatedly tells the owner to go to 'the Strategy page,' a page that has no iOS destination — worth folding into the same fix rather than treated as isolated copy.
- [ios-engineering] ios/SocraticTrade/MobileStore.swift:695-703 — shouldReleaseCommandAttempt returns true for EVERY .serverError, including 502/503/504/52x edge errors where the origin may already have committed the command; the next tap mints a fresh idempotency key, and the server dedupes only on that key (src/lib/mobile-api.ts:624-627), so an ambiguous 5xx + retry can double-run strategy.run_once / re-submit an ap…
- [ios-engineering] ios/SocraticTrade/MobileStore.swift:223,303 — a single failed background reload (SSE-triggered scheduleReload hitting a deploy-swap 502, no retry) sets snapshotLoadFailed and immediately marks the desk Stale/locks Approve/Run regardless of how fresh lastUpdatedAt is; distinct from the 180s clock in ios-03 and makes the no-retry gap in ios-12 user-visible several times a day.
- [ios-engineering] .github/workflows/ios-build.yml:40 + repo ruleset main-protection (id 17945518) — required_status_checks is only `verify`; the Swift compile job is advisory, so a PR that breaks `ios/**` compilation can auto-merge before/without the Mac runner result (the 2026-08-13 duplicate-@Binding incident class). Add the iOS job as a required check or fold a Swift gate into `verify`.
- [ios-engineering] ios/SocraticTrade/MobileControlView.swift:86 — TabPreferences is a third ObservableObject (expert listed only MobileStore + PushNotificationCoordinator); ios/CLAUDE.md:38 also documents MobileStore as '@Observable' — doc drift to fix alongside ios-11.
- [desktop-web-ux] app/console/page.tsx:1025 — ProposalRow's local `chipTone` reimplements a narrower status→tone map than the already-imported `isNotPlacedStatus` helper (app/console/lib/action-verbs.ts:30-32, imported at page.tsx:46 but never called). SocraticDecisionStatus values `rejected`, `rejected_by_broker`, `expired`, and `withdrawn` (src/lib/socratic-runtime.ts:449-453) all fall through chipTone's catch-all…
- [desktop-web-ux] app/console/page.tsx:1011 — `isPendingOrFailed` includes a 'failed' branch, but grepping every `results.push({ ... status: ... })` in src/lib/strategy.ts (the only producer of `latest.proposals[].status`) shows the only statuses ever pushed are 'proposed', 'blocked', 'error', or a `recoveredStatus`/`proposalStatus` variable — 'failed' never appears to be a value strategy.ts actually assigns to a pro…
- [public-site-growth] app/how-it-works/page.tsx:93 — the AUTHORITY_ITEMS "Scope: symbol, watchlist, strategy, sector, account, or market regime." bullet is also fabricated, not just the two the expert flagged in dpub-03: StrategyAuthority (src/lib/types.ts:24) is a single account-level propose/decide flag with no per-symbol/watchlist/sector/regime authority scoping anywhere in src/ or app/ (confirmed by grep and by S…
- [public-site-growth] app/welcome/page.tsx, app/how-it-works/page.tsx, app/login/page.tsx — none of the public growth/conversion surfaces mention the native iOS app at all (grepped for iOS/App Store/TestFlight/iPhone across all three, zero hits), despite CLAUDE.md stating current product direction is explicitly 'Clients are iOS + the website' and a real, actively-shipped ios/SocraticTrade app (with its own LoginView.…
- [mobile-web-ux] app/console/ui/sheet.tsx: TabsSheet close button (nav.tsx ~276-283, same pattern as sheet.tsx:157-164) and the SymbolDrawer close (symbol-drawer.tsx ~71, h-8 w-8=32px) are already covered by mweb-13's 'same at nav.tsx:276-283' note -- not a separate miss, just confirming breadth.
- [mobile-web-ux] app/console/components/consent-gate.tsx: the same fixed-centered-no-scroll pattern (no max-h/overflow-y) also appears on other full-screen confirm-style overlays outside the ones cited (worth a broader grep for `flex items-center justify-center` + `.con-card` combos across app/console/**/*.tsx) -- not independently verified here, flagged as an extension of mweb-18 rather than a new finding.
- [mobile-web-ux] app/console/console.css: RealityBanner (paper/none banner) height was asserted by mweb-16 as '~31px' without a cited line -- I did not independently verify that specific banner's CSS height; the sticky-stack architecture claim is solid but that one figure is unverified.
- [mobile-web-ux] None of the 25 findings appear to be already covered by an in-flight PR in STATUS.md's top ~80 lines -- all recent PRs (#2845/#2847/#2848 etc.) are backend strategy-run/broker-timeout fixes unrelated to mobile-web UX, so no PARTIAL-for-in-flight-PR corrections apply here.
- [api-contract-web-ios] src/lib/mobile-api.ts:1297 mobileControlCatalog().realtime.eventTypes advertises 6 event names, but app/api/mobile/events/route.ts:28,36-38 also emits `ready`, `dashboard.chat-turn` and `dashboard.pending-learned-change` (events.ts:12 union) — the catalog is the only contract doc for the SSE stream and it is already drifted; fold into the api-09 allowlist fix.
- [api-contract-web-ios] app/api/mobile/commands/route.ts:25 + src/lib/mobile-api.ts:689: `?limit=abc` is not just 'LIMIT NaN' — the clamp lets NaN through and better-sqlite3 rejects it with 'datatype mismatch' (verified locally), and the GET has no try/catch, so it is an unhandled 500 with a stack in prod logs; sharpen api-11's rec to coerce/validate limit.
- [frontend-performance] app/console/lib/useConsoleData.tsx:238 (POLL_MS=15_000) + app/console/lessons/learned-context.tsx:310 and :630 (two independent setInterval polls: main list + a separate archive poll gated on `open`) + app/console/watchlist/page.tsx:77 all run independent, uncoordinated setInterval polling loops with their own visibilitychange handling. None are staggered/jittered relative to each other or to…
- [frontend-performance] No file in the repo defines or imports the `server-only` npm guard (`grep -rln '"server-only"' src/ app/ package.json` returns nothing, and it is not even a package.json dependency). dweb-01's fix correctly proposes adding it to src/lib/db.ts/db-settings.ts/db-api-keys.ts, but the finding doesn't note that this is a systemic gap with zero enforcement anywhere in the codebase — any other 'use c…
- [trading-money-path] src/lib/alpaca.ts:716 (+ trackHealth :256-286): the REST createOrder is wrapped in trackHealth, which RETRIES once on isTransientNetworkError (network-errors.ts:49: ECONNRESET, socket hang up, EPIPE, 'other side closed', ETIMEDOUT) — errors that can occur AFTER the POST reached Alpaca. The retry reuses client_order_id=refId, Alpaca answers 422 'client_order_id must be unique', and strategy-execu…
- [trading-money-path] src/lib/alpaca.ts:335-341 callMcp never checks result.isError (MCP tool-error convention); a tool-level rejection text is JSON.parse'd, fails, and is returned as a bare string, so placeEquityOrder (:772-783) yields a string with no orderId. Downstream survives only because strategy-execution.ts:1422-1431 keeps the row 'placing'/uncertain on a missing orderId — so every MCP-side broker rejection…
- [trading-money-path] ios/SocraticTrade/OrderCancel.swift:125 carries the same 'No new order is placed.' promise as the console cancel sheet; for app-managed protective stops (tsx-08) it is false on iOS too — any fix to cancel-sheet.tsx:87 must land on both surfaces.
- [performance-accounting] src/lib/strategy-tuning.ts:415-418 + :603 — the tuning/LLM-review context's `recentFills` is `listFillEvents(accountNumber, source, 30)` = ASC LIMIT 30 = the OLDEST 30 fills ever, mislabeled 'recent'; distinct from perf-01 because it trips at 31 fills, so it is almost certainly live today.
- [performance-accounting] src/lib/cash-flows.ts:181-219 with strategy-execution.ts:1568-1571 — the benchmark TWR path is NOT 'mostly immune' to the perf-02 mechanism: any off-app/manual trade (owner trades manually per that comment) has no fill row, so market P&L on that day is inferred as a verified deposit/withdrawal and enters TWR at benchmark.ts:137-141.
- [performance-accounting] app/console/lib/derive.ts:424-432 with src/lib/performance.ts:1658-1666 — for a broker/paper account whose paperEquityCurve is the synthetic 100+realized curve, deriveDayPnl takes baseline.equity≈100 and prints Day P&L ≈ the entire account equity (edge: requires zero paper snapshots; no pruning found).
- [performance-accounting] ios/SocraticTrade/ResultsView.swift:78-84 — 'Avg. Return' tile omits the capital-weighted / closed-capital basis the web tile discloses (app/console/results/page.tsx:976); same number, less honest label on the phone.
- [performance-accounting] src/lib/performance.ts:1709 — winRate counts pnl === 0 (flat/scratch closes) as losses (`lot.pnl > 0`), so scratch exits depress the displayed win rate; minor but the tile does not say 'wins ÷ all closes incl. flat'.
- [performance-accounting] src/lib/risk-breaker.ts:83 vs src/lib/db-execution.ts:15 vs app/console/lib/derive.ts:426 — three different 'day' boundaries (UTC breaker, America/New_York order stats, Central Day P&L); the perf-08 finding assumed the app is otherwise Central-consistent, which it is not.
- [backend-reliability] src/lib/web-sources/sec-filings.ts:625 + :736-756 — in the full-body write class every filing's chunks are mirrored into document_chunks_fts TWICE (persistLocalComplete on the bare accession, then again on the managed doc_id accession inside one sync transaction); insertDocumentChunkFts dedupes on (hash,symbol,source,accession) so both copies persist. Doubles FTS corpus size (which berel-01 mak…
- [backend-reliability] src/lib/strategy-tuning.ts:418 — `listFillEvents(accountNumber, source, 30, userId)` with the ASC ordering returns the 30 OLDEST fills as the LLM's 'recent fills' context (and :417 slices the first 30 of the oldest-500 window); a distinct, always-wrong site even below the 500-fill threshold once an account has >30 fills.
- [backend-reliability] src/lib/scheduler.ts:936-946 — pending-fill-reconcile lane has neither an in-flight set nor a deadline (stop-monitor/stale-exit at least have sets; safety-maintenance.ts:32-35 wraps the same call in withDeadline); combined with the unguarded setInterval tick (:411) it stacks a full Alpaca order-history walk (alpaca.ts:491) per overlapping tick per account.
- [backend-reliability] app/api/health/route.ts:270 vs docs/rollouts/2026-08-09-event-loop-stall-instrumentation.md:337-350 — the health route's design rationale ('a 503 here restarts Docker and re-halts Green/Red') contradicts the repo's own docker-inspect evidence that unhealthy never restarts and that the live healthcheck is Coolify-injected (retries=10) rather than Dockerfile:75 (retries=3); the effective probe pa…
- [backend-reliability] src/lib/rag/persist-local-complete.ts:48-53 — the per-row insertDocumentChunkFts loop is not wrapped in a transaction, so a full 10-K persists as N separate auto-commit write-lock cycles (the exact 2026-08-10 stall shape db-learning.ts:1628-1636 documents as the reason insertDocumentChunkFtsBatch exists), while the yielding batch helper sits unused by this caller.
- [security-auth] app/api/auth/robinhood/callback/route.ts:56 — public POST reads an unbounded `request.json()` (same class as sec-06); its only throttle is `cf-connecting-ip`, which a direct-origin caller (sec-02) can set per request to defeat the bucket. Use readJsonWithLimit + the shared unknown-ip bucket when the request lacks a cf-ray.
- [security-auth] src/lib/request-user.ts:28-32 — resolveRequestUserFromEmail maps a missing/absent identity header to DEV_USER_ID='local' (the owner) with no production guard; this single line is the shared sink for sec-01 and sec-07 and should deny in production when x-authenticated-identity-source is absent, independent of any middleware fix.
- [security-auth] src/lib/auth/auth.ts:135-152 — signIn callback verifies GitHub emails via /user/emails but never checks Google's `profile.email_verified`; Google accounts are effectively always verified so likelihood is low, but the allowlist keys on the bare email, so add the check for parity (P3).
- [llm-agent-architecture] src/lib/red-team.ts:196 -- deriveExecutionState(policy, activeAccount) has the SAME active-account bug as llm-02's venue derivation (uses getActiveConnectedAccount's account, not the account under review), compounding llm-02: the execution-mode context handed to the Red Team model itself (executionMode/executionModeClarification in userContent, ~line 234-235) can describe the wrong account's…
- [llm-agent-architecture] src/lib/learning-review.ts:754 -- the daily review's own model resolution (`policy.learningReviewModel?.trim()`) reads getPolicy(userId) with no connectedAccountId, so in a multi-account setup the reviewer model is always the ACTIVE account's choice regardless of which account's pending rows are being judged -- same class of active-account bug as llm-02/llm-05, but on the reviewer's own mode…
- [llm-agent-architecture] src/lib/strategy.ts BULL_PROPOSAL_REQUIRED_KEYS (used identically for both the JSON-schema 'required' array AND the jsonrepair completeness gate) has no test asserting the two stay in sync with the properties object -- llm-01 is exactly the failure mode a `Object.keys(properties) subset-of required` unit test (as llm-01's own rec proposes) would have caught at PR #2740's merge; worth confirm…
- [llm-agent-architecture] app/console/strategy/page.tsx's Red Team hint (llm-14) also omits that exits/covers are structurally exempt from ANY review per strategy.ts's own comment ('Exits and net-risk-reducing trades are structurally EXEMPT') -- the owner-facing copy never states this at all, which is a stronger disclosure gap than just the stale two-pass wording the finding flags.
- [market-data-integrity] src/lib/data-providers.ts:3193-3199 (Finnhub) — same class of bug as mdi-03/nasdaq-quote: Finnhub's session `volume` field is cached under the generic 6h fundamentals TTL with no per-field timestamp, so it can also be stamped with a fabricated 'now' fetchedAt via the same takeScalar cascadeFetchedAt fallback — mdi-03's fix should explicitly cover Finnhub's volume too, not just nasdaq-quote.
- [market-data-integrity] src/lib/market-hours.ts:288 isWeekendOrHolidayClosureAhead has the identical over-broad extension on the evening/day BEFORE any single mid-week holiday (checks only 'is tomorrow a trading day'), not just weekends — e.g. a Wednesday write ahead of a Thursday holiday extends past Thursday to Friday's open. Worth explicitly covering in mdi-01's fix/tests, not just the Friday case.
- [market-data-integrity] src/lib/data-providers.ts:2366-2371 — AlpacaSnapshotEnrichmentProvider defaults to feed 'iex' but this choice is never surfaced in console/iOS UI; combined with mdi-09's volume-mixing finding, users are never told that Alpaca prices/volumes may be IEX-only (a small slice of consolidated tape) absent a paid SIP subscription.
- [qa-test-strategy] No React Testing Library (or jsdom/happy-dom) anywhere in the toolchain — package.json has zero @testing-library/* deps and vitest.config.ts sets no `environment` (defaults to node), so the four *.test.tsx files (e.g. test/console-sheet.test.tsx) only test extracted pure functions, never actually render a React component. A broken conditional render in any app/console/** component (e.g. a button t…
- [qa-test-strategy] ios/SocraticTradeTests has zero XCUITest target — only 15 XCTest (unit/model) files exist (ios/SocraticTradeTests/*.swift, confirmed via `ls`), so there is no automated on-device/simulator UI interaction test at all (tap a button, see a screen change) for the native app, only decode/derivation logic tests. This compounds qa-01/qa-02: even if `xcodebuild test` were added to CI as those findings rec…
- [qa-test-strategy] scripts/ios-scheduled-ship-gate.sh (referenced by ios-ship.yml's 30-min cron) has no dependency on ios-build.yml's compile-check result at all — the gate only checks 'is HEAD the last-shipped sha' plus a path-changed check (per ios-ship.yml's own comments), so even a PASSING ios-build.yml run on a prior commit provides no guarantee for what the cron actually ships; qa-01's suggested fix (refuse to…
- [qa-test-strategy] test/global-setup.ts's TMPDIR-scoped isolation (vitest.config.ts's runTmpRoot) only protects against SQLite temp-file leaks; it does nothing to prevent or detect the live-network calls qa-05 identifies — worth noting the two problems (temp-file leak vs. live-network nondeterminism) are solved by unrelated mechanisms and a reviewer landing qa-05's fix should not assume global-setup.ts already cover…
- [ops-observability-admin] app/api/health/route.ts:305 -- getOpenRouterCreditStatus is awaited inline in the GET handler with maxWaitMs:1500, meaning every /api/health poll (including the Dockerfile's 30s-interval, 5s-timeout probe) can itself add up to 1.5s of outbound network latency -- this compounds sre-04's healthcheck-timeout risk beyond what sre-04 already lists (it names the credits fetch but the finding's ow…
- [ops-observability-admin] src/lib/db-health.ts (alertConnectionFailure/alertStorageWarning) -- both cooldown-gated alert functions were not checked for WHICH notification channels they fan out to (push/email/Twilio); if Twilio/Pushover delivery itself silently no-ops when unconfigured, that's a second layer of 'health signal computed but nobody paged' beyond what sre-02/sre-10 already cover for scheduler/credits --…
- [ops-observability-admin] scripts/verify-deploy-sha.sh itself (not just its absence from CI) -- did not verify whether the script correctly handles the case where origin/main has NO commits ahead (exit 0 too eagerly) versus genuinely stale prod, which would matter for sre-07's proposed cron-based fix.
- [ops-observability-admin] app/api/admin/backup-status route (referenced by sre-01's recommendation and implicitly by sre-03) was not independently opened to confirm what it currently reports for the two litestream-disabled markers -- sre-03 infers its behavior from runtime-health.ts/route.ts patterns rather than reading backup-status's own code directly; worth a follow-up read before implementing the fix.

## 9. Strengths — Do Not Churn

Deduped lightly; each lane's own words, prefixed with the lane.

- [ios-hig-ux] Two-space sentence rule is applied consistently across every user-facing Swift string (a grep for single-space sentence gaps in string literals returns only 'Avg. Return') — do not churn.
- [ios-hig-ux] One deep-link router (DeepLink.destination(for:)) shared by universal links and tapped pushes, with https-only + host allow-list and proposal-id validation; PushPayload only extracts and never routes — clean and testable.
- [ios-hig-ux] SnapshotScaffold gives every tab the same loading / empty / stale / pull-to-refresh / TimelineView freshness contract, and MobileStore gates commands on the server-advertised control catalog while never gating protective commands (Stop / Close Only / Wind Down) — good correctness-harde…
- [ios-hig-ux] Owner-customizable tab bar mirrors the web mobile tabs (min 2 / max 4, canonical order, More always reachable, deep links reroute into the More stack) using the iOS 26 `Tab` builder — a genuinely native tab model.
- [ios-hig-ux] Honest data conventions carried to iOS: P/E 'n/a' vs '—', Red Team failure kinds mirrored from red-team-routing, benchmark 'cash flows neutralized' caption, realized P&L shown as '—' when there is no fill ledger, and no fabricated numbers anywhere.
- [ios-hig-ux] Typography uses `Font.custom(_:size:relativeTo:)` so Dynamic Type scales the whole app, and icon-only controls carry accessibilityLabel/hint (watchlist remove, cancel order, pin toggles, tab customization hints); APNs environment is read from the embedded provisioning profile rather th…
- [ios-engineering] Web sign-in handoff is genuinely well designed: PKCE-bound one-time code in server memory (src/lib/mobile-auth-handoff.ts), the session JWT never crosses the custom URL scheme, `socratictrade://` is rejected as a content route in DeepLink.swift, and `resolvedURL` pins https + host…
- [ios-engineering] APNs environment is resolved from the embedded provisioning profile's `aps-environment`, not `#if DEBUG`, with a pure decision table and tests (PushNotifications.swift:29-90) — this is the failure mode most apps ship; keep it.
- [ios-engineering] Command submission is idempotent end to end: `CommandAttemptTracker` + `Idempotency-Key: <type>:<uuid>` header, key retained on network/decoding errors and released on server errors, with reconciliation against `recentCommands` (MobileStore.swift:4-71, 369-457).
- [ios-engineering] Deep-link routing is single-sourced and contract-tested against the server (DeepLink.swift ↔ src/lib/push-deep-links.ts ↔ test/apns-deep-link-contract.test.ts + PushNotificationTests) and pure/total — every unknown input maps to nil.
- [ios-engineering] Error copy discipline: Cloudflare 52x mapped to plain language, Apple/APNs jargon scrubbed from settings copy, two-space sentences honored, and protective commands (stop/close-only/wind-down/reject/cancel) explicitly exempt from freshness/catalog gates — matches the owner's adviso…
- [ios-engineering] Snapshot decoding tolerates missing sections and a malformed `catalog` (try? + built-in fallback), `RedTeamVerdict` accepts legacy string and object shapes, and OrderCancel mirrors the server's working-state predicate verbatim with rationale — good API-drift posture at the section…
- [desktop-web-ux] Chrome control model is right and well-worded: single Run once, Start/Stop Agent, STOP-never-sells copy in ControlSheet, typed WIND DOWN for the only verb that sells (chrome.tsx 352-523).
- [desktop-web-ux] Approval card + bulk approve: collapsed receipt, 'Approve live' + BROKER tag, paste-blocked typed batch sheet, per-row re-check messaging, 20-per-batch rate guard (approvals/page.tsx, approval-card.tsx).
- [desktop-web-ux] Guardrails page: Essentials / Protective stops with StopFlowDiagram / Advanced rulebook collapsed by default, cap-utilization meters beside the caps, advisory framing — best-in-product configure surface.
- [desktop-web-ux] Results honesty: TWR vs SPY with 'benchmark unavailable' state and phantom-transfer exclusion, gated Red Team efficacy and Signal-health/Lookahead audits with plain tooltips (results/page.tsx 230-345).
- [desktop-web-ux] Orders blotter: replace-at-market/cancel with precise tooltips, stale-limit banner, per-column explanations; consistent tabular-nums and Central-Time fmtExact hover everywhere.
- [desktop-web-ux] Shell robustness: honest slow-first-load copy, retry card, self-skeleton routes, dirty-guard on nav, page-width canon (CONSOLE_PAGE_WIDTH) with documented two-column exceptions.
- [public-site-growth] Anti-scrape posture is coherent and layered: default-noindex metadata, AI-crawler opt-out list in robots.ts, /framework served only through a UA-gated API with `tdm-reservation` and a humanOnlyNote — do not churn.
- [public-site-growth] Disclosure boilerplate is consistent and legally sensible across welcome, how-it-works, framework and terms (not advice / not BD / not RIA / substantial risk / simulated ≠ future / you set authority), and the login clickwrap is versioned via LEGAL_NOTICE_VERSION so re-consent i…
- [public-site-growth] Privacy Policy is unusually concrete for a solo product: names the 7-day backup TTL from litestream.coolify.yml, self-serve deletion path, SMS STOP/HELP, and the mandatory market-data pool with an explicit list of what is never pooled.
- [public-site-growth] /framework content is candid in a way that builds trust (a standing “Limits” list — cold start, factor weights were guesses, counterfactuals ignore fills) — exactly the E-E-A-T signal docs/go-to-market.md asks for.
- [public-site-growth] Login is minimal, brand-consistent with iOS (same candlestick wordmark, provider order), and auth gating fails closed (GitHub verified-email check, Apple email caveat surfaced in UI).
- [public-site-growth] Public pages are token-based (bg-bg/text-fg/pos/neg), light by default per the ruling, and stack cleanly at phone widths (flex-col footers, full-width CTAs with min-h-11).
- [mobile-web-ux] Bottom tab bar is genuinely mobile-first: owner-pinnable tabs persisted per browser (mobile-tabs.ts), a More sheet that measures the real bar offset via ResizeObserver/visualViewport (nav.tsx:372-393), safe-area padding inside the painted bar, near-opaque blur surface, and a documen…
- [mobile-web-ux] Money actions on phones are already treated seriously: Approve/Reject stick above the tab bar (.ac-actions), typed confirmation is case-insensitive with autoCapitalize=characters and paste disabled, STOP is a 44px coarse-pointer target that never squeezes, and Run once/Start/Stop pr…
- [mobile-web-ux] Sheet becomes a bottom sheet under 768px with a focus trap and opener-focus restore, the symbol drawer goes full-width with overscroll containment, and toasts are lifted above the tab bar — the overlay architecture is right, it just needs scroll-lock/history/keyboard finishing.
- [mobile-web-ux] Anti-zoom discipline exists at the token level (16px input floor at ≤640 in both globals.css and console.css) and numeric fields use inputMode="decimal"; the two leaks found are inline/specificity exceptions, not a missing policy.
- [mobile-web-ux] Dense desktop tables already have phone card variants for Orders, Positions and Scan; public how-it-works/framework pages ship separate portrait SVG diagram variants; prefers-reduced-motion is honored console-wide (including the infinite-animation iteration clamp).
- [mobile-web-ux] The whole console is tokenized (--con-fs-*, --con-* colors, --radius-*), so phone-specific tuning (type bump, tab-bar height var, theme-color sync) is a few CSS lines rather than a per-component sweep.
- [web-accessibility] Token discipline is real: light neutrals (--con-fg 16.4:1, --con-muted ≥6.4:1, --con-faint ≥5.1:1 on every light surface) and the whole dark palette (every tone ≥5.4:1, chip-on-soft 5.5–8.8:1, filled-button contrast pairs 6.4–10.9:1) clear AA; the two dark blocks are byte-identi…
- [web-accessibility] Focus and motion basics are right: global .console-root :focus-visible ring (:230-233) plus restored input focus-visible (:634-640); prefers-reduced-motion clamps duration AND iteration count so infinite pulses settle (:1200-1210); intro canvas, header logo, and loading brand al…
- [web-accessibility] Overlay engineering is above average: useFocusTrap has a real surface stack, restores opener focus, and refuses to open over the consent gate; the command palette is a textbook combobox/listbox with aria-activedescendant; the symbol drawer is a named modal dialog; scan tabs use…
- [web-accessibility] Word-first status everywhere the money is: SignedText always carries +/−, StateChip pairs an aria-hidden Dot with words, reality chips are words not colors, LiveTag reads BROKER, Approve buttons name side+symbol, all 15 Toggles pass a label, icon-only buttons (IconButton, RunOnc…
- [web-accessibility] Public pages (welcome, how-it-works, privacy, terms) render server-side with a single h1, ordered h2/h3, header/main/footer landmarks, per-page <title>, and role=img + aria-label on every diagram/illustration; the framework SVGs are labeled and decorative trace art is aria-hidde…
- [api-contract-web-ios] Mobile command plane is genuinely well designed: per-user idempotency keys (header or body, regex-bounded), immediate protective commands that pre-empt queued risk-increasing work, order.cancel expectedAccountNumber and policy.patch expectedCurrent stale-view guards, and a st…
- [api-contract-web-ios] Server-advertised control catalog + iOS `serverAdvertises`/`canSubmit` handshake means an older/newer app never offers a command the server will refuse (mobile-api.ts:1288-1308; MobileStore.swift:233-270; MobileModels.swift:80-98 with a deliberately lenient decode).
- [api-contract-web-ios] Identity is middleware-only and fail-closed: client identity headers are stripped, no route reads userId from body/query (grep clean), all 25 /api/admin routes carry an in-handler requireAdmin/token check, and the CSRF gate is designed so native URLSession (no Origin/Sec-Fetc…
- [api-contract-web-ios] Mobile web-auth handoff is PKCE-bound, one-shot, 2-minute TTL, timing-safe compare — the session JWT never rides the custom URL scheme (src/lib/mobile-auth-handoff.ts:50-95, app/api/mobile/auth-redirect/route.ts).
- [api-contract-web-ios] Swift decoders are defensive where it matters: RedTeamVerdict accepts legacy string/partial shapes, MarketScanResponse falls back across asOf/generatedAt/scannedAt, ChatDraft accepts snake/camel keys, and the 503 scan_quotes_unavailable body is decoded so Scan shows the real…
- [api-contract-web-ios] A cross-language contract test already exists and works (test/apns-deep-link-contract.test.ts reads the Swift sources) — the pattern to extend, not invent.
- [frontend-performance] Server-side snapshot assembly is genuinely well engineered: independent upstreams race in one Promise.all with per-section deadlines and a `timedOutSections` log line (dashboard.ts:373-390, 596-618), fills/P&L are fetched once and threaded to every consumer, proposal lookups…
- [frontend-performance] The client refresh layer (app/console/lib/useConsoleData.tsx) is carefully reasoned: background triggers coalesce instead of aborting an in-flight fetch, foreground refreshes supersede, a 35 s per-attempt deadline retries genuine hangs, and the 'slow first load' notice is adv…
- [frontend-performance] Fonts are done right: self-hosted Lato via next/font/local with `display: swap`, `adjustFontFallback: "Arial"`, latin/latin-ext subsets, 8 files totalling ~69 KB, no build-time network dependency (app/fonts/lato.ts).
- [frontend-performance] Scan table uses react-virtuoso with a memoized ScanTable and memoized sort/columns (scan-table.tsx:180,275-294); strategy_run audit payloads were bounded (audit-bounded-run.ts) and the ticker-logo route has disk cache + `public, max-age=86400, stale-while-revalidate=604800`.
- [frontend-performance] CSS footprint is small and static (two stylesheets, ~18 KB gz total) with an unlayered con-* token system and no runtime CSS-in-JS; the mobile snapshot route (app/api/mobile/snapshot/route.ts) already demonstrates the right 'pick fields' pattern the web snapshot should adopt.
- [frontend-performance] Sentry is env-gated, PII is redacted through `redactForTelemetry`, and Session Replay is off by default with mask-all-text/block-all-media when enabled (instrumentation-client.ts).
- [trading-money-path] Placement is genuinely crash-safe: durable 'placing' intent + client_order_id idempotency (refId) + CAS claim on status='proposed' + per-account mutation lease + refId-based reconcilePlacementError/flagStalePlacingIntents (strategy-execution.ts:1143-1258, 1948-2042). Do not chu…
- [trading-money-path] Guardrail philosophy is encoded in ONE place (policy.ts:246-305 HARD_GATE_REASON_PATTERNS) with escalatable time-context caps and pushEscalatable — matches the owner ruling that only physical/broker/accounting impossibilities are hard.
- [trading-money-path] Broker-side classification is careful: OrderValidationError vs definitive 4xx vs uncertain, synchronous rejected/canceled states never booked as placed, and broker-order-constraints.ts audits every reshape/block pre-submission.
- [trading-money-path] Cancel doctrine (order-cancel.ts) — a standalone cancel never waits on the lease, pre-read is advisory and time-boxed, works while Stopped, shared verbatim by console and mobile.
- [trading-money-path] Approval-time repricing routes MATERIAL drift back to the human on live+typed confirmation and persists the exact order the broker will see before the claim (strategy-execution.ts:327-481).
- [trading-money-path] Cross-account isolation holds: identity only from middleware (request-user.ts), every ledger read/write scoped by user_id + account_number, mobile cancel refuses when expectedAccountNumber != selected.
- [performance-accounting] Multi-period TWR with an explicit capital-regimes table, a sanity bound that demotes unreconcilable inferred transfers to 'inferred — unverified', and a first-class 'benchmark unavailable' state instead of a fake 0.00% (benchmark.ts:114-180, 286-308; results/page.tsx:229-34…
- [performance-accounting] FIFO lot engine matches only same-side lots (sell→long, cover→short), computes signed unrealized for shorts correctly, and is regression-tested (performance.ts:590-660; test/short-pnl-sign.test.ts).
- [performance-accounting] Capital-weighted closed-trade return replaced the naive per-trade mean, with an honest tooltip explaining what it is and is not (performance.ts:1712-1744; results/page.tsx:978).
- [performance-accounting] Lot-ledger reconciliation against live positions suppresses tax math on mismatched symbols rather than printing confident wrong numbers (tax.ts:341-347, 367-371) — the right instinct.
- [performance-accounting] Day P&L states its baseline, flags stale baselines loudly, and the 'Deployed today' chip (C9) is fixed (derive.ts:398-467; chrome.tsx:1031).
- [performance-accounting] Bayesian shrinkage priors on small-n scorecards (performance.ts:1484-1494) and separate paper/brokerage books that never share an axis (results/page.tsx:3-7) are the correct discipline.
- [backend-reliability] Order placement is idempotent end to end: client_order_id = refId on Alpaca (alpaca.ts placeEquityOrder) plus refId->clientOrderId recovery in strategy-execution.ts:2057-2146, so retries, crashes and stale-run sweeps cannot double-place — do not churn this.
- [backend-reliability] Versioned migrations are done right: PRAGMA user_version stamped inside a BEGIN IMMEDIATE transaction per migration (db.ts:3250-3273), with the 2026-08-12 SQLITE_BUSY lesson encoded in code, not just a doc.
- [backend-reliability] The production exit-code contract (exit-guard.ts + coolify-prod-start.sh run_app supervisor, direct node_modules/.bin/next invocation, exit 40/41/42/43/130/143 map) gives every container exit a receipt — rare and valuable.
- [backend-reliability] Concurrency primitives are consistently globalThis-pinned and lease-based: single-leader scheduler lease, per-account mutation lease with TTL/heartbeat (account-mutation.ts), per-account strategy lock with guard heartbeat, per-lane in-flight sets, and the SEC worker's serializ…
- [backend-reliability] Every scheduler lane is journaled (task-journal.ts) with status/summary and pruned on a fixed retention, and audit-prune runs bounded daily batches — the observability skeleton to hang berel-06's loop-lag metric on already exists.
- [backend-reliability] The health route's 'degrade, never 503' reasoning is written down at each check with the restart->re-halt loop it avoids, and the litestream tier-freshness model honestly reports 'not observable' instead of fabricating a green state.
- [security-auth] Edge identity pipeline is genuinely defense-in-depth: middleware strips client identity headers on every path (public included), forwards provenance + loginAt, CF Access header trust requires JWKS-verified JWT with audience + email cross-check, admin gate accepts only verified prove…
- [security-auth] No route in app/api reads userId from body/query; every user-scoped [id] route audited (connected-accounts, profiles, overlays, learned-context, socratic decisions/framework, mobile commands, proposals) passes the session-derived userId into the db call; SSE stream filters by subscr…
- [security-auth] Secrets at rest are done right: AES-256-GCM v1 envelope with strict isEncryptedValue, plaintext no longer decrypts (fail closed), boot refuses production without a valid ENCRYPTION_KEY, and API responses only ever expose maskApiKeyPreview / projected account rows.
- [security-auth] Token comparisons (admin, ops, App-B import, TradingView, congress HMAC, mobile PKCE) are constant-time; the mobile handoff is PKCE-bound, one-shot, consume-before-compare, and never puts the JWT in the custom-scheme URL.
- [security-auth] Public endpoints that were fixed (csp-report, apple auth, both webhooks) use streaming byte caps rather than content-length; broker baseUrl and notification webhooks go through host allowlists / egress-guard (prior S2 closed); chat tools are read-only/draft only; Sentry has sendDefa…
- [security-auth] iOS surface is conservative: no ATS exceptions, https-only universal links, custom scheme rejected for content routes, WKWebView admin portal locked to one host with session-expiry detection.
- [llm-agent-architecture] Bull parse path is genuinely hardened: ambiguity guards, jsonrepair gated behind a completeness/enum/range filter with schema-key projection, truncation-aware reasons, and audited failover per hop (strategy.ts:6111-6180, 6713-6808) — do not churn.
- [llm-agent-architecture] Red Team is fail-closed with a precise failureKind taxonomy, no model defaults, strict-shape verdict validation, and honest exhausted-chain wording; unavailable openings are held for the human with a persisted reason (red-team.ts:131-168, 428-537; strategy.ts:2626-2658).
- [llm-agent-architecture] Prompt-safety discipline is consistent across surfaces: containPromptText/containPromptDataTree, deterministic injection scan with audit receipts, fenced owner prompt + reflection, evidence packs with content hashes and budget receipts (strategy.ts:5059-5075, 5666-5743; cha…
- [llm-agent-architecture] Failover-aware attribution reaches the owner on both surfaces: proposedByModel / reviewedByModel / redTeamVerdict.failureKind are persisted and rendered on the approval card and in iOS ProposalsView, with 'no fallback to Green' honored in the UI.
- [llm-agent-architecture] Cost governance plumbing exists and is layered sensibly: per-user/day budget admission with reservation, provider cooldown planning, run-scoped usage-budget downgrades that never persist to policy, and per-model×context usage rows with cache-token accounting.
- [llm-agent-architecture] Chat orchestrator: no execution path, coach-learning receipts prepended to the reply, per-turn evidence-pack manifest, kb_search results contained as 'rag', and a golden no-execute eval gate.
- [market-data-integrity] Per-field provenance is real and consistent: `sources` + `fieldObservations` per cell, honest 'n/a' (computed no-ratio) vs '—' (unavailable) on web (columns.tsx) and iOS (AppFormat.peRatioDisplay), and MarketScan.source is derived from providers that actually contributed (co…
- [market-data-integrity] The quote-staleness gate follows the owner's philosophy correctly: it never blocks or escalates; it converts a stale opening to a limit at the intended entry (policy.ts:395-471), and venue-delayed feeds age the snapshot rather than the print.
- [market-data-integrity] Symbol and price boundaries are handled at the adapter edge: BRK/B -> BRK-B (market.ts normalizeMarketDataSymbol), toAlpacaSymbol/fromAlpacaSymbol, and roundAlpacaPrice at every Alpaca order field, so internal state stays canonical.
- [market-data-integrity] Yahoo adjclose handling scales O/H/L by the adjustment factor so candles stay consistent (history.ts:770-793), and the benchmark refuses to print a fake 0.00% (assessBenchmarkSeries) instead of a truncated comparison.
- [market-data-integrity] Market-hours math is DST-safe and ET-anchored (etWallClockToUtcMs, computus Good Friday, observed-holiday shifts) with good test coverage — the mdi-01 defect is a gating condition, not the calendar itself.
- [market-data-integrity] Screener failure modes are loud: ScanQuotesUnavailableError instead of a silent empty table, and the 15s/BROWSER_UA/abort-reason fix for api.nasdaq.com is documented in-line.
- [ux-copy-editor] iOS string literals honor the two-space rule end-to-end: 62 double-space sentence gaps and zero single-space ones across ios/SocraticTrade/*.swift, and MobileAPIClient.swift error copy is plain-language, vendor-free and two-spaced ("Socratic Trade is unreachable right now. Try agai…
- [ux-copy-editor] All 15 iOS navigationTitle calls pair with .navigationBarTitleDisplayMode(.inline) (MobileControlView/Home/Proposals/Assets/Activity/Insights/Coach/Scan/Guardrails/Results/More/Admin Portal/consent), and AppFormat.money(compact:) already emits lowercase k/m/b — do not churn.
- [ux-copy-editor] app/console/lib/format.ts centralizes timestamps on CENTRAL_TIME_ZONE (timeAgo/fmtExact/fmtClock/fmtDay/dayKey) and defines SENTENCE_GAP; app/console/lib/labels.ts guarantees no raw snake_case enum ever reaches the user (plainLabel fallback + explicit label maps).
- [ux-copy-editor] The n/a vs — convention (computed no-ratio vs unavailable) is documented and implemented consistently for P/E in scan/columns.tsx, ui/drilldown-data.ts and iOS AppComponents.swift:210-214.
- [ux-copy-editor] Web reality tag already renders BROKER instead of LIVE (ui/primitives.tsx:177) and the iOS Home hero deliberately shows only a paper badge per the owner's preference — the right pattern to copy to the remaining Live callouts.
- [ux-copy-editor] Approvals empty state (approvals/page.tsx:399-419) is the model for owner-density copy: one paragraph, SENTENCE_GAP-separated, names the ⚡ Run once control the way it renders, and adapts to the stopped state.
- [qa-test-strategy] Protective-stop lanes are the best-tested money path in the repo: test/broker-protective-stops.test.ts (1,593 lines) and test/synthetic-stops.test.ts (1,890 lines) carry adversarial cases from multiple Codex review rounds (pending_cancel never orphans, ordersListed:false never as…
- [qa-test-strategy] Money-path tests use real policy.ts / performance.ts / db with the test-only TestBrokerGateway: zero test files mock policy or performance wholesale, so sizing, wash-sale, PDT and P&L math (incl. short/cover in policy.test.ts and short-pnl-sign.test.ts) are exercised for real.
- [qa-test-strategy] test/apns-deep-link-contract.test.ts is a genuine cross-language contract test (reads the Swift source and asserts the server emits exactly those URLs) mirrored by PushNotificationTests.swift — the right template for the snapshot contract gap.
- [qa-test-strategy] Temp-DB hygiene is solved structurally: vitest.config.ts redirects TMPDIR/TMP/TEMP into a per-run agentic-vitest-* dir and test/global-setup.ts removes it and sweeps >6h leftovers (fixed a 178k-file/130 GB leak).
- [qa-test-strategy] The `verify` gate is designed fail-closed (classify → gate; --no-renames on the docs diff; hourly backstop for bot-merged commits) and the iOS ship-gate bash script has its own offline unit tests run on every PR.
- [qa-test-strategy] The iOS test target was made actually runnable on 2026-08-12 (TEST_HOST + PRODUCT_MODULE_NAME fixes in ios/project.yml) and already enforces owner copy rules (UserFacingCopyTests, OrderCancelTests two-space check) — a base worth wiring into CI.
- [ops-observability-admin] /api/health is unusually well-reasoned: two-audience projection (public vs x-ops-token) that keeps ok/503 byte-identical, explicit 'degrade only, never 503' rules to avoid the restart→boot-interlock loop, retired-vendor filtering, and every check wrapped so monitoring can…
- [ops-observability-admin] Backup monitoring depth is real: three independent litestream signals (IPC freshness, per-compaction-tier LTX freshness with an explicit 'not-observable/empty-wedged' vocabulary, and litestream's own log markers), a coverage-blind alert, plus the weekly R2 cold snapshot as…
- [ops-observability-admin] The 2026-08-13 honest-server-stats rewrite genuinely separates 'measured zero' from 'not measured' (describeResourcesObservation, unobservedHostFacts, staleScope) and removed the fabricated runner rows — do not regress it.
- [ops-observability-admin] Notification plumbing is solid: multi-channel with transient-error retry, APNs dead-token retirement and auth-error audit, notify.sent/notify.error receipts, a real 'Send test' in Settings > Delivery, per-condition mutes, and Pushover-first with email as paid fallback (src…
- [ops-observability-admin] verify-deploy-sha.sh is a model operator script: containment predicate instead of equality, distinct exit codes per operator action, ASCII-only, self-tested (scripts/verify-deploy-sha.sh header).
- [ops-observability-admin] Sentry hygiene: DSN-gated, sendDefaultPii:false, redactForTelemetry on every runtime, provider-key scrubbing before health log storage, and stable fingerprints by lane id so one outage does not fragment into six issues (db-health.ts:596-620, provider-rate-limit.ts:522-540).

## 10. Coverage Notes Per Lane

- **iOS HIG / product** (22 findings): Read in full on current main (c55c2e642): SocraticTradeApp, MobileControlView, HomeView, ProposalsView, MarketsView, ScanView, ResultsView, ActivityView, InsightsView, CoachView, GuardrailsView, SymbolInfoSheet, LoginView, AppComponents, AppTypography, DeepLink, MobileModels, DeskModels (models + DeskCopy), PushNotifications (environment/coordinator surface), MobileStore (canSubmit/submit/load/events), MobileAPIClient outline, AdminPortalView head, PolicyTightening (rituals + error paths); cross-checked web parity against app/console/components/nav.tsx, assistant/chat.tsx + markdown.tsx + dra…
- **iOS engineering** (22 findings): Read in full: MobileStore.swift, MobileAPIClient.swift, MobileModels.swift, DeskModels.swift, PushNotifications.swift, DeepLink.swift, LoginView.swift, SocraticTradeApp.swift, PolicyTightening.swift, OrderCancel.swift, ProposalPriceReview.swift, AdminPortalView.swift, ios/project.yml, Info.plist, entitlements, ios/CLAUDE.md, scripts/ios-ship-testflight.sh + ios-fleet-pin.sh, .github/workflows/ios-build.yml + ios-ship.yml (head), MobileModelsTests.swift, and the server counterparts they depend on (app/api/mobile/auth/*, auth-redirect, mobile-auth-handoff.ts, session-token.ts, auth.ts session c…
- **Desktop-web UX** (20 findings): Read in full: console layout/shell/nav/chrome, page.tsx (Home), approvals page, decisions index, positions, readiness-checklist, needs-attention, command-palette, primitives, sheet, useConsoleTheme, lessons/assistant/usage/connections pages, admin layout + overview head; outlined via grep/partial reads: settings, guardrails, strategy, results, scan, orders, activity, watchlist, macro, lessons inbox, alert-center, console.css tables/nav. Curl of localhost:3005 confirmed every /console route SSRs only 'Loading your Socratic Trade console…' (pure client render), so empty-state judgments come fro…
- **Public site / growth** (20 findings): Read in full: app/welcome/page.tsx + decision-trace-illustration.tsx, app/login/page.tsx, app/how-it-works/page.tsx, app/framework/{page,content,framework-viewer,ua-gate}, app/strategy/page.tsx, app/privacy-policy, app/terms-and-conditions, app/access-denied, app/logout/route.ts, app/layout.tsx, app/robots.ts, app/sitemap.ts, app/manifest.ts, app/page.tsx, middleware.ts (public prefixes + redirect paths), src/lib/auth/auth.ts (pages/callbacks), src/lib/legal-notice.ts, src/lib/landing-page.ts, src/lib/push-deep-links.ts, socratic-runtime.ts override sizing + strategy.ts call site, docs/go-to-…
- **Mobile-web UX** (25 findings): Read in full: app/console/console.css, app/globals.css, app/layout.tsx, app/console/layout.tsx, components/shell.tsx, chrome.tsx (banner, scope, state chip, run-once, user menu, freshness), nav.tsx (rail, More sheet, tab bar), lib/mobile-tabs.ts, lib/useConsoleTheme.ts, ui/sheet.tsx, ui/symbol-drawer.tsx, ui/primitives.tsx (Card/Btn/IconButton/Toggle/Segmented/Tooltip/NumInput), components/command-palette.tsx (theme action), consent-gate.tsx, policy-form.tsx (rows + save bar), approval-card.tsx (header/collapsed body/footer/typed-confirm sheet), scan/scan-table.tsx + scan/page.tsx tail, posit…
- **Web accessibility** (20 findings): Read in full: app/console/console.css and app/globals.css (all text/background token pairs computed with a WCAG luminance script — light and dark, chips on soft fills, filled-button contrast pairs, reality banner washes, input/toggle boundaries), app/console/ui/primitives.tsx, sheet.tsx, focus-trap.ts, toast.tsx, symbol-drawer.tsx, components/shell.tsx, chrome.tsx (ScopeSelector/StateChip/RunStateButton/UserMenu/freshness), nav.tsx, command-palette.tsx, readiness-checklist.tsx, allocation.tsx, approval-card.tsx (header/actions/typed-confirm/receipt), scan/scan-table.tsx + scan/page.tsx, page.…
- **API contract (web + iOS)** (19 findings): Read in full: ios/SocraticTrade/MobileAPIClient.swift, MobileModels.swift, DeskModels.swift, MobileStore.swift (load/events/submit/error paths), OrderCancel.swift, PolicyTightening (payload builder), LoginView web-auth, CoachView bootstrap, AdminPortalView allowlist; src/lib/mobile-api.ts (all 1326 lines), middleware.ts identity/CSRF/public-prefix sections, src/lib/auth/csrf.ts, request-user.ts, auth/admin.ts, rate-limit.ts, events.ts, mobile-auth-handoff.ts; every route iOS calls (mobile/snapshot, commands, commands/[id], events, consent, push/register, account-deletion request+confirm, auth…
- **Frontend performance** (17 findings): Read: app/layout.tsx, app/console/layout.tsx, components/shell.tsx, lib/useConsoleData.tsx, lib/useConsoleTheme.ts, lib/useTickerLogoDisplay.ts, ui/ticker-logo.tsx, ui/primitives.tsx (Tooltip/Ago), ui/header-logo.tsx, components/intro-canvas.tsx (loop), components/nav.tsx (links), scan/{page,scan-table,use-live-scan}, orders/page.tsx (cancel/history), activity/page.tsx (memo), connections/page.tsx + settings/brokers.tsx import chain, results/page.tsx (skim), alert-center.tsx (ack flow), lessons/watchlist snapshot usage; next.config.mjs, instrumentation-client.ts, package.json; src/lib/dashboa…
- **Trading money path** (19 findings): Read in full: strategy-execution.ts executeProposal (232-1519), reconcilePendingFills/reconcilePlacementError, policy.ts gates 236-670, alpaca.ts placement/orders/tif/callMcp, tradier.ts placeEquityOrder, robinhood.ts toMcpOrder/placeEquityOrder, order-cancel.ts, order-replacement.ts (1-130, 271-500, 732-856), stale-limit-orders.ts, approval-reprice.ts, execution-mode.ts, db-execution.ts stats/locks, db-proposals.ts CAS functions, scheduler.ts per-account tick (820-1000), synthetic-stops.ts registration + fire head (520-810, 940-960), broker-protective-stops.ts (60-340, 1090-1135, 1395-1515),…
- **Performance accounting** (24 findings): Read in full or in the relevant sections: src/lib/performance.ts, cash-flows.ts, benchmark.ts, db-fills.ts, dashboard.ts (performance/benchmark block), app/console/lib/derive.ts (Day P&L, mark-to-market, equity window), app/console/results/page.tsx, app/console/page.tsx (metrics), app/console/components/equity-chart.tsx, positions.tsx (unrealized), src/lib/tax.ts (summary/wash), risk-breaker.ts, outcome-engine.ts (measureCase/findRealizedLot), backtest.ts (curve/annualization), history.ts (adjusted closes), alpaca-account-insights.ts, execution-cost.ts, strategy-execution.ts (reconciliation w…
- **Backend reliability** (16 findings): Read in full or in the relevant regions on current main (c55c2e642): src/lib/scheduler.ts, src/lib/db.ts (getDb pragmas, fence triggers, runMigrations, FTS DDL, indexes), src/lib/db-learning.ts FTS helpers, src/lib/rag/sec-ingest-worker.ts, src/lib/rag/fts-mirror-bound.ts, src/lib/rag/persist-local-complete.ts, src/lib/web-sources/sec-filings.ts mirror, src/lib/slow-sync-guard.ts, src/lib/exit-guard.ts, instrumentation.ts, src/lib/background-worker-startup.ts, scripts/coolify-prod-start.sh, Dockerfile, app/api/health/route.ts, src/lib/runtime-health.ts (log scan), src/lib/streams/alpaca-trade…
- **Security / auth** (14 findings): Read in full: middleware.ts, src/lib/auth/* (admin, identity, csrf, session-edge, session-token, strip-identity, apple-web, auth), request-user.ts, mobile-auth-handoff.ts, ops-auth.ts, securities-import-auth.ts, rate-limit.ts, public-origin.ts, all /api/mobile routes, /api/admin gating (all 23), keys route, health/csp-report/webhooks/logos/framework-content, events/stream, memory, orders/cancel, proposals approve/reject, policy route, connected-accounts, robinhood OAuth start/callback/tokens, db-api-keys encryption paths, next.config.mjs, instrumentation.ts, coolify-prod-start.sh + infisical-…
- **LLM / agent architecture** (20 findings): Read on current main (c55c2e642): src/lib/strategy.ts (proposeTrades 4700-6340 incl. prompt assembly, containment, schema, failover, parse; run loop 2440-2720 Red review; rotation/budget merge 1060-1135; recordLlmOutcome), strategy-prompts.ts (measured Bull ≈4.8k tokens), red-team.ts, retry-red-team.ts, strategy-execution.ts approval re-review, llm-call.ts (response_format per transport), llm-provider.ts, llm-request.ts (soft/hard timeouts, failover statuses), model-rotation.ts, llm-usage.ts, strategy-tuning.ts requestLlmTuning/schema/toPatch, proposal-revalidation.ts, learning-review.ts (app…
- **Market-data integrity** (18 findings): Read on current main (c55c2e642): src/lib/market.ts (scan pipeline, mergeQuoteData, mergeGroupedBarData, quote-only fallback, applyEnrichment), data-providers.ts (provider wiring, CascadingEnrichmentProvider.enrich/takeScalar/EMPTY_SOURCED, nasdaq-quote, Alpaca snapshot, Robinhood/Finnhub volume, mock tier), quotes-cascade.ts, market-hours.ts (+test/market-hours.test.ts; verified behavior by running the module under tsx), history.ts + history-cache.ts, benchmark.ts + cash-flows isoDate, yahoo-finance.ts, alpaca.ts getEquityQuotes/fillMissingQuotesWithClose, policy.ts staleness/entry-drift gat…
- **UX copy** (22 findings): Read docs/FLEET-UI-COPY.md in full; STATUS.md top; the three prior reviews' copy sections. Web: nav.tsx, chrome.tsx (account switcher/run-state/blockers), approvals/page.tsx, approval-card.tsx (parts), lib/derive.ts, lib/labels.ts, lib/format.ts, settings/help.tsx, settings/brokers.tsx (connect sheets), settings/page.tsx TOC + cards, consent-gate.tsx, guardrails/field-defs.ts + page.tsx universe group, macro/indicators.ts (grep), assistant/chat.tsx + draft-card.tsx + models + app/ui/llm-model-catalog.ts, ui/drilldown-data/sections, scan/columns + smart-money, orders/page.tsx (grep), error.tsx…
- **QA / test strategy** (18 findings): Read (read-only, /Users/jay/apps/trading-monet @ c55c2e642): test/ structure (608 vitest files + 4 tsx, ~146.7k LOC, ~7,010 tests per STATUS), vitest.config.ts, test/global-setup.ts, playwright.config.ts + the single e2e spec, all 14 .github/workflows (ci/e2e/ios-build/ios-ship/security/codex-autofix), scripts/land.sh, scripts/ios-ship-testflight.sh, eslint.config.mjs (ran eslint read-only on app/console and src+app/api+app/admin under node@24 to count warnings), ios/project.yml test target, all 15 ios/SocraticTradeTests files, MobileStore/MobileModels/MobileAPIClient, STATUS.md top ~70 lines…
- **Ops / observability / admin** (20 findings): Read in full: app/api/health/route.ts, app/api/ready/route.ts (top), app/admin/layout.tsx, app/admin/page.tsx, app/api/admin/backup-status, r2-usage, server-knobs, connections-health, emit-test, trigger-test routes, server-metrics route (lines 1-500), sentry.*.config.ts, instrumentation*.ts, src/lib/observability.ts (top), db-health.ts alert paths (570-843), notify.ts (300-643), scheduler.ts tick/heartbeat/boot-interlock (293-500), broker-health.ts (150-300), trading-liveness.ts (1-80), r2-usage kill-switch, r2-cold-snapshot header, coolify-prod-start.sh markers/log tee, Dockerfile HEALTHCHEC…

### Related Report-Only Audits (Cursor, 2026-08-17 — read alongside this)

Cursor landed a parallel set of report-only audits the day before this review; overlap is expected and intentional (two independent readers).  Merged on `main`: `docs/audits/2026-08-17-blind-spots.md` (#2810 — legal/regulatory F-LEG-1..7, product-strategy, a11y F-A11Y-1..3, i18n, DX F-DX-1..3), `2026-08-17-purchases-stripe-storekit.md` (#2809), `2026-08-17-cross-app-coordination.md`, `2026-08-18-pinecone-store-vs-condense.md` (#2811).  Open PRs at time of writing: #2803 (RAG ingest/retrieval/learning), #2804 (web / mobile-web / iOS parity), #2805 (brokers + data-cascade reliability), #2806 (security + reliability), #2807 (architecture + backend), #2808 (trading-outcomes validation).  Where this review and those audits agree (e.g. blind-spots F-A11Y-2 "no skip link" = `a11y-05`; F-DX-2 "required CI never runs Playwright or Swift tests" = `qa-01`/`qa-02`/`qa-06`; F-A11Y-3 iOS UIKit chrome ignores Dynamic Type), treat that as two-reader confirmation.

## 11. Suggested Sequencing

Three tranches, sized for a solo owner + agents.  Ids are the work items; effort is the panel's S/M/L.

### Tranche 1 — This Week (money, truth, and the things that page nobody)
- Money path: `tsx-01` (S), `tsx-02` (M), `tsx-08` (S), `tsx-04` (S), `tsx-09` (S), `api-02`/`tsx-03` (S — one change in `mobile-api.ts runCommand`).
- P&L truth: `perf-01`/`berel-02` (S — drop the ASC LIMIT), `perf-02` (S), `perf-03` (S), `perf-07` (S), `perf-11` (M).
- LLM: `llm-01` (S — add `exitPlan` to `BULL_PROPOSAL_REQUIRED_KEYS` + schema invariant test), `llm-02` (S), `llm-04` (S), `llm-06` (S), `llm-10` (S).
- Data freshness: `mdi-01` (S), `mdi-04` (S), `mdi-05` (S), `mdi-03` (M).
- Security: `sec-01` (S — mirror the `ENCRYPTION_KEY` boot guard), `sec-06` (S), LIVE-20 (S).
- Structure: LIVE-01 root cause (`server-only` on `db.ts`, pure venue helper, dev bundler = prod) (S), `qa-03` (S).
- iOS: `ios-engineering:ios-01` (S), `ios-hig-ux:ios-01` (M), `api-01` (S), `api-03` (S), LIVE-15 (S), `qa-01` (S — `xcodebuild test` in `ios-build.yml`).
- Ops: `sre-02` (M — push on scheduler stale / liveness degraded / low credit), `sre-03` (M).

### Tranche 2 — Next (reliability, gates, and the daily-use surfaces)
- Backend: `berel-01` (M — indexable FTS identity), `berel-03` (M — broker deadlines), `berel-04` (M), `berel-08` (S), `dweb-02` perf / `tsx-05` / `berel-09` (M — stop walking the whole order history), `mdi-02` (M).
- Gates: `qa-02` (M — Swift build in the required gate), `qa-04` (M — snapshot contract fixture), `qa-05` (M — network guard), `qa-06`, `qa-07`, `qa-11`.
- Web console: `dweb-01` (M), `dweb-02` (S), `a11y-01` (S), `a11y-02` (M), `mweb-01` (S), `mweb-03`/`mweb-04` (S), `dweb-05` (M — Decisions in nav / one proposal lifecycle), LIVE-02, LIVE-05, LIVE-13.
- iOS: `ios-engineering:ios-03` (M), `api-07` (M), `ios-05` (S), `ios-10` eng (S — PrivacyInfo), `ios-hig-ux:ios-02` (M), `ios-10` (positions P&L, M), LIVE-14, LIVE-16, `copy-04` (S).
- Ops: `sre-01` (M), `sre-05`, `sre-11` (S), `sre-12` (S), `sre-13` (S), `sre-14` (M — one restore drill).
- Copy: `copy-03` (S), `copy-05`/`copy-07` (M), `copy-11` (S), `copy-16` (S), LIVE-03/LIVE-04 (S).

### Tranche 3 — Later (structure, polish, and owner decisions)
- Owner decisions to make explicitly: LIVE-17 (Autopilot typed-phrase ritual has no off-switch), `tsx-11` (hard-coded 5% headroom gate), `ios-08` (destructive-role alert on brokerage switch), `sec-04` (session revocation model), the "reject while stopped" block (LIVE-05).
- Architecture: `berel-07` (RAG work off the serving thread), `perf-14` (wire broker-authoritative equity/cash ledger), `llm-07` (Red Team content compaction), `llm-16` (store OpenRouter `usage.cost`), `api-11` (one error envelope + zod), `dweb-03` perf (snapshot payload diet), `dweb-11` perf (kill the HTML→JS→API waterfall), `sec-02` (origin lockdown), `sec-03` (next-auth upgrade).
- Public site: `dpub-01`…`dpub-06`.  Mobile-web polish: `mweb-05`…`mweb-15`.  Copy sweep: `copy-01`/`copy-02` (two-space enforcement helper + lint), `copy-08`.  All P3 rows in §5.


---

# Part II — Adversarial Re-Verification, Gap Coverage, And The Deduped Fix Plan (2026-08-19)

Part I ended with an honest weakness: the first verification round confirmed 336 of 336 findings and refuted none, which is evidence of leniency rather than of correctness — and several lanes admitted they had only grepped surfaces they should have read.  Part II closes both gaps.  A second round of skeptics was told to **refute** every P1, five new lanes read the surfaces the first pass skipped (each with its own skeptic), and one agent deduped the whole register into work items with a single root cause each.  Corrections below **supersede Part I**, including headlines I wrote in `STATUS.md` and in the Slack closeout.

## A. Second-Round Verdicts On Every P1

All 40 Part I P1 findings were re-attacked by a skeptic instructed to disbelieve them and to require a concrete repro chain (file:line per step) before upholding.  Result: **UPHELD** 27, **UPHELD_NARROWED** 11, **ALREADY_FIXED** 2.  10 were downgraded below P1.  Money-path, accounting, backend and security batches ran on the frontier tier; the rest on the mid tier.

### Corrections That Change What Part I Said

#### **[api-contract-web-ios:api-03]** iOS Scan times out at 25s while the server scan budget is now 35s (#2830 today) — slow scans surface as "Check your connection"

- **Verdict** ALREADY_FIXED · confidence high
- **What Part I got wrong** The finding accurately describes a real defect that existed in the code snapshot it was written against (and STATUS.md's 2026-08-19 entries confirm testers on the currently-SHIPPED TestFlight binary 1.0.68 are still hitting it in the field, since that binary predates PR #2850). But on the branch as currently reviewed, the client-side constant is already fixed. This should be reclassified from a code defect to a deployment/rollout gap (old TF build vs new main).
- **What is actually true** ios/SocraticTrade/MobileAPIClient.swift:132-137 on the reviewed branch's current HEAD reads timeoutInterval = 50 with a comment stating the 35s server budget explicitly ('Client wait is longer so a late seed/200 is not dropped as a dead table'). `git show 1ca5073b7 -- ios/SocraticTrade/MobileAPIClient.swift` shows this was changed from 25->50 (comment from '20s' to '35s') as part of merged PR #2850. `git merge-base --is-ancestor 1ca5073b7 HEAD` confirms this commit is an ancestor of the reviewed branch's HEAD (5d453afef). app/api/scan/route.ts:22 INTERACTIVE_SCAN_BUDGET_MS=35_000 is unchanged, so the client now waits 50s -- 15s of slack beyond the server budget, exceeding even the finding's own recommended 45s fix.
- **Blast radius** None on the reviewed code. On the older shipped TF 1.0.68 binary still in testers' hands, the blast radius remains as described: a legitimately slow (25-35s) scan aborts client-side and shows a generic network error instead of results -- a UX annoyance, not a money-safety issue (no order is placed from a timed-out scan).

#### **[ux-copy-editor:copy-04]** iOS tells the user to edit the universe "on the Strategy page" — the app has no Strategy screen and on the web the universe edito…

- **Verdict** ALREADY_FIXED · confidence high
- **What Part I got wrong** Fully fixed by PR #2857, commit 437f0a4e4 ('iOS Guardrails names + fold #2849 Desk subtitle'), which is an ANCESTOR of this branch's tip (git log: 5d453afef docs-review commit is a child of 437f0a4e4). Verified current ios/SocraticTrade/HomeView.swift:94/109 no longer says 'Strategy page' — the checklist detail now reads `DeskCopy.universeNeedsIndex` = 'Choose at least one base index (e.g. S&P 500) or add extra symbols so the strategy has names to scan.' and the follow-up hint reads `DeskCopy.universeRefreshAfterGuardrails` = 'Add an index or extra symbols on Guardrails, then pull to refresh here.' (DeskModels.swift:668-673). Verified GuardrailsView.swift no longer contains 'Universe edits stay on the Strategy page' (grep for 'Strategy page' across ios/ and app/ returns only two Swift TEST assertions that it's ABSENT: ios/SocraticTradeTests/DeskModelsTests.swift:283 `XCTAssertFalse(Desk…
- **What is actually true** N/A — fixed on this branch's own history before the review-docs commit was added.
- **Blast radius** None currently reachable — the copy now correctly names Guardrails as the destination, matching the real web location at app/console/guardrails/page.tsx:425, and two regression tests pin the fix against recurrence.

#### **[backend-reliability:berel-02]** listFillEvents returns the OLDEST 500 fills (ORDER BY filled_at ASC LIMIT 500) so P&L, open lots, tax, model-stats and dashboard…

- **Verdict** UPHELD_NARROWED → **P2** · confidence high
- **What Part I got wrong** Two claims do not hold up. (a) 'A 15-minute-cadence account reaches 500 fills in weeks' — the shipped cadence is defaults.ts:165 runCadenceMinutes: 60 with defaults.ts:138 maxProposalsPerRun: 3 and defaults.ts:137 maxDailyOrders: 10, so the ceiling is 10 fills/day and the floor for 500 is ~50 trading days on ONE book; there is no 15-minute cadence in the defaults. Whether any prod (account, source) has actually crossed 500 is not answerable from this tree — both local DBs have 0 fill_events rows. (b) The blast list is too wide: it does not reach the order path. Daily order/notional caps read trade_proposals unbounded (db-execution.ts:58-62), the PDT gate reads fill_events date-bounded with no LIMIT (db-execution.ts:155), and pending-reconciliation reads have no LIMIT (db-fills.ts:356-372).
- **What is actually true** Same defect as perf-01, cited correctly. src/lib/db-fills.ts:332 `ORDER BY filled_at ASC LIMIT ?` returns the OLDEST N; I confirmed all 16 non-test call sites pass 500 except strategy-tuning.ts:418 which passes 30 (rg over src/ and app/: robinhood-pnl-crosscheck.ts:60, tax.ts:337, experience-memory.ts:263 and 350 (5000), performance.ts:460/461/468/492/493, dashboard.ts:675/676, outcome-engine.ts:964, strategy-tuning.ts:335/409/410/417/418, strategy.ts:824/825, app/api/llm-usage/model-stats/route.ts:58). The aggravation this finding names is the part I can prove bites on current data: strategy-tuning.ts:415-419 picks `listFillEvents(accountNumber, source, 30, userId)` (30 OLDEST) and strategy-tuning.ts:603 ships `fills.slice(0, 20)` to the model as `recentFills`; strategy-tuning.ts:420-421 + performance.ts:1354 gate the whole weight-shift path behind >=20 closed lots (>=40 fills), so the…
- **Blast radius** Identical to perf-01 (same query). Proven-live portion: the paid strategy-tuning LLM review argues from the 20 oldest fills labelled 'recentFills' (strategy-tuning.ts:603) and derives `currentRegimeFromLots` from a truncated lot replay (strategy-tuning.ts:334-342), so weight nudges that steer which names get proposed are grounded in the account's first trades. Latent portion (>500 rows on one book): Realized P&L / win rate / avg return / Tax open lots / model-stats / iOS fill history freeze sil…

#### **[backend-reliability:berel-03]** Broker calls have no HTTP timeout (Alpaca SDK axios default 0; Tradier fetch has no signal) and the scheduler's stop-monitor / st…

- **Verdict** UPHELD_NARROWED · confidence medium
- **What Part I got wrong** Two claims in the finding (and one in the first-round verify note) are wrong and I cut them. (1) The verify note's 'getPositions/getEquityOrders/getEquityQuotes/place paths are unbounded' is false for two of them: `getEquityPositions` (src/lib/alpaca.ts:481-490) and `getEquityOrders` (src/lib/alpaca.ts:511-526) BOTH wrap the SDK call in `awaitWithFirstCallRetry` with a 16 s+8 s budget whose `onFinalTimeout` throws (src/lib/inflight-deadline.ts:19-20, :100-140), as does `readAccount` (:293-305). The genuinely unbounded set is quotes, place, cancel, nested-order GET and option positions. (2) The '~15 min on Linux TCP retransmit' figure applies only to the Alpaca axios/Node-http path; the Tradier `fetch` at tradier.ts:339 runs on undici, whose default headersTimeout/bodyTimeout are 300 s, so that hang is bounded at ~5 minutes, not 15. Narrowed accordingly: this is 'the protective lanes go…
- **What is actually true** Sequence, all on current code: 1. The Alpaca SDK issues every request as `axios(req)` with no `timeout` key (node_modules/@alpacahq/alpaca-trade-api/dist/api.js:4-22 and :24-42) and axios' default timeout is 0, and src/lib/alpaca.ts:247 constructs `new Alpaca(options)` without touching it — so a half-open socket never rejects. 2. `trackHealth` (src/lib/alpaca.ts:256-286) retries only THROWN transient errors; a hang is not a throw, so it never retries or aborts. 3. Of the Alpaca methods, `getLatestQuotes` (src/lib/alpaca.ts:563), `createOrder` (:633, :737, :814), `cancelOrder` (:862, :904), `sendRequest` (:884) and `getOptionPositions` (:840) carry NO deadline at the adapter. 4. The scheduler's synthetic-stop-monitor lane adds its key at scheduler.ts:919 and clears it only in `.finally` at :931 — no `withDeadline`. The stale-limit-scan lane is the same shape (:884 add, :905 `.finally`).…
- **Blast radius** Worst realistic outcome: one hung Alpaca socket inside `runSyntheticStopMonitor` (synthetic-stops.ts:688 quotes, or :943 the exit order itself) latches `stopMonitorInFlight` for that account forever, so the account's synthetic trailing stops stop being evaluated on every subsequent 60 s tick with no error surfaced — a position whose stop should have exited instead rides the full drawdown until the owner notices manually. Because the same hang holds the account-mutation lease, stale-exit remedia…

#### **[frontend-performance:dweb-02]** getEquityOrders pulls the ENTIRE Alpaca order history (up to 50 pages x 500) on every dashboard snapshot and 20 other call sites,…

- **Verdict** UPHELD_NARROWED → **P2** · confidence high
- **What Part I got wrong** I cut three things. (1) The headline '50 pages x 500' is conditional, not the normal case: the walk breaks at src/lib/alpaca.ts:541 `if (page.length < PAGE || added === 0 || !oldest || oldest === until) break;`, so an account with <=500 lifetime order records makes exactly ONE call. Nothing in the review, STATUS.md, or ops-snapshot gives a receipt for this account's order count, so 'N sequential round-trips per snapshot' is unproven. (2) The causal link to the `getPortfolioBundle 8000+7000ms` timeouts is a hypothesis, not evidence — STATUS.md attributes that window to a broker-wide slow-first-read profile (500/500 ok, avg ~3.1 s, max 14.4 s across ALL alpaca-broker calls) and to `ftsMirrorSlice` pinning the loop 6-12 s, neither of which is order pagination. (3) 'ships all of it to every client each poll' overstates by the cache: it is ~1 fetch per 15 s per (user, account), single-flight…
- **What is actually true** What is provable, step by step: 1. src/lib/alpaca.ts:501-543 `getEquityOrders` always asks for `status: "all"`, `limit: 500` — MCP arg at :502, REST at :513-518 — i.e. the full lifetime order history, never scoped to open or to a date window. 2. src/lib/dashboard.ts:485-501 calls it inside the `Promise.all([getPortfolio, getEquityPositions, getEquityOrders])` bundle on every dashboard snapshot build. 3. That build is reached from a 15 s client poll (app/console/lib/useConsoleData.tsx:23 `POLL_MS = 15_000`) through a ~10 s single-flight TTL cache (src/lib/dashboard-snapshot-cache.ts:12, :51-70), so roughly one full-history fetch per 15 s per (user, account) with a console tab open. 4. The scheduler adds two more per 60 s tick per account (src/lib/scheduler.ts:861 drain, :888 stale-limit-scan), and 20 call sites total exist across order-replacement, synthetic-stops, strategy-execution, or…
- **Blast radius** No order can be placed or mis-placed by this — it is a read. Worst realistic outcome: when the 16 s+8 s bundle budget does fire, dashboard.ts:496 returns `[undefined, [], []]`, so Portfolio, Positions AND Orders all render empty at once on the console and the owner can read 'no resting stop orders' when stops in fact exist — a wrong-belief risk on a protective-order screen, not a wrong-trade risk. Steady-state cost is one `status:"all"` limit-500 Alpaca query per 15 s per open console tab plus…

#### **[llm-agent-architecture:llm-03]** Delta-only macro pruning strips unchanged FRED fields from every run after the first — the stateless model never sees values the…

- **Verdict** UPHELD_NARROWED → **P2** · confidence medium
- **What Part I got wrong** This is real and unaddressed, but it is a prompt-quality/context-completeness defect, not a deterministic execution bug -- it never causes a wrong order, duplicate order, or cross-account leak on its own; it only degrades the INPUT quality of one LLM judgment call among many. The codebase already treats several other missing/partial-context conditions as normal, fail-open, non-blocking behavior by design (upcomingEconomicEvents, marketSignals, etc. at strategy.ts:5000-5010 are explicitly 'failure-tolerant -- never blocks a run'), so this class of defect is consistent with a P2, not the P1 tier reserved elsewhere in this batch for cross-account leaks and total-failure schema bugs. It is also worth noting `pruneMacro` was original, owner-authored design (commit 41ede025a, 2026-06-16, explicit intent: 'delta-only macro' token savings) -- the flaw is a foreseeable-but-unaddressed side effec…
- **What is actually true** macro.ts:586-616 `pruneMacro` keeps only vix/fedFundsRate/dgs10Treasury/asOf (MACRO_ALWAYS_KEEP, line 587) plus any field whose value differs from `previous[key]`; every other field is pushed onto `omitted` (name only, no value). strategy.ts:4980-4988 calls this with `previousMacro = getInternalSetting(macroCacheKey)` where `macroCacheKey = \`last_macro_sent:${input.userId}\`` (userId-only, not account-scoped) and immediately calls `setInternalSetting(macroCacheKey, macro)` (line 4984) BEFORE the LLM request is built or sent (the request body isn't assembled until ~5880+), so even a failed/errored LLM call still marks the full macro snapshot as 'sent'. `macroeconomicData` (4985-4988) folds the survivors plus `unchangedSinceLastRun: macroOmitted` (bare names) into userContent, which is JSON.stringify'd into the actual Bull prompt and also spread verbatim into `adversaryContext` for Red T…
- **Blast radius** On any run after the first same-calendar-day run for a user (multiplied further since the cache key is user-scoped and shared across ALL that user's accounts), the Bull AND Red Team prompts omit the actual numeric values of most FRED macro fields (hyCreditSpread, dgs2Treasury, CPI, unemployment, usdIndex, wtiOil, corePCE) even though the system prompt explicitly instructs reading some of them for recession-risk judgment. Worst realistic outcome: a real risk-adding position gets opened/sized by…

#### **[llm-agent-architecture:llm-04]** Daily LLM learning review auto-approves strategy-directive pending rows into the strategy prompt as TRUSTED owner text (contract…

- **Verdict** UPHELD_NARROWED → **P2** · confidence medium
- **What Part I got wrong** The register's framing treats this purely as a contract violation/bug, but git history shows this is a deliberate, owner-made design choice, not an accidental gap: commit 3be0c041d (2026-07-09, the feature's introduction) originally shipped 'decide' as 'owner opt-in' with the DEFAULT mode being 'annotate' (audit-only, nothing mutated). Commit 6f1aaf87d (2026-07-10, one day later, titled 'decide-default', authored by the owner/MONET seat) explicitly flipped the default to 'decide'. This is exactly the CLAUDE.md-endorsed pattern ('any rule the app enforces gets a user-controlled off-switch... If the owner set it, follow their intent and let them change or override it') -- the owner already has, and has already exercised, full control over this exact toggle. The parent feature is ALSO off by default (`learningReviewEnabled: false`), a double opt-in. What genuinely survives as a defect: (1)…
- **What is actually true** src/lib/chat/coach-learning.ts:8-9 documents 'Explicit strategy directives queue as strategy-directive pending (human approval required before they touch the strategy prompt).' learning-review.ts's `applyLearningReviewVerdicts` (640-663) calls `applyApprovedPending(pending, nowIso)` on a 'keep' verdict for ANY `learned_context_pending` row with no `riskTier` exclusion (confirmed by grep: no 'strategy-directive' branch/filter anywhere in learning-review.ts). `applyApprovedPending` (learned-context/store.ts:397-405) for `riskTier === 'strategy-directive'` calls `getStrategyPrompt(pending.userId)` + `mergeStrategyDirectiveBlock` + `setStrategyPrompt` -- the identical mutation path the human-approve UI route uses, with no distinguishing tag or reduced trust marker on the resulting text. defaults.ts:89-90 confirms `learningReviewEnabled: false` (feature is OFF until the owner explicitly turn…
- **Blast radius** Only bites when the owner has explicitly enabled `learningReviewEnabled` (off by default) and left `learningReviewMode` at its 'decide' default (or chosen it deliberately), AND a strategy-directive candidate is sitting in the pending queue with corrupted or subtly-adversarial content (most plausible vector: content ingested from a fetched URL via coach-learning.ts's SSRF-safe fetch, or crafted phrasing in a chat message). If all three hold, the once-daily reviewer LLM -- not a human -- can prom…

#### **[ops-observability-admin:sre-01]** No at-a-glance 'is the trader alive' panel on web/admin/iOS; run-state chip is intent, not liveness

- **Verdict** UPHELD_NARROWED → **P2** · confidence high
- **What Part I got wrong** The headline "No at-a-glance 'is the trader alive' panel on web/admin/iOS" is wrong for two of the three surfaces, and I cut it. (a) The console home renders run liveness immediately beside the StateChip: app/console/page.tsx:154-172 prints "Last run {status} · {Ago finishedAt}" in `--con-neg` tone with the persisted failure cause when the run failed (describeLastRun, app/console/lib/last-run.ts:50-60), line 179-186 adds an Activity deep link on failure and "Next {timeUntil} · {cadence}m cadence". A three-day-old or failed last run is visible on the default page without scrolling. (b) iOS Home's third card is ScheduleOverviewCard (ios/SocraticTrade/HomeView.swift:27 and 659-675): Last Run / Next Run / Cadence, where lastRunAt is the real strategy-run start (src/lib/scheduler.ts:825, getLastStrategyRunStartedAt) — not intent. What is genuinely missing is the /admin consolidation, the chi…
- **What is actually true** What survives, verified: 1. `app/admin/page.tsx:99-106` fetches exactly six endpoints (connections-health, llm-usage, rag-coverage, server-metrics, chat-history, r2-usage). Neither /api/health nor /api/admin/backup-status is among them. 2. The one server endpoint it does call for infrastructure, `app/api/admin/server-metrics/route.ts:161,384`, returns `os.uptime()` / Coolify host metadata only — I grepped it for scheduler/heartbeat/lastTick/releaseSha and found nothing. 3. The signals exist but land only in probes: `checks.schedulerStale` (app/api/health/route.ts:82-88), `checks.tradingLiveness` (route.ts:128-140), and the same two in app/api/ready/route.ts. A repo-wide grep for consecutiveFailedRuns / lastCompletedRunAt / tradingLiveness / schedulerLastTick over app/ and ios/ returns only those two route files — no UI consumer anywhere. 4. `deriveStateInfo` (app/console/lib/derive.ts:1…
- **Blast radius** No money moves and no order is affected — this is purely how fast the operator notices a stopped engine. On both clients the owner already sees last-run age and a red failure line on the default screen, so the realistic cost is that diagnosing WHY (dead tick vs. failing runs vs. paused replication) requires opening /api/health or the token-gated ops snapshot instead of one admin card, and that a five-run failure streak reads "Running · Autopilot" in the chip while the adjacent line reads "Last…

#### **[ops-observability-admin:sre-02]** schedulerStale, tradingLivenessDegraded and OpenRouter low-balance never trigger an in-app push; only external keyword monitors c…

- **Verdict** UPHELD_NARROWED → **P2** · confidence high
- **What Part I got wrong** I cut the third of the finding that is (c), and one supporting claim. (c) OpenRouter low balance is an explicit owner decision, not a defect: src/lib/openrouter-credits.ts:1-13 states "Per owner: no in-app fallback and no in-app alerting — instead expose the credit balance on the public /api/health probe so an EXTERNAL watchdog (Uptime Robot) can alert when it drops, independently of the app itself." The recommendation to fire `budget_alert` when credits.ok flips false directly contradicts that. It is also not a hypothetical layer: docs/EFFORT-LOG.md:493 records "Uptime Robot monitors created and alert successfully" as DEPLOYED / PRODUCTION VERIFIED 2026-07-18, and the 2026-08-14 entry references "the paired credits-low keyword monitor" firing. Second cut: "the 180 min default is far slacker than a 15-30 min run cadence" is wrong at the shipped default — src/lib/defaults.ts:164 sets run…
- **What is actually true** The (a)+(b) half is real. Sequence: 1. The scheduler wedges or every run fails. app/api/health/route.ts:88 sets `checks.schedulerStale = true` at >5 min heartbeat age; route.ts:128-140 sets `checks.tradingLiveness.degraded`. 2. Neither flips the response code: `ok` starts true at route.ts:67 and is set false at only two sites — route.ts:78 (DB read failure) and route.ts:279 (hard-stopped dependency). The final line is `Response.json({ ok, checks }, { status: ok ? 200 : 503 })`, so a stalled scheduler answers HTTP 200 and can only be caught by keyword-matching the JSON body. 3. Nothing pushes it: `grep -c 'sendNotification|notify' src/lib/trading-liveness.ts src/lib/openrouter-credits.ts` returns zero matches in both files, and src/lib/scheduler.ts imports sendNotification at line 25 with exactly one call site, line 372 (autonomy_halted_on_boot). 4. The in-code external fallback is off b…
- **Blast radius** No wrong order is ever placed by this; the exposure is the engine going quiet while the owner is away from the app. Worst realistic outcome: an autopilot account stops producing runs and nothing reaches the owner's phone, so for the 3-hour stale threshold and then indefinitely (nothing pages on the flag), there are no new entries, no strategy-driven exits, and no stale-exit remediation. Open positions keep only whatever broker-held bracket legs were attached at entry, so the loss is opportunity…

#### **[ops-observability-admin:sre-03]** Backup status can lie: litestream kill-switch markers are invisible to /api/health and Backup Status, and the R2 'writes paused'…

- **Verdict** UPHELD_NARROWED → **P2** · confidence medium
- **What Part I got wrong** The title "Backup status can lie" needs narrowing: it never lies in the dangerous direction. (1) The generic marker does NOT produce a false all-clear — app/api/health/route.ts:445 sets `checks.storageDegraded = true` and fires `alertStorageWarning("litestream_state_unreadable", ...)`, so the owner is alerted; the defect is that the alert misnames a deliberate owner pause as an unreadable state, which is alert-fatigue and wrong copy, not silence. (2) The R2 card's error is a FALSE ALARM (claims paused while B2 replicates every 60s), i.e. pessimistic, not reassuring. Neither path can make a broken backup look healthy. Also worth noting the degraded flag does not 503 (route.ts:67/78/279 are the only `ok` writes), so this cannot flip the Docker HEALTHCHECK — I checked that specifically because it would have raised severity. Severity accordingly drops to P2: operator confusion and a stale b…
- **What is actually true** Both code gaps are real; the second one has a documented live trigger. R2 card (the actual false statement): 1. `app/api/admin/r2-usage/route.ts:29` returns `replicationDisabled: isR2ReplicationDisabled(cfg)`, and that helper (src/lib/r2-usage.ts:571-577) is a bare `existsSync(cfg.disableMarkerPath)` — no endpoint check. The WRITE site is gated (`isLitestreamReplicaCloudflareR2()` at src/lib/r2-usage.ts:766), proving the authors knew the gate was needed and applied it in only one of the two places. 2. `scripts/coolify-prod-start.sh:200-212` ignores that marker whenever AWS_S3_ENDPOINT is not Cloudflare R2, so litestream DOES start; litestream.coolify.yml:1-8 confirms the active replica is Backblaze B2 with sync-interval 60s. 3. `app/admin/page.tsx:497-503` renders, on `r2?.replicationDisabled` alone, "Socratic Trade litestream→R2 writes are **paused** (free-tier kill-switch)". 4. The ma…
- **Blast radius** No money and no data at risk from the bug itself — B2 replication is running the whole time the card says it is paused. Realistic harm is decision quality during an incident: the Operator Overview's only backup statement is wrong today (a leftover Aug-4 marker on the prod volume, per the B2 rollout's own Next-steps item), so the owner either wastes time "resuming" replication that never stopped, or learns to ignore that banner and misses the day it is true. Second-order: if the owner ever uses…

#### **[performance-accounting:perf-01]** Fill ledger keeps the OLDEST 500 fills per source; newest fills silently vanish from every P&L figure

- **Verdict** UPHELD_NARROWED → **P2** · confidence high
- **What Part I got wrong** Three overstatements. (a) 'newest fills silently vanish from every P&L figure' is unconditional in the title but conditional in fact — it needs >500 rows for one (account, source), and nothing in this tree establishes that: both local DBs (/Users/jay/Code/Socratic.Trade/data/app.db and /Users/jay/apps/trading-monet/data/app.db) have zero fill_events rows, and defaults.ts:137 caps maxDailyOrders at 10, so >=50 trading days of maxed-out activity are needed to reach 500 on one book. It is a latent defect, not a demonstrated live one. (b) It does NOT reach order safety, contrary to the 'cross-surface' framing: the daily order/notional caps read trade_proposals with no LIMIT (db-execution.ts:58-62, 92-96), the PDT day-trade gate uses a date-bounded fill_events query with no LIMIT (db-execution.ts:155), and broker reconciliation uses listPendingBrokerReconciliationFills (db-fills.ts:356-372,…
- **What is actually true** Code defect is certain. (1) src/lib/db-fills.ts:329-337 — `listFillEvents(accountNumber, source, limit=500, userId)` is `SELECT * FROM fill_events ... ORDER BY filled_at ASC LIMIT ?`, i.e. the OLDEST N rows, while the sibling `listPortfolioSnapshots` (db-fills.ts:221-243) was already fixed to the newest-DESC-subquery pattern with a comment naming this exact bug. (2) src/lib/db.ts:739-766 and 2331-2345 are the only DELETEs on fill_events (migration dedupe + account deletion) — there is no retention/pruning, so rows only accumulate. (3) Every accounting reader passes 500: dashboard.ts:675-676, performance.ts:460/461/468/492/493, tax.ts:337, strategy.ts:824-825, strategy-tuning.ts:335/409/410/417, outcome-engine.ts:964, experience-memory.ts:263, robinhood-pnl-crosscheck.ts:60, app/api/llm-usage/model-stats/route.ts:58. (4) So on fill #501 for one (account, source): calculatePnl (performanc…
- **Blast radius** Reporting + learning only. Once one book passes 500 fills: Results/iOS Realized P&L, win rate, avg return, the fills list, the Tax open-lot ledger and the thesis/regime/factor scorecards all freeze at the pre-#500 state and never move again, with no on-screen indication. The one concrete money leak is the wash-sale lockout (tax.ts:138-163 <- performance.ts:748): a loss exit booked after row #500 never locks the symbol, so the strategy can rebuy inside the 61-day window and forfeit the deduction…

#### **[qa-test-strategy:qa-05]** No global network guard in vitest; suite still reaches SEC/Yahoo/SiliconFlow

- **Verdict** UPHELD_NARROWED → **P2** · confidence high
- **What Part I got wrong** Downgraded P1 -> P2: verified both named live-network sinks are read-only informational calls — SiliconFlow/OpenRouter embeddings (src/lib/vector-db.ts:2440,2611) and SEC/Yahoo market-data fetchers — with no order-placement or fund-movement code path reachable from a test process. The underlying claim (no global fetch guard exists; CI hits live third-party hosts) is fully correct, but the worst realistic outcome is CI flakiness and small wasted third-party API quota, not corrupted trading data, a wrong order, or financial loss — a materially lower blast radius than qa-01 through qa-04, which touch order/proposal/ship-pipeline correctness on a real account.
- **What is actually true** 1) vitest.config.ts has no `setupFiles` entry at all — only `globalSetup: './test/global-setup.ts'` (confirmed by reading the full file), which is purely per-run TMPDIR/TMP/TEMP plumbing for temp SQLite DBs, not a network guard (confirmed by reading global-setup.ts in full). 2) Any test file that reaches src/lib/vector-db.ts (embeddings/rerank against https://api.siliconflow.cn or https://openrouter.ai, lines 2440/2611) or a market-data fetcher without a per-file mock makes a real outbound HTTP call. 3) STATUS.md:114 (current, from the 2026-08-18 RAG rollout) confirms verbatim: 'Full npm test still hits leftover SiliconFlow + SEC/Yahoo 404s (untouched).' 4) This produces nondeterministic timeouts/429s under CI's ubuntu-latest runner, consistent with maxWorkers being pinned to 1.
- **Blast radius** CI reliability/cost only: intermittent test failures and unnecessary calls to SiliconFlow/SEC/Yahoo endpoints during every `npm test` run; no money-path or user-facing-data-integrity impact is reachable from this gap.

#### **[trading-money-path:tsx-01]** Alpaca MCP placement falls back to REST after any MCP error and re-submits the same client_order_id; the duplicate 422 is then fi…

- **Verdict** UPHELD_NARROWED → **P3** · confidence high
- **What Part I got wrong** Two overstatements. (1) The title and the STATUS.md headline ('MCP-fallback duplicate orders') say duplicate orders; the code cannot produce a duplicate at Alpaca — reusing the identical client_order_id is precisely what makes Alpaca refuse the second submission. The harm is misclassification and lost bookkeeping, not a double order. (2) 'no stop plan' overstates it: the orphaned position is still protected, because reconcileBrokerProtectiveStops and the synthetic monitor enumerate positions from `gateway.getEquityPositions` (src/lib/synthetic-stops.ts:248-250) and pass them at :443, so a position the app never booked still gets a broker/synthetic stop armed. Only the per-position LLM stop PLAN (getStopPlans, filtered by live basis at synthetic-stops.ts:272-274) is missing. (3) Severity: P1 assumes a live path. There is none — no UI, and the built-in host allowlist makes the failure imp…
- **What is actually true** The code shape is exactly as described, but the MCP transport is unreachable from any product surface, so this is latent config-gated code, not a live money path. Code is real: src/lib/alpaca.ts:307-349 `callMcp` wraps the whole MCP round trip in one try/catch and calls `fallbackFn()` on ANY throw (AbortSignal.timeout(ALPACA_MCP_FETCH_MS=8s, inflight-deadline.ts:21), `Alpaca MCP HTTP <5xx>` at :328, `body.error` at :332, or a JSON parse failure). src/lib/alpaca.ts:766 sets `client_order_id: input.refId` on the MCP args and :640 sets the identical `client_order_id: input.refId` on the REST fallback, so the retry collides. src/lib/alpaca.ts:977 `formatAlpacaOrderError` prefixes `HTTP <status>`, and src/lib/strategy-execution.ts:1279 (`if (placeError instanceof OrderValidationError || /\bHTTP 4\d\d\b/i.test(message))`) plus src/lib/strategy.ts:3979 short-circuit that 422 to `updateProposal…
- **Blast radius** Zero today: production runs two Alpaca REST accounts (STATUS.md ops snapshot 2026-08-18 21:37:42Z), and no UI can create an alpaca-mcp account. If the owner ever stands up a self-hosted Alpaca MCP gateway and whitelists it via EGRESS_EXTRA_ALLOWED_HOSTS, each MCP timeout (8s) on an approval can leave one real, possibly filled entry — at this app's sizing typically a few hundred to a few thousand dollars of notional, e.g. 4 shares of a ~$470 name ≈ $1.9k — recorded as 'Rejected by broker': no fi…

### Upheld With A Proven Repro

These survived the attack; the repro column is the sequence that makes each one bite, and the blast radius is what it costs when it does.

| uid | title | blast radius (as proven) | conf |
|---|---|---|---|
| api-contract-web-ios:api-01 | iOS Guardrails stop-loss rows always show "—": FullPolicy reads stopLossPct at top level, server nests it under riskRul… | Every iPhone user, every time they open Guardrails, sees Stop Loss / Trailing Stop / Short Stop all showing an em-dash regardless of the actual configured value (e.g. a live 8% stop from DEFAULT_RISK_RULES). Display-only -- does not change actual stop/order b… | high |
| api-contract-web-ios:api-02 | proposal.approve command reports "succeeded" (green check on iOS) when the broker order was blocked / not placed / erro… | No wrong order is placed; the failure is a missed intended action reported as a completed one. Worst realistic case: a phone approval of a protective exit returns `busy` because the scheduled run holds the strategy lock (strategy-execution.ts:298), the phone… | high |
| backend-reliability:berel-01 | FTS5 idempotency DELETE/COUNT use equality on non-rowid columns = full-table scan per chunk; this, not tokenization, is… | No direct money loss — this is an event-loop pin, not an order path. Each mirrored chunk blocks the single Node process for one full scan of the FTS corpus (measured slope; ~300 ms/chunk at the live corpus size per fts-mirror-bound.ts:21). Money-adjacent cons… | high |
| backend-reliability:berel-04 | filing-body-ingest, persistLocalComplete and document-summarizer still mirror whole documents into FTS synchronously wi… | No direct money loss; this is the loop-pin class that produced the 08-13/08-14/08-18 incidents. Worst realistic outcome: one previously-unseen 10-K on the daily filing-body pass blocks the single Node process for ~4-5 minutes with zero yield (933 chunks x ~30… | medium |
| desktop-web-ux:dweb-01 | Home proposal rows: dead/wrong Approve button, wrong status tones, auto-firing live confirm | (a) A genuinely FAILED order attempt renders with a green success-colored chip captioned e.g. 'Buy Failed' -- real risk of misreading a failed trade as executed. (b) Tapping Approve on any blocked latest-run row always 404s -- a dead control (no wrong order r… | high |
| frontend-performance:dweb-01 | /console/connections client bundle ships the server DB layer (SQL schema, migrations, key-store logic): 1.05 MB gz vs ~… | Every /console/connections page load (and, per the finding's architecture, background App-Router prefetch from console-rail links on other pages) ships internal SQL schema strings, query text, encryption-key references, and env-var names to the browser -- an… | high |
| ios-engineering:ios-01 | Sign-out / expiry / account deletion never clears the UserDefaults snapshot cache — next launch boots the previous acco… | After sign-out or account deletion, a subsequent offline (or delayed) cold launch shows the PRIOR account's live portfolio/orders/positions as if current -- real risk of a user acting on stale account state, or a deleted account's financial data persisting re… | high |
| ios-hig-ux:ios-01 | Guardrail/account edits inside Account & Settings fail silently — store.error is never rendered in the sheet, and the s… | Owner adjusts a live-account guardrail (raise/lower a notional or %NAV cap, toggle Ask-First<->Autopilot) from Account & Settings; on a stale-cap race or a server-side command failure, they get zero visible feedback and may believe the change applied when it… | high |
| llm-agent-architecture:llm-01 | Bull strict JSON schema lists `exitPlan` in properties but not in `required` — OpenAI-family Green seats 400 since 2026… | Every strategy run whose resolved Green (or Red) endpoint is an OpenAI-family model (gpt-5.6-terra/sol/luna, gpt-5.4-mini/nano, gpt-4o, or any user-configured OpenAI-native key) gets an automatic HTTP 400 and fails over to the next configured model/seat -- ex… | high |
| llm-agent-architecture:llm-02 | Red Team reviewer resolves the ACTIVE console account, not the account under review (venue/short capability, execution… | With two-plus accounts connected under one user and scheduled concurrently (the exact live pattern this session's own STATUS.md shows -- Paper `PA33IDTHMFK9` and Roth both scheduled today) the Red Team veto on a risk-adding open can (a) auto-reject a legitima… | high |
| market-data-integrity:mdi-01 | Weekend TTL extension fires during Friday's live session: screener/OHLC/enrichment caches freeze until Monday | Every Friday (and every day before a full-close holiday), after the first ~5-minute screener refresh of the day, Market Scan ranking/momentum/breadth and 6h enrichment freeze until Monday's open (or the next trading day for a holiday) -- confirmed live and cu… | high |
| market-data-integrity:mdi-02 | Daily-bar cache served as 'fresh' for 3 calendar days: series lags up to two sessions and today's partial bar is persis… | Twice a week (Tue+Wed after any Monday close), daily-bar-derived technical indicators feeding strategy.ts's real proposal generation, the SPY benchmark comparison, and the /api/history chart silently run on a 2-session-stale series with no staleness flag anyw… | high |
| market-data-integrity:mdi-03 | Cascade stamps cache-served provider values with a 'now' fetchedAt/asOf; nasdaq-quote price/change/volume are cached 6h… | On every enrichment cascade run with Alpaca connected (the default broker) -- i.e. routinely, not an edge case -- the ADV/market-impact guardrail on real opening orders is computed against a volume figure that can be up to 6 hours stale while always LOOKING l… | high |
| market-data-integrity:mdi-04 | asOf fabricated from the wall clock (Yahoo quote-only fallback, ROIC profile price, CBOE, grouped VWAP) defeats the quo… | For any opening BUY/SHORT on a symbol priced only via the quote-only Yahoo fallback or the ROIC last-resort tier (routine for names outside the Nasdaq screener's coverage), a genuinely stale/delayed print can be submitted as a live MARKET order instead of the… | high |
| mobile-web-ux:mweb-01 | Phone chrome bar over-budget since run-state label became "Stop Agent"/"Resume Agent" — account scope squeezed to ~0-15… | On common ~360-390px phone viewports, the account-scope selector -- the only control naming which brokerage account (paper/live, broker, last-4) is currently loaded -- is squeezed toward unreadability. This is a legibility/glanceability risk for a multi-accou… | medium |
| performance-accounting:perf-02 | Home Day P&L is zeroed on ordinary trading days: flow inference runs with NO fills and reads market P&L as a deposit/wi… | The Home headline. On any day the strategy trades net-directionally (net buys or net sells) and the book moves at least 0.5% of equity and at least 25% of the cash-vs-position swap, Day P&L on /console — desktop and phone widths — prints exactly $0.00 (0.00%)… | high |
| performance-accounting:perf-03 | Short/cover cash sign is inverted in flow inference → phantom deposits/withdrawals on every short trade | The 'Versus the market' scoreboard and the Capital regimes table on /console/results (and the same numbers on the iOS snapshot). Every app-booked short mints a phantom deposit of 2x its notional and every cover a phantom withdrawal of 2x, each of which is a f… | high |
| qa-test-strategy:qa-01 | iOS unit tests never execute in CI or before TestFlight ship | Money-adjacent: the untested surface includes order-cancel (OrderCancelTests.swift) and price-review (ProposalPriceReviewTests.swift) logic on a real connected brokerage account. No dollar figure is provable from code alone (depends on what regression actuall… | high |
| qa-test-strategy:qa-02 | Swift build result is not part of the required merge gate | All subsequent iOS fixes queue behind a broken main until a human notices the ios-build.yml red X or the ios-ship.yml archive failure — there is no alerting. On a project largely driven by autonomous agents working on other lanes, that could be hours to days… | high |
| qa-test-strategy:qa-03 | Docs-only CI fast path skips the build for build-time-imported docs/benchmarks JSON | Worst case: a JSON-syntax-breaking edit auto-merges and breaks Coolify's `next build` on the next production deploy — a production outage on socratictrade.com's LLM-usage/model-stats surface, first caught by the deploy itself, not CI. Lesser case: a syntactic… | high |
| qa-test-strategy:qa-04 | No API-to-iOS snapshot contract test; a server rename silently blanks phone lists | Wrong information, not a crash: on a real connected brokerage account, the iOS app could show 'nothing pending' or 'no positions held' when real open proposals or exposure exist, with zero automated signal at any CI stage. No direct order-placement path, but… | high |
| security-auth:sec-01 | Production boots with no AUTH_SECRET guard: a missing secret turns every anonymous request into the owner | Requires a selective env regression (AUTH_SECRET gone, ENCRYPTION_KEY intact). In that state the origin is directly reachable (per sec-02) and every anonymous request is the owner. Worst realistic money outcome: an unauthenticated caller enumerates a pending… | high |
| trading-money-path:tsx-02 | Auto stale-exit remediation cancel-replaces ANY working sell/cover limit or stop_limit older than 15 min with a MARKET… | Every bracket-entered long is force-closed at MARKET roughly 15 minutes after its entry fills, wiping out both the 20% take-profit and the 8% stop the same bracket installed (they are one Alpaca OCO group), and it repeats for every bracketed entry the strateg… | high |
| trading-money-path:tsx-03 | iOS shows 'Approved — waiting for desk refresh' (green check) for approvals that returned busy/blocked/rejected_by_brok… | No unintended order is ever placed — the harm is the inverse: an order the owner believes is live never reached the broker. Worst realistic case: the owner approves a protective Risk-Exit proposal from the phone while a scheduled run holds the strategy lock,… | high |
| ux-copy-editor:copy-03 | Macro page says below-median buys are "hard-vetoed" in risk-off/crisis regimes; the code is advisory tag-not-drop | No dollar figure provable from code (depends on whether the agent actually attaches an override thesis in a given crisis), but the copy directly misdescribes how much protection exists during a market panic on a real account, and contradicts the owner's own d… | high |
| web-accessibility:a11y-01 | Home 'Recent decisions' rows are click-only <div>s — details + Approve flow unreachable by keyboard | Any keyboard-only or switch-control user, on both instances of this row (Latest strategy run + Previous Trades on Home), cannot open Proposal Details or the Approve/Reject-adjacent flow at all -- a complete, silent feature loss for that input modality, not a… | high |
| web-accessibility:a11y-02 | Light theme: tone-colored text on soft fills and the Stop/Start button fail WCAG AA (3.0–4.3:1); --con-info fails on ev… | Every light-theme user (the owner's own default per the LIGHT-default ruling) reading a status chip or the Stop/Start Agent button -- i.e. essentially every glance at the console -- is looking at text 7-45% below the WCAG AA contrast floor. Not a trading-logi… | high |

## B. Gap Coverage — Five Surfaces Part I Only Skimmed

50 new findings across five lanes, each re-checked by its own skeptic (**UPHELD** 45, **UPHELD_NARROWED** 5).  These are additive to Part I's register.

### Console pages read in full (settings, guardrails, strategy, results, orders, activity, macro, decisions)

- **[pages-01]** (P1 · bug · desktop-web · effort M) "Import from account" silently flips a live account into Autopilot and imports every guardrail with zero typed confirmation
  - **Where** `app/console/strategy/page.tsx:1021` · **Evidence** doImport() calls importAccountSettings(policy.connectedAccountId, source.id) behind only a plain warning box and an "Import settings" button — no TypedConfirm phrase, no reality.tone==='live' gate. Server side, src/lib/db-profiles.ts:895-901 does `stripIdentityFields(stripUserFields(sourcePolicyRaw…
  - **Why** Every other place in the app that can flip an account from Ask-first to Autopilot (guardrails/page.tsx AutonomyCard) requires typing the literal word "AUTOPILOT", and every other place that loosens a guardrail on a live account requires typing CONFIRM (`needsTyped = reality.tone === "live" && hasLooser && policy.requireTypedConfirmation !== false`, used identically in policy-form.tsx:335 and strategy/page.tsx:1259 f…
  - **Fix** Route importAccountSettings through the same needsTyped/TypedConfirm gate used by PolicySaveBar and AiReviewPanel: diff the source policy against the target's current policy the same way reviewChanges() does, and require the CONFIRM phrase whenever the target is live and any changed field is a loosening (including strategyAuthority propose→decide). At minimum, explicitly preserve the target's own strategyAuthority t…
- **[pages-02]** (P1 · bug · desktop-web · effort M) "Show results net of estimated tax" toggle saves successfully but never changes any number on Results
  - **Where** `app/console/strategy/tax-settings.tsx:213` · **Evidence** tax-settings.tsx:210-225 renders a Toggle labeled "Show results net of estimated tax" with title "On: P&L on the Results screen is shown after subtracting estimated taxes at the rates above", bound to policy.taxSettings.subtractFromResults and auto-saved via savePolicy. Grepping the whole repo, `su…
  - **Why** This is a control whose label makes an explicit, verifiable claim about what it does to the money numbers on Results, and the claim is false in both directions — flipping it on shows no different Realized/Unrealized P&L, and flipping it off doesn't restore anything either, because nothing ever reads it in the display path. A user trying to see their after-tax performance has no way to do so despite the setting exist…
  - **Fix** Either wire subtractFromResults into BucketCard/TaxBlock (subtract tax.estimatedShortTermTax/estimatedLongTermTax pro-rata from the realized figures shown, or add a distinct 'net of tax' stat) or remove the toggle and its copy until it's implemented.
- **[pages-03]** (P1 · bug · desktop-web · effort S) Webhook channel's copy promises rich Discord/Slack embeds it never sends, and "Send test" never exercises the one webhook field that actually does that
  - **Where** `app/console/settings/delivery.tsx:37` · **Evidence** delivery.tsx:37 CHANNEL_TITLE.webhook = "An HTTPS POST with a JSON payload to any URL you control (chat webhooks get rich embeds)." — describing the toggle-based "Webhook" channel. But that channel's actual sender, CHANNELS.webhook.send in src/lib/notify.ts:262-290, always POSTs plain `{source, kin…
  - **Why** A user who fills in the legacy "Webhook URL (optional)" field expecting the promised rich embeds (which really do exist, just on the other field) gets a plain JSON POST from the toggle-based channel instead if they toggle it on with the same/a different URL — and if they rely on the legacy field alone (as the always-on mechanism it's documented to be), "Send test" gives false confidence: it reports success/failure f…
  - **Fix** Either move the Discord rich-embed logic into the toggle-based CHANNELS.webhook.send so the copy is true, or have the test endpoint also probe policy.notificationSettings.webhookUrl and report it as its own row in the test results.
- **[pages-04]** (P2 · defect · desktop-web · effort S) Decisions index and trace pages merge every connected account's decisions into one list/detail with no account label or filter
  - **Where** `app/console/decisions/page.tsx:28` · **Evidence** page.tsx:28 calls `fetch("/api/socratic/decisions?limit=100")` with no `connectedAccountId`, even though app/api/socratic/decisions/route.ts explicitly supports that query param (`listSocraticDecisionCases(userId, { limit, connectedAccountId, runId })`). decisions-list.tsx never renders `decision.c…
  - **Why** Every other console page explicitly scopes to the active account and says so ("Showing the active account only — switch the account scope (top bar) to see another account's orders", orders/page.tsx:311-316; Activity's unified feed labels "Account: {label}" per row, activity/page.tsx:284-290). The console's own glossary states "Active account: Exactly one account is active at a time; the entire console... is scoped t…
  - **Fix** Pass the active account's connectedAccountId to /api/socratic/decisions by default (with an explicit "all accounts" toggle if cross-account viewing is intentional), and render an account/environment chip per row and on the trace page, matching Activity's pattern.
- **[pages-05]** (P2 · defect · desktop-web · effort M) Strategy → Presets has no way to create, rename, or delete a preset — view/apply only, despite full CRUD existing server-side
  - **Where** `app/console/strategy/page.tsx:925` · **Evidence** The Presets card (925-985) only renders `snapshot.profiles` and an "Apply to this account" button calling copyProfileToAccount/activateProfile. Grepping app/console for any call to `POST /api/profiles` (create) or `DELETE /api/profiles/[id]` (delete) turns up nothing, yet app/api/profiles/route.ts…
  - **Why** The page's own copy ("A saved strategy bundle... Applying one COPIES it onto the active account", help.tsx:193-196) implies presets are something the owner builds and manages, but there is no button anywhere in app/console to save the current account's strategy as a new preset, rename an existing one, or delete one — the library can only ever contain whatever got seeded some other way. This is a real "can the owner…
  - **Fix** Add "Save current strategy as new preset" (POST /api/profiles with this account's policy/prompt/weights), and rename/delete actions per row, wired to the existing PUT/DELETE routes.
- **[pages-07]** (P2 · defect · desktop-web · effort S) Macro's "How the strategist uses this label" disclosure repeats the same inaccurate "vetoed outright" claim the regime meaning text already got flagged for
  - **Where** `app/console/macro/indicators.ts:753` · **Evidence** REGIME_USAGE entry "It gates entries": "In Risk-Off or Crisis, buys scoring below the scan median are vetoed outright, and an optional cap limits how large any new position may open in Crisis/Inverted regimes." This is the same overstated claim as regimeInfo()'s meaning text at indicators.ts:698/70…
  - **Why** Same underlying inaccuracy (advisory, agent-decides-and-logs, not a hard block — per the owner's standing "guardrails: nothing hard" ruling) surfacing at a second, unfixed location; fixing only line 698/706 would leave this one still wrong.
  - **Fix** Same fix as the registered finding, applied to both locations: change "vetoed outright"/"hard-vetoed" to advisory language consistent with ADVISORY_NOTE used elsewhere in field-defs.ts.
  - Sharpens `ux-copy-editor:copy-03`
- **[pages-06]** (P3 · bug · desktop-web · effort S) Legal acceptance timestamp renders in the browser's local timezone, not Central Time
  - **Where** `app/console/settings/legal.tsx:40` · **Evidence** `legal?.accepted && legal.acceptedAt ? \`Accepted on ${new Date(legal.acceptedAt).toLocaleString()}.\` : ...` — no `timeZone` option. Every other timestamp helper in the console routes through app/console/lib/format.ts's CENTRAL_TIME_ZONE-pinned formatters (format.ts:10, "Owner ruling (2026-08-11):…
  - **Why** Directly violates the standing owner ruling that every user-facing timestamp displays in Central Time regardless of the viewer's device timezone — this is the one console timestamp that instead shows whatever timezone the browser is set to.
  - **Fix** Replace with the shared format.ts helper (e.g. fmtExact/formatTimestamp) used everywhere else in the console.
- **[pages-08]** (P3 · defect · desktop-web · effort S) Two auto-saving number fields fire a write on every keystroke instead of on blur, breaking the app's own established pattern
  - **Where** `app/console/settings/page.tsx:874` · **Evidence** settings/page.tsx:866-880 (DataSourcesCard number row): `onValueChange={(n) => { if (Number.isFinite(n) && n !== Number(row.value)) void saveOne(row.id, n); }}` — RawNumInput's onValueChange fires on every keystroke (primitives.tsx:310-315 confirms `onChange` calls `onValueChange` per input event),…
  - **Why** Every other numeric auto-save field in the app (ScanShapeCard settings/page.tsx:593-598, tax-settings.tsx short/long-term rates, llm-budget.tsx, learning-review.tsx, Strategy's own scoring-weights inputs) deliberately holds local text and commits on blur specifically to avoid firing a save per keystroke. These two fields silently deviate: typing a two-digit number issues multiple PATCH requests, and for Max Active a…
  - **Fix** Switch both to the same local-draft-until-blur pattern already used by every sibling numeric field (RawNumInput + explicit onBlur commit).

*Coverage:* Read in full: app/console/settings/page.tsx, delivery.tsx, sharing.tsx, legal.tsx, learning-review.tsx, llm-budget.tsx, danger.tsx, help.tsx, lib.ts; app/console/guardrails/page.tsx, field-defs.ts, stop-flow.tsx; app/console/strategy/page.tsx (all ~1522 lines), overlays-panel.tsx, tax-settings.tsx; app/console/orders/page.tsx (first ~420 lines plus targeted reads of lib.ts, api.ts, cancel-sheet.tsx, replace-market-sheet.tsx); app/console/results/page.tsx (header/benchmark/scorecard/red-team-eff…

### Admin surface as an operator product

- **[admin-03]** (P2 · opportunity · backend · effort M) Several real, cost/state-mutating operator capabilities (learning-ledger revert, tuning dry-run, congress-score-eval, RAG/EarningsCalls backfills) have zero admin UI -- curl-only, JSON body shape read out of code comments
  - **Where** `app/admin/layout.tsx:43` · **Evidence** NAV_ITEMS lists exactly 10 pages, none of which reach learning-ledger, tuning-dry-run, congress-score-eval, congress-share, refresh-websource, sec-ingest, reindex-8k, reindex-10k, or earningscalls. app/api/admin/learning-ledger/route.ts:9 literally calls itself "panel P0-4" and app/api/admin/tuning…
  - **Why** learning-ledger's POST reverts a live scoring-weight mutation (with before/after weight vectors) and is admin-gated specifically "because a revert mutates live policy" (route.ts:16) -- exactly the kind of action the rest of this admin surface (e.g. Operations' real toggle switches) treats as something an operator should be able to see and act on from a page, not something they curl. The same is true of the RAG/Earni…
  - **Fix** Wire learning-ledger (list + revert) and tuning-dry-run into the Admin nav first, since both already have complete read+act backend pairs; the naming suggests this was the original plan.
- **[admin-06]** (P2 · risk · backend · effort M) The tamper-evident audit hash-chain has no admin viewer, and verifyAuditChain() is never called outside a unit test
  - **Where** `app/api/audit/route.ts:8` · **Evidence** return NextResponse.json(listAudit(200, resolveRequestUserId(request))); -- no requireAdmin call, capped to 200 rows, no `kind` filter, scoped only to the caller's own userId. Repo-wide `grep -rn "verifyAuditChain"` (excluding node_modules) returns only its definition at src/lib/db.ts:3372 and test…
  - **Why** src/lib/db.ts's audit() builds a genuinely hash-chained audit_events table (chain_hash/prev_chain_hash, see auditChainMaterial at db.ts:3344) precisely so a knob change, a learning-ledger revert, or a securities import can be proven untampered. That investment is currently invisible in the product: there is no Admin > Audit Log page, no nav item, and the one HTTP route that reads audit_events is unused by any UI and…
  - **Fix** Add an admin-gated Admin > Audit Log page (list recent audit_events, filterable by kind, admin identity) that also surfaces verifyAuditChain()'s result; consider running the chain check on a schedule with an alert on mismatch.
- **[admin-01]** (P3 · bug · desktop-web · effort S) Chat Transcript admin page bypasses requireAdmin and does not actually show "every chat turn"
  - **Where** `app/admin/transcript/transcript-client.tsx:31` · **Evidence** const res = await fetch("/api/chat-history?limit=200"); -- app/api/chat-history/route.ts:8 is `const userId = resolveRequestUserId(request);` with no requireAdmin call at all; app/admin/layout.tsx:101-103 describes the nav item as "Every chat turn, with the model that produced each assistant reply."
  - **Why** Every other admin data probe (connections-health, llm-usage, rag-coverage, server-metrics, r2-usage, data-catalog, enrichment-coverage, backup-status, server-knobs) is behind the shared requireAdmin() gate and correctly 403s a non-admin session -- app/admin/page.tsx even has dedicated `allForbidden`/`probeErrors` handling to show "Operator access required" for exactly that case. `/api/chat-history` has no admin chec…
  - **Fix** Gate an admin-scoped transcript read behind requireAdmin, explicitly reading the primary/local user's turns (or accepting an explicit userId param) instead of reusing the per-caller /api/chat-history endpoint.
  - **Narrowed by verifier** The finding overstates live exploitability. middleware.ts (lines ~43-50, ~346-347) shows that with Auth.js sessions (the production identity source), empty ALLOWED_EMAILS means ONLY the primary operator email can authenticate at all -- and the primary email is always isAdminEmail()==true via the is…
- **[admin-02]** (P3 · risk · backend · effort M) RAG purge-legacy is an irreversible corpus-wide vector deletion reachable only by curl, guarded by a confirm string the code itself says is scope-mismatched
  - **Where** `app/api/admin/reembed/route.ts:35` · **Evidence** "SCOPE WARNING: despite the token's historical name it deletes vectors from EVERY non-current embedding space for the covered docTypes ... not only voyage-finance-2's." -- the POST handler at lines 80-88 gates this solely on `body.confirm === "purge-voyage-vectors"` plus the shared admin token/emai…
  - **Why** This action bulk-deletes RAG vectors across every embedding space that isn't the currently-active one for the covered doc types -- broader than its own confirm string implies, per the route's own comment. It is reachable only by hand-crafting an authenticated curl POST with that exact (misleadingly-named) confirm string; there is no UI that shows what would be deleted before it happens, no dry-run visible outside a…
  - **Fix** Surface this as a guarded Admin panel (in the style of Operations' knob resets) that lists the exact embedding spaces/docTypes about to be purged before firing the destructive POST, and/or rename the confirm string to match actual scope.
  - **Narrowed by verifier** The finding undersells the actual guardrails. purgeLegacyEmbeddingSpaceLocked (src/lib/rag/corpus-reembed.ts:1311-1388) refuses unless the target model is not the active one, AND requires a completed, zero-failure, non-symbol-scoped full corpus scan under the CURRENT active space (the completeUnder…
- **[admin-04]** (P3 · improvement · desktop-web · effort S) Overview's CPU-load number drops the "scaling unverified, may read up to 8x low" caveat that the Server Stats detail page carries for the identical value
  - **Where** `app/admin/page.tsx:448` · **Evidence** <span className="con-num font-semibold">{server?.metrics?.cpu ? `${currentCpu.toFixed(1)}%` : "Unavailable"}</span> -- no caveat text or title anywhere on this card, versus app/admin/server/server-metrics-client.tsx:175-178's CPU_SCALING_CAVEAT ("...this value may read low") and the visible caption…
  - **Why** app/api/admin/server-metrics/route.ts:604-613 documents this transform as an explicitly UNVERIFIED 8x risk ("the only support ... is an uncited comment ... which would make this an 8x under-report on this 8-core cx43 and keep the meter green through a saturation incident"). The Server Stats detail page was carefully rewritten to disclose that risk inline. The Overview page -- the one literally named to give status "…
  - **Fix** Reuse CPU_SCALING_CAVEAT (or a shortened title-only version) on the Overview card, or replace the bare percentage there with the same "unverified" language used on the detail page.
- **[admin-05]** (P3 · improvement · desktop-web · effort S) Admin chrome never shows which admin identity is signed in, despite the allowlist supporting multiple distinct admins and knob writes already being mis-audited as a fixed user
  - **Where** `app/admin/layout.tsx:217` · **Evidence** fetch("/api/auth/session").then((res) => res.json()).then((s) => setImageUrl(s?.user?.image)) -- only `image` is read and stored; the profile dropdown (lines 262-303) renders "Profile" / a theme picker / Back to Console / Sign Out, and no email or name string anywhere in the component.
  - **Why** src/lib/auth/admin.ts:44-50 (ADMIN_USER_EMAILS) is a documented allowlist that supports more than one distinct admin identity, and app/api/admin/server-knobs/route.ts:80 already audits every knob write under a hardcoded userId of "local" regardless of who actually flipped it. With no on-screen identity anywhere in the admin chrome, an operator using powerful tools (knob flips today; ledger reverts and backfills if a…
  - **Fix** Show the resolved session email in the profile trigger or dropdown header -- the fetch already receives it in `s.user.email`, it is just discarded.
- **[admin-07]** (P3 · improvement · backend · effort S) Enrichment Coverage report is a single unscoped process-global with no account/run attribution in a multi-account app
  - **Where** `src/lib/enrichment-coverage.ts:156` · **Evidence** let lastCoverageReport: EnrichmentCoverageReport | null = null; -- the EnrichmentCoverageReport interface (lines 123-135) has fields for asOf/symbolCount/fields/sourceWinTotals/providerFailures/contributingSources but no userId, connectedAccountId, or policy identifier of any kind.
  - **Why** app/api/admin/enrichment-coverage/route.ts serves getLastEnrichmentCoverageReport() verbatim -- whichever scan (any connected account, any user) ran most recently across the whole process overwrites the single in-memory report with no attribution. In a multi-connected-account app (the product's own stated model), an operator viewing Admin > Enrichment Coverage cannot tell which account's cascade run produced the num…
  - **Fix** Key the last-report cache by (userId, connectedAccountId) and show which account/run produced the displayed report, or keep a short history instead of a single overwritten snapshot.
  - **Narrowed by verifier** The finding's 'multi-account app, one account's scan silently masks another's' framing overstates the risk. src/lib/data-providers.ts (~1841-1842) own comment, 'The latest store is what interactive scans and other users read', confirms enrichment/fundamentals data is deliberately shared market-wide…
- **[admin-08]** (P3 · improvement · backend · effort S) A route under /api/admin/ intentionally skips the shared requireAdmin gate with no marker distinguishing it from the ~25 sibling routes that all use it
  - **Where** `app/api/admin/securities/import/route.ts:10` · **Evidence** import { verifySecuritiesImportToken } from "@/lib/securities-import-auth"; -- this file has no `requireAdmin` import at all, unlike every other route.ts under app/api/admin/ (confirmed: `grep -n requireAdmin` across all app/api/admin/*/route.ts files returns a hit in every route except this one).
  - **Why** This route is a legitimate, well-documented inter-app (Congress.Trade -> Socratic.Trade) ingest endpoint with its own bearer-token auth, default-closed when unconfigured -- not a defect on its own merits. But it lives at a URL prefix (/api/admin/) that every other route treats as "gated by the shared operator requireAdmin() check," which is exactly the assumption this review started from before reading the file. A f…
  - **Fix** Move this route out of the /api/admin/ prefix (e.g. /api/integrations/congress-trade/securities-import) so the prefix reliably means "gated by requireAdmin" for every route under it, or add a header comment making the divergent auth model impossible to miss on a prefix-based audit.

*Coverage:* Read in full: app/admin/layout.tsx, app/admin/page.tsx (Overview), and all ten admin subpages' client components plus their app/api/admin/* routes -- connections-health, llm-usage (+model-merge.ts), rag-coverage, enrichment-coverage, data-catalog, operations/server-knobs, server/server-metrics (route + client, targeted sections of the 1129-line client), backups/backup-status, transcript (client + long-turn.ts + markdown.tsx), plus src/lib/auth/admin.ts, middleware.ts's identity/admin-adjacent g…

### Coach / assistant, end to end (including the trade-staging path)

- **[coach-01]** (P1 · bug · cross-surface · effort S) draft_order silently coerces any non-"sell" side value (case-sensitive) to "buy", including "short"/"cover"/typos
  - **Where** `src/lib/chat/tools.ts:92` · **Evidence** const side = input.side === "sell" ? "sell" : "buy"; // line 92; line 94 has the same pattern: const order_type = input.order_type === "limit" ? "limit" : "market";
  - **Why** This is a strict, case-sensitive equality check with no rejection path. Any side value that is not the exact lowercase string "sell" -- "Sell", "SELL", "short", "cover", a typo, or an out-of-schema value a less-compliant provider emits -- is silently mapped to "buy", the OPPOSITE economic direction from a sell/short. The Coach model catalog includes Gemini, Grok, Mistral, DeepSeek, Moonshot, and any free-typed custo…
  - **Fix** Reject the draft (return {error:"INVALID_SIDE"}) when input.side is not exactly "buy" or "sell" (case-insensitive compare, then fail closed on anything else, mirroring create_alert's normalizeAlertOp pattern), and do the same for order_type. If short/cover intent is out of scope for chat, say so explicitly in the draft error rather than silently rewriting the order to a buy.
- **[coach-02]** (P2 · bug · cross-surface · effort M) Neither the chat "Cancel" button nor the 120s turn deadline actually aborts an in-flight LLM provider request
  - **Where** `src/lib/chat/llm.ts:333` · **Evidence** async function defaultTransport(body, apiKey) { const res = await llmFetch("https://api.anthropic.com/v1/messages", { method: "POST", headers: {...}, body: JSON.stringify(body) }); ... } -- no `signal` is ever passed, so llm-request.ts:402 falls back to `signal: init.signal ?? AbortSignal.timeout(L…
  - **Why** AnthropicLLM.run/OpenAILLM.run only check `args.abortSignal?.aborted` (llm.ts:395, 537) and the deadline budget (via decideStageBudget) at the TOP of each tool-loop step, BEFORE calling `this.transport(...)`. The Transport type itself is `(body, apiKey) => Promise<any>` -- it never receives the AbortSignal, so once a provider HTTP call is in flight there is no way to interrupt it; it can only be interrupted by its o…
  - **Fix** Thread `args.abortSignal` into the Transport signature and pass it as `signal` on every llmFetch call in defaultTransport/defaultOpenAITransport/makeOpenAITransport (composed with the existing timeout signal via e.g. AbortSignal.any). Also give the client's `/api/chat` fetch its own AbortController tied to the Cancel button so cancel takes effect locally without waiting on the server.
- **[coach-04]** (P2 · risk · backend · effort M) Chat conversation history has no account scoping -- the last-10-turn context replayed into every prompt spans ALL of a user's connected accounts
  - **Where** `src/lib/types.ts:3011` · **Evidence** export interface ChatTurn { id: string; userId: string; role: ChatTurnRole; text: string; citations: string[]; intent: string | null; redacted: boolean; model?: string | null; clientTurnId?: string | null; createdAt: string; } -- no connectedAccountId field anywhere on the row; orchestrator.ts:143…
  - **Why** A user with multiple connected accounts (e.g. a live account and a paper/practice account) who switches the active account mid-conversation gets a multi-turn LLM context that silently mixes both accounts' answers with no account-boundary marker in the replayed text -- the model has no way to know a prior turn's positions/P&L/portfolio numbers belonged to a different account than the one the current tool calls now re…
  - **Fix** Either scope chat_turns (and the 10-turn replay window) by connectedAccountId so switching accounts starts a fresh context, or inject an explicit account-boundary marker into the replayed history whenever the active account differs from the account each historical turn was answered under (would require persisting connectedAccountId per turn either way).
- **[coach-05]** (P2 · risk · api · effort S) Dead, unauthenticated POST /api/chat-history lets a user forge arbitrary chat turns -- including fabricated "assistant" replies -- into their own persisted transcript
  - **Where** `app/api/chat-history/route.ts:28` · **Evidence** export async function POST(request: Request) { const body = ...; const userId = resolveRequestUserId(request, body); if (body.role !== "user" && body.role !== "assistant") return ...400; ... const turn = appendTurn(userId, { role: body.role, text: body.text, citations: ..., intent: ... }); return N…
  - **Why** This handler accepts an arbitrary role ("user" OR "assistant") and arbitrary text/citations straight from the request body and writes it into the caller's own chat_turns via the same appendTurn used by the real orchestrator -- with no check that this ever went through an actual model call. Grepping the whole app/ios tree shows nothing calls it: chat.tsx only GETs and DELETEs /api/chat-history, and the real assistant…
  - **Fix** Remove this POST handler if truly unused, or restrict it to a genuinely internal caller (it should not be a public route at all given nothing calls it); at minimum require role to always be recorded via the orchestrator's own appendTurn path rather than a free-form client-supplied write.
- **[coach-06]** (P2 · risk · backend · effort M) Coach URL-lesson capture stores fetched web-page text verbatim (no summarization despite the name) as auto-approved "fact"-tier learned context, protected only by a financial-risk keyword classifier, not a general injection detector
  - **Where** `src/lib/chat/coach-learning.ts:567` · **Evidence** const lesson = summarizeArticleText(fetched.text, fetched.url); ... const result = await ingestLearned(args.userId, { kind: "fact", subject: "coach-url-lesson", value: lesson.slice(0, 800), source: `owner-coach-url:${fetched.url}`, confidence: 0.7, intent: "article lesson" }, "ingest", {...}); -- s…
  - **Why** A user pasting a URL in chat causes the server to fetch it (SSRF-gated, which is well done) and store roughly the first 480-800 characters of the raw page text as a durable "fact"-tier learned_context row. Because origin is "ingest" (not "chat"), it is NOT hard-capped/dropped when the risk classifier fires, and it is NOT auto-written when it does -- classifyRiskTier (src/lib/learned-context/classify.ts:366-419) is a…
  - **Fix** Either run the fetched article text through the SAME containPromptText injection scan at ingest time (not just at prompt-assembly read time) and drop/quarantine on any hit before it ever reaches the pending/fact write path, or route ALL coach URL-lesson candidates through the pending-approval queue regardless of the keyword classifier's verdict, given the content originates from an arbitrary external host the user d…
- **[coach-09]** (P2 · bug · cross-surface · effort M) When the tool loop needs more than MAX_STEPS provider round-trips, the reply silently collapses to just the bare disclaimer with no partial-answer or truncation signal
  - **Where** `src/lib/chat/llm.ts:436` · **Evidence** for (let step = 0; step < MAX_STEPS; step++) { ... if (resp.stop_reason !== "tool_use" || toolUses.length === 0) break; ... /* execute tools, push results, loop continues */ } -- MAX_STEPS = 5 (llm.ts:50). If step index 4 (the last allowed iteration) STILL returns tool_use, the tool results are exe…
  - **Why** `text` retains whatever the LAST model response contained -- typically empty or near-empty, since a response whose stop_reason is tool_use rarely carries prose alongside the tool call. The function then returns `withDisclaimer(text)` (Anthropic) or `text || DISCLAIMER` (OpenAI), i.e. the user's answer is silently reduced to just "This is general information, not personalized financial advice." with zero substantive…
  - **Fix** On exiting the loop specifically because the last step still wanted tools (rather than because the model naturally stopped), either allow one final no-tools completion call to summarize what was gathered so far, or set an explicit flag/text ("I gathered partial information but ran out of steps -- ask again for more detail") instead of falling through to a bare disclaimer.
- **[coach-10]** (P2 · improvement · cross-surface · effort M) draft_order has no dollar-notional field or guidance, risking share-count/dollar-amount confusion when a user says "buy $X of SYMBOL"
  - **Where** `src/lib/chat/tools.ts:58` · **Evidence** const DRAFT_ORDER_SCHEMA: Record<string, unknown> = { type: "object", ..., required: ["symbol","side","qty"], properties: { symbol:{...}, side:{...}, qty:{type:"number"}, order_type:{...}, limit_usd:{...}, rationale:{...} } }; -- qty is unlabeled (no units), there is no notional_usd field, and the…
  - **Why** Users very commonly speak in dollar terms ("buy $1000 of TSLA", "put $500 into AAPL"). Since draft_order only accepts a raw share `qty`, the model must either ask a clarifying question (not enforced by anything) or silently compute qty = notional/price itself, and nothing in the tool contract requires it to fetch a fresh quote first -- under time/step pressure (MAX_STEPS=5, see coach-09) a model could plausibly pass…
  - **Fix** Add an optional `notional_usd` field to the draft_order schema, and have the tool (server-side) fetch a live quote and compute qty itself when notional is given instead of trusting the model's arithmetic; update the tool description to explicitly require calling get_quote before converting any dollar-denominated request to a share qty.
- **[coach-03]** (P3 · defect · cross-surface · effort S) ChatDraft ships permanently-hardcoded placeholder fields ("Test (local)" account_label, is_real:false, blocked:false, warnings:[]) that no client renders
  - **Where** `src/lib/chat/tools.ts:105` · **Evidence** account_label: ctx.deps.accountLabel ?? "Test (local)", is_real: false, blocked: false, warnings: [], // tools.ts:105-108; ctx.deps.accountLabel is itself hardcoded to the literal string "Test (local)" in buildProductionDeps (src/lib/chat/orchestrator.ts:770), so account_label is ALWAYS "Test (loca…
  - **Why** These four fields are set exactly once at draft creation and never mutated anywhere else in the codebase (grep confirms zero other writers). account_label is always the literal string "Test (local)" regardless of the actual connected account (which could be a real live broker account) -- a hardcoded test/paper-mode label the owner's rulings explicitly forbid attaching to real data. is_real is always false and blocke…
  - **Fix** Either wire real values through (pass the caller's actual account label/environment into ToolDeps.accountLabel from the active connected account rather than a hardcoded default, and drop is_real/blocked/warnings from the tool's output entirely since policy evaluation genuinely happens later in /api/proposals/from-draft), or remove the dead fields from the ChatDraft type so the wire contract doesn't advertise informa…
- **[coach-07]** (P3 · defect · ios · effort S) iOS Coach Send button has no client-side gate for an unselected model, unlike web's explicit "Coach has no hidden default" block
  - **Where** `ios/SocraticTrade/CoachView.swift:148` · **Evidence** private var canSend: Bool { !isSending && !draft.trimmingCharacters(in: .whitespacesAndNewlines).isEmpty } -- no check on `selectedModel.isEmpty`, unlike web's `canSend = !sending && !clearing && !keyMissing && !customPending && !modelUnselected && input.trim().length > 0` (chat.tsx:373).
  - **Why** CoachModelCatalog.firstAvailable (DeskModels.swift:198-202) auto-selects a model as soon as bootstrap()'s `providers` fetch succeeds, so selectedModel is usually non-empty in practice. But if fetchChatProviders() throws (the async-let sequential await in bootstrap() means a `history` success followed by a `providers` failure leaves `providers` at its default empty dict and skips the auto-select block entirely, Coach…
  - **Fix** Add `!selectedModel.isEmpty` to canSend, and surface a clear "choose a model" prompt (mirroring the web composer placeholder) when it is empty, instead of only relying on the auto-select happy path.
- **[coach-08]** (P3 · defect · backend · effort S) The chat route and the orchestrator independently mint two DIFFERENT random turnKeys when no clientTurnId is supplied
  - **Where** `src/lib/chat/orchestrator.ts:137` · **Evidence** const turnKey = `chat:${userId}:${clientTurnId ?? globalThis.crypto.randomUUID()}`; // orchestrator.ts:137, called from inside makeOrchestrator -- app/api/chat/route.ts:155 computes the SAME expression independently before calling registerChatTurn/orchestrate, so when clientTurnId is undefined each…
  - **Why** In normal web/iOS usage this is latent because both clients always generate and send a clientTurnId, so the two computations agree. But any direct API caller (a script, a future integration, or a test) that omits clientTurnId gets a turnKey back in the HTTP response (route.ts:176, used for turn-registry lookups and Cancel) that does NOT match the turnKey embedded in that same turn's own `chat.turn`/`chat.evidence_pa…
  - **Fix** Generate the turnKey once in the route (already done, route.ts:155) and pass it into orchestrate()/handleTurn() as an explicit argument instead of having the orchestrator recompute its own copy from clientTurnId.
- **[coach-11]** (P3 · defect · backend · effort S) kb_search's k parameter has no upper bound, unlike every sibling numeric tool input in the same file
  - **Where** `src/lib/chat/tools.ts:155` · **Evidence** k: { type: "integer", minimum: 1 } -- tools.ts:155, forwarded unclamped: `k: typeof input.k === "number" ? input.k : 5` (tools.ts:165) straight into retrieveContextDetailed(args.query, symbol, args.k ?? 5, ...). Compare get_earnings_calendar's days (`Number.isInteger(days) && days !== 0 && days >=…
  - **Why** Every other numeric chat-tool input in this file is treated as untrusted model output and explicitly range-checked server-side per the file's own stated philosophy ("Server-side validation -- the model's input is untrusted regardless of any schema claim.", tools.ts:90). kb_search's k breaks that pattern: a model (or a prompt-injected instruction steering it) requesting an unreasonably large k would forward straight…
  - **Fix** Add a maximum to the k schema (e.g. 1-20, matching search_instrument's pattern) and clamp server-side in the same style as the other tools in this file.

*Coverage:* Read in full: app/console/assistant/{chat.tsx,draft-card.tsx,markdown.tsx,models.tsx,page.tsx}; src/lib/chat/{types.ts,orchestrator.ts,tools.ts,llm.ts,prompt.ts,promote-draft.ts,stage-budget.ts,turn-registry.ts,coach-learning.ts}; app/api/chat/route.ts, app/api/chat/cancel/route.ts, app/api/chat/providers/route.ts, app/api/chat-history/route.ts, app/api/proposals/from-draft/route.ts; src/lib/chat-history.ts, src/lib/user-write-fence.ts, src/lib/request-user.ts, src/lib/prompt-safety.ts (contain…

### Watchlist + price alerts + notification delivery, end to end

- **[alerts-01]** (P1 · bug · backend · effort M) Price alerts silently stop evaluating when no broker account is active
  - **Where** `src/lib/alerts.ts:68` · **Evidence** const accountNumber = policy.accountNumber; if (!accountNumber) return [];
  - **Why** checkPriceAlerts() calls getPolicy(userId) with no connectedAccountId, which resolves to whichever connected account is currently 'active' for that user (src/lib/db-profiles.ts resolveAccount -> getActiveConnectedAccount). PriceAlert rows are user-scoped, not account-scoped (types.ts:127-138), and the watchlist page banner literally says 'yours across every account' -- yet evaluation is gated on one account's accoun…
  - **Fix** Evaluate price alerts against an account chosen explicitly for quote purposes (not the mutable 'active' pointer), or fall back to any workable connected account, and emit an explicit 'alerts paused: no account' audit/UI state when none exists at all.
- **[alerts-02]** (P1 · bug · backend · effort S) checkPriceAlerts swallows broker-quote errors with zero logging
  - **Where** `src/lib/alerts.ts:74` · **Evidence** let quotes: Record<string, { price?: number }> = {}; try { quotes = await gateway.getEquityQuotes(accountNumber, symbols); } catch { return []; }
  - **Why** Any exception from the broker gateway (auth failure, rate limit, network outage) is caught and discarded with an empty catch block -- no console.error, no audit() call, nothing recorded anywhere. checkAllUserPriceAlerts()'s own .catch() (alerts.ts:120) never fires because checkPriceAlerts() resolves normally with []. A persistent broker problem on one user's active account means every armed alert is silently re-chec…
  - **Fix** At minimum audit()/console.error the caught exception, mirroring how checkAllUserPriceAlerts already logs per-user errors, so a sustained broker failure is discoverable instead of indistinguishable from 'nothing to check.'
  - **Narrowed by verifier** The finding's framing ('any exception from the broker gateway') slightly overstates the practical blast radius: Alpaca/Tradier/Robinhood's own getEquityQuotes implementations already self-heal internally (try/catch + Yahoo-daily-close fallback in alpaca.ts:562-579 and tradier.ts:620-644; per-chunk…
- **[alerts-03]** (P2 · bug · cross-surface · effort M) Price-alert push deep link's ?symbol= is dropped by both web and iOS
  - **Where** `src/lib/push-deep-links.ts:99` · **Evidence** case "price_alert": return routing.symbol ? `/console/watchlist?symbol=${encodeURIComponent(routing.symbol)}` : "/console/watchlist";
  - **Why** The server builds a symbol-specific URL and the file's header comment calls the query param 'additive' routing for 'a client that prefers structured routing.' Neither client honors it: app/console/watchlist/page.tsx never calls useSearchParams or reads the URL, so tapping a 'Price alert: NVDA' notification on the web lands on the full unfiltered watchlist/alerts list. ios/SocraticTrade/DeepLink.swift:75-77 matches t…
  - **Fix** Implement symbol-aware routing on both the watchlist page (scroll-to/highlight via useSearchParams) and DeepLink.swift (a `.watchlist(symbol:)` destination mirroring `.proposal(id:)`), or stop implying finer-grained routing exists in the push-deep-links.ts comment.
  - Sharpens `ios-engineering:ios-16`
- **[alerts-04]** (P2 · bug · ios · effort S) iOS watchlist/alert commands queue behind a 30-minute strategy run
  - **Where** `src/lib/mobile-api.ts:88` · **Evidence** const IMMEDIATE_MOBILE_COMMAND_TYPES = new Set<MobileCommandType>([ ...IMMEDIATE_PROTECTIVE_COMMAND_TYPES, "account.activate", "order.cancel" ]);
  - **Why** watchlist.add, watchlist.remove, alert.create, and alert.delete are not in IMMEDIATE_MOBILE_COMMAND_TYPES, so from iOS they go through the single, strictly-serial mobile_commands worker (claimNextQueuedCommand picks the globally oldest queued row one at a time, mobile-api.ts:701-717) alongside strategy.run_once, which the same file documents as legitimately taking up to STALE_MOBILE_COMMAND_THRESHOLD_MS = 30 minutes…
  - **Fix** Add watchlist.add/remove and alert.create/delete to IMMEDIATE_MOBILE_COMMAND_TYPES -- none of them touch the broker or need serialization with a strategy run.
  - Sharpens `api-contract-web-ios:api-05`
- **[alerts-05]** (P2 · risk · backend · effort M) checkPriceAlerts/checkAllUserPriceAlerts have zero test coverage
  - **Where** `test/watchlist-alerts.test.ts:1` · **Evidence** describe("price alerts", () => { it("normalizes comparison operators"...); it("creates, lists, and deletes alerts"...); it("rejects invalid alert input"...); }) -- grep across test/ for checkPriceAlerts|checkAllUserPriceAlerts returns zero hits anywhere in the repo.
  - **Why** The entire money-relevant behavior of this feature -- does a crossing get detected, does a missing/invalid quote get skipped instead of false-triggering, does a broker error leave the alert armed for a later retry, does a trigger call sendNotification exactly once -- lives in checkPriceAlerts/checkAllUserPriceAlerts (src/lib/alerts.ts) and is completely untested. Only CRUD (create/list/delete) and input validation a…
  - **Fix** Add unit tests covering: a crossing that should fire, a quote below/above threshold that should not, a missing/zero quote (skip), a broker-gateway throw (should not crash and should leave the alert armed), and no-active-account (should return [] without calling the gateway), using an injected fake gateway/policy the way other broker-dependent tests in this repo already do.
- **[alerts-06]** (P2 · risk · backend · effort M) Price alerts fire against raw quotes with no staleness check
  - **Where** `src/lib/alerts.ts:83` · **Evidence** const currentPrice = quote?.price; if (typeof currentPrice !== "number" || currentPrice <= 0) continue; const hit = alert.op === "<" ? currentPrice < alert.price : currentPrice > alert.price;
  - **Why** The order-placement path has a dedicated staleness gate (quoteAgeSecForStalenessGate in src/lib/quotes-cascade.ts, applied in policy.ts:395-475) that checks quote age before acting on a price. checkPriceAlerts uses none of that infrastructure -- it takes whatever `.price` the broker gateway returns with no age check at all. A delayed/cached quote (a provider outage falling back to a stale cached value, or a venue-de…
  - **Fix** Reuse quoteAgeSecForStalenessGate (or the quote's own asOf/timestamp) to skip evaluating an alert against a quote older than a small threshold, and/or annotate the triggered notification with quote age so a delayed trigger is honestly labeled.
- **[alerts-07]** (P2 · bug · backend · effort M) An alert is marked 'triggered' before delivery is attempted, with no retry if every channel fails
  - **Where** `src/lib/alerts.ts:88` · **Evidence** const updated = markPriceAlertTriggered(alert.id, userId, currentPrice); if (!updated) continue; triggered.push(updated); audit("alert.triggered", {...}); await sendNotification(...)
  - **Why** markPriceAlertTriggered flips the alert's DB status to 'triggered' (one-shot, non-reversible per src/lib/db-api-keys.ts:1777-1781) before sendNotification is even awaited. Since price alerts never re-arm by design, if every enabled delivery channel fails at send time (a revoked Pushover token, a wrong ntfy topic, an expired APNs cert, or simply zero channels configured yet), sendNotification records a status:'failed…
  - **Fix** Either don't flip the alert to 'triggered' until sendNotification reports at least one successful channel (retrying next tick if delivery fully failed), or surface undelivered-but-triggered alerts more prominently on the Watchlist page instead of blending in with delivered ones.
- **[alerts-08]** (P3 · improvement · cross-surface · effort M) Price alerts can only be created or deleted -- no edit, pause, or snooze
  - **Where** `app/api/alerts/route.ts:1` · **Evidence** export async function GET... export async function POST... export async function DELETE... -- no PUT/PATCH handler; src/lib/alerts.ts exports only createAlert/listAlerts/removeAlert, no updateAlert.
  - **Why** Changing an armed alert's price/note, or temporarily pausing it without losing it, requires delete + recreate on both web and iOS, discarding the original id/createdAt. The review brief specifically asks about edit/mute/snooze lifecycle actions and none exist for price alerts -- the Alert Center's 24h mute is display-only, for a different set of event types, and doesn't apply to price_alert at all.
  - **Fix** Add a PUT/PATCH /api/alerts endpoint (and a matching mobile-api.ts alert.update command) for price/op/note edits, and consider a simple armed/paused toggle for snoozing without deleting.
- **[alerts-09]** (P3 · defect · backend · effort S) Watchlist and price-alert symbol validation use two different regexes
  - **Where** `src/lib/alerts.ts:17` · **Evidence** const SYMBOL_RE = /^[A-Z.]{1,10}$/; (alerts.ts) vs return /^[A-Z][A-Z0-9.]{0,9}$/.test(normalized) && !normalized.includes("..") && !normalized.endsWith("."); (index-universes.ts:311, isValidAppSymbol)
  - **Why** The watchlist validator (isValidAppSymbol, used by watchlist.ts and mobile-api.ts's watchlist.add/remove decode) requires a leading letter and rejects '..' or a trailing '.'. The price-alert validator (SYMBOL_RE, used by createAlert for both the web route and the iOS alert.create command) allows any 1-10 character string of letters/dots, including '...', a leading dot, or a bare '.'. A malformed symbol like '...' pa…
  - **Fix** Have createAlert call the same isValidAppSymbol used by the watchlist path instead of maintaining a second, looser regex.
- **[alerts-10]** (P3 · defect · backend · effort S) A price-alert's notification row is tagged with whatever account happened to be active, not with anything meaningful to the alert
  - **Where** `src/lib/notifications.ts:216` · **Evidence** const connectedAccountId = options.connectedAccountId ?? policy.connectedAccountId;
  - **Why** checkPriceAlerts calls sendNotification without an explicit connectedAccountId, so it falls back to policy.connectedAccountId -- whichever account was 'active' at the moment the 60-second tick ran. PriceAlert rows are user-scoped, not account-scoped (types.ts:127-138), so this tag is incidental, not meaningful. app/console/activity/page.tsx:213 filters the Activity feed to the currently active account, so a price-al…
  - **Fix** Pass connectedAccountId: undefined explicitly for price_alert notifications so they are never filtered by account-scoping logic, matching their actual account-independent semantics.
- **[alerts-11]** (P3 · defect · cross-surface · effort S) Price-alert note length is capped on iOS (500 chars) but unbounded on the web API
  - **Where** `app/api/alerts/route.ts:31` · **Evidence** note: typeof body.note === "string" ? body.note : undefined (app/api/alerts/route.ts, no length check) vs const note = asOptionalString(payload.note, 500); (src/lib/mobile-api.ts:584, iOS path caps at 500)
  - **Why** createAlert() itself never truncates note (`input.note?.trim() ?? ""`), so the web REST endpoint accepts an arbitrarily long note while the iOS mobile-command decode path caps the same field at 500 characters before it ever reaches createAlert. A very long note created via the web can then break the Discord webhook embed, which renders it verbatim (`description = String(alert.note)` in notifications.ts's formatDisco…
  - **Fix** Cap the note length once, inside createAlert() (or the price-alert insert in db-api-keys.ts), so both entry points share the same limit instead of iOS silently being stricter than the API it calls.

*Coverage:* Read in full: src/lib/alerts.ts, src/lib/notifications.ts, src/lib/notify.ts, src/lib/apns.ts, src/lib/push-deep-links.ts, src/lib/db-notifications.ts, src/lib/watchlist.ts, src/lib/watchlist-digest.ts, app/console/watchlist/page.tsx, app/api/alerts/route.ts, app/api/watchlist/route.ts, app/api/notifications/{route,ack,mute,test}/route.ts, app/console/components/alert-center.tsx, app/console/activity/page.tsx (account-scoping section), app/api/mobile/snapshot/route.ts, relevant slices of src/li…

### First-run and account lifecycle (connect → activate → run; sign-out, expiry, deletion)

- **[lifecycle-01]** (P1 · bug · cross-surface · effort S) A disconnected ('draining') account can be reactivated and becomes the live trading target while the scheduler is still tearing it down
  - **Where** `src/lib/db-api-keys.ts:1323` · **Evidence** setActiveConnectedAccount(id, userId) only checks the row exists for this user, then flips is_active — it never reads or clears is_draining: `db.prepare("UPDATE connected_accounts SET is_active = 0 WHERE user_id = ?").run(userId); db.prepare("UPDATE connected_accounts SET is_active = 1 WHERE id = ?…
  - **Why** A user who clicks Disconnect then immediately re-Loads/re-Uses the same account (e.g. changed their mind, or it's still showing in the list because purge hasn't run yet) makes that account the full policy target (getPolicy -> resolveAccount -> getActiveConnectedAccount, which also has no isDraining filter) while the scheduler's drain routine is independently cancelling its open orders and racing to delete the row's…
  - **Fix** Have setActiveConnectedAccount reject (or auto-clear is_draining and cancel the pending purge) for a draining row; disable Load/Use and grey the Switch/Use rows client-side wherever isDraining is true (brokers.tsx, chrome.tsx ScopeSelector, HomeView.swift) instead of only checking isActive.
- **[lifecycle-02]** (P1 · defect · desktop-web · effort S) Settings > Broker connections mislabels every non-loaded account 'Inactive' even when it is actively trading in the background
  - **Where** `app/console/settings/brokers.tsx:189` · **Evidence** `const stateInfo = isCurrentLoaded && snapshot?.policy ? deriveStateInfo(snapshot.policy) : null;` (isCurrentLoaded === account.isActive). The chip render at lines 271-280 falls back to `<Chip tone="muted">{account.isActive ? "Loaded" : "Inactive"}</Chip>` whenever stateInfo is null -- which is eve…
  - **Why** A second connected account (e.g. a live Roth IRA) that is genuinely running Autopilot right now shows the same static 'Inactive' chip as an account that has never been armed. For a real-money multi-account trading app this is a materially misleading state readout on the exact screen (Settings > Broker connections) a user checks to answer 'is anything happening in my other account.'
  - **Fix** Compute stateInfo from `snapshot.connectedAccountPolicies?.[account.id]` for every row (as ScopeSelector already does), not only for the currently-loaded one.
- **[lifecycle-06]** (P1 · defect · ios · effort M) iOS first-run checklist tells the user to connect a broker 'in Account & Settings' but that screen has no broker-connect UI at all
  - **Where** `ios/SocraticTrade/HomeView.swift:87` · **Evidence** Checklist row detail (line 87): "Connect Alpaca or Robinhood in Account & Settings, then select it here." But AccountSettingsView's accountsSection (lines 818-848) only ever renders ConnectedAccountSettingsRow for EXISTING accounts (activate-only, via 'Use') -- there is no Connect Alpaca/Robinhood/…
  - **Why** A first-time iOS-only user (downloaded the app without ever visiting the website) hits a genuine dead end: the checklist's own copy claims the fix is inside the very screen it links to, and only the empty-state variant of that same screen tells the truth.
  - **Fix** Already tracked in the review register as ios-hig-ux:ios-09 (same file/line, broader remediation options incl. reusing AdminPortalView's cookie-sharing web view). Cross-confirming from the account-lifecycle trace: fix the misleading line-87 copy at minimum even if a full in-app connect flow isn't built.
  - Sharpens `ios-hig-ux:ios-09`
- **[lifecycle-03]** (P2 · defect · backend · effort S) First-broker auto-activation is inconsistent: Robinhood auto-loads the first connected account, Alpaca/Tradier/eToro/Public/Webull never do
  - **Where** `app/api/connected-accounts/route.ts:100` · **Evidence** Robinhood connect: `isActive: body.isActive ?? existing?.isActive ?? !getActiveConnectedAccount(userId)` -- auto-activates when nothing else is active. The generic (Alpaca/Tradier/eToro/Public/Webull) upsert at line 212 uses `isActive: body.isActive ?? false` -- never auto-activates, even for a bra…
  - **Why** Alpaca is the most prominent 'Connect' button in the brokers.tsx header (listed before Tradier/Public), so most first-run users will complete readiness step 1 ('Connect a broker') via Alpaca and land on an unexplained-feeling extra manual step (readiness step 2, 'Select an active account') that a Robinhood-first user never sees. The checklist itself doesn't call out that this differs by broker.
  - **Fix** Make the generic branch auto-activate on first connect too, matching Robinhood: `isActive: body.isActive ?? existing?.isActive ?? !getActiveConnectedAccount(userId)`.
- **[lifecycle-04]** (P2 · defect · desktop-web · effort S) Website session expiry inside an already-open console tab has no recovery path -- stuck behind a stale 'Unauthorized' banner, Run Once fails with a misleading generic message
  - **Where** `app/console/lib/useConsoleData.tsx:139` · **Evidence** middleware.ts:412-419 answers an unauthenticated `/api/*` request with a plain 401 (`new NextResponse("Unauthorized", { status: 401 })`), never a redirect (redirects only fire for page navigation, not fetch()). useConsoleData's runFetch (lines 139-150) treats that identically to any other transient…
  - **Why** A session that expires (or is invalidated) while the console tab stays open never tells the user to sign back in -- it just quietly degrades to stale data forever (polling every 15s, 401 every time) and, if they try to act, sends them to Activity to investigate 'why the run failed' instead of back to /login.
  - **Fix** Have the console's fetch client special-case 401 (distinct from other ConsoleApiErrors): stop the poll loop and route to /login (or show an explicit 'sign in again' banner) instead of the generic staleness/failure paths.
- **[lifecycle-05]** (P2 · defect · ios · effort M) iOS readiness checklist has no way to represent (or fix) missing LLM key / Green-team model, unlike the website's 5-step checklist
  - **Where** `ios/SocraticTrade/MobileModels.swift:113` · **Evidence** `struct Readiness: Decodable { let hasAccount: Bool; let hasUniverse: Bool }` -- no llm/model field at all. The server's mobileReadiness (src/lib/mobile-api.ts:1315-1316) only ever computes `hasAccount`/`hasUniverse`, matching the client shape. HomeView.swift:9 gates the entire 'ready' vs 'setup' U…
  - **Why** A first-run iOS user who connects a broker and sets a universe sees the app declare itself fully 'ready to trade' and offers Run Once, even though no LLM key/model is configured -- the only way they discover the real blocker is a failed Run Once, and even then there is no in-app screen to fix it (they must switch to the website).
  - **Fix** Add an llmConfigured/hasGreenModel field to the mobile snapshot's readiness payload and gate the iOS 'ready' state on it too, or explicitly acknowledge in the checklist copy that LLM configuration must happen on the website.
- **[lifecycle-11]** (P2 · defect · ios · effort M) iOS account-deletion confirm silently marks all five web acknowledgement checkboxes 'true' server-side without ever showing them to the user
  - **Where** `app/api/mobile/account-deletion/confirm/route.ts:40` · **Evidence** The mobile confirm route hardcodes every acknowledgement the web flow requires the user to individually tick: `deleteAppData: true, deleteBrokerConnections: true, understandBrokerPositionsRemain: true, understandProviderRevocation: true, understandCanSignInAgain: true` (lines 47-51), gated only on…
  - **Why** For a permanently irreversible, typed-phrase-gated action, the server treats an iOS user as having made five specific acknowledgements (including provider-revocation and broker-positions-remain caveats) they were never asked to individually confirm -- a real consent-integrity gap between what the record says and what the user did.
  - **Fix** Either surface the same five items as toggles the iOS user must check before Delete becomes enabled, or accept a single narrower 'I understand this is permanent' acknowledgement server-side instead of claiming all five were individually confirmed.
- **[lifecycle-12]** (P2 · bug · desktop-web · effort M) Settings > Broker connections' pending-proposal count for non-loaded accounts is dead code -- it always reads zero
  - **Where** `app/console/settings/brokers.tsx:191` · **Evidence** `const pendingCount = isCurrentLoaded ? (snapshot.pendingProposals ?? []).length : (snapshot.pendingProposals ?? []).filter(p => p.proposal?.accountId === account.id || p.accountId === account.id).length;` -- but `snapshot.pendingProposals` is built server-side in dashboard.ts:690 as `accountNumber…
  - **Why** The pending-proposal Chip on every non-loaded account row (line 281-285) will show nothing even when that account genuinely has pending proposals waiting (the scheduler runs every connected account's strategy independently of which is loaded) -- worse than simply omitting the count, this silently implies 'zero pending' when the true number is unknown. This is the same underlying gap the register already tracks at a…
  - **Fix** Fetch (or have the snapshot include) a per-account pending-proposal count across all connected accounts, not just the active one's, and wire brokers.tsx's non-loaded rows to it.
  - Sharpens `desktop-web-ux:dweb-12`
- **[lifecycle-07]** (P3 · defect · desktop-web · effort S) Disconnect confirmation claims immediate credential removal; actual deletion is deferred to a background scheduler drain/purge
  - **Where** `app/console/settings/brokers.tsx:504` · **Evidence** Confirm-dialog copy: "This removes the connection and its stored credentials from this app." Server-side, deleteConnectedAccount (src/lib/db-api-keys.ts:1373-1380) only sets `is_draining = 1, is_active = 0`; the row (including encrypted api_key/api_secret) and purgeConnectedAccount's actual DELETE…
  - **Why** The word 'removes ... from this app' reads as immediate, but nothing is actually deleted at click time -- the window this leaves open is exactly what lifecycle-01 exploits (the account is still fully live and reactivatable during the drain).
  - **Fix** Either make the disconnect path synchronous when there is nothing to drain (no open orders), or change the copy to say the removal completes once open orders are settled, and surface a visible 'disconnecting' state on the row in the interim.
- **[lifecycle-08]** (P3 · opportunity · cross-surface · effort M) No self-service data export exists before the irreversible, typed-phrase account deletion flow
  - **Where** `app/console/settings/danger.tsx:156` · **Evidence** danger.tsx:156-161 describes deletion as permanently removing "stored API keys, broker connections and OAuth tokens, settings, strategy profiles, watchlists, alerts, chat history, memories, proposals, fills, snapshots, notifications, and private learned context." `grep -rln "export\|dataExport\|GDP…
  - **Why** A user who wants to keep a record of their decision trace / fill / P&L history before deleting has no in-app way to get it out first; the only fallback (asking the operator to restore a 7-day backup) doesn't scale past a single-operator deployment and isn't mentioned anywhere in the deletion flow itself.
  - **Fix** Add a lightweight 'export my data' (JSON/CSV) action to Settings, offered as a step before the deletion flow's typed confirmation.
- **[lifecycle-09]** (P3 · improvement · backend · effort M) Alpaca/eToro/Public/Webull connect never validates credentials against the broker's API before marking the account 'connected' (Tradier does)
  - **Where** `app/api/connected-accounts/route.ts:190` · **Evidence** The Alpaca/eToro/public/webull path (lines 190-213) calls `upsertConnectedAccount(...)` directly with whatever the user typed, with no live probe. Compare the Tradier path (lines 218-250), which explicitly calls `gw.getAccounts()` against the real API to resolve the account number and will surface…
  - **Why** A typo'd Alpaca key/secret sails straight through readiness checklist step 1 ('Connect a broker' -> complete) and step 2 ('Select an active account' -> complete once loaded); the first real signal of trouble is a failed Run Once, at which point classifyRunFailure's generic 'account' bucket (chrome.tsx ~line 690) gives a vague 'Account problem' rather than 'these credentials were never checked.'
  - **Fix** Add a lightweight getAccounts()-style probe on Alpaca/eToro/Public/Webull connect, matching the Tradier path, and surface a clear 400 if it fails.
  - **Narrowed by verifier** The finding's contrast ('Tradier does [validate]') overstates Tradier's coverage — the gw.getAccounts() probe only runs on the no-account-number path, not on every Tradier connect. The core claim (Alpaca/eToro/Public/Webull never validate) is fully confirmed; Tradier validates in one of its two pat…
- **[lifecycle-10]** (P3 · defect · cross-surface · effort S) Switching into a live (real-money) account requires an explicit confirmation on iOS but is a single click on the website
  - **Where** `ios/SocraticTrade/HomeView.swift:1050` · **Evidence** iOS: tapping 'Use' on a non-paper account sets `confirmingLiveActivation = true`, which shows an alert ("Use Brokerage Account? ... Approved actions will use this account.", lines 1066-1071) before calling activate(). Website: chrome.tsx ScopeSelector's switchTo (lines 115-127) and brokers.tsx's Lo…
  - **Why** The exact same action -- making a live brokerage account the one Run Once/approvals will trade through -- has meaningfully different friction depending on which client the owner happens to be using, which is confusing on its own and, per the owner's 'no extra are-you-sure ceremony beyond a normal order confirmation' ruling, worth resolving in one direction deliberately rather than leaving as an accidental platform d…
  - **Fix** Pick one behavior (most likely: no confirm, matching the owner's stated preference against extra ceremony) and align both clients.

*Coverage:* Read end-to-end: app/welcome/page.tsx, app/login/page.tsx, middleware.ts, app/console/components/consent-gate.tsx, app/console/components/readiness-checklist.tsx + app/console/lib/derive.ts (deriveReadinessChecklist/deriveStateInfo/activeConnectedAccount), app/console/settings/brokers.tsx (full), app/console/components/chrome.tsx (ScopeSelector + RunOnceButton + classifyRunFailure + freshness/error display), app/console/lib/useConsoleData.tsx and api.ts (auth-error handling), app/api/connected-…

### Verifier-Added In The Gap Lanes (16)

- app/console/strategy/page.tsx:950-978 'Apply to this account' preset button has EVEN LESS friction than the Import flow (pages-01) for the identical strategyAuthority-flip risk: onClick goes straight from a single Btn click to `await copyProfileToAccount(profile.id, activeAccount.id)` with no warning box, no Sheet, no confirmation of any kind (contrast ImportFromAccountControl…
- app/console/strategy/page.tsx:966-967 activateProfile() fallback (the no-connected-account branch of the same handler) writes profile.policy — including strategyAuthority — into the base/library policy via activateStrategyProfile (src/lib/db-profiles.ts:685-705, `writePolicyBlobPreservingUserFields(userId, mergePolicy({...profile.policy, activeProfileId: id}), now)`), again wi…
- app/admin/layout.tsx (whole file) plus every app/admin/**/page.tsx (e.g. app/admin/backups/page.tsx, app/admin/transcript/page.tsx, app/admin/server/page.tsx): the entire /admin/* page tree has zero server-side or page-level admin gate -- AdminLayout and every page component are plain client components with no session/role check of their own. Today this is masked because only…
- app/api/admin/securities/import/route.ts lines ~41, ~74, ~84: audit calls for securities_import_rejected/securities_import_error/securities_import carry NO userId/actor argument at all (unlike every other admin audit() call in this codebase, e.g. app/api/admin/server-knobs/route.ts:81 which at least hardcodes local) -- the inbound inter-app ingest audit trail has no actor attr…
- app/admin/operations/operations-client.tsx (whole file): the Operations page lets an admin flip real runtime kill-switches/knobs (src/lib/server-knobs.ts) but shows no history of past knob changes anywhere in the UI -- every write is audited (server_knob.changed) but there is no way to see who/what changed a knob last and when without going around the product entirely (same ro…
- src/lib/rag/corpus-reembed.ts:1253 PURGE_CONFIRM_TOKEN plus the check at line 1319: the confirm string is a hardcoded literal compared with plain !==, not a constant-time compare -- unlike every ADMIN_REINDEX_TOKEN check in src/lib/auth/admin.ts which deliberately uses timingSafeEqualStr specifically to avoid a timing side-channel. Low-value target since it is a known fixed st…
- app/admin/page.tsx:216 ADMIN_PROBE_KEYS = [connections, llm, rag, server, r2] is a manually maintained list that must be kept in sync by hand with which admin/page.tsx probes exist -- the file's own comment admits this exact class of bug already bit it once (adding a seventh admin card would have broken it outright). Nothing enforces the list stays complete if a new probe is a…
- app/admin/data-catalog/page.tsx and app/admin/rag-coverage/page.tsx, unlike backups/connections/llm-usage/server/transcript siblings: these two page.tsx files set no export const metadata title at all, so their browser tab title falls back to the app-wide default instead of a page-specific one -- a minor but inconsistent pattern across the admin surface (5 of 9 sub-pages have…
- containPromptData's depth cap silently EXEMPTS deeply-nested content from prompt-injection sanitization instead of sanitizing it: src/lib/chat/orchestrator.ts:73-93, specifically line 85 `if (depth >= 8 || value === null || typeof value !== "object") return value;` -- once recursion hits 8 levels of object/array nesting it returns the WHOLE subtree unprocessed, including any s…
- Neither AnthropicLLM.run nor OpenAILLM.run bounds the number of tool_use/tool_calls blocks a single provider response can request, and the abort/deadline checks (llm.ts:395-414, 537-556) fire ONLY between the <=5 top-level steps -- never between individual tool executions inside one step's `for (const tu of toolUses)` loop (llm.ts:439-448) or `for (const tc of calls)` loop (ll…
- iOS's own client-side chat request timeout is shorter than the server's known worst-case processing time, with no cancel coordination: MobileAPIClient.swift:117 hardcodes `timeout: 90` seconds for sendChat, while app/api/chat/route.ts:166 sets the server's own soft deadline to `Math.max(15_000, CHAT_TURN_DEADLINE_MS ?? 120_000)` = 120s (and per coach-02/coach-09's own analysis…
- Real, actively-scanned S&P 500 constituents with a hyphenated share class (BRK-B, BF-B, ...) cannot be watched OR price-alerted at all -- both validators reject the hyphen the app's own canonical format requires. src/lib/alerts.ts:17 `SYMBOL_RE = /^[A-Z.]{1,10}$/` and src/lib/index-universes.ts:311 `isValidAppSymbol` regex `/^[A-Z][A-Z0-9.]{0,9}$/` both omit `-`, while src/lib…
- Price alerts can be armed on ANY symbol, watched or not -- the web form's own title says so verbatim ('Ticker to alert on -- any symbol, watched or not.', app/console/watchlist/page.tsx around the alert-symbol field) -- but the armed-alert list row (page.tsx:369-418) renders only symbol/op/threshold/status/note/timestamp, with NO live quote column. The Watchlist table above it…
- The actual root-cause fix for alerts-01 and alerts-02 (both silent-alert-outage findings) already exists in the codebase and is simply not used by alerts.ts. src/lib/quotes-cascade.ts:149 `fetchFreshQuotesCascade(symbols, userId, accountNumber?, connectedAccountId?)` takes accountNumber/connectedAccountId as OPTIONAL parameters and, per its own header comment (quotes-cascade.t…
- app/api/connected-accounts/route.ts:80-101 (Robinhood connect/Sync) + src/lib/db-api-keys.ts:1252-1295 (upsertConnectedAccount): re-syncing Robinhood matches an EXISTING row purely by `broker==="robinhood" && accountNumber===agentic.accountNumber` — with no is_draining filter on that lookup (listConnectedAccounts returns draining rows too) — and reuses that row's id. Because u…
- app/api/connected-accounts/route.ts:190-216 (generic upsert path for Alpaca/Tradier/eToro/Public/Webull) + src/lib/db.ts:3696-3712 (CREATE TABLE connected_accounts has no UNIQUE constraint on (user_id, broker, account_number)): unlike the Robinhood path, this branch never looks up an existing row by broker+accountNumber — `const connectedAccountId = body.id ?? crypto.randomUUI…

## C. Deduped Work Plan — 45 Clusters

Every finding in the register (Part I + Part II, minus what the second round refuted or found already fixed) folded into work items that share ONE root cause, each with the single change that closes the whole cluster.  This is the list to work from — not the raw finding count.

### Tranche 1 — Money Correctness, Wrong Numbers, Security, Silent Stalls (22)

#### `account-write-guards` — Account-scoped write paths have no guards: one click can arm Autopilot or reactivate a draining account
- **P1 · cross-surface · effort M** · 4 findings: `pages-01`, `lifecycle-01`, `lifecycle-07`, `lifecycle-11`
- **Root cause** importAccountSettings (src/lib/db-profiles.ts:895) strips only identity and user-level fields, so strategyAuthority copies through and a single "Import settings" click can flip a live account from Ask-first to Autopilot with none of the TypedConfirm ceremony app/console/guardrails/page.tsx:700 requires; applyProfileToAccount (db-profiles.ts:731) and activateStrategyProfile (:685) do not strip it at all. setActiveConnectedAccount (src/lib/db-api-keys.ts:1323) never reads is_draining, and nothing ever clears the flag that deleteConnectedAccount sets at :1381, so a disconnected account can be re-loaded as the live target while the scheduler drains and purges it.
- **The one change** Treat strategyAuthority and is_draining as account-owned state in src/lib/db-profiles.ts:685/731/895 and src/lib/db-api-keys.ts:1323 — preserve authority the way systemState already is, refuse or clear draining on activate and on re-upsert — and route any authority change through the existing typed-confirm gate.

#### `broker-io-deadlines` — Broker I/O is unbounded in time and scope, and the protective scheduler lanes latch on it
- **P1 · backend · effort M** · 6 findings: `backend-reliability:berel-03`, `backend-reliability:berel-08`, `backend-reliability:berel-09`, `trading-money-path:tsx-05`, `frontend-performance:dweb-02`, `backend-reliability:berel-15`
- **Root cause** The Alpaca SDK is constructed with no timeout (src/lib/alpaca.ts:247; axios default 0), so getLatestQuotes/createOrder/cancelOrder can hang indefinitely — and the stop-monitor and stale-exit lanes clear their in-flight key only in .finally (src/lib/scheduler.ts:919/931) with no withDeadline, so one hung socket latches the lane and that account's synthetic stops stop being evaluated forever. The tick itself has no re-entrancy guard (scheduler.ts:411). Separately getEquityOrders asks for status:"all", limit 500 with pagination (alpaca.ts:502/508-518) on ~20 call sites including a 15s dashboard poll and two 60s scheduler lanes, and the trade-updates stream has no idle watchdog (src/lib/streams…
- **The one change** Pass a deadline into every Alpaca/Tradier call and wrap each scheduler lane in withDeadline the way src/lib/safety-maintenance.ts:69-78 already does, and scope getEquityOrders (src/lib/alpaca.ts:501) to open orders plus a bounded date window.

#### `cash-flow-and-trading-day` — Day P&L reads $0.00 and shorts mint phantom deposits: flows are inferred, not read, and the day is not the trading day
- **P1 · cross-surface · effort L** · 8 findings: `performance-accounting:perf-02`, `performance-accounting:perf-03`, `performance-accounting:perf-08`, `performance-accounting:perf-14`, `performance-accounting:perf-16`, `performance-accounting:perf-22`, `market-data-integrity:mdi-13`, `trading-money-path:tsx-17`
- **Root cause** app/console/lib/derive.ts:443 calls inferExternalCashFlows([baseline, fakeCurrent], []) with a hardcoded empty fill list, so on any day the strategy trades directionally tradeCash is 0 (src/lib/cash-flows.ts:157-164), the guard at :187-208 sets flow = deltaEquity, and Home prints exactly $0.00 (0.00%). src/lib/cash-flows.ts:164 also gives short/cover the wrong cash sign, minting a 2x-notional phantom deposit per short. Day keys are UTC (cash-flows.ts:33/129-142) while owner timestamps are Central, the risk-breaker HWM is UTC and not flow-adjusted (src/lib/risk-breaker.ts:86) and daily caps roll at NY midnight (src/lib/db-execution.ts:15). The broker's own equity/cash-activity ledger is impl…
- **The one change** Make the broker cash-activity ledger the flow source — wire src/lib/alpaca-account-insights.ts:167 into the dashboard and benchmark paths — and keep inference only as a labeled fallback with the short/cover sign, the real fill list and one shared Central trading-day helper reused by risk-breaker.ts:86 and db-execution.ts:15.

#### `coach-tools-and-turns` — Coach coerces invalid tool inputs instead of rejecting them, and cannot be cancelled mid-call
- **P1 · cross-surface · effort M** · 8 findings: `coach-01`, `coach-02`, `coach-07`, `coach-08`, `coach-09`, `coach-10`, `coach-11`, `api-contract-web-ios:api-08`
- **Root cause** src/lib/chat/tools.ts:92 maps any side that is not exactly "sell" — "Sell", "short", a typo — to "buy", the opposite economic direction, and :94/96 silently drops a limit price the same way; downstream promote-draft only re-checks that side is buy|sell, which is trivially true after coercion. qty carries no unit and there is no notional field (tools.ts:58), and k has no maximum (tools.ts:155) unlike its clamped siblings. Separately no transport receives args.abortSignal (src/lib/chat/llm.ts:332-346, :688-705), so Cancel and the 120s deadline cannot stop an in-flight provider call, and exhausting the 5-step loop returns only the disclaimer (llm.ts:436-459).
- **The one change** Fail closed on out-of-enum tool inputs and clamp/convert numerics server-side in src/lib/chat/tools.ts (mirroring normalizeAlertOp's fail-closed shape), and thread args.abortSignal into every transport in src/lib/chat/llm.ts so cancel and the turn deadline actually abort.

#### `console-ships-too-much` — The console ships server DB internals to the browser and the whole universe on every 15s poll
- **P1 · desktop-web · effort M** · 7 findings: `frontend-performance:dweb-01`, `frontend-performance:dweb-03`, `frontend-performance:dweb-04`, `frontend-performance:dweb-05`, `frontend-performance:dweb-15`, `api-contract-web-ios:api-04`, `api-contract-web-ios:api-15`
- **Root cause** app/console/settings/brokers.tsx:12 value-imports @/lib/venue-contract, which pulls source-settings → db-api-keys → db into the client bundle: SQL schema, migrations and key-store logic reach the browser, and this exact chain produced the 2026-08-18 'process.uptime is not a function' production crash (fixed symptomatically in 01f4e73e2, whose own comment names the chain). No module in the repo imports the server-only guard. On the payload side src/lib/dashboard.ts:1108 serializes raw audit[], the full 500-symbol quotesBySymbol and the entire order history into a 15s poll (app/console/lib/useConsoleData.tsx:23), and post-action refreshes are served from the 10s cache so acked alerts stay on…
- **The one change** Add server-only to the src/lib/db* modules and split the pure mergeAccountCapabilities helper out of venue-contract so brokers.tsx no longer drags the DB layer, then project the snapshot at src/lib/dashboard.ts:1108 down to the fields the UI actually reads and bust the cache on mutation.

#### `event-loop-pins` — RAG mirroring pins the single serving+trading event loop for minutes at a time
- **P1 · backend · effort L** · 7 findings: `backend-reliability:berel-01`, `backend-reliability:berel-04`, `backend-reliability:berel-06`, `backend-reliability:berel-07`, `backend-reliability:berel-10`, `backend-reliability:berel-11`, `backend-reliability:berel-12`
- **Root cause** src/lib/db-learning.ts:1613-1616 makes idempotency a DELETE with equality on non-rowid FTS5 columns, which FTS5 serves as a full-corpus scan — measured 4.3ms/row at 10k rows rising to 26.3ms/row at 60k, and ~300ms/chunk at the live corpus per the repo's own receipt in src/lib/rag/fts-mirror-bound.ts:20-23, while the INSERT it was blamed on costs 0.1-0.4ms. src/lib/rag/persist-local-complete.ts:46-54 and src/lib/web-sources/sec-filings.ts:624-631/736-756 then loop that call over a whole filing with no yield, no transaction, and no hasInFlightStrategyWork() gate (scheduler.ts:589-602) — unlike the bounded SEC worker at sec-ingest-worker.ts:462-475. There is no event-loop lag instrumentation a…
- **The one change** Give the FTS mirror a rowid/side-key so idempotency becomes an indexed lookup instead of a scan (src/lib/db-learning.ts:1613 plus the DDL at src/lib/db.ts:2148), and route every mirror caller through the existing planFtsMirrorSlice + yieldEventLoop + strategy gate the SEC worker already uses.

#### `green-request-schema` — Bull strict JSON schema is invalid, so every OpenAI-family Green seat 400s before generation
- **P1 · backend · effort S** · 2 findings: `llm-agent-architecture:llm-01`, `llm-agent-architecture:llm-08`
- **Root cause** src/lib/strategy.ts:5870 declares exitPlan inside the Bull item's properties while BULL_PROPOSAL_REQUIRED_KEYS (strategy.ts:6653, used at :5851 for the required array) omits it, under additionalProperties:false — OpenAI strict mode requires required ⊇ properties, so any native-OpenAI or openai/* routed seat is rejected at the API layer 100% of the time (the fast 400 misdiagnosed as 'terra unservable' in #2831). The same file then trusts provider schema enforcement on json_object transports (strategy.ts:6115) where nothing enforces it.
- **The one change** Derive required from properties at src/lib/strategy.ts:5851 (or add exitPlan to BULL_PROPOSAL_REQUIRED_KEYS at :6653), add the invariant test that required ⊇ properties, and add a post-parse validator for the json_object transports.

#### `home-proposal-rows` — Home's latest-run rows use synthetic ids and a private status map: green chips on failures, an Approve button that 404s, no keyboard access
- **P1 · desktop-web · effort M** · 3 findings: `desktop-web-ux:dweb-01`, `desktop-web-ux:dweb-18`, `web-accessibility:a11y-01`
- **Root cause** app/console/page.tsx:79 composes the row id as `${runId}-${symbol}-${status}` while real proposal ids are crypto.randomUUID (src/lib/strategy.ts:3361), so handleApprove always 404s at app/api/proposals/[id]/approve/route.ts:26 — and page.tsx:1011 offers that button on 'blocked' rows, a routine strategy outcome. page.tsx:1025's local chipTone knows only 'blocked'/'failed'/'pending', so the real statuses 'error' and 'proposed' fall through to green while labels.ts renders the word "Failed". The row itself is a click-only div (page.tsx:1029-1030) with no role, tabIndex or key handler, so the details sheet and its Approve flow are unreachable by keyboard.
- **The one change** Render those rows from the persisted proposals (real ids) through the shared helpers in app/console/lib/action-verbs.ts / labels.ts, and make the row a keyboard-operable control with the nested SymbolButton excluded from the activation target.

#### `identity-fails-open` — Identity resolution falls back to the owner instead of failing closed
- **P1 · backend · effort M** · 3 findings: `security-auth:sec-01`, `security-auth:sec-07`, `security-auth:sec-08`
- **Root cause** middleware.ts:385-388 sets trustedEmail = PRIMARY_EMAIL whenever isAuthConfigured() is false, and nothing asserts AUTH_SECRET in production — instrumentation.ts:47 asserts only ENCRYPTION_KEY — so a selective env regression makes every anonymous request the owner, with x-authenticated-user-email stamped at middleware.ts:420 and no route outside src/lib/auth/admin.ts checking identity provenance. The same shape repeats at src/lib/request-user.ts:28-34 (missing header → DEV_USER_ID 'local') and across 228 db functions that default userId="local" (src/lib/db-learning.ts:12); middleware.ts:435's matcher additionally skips any path ending in a static extension.
- **The one change** Assert AUTH_SECRET at boot when DB_BOOTSTRAP=live alongside the existing ENCRYPTION_KEY assert (instrumentation.ts:47), gate the middleware.ts:385 local fallback on that same production marker (the dev branch is pinned by ~20 tests, so add rather than remove), and drop the userId="local" defaults so a forgotten argument is a type error.

#### `ios-state-outcome-truth` — iOS shows the previous account's data after sign-out and never surfaces the outcome of a money action
- **P1 · ios · effort M** · 8 findings: `ios-engineering:ios-01`, `ios-engineering:ios-04`, `ios-engineering:ios-07`, `ios-engineering:ios-14`, `ios-engineering:ios-15`, `ios-hig-ux:ios-01`, `ios-hig-ux:ios-11`, `api-contract-web-ios:api-12`
- **Root cause** clearLocalSession (ios/SocraticTrade/MobileStore.swift:628-650) nils in-memory state but never removes the UserDefaults blob at :136-144, so a cold launch after sign-out or account deletion re-reads it, sets isAuthenticated from disk, and offline renders the prior account's holdings as current; init also stamps that cached blob lastUpdatedAt = Date() so it shows "Updated · now". On the write side, store.error has exactly one renderer (SnapshotScaffold, AppComponents.swift:566) which sits at scroll-offset 0 behind the settings sheet and is cleared by load() at MobileStore.swift:292, so a failed guardrail edit is invisible; 401 handling differs per passthrough (:463) and success has no acknow…
- **The one change** Make one cache/session owner on the Swift side — clear the UserDefaults snapshot (and move the cookie to the Keychain) inside clearLocalSession, persist the real capture time, and render store.error and success as an .alert/toast anchored to the presenting view instead of a card at scroll-top that load() wipes.

#### `market-cache-freshness` — Calendar-day freshness math freezes screener, OHLC and enrichment caches during live sessions
- **P1 · backend · effort M** · 7 findings: `market-data-integrity:mdi-01`, `market-data-integrity:mdi-02`, `market-data-integrity:mdi-08`, `market-data-integrity:mdi-10`, `market-data-integrity:mdi-11`, `market-data-integrity:mdi-12`, `market-data-integrity:mdi-15`
- **Root cause** Freshness is measured in calendar days, not trading sessions. src/lib/market-hours.ts:283-311 isWeekendOrHolidayClosureAhead returns true for the whole of Friday (and the whole day before any holiday), so expiresAtRespectingMarketClose pushes a 5-minute screener TTL to Monday's open — verified: a Fri 10:00 ET write expires Mon 09:30 ET. src/lib/history.ts:115-126 isBarSeriesFresh accepts a 3-calendar-day-old last bar, so a Monday write suppresses any refetch through Tuesday and Wednesday. The same module has no early-close calendar (market-hours.ts:35), and the universe/history cascades below it mix adjustment bases and a 2026-06-14 static index snapshot.
- **The one change** Replace the calendar-day helpers with a session-aware pair — extend TTL only after the session actually closes (src/lib/market-hours.ts:288/305) and measure bar freshness in elapsed sessions (src/lib/history.ts:115) — and add the half-day/early-close table the same module needs.

#### `order-provenance-guard` — Automation cancel-replaces orders it does not own (bracket take-profit legs, owner GTC sells, manually cancelled stops)
- **P1 · backend · effort M** · 4 findings: `trading-money-path:tsx-02`, `trading-money-path:tsx-14`, `trading-money-path:tsx-08`, `ops-observability-admin:sre-12`
- **Root cause** No enqueue or pump path checks who placed the order. src/lib/order-replacement.ts:754-757 filters only on side and state==="held", so a just-activated bracket take-profit leg (state "new", 15 min after entry fill) passes; the pump re-checks only type/held/quantity at :333-364 and never reads EquityOrder.orderClass (src/lib/types.ts:939, mapped at src/lib/alpaca.ts:942); it then cancels and places a MARKET order at :452-477. The protective-stop reconciler (src/lib/order-cancel.ts:198) re-places a stop the owner just cancelled, contradicting the cancel sheet's "No new order is placed."
- **The one change** Add an order-provenance check (orderClass/legs + app-placed clientOrderId + a manual-cancel tombstone) at src/lib/order-replacement.ts:754 and src/lib/order-cancel.ts:198 so bracket legs and owner-placed/owner-cancelled orders are never auto-replaced, and drop the per-tick deferral spam plus the misleading alert at order-replacement.ts:760-767.

#### `per-account-visibility` — Multi-account screens show the active account's data labeled as every account's
- **P1 · desktop-web · effort M** · 5 findings: `lifecycle-02`, `lifecycle-12`, `desktop-web-ux:dweb-12`, `pages-04`, `api-contract-web-ios:api-10`
- **Root cause** app/console/settings/brokers.tsx:189 computes stateInfo only when isCurrentLoaded, so every other connected account chips a static "Inactive" even while it is genuinely running Autopilot (the scheduler runs each account independently, src/lib/scheduler.ts:810); brokers.tsx:191 then filters snapshot.pendingProposals, which src/lib/dashboard.ts:690 already scoped to the active account, so the per-account pending count is structurally always zero. app/console/decisions/page.tsx:28 fetches /api/socratic/decisions with no connectedAccountId although the route supports it, interleaving both accounts' BUY/SELL traces with no per-row label.
- **The one change** Feed every per-account row from snapshot.connectedAccountPolicies (already built at src/lib/dashboard.ts:350-368 and already used by chrome.tsx:141) plus a real per-account pending count, and scope /console/decisions by connectedAccountId with a visible account chip per row.

#### `placement-outcome-truth` — Approve reports success when nothing was placed, and misfiles retryable broker errors as terminal
- **P1 · cross-surface · effort L** · 6 findings: `api-contract-web-ios:api-02`, `trading-money-path:tsx-03`, `trading-money-path:tsx-15`, `trading-money-path:tsx-12`, `trading-money-path:tsx-04`, `trading-money-path:tsx-01`
- **Root cause** executeProposal returns a status object instead of throwing for every non-placement outcome (src/lib/strategy-execution.ts:239-246 declares Promise<{status:string}>; busy at :298, blocked at :498/766/1113/1136/1251/1302, not_placed at :1338/1394, error at :1319-1473) and every consumer treats "resolved" as "placed": src/lib/mobile-api.ts:1080 stamps finishCommand(command,"succeeded",result), the Swift MobileCommand has no result field (ios/SocraticTrade/MobileModels.swift:511-520) so ProposalsView.swift:481 paints a green "Approved", and the same resolver files any HTTP 4xx — 429 and 408 included — as terminal rejected_by_broker at src/lib/strategy-execution.ts:1279.
- **The one change** Introduce one PlacementOutcome resolver at src/lib/strategy-execution.ts:1279/:239 that classifies {placed, blocked, busy, retryable, rejected}; return it verbatim with an honest command status at src/lib/mobile-api.ts:1080, decode result.status/reasons in ios/SocraticTrade/MobileModels.swift:511, and render it on both clients the way app/console/components/approval-card.tsx:347-366 already does.

#### `price-alert-evaluation` — Price alerts stop evaluating silently — no active account, one broker, swallowed errors, no age check
- **P1 · backend · effort M** · 5 findings: `alerts-01`, `alerts-02`, `alerts-05`, `alerts-06`, `alerts-09`
- **Root cause** src/lib/alerts.ts:67-69 returns [] whenever the console-active account has no accountNumber (zero connected accounts, or the active one was deleted — deleteConnectedAccount at src/lib/db-api-keys.ts:1376 promotes nobody), even though PriceAlert rows are user-scoped; :73-78 catches any gateway throw in an empty catch with no log or audit, so a Public.com/eToro API error is indistinguishable from "nothing to check"; :83 acts on the price with no staleness check although quoteAgeSecForStalenessGate exists; and :17's SYMBOL_RE disagrees with isValidAppSymbol (src/lib/index-universes.ts:311), so malformed symbols sit armed forever. Zero tests cover the evaluation loop.
- **The one change** Switch checkPriceAlerts to the user-scoped fetchFreshQuotesCascade(symbols, userId) (src/lib/quotes-cascade.ts:149) instead of gateway.getEquityQuotes on the active account — this closes the no-account gate and the single-broker error in one edit — then log the caught error, apply quoteAgeSecForStalenessGate, share isValidAppSymbol, and add the evaluation tests.

#### `quote-value-provenance` — Quote pipeline fabricates asOf and picks the wrong field as price, defeating the staleness gate
- **P1 · cross-surface · effort L** · 9 findings: `market-data-integrity:mdi-03`, `market-data-integrity:mdi-04`, `market-data-integrity:mdi-05`, `market-data-integrity:mdi-06`, `market-data-integrity:mdi-07`, `market-data-integrity:mdi-09`, `market-data-integrity:mdi-16`, `market-data-integrity:mdi-17`, `market-data-integrity:mdi-18`
- **Root cause** Values that were not just observed are stamped with the wall clock: src/lib/data-providers.ts:1571-1590 takeScalar writes cascadeFetchedAt = new Date().toISOString() for any field lacking its own asOf, so a 6h-cached nasdaq-quote volume wins recency arbitration at src/lib/evidence-facts.ts:132-155 and overwrites base.volume; src/lib/market.ts:1548 (Yahoo quote-only) and src/lib/quotes-cascade.ts:397 (ROIC profile price) hardcode asOf: new Date(), so policy.ts:404-413's opening-order staleness gate always reads ~0s old; and src/lib/alpaca.ts:551-569 returns the ASK as price, which becomes the displayed price, watchlist price, alert trigger and stop mark.
- **The one change** Make asOf non-forgeable — carry the provider's own observation timestamp (or leave it undefined, which src/lib/quotes-cascade.ts:47-61 already treats as not-fresh) through takeScalar, toQuoteOnlyMarketQuote and the ROIC/CBOE/VWAP tiers, use mid/last instead of ask at alpaca.ts:551, and surface the resulting age in the drilldown, watchlist and scan chips.

#### `run-scoped-account` — Run-scoped code re-resolves the console-active account instead of the account being traded
- **P1 · backend · effort M** · 3 findings: `llm-agent-architecture:llm-02`, `llm-agent-architecture:llm-05`, `coach-04`
- **Root cause** src/lib/red-team.ts:192 calls getActiveConnectedAccount(userId) and :203 calls getStrategyPrompt(userId) with no account argument, even though debateProposal was handed runPolicy carrying the correct connectedAccountId — so with two accounts scheduled concurrently the adversarial reviewer can veto on the wrong venue's short capability and reason from another account's custom strategy prompt (deriveExecutionState at :196 has the same bug). src/lib/learned-context/store.ts:401 appends approved directives to the active account's prompt regardless of the pending row's account, and chat turns carry no account at all (src/lib/types.ts:3011) so the 10-turn replay mixes accounts.
- **The one change** Thread the owning connectedAccountId explicitly through src/lib/red-team.ts:192/196/203, src/lib/learned-context/store.ts:401 and the chat_turns row plus its replay window, and delete every getActiveConnectedAccount call from run-scoped paths.

#### `web-ios-contract-drift` — Hand-mirrored Swift decoders drift from route payloads with no contract test — live stop-losses render as em-dashes
- **P1 · cross-surface · effort M** · 11 findings: `api-contract-web-ios:api-01`, `api-contract-web-ios:api-06`, `api-contract-web-ios:api-13`, `api-contract-web-ios:api-14`, `api-contract-web-ios:api-16`, `qa-test-strategy:qa-04`, `qa-test-strategy:qa-18`, `performance-accounting:perf-13`, `ios-hig-ux:ios-05`, `api-contract-web-ios:api-11`, `api-contract-web-ios:api-17`
- **Root cause** FullPolicy decodes stopLossPct/trailingStopPct/shortStopLossPct from top-level CodingKeys (ios/SocraticTrade/DeskModels.swift:383-388) while GET /api/policy returns them nested under riskRules (src/lib/types.ts:1188, route at app/api/policy/route.ts:42), so every iPhone Guardrails view shows em-dashes for a configured 8% stop. Nothing ties the two sides together: app/api/mobile/snapshot/route.ts:16-54 is a bare object literal, MobileModels.swift:41-67 decodes every list with `?? []` so a rename blanks a screen silently, and the only Swift decode test is a hand-authored fixture (MobileModelsTests.swift:409). The same class drops recordedAt, benchmarkUnavailable and benchmarkSymbol.
- **The one change** Emit a shared fixture from the real routes (snapshot + policy JSON) and decode it in both `npm test` and the XCTest suite, failing on absent or unknown keys — starting by flattening riskRules in app/api/policy/route.ts:42 or decoding the nested container in DeskModels.swift:372.

#### `approval-path-integrity` — Approving is starved by the run lock and the size that gets placed is not the size that was reviewed
- **P2 · cross-surface · effort L** · 6 findings: `trading-money-path:tsx-09`, `trading-money-path:tsx-10`, `trading-money-path:tsx-11`, `llm-agent-architecture:llm-06`, `api-contract-web-ios:api-05`, `alerts-04`
- **Root cause** executeProposal contends for the whole-run strategy lock (src/lib/strategy-execution.ts:297), so an approval during a 15-20 minute autonomous run returns busy — the console gives up after ~2 min and iOS shows a green check. Mobile approvals also queue FIFO behind strategy.run_once because they are absent from IMMEDIATE_MOBILE_COMMAND_TYPES (src/lib/mobile-api.ts:88), whose claim loop is globally ordered with no per-user partition. On the sizing side, approval-time ratio re-anchoring moves EXIT limits with the market (src/lib/approval-reprice.ts:100), a hard-coded 5% headroom shrinks the owner's own cap (src/lib/policy.ts:567), and Retry Red Team stamps approve-at-half without applying the h…
- **The one change** Give approvals a per-account lease instead of the whole-run lock at src/lib/strategy-execution.ts:297 (and add the non-broker command types to the immediate set at mobile-api.ts:88), and make the reprice, haircut and headroom rules apply to the order that is actually placed rather than to the displayed card.

#### `pnl-basis-labels` — Performance tiles print numbers without the basis or window they were computed on
- **P2 · cross-surface · effort M** · 7 findings: `performance-accounting:perf-04`, `performance-accounting:perf-05`, `performance-accounting:perf-06`, `performance-accounting:perf-15`, `performance-accounting:perf-18`, `performance-accounting:perf-17`, `performance-accounting:perf-19`
- **Root cause** src/lib/performance.ts:497 caps the "Versus the market" TWR at ~100 snapshots (≈7 trading days) while app/console/results/page.tsx frames it as account performance; "Realized/Unrealized P&L" means lot-ledger on Results, broker cost on Home and broker positions on iOS with no label on any of them (results/page.tsx:957-994); win rate 0% and avg return +0.00% render for accounts with zero closed lots; app/console/lib/derive.ts:804 divides by NET signed cost basis so shorts inflate the percentage; and performance.ts:1664 fabricates a $100 paper equity base that is rendered as money.
- **The one change** Attach an explicit basis + window label (and an em-dash empty state instead of 0%) to every performance figure at app/console/results/page.tsx:940-1000, ios/SocraticTrade/ResultsView.swift and app/console/lib/derive.ts:804, sourced from the same struct that computed the number.

#### `prompt-trust-boundary` — Non-owner content can reach the always-trusted strategy prompt with no human and a scanner that is inert in production
- **P2 · backend · effort M** · 5 findings: `llm-agent-architecture:llm-04`, `llm-agent-architecture:llm-13`, `llm-agent-architecture:llm-18`, `coach-05`, `coach-06`
- **Root cause** src/lib/learning-review.ts:640-663 applies any 'keep' verdict with no riskTier exclusion, and applyApprovedPending (src/lib/learned-context/store.ts:397-405) merges strategy-directive rows straight into the strategy prompt — the one region the app never scans (src/lib/prompt-safety.ts:224) — while learningReviewMode defaults to 'decide' (src/lib/defaults.ts:90). Candidates get there from src/lib/chat/coach-learning.ts:567, which stores 480 chars of arbitrary fetched page text as auto-write 'fact' tier, past a classifier scoped to financial-risk vocabulary; the second-layer semantic gate resolves MockLLM in production (src/lib/learned-context/semantic-gate.ts:124); and app/api/chat-history/r…
- **The one change** Apply the injection scan at ingest rather than only at prompt assembly, and require a human for anything that lands in the strategy prompt regardless of review mode — src/lib/chat/coach-learning.ts:567, src/lib/learning-review.ts:660, src/lib/learned-context/store.ts:401 — plus delete the unused POST writer at app/api/chat-history/route.ts:28.

#### `realized-pnl-ledger` — Realized-P&L ledger reads the oldest fills and mismatches lots, so P&L, win rate, tax and wash-sale locks are wrong
- **P2 · backend · effort L** · 9 findings: `performance-accounting:perf-01`, `backend-reliability:berel-02`, `performance-accounting:perf-07`, `performance-accounting:perf-09`, `performance-accounting:perf-10`, `performance-accounting:perf-11`, `performance-accounting:perf-12`, `performance-accounting:perf-21`, `performance-accounting:perf-20`
- **Root cause** src/lib/db-fills.ts:332 is SELECT ... ORDER BY filled_at ASC LIMIT ? — the OLDEST N rows — while the sibling listPortfolioSnapshots (db-fills.ts:221-243) was already fixed to a newest-DESC subquery for this exact bug; all 16 callers pass 500 and src/lib/strategy-tuning.ts:418 passes 30, so the paid tuner's "recentFills" are provably the account's first 20 fills. The matcher on top is also wrong: closes with no app lot are silently dropped (src/lib/performance.ts:599), a lot is graded on its first partial exit only (src/lib/outcome-engine.ts:966), partial closes count as round trips (performance.ts:1707), short-side lots are skipped in tax (src/lib/tax.ts:352), a synthetic 20bps haircut is a…
- **The one change** Fix the read to the newest-DESC subquery already used at db-fills.ts:221-243 and make the lot matcher round-trip aware in src/lib/performance.ts:599-1707 (group partials per lot, book unmatched closes into an explicit pre-app bucket, include short lots in tax, drop the synthetic haircut).

### Tranche 2 — Reliability, Gates, Daily-Use Surfaces (16)

#### `copy-claims-and-rulings` — Shipped copy asserts guardrails the engine does not have and reintroduces the paper/live ceremony the owner ruled out
- **P1 · cross-surface · effort M** · 20 findings: `ux-copy-editor:copy-03`, `ux-copy-editor:copy-11`, `ux-copy-editor:copy-12`, `ux-copy-editor:copy-13`, `ux-copy-editor:copy-16`, `pages-07`, `public-site-growth:dpub-03`, `public-site-growth:dpub-04`, `public-site-growth:dpub-05`, `public-site-growth:dpub-12`, `public-site-growth:dpub-18`, `public-site-growth:dpub-19`, `llm-agent-architecture:llm-19`, `api-contract-web-ios:api-19` …
- **Root cause** app/console/macro/indicators.ts:698/706 and again at :753 tell the owner that below-median buys are "hard-vetoed" in risk-off/crisis regimes, while src/lib/strategy-risk.ts:187-197 keeps and tags them (its own comment reads 'advisory pre-veto (tag-not-drop)') and socraticOverrideMode defaults to 'execute'; app/how-it-works/page.tsx:94 lists delegation controls that do not exist and app/framework/content.ts:226 states a sizing invariant the override path contradicts. The same class of copy shouts "(NOT Real Money)" / red Live pills (app/console/settings/brokers.tsx:626, ios/SocraticTrade/ProposalsView.swift:366) and adds an iOS-only confirm for live activation the web does not have.
- **The one change** Run one accuracy pass that rewrites every guardrail claim to the engine's actual advisory behavior and strips the paper/live ceremony, sourcing the guardrail-semantics sentences from a single shared constant so console, iOS and the public pages cannot drift apart again.

#### `dead-controls` — Shipped controls whose backend does nothing, or something other than the label claims
- **P1 · cross-surface · effort M** · 7 findings: `pages-02`, `pages-03`, `pages-05`, `desktop-web-ux:dweb-07`, `ops-observability-admin:sre-20`, `admin-03`, `admin-06`
- **Root cause** policy.taxSettings.subtractFromResults is written by app/console/strategy/tax-settings.tsx:213 and read by nothing in the Results render path (app/console/results/page.tsx:940-994 renders P&L unmodified), so "Show results net of estimated tax" changes no number in either direction. The Webhook channel's copy promises Discord embeds that only the separate legacy webhookUrl path builds (app/console/settings/delivery.tsx:37 vs src/lib/notify.ts:262 and src/lib/notifications.ts:330-372), and "Send test" never probes that legacy field. Presets has no create/rename/delete although /api/profiles implements POST/PUT/DELETE; the admin nav omits learning-ledger revert and the RAG backfills entirely;…
- **The one change** For each control, either wire it to the behavior its label claims or remove it — starting with subtractFromResults in app/console/results/page.tsx:940-994, the legacy webhook probe in /api/notifications/test, and preset CRUD against the existing /api/profiles routes.

#### `merge-gate-blindspots` — The required merge gate never compiles or runs the iOS app, and can be skipped entirely for build-imported files
- **P1 · cross-surface · effort M** · 5 findings: `qa-test-strategy:qa-01`, `qa-test-strategy:qa-02`, `qa-test-strategy:qa-03`, `qa-test-strategy:qa-11`, `ios-engineering:ios-09`
- **Root cause** .github/workflows/ci.yml:305's required `verify` job depends only on classify + verify-hosted and never on ios-build.yml, which itself only runs `xcodebuild build` (.github/workflows/ios-build.yml:70-78) — `xcodebuild test` appears nowhere in .github/ or scripts/, so 15 XCTest files covering order-cancel and price-review logic never execute before TestFlight. ci.yml:141's classify regex treats all of docs/** as documentation even though app/api/llm-usage/model-stats/route.ts:9/19 imports docs/benchmarks/*.json at build time, so such an edit auto-merges with tsc/test/build skipped and breaks first in the production deploy. scripts/land.sh:156 runs the same trio and never compiles Swift.
- **The one change** Add an `xcodebuild test` job, make the Swift job a required context in the main-protection ruleset next to `verify`, and exclude build-imported paths from the docs-only regex at ci.yml:141 (mirrored in e2e.yml:77 and scripts/land.sh:156).

#### `phone-touch-viewport` — The phone build inherits desktop control sizes and ignores the visual viewport
- **P1 · mobile-web · effort M** · 15 findings: `mobile-web-ux:mweb-01`, `mobile-web-ux:mweb-05`, `mobile-web-ux:mweb-06`, `mobile-web-ux:mweb-07`, `mobile-web-ux:mweb-08`, `mobile-web-ux:mweb-09`, `mobile-web-ux:mweb-10`, `mobile-web-ux:mweb-12`, `mobile-web-ux:mweb-13`, `mobile-web-ux:mweb-16`, `mobile-web-ux:mweb-18`, `mobile-web-ux:mweb-19`, `mobile-web-ux:mweb-21`, `web-accessibility:a11y-13` …
- **Root cause** app/console/components/chrome.tsx:326 renders the full 'Stop Agent'/'Resume Agent' label in a flex-shrink:0 button (console.css:471-484) alongside a non-shrinking StateChip, avatar and ⌘K trigger, so the only min-w-0 sibling — the account scope selector, the control naming which brokerage account is loaded — collapses at 360-390px; the file's own comment computed its ~73px budget against the older short STOP label. The coarse-pointer 44px floor covers only .con-btn (console.css:458), overlays neither lock scroll nor size with dvh (console.css:858, :1220), inputs at 15px/11px trigger iOS auto-zoom (console.css:1251), and sheets ignore browser history.
- **The one change** Apply the coarse-pointer 44px floor and a 16px input minimum to every interactive class rather than just .con-btn, truncate or icon-ify the chrome run-state label below the sm breakpoint, and give every overlay scroll-lock plus dvh/visualViewport sizing and a history entry.

#### `signin-return-first-run` — Entry paths lose their destination or dead-end: expired sessions, OAuth errors, and iOS first-run
- **P1 · cross-surface · effort M** · 11 findings: `public-site-growth:dpub-01`, `public-site-growth:dpub-02`, `public-site-growth:dpub-17`, `lifecycle-03`, `lifecycle-04`, `lifecycle-05`, `lifecycle-06`, `lifecycle-09`, `ios-hig-ux:ios-09`, `ux-copy-editor:copy-14`, `desktop-web-ux:dweb-16`
- **Root cause** middleware.ts:412-419 answers an unauthenticated /api/* with a bare 401 and drops the destination on page redirects, so a push deep link lands on the dashboard after sign-in and app/console/lib/useConsoleData.tsx:139-150 treats that 401 as any transient error — the tab polls stale data forever and Run once reports a generic 'The run failed'. Every Auth.js error routes to /access-denied (src/lib/auth/auth.ts:197) with no way out, and /login renders for already-authenticated users. On iOS the first-run checklist says to connect a broker 'in Account & Settings' (ios/SocraticTrade/HomeView.swift:87) while that very screen's empty state says to use the website (:844), and readiness ignores the L…
- **The one change** Carry the destination through sign-in and special-case 401 in the console fetch client (stop polling, route to /login), and make the iOS first-run checklist either open the AdminPortalView cookie-sharing web view or state plainly that connecting happens on the website.

#### `visual-tokens-theme-contrast` — Tone tokens fail WCAG AA at the sizes they are used, and light-default is not the default in several hosts
- **P1 · cross-surface · effort M** · 14 findings: `web-accessibility:a11y-02`, `web-accessibility:a11y-15`, `web-accessibility:a11y-17`, `mobile-web-ux:mweb-25`, `ios-hig-ux:ios-07`, `mobile-web-ux:mweb-02`, `mobile-web-ux:mweb-03`, `mobile-web-ux:mweb-04`, `desktop-web-ux:dweb-13`, `public-site-growth:dpub-15`, `frontend-performance:dweb-07`, `frontend-performance:dweb-17`, `ios-hig-ux:ios-13`, `ios-hig-ux:ios-18`
- **Root cause** app/console/console.css:40-73 defines tone text on soft fills that measures 3.68-4.34:1 (independently recomputed) at 11px/12.5px — below the 4.5:1 bar since neither size is 'large text' — including the Stop/Start Agent control; the Approve button's LIVE tag is 9.5px (console.css:578) and input borders are 1.5:1 (console.css:612). iOS uses system Color.green/Color.orange (ios/SocraticTrade/AppComponents.swift:13) and requests Lato weights the bundle does not ship. On the theme side, 'System' is stored by deleting the key so it reads back Light (app/console/lib/useConsoleTheme.ts:37), ⌘K's toggle writes a different hook instance, and <Toaster theme="system"> (app/layout.tsx:80) plus TickerLo…
- **The one change** Darken the tone tokens to at least 4.5:1 at the app's own micro sizes (open PR #2795 already does the console half) and make one theme resolver the single source for the console, the toast host, TickerLogo and iOS so light is the shipped default everywhere.

#### `admin-honesty` — Operator surfaces present placeholders, unattributed writes and mislabeled scope as fact
- **P2 · desktop-web · effort M** · 10 findings: `ops-observability-admin:sre-06`, `ops-observability-admin:sre-09`, `ops-observability-admin:sre-13`, `ops-observability-admin:sre-17`, `admin-01`, `admin-02`, `admin-04`, `admin-05`, `admin-07`, `admin-08`
- **Root cause** app/admin/page.tsx:448 renders a bare CPU percentage with no caveat although app/api/admin/server-metrics/route.ts:604-613 documents the scaling as unverified and potentially an 8x under-report (the detail page at server-metrics-client.tsx:175 carries the caveat), and substitutes 0.0% / '0 Running' when a probe fails. app/api/admin/server-knobs/route.ts:80 audits every knob write as user 'local' regardless of which admin flipped it, and the admin chrome never shows the signed-in identity (app/admin/layout.tsx:217 reads only the avatar image). /admin/transcript calls /api/chat-history, which has no requireAdmin and is scoped to the caller's own turns, while the nav calls it 'every chat turn'…
- **The one change** Carry the caveat and the unknown state through to the Overview card instead of substituting zeros, record and display the real admin identity on knob writes, and put one requireAdmin gate on the /admin page tree plus an admin-scoped transcript read.

#### `alert-push-delivery` — A notification's lifecycle ends before delivery is known, and the tap lands on the wrong screen
- **P2 · cross-surface · effort M** · 11 findings: `alerts-03`, `alerts-07`, `alerts-08`, `alerts-10`, `alerts-11`, `ops-observability-admin:sre-10`, `ios-engineering:ios-05`, `ios-engineering:ios-06`, `ios-engineering:ios-16`, `ios-engineering:ios-17`, `ios-hig-ux:ios-16`
- **Root cause** src/lib/alerts.ts:88-89 flips the alert to the one-shot 'triggered' state before sendNotification is awaited, and sendNotification never throws on total delivery failure (src/lib/notifications.ts:265-279 folds every error into a returned status) — so a revoked token or zero configured channels loses the only chance to notify. The row is tagged with whatever account happened to be active (notifications.ts:216) so it can drop out of the account-filtered Activity feed. The server builds a symbol-specific deep link (src/lib/push-deep-links.ts:99) that neither client reads (app/console/watchlist/page.tsx has no useSearchParams; ios/SocraticTrade/DeepLink.swift:75-77 discards the query), unlike t…
- **The one change** Make delivery the state transition — mark an alert triggered only once a channel accepted it, retrying otherwise — and implement symbol-aware routing plus badge/category/thread-id on both clients, mirroring the proposal deep-link that already works.

#### `auth-session-hardening` — Sessions are unrevocable 30-day bearer tokens behind a soft perimeter
- **P2 · cross-surface · effort L** · 13 findings: `security-auth:sec-02`, `security-auth:sec-03`, `security-auth:sec-04`, `security-auth:sec-05`, `security-auth:sec-06`, `security-auth:sec-09`, `security-auth:sec-10`, `security-auth:sec-11`, `security-auth:sec-12`, `security-auth:sec-13`, `security-auth:sec-14`, `ios-engineering:ios-02`, `ios-engineering:ios-18`
- **Root cause** src/lib/auth/session-token.ts:6 issues a stateless 30-day JWT with no server-side record, so sign-out is a cookie delete and there is no revocation, no 'sign out everywhere' and no absolute lifetime; iOS then holds that cookie in non-Keychain storage with no app lock. The handoff around it is loose: app/api/mobile/auth-redirect/route.ts:24 mints a session code for any authenticated GET with a caller-supplied code_challenge, app/api/mobile/auth/apple/route.ts:23 uses no nonce, and app/api/mobile/push/register/route.ts:84 reassigns another user's device token. The origin answers directly at <PROD_ORIGIN_IP>:443 with Host: socratictrade.com (middleware.ts:173), so the WAF, bot rules and IP trust…
- **The one change** Back sessions with a server-side record (id in the JWT, revocable, absolute lifetime) and bind the mobile handoff to that record with a nonce and a client-bound challenge, then restrict the origin to Cloudflare IPs so the edge rules become load-bearing.

#### `broker-order-shape` — Each broker adapter rewrites the approved order differently and records nothing
- **P2 · backend · effort M** · 6 findings: `trading-money-path:tsx-06`, `trading-money-path:tsx-07`, `trading-money-path:tsx-13`, `trading-money-path:tsx-16`, `trading-money-path:tsx-18`, `trading-money-path:tsx-19`
- **Root cause** There is no shared venue-normalization step: src/lib/alpaca.ts:157 keeps time_in_force=gtc on extended-hours limits that Alpaca rejects with a 422, alpaca.ts:741 never forwards extended_hours on the MCP path, src/lib/robinhood.ts:972 silently converts a fractional or extended-hours limit into a regular-hours MARKET order while the persisted proposal and fill receipt still say 'limit', src/lib/tradier.ts:789 sends the unrounded limit price and always attaches a stop field, getEquityTradability is a stub returning tradable/fractional true for every symbol (alpaca.ts:582), and the hard-gate copy names Robinhood for Alpaca accounts (src/lib/policy.ts:326).
- **The one change** Put one venue-normalization step in front of every adapter's place call that resolves TIF, extended-hours, rounding and fractional support per broker and audits any rewrite, and make the tradability probe real so the pre-flight check means something.

#### `console-a11y-keyboard` — Shared console overlay and menu primitives skip the keyboard and announcement contract
- **P2 · desktop-web · effort M** · 8 findings: `web-accessibility:a11y-03`, `web-accessibility:a11y-05`, `web-accessibility:a11y-06`, `web-accessibility:a11y-08`, `web-accessibility:a11y-09`, `web-accessibility:a11y-10`, `web-accessibility:a11y-16`, `web-accessibility:a11y-20`
- **Root cause** The primitives every page composes are non-conformant: app/console/ui/sheet.tsx:116's focus sentry and Escape handler are not stack-aware, so Escape on a drawer or palette above a Sheet closes the Sheet underneath; app/console/components/shell.tsx:180 has no skip link past ~22 focus stops; app/console/ui/toast.tsx:38 announces errors politely and auto-dismisses in 9s with no hover/focus pause, and typed-confirm server refusals are never announced at all; approval cards have no accessible name and repeat a bare 'Reject' (approval-card.tsx:455); the account-scope menu uses role=menu with no arrow-key handling plus an invisible full-screen focusable button (chrome.tsx:235).
- **The one change** Fix the shared primitives once — an overlay stack for focus/Escape in sheet.tsx, assertive non-auto-dismissing error toasts, accessible names on menu/dialog/card roots and a skip link in shell.tsx — so all 20+ call sites inherit the behavior.

#### `console-ia-forms-blotters` — Console pages disagree about where things live, how they save, and how they are searched
- **P2 · desktop-web · effort L** · 15 findings: `desktop-web-ux:dweb-02`, `desktop-web-ux:dweb-03`, `desktop-web-ux:dweb-04`, `desktop-web-ux:dweb-05`, `desktop-web-ux:dweb-06`, `desktop-web-ux:dweb-08`, `desktop-web-ux:dweb-09`, `desktop-web-ux:dweb-10`, `desktop-web-ux:dweb-11`, `desktop-web-ux:dweb-17`, `pages-08`, `ux-copy-editor:copy-20`, `desktop-web-ux:dweb-20`, `performance-accounting:perf-23` …
- **Root cause** Three different save models coexist across the Configure pages (app/console/guardrails/page.tsx:335), and two numeric fields PATCH on every keystroke against the blur-commit pattern every sibling uses (app/console/settings/page.tsx:874 and strategy/overlays-panel.tsx:170, where a mid-edit empty field saves the fallback 2). /console/decisions is orphaned from the nav (decisions/page.tsx:1), the proposal lifecycle spans five surfaces, a bare 'r' key fires a strategy run with two mounted RunOnce buttons (command-palette.tsx:128), blotters cannot be searched, filtered or exported (activity/page.tsx:216), the Positions blotter lives only on Home without last price or exit action, and Results has…
- **The one change** Standardize the page contract — one save model (local draft plus explicit commit), one hotkey/verb registry, one blotter component with search/filter/export — and put every route in the nav section it belongs to.

#### `ios-network-decode` — One transient response or one bad row degrades the whole iOS app
- **P2 · ios · effort M** · 7 findings: `ios-engineering:ios-03`, `ios-engineering:ios-08`, `ios-engineering:ios-12`, `ios-engineering:ios-13`, `ios-engineering:ios-21`, `api-contract-web-ios:api-07`, `api-contract-web-ios:api-09`
- **Root cause** A single failed background reload flips snapshotLoadFailed and the 3-minute staleness gate then disables Approve/Run/Watchlist/Policy (ios/SocraticTrade/MobileStore.swift:223) with no polling fallback; GETs have no 5xx retry and the Cloudflare mapping stops at 523 (MobileAPIClient.swift:373), so deploy-swap 502/503s reach the user raw. SSE reconnects on a fixed 5s loop and every scene-active tears down a healthy stream (MobileStore.swift:343), while every SSE frame triggers a full snapshot reload. Decoding is all-or-nothing: one null required field inside any list element fails the entire snapshot (MobileModels.swift:240) and duplicate command ids trap outright (MobileStore.swift:46).
- **The one change** Make decoding element-tolerant — drop and count bad rows instead of failing the payload — and add bounded retry with backoff for idempotent GETs and SSE, so a deploy swap or one malformed row degrades a section rather than the app.

#### `llm-catalog-routing-cost` — Model choice, receipts and cost each derive from a stale or partial source
- **P2 · cross-surface · effort M** · 11 findings: `llm-agent-architecture:llm-03`, `llm-agent-architecture:llm-07`, `llm-agent-architecture:llm-09`, `llm-agent-architecture:llm-10`, `llm-agent-architecture:llm-11`, `llm-agent-architecture:llm-12`, `llm-agent-architecture:llm-14`, `llm-agent-architecture:llm-15`, `llm-agent-architecture:llm-16`, `llm-agent-architecture:llm-17`, `llm-agent-architecture:llm-20`
- **Root cause** src/lib/model-rotation.ts:73 hardcodes a 'dead' OpenRouter list that drops slugs the live catalog still serves; app/console/components/approval-card.tsx:127 asserts 'this run's rotation pick, not a failover' even when an implicit fallback answered; src/lib/chat/llm.ts:655 routes Coach to native providers while the engine and the save-gate assume universal OpenRouter; late post-soft-timeout responses are audited but never metered (src/lib/strategy.ts:6594) and OpenRouter's billed usage.cost is never requested or stored (src/lib/llm-usage.ts:180), so /console/usage is derived from a hand-maintained price table with no embedding/rerank row; and Red Team re-sends the entire Green user content (…
- **The one change** Make the live OpenRouter catalog plus the response's own billed cost the source of truth for rotation, receipts and the Usage page, and compact the Red Team payload to the documented RedTeamReviewContext subset.

#### `ops-liveness-durability` — Nothing pages when the desk stops, and the backup/disk story is unverified
- **P2 · cross-surface · effort L** · 15 findings: `ops-observability-admin:sre-01`, `ops-observability-admin:sre-02`, `ops-observability-admin:sre-03`, `ops-observability-admin:sre-04`, `ops-observability-admin:sre-05`, `ops-observability-admin:sre-07`, `ops-observability-admin:sre-08`, `ops-observability-admin:sre-11`, `ops-observability-admin:sre-14`, `ops-observability-admin:sre-15`, `ops-observability-admin:sre-16`, `backend-reliability:berel-05`, `backend-reliability:berel-13`, `backend-reliability:berel-14` …
- **Root cause** app/api/health/route.ts:88 and :128 set checks.schedulerStale and checks.tradingLiveness.degraded without changing the 200 (ok flips only at :78 and :279), and nothing pushes on them — src/lib/scheduler.ts has a single sendNotification call, line 372 — so a wedged scheduler is discoverable only by an external keyword monitor. The Docker HEALTHCHECK gives that same heavy route 5s (Dockerfile:75) while documented stalls exceed it. The R2 'writes paused' banner reads a leftover marker with no endpoint check (src/lib/r2-usage.ts:571 vs scripts/coolify-prod-start.sh:200-212, live replica is B2), nothing rotates litestream-runtime.log (coolify-prod-start.sh:223), no table has retention (src/lib/d…
- **The one change** Make liveness a first-class push plus one operator card — fire a notification on schedulerStale/tradingLivenessDegraded, split a cheap liveness route for the container probe, gate the R2 banner on isLitestreamReplicaCloudflareR2(), and add log rotation plus table retention.

#### `test-coverage-ci-cost` — The suite covers shapes but not money paths, reaches the live internet, and serializes 14 minutes into every run
- **P2 · backend · effort L** · 12 findings: `qa-test-strategy:qa-05`, `qa-test-strategy:qa-06`, `qa-test-strategy:qa-07`, `qa-test-strategy:qa-08`, `qa-test-strategy:qa-09`, `qa-test-strategy:qa-10`, `qa-test-strategy:qa-12`, `qa-test-strategy:qa-17`, `ios-engineering:ios-22`, `qa-test-strategy:qa-13`, `qa-test-strategy:qa-15`, `qa-test-strategy:qa-16`
- **Root cause** vitest.config.ts has no setupFiles at all — only a globalSetup that plumbs TMPDIR — so nothing prevents a test from reaching SiliconFlow, SEC or Yahoo (STATUS.md still records those 404s), and maxWorkers:1 serializes ~7,000 tests into ~14 minutes of every CI run. 63 of 122 routes have no route-level test including single-proposal approve (app/api/proposals/[id]/approve/route.ts:11), TestBrokerGateway always fills instantly (src/lib/robinhood.ts:920) so only the happy path is reachable, MobileStore.submit/load are untested despite an existing URLProtocol harness, there is no coverage tooling, and the codex-autofix workflow fails on every review event.
- **The one change** Add a vitest setup file that fails any un-mocked outbound fetch plus a partial-fill/reject mode on TestBrokerGateway, then write route tests for approve/replace/pause and re-enable parallel workers behind the per-worker temp DB the config already provisions.

### Tranche 3 — Structure, Polish, Owner Decisions (7)

#### `a11y-structure-naming` — Document structure and accessible names are stylistic rather than semantic
- **P2 · cross-surface · effort M** · 7 findings: `web-accessibility:a11y-04`, `web-accessibility:a11y-07`, `web-accessibility:a11y-11`, `web-accessibility:a11y-12`, `web-accessibility:a11y-14`, `web-accessibility:a11y-18`, `web-accessibility:a11y-19`
- **Root cause** Every console route shares the document title 'Socratic Trade' (app/console/layout.tsx:5, WCAG 2.4.2); 68 heading-styled divs carry con-card-title against 16 real h2/h3 and the collapsible Card demotes its h2 to a span (ui/primitives.tsx:35); none of the 23 console tables has a caption or aria-label and 105 of 106 th elements lack scope (components/positions.tsx:89); the Meter progressbar has no accessible name at any call site (primitives.tsx:201); roughly 147 explanations live only in native title= on non-interactive elements (primitives.tsx:485); readiness state is conveyed by icon and border color alone (readiness-checklist.tsx:27); and the login page has no heading.
- **The one change** Fix the primitives and the layout metadata once — per-route titles, real heading elements inside Card, caption/scope in the table primitive, names on Meter and Tooltip — so every page inherits the structure.

#### `console-first-paint-bundle` — First console paint waits on a large bundle and then a single blocking snapshot
- **P2 · desktop-web · effort M** · 7 findings: `frontend-performance:dweb-06`, `frontend-performance:dweb-08`, `frontend-performance:dweb-09`, `frontend-performance:dweb-10`, `frontend-performance:dweb-11`, `frontend-performance:dweb-12`, `frontend-performance:dweb-13`
- **Root cause** app/console/layout.tsx:22 makes HTML → ~1.5 MB raw JS → /api/dashboard a strict waterfall, and routes that fetch their own data still wait behind the full-screen snapshot loader (components/shell.tsx:67). The single console context re-renders every page three times per poll and once per SSE event because fetching and stream health share the context value with snapshot (lib/useConsoleData.tsx:341). Sentry Session Replay (~38 KB gz) ships although replay is env-disabled (instrumentation-client.ts:31), framer motion (~40 KB gz) loads on every page for one tooltip fade (ui/primitives.tsx:7), three heavy client libraries are declared but never imported, and a 1 MB PNG is the primary favicon.
- **The one change** Split the context so data and transport health do not re-render together, drop the disabled Replay bundle, the motion dependency and the unused libraries, and let self-fetching routes paint before the snapshot resolves.

#### `copy-consistency-rules` — Owner copy rules are unenforced on the web: two-space gaps, Title Case, money format, Central time
- **P2 · cross-surface · effort M** · 21 findings: `ux-copy-editor:copy-01`, `ux-copy-editor:copy-02`, `ux-copy-editor:copy-05`, `ux-copy-editor:copy-06`, `ux-copy-editor:copy-07`, `ux-copy-editor:copy-08`, `ux-copy-editor:copy-09`, `ux-copy-editor:copy-10`, `ux-copy-editor:copy-15`, `ux-copy-editor:copy-17`, `ux-copy-editor:copy-18`, `ux-copy-editor:copy-19`, `ux-copy-editor:copy-21`, `ux-copy-editor:copy-22` …
- **Root cause** SENTENCE_GAP is used in 9 files against roughly 600 web copy strings (app/console/lib/format.ts:54), and the double ASCII spaces typed into JSX collapse to one at render (app/console/components/consent-gate.tsx:107) so source looks compliant and the screen is not; public pages have zero NBSP gaps. Headings and buttons are sentence case against the Title Case rule and against iOS (chrome.tsx:789), one run state carries three names and one stop control four (derive.ts:171), the same guardrail is labeled differently on web and iOS (GuardrailsView.swift:69), compact money renders uppercase suffixes (drilldown-data.ts:674), and app/console/settings/legal.tsx:40 calls toLocaleString() with no tim…
- **The one change** Add the copy-rule lint the iOS suite already has — two-space gap, Title Case headings and buttons, lowercase compact money, Central-time timestamps, one term per concept — and run a single pass to make the strings pass it.

#### `ios-build-compliance` — iOS project-level hygiene: no privacy manifest, hardcoded production origin, no strict concurrency
- **P2 · ios · effort M** · 5 findings: `ios-engineering:ios-10`, `ios-engineering:ios-11`, `ios-engineering:ios-19`, `ios-engineering:ios-20`, `api-contract-web-ios:api-18`
- **Root cause** ios/project.yml:39 ships no PrivacyInfo.xcprivacy although the app uses UserDefaults, a required-reason API — ITMS-91053 on upload; the production origin is hardcoded in LoginView.swift:265, AdminPortalView and DeepLink with no debug override, so every simulator run hits production despite MobileAPIClient.productionBaseURL existing to prevent that drift; project.yml:18 builds in Swift 5.9 language mode with no strict-concurrency checking; and MobileStore/PushNotificationCoordinator/TabPreferences are ObservableObject against the repo's own @Observable rule, re-publishing on every SSE line.
- **The one change** One ios/project.yml pass — add the privacy manifest, a build-configuration-driven base URL and strict concurrency — plus the @Observable migration the repo rule already requires.

#### `ios-parity-gaps` — iOS renders a read-only subset of the screens it mirrors
- **P2 · ios · effort L** · 11 findings: `ios-hig-ux:ios-02`, `ios-hig-ux:ios-03`, `ios-hig-ux:ios-04`, `ios-hig-ux:ios-06`, `ios-hig-ux:ios-10`, `ios-hig-ux:ios-15`, `ios-hig-ux:ios-19`, `ios-hig-ux:ios-20`, `ios-hig-ux:ios-21`, `ios-hig-ux:ios-22`, `performance-accounting:perf-24`
- **Root cause** Coach replies print raw markdown (ios/SocraticTrade/CoachView.swift:272) and its Suggested Draft cannot be staged although the copy says to approve it in Proposals (:323); the Scan header advertises three side-by-side columns that every row stacks vertically (ScanView.swift:158); the Activity tab is described as everything the agent did but shows only fills and the phone's own commands (MobileControlView.swift:64), so a run_failed push lands on a screen that cannot show the failure; Positions omit per-position P&L (MarketsView.swift:140); Results has no equity curve (ResultsView.swift:57); the symbol sheet offers no actions (SymbolInfoSheet.swift:46); triage is one-at-a-time; and Account &…
- **The one change** Close the parity list against the console screens these mirror — a shared markdown renderer, the decision-journal feed, per-position P&L and an equity curve — and surface Account & Settings from More as well as Home.

#### `phone-layout-density` — Phone layouts are the desktop tree at desktop density
- **P2 · mobile-web · effort M** · 9 findings: `mobile-web-ux:mweb-11`, `mobile-web-ux:mweb-14`, `mobile-web-ux:mweb-15`, `mobile-web-ux:mweb-17`, `mobile-web-ux:mweb-20`, `mobile-web-ux:mweb-22`, `mobile-web-ux:mweb-23`, `frontend-performance:dweb-14`, `frontend-performance:dweb-16`
- **Root cause** app/console/console.css:90 defines no phone type-scale bump, so 412 uses of 11px and 185 of 12.5px render at desktop sizes on a 6-inch screen; /console/watchlist keeps a 5-column table whose row actions scroll off (watchlist/page.tsx:227); Home buries portfolio value and day P&L under the readiness hero and strategy bar (page.tsx:217); Scan's phone branch is an unsorted, unfilterable, unvirtualized card wall mounted alongside the hidden desktop tree (scan-table.tsx:505); the equity chart scales to ~70px tall (equity-chart.tsx:11); the Coach composer's Enter always sends with no way to type a newline; and two full-viewport animated gradient orbs keep animating under the opaque console (app/g…
- **The one change** Give the phone breakpoint its own type scale and card variants, and render one tree per breakpoint instead of mounting both.

#### `public-site-funnel-seo` — The public site's two halves do not connect and its metadata is stale
- **P3 · public-site · effort M** · 11 findings: `public-site-growth:dpub-07`, `public-site-growth:dpub-08`, `public-site-growth:dpub-09`, `public-site-growth:dpub-10`, `public-site-growth:dpub-11`, `public-site-growth:dpub-13`, `public-site-growth:dpub-14`, `public-site-growth:dpub-16`, `public-site-growth:dpub-20`, `mobile-web-ux:mweb-24`, `ops-observability-admin:sre-19`
- **Root cause** An anonymous visitor never reaches the marketing page (app/page.tsx:14), /welcome offers only a mailto 'Request access' with no Sign in (welcome/page.tsx:114) and /login never explains the product, while /framework — the accurate explainer — is orphaned and /how-it-works is the only linked one (welcome/page.tsx:137). Metadata is stale or overridden: the sitemap still advertises the deleted /design/socratic-trade route (app/sitemap.ts:15), Cloudflare's managed Content Signals robots prepends Allow: / ahead of the app's Disallow: / (app/robots.ts:47), the root canonical '/' is inherited by every page that does not override it (app/layout.tsx:41), summary_large_image is declared with no image,…
- **The one change** Connect the funnel — Sign in on /welcome, an explainer link on /login, one canonical explainer page — and correct the metadata: per-page canonical, accurate sitemap entries, an OG image, and a robots policy that matches what is actually served.

*On what was left out of clusters:* No finding was left as an orphan: all 384 in-scope items (334 register findings after excluding the two ALREADY_FIXED ones, plus the 50 gap-lane findings) are assigned to exactly one of the 45 clusters, with zero duplicates and zero uncovered P1/P2 members (37 P1 + 178 P2 after applying the second-round severity corrections). Excluded per instruction: api-contract-web-ios:api-03 (iOS 25s scan timeout — ALREADY_FIXED on this branch by PR #2850, commit 1ca5073b7, now 50s vs a 35s server budget; it survives only as a rollout gap on the shipped TF 1.0.68 binary) and ux-copy-editor:copy-04 (iOS "Strategy page" copy — ALREADY_FIXED by PR #2857, commit 437f0a4e4, with two Swift tests pinning the f…

## D. Implementation Plans For Tranche 1 (22)

Written against current code so another agent can execute without re-deriving the analysis.  Each names the edit sites, the contract after the change, and the test that must fail first.

### `account-write-guards` (effort M)

Verified exactly as described. strategyAuthority is an account-level TradingPolicy field (not in USER_LEVEL_POLICY_FIELDS, src/lib/db-profiles.ts:21-48) so it flows unguarded through copyPolicyConfigToActiveAccount (:382-403, used by createStrategyProfile/updateStrategyProfile/activateStrategyProfile), applyProfileToAccount (:731-764), and importAccountSettings (:862-918) — all three preserve the target's systemState but none preserves strategyAuthority, so a saved profile or another account's settings can silently arm/disarm Autopilot with zero TypedConfirm ceremony. Separately, setActiveConnectedAccount (src/lib/db-api-keys.ts:1323-1336) never checks is_draining, and deleteConnectedAccount (:1376-1384) never clears it or promotes another account, so a disconnected/draining account can b…

- **Files** `src/lib/db-profiles.ts`, `src/lib/db-api-keys.ts`
- **Approach** In src/lib/db-profiles.ts: (1) copyPolicyConfigToActiveAccount (:382-403) — change `const currentState = peekPolicy(userId, account.id).systemState;` to capture the whole current policy (`const current = peekPolicy(userId, account.id);`) and spread `strategyAuthority: current.strategyAuthority` alongside the existing `systemState: current.systemState` in the `policy:` object passed to writeAccountStrategyState (this single choke point fixes create/update/activateStrategyProfile in one edit, mirroring the existing systemState-preservation comment at :391-396). (2) applyProfileToAccount (:731-764) — change `const currentState = getPolicy(userId, connectedAccountId).systemState;` to `const current = getPolicy(userId, connectedAccountId);` and add `strategyAuthority: current.strategyAuthority` to the mergePolicy(...) call at :745-750 alongside the existing `systemState: currentState`. (3) importAccountSettings (:862-918) — change `const targetSystemState = peekPolicy(userId, targetConnectedAccountId).systemState;` to `const targetPolicy = peekPolicy(...)` and add `strategyAuthority: targetPolicy.strategyAuthority` to the mergePolicy(...) call at :896-901 alongside `systemState: targetSystemState`. No new TypedConfirm ceremony is added — the fix is that authority simply never travels through these three copy paths, so the ONLY way to change it stays the existing guardrails-page TypedConfirm flow (app/console/guardrails/page.tsx:700-725), consistent with the owner's 'no new blocking ceremony' rule. In src/lib/db-api-keys.ts: setActiveConnectedAccount (:1323-1336) — inside the existing db.transaction(), change the `exists` SELECT to also fetch `is_draining`, and throw `new Error("This account is disconnected and being wound down — it can no longer be made active.")` when `is_…
- **Tests that must fail first**
  - test/copy-config-preserves-arming.test.ts — add a case mirroring the existing systemState pair (lines 17-53): active account's strategyAuthority is 'propose'; activate a library profile whose saved policy.strategyAuthority is 'decide'; assert db.getPolicy(u, acct).strategyAuthority stays 'propose'…
  - test/strategy-copy-to-account.test.ts — add a case mirroring the existing run-state test (lines 46-66): target account's strategyAuthority is 'propose'; applyProfileToAccount a profile whose policy.strategyAuthority is 'decide'; assert the target keeps 'propose' — MUST FAIL today.
  - test/settings-import.test.ts — add a case mirroring 'preserves the target's systemState' (lines 58-75): target's strategyAuthority is 'propose', source's is 'decide'; importAccountSettings(userId, source, target); assert result.strategyAuthority and db.getPolicy(userId, target).strategyAuthority ar…
  - New assertion (can live in a new test/set-active-connected-account-draining.test.ts or alongside persistence-notification.test.ts's existing 'missing-account' throw test at line 146): upsertConnectedAccount, deleteConnectedAccount(id) to mark it draining, then expect(() => setActiveConnectedAccount…
- **Risks / what could break** None of the grepped existing tests (test/copy-config-preserves-arming.test.ts, test/strategy-copy-to-account.test.ts, test/settings-import.test.ts, and the ~35 files calling setActiveConnectedAccount) construct a scenario where strategyAuthority differs between source/profile and target, or where an account is draining before activation — so none should need updating, only extending. Double-check src/lib/db-profiles.ts:150 (`isTieredWrite`) and any other reader of USER_LEVEL_POLICY_FIELDS is unaffected since strategyAuthority is not being added to that set, only explicitly re-pinned at three write sites. No schema/migration change.

### `approval-path-integrity` (effort L)

Four sub-findings, with one premise correction and one that does NOT hold as stated after reading the code. (1) `executeProposal` (src/lib/strategy-execution.ts:297) calls `acquireStrategyLock(lockOwner, userId, policy.connectedAccountId)` with NO retry — verified the lock is ALREADY per-account (db-execution.ts:184-186 `strategyLockKey` includes `connectedAccountId` when given, and strategy.ts:460's own run-lock acquisition passes the same `connectedAccountId`), so the review's 'give approvals a per-account lease' framing is already true at the DB layer; the REAL bug is that the lock check is a single instant-fail with zero retry, so an approval that lands mid-run on the SAME account is starved for however long that run holds the lock, when a short bounded wait (matching the existing `LA…

- **Files** `src/lib/strategy-execution.ts`, `src/lib/mobile-api.ts`, `src/lib/policy.ts`, `src/lib/types.ts`, `src/lib/retry-red-team.ts`
- **Approach** (1) In strategy-execution.ts, wrap the single `acquireStrategyLock(...)` check (line 297) in a small bounded retry loop reusing `LANE_WAITS.approvalPlacement` (30_000ms) from account-mutation.ts: poll every ~500ms-1s up to that budget before returning `{status:'busy',...}`, matching the existing 'human click may briefly wait' philosophy already codified for the broker-mutation lease later in the same function. (2) In mobile-api.ts, do NOT move `proposal.approve` into `IMMEDIATE_MOBILE_COMMAND_TYPES` (would drop it from `cancelQueuedRiskIncreasingCommands`'s halt-interception reach — a real safety regression). Instead restructure `processPendingMobileCommands` (line 1137) so it can run commands for DIFFERENT `(userId, connectedAccountId)` pairs concurrently instead of one global serial `await` chain: track an in-flight set keyed by `(userId, connectedAccountId)` (not one global boolean), have `claimNextQueuedCommand` (or a new variant) skip a queued row whose account key is already in-flight rather than strictly claiming oldest-first, and dispatch claimed commands via `Promise.allSettled` up to the existing `limit` (currently 3-10). This preserves FIFO ordering WITHIN one account (required for the TOCTOU cap-check correctness the strategy lock protects) while unblocking a different account's queued approve from an unrelated account's long-running command — the actual real-world complaint. (3) No code change to approval-reprice.ts — write up the provenance-aware design in the PR description and raise with the owner whether STRATEGY-GENERATED (not chat-drafted) exit limits should also be reprice-exempt, since the current behavior appears intentional (extensively comment-justified, adversarially probed) rather than a defect. (4) In src/lib/policy.ts, change `applyOpeningOr…
- **Tests that must fail first**
  - new test in test/retry-red-team.test.ts — mock `debateProposal` to return `{ verdict: 'approve-at-half', available: true, rejected: false, reason: '...' }` for a proposal with `quantity: 10`, call `retryProposalRedTeam`, then re-fetch the row via `getProposal` and assert `row.proposal.quantity ===…
  - test/policy.test.ts — extend the existing headroom test: `expect(applyOpeningOrderHeadroom(4.99)).toBe(4.74)` (unchanged, default-arg backward compat) plus a new `expect(applyOpeningOrderHeadroom(4.99, 10)).toBe(4.49)` (10% headroom) — fails today (function has no second param) and passes after.
  - new test for strategy-execution.ts's executeProposal — hold the per-account strategy lock (simulate a concurrent run via `acquireStrategyLock`), call `executeProposal` for a proposal on that SAME account, release the lock ~2s later from a parallel timer, assert `executeProposal` eventually succeeds…
- **Risks / what could break** The mobile-worker concurrency restructure ((2) above) is the riskiest single item in this cluster — it touches the core command dispatcher and must preserve the existing halt-interception (`cancelQueuedRiskIncreasingCommands`) and stale-command sweep (`STALE_MOBILE_COMMAND_THRESHOLD_MS`) semantics exactly; test with the existing test/mobile-api.test.ts and test/stale-mobile-commands.test.ts suites, and consider landing (1)+(4)+(5) (all narrowly-scoped, independently valuable) as a first PR, with (2) as a separate, more carefully reviewed follow-up given its blast radius. (3) is explicitly a no-code-change recommendation — do not let it get folded into the same PR as an actual fix without an…

### `broker-io-deadlines` (effort M)

Partially confirmed, partially superseded by a recent fix (commit d4299bec6, 2026-08-18, this same day per git log) that already added a first-call+retry budget (awaitWithFirstCallRetry/outcomeOrTimeout, src/lib/inflight-deadline.ts) around Alpaca READ calls only: getAccount(s), getPositions, getOrders/getEquityOrders, getEquityQuotes all now bound at 16s+8s. The genuinely still-unbounded gap is the WRITE path: placeEquityOrder/placeOptionOrder's createOrder calls (alpaca.ts:633,737,814) and cancelEquityOrder/cancelBracketSiblingLegs's cancelOrder/sendRequest calls (:862,884,904) go through trackHealth (:256-287) directly with no deadline at all — trackHealth's own retry logic only fires on a REJECTED promise (isTransientNetworkError), never on one that simply never settles. Separately co…

- **Files** `src/lib/alpaca.ts`, `src/lib/scheduler.ts`, `src/lib/streams/alpaca-trade-updates-stream.ts`
- **Approach** (1) src/lib/alpaca.ts: import `withDeadline` from './safety-maintenance' (or promote it to a small shared util if that would create a cycle — safety-maintenance.ts does not import alpaca.ts, so a direct import is safe) and a new exported constant `ALPACA_WRITE_DEADLINE_MS = 20_000` (longer than the 16s read budget, since order placement is not retried in parallel the way reads are). In trackHealth (:256-287), wrap the inner `const result = await fn();` (:262) as `const result = await withDeadline(fn(), ALPACA_WRITE_DEADLINE_MS, "Alpaca broker call timed out");` — this closes the gap for EVERY trackHealth-wrapped call (creates/cancels included) in one place, and is pure defense-in-depth (a strict no-op) for the read call sites that already have their own outer awaitWithFirstCallRetry budget, since that outer race already wins first at 16s < 20s. Do NOT add a parallel-retry for createOrder/cancelOrder (unlike the read paths) — every createOrder call already carries a deterministic `client_order_id: input.refId` (:640,705,821), so a timeout should surface as a clean rejection and let the EXISTING ambiguous-retry-with-same-refId machinery at the strategy/execution layer (documented on SyntheticTrailingStop.lastAttemptRefId in db-api-keys.ts) decide whether to retry — firing a second concurrent createOrder from inside trackHealth itself would reintroduce exactly the double-order risk that machinery exists to avoid. (2) src/lib/alpaca.ts, getEquityOrders (:501-546): add a `since` bound to the pagination loop (e.g. stop paginating once a page's oldest `created_at` predates a caller-configurable window, default matching the app's existing stale-order/reconciliation horizons) and change `status: "all"` to `status: "open"` for the majority of call sites that only need open order…
- **Tests that must fail first**
  - New test in test/alpaca-brackets.test.ts (or a new alpaca-broker-deadline.test.ts) using vi.useFakeTimers() per the test/inflight-deadline.test.ts pattern: mock createOrder to `return new Promise(() => undefined)` (never resolves), call placeEquityOrder(...), advance fake time by ALPACA_WRITE_DEADL…
  - Same pattern for cancelEquityOrder mocking cancelOrder as a never-resolving promise — MUST FAIL/HANG before the fix.
  - New scheduler test: mock getBrokerGateway's runSyntheticStopMonitor dependency (or the gateway call it makes) to hang; run one tick(); advance fake timers past BROKER_TIMEOUT_MS; assert stopMonitorInFlight no longer contains the account key (i.e. the lane unlatches) — MUST FAIL before the fix (the…
  - New scheduler re-entrancy test: make tick()'s body await a controllable never-resolving promise (inject via a mocked per-account health check); call startScheduler() (or invoke the tick function directly twice back-to-back before the first resolves); assert the per-account loop body (a spy) was ent…
  - New alpaca-trade-updates-stream test: open the stream with a mock WebSocket, advance fake time past the idle threshold without sending any message, assert ws.close() (or a reconnect) was triggered — MUST FAIL before the fix (no watchdog exists).
- **Risks / what could break** trackHealth's deadline must stay ABOVE the read paths' own outer budgets (16s+8s=24s worst case for the retry-extended reads) or it will start firing INSIDE an intentional retry window and break the already-landed 2026-08-18 fix — size ALPACA_WRITE_DEADLINE_MS carefully and re-run test/inflight-deadline.test.ts's live-max-14s cases after the change to confirm no regression. Narrowing getEquityOrders's default status/window is the highest-blast-radius change in this cluster — it MUST NOT silently break any caller that relies on seeing filled/canceled/all-status history (grep call sites and audit each one's actual need before changing defaults; prefer an explicit opt-in param over a silent de…

### `cash-flow-and-trading-day` (effort L)

Confirmed the core defects. deriveDayPnl (app/console/lib/derive.ts:416-454) has NO fills parameter in its signature at all, and its only caller (app/console/page.tsx:62) passes none — so line 443's `inferExternalCashFlows([baseline, fakeCurrent], [])` hardcodes an empty fill list on every call; whenever the strategy trades directionally that day, tradeCash computes to 0 inside cash-flows.ts's loop (140-165, since sortedFills is always []), the missing-fill guard at cash-flows.ts:187-208 treats the whole equity delta as a transfer or zeroes it out, and Day P&L can print exactly $0.00. Confirmed the short/cover sign bug precisely: cash-flows.ts:164 `tradeCash += f.side === 'sell' || f.side === 'cover' ? f.notional : -f.notional;` — a SHORT should INCREASE cash (broker credits short-sale pr…

- **Files** `src/lib/cash-flows.ts`, `src/lib/risk-breaker.ts`, `src/lib/dashboard.ts`, `app/console/lib/derive.ts`, `app/console/page.tsx`
- **Approach** 1) SIGN FIX (smallest, highest-confidence, do first): cash-flows.ts:164 — change `f.side === 'sell' || f.side === 'cover' ? f.notional : -f.notional` to `f.side === 'sell' || f.side === 'short' ? f.notional : -f.notional` (short and sell both increase cash; buy and cover both decrease it — cover now correctly falls to the else/-notional branch). 2) REAL FILLS instead of []: give deriveDayPnl a `fills: FillEvent[]` parameter (app/console/lib/derive.ts:416-421 signature), pass real same-day fills at line 443 instead of `[]`. Thread this from the caller: app/console/page.tsx:62 needs a fills list scoped to [baseline.timestamp, now] — add a small server-side query (new function in src/lib/db-fills.ts, e.g. `listFillsSince(accountNumber, source, sinceIso, userId)`, a thin wrapper filtering the account's fills by filledAt >= sinceIso) exposed through the dashboard snapshot (src/lib/dashboard.ts's computeDashboardSnapshot, ~343+) as e.g. `snapshot.recentFillsForDayPnl`, and pass that array into deriveDayPnl at page.tsx:62. 3) PRIMARY SOURCE per the register's own recommendation — wire the broker's own ledger ahead of inference: in dashboard.ts (or a new small helper module), call `fetchAlpacaAccountActivities(userId, { activityTypes: ['CSD','CSW','ACATC','ACATS','FEE','DIV'] })` (deposit/withdrawal/transfer/fee/dividend activity types) for the account's active period, sum net_amount for transfer-class activities into a `brokerReportedFlow` figure, and have deriveDayPnl prefer `brokerReportedFlow` when present/non-Alpaca-account-agnostic-empty, falling back to the (now-fixed) fills-based inference only when the broker ledger call fails or returns nothing (non-Alpaca brokers). Label which source won on the DayPnl result (`cashFlowAdjusted: 'broker-ledger' | 'inferred' | false`)…
- **Tests that must fail first**
  - NEW unit test in test/cash-flows.test.ts (or wherever cash-flows tests live): construct two equity-curve points with a SHORT fill of notional 1000 between them and no external transfer; assert `inferExternalCashFlows(...)` returns an EMPTY flow map (or a flow within materiality threshold of 0), not…
  - NEW test for deriveDayPnl: pass a real fills array containing one directional buy fill between baseline and now, plus a portfolio.cash delta consistent with ONLY that trade (no external transfer) — assert `pnl` reflects the mark-to-market move, not $0.00. FAILS today (deriveDayPnl has no fills para…
  - NEW test for risk-breaker.ts: call recordAndEvaluateDrawdownBreaker with `now` set to 7:30pm CT (which is after UTC midnight the 'next' calendar day) on two consecutive calls representing the 'same' NY trading day; assert `startOfDayEquity` is NOT reset between them. FAILS today (UTC day-key rolls…
- **Risks / what could break** deriveDayPnl and app/console/page.tsx are on the money-facing Home dashboard's most-viewed tile — any regression here is immediately visible to the owner. The broker-activities wiring (step 3) is the largest, riskiest piece (new external API call in the dashboard's parallel-fetch chain, needs a timeout/failure fallback so a slow/failed Alpaca activities call never blocks or breaks the dashboard — mirror the existing `Promise.race`/timeout pattern already used elsewhere in dashboard.ts) and should land as a separate PR after the sign-fix + fills-plumbing land and are verified; do not bundle all four steps into one commit. Existing tests likely pin the CURRENT `[]`-fills / UTC-day behavior —…

### `coach-tools-and-turns` (effort M)

Two independent, both real, money-adjacent bugs plus one correctness gap. (1) src/lib/chat/tools.ts's `draft_order.execute` (line 92) does `input.side === "sell" ? "sell" : "buy"` — ANY non-exact-'sell' value (case variant, 'short', a typo, missing) silently becomes 'buy', the OPPOSITE economic direction; downstream `chatDraftToProposal` (src/lib/chat/promote-draft.ts:16) only re-validates `draft.side === 'buy' || 'sell'`, which is trivially true after the coercion already happened, so it can never catch this. Line 94's `order_type` has the identical bug shape: any non-exact-'limit' value silently becomes 'market', and because line 96's `limit_usd` ternary keys off that same coerced `order_type`, a legitimate 'limit $150' instruction with a typo'd order_type silently loses the price and b…

- **Files** `src/lib/chat/tools.ts`, `src/lib/chat/llm.ts`
- **Approach** tools.ts fail-closed side/order_type (mirroring `normalizeAlertOp`'s shape in src/lib/alerts.ts:19-23, the established fail-closed pattern in this codebase): add `function normalizeDraftSide(raw: unknown): 'buy'|'sell'|null { const v = String(raw ?? '').trim().toLowerCase(); return v === 'buy' ? 'buy' : v === 'sell' ? 'sell' : null; }` and a sibling `normalizeOrderType(raw: unknown): 'market'|'limit'|null { const v = String(raw ?? 'market').trim().toLowerCase(); return v === 'market' ? 'market' : v === 'limit' ? 'limit' : null; }`. In `draft_order.execute` (~line 90-97), replace the two coercing ternaries: `const side = normalizeDraftSide(input.side); if (!side) return { error: 'INVALID_SIDE', details: \`side must be "buy" or "sell" (got ${JSON.stringify(input.side)})\` };` and the same shape for `order_type` before computing `limit_usd`. Clamp `k` in kb_search's execute (~line 164): `k: typeof input.k === 'number' ? Math.min(Math.max(Math.trunc(input.k), 1), 20) : 5` and add `maximum: 20` to the schema (line 155). llm.ts abortSignal threading: change `type Transport = (body: any, apiKey: string) => Promise<any>;` (330) and `type OpenAITransport = (body: any, apiKey: string) => Promise<any>;` (464) to accept a third optional `signal?: AbortSignal` param. In `defaultTransport` (331-345) and `defaultOpenAITransport` (466-...) and the closure returned by `makeOpenAITransport` (~689), accept that third param and, when present, compose it with a FRESH internal timeout rather than replacing llm-request.ts's shared 60s `LLM_TIMEOUT_MS` backstop (do NOT touch the shared `llmFetch` function itself — it's used by many non-chat callers including `fetchLlmWithRetry`'s per-attempt-timeout composition, which is too subtle/high-blast-radius to safely alter in this pass): `const combi…
- **Tests that must fail first**
  - new test in test suite for tools.ts (check for an existing test/chat-tools.test.ts, else create one) — `execute({ side: 'Short', qty: 5 }, ctx)` returns `{ error: 'INVALID_SIDE', ... }`, NOT a buy draft — fails today (produces a `side: 'buy'` draft), passes after.
  - test/chat-llm.test.ts — new test 'passes abortSignal as the third argument to transport' (models the existing 'passes the api key in the second argument to transport' test at line ~83): `const controller = new AbortController(); await llm.run({ ...baseArgs, abortSignal: controller.signal }); expect…
- **Risks / what could break** The `AbortSignal.any` composition needs Node 20.3+ (repo pins Node 24 via .nvmrc, confirmed fine) — do NOT touch the shared `llmFetch`/`fetchLlmWithRetry` in llm-request.ts, whose per-attempt-timeout composition (line ~555-559, explicitly commented 'a caller-supplied init.signal is respected as-is') has subtle behavior other non-chat callers (framework-review.ts, learning-review.ts, outcome-engine.ts, post-mortem.ts, proposal-revalidation.ts, strategy-tuning.ts) depend on; scoping the fix to chat/llm.ts's own transport wrappers avoids that landmine entirely. The `order_type`/`side` fail-closed change is a genuine behavior change for any caller that was relying on the old silent-coercion (no…

### `console-ships-too-much` (effort M)

Two independent problems share one 'the console ships too much to the browser' theme. (1) app/console/settings/brokers.tsx value-imports `mergeAccountCapabilities` from src/lib/venue-contract.ts, and because that module's TOP-LEVEL import is `resolveSourceBool` from source-settings.ts -> db-api-keys.ts/db-settings.ts -> the db.ts barrel (better-sqlite3, SQL schema, migrations, key-store), the WHOLE db layer's JS compiles into the client bundle even though `mergeAccountCapabilities` itself never calls `resolveSourceBool` (only the separate `deriveVenueContract` does, in the same file) — this is exactly the import chain that produced the 2026-08-18 'process.uptime is not a function' production crash (fixed only symptomatically in 01f4e73e2, which guarded one call site but left the root chai…

- **Files** `src/lib/venue-contract.ts (split)`, `src/lib/venue-contract-pure.ts (new)`, `app/console/settings/brokers.tsx`, `src/lib/db.ts`, `src/lib/db-settings.ts`, `src/lib/db-api-keys.ts`, `src/lib/dashboard.ts`, `app/dashboard-types.ts`, `app/api/scan/route.ts`, `app/api/notifications/ack/route.ts`, `src/lib/order-cancel.ts`, `app/api/orders/replace-market/route.ts`, `package.json`
- **Approach** Bundle boundary (P1, dweb-01): `npm install server-only` (Vercel's zero-runtime marker package; not currently a dependency — `npm ls server-only` returns empty). Add `import "server-only";` as the first import in src/lib/db.ts, src/lib/db-settings.ts, src/lib/db-api-keys.ts (server-only's package.json `browser` export throws when a client bundle tries to include it, turning any FUTURE accidental client import into a build-time error instead of a silent 1MB+ leak). Then split venue-contract.ts: create src/lib/venue-contract-pure.ts containing everything that does NOT touch resolveSourceBool — `knownBrokerLimits` (line 73), `emptyCapabilities`, `mergeAccountCapabilities` (line 195), `brokerDisplayLabel` (line 219), `resolveSessions`, `buildPromptLines`, and the `VenueContract`/capability types — re-exported from venue-contract.ts for existing server-side importers so nothing else breaks, and change brokers.tsx:12 to `import { mergeAccountCapabilities } from "@/lib/venue-contract-pure"` instead. Leave `deriveVenueContract` (the ONLY function that calls `resolveSourceBool`, at line 250) in venue-contract.ts unchanged. Verify with `next build` + `grep -c 'getDb\|SELECT ' .next/static/chunks/app/console/connections/page-*.js` (should go to 0). Payload/cache (P2, dweb-03/04/05): in src/lib/dashboard.ts drop the `clientAudit` const and the `audit: clientAudit,` line from the returned snapshot object (~line 1108) — `unifiedFeed`/`auditFeed` (built earlier in the same function from the local `audit` array) are untouched, only the raw top-level field is removed; change `DashboardSnapshot.audit` (app/dashboard-types.ts) from `AuditEvent[]` to `audit?: AuditEvent[]` (additive/optional, matching every other backward-compat field in this type — test fixtures that still set `audit: []…
- **Tests that must fail first**
  - new test/venue-contract-pure-boundary.test.ts — `import { mergeAccountCapabilities } from '../src/lib/venue-contract-pure'` and assert it resolves with no error and produces the same output as an existing test/venue-contract.test.ts case (e.g. 'Tradier is whole-share, no trail, no option orders') —…
  - test/dashboard-snapshot-cache.test.ts or a new test/dashboard-audit-projection.test.ts — call `computeDashboardSnapshot`/`getDashboardSnapshot` against a seeded temp DB with at least one `market_scan` audit row present, and assert `snapshot.audit` is `undefined` — FAILS today (returns the 100-row c…
  - new test/notifications-ack-cache-invalidation.test.ts — seed the dashboard snapshot cache via `setCachedDashboardSnapshot`, POST to the ack route's handler, then assert `getCachedDashboardSnapshot(key)` is `undefined` — fails today (no invalidation call), passes after.
- **Risks / what could break** The server-only guard is the highest-leverage, lowest-risk part of this cluster (a genuinely additive dependency + a compile-time tripwire) — land it first/separately if splitting the PR. The `quotesBySymbol` trim is the riskiest single edit: verify against app/console/ui/symbol-drilldown.tsx's `resolveQuote` fallback (line 267) and orders/page.tsx:279 with a manual click-through on a symbol that is NOT held/ordered/pending but IS a topCandidate, to confirm its quote still resolves post-trim. Dropping `snapshot.audit` is safe per an exhaustive `grep -rn 'snapshot.audit'` across app/ and ios/ (zero hits) but re-run that grep right before landing in case another agent added a consumer in the…

### `event-loop-pins` (effort L)

Confirmed with one correction: the adaptive-yield batch helper (insertDocumentChunkFtsBatch, db-learning.ts:1666-1698) and the tick/budget planner (planFtsMirrorSlice, src/lib/rag/fts-mirror-bound.ts) already exist and are already correctly wired into src/lib/rag/sec-ingest-worker.ts:456-476 (with hasInFlightStrategyWork gating at :464) — that ONE caller is NOT the bug. The real gaps are: (a) the per-row idempotency DELETE inside insertDocumentChunkFts (db-learning.ts:1614-1617) and inside insertDocumentChunkFtsBatch's own runGroup (:1671-1684) is a full-table-scan `WHERE content_hash=? AND symbol=? AND source=? AND accession=?` on an FTS5 virtual table (no B-tree exists on those columns for anything but MATCH), which is the true O(corpus) cost underlying every other symptom; (b) src/lib/…

- **Files** `src/lib/db.ts`, `src/lib/db-learning.ts`, `src/lib/rag/persist-local-complete.ts`, `src/lib/web-sources/sec-filings.ts`, `test/pinecone-write-class.test.ts`, `test/sec-ingest-worker.test.ts`
- **Approach** (1) src/lib/db.ts: add migration version 85 (after the v84 block ending at :3151, following the exact ALTER-TABLE-guarded pattern used throughout this file) creating `CREATE TABLE IF NOT EXISTS document_chunks_fts_index (content_hash TEXT NOT NULL, symbol TEXT NOT NULL, source TEXT NOT NULL, accession TEXT NOT NULL, fts_rowid INTEGER NOT NULL, PRIMARY KEY (content_hash, symbol, source, accession))` then a one-time backfill `INSERT OR REPLACE INTO document_chunks_fts_index (content_hash, symbol, source, accession, fts_rowid) SELECT content_hash, symbol, source, accession, rowid FROM document_chunks_fts` (a single full pass, run once at migration time — not a per-request cost). (2) src/lib/db-learning.ts: rewrite insertDocumentChunkFts (:1600-1622) to first `SELECT fts_rowid FROM document_chunks_fts_index WHERE content_hash=? AND symbol=? AND source=? AND accession=?` (an indexed PK lookup), `DELETE FROM document_chunks_fts WHERE rowid = ?` using that fts_rowid when found (FTS5 rowid-delete is fast, unlike a column-value delete), then INSERT the new row, capture `info.lastInsertRowid`, and `INSERT INTO document_chunks_fts_index (...) VALUES (...) ON CONFLICT(content_hash,symbol,source,accession) DO UPDATE SET fts_rowid=excluded.fts_rowid`. Apply the identical lookup+rowid-delete+insert+upsert sequence inside insertDocumentChunkFtsBatch's `runGroup` transaction (:1679-1684), replacing the current `del.run(...)` scan-delete, with the 3 prepared statements hoisted outside the loop like the existing `del`/`ins` statements are today. (3) src/lib/rag/persist-local-complete.ts: change persistLocalComplete (:42-62) to `async`, and replace its `for (const chunk of input.chunks) { insertDocumentChunkFts(...) }` loop with the SAME plan-slice-then-yield-then-check-hasInFlightStrateg…
- **Tests that must fail first**
  - New assertion in test/sec-ingest-worker.test.ts's 'insertDocumentChunkFtsBatch' describe block (after the existing case at lines 236-261): after inserting rows and re-inserting the SAME (content_hash,symbol,source,accession) key with new text, assert `SELECT COUNT(*) FROM document_chunks_fts_index…
  - New test proving persistLocalComplete now yields: mock/spy yieldEventLoop (or hasInFlightStrategyWork returning true after the first slice) and call the now-async `await persistLocalComplete({..., chunks: <more than FTS_MIRROR_MAX_CHUNKS_PER_TICK rows>})`; assert yieldEventLoop (or the strategy-gat…
  - test/pinecone-write-class.test.ts:147-158 — this existing synchronous `it(...)` call to persistLocalComplete MUST be updated to `it("...", async () => { ... await persistLocalComplete({...}); ... })` since the function becomes async; without this update the test would silently race the now-unresolv…
- **Risks / what could break** The document_chunks_fts_index backfill INSERT...SELECT at migration time is itself an O(corpus) full scan — but it runs exactly ONCE, at deploy/boot, not per-request, so it is in the same cost class as any other migration and acceptable even on a large production corpus (unlike the current per-request cost this whole cluster exists to fix). document-summarizer.ts:466 does its OWN direct bulk `DELETE FROM document_chunks_fts WHERE source=? AND accession=?` (not per-row, not through insertDocumentChunkFts) — left untouched by this plan; it will leave orphaned document_chunks_fts_index rows for that (source,accession) pair, which is harmless (a stale side-key pointing at a since-deleted fts_ro…

### `green-request-schema` (effort S)

Confirmed exactly. The Bull proposal JSON-schema `properties` object (src/lib/strategy.ts:5852-5871) declares 17 keys including `exitPlan` (:5869-5870), but `required: [...BULL_PROPOSAL_REQUIRED_KEYS]` (:5851) spreads a 16-key constant (:6653-6670) that omits `exitPlan`. Under `additionalProperties:false` + OpenAI/Gemini `strict:true` (wired at src/lib/llm-call.ts:512/515/529), strict mode requires required to be exactly the property key set — so every native-OpenAI, Gemini, or any openai/*-routed Green seat 400s before generation. DeepSeek and any openAiJsonObject-forced or Gemini-schema-fallback path uses bare `json_object` (llm-call.ts:510/517), which the app does not itself validate on the happy-path JSON.parse (strategy.ts's parse around the extractJsonPayload/JSON.parse branch) — on…

- **Files** `src/lib/strategy.ts`
- **Approach** (1) Add `"exitPlan",` to BULL_PROPOSAL_REQUIRED_KEYS (src/lib/strategy.ts:6653-6670, insert between "bracketTakeProfit" and "stopPlan") — this is the single source spread into both the schema's `required` array (:5851) and filterRepairedProposals's completeness gate (:6759, :6810), per the function's own doc comment at :6648-6651 that these must never drift; exitPlan stays nullable in the schema (`type: ["string","null"]` at :5870) so this only requires the KEY be present, not a non-null value, consistent with autonomyOverride/bracketStopLoss/bracketTakeProfit/stopPlan's existing nullable-but-required treatment. (2) Add a genuine drift-catching invariant test (not just the existing smoke-check at test/strategy-hardening.test.ts:865-871) that captures the ACTUAL outgoing request schema the way test/strategy-candidate-enforcement.test.ts:100-170 already does — mock fetch, run runStrategyOnce() with an OpenAI-routed policy, read `bullRequest.text.format.schema` (Responses) or `.response_format.json_schema.schema` (chat-completions), and assert `Object.keys(schemaObj.properties.proposals.items.properties).sort()` deep-equals `[...schemaObj.properties.proposals.items.required].sort()`. (3) Add a post-parse completeness check on the happy-path JSON.parse branch (the `try { const parsed = JSON.parse(extractJsonPayload(text))... return { text, proposals: parsed.proposals ?? [], ... }` block a few hundred lines after :6100, immediately following the successful-parse `try`): route `parsed.proposals` through the SAME per-item completeness predicate filterRepairedProposals already applies (key presence + type checks) before returning them, not only on the jsonrepair fallback path — so a json_object transport (DeepSeek, or a Gemini schema-translation fallback) that returns syntacti…
- **Tests that must fail first**
  - test/strategy-hardening.test.ts:865-871 — strengthen 'the completeness gate and the structured-output schema share one required-keys source' to `expect(BULL_PROPOSAL_REQUIRED_KEYS).toContain("exitPlan")` — FAILS before the fix (exitPlan absent).
  - New test in test/strategy-candidate-enforcement.test.ts (or a new file) per approach step (2): assert schema property keys === schema required keys for the Bull proposals item — MUST FAIL before the fix (required has 16 keys, properties has 17; a strict-mode OpenAI call built from today's schema li…
  - test/strategy-hardening.test.ts's existing filterRepairedProposals tests (e.g. around lines 830-863) — build a `complete()` fixture that now must also include an `exitPlan` field for the completeness gate to accept it; confirm a repaired proposal missing exitPlan is now correctly DROPPED (previousl…
- **Risks / what could break** Any Bull system-prompt or few-shot example that already always emits exitPlan is unaffected. Any LIVE in-flight strategy run using a cached/older request shape is not a concern (this is a code-level constant, not persisted data). The main risk is the jsonrepair completeness gate at filterRepairedProposals now requiring exitPlan too — verify no existing 'kept' fixture in test/strategy-hardening.test.ts omits exitPlan; if any do, they need an exitPlan field added to their `complete()` helper fixture. Also check LLM_OUTPUT_TOKEN_CAPS.strategyProposal has headroom for one more field per proposal — negligible since exitPlan can be null.

### `home-proposal-rows` (effort M)

Home's 'Latest strategy run' / 'Previous Trades' rows are built with synthetic/wrong-id-space ids that make Approve either 404 or hit the wrong DB row, the chip-tone logic doesn't know about the real 'error'/'proposed' statuses so they render green like a fill, the `??` fallback never fires because latest.proposals can be an empty (not missing) array, and the row is a plain onClick div with zero keyboard access. Root cause of the id bug: strategy.ts already mints a real `crypto.randomUUID()` proposalId and inserts it into trade_proposals for every proposal, but never returns that id back on the trace item pushed into `results` (dashboard-types.ts StrategyDecision.proposals[] has no `id` field), so the UI has no real id to approve against for 'Latest strategy run' rows, and 'Previous Trade…

- **Files** `src/lib/strategy.ts`, `app/dashboard-types.ts`, `src/lib/dashboard-feed.ts`, `app/console/page.tsx`
- **Approach** 1) src/lib/strategy.ts: inside the `for (const proposal of proposals)` loop (starts ~line 3356, `const proposalId = crypto.randomUUID()` declared right after entry), add `id: proposalId` to the object literal at every `results.push({...})` call site in that loop (22 sites: lines 3382, 3431, 3667, 3689, 3746, 3767, 3788, 3822, 3866, 3924, 3976, 4012, 4036, 4074, 4087, 4099, 4112, 4131, 4152, 4199, 4208, 4229 — verified `proposalId` is in lexical scope at every one, including inside the async mutation-lease closure). 2) app/dashboard-types.ts `StrategyDecision.proposals` (line 49) and src/lib/dashboard-feed.ts `StrategyDecisionLike.proposals` (line 41): add `id?: string` to the array element type (optional — legacy persisted `strategy_run` audit rows predate this field). `auditBoundedStrategyRunResult` (audit-bounded-run.ts) spreads `...result` untouched except marketScan, so the new field flows through to the persisted audit payload and back out via `latestStrategyRun.proposals[].id` with no other change needed. 3) app/console/page.tsx: in `decisionFromProposal` (line 774) add a 6th param `approvable: boolean = false` and set it on the returned `DecisionRowData` (add `approvable?: boolean` to the type at line 588). In `decisionFromPending` (789) pass `approvable: true` (it already carries a real `pending.id`). In `decisionFromSocratic` (754) explicitly pass/set `approvable: false` — 'Previous Trades' rows must never expose Approve since `decision.id` is a SocraticDecisionCase id, not a trade_proposals id (the exact same 404/wrong-row bug class, currently reachable whenever a previousTrades row's status is 'blocked'). Rewrite the `latestProposals` derivation (lines 77-80): build the trace-derived array into a local const first (`.map` over `latest?.proposals?.slice(0,5)`…
- **Tests that must fail first**
  - test/e2e-money-path.test.ts — extend the existing 'runs strategy through to execution' test: after `const aaplProposal = result.proposals.find(...)`, add `expect(aaplProposal?.id).toEqual(expect.any(String))` and, using the already-imported db layer, `expect(getProposal(aaplProposal!.id!, 'local')?…
  - test/console-action-rows.test.ts — add a new describe for a small exported pure helper (extract the chip-tone ternary above into an exported `proposalChipTone(status: string): 'pos'|'warn'|'accent'` in app/console/lib/action-verbs.ts so it's unit-testable outside page.tsx): assert `proposalChipTone…
  - new test (React Testing Library, colocate as test/console-proposal-row-a11y.test.tsx if one doesn't already exist for page.tsx internals) — render a latestProposals-derived ProposalRow with status 'blocked' and assert no 'Approve Proposal' button is present (before the fix, `isPendingOrFailed` is t…
- **Risks / what could break** The 22-site strategy.ts edit is mechanical but high blast-radius by file (the most sensitive module in the repo) — run the full E2E money-path + strategy-* suite, not just the one new assertion, since any missed/mistyped site would leave that one proposal's trace item without an id (degrading gracefully to `approvable:false`, not a crash, but worth catching in review). Grep `test/*.ts` for `.proposals[0].status` / `.proposals.find` before landing to confirm no existing test snapshot-matches the full proposals[] object shape (an added `id` field would break an exact-equality assertion, not a `.toMatchObject`). The `decisionFromSocratic` `approvable:false` change is a real, if small, behavior…

### `identity-fails-open` (effort M)

Confirmed all three cited mechanisms precisely, and confirmed the existing test suite's shape so the fix can be scoped to ADD a gate without breaking pinned dev/test behavior. middleware.ts:165-167 `isAuthConfigured()` returns `Boolean(process.env.AUTH_SECRET) || isFlagOn(process.env.CF_ACCESS_TRUST_EMAIL_HEADER)`. middleware.ts:384-388 (exact lines, confirmed): `if (!trustedEmail && !isAuthConfigured()) { trustedEmail = PRIMARY_EMAIL; identitySource = AUTHENTICATED_IDENTITY_SOURCES.localFallback; }` — this fallback fires for EVERY request whenever AUTH_SECRET is unset and CF Access trust is off, with NO check for whether this is a real production boot; a selective env regression (AUTH_SECRET dropped from Coolify/Infisical while the app otherwise boots fine) silently makes every anonymous…

- **Files** `instrumentation.ts`, `src/lib/auth-secret-guard.ts (new, small)`, `middleware.ts`, `src/lib/request-user.ts`
- **Approach** 1) Boot-time assertion (mirrors assertEncryptionKeyConfiguredInProduction's exact pattern): new small module src/lib/auth-secret-guard.ts exporting `export function assertAuthSecretConfiguredInProduction(env: NodeJS.ProcessEnv = process.env): void { if (env.DB_BOOTSTRAP !== 'live') return; if (env.AUTH_SECRET && env.AUTH_SECRET.trim().length > 0) return; if (isFlagOn(env.CF_ACCESS_TRUST_EMAIL_HEADER) && env.CF_ACCESS_TEAM_DOMAIN?.trim() && env.CF_ACCESS_AUD?.trim()) return; throw new Error('AUTH_SECRET is missing in a live production boot (DB_BOOTSTRAP=live) and Cloudflare Access trust is not fully armed either. Refusing to boot: without a real identity source configured, every request would resolve as the owner via the dev/local fallback. Set AUTH_SECRET (or fully configure CF_ACCESS_TRUST_EMAIL_HEADER + CF_ACCESS_TEAM_DOMAIN + CF_ACCESS_AUD) before starting.'); }` (reuse/import the same `isFlagOn` helper middleware.ts already has, or duplicate the tiny predicate — check whether it's exported from middleware.ts first since middleware.ts is edge-runtime-only and this guard needs to run in instrumentation.ts's Node runtime). Call it in instrumentation.ts's register() (~47), immediately after the existing `assertEncryptionKeyConfiguredInProduction()` call, same style/position. 2) Middleware fallback gate (the core fix — one added condition): change middleware.ts:384-388's guard from `if (!trustedEmail && !isAuthConfigured())` to `if (!trustedEmail && !isAuthConfigured() && process.env.DB_BOOTSTRAP !== 'live')`. When DB_BOOTSTRAP==='live' and auth is unconfigured, trustedEmail stays null/empty and execution falls through unchanged to the existing final `else { fail closed: 401 for /api/*, redirect to /login otherwise }` branch (no other code path changes needed — the fail…
- **Tests that must fail first**
  - NEW test in a new/adjacent instrumentation or auth-secret-guard test file: `assertAuthSecretConfiguredInProduction({DB_BOOTSTRAP:'live'})` (no AUTH_SECRET, no CF Access config) throws; `assertAuthSecretConfiguredInProduction({DB_BOOTSTRAP:'live', AUTH_SECRET:'x'})` does not throw; `assertAuthSecret…
  - NEW test in test/middleware-auth.test.ts: `vi.stubEnv('DB_BOOTSTRAP','live'); vi.stubEnv('AUTH_SECRET',''); vi.stubEnv('CF_ACCESS_TRUST_EMAIL_HEADER','');` then call middleware() on `/api/dashboard` with no session cookie — assert `res.status === 401` (or redirect for a page route), NOT 200 with `x…
  - NEW test for request-user.ts: call `resolveRequestUser(request)` with DB_BOOTSTRAP='live' stubbed and a Request object that has NO `x-authenticated-user-email` header at all (not even empty) — assert it throws/refuses rather than returning `{userId: 'local'}`. FAILS today; PASSES after the has()-vs…
- **Risks / what could break** This is the highest blast-radius plan in the set — a wrong DB_BOOTSTRAP check in local/CI/Cursor-Cloud environments (which do NOT set DB_BOOTSTRAP at all) would leave the dev fallback fully intact there (DB_BOOTSTRAP undefined !== 'live'), which is exactly the desired outcome, but double-check the Coolify prod boot script (scripts/coolify-prod-start.sh:184) actually exports DB_BOOTSTRAP into the Next.js server process's env (not just a wrapper shell's env) before relying on it in middleware.ts's edge/node runtime — verify with `docker exec` or the ops-snapshot script rather than assuming. The ~34-test middleware-auth.test.ts suite and the two request-user.ts-adjacent test files must ALL be…

### `ios-state-outcome-truth` (effort M)

Confirmed exactly, plus one additional confirmed mechanism. clearLocalSession (ios/SocraticTrade/MobileStore.swift:628-650) nils every in-memory @Published property but never calls UserDefaults.standard.removeObject for the 'cached_mobile_snapshot_data' key (cacheKey, :136); init (:147-154) unconditionally re-reads that blob on cold launch via loadCachedSnapshot(), sets isAuthenticated = (cached != nil) — true — and fabricates lastUpdatedAt = Date() rather than the blob's real capture time, so a signed-out/deleted account's stale holdings render as the CURRENT, freshly-loaded state offline. store.error (@Published, single global property) has exactly one renderer — InlineErrorBanner inside SnapshotScaffold (AppComponents.swift:580-586), which lives on the home view's scroll content at the…

- **Files** `ios/SocraticTrade/MobileStore.swift`, `ios/SocraticTrade/MobileAPIClient.swift`, `ios/SocraticTrade/PolicyTightening.swift`
- **Approach** (1) MobileStore.swift, clearLocalSession() (:628-650): add `UserDefaults.standard.removeObject(forKey: Self.cacheKey)` (and a new companion timestamp key, see (2)) alongside the existing in-memory resets, so a cold launch after sign-out/account-deletion has nothing stale to read back. (2) Persist the REAL capture time instead of fabricating it: add `private static let cacheTimestampKey = "cached_mobile_snapshot_saved_at"`; in saveCachedSnapshot (:143-145) also write `UserDefaults.standard.set(Date().timeIntervalSince1970, forKey: cacheTimestampKey)` (call this from load()'s success path at :288, same call site); in loadCachedSnapshot (:138-141) also read that companion double back; in init (:147-154) set `lastUpdatedAt = cached == nil ? nil : (real saved timestamp, or nil if the companion key is missing)` instead of `Date()` — this makes isSnapshotStale(:221-224)'s existing >180s check correctly treat old cached data as stale immediately on cold launch (today it looks artificially fresh for 180s). (3) Add `func hasSessionCookie() -> Bool { HTTPCookieStorage.shared.cookies?.contains(where: ownsCookie) ?? false }` to MobileAPIClient.swift (near ownsCookie, :350-356) — the session's real authority already lives in the system cookie jar, which clearLocalSession already correctly purges (:633-635); change MobileStore.init's `isAuthenticated = cached != nil` (:151) to `isAuthenticated = previewSnapshot != nil || (cached != nil && client.hasSessionCookie())` so 'we have a cached blob' can no longer stand in for 'this session is actually valid' — the `previewSnapshot != nil` branch is kept unconditional so ControlCatalogTests.swift's three MobileStore(client:previewSnapshot:) fixtures (which never set up real cookies) are unaffected. (4) Add a view-anchored outcome signal deco…
- **Tests that must fail first**
  - New XCTest in ios/SocraticTradeTests (e.g. extend MobileModelsTests.swift or a new MobileStoreSessionTests.swift): seed UserDefaults with a fake cached_mobile_snapshot_data blob for account A, construct MobileStore(client:) (no previewSnapshot, so it reads the real cache), assert snapshot/isAuthent…
  - New test: seed the cache blob without the new cacheTimestampKey (simulating data written before this change, or the fresh-write path); after clearLocalSession + a real load() cycle writes both keys; construct a new MobileStore and assert lastUpdatedAt equals the persisted save time (not the constru…
  - New test: construct MobileStore with a cached blob present but NO session cookie in HTTPCookieStorage; assert isAuthenticated == false — MUST FAIL before the fix (isAuthenticated is derived from cache presence alone). Confirm the existing three ControlCatalogTests.swift call sites (lines 68-71, 84-…
  - New test exercising submit()'s two outcome branches directly (mock MobileAPIClient's submit to succeed/fail): assert store.lastCommandOutcome is set to .success/.failure accordingly on each call — MUST FAIL before the fix (no such property exists).
- **Risks / what could break** Changing isAuthenticated's derivation could regress the 'offline first paint' UX the cache exists for — verify SocraticTradeTests/MobileModelsTests.swift:158 and :367's MobileStore(client:) construction sites (no previewSnapshot passed) to see whether they rely on cache-only isAuthenticated=true; if so they will need an explicit cookie stub (there may already be a test-only cookie-seeding helper, or one needs adding as part of this change). This is a Swift/iOS-only change — CLAUDE.md's iOS build-loop rule applies: `npm run lint`/`tsc`/`npm test`/`npm run build` do NOT compile Swift at all, so `xcodebuild build -project 'ios/Socratic Trade.xcodeproj' -scheme SocraticTrade -destination 'gener…

### `market-cache-freshness` (effort M)

Verified both anchor functions directly. src/lib/market-hours.ts's isWeekendOrHolidayClosureAhead (288-292) is `if (!isEtCalendarTradingDay(today)) return true; return !isEtCalendarTradingDay(addEtCalendarDays(today, 1));` — for ANY time on a Friday, today IS a trading day (first branch false) but tomorrow (Saturday) is not, so this returns true for the ENTIRE Friday session, not just after close. expiresAtRespectingMarketClose (305-311) then extends any cache write made at any point on Friday to `nextMarketOpenStrictlyAfterMs` = Monday 09:30 ET, freezing 5-minute screener TTLs, OHLC caches, and 6h enrichment caches through what should be a fully live trading day (and identically the day before any holiday). src/lib/history.ts's isBarSeriesFresh (115-124) computes `ageMs = now - lastBarMs…

- **Files** `src/lib/market-hours.ts`, `src/lib/history.ts`
- **Approach** 1) market-hours.ts: replace isWeekendOrHolidayClosureAhead's calendar-day check with a session-boundary check. Add `const MARKET_CLOSE_HOUR_ET = 16; const MARKET_CLOSE_MINUTE_ET = 0;` (mirroring the existing MARKET_OPEN_HOUR_ET/MINUTE_ET at line 263) and an early-close table `const EARLY_CLOSE_DATES: Record<string, {hour:number;minute:number}> = {}` populated per-year the same way getMarketHolidays (113) builds its holiday set (day-after-Thanksgiving = 4th Thursday of Nov + 1 day; Christmas Eve = Dec 24 when a weekday; July 3 when a weekday and not itself a holiday) — export a `getEarlyCloses(year)` alongside the existing `getMarketHolidays(year)`. Rewrite isWeekendOrHolidayClosureAhead(nowMs) to: compute `todayParts = etDateParts(new Date(nowMs))`; if `!isEtCalendarTradingDay(todayParts)` return true (unchanged, correct); else compute today's actual close instant via `etWallClockToUtcMs(todayParts, closeHourFor(todayParts), closeMinuteFor(todayParts))` where closeHourFor/closeMinuteFor consult EARLY_CLOSE_DATES for today's Y-M-D key, defaulting to MARKET_CLOSE_HOUR_ET/MINUTE_ET; if `nowMs < todayCloseMs` return false (today's session hasn't ended yet — do NOT extend, a normal-TTL refetch is correct and desired); else (today's session has ended) fall through to the existing `!isEtCalendarTradingDay(addEtCalendarDays(todayParts, 1))` check unchanged. Also apply EARLY_CLOSE_DATES to currentMarketSession's hardcoded `CLOSE = 16*60` (8-60) so half-days correctly read 'closed' 13:00-16:00 ET (mdi-10) — thread the date through the existing Intl.DateTimeFormat parts already computed there. 2) history.ts: replace isBarSeriesFresh's flat 3-calendar-day window with a trading-session-aware check. New contract: `isBarSeriesFresh(bars, now)` (drop the maxStalenessDays parameter — a…
- **Tests that must fail first**
  - NEW test in a market-hours test file: `isWeekendOrHolidayClosureAhead` (or the exported `expiresAtRespectingMarketClose` it feeds) called with a Friday 2026-08-21T14:30:00Z (10:00 ET) and a 5-minute baseTtlMs — assert `expiresAtRespectingMarketClose(...)` returns `nowMs + 5*60_000` (the naive expir…
  - Same function called with a Friday 2026-08-21T21:00:00Z (17:00 ET, after close) — assert it DOES extend to Monday open (the genuine weekend-closure case must still work).
  - NEW test for isBarSeriesFresh: build a bars array whose last bar is dated Monday, call with `now` = Wednesday same week (no early close/holiday between) — assert `isBarSeriesFresh` returns false (stale — Tuesday's session bar is missing) where the OLD 3-day-window version returned true. FAILS today…
  - NEW test for getEarlyCloses/currentMarketSession: `currentMarketSession(new Date('2026-11-27T18:30:00Z'))` (13:30 ET, day after Thanksgiving) should return 'closed' or 'post', not 'regular' — the register's own tsx probe confirms today's code returns 'regular'.
- **Risks / what could break** expiresAtRespectingMarketClose and isBarSeriesFresh are both hot, widely-called cache-freshness primitives (screener, OHLC, enrichment, universe caches per mdi-01's evidence) — a mis-implemented close-time comparison could flip from 'never extends' (worse: burns provider quota every tick on weekends, the original bug this function was written to fix) to 'always extends' (reintroducing mdi-01) if the early-close/close-hour math has an off-by-one. Test both directions explicitly (Friday morning must NOT extend; Friday evening MUST extend) before landing. isBarSeriesFresh's signature change (dropping maxStalenessDays) is a breaking change to its exported contract — grep all callers (only histo…

### `order-provenance-guard` (effort M)

Confirmed: no enqueue or pump path checks who placed an order before auto-cancel-replacing it. src/lib/stale-limit-orders.ts's listStaleLimitOrders (23-49) lists every working sell/cover limit/stop_limit order older than staleLimitOrderMinutes with the ONLY exclusion being isWorkingOrderState — it does not read EquityOrder.orderClass at all. isHeldExitLeg (132-134) — used to suppress the ALERT, not enqueue — only checks state==='held', so an ACTIVATED bracket take-profit leg (state flips held->new when its entry fills, orderClass stays 'bracket'/'oco'/'oto' per types.ts:939's comment) is neither excluded from listStaleLimitOrders NOR from the alert. src/lib/order-replacement.ts's autoRemediateStaleExitOrders (732-793) enqueues a cancel_requested row for every item in that list whose side…

- **Files** `src/lib/order-replacement.ts`, `src/lib/stale-limit-orders.ts`, `src/lib/order-cancel.ts`, `src/lib/broker-protective-stops.ts`, `app/console/orders/cancel-sheet.tsx`, `ios/SocraticTrade/OrderCancel.swift`
- **Approach** 1) tsx-02 (AUTO cancel-replace must never touch a bracket leg or a non-app order): in order-replacement.ts's autoRemediateStaleExitOrders loop (~750-758), import `isBracketOrderClass` from './broker-side' and add, right after the existing `if (String(item.order.state...) === 'held') continue;`: `if (isBracketOrderClass(item.order.orderClass)) { out.deferred++; audit('stale_exit_auto_remediation_skipped_bracket_leg', {orderId: item.order.id, symbol, side, orderClass: item.order.orderClass}, userId, input.policy.connectedAccountId); continue; }` and `if (!item.order.clientOrderId) { out.deferred++; audit('stale_exit_auto_remediation_skipped_not_app_placed', {orderId: item.order.id, symbol, side}, userId, input.policy.connectedAccountId); continue; }` (our own placement always sets client_order_id=refId per alpaca.ts:746/674 and robinhood.ts:442 — absence means the owner placed it directly at the broker). Deliberately do NOT touch listStaleLimitOrders itself or replaceStaleLimitOrderWithMarket (order-replacement.ts:99-256, the owner-initiated manual replace) — an owner explicitly clicking 'replace with market' on their own resting take-profit leg is a legitimate intentional action, only the AUTOMATED 15-minutes-later path is the bug. 2) tsx-14 (misleading alert + audit spam on bracket legs): in stale-limit-orders.ts's notifyStaleLimitOrders loop (~66-68), broaden the existing suppression from `if (isHeldExitLeg(item.order)) continue;` to `if (isHeldExitLeg(item.order) || isBracketOrderClass(item.order.orderClass)) continue;` (isBracketOrderClass already imported for step 1) so an ACTIVATED bracket leg stops generating the 'cancel/reprice before replacing' push too. Route the per-tick 'stale_exit_auto_remediation_deferred' audit at order-replacement.ts:760-767 through `aud…
- **Tests that must fail first**
  - test/stale-limit-orders.test.ts — extend the existing 'alerts a bracket exit leg once it activates' fixture (line ~76) with `orderClass: 'bracket'` on the activated-and-stale order and add a NEW assertion `expect(listStaleLimitOrders([...], {staleLimitOrderMinutes:15}, now)).toEqual([])` when order…
  - NEW test in a file adjacent to order-replacement.test.ts (e.g. add to that file): construct a stale sell limit order with `orderClass: 'bracket'`, call autoRemediateStaleExitOrders, assert `out.attempted === 0` and no row was inserted into order_replacements for that orderId (query order_replacemen…
  - NEW test: cancel a broker_protective_stops-tracked order via cancelWorkingOrder, then call reconcileBrokerProtectiveStops for the same symbol/position (unchanged quantity) and assert NO new broker order was placed (placed counter stays 0) and the position row remains stop-less. FAILS today (the sto…
- **Risks / what could break** test/order-replacement.test.ts (856+ region) and test/broker-protective-stops.test.ts pin a lot of existing cancel/replace/reconcile behavior with `order()`/fixture helpers that likely omit orderClass and clientOrderId by default (undefined) — verify the new guards don't accidentally exclude those fixtures' intended-eligible orders (undefined clientOrderId would now be treated as 'not app-placed' and skip auto-remediation; if any existing test relies on auto-remediation firing for a fixture with no clientOrderId set, it will need `clientOrderId: 'some-ref'` added). isBracketOrderClass(undefined) correctly returns false (typeof check), so untagged fixtures are unaffected by that half. The po…

### `per-account-visibility` (effort M)

Verified: app/console/settings/brokers.tsx's `renderAccountRow` (line 189) computes `stateInfo` only `isCurrentLoaded && snapshot?.policy ? deriveStateInfo(snapshot.policy) : null` — every OTHER connected account always renders a static null-state chip even while genuinely running Autopilot, despite the snapshot already carrying a real per-account projection (`snapshot.connectedAccountPolicies`, built read-only via `peekPolicy(userId, account.id)` in src/lib/dashboard.ts:350-368, and ALREADY correctly consumed this exact way by app/console/components/chrome.tsx:140-141's account-switcher). The `pendingCount` at line 191 filters `snapshot.pendingProposals`, which src/lib/dashboard.ts:690 (`listPendingProposals(accountNumber, userId)` where `accountNumber = policy.accountNumber`, the ACTIVE…

- **Files** `app/console/settings/brokers.tsx`, `src/lib/dashboard.ts`, `src/lib/db-proposals.ts`, `app/dashboard-types.ts`, `app/console/decisions/page.tsx`, `app/console/decisions/decisions-list.tsx`
- **Approach** brokers.tsx: replace the `isCurrentLoaded ? deriveStateInfo(snapshot.policy) : null` line (189) with `const policyForAccount = snapshot.connectedAccountPolicies?.[account.id]; const stateInfo = policyForAccount ? deriveStateInfo(policyForAccount) : null;` — this is the exact pattern already proven correct in chrome.tsx:140-141, applied uniformly to every account row (active or not). For pendingCount: add a lightweight `countPendingProposals(accountNumber: string, userId: string): number` to src/lib/db-proposals.ts (`SELECT COUNT(*) as n FROM trade_proposals WHERE account_number = ? AND user_id = ? AND status = 'proposed'` — deliberately a COUNT query, not `listPendingProposals`, to avoid parsing full proposal/decision JSON blobs for every connected account on every snapshot). In src/lib/dashboard.ts, alongside the existing `connectedAccountPolicies` construction (~line 350-368), add a parallel `connectedAccountPendingCounts = Object.fromEntries(connectedAccounts.map((account) => [account.id, account.accountNumber ? countPendingProposals(account.accountNumber, userId) : 0]))` and include it in the returned snapshot object; add `connectedAccountPendingCounts?: Record<string, number>` to `DashboardSnapshot` (app/dashboard-types.ts, additive/optional). In brokers.tsx, replace the whole `pendingCount = isCurrentLoaded ? ... : ...filter(...)` block (lines 191-194) with a single `const pendingCount = snapshot.connectedAccountPendingCounts?.[account.id] ?? 0;` — correct for every account uniformly, no special-casing. For /console/decisions: convert app/console/decisions/page.tsx from its bare `fetch` to `useConsoleData()` (matching every other console page) so it has `snapshot.connectedAccounts` for an id->label map (`Object.fromEntries(snapshot.connectedAccounts.map(a => [a.i…
- **Tests that must fail first**
  - new test/db-proposals-count.test.ts — seed two proposed-status trade_proposals rows for account A and zero for account B, assert `countPendingProposals('A', 'local') === 2` and `countPendingProposals('B', 'local') === 0` — new function, no prior test, straightforward pin.
  - new test (or extend an existing dashboard.test.ts) — build a snapshot with two connected accounts where only the INACTIVE one has a proposed proposal, assert `snapshot.connectedAccountPendingCounts[<inactive account id>] === 1` — FAILS today (that count doesn't exist in the snapshot at all) and PAS…
  - new React test for decisions-list.tsx — render `DecisionsList` with `decisions` spanning two different `connectedAccountId`s and an `accountLabelById` map with 2 entries, assert both account labels are visible in the rendered output — fails today (component renders no account text at all).
- **Risks / what could break** No existing test directly exercises brokers.tsx's `renderAccountRow` (grepped test/*.ts and test/*.tsx for brokers.tsx/renderAccountRow — zero hits), so this is a clean fix with no pinned-behavior regression risk on that front. `countPendingProposals` is a new, narrowly-scoped read-only query — no migration concern. The decisions/page.tsx switch from bare fetch to `useConsoleData()` changes its loading/polling behavior (picks up the 15s snapshot poll instead of a one-shot fetch-on-mount) — verify this doesn't introduce an unwanted extra network waterfall or change the page's existing loading-state UX noticeably; if that's undesirable, an alternative is a second, smaller fetch of just `snaps…

### `placement-outcome-truth` (effort L)

executeProposal (src/lib/strategy-execution.ts:232-1490) returns a {status,...} object instead of throwing for every non-placement outcome (busy/blocked/error/not_placed), and mobile-api.ts's runCommand treats any resolved value as success. Confirmed live: mobile-api.ts:965-968 calls executeProposal and returns the result verbatim; executeMobileCommand (mobile-api.ts:1077-1080) always stamps finishCommand(command,'succeeded',result) unless something THROWS. PublicMobileCommand (mobile-api.ts:134-147) already carries `result?: unknown` in the wire payload, but ios/SocraticTrade/MobileModels.swift's MobileCommand struct (lines 511-521) has no `result` field, so MobileStore.swift:214-215 maps status 'succeeded' -> .succeeded and ProposalsView.swift:481 paints 'Approved — waiting for desk ref…

- **Files** `src/lib/mobile-api.ts`, `src/lib/strategy-execution.ts`, `src/lib/strategy.ts`, `src/lib/alpaca.ts`, `app/console/lib/approve-feedback.ts (new)`, `app/console/page.tsx`, `app/console/components/approval-card.tsx`, `ios/SocraticTrade/MobileModels.swift`, `ios/SocraticTrade/ActivityView.swift`
- **Approach** 1) mobile-api.ts: add `class ProposalNotPlacedError extends Error { result: unknown; constructor(message: string, result: unknown) { super(message); this.name = 'ProposalNotPlacedError'; this.result = result; } }` near the other command error classes (~line 166-184). Add `const PLACEMENT_SUCCESS_STATUSES = new Set(['placed','filled','paper'])`. In runCommand's 'proposal.approve' case (965-968), after calling executeProposal, `if (!PLACEMENT_SUCCESS_STATUSES.has(result.status)) throw new ProposalNotPlacedError(result.reasons?.join(' ') || `Order was not placed (status: ${result.status}).`, result);` else return result. In errorPayload() (1056-1075) add a branch: `if (error instanceof ProposalNotPlacedError) return { message: error.message, result: error.result };` — this makes finishCommand(command,'failed',payload.result,payload.message) stamp status='failed' with the real reason as command.error (which ProposalsView.swift's existing `.failed(action, message)` branch and ActivityView's `command.error` label already render honestly — no further iOS change needed to kill the false-green-check bug). 2) For 'strategy.run_once' (952-956): do NOT broaden the throw condition (skipped_market_closed/skipped_budget are not errors — throwing would misrepresent them as failures). Instead add `interface MobileCommandResultSummary { status?: string; summary?: string }` decoded on iOS: in MobileModels.swift add `let result: MobileCommandResult?` to `MobileCommand` where `struct MobileCommandResult: Decodable { let status: String?; let summary: String? }` (extra JSON keys are ignored by Decodable by default, so this is safe against the richer server payload). In ActivityView.swift's CommandActivityRow, render `command.result?.summary` under the status pill when present and non-empty,…
- **Tests that must fail first**
  - test/mobile-api.test.ts — NEW test 'marks proposal.approve failed when executeProposal reports busy': reuse test/approval-lock.test.ts's setPaperPolicy/makeProposalId pattern, acquireStrategyLock('simulated-run', userId, accountId) before queueMobileCommand({commandType:'proposal.approve', payload:…
  - test/order-confirmation-status.test.ts — extend the mocked Alpaca `createOrder` with a module-level `let mockOrderThrowError: (Error & {response?:{status:number}}) | null = null` that createOrder throws when set (instead of returning). NEW test: set `mockOrderThrowError = Object.assign(new Error('T…
- **Risks / what could break** Existing tests pin the CURRENT 4xx-is-always-terminal behavior and must be re-checked: test/order-confirmation-status.test.ts:192-207 (400/'rejected' synchronous state — different code path, unaffected) and test/placement-reconcile.test.ts:387-400 ('throw + order PRESENT (declined) -> rejected_by_broker') — confirm that test's simulated error message does NOT fall into the new 'retryable' bucket (it should stay 'rejected_terminal' if it's a genuine 400/403/422). The mobile-api throw-on-non-success change must not fire for proposal.reject or other command types (scoped narrowly to the 'proposal.approve' case only). tsx-12 (a Retry button/endpoint for not_placed) is NOT included here — flag a…

### `pnl-basis-labels` (effort M)

Confirmed the core shape. app/console/results/page.tsx's BucketCard (940-1000) renders 'Realized P&L' / 'Unrealized P&L' / 'Win rate' / 'Avg return / closed capital' with a basis tooltip ONLY on the 'Avg return' field (title attribute at line ~975) — Realized and Unrealized carry no basis label at all, and per the register's independently-verified note, the THREE surfaces compute genuinely different numbers for the same nominal metric: Results' Unrealized comes from perf.liveUnrealizedPnl (calculatePnl over app-booked lots, performance.ts:640-660), Home's derive.ts:797-800 deriveMarkToMarket uses broker averageCost x quantity (a different basis), and iOS's ResultsView.swift AccountMetrics.displayedUnrealized prefers broker positions with a ledger fallback (MobileModels.swift:254-270) — a…

- **Files** `app/console/results/page.tsx`, `app/console/lib/derive.ts`, `src/lib/performance.ts`, `ios/SocraticTrade/ResultsView.swift`
- **Approach** 1) Zero-lot honesty (perf-06, S, do first — cheapest and most visible): add `closedLotCount: number` to PerformanceSummary (performance.ts, alongside where liveWinRate/liveAvgReturn are computed ~1707-1721), computed as `lots.length` before the winRate/averageReturn calculation. In results/page.tsx's BucketCard, gate the Win rate and Avg return cells on `closedLotCount > 0` (replace the current always-numeric render at ~957/975 with `EM_DASH` when `closedLotCount === 0`, mirroring the pattern the file already uses elsewhere for missing data — 'never fabricate' per format.ts's own header comment). In ios/SocraticTrade/ResultsView.swift (~71-84), add the same `closedLotCount > 0` gate before formatting winRate/avgReturn, falling back to the app's standard em-dash placeholder. 2) Basis labels on Results tiles (perf-05, S): add a `title=` tooltip (matching the existing pattern on the Avg-return cell) to the Realized P&L and Unrealized P&L `<div className='con-card-title'>` headers in BucketCard (940-953): Realized -> 'All-time, app-booked closed lots. Exits of positions this app did not open (pre-app holdings, manual trades) are not included.'; Unrealized -> 'Broker cost basis (average entry price x quantity), not the app's lot ledger.' Pick ONE canonical basis per surface per the register's recommendation: since Home (derive.ts) and iOS already prefer broker-cost-basis, change Results' BucketCard Unrealized to also source from broker cost basis (deriveMarkToMarket-equivalent) rather than perf.liveUnrealizedPnl, reserving the lot-ledger figure for the Tax/lots section specifically labeled 'app-booked lots' — this is the one actual behavior change in this item; everything else is copy. 3) Name the SPY benchmark window (perf-04, S): in results/page.tsx's benchmark Stat (~234…
- **Tests that must fail first**
  - NEW test: PerformanceSummary computed from zero closed lots — assert `closedLotCount === 0` and that a snapshot test / render of BucketCard shows EM_DASH for Win rate and Avg return, not '0%'/'+0.00%'. FAILS today (renders fabricated zeros); PASSES after the closedLotCount gate.
  - NEW test for derive.ts's mark-to-market percentage: build one long position (cost basis +1000) and one short position (cost basis -1000, i.e. net signed sum = 0) with a real unrealized gain on the short; assert `unrealizedPct` is NOT undefined/0 (today's `costBasis > 0` check fails when net signed…
  - NEW test for performance.ts's synthetic paper curve: call the curve-builder with paperSnapshots=[] and a non-empty fills list; assert the returned curve is empty (or however the fix represents 'no real equity data'), not `[{equity: 100+realized, ...}, ...]`. FAILS today; PASSES after removing the f…
- **Risks / what could break** Item 2 (switching Results' Unrealized P&L source from the lot ledger to broker cost basis) is a real behavior/number change, not just a label — verify no existing test in test/*.ts (grep for 'liveUnrealizedPnl' assertions in results-page-adjacent tests) pins the current lot-ledger-sourced value; if any does, it is pinning the inconsistency this item intentionally removes and should be updated, not left red without review. The zero-lot em-dash gate must not accidentally suppress a genuinely-zero (0%) win rate for an account that DOES have exactly one break-even closed lot — gate strictly on `closedLotCount > 0`, not on `winRate === 0`. iOS changes need the standard `xcodebuild build` verific…

### `price-alert-evaluation` (effort M)

checkPriceAlerts (src/lib/alerts.ts:63-116) gates on the console-active account's accountNumber, calls only that one broker gateway, swallows any gateway error into an empty result with no log/audit, applies no staleness check, and validates symbols with a looser regex than the rest of the app — confirmed exactly as described; premise holds.

- **Files** `src/lib/alerts.ts`
- **Approach** In src/lib/alerts.ts: (1) delete `getBrokerGateway` import and the `accountNumber`/gateway-based quote fetch at lines 67-78; replace with `const policy = getPolicy(userId);` (still needed for sendNotification) plus `const quotes = await fetchFreshQuotesCascade(symbols, userId)` imported from './quotes-cascade' — this is USER-scoped (cascades across every connected broker plus Alpaca/Yahoo/ROIC fallbacks per its own doc comment at quotes-cascade.ts:132-148), so it works with zero, one, or many connected accounts and never needs an active account's accountNumber. (2) Wrap the cascade call's catch block to `console.error` and `audit("alert.check_error", { userId, symbols, error }, userId)` before returning `[]`, so a real provider outage is distinguishable from "nothing armed" in the audit trail (mirrors the pattern already used at quotes-cascade.ts:236-238 for its own internal levels). (3) Inside the per-alert loop (current lines 80-114), after resolving `quote = quotes[alert.symbol]`, call `quoteAgeSecForStalenessGate(quote, Date.now())` (imported from './quotes-cascade') and `continue` (skip, don't trigger) when `missing` or `ageSec > (policy.maxQuoteAgeSec ?? 120)` — mirrors the exact isStale formula at src/lib/policy.ts:413. (4) Delete the local `SYMBOL_RE` regex at line 17 and its use in `createAlert` (line 33); import and use `isValidAppSymbol` from './index-universes' instead, so an alert can never be armed on a symbol the rest of the app (watchlist/universe) would already reject.
- **Tests that must fail first**
  - New test/price-alerts-evaluation.test.ts (file does not exist today — zero tests cover this loop): (a) 'triggers with zero connected accounts, using cascade market data alone' — seed a user with an armed alert and NO connected accounts, mock/stub fetchFreshQuotesCascade (or seed a real Yahoo-fallba…
  - (b) 'logs and audits a cascade failure instead of returning silently' — force fetchFreshQuotesCascade to reject, call checkPriceAlerts, assert an 'alert.check_error' audit row exists — MUST FAIL today because the empty catch at alerts.ts:76-78 has no audit/log call.
  - (c) 'does not trigger on a stale quote' — seed an armed alert whose threshold the quote crosses, but stamp the quote's asOf older than policy.maxQuoteAgeSec, assert the alert stays armed (not triggered) — MUST FAIL today because current code has no staleness check at all (line 83-86 acts on any num…
  - (d) 'rejects a symbol createAlert would reject elsewhere' — call createAlert with a symbol isValidAppSymbol rejects but the old SYMBOL_RE accepted (e.g. a leading-dot or double-dot string), assert { error: 'INVALID_SYMBOL' } — MUST FAIL today since the old regex accepts it.
- **Risks / what could break** fetchFreshQuotesCascade is heavier than a single gateway.getEquityQuotes call (it tries multiple providers in series) — checkAllUserPriceAlerts (alerts.ts:118-122) already runs per-user sequentially on a scheduler cadence, so latency grows modestly per user with armed alerts; acceptable since alerts already tolerate a several-second cascade elsewhere in the app. Tightening symbol validation via isValidAppSymbol is a behavior change for createAlert: it now allows digits (old regex disallowed them entirely) and rejects leading/double/trailing dots the old regex allowed — grep test/watchlist-alerts.test.ts and any other createAlert callers (chat-injection.test.ts, chat-orchestrator.test.ts, at…

### `prompt-trust-boundary` (effort M)

Confirmed multi-part trust-boundary gap in the learning pipeline that feeds the strategy prompt. src/lib/learning-review.ts's `applyLearningReviewVerdicts` (line 631) applies ANY 'keep' verdict on a `learned_context_pending` row via `applyApprovedPending(pending, nowIso)` (learned-context/store.ts:397-405) with no check of `pending.riskTier` — and `applyApprovedPending` DIRECTLY calls `setStrategyPrompt(merged, ...)` when `riskTier === 'strategy-directive'` (store.ts:400-404), merging content straight into the one prompt region the app never scans for injected instructions. `learningReviewMode` defaults to `'decide'` (defaults.ts:90) — fully automated, no human in the loop — once the (opt-in, off-by-default) learning review feature is enabled at all. The candidate pipeline feeding this: s…

- **Files** `src/lib/learning-review.ts`, `src/lib/learned-context/store.ts`, `src/lib/learned-context/semantic-gate.ts`, `app/api/chat-history/route.ts`
- **Approach** Require a human for anything landing in the strategy prompt, regardless of `learningReviewMode`: in `applyLearningReviewVerdicts` (learning-review.ts, inside the `verdict.verdict === 'keep'` branch ~line 653), before calling `applyApprovedPending`, check `pending.riskTier === 'strategy-directive'` — if so, do NOT auto-apply even under `'decide'` mode; instead route it the same way the function already handles 'defer' (leave status pending, attach the reviewer's reasoning via `setPendingLearnedContextReviewNote`, so it surfaces in the human review queue) and audit a new `learning_review_strategy_directive_held_for_human` event so this is visible, not silent. This is a narrow, additive carve-out — every other riskTier ('fact', 'risk') keeps today's `learningReviewMode`-driven behavior unchanged, so it doesn't reintroduce blocking ceremony on the vast majority of learned-context flow, only on the one tier that becomes literal LLM instructions. Ingest-time scan placement: since `ingestLearned` already runs `classifyWithSemanticGate` on every candidate regardless of caller-supplied `kind` (confirmed), the scan IS already 'at ingest' for the classifier's purposes — the actual gap is observability, not placement. In semantic-gate.ts's `classifyWithSemanticGate` (~line 121, the `try` block around the LLM call), check `llm instanceof MockLLM` (or add an explicit `resolvedMock: boolean` return from `getLLM`/a cheap `isMockLLM(llm)` type guard) when `opts.llm` was not explicitly injected, and `audit('semantic_gate_mock_llm_fallback', { userId: opts.userId, subject: candidate.subject }, opts.userId)` when it fires in a real (non-test) call — this makes the previously-silent 'safety net is inert' condition observable in the audit trail without changing the fail-safe behavior itself…
- **Tests that must fail first**
  - test/learning-review.test.ts — new test: build a `learned_context_pending` row with `riskTier: 'strategy-directive'`, a verdict of `'keep'`, call `applyLearningReviewVerdicts`, assert (a) `getStrategyPrompt(userId)` does NOT contain the pending row's value, and (b) the row's status stays `'pending'…
  - test/semantic-gate.test.ts — new test: call `classifyWithSemanticGate` without an injected `opts.llm` and with no LLM credential resolvable for the test userId (forcing MockLLM), assert an audit row of kind `semantic_gate_mock_llm_fallback` was written — fails today (no such audit call exists anywh…
  - new/extended test for app/api/chat-history/route.ts — assert the route module exports no `POST` (or that a POST request 405s) — fails today (POST succeeds and writes a turn), passes after deletion.
- **Risks / what could break** The strategy-directive carve-out changes real behavior for anyone who has BOTH `learningReviewEnabled: true` AND relies on fully automated 'decide' mode approving strategy-directive-tier lessons today — per CLAUDE.md this is a sole-user app and the feature is off by default, so blast radius is low, but confirm with the owner this is acceptable before landing (it's a deliberate, scoped exception to their 'decide' mode, not a blanket new ceremony — every other tier is untouched). The MockLLM audit-only change is pure observability, zero behavior change, essentially risk-free. Deleting the chat-history POST route is safe per the exhaustive grep of every client caller, but re-run that grep imme…

### `quote-value-provenance` (effort L)

Confirmed the core defect at all three cited anchors. (1) src/lib/data-providers.ts's takeScalar (~1560-1590): `asOf = supplied?.asOf ?? currentRecord?.fieldDates?.[field] ?? ... ?? cascadeFetchedAt` where `cascadeFetchedAt = new Date().toISOString()` (line 1534) — any provider field lacking its own timestamp (e.g. parseNasdaqQuoteInfo's price/change/volume, cached 6h under generic ttlMs()) gets stamped as observed 'now'. evidence-facts.ts's arbitrateFieldObservation (100-155) is recency-first for volume, so a 6h-stale cached value with a fabricated 'now' timestamp wins over a genuinely fresh Alpaca dailyBar.v. (2) src/lib/market.ts's toQuoteOnlyMarketQuote (1523-1528) hardcodes `asOf: new Date().toISOString()` even though `quote.asOf` (YahooFinanceQuote.asOf, yahoo-finance.ts:76-77 — der…

- **Files** `src/lib/data-providers.ts`, `src/lib/market.ts`, `src/lib/quotes-cascade.ts`, `src/lib/alpaca.ts`, `app/console/watchlist/page.tsx`, `app/console/ui/symbol-drilldown.tsx`
- **Approach** PRIMARY (do first, highest leverage): (a) market.ts:1523-1528 toQuoteOnlyMarketQuote — change `asOf: new Date().toISOString()` to `asOf: quote.asOf` (already on YahooFinanceQuote; undefined when Yahoo gave no regularMarketTime, which correctly falls through to isQuoteFresh's existing 'missing -> stale' branch). (b) quotes-cascade.ts:397 ROIC profile block — same swap: drop the fabricated `new Date().toISOString()`; if ROIC's response has no timestamp field, leave `asOf: undefined` rather than inventing one (checked: `isQuoteFresh` already handles undefined asOf as not-fresh, so downstream `if (isQuoteFresh(q, nowMs, maxAgeMs)) result[symbol] = q;` naturally excludes it — matches existing intent, just currently defeated by the fabrication). (c) alpaca.ts:548-556 getEquityQuotes — change `price: ask ?? bid ?? 0` to a real mid: `price: (typeof bid === 'number' && typeof ask === 'number') ? (bid + ask) / 2 : ask ?? bid ?? 0`, keep bid/ask on the BrokerQuote object unchanged (already present) so tooltips can still show ask/bid explicitly; do NOT touch intradayChangePct here (out of scope — that's computed elsewhere from prevClose, already tracked separately per mdi-05's own note). SECONDARY (data-providers.ts takeScalar, mdi-03 — larger, needs its own care because 20+ providers funnel through this one function): stop defaulting `fetchedAt`/`asOf` to `cascadeFetchedAt` for FIELDS that came from a cache write with no observation timestamp of their own — instead, when a provider's raw parse (e.g. parseNasdaqQuoteInfo) does not itself carry a timestamp, store `undefined` for that field's `asOf` (keep `fetchedAt: cascadeFetchedAt` — that one IS honestly 'when we fetched the cache entry', just not 'when the underlying value was true') and update evidence-facts.ts's `recency()` me…
- **Tests that must fail first**
  - NEW unit test for market.ts's toQuoteOnlyMarketQuote: feed a YahooFinanceQuote with `asOf: '2026-08-15T14:00:00Z'` and a `now` several hours later; assert the returned MarketQuote.asOf equals the INPUT asOf (not a value close to `now`). FAILS today (asOf is always ~now); PASSES after the fix.
  - NEW unit test for quotes-cascade's isQuoteFresh/quoteAgeSecForStalenessGate integration: after the ROIC-block fix, assert a ROIC-sourced quote whose upstream gave no price timestamp is excluded from `result[symbol]` (i.e. `isQuoteFresh` returns false for it) rather than always passing. FAILS today…
  - NEW unit test for alpaca.ts getEquityQuotes: mock getLatestQuotes returning `{bp: 199, ap: 201}`; assert `quotes[symbol].price === 200` (the mid), not 201 (the ask). FAILS today; PASSES after the mid-price change. Check existing tests asserting `price === ask` for this function first (grep test/*.t…
- **Risks / what could break** policy.ts's staleness gate (395-413) and any other consumer of MarketQuote.asOf/BrokerQuote.asOf that currently relies on it always being a parseable recent timestamp (rather than sometimes undefined) needs auditing — grep for `new Date(quote.asOf)` / `Date.parse(...asOf)` call sites without a `quote.asOf &&` guard; the market.ts and quotes-cascade.ts fixes intentionally make asOf undefined more often, which is correct per isQuoteFresh's own contract but could newly surface a NaN-date bug in a caller that never expected undefined before. The alpaca.ts mid-price change affects any test asserting the current ask-as-price behavior (search test/*.ts for `getEquityQuotes` assertions on `.price`)…

### `realized-pnl-ledger` (effort L)

Confirmed the primary defect precisely. src/lib/db-fills.ts's listFillEvents (329-337) is `ORDER BY filled_at ASC LIMIT ?` — with the sibling listPortfolioSnapshots (221-243) already fixed for this exact class of bug via a newest-DESC-subquery-then-ASC pattern (documented inline: 'ASC+LIMIT previously kept the OLDEST slice and dropped recent equity'), listFillEvents was never given the same fix. Confirmed 18 call sites across the codebase pass `limit=500` (dashboard.ts:675-676, experience-memory.ts:263, outcome-engine.ts:964, performance.ts:460-493, robinhood-pnl-crosscheck.ts:60, strategy-tuning.ts:335/409-410/417-418, tax.ts:337, strategy.ts:824-825, app/api/llm-usage/model-stats/route.ts:58) and strategy-tuning.ts:418 passes 30 — every one of these is asking for 'the most recent N fill…

- **Files** `src/lib/db-fills.ts`, `src/lib/performance.ts`, `src/lib/outcome-engine.ts`, `src/lib/tax.ts`, `src/lib/types.ts`
- **Approach** 1) PRIMARY, do first (fixes P&L/tax/tuning/experience-memory/dashboard/LLM-context all at once): rewrite listFillEvents (db-fills.ts:329-337) to the same newest-DESC-subquery pattern already proven at listPortfolioSnapshots (221-243): `SELECT * FROM (SELECT * FROM fill_events WHERE account_number = ? AND source = ? AND user_id = ? ORDER BY filled_at DESC LIMIT ?) newest ORDER BY filled_at ASC` (and the no-source variant identically). No caller signature changes needed — every one of the 18 call sites keeps working, just correctly. 2) Round-trip-aware grading (perf-09, S-effort once #1 lands): in outcome-engine.ts's findRealizedLot (962-970), instead of `closedLots.find(...)` (first match), collect ALL ClosedLots sharing the same `entryAt`, sum their `pnl`, and only treat the case as terminal once the summed closed quantity reaches the entry fill's quantity (compare against `entryFill.quantity`) — partial trims should keep the case open, not grade it early. 3) Round-trip win rate (perf-11, M-effort, larger — group before counting): add a `groupClosedLotsByRoundTrip(lots: ClosedLot[]): ClosedLot[][]` helper (key = entryAt + symbol + side, or an explicit entryRunId if ClosedLot carries one) in performance.ts, and change winRate (1707-1710) and the avg-return aggregator to operate on ONE synthetic aggregated-pnl entry per round trip (sum pnl across the group, mark won/lost by the group's summed pnl) rather than raw ClosedLots; update every UI copy that says 'n=' trades to reflect round trips, not lot-closes. 4) Unmatched-close visibility (perf-12, S-effort): in performance.ts's FIFO loop (~591-599), when `idx === -1` (no matching open lot), instead of a bare `break`, increment a counter (e.g. `unmatchedCloses++`) and push an audit-style entry to a new returned array `unmat…
- **Tests that must fail first**
  - NEW test in a fill-events test file: insert 501 fill_events rows for one account/source with strictly increasing filled_at timestamps, call `listFillEvents(accountNumber, source, 500, userId)`, assert the returned array's LAST element's `filledAt` equals the 501st (newest) inserted fill's filledAt,…
  - NEW test for outcome-engine's findRealizedLot: build fills for one entry + two partial closing fills (a profitable trim, then a stopped-out remainder) sharing the same entryAt; assert the graded outcome's pnlUsd equals the SUM of both closes (net loss), not just the first trim's profit. FAILS today…
  - NEW test for tax.ts: construct one closed SHORT lot with a loss; call the short-term-realized aggregator; assert it appears in shortTermRealized. FAILS today (side!=='long' skips it); PASSES after the guard fix.
- **Risks / what could break** The listFillEvents fix alone touches 18 call sites' EFFECTIVE data (not their code) — any test asserting a SPECIFIC ordering or exact subset of fills from a fixture with MORE than `limit` rows will change output; grep test/*.ts for fixtures inserting >500 (or >30 for strategy-tuning) fills before landing (none found in this pass, but re-check). The round-trip win-rate regrouping (step 3) changes a widely-displayed metric's NUMBER (not just its label) on Results/Home 'By thesis' tables — needs explicit product sign-off on the grouping key (entryAt+symbol+side vs an explicit run/episode id) since a wrong key could merge two genuinely separate round trips on the same symbol. The paper-cost-mod…

### `run-scoped-account` (effort M)

Confirmed. debateProposal (src/lib/red-team.ts:177-233) resolves `activeAccount = getActiveConnectedAccount(userId)` (:192) and `basePrompt = getStrategyPrompt(userId)` (:203, no account arg) even though its caller (strategy.ts:2516) already passes the run-scoped `runPolicy` carrying the correct connectedAccountId as `policyOverride`; `deriveExecutionState(policy, activeAccount)` at :202 inherits the same wrong-account activeAccount. src/lib/learned-context/store.ts:401-403 (applyApprovedPending, strategy-directive tier) calls getStrategyPrompt(pending.userId) / setStrategyPrompt(merged, pending.userId) with no account arg despite pending.connectedAccountId being available and used two branches later (:427-428) — both silently resolve to whatever account is currently 'active' in the conso…

- **Files** `src/lib/red-team.ts`, `src/lib/learned-context/store.ts`, `src/lib/types.ts`, `src/lib/db.ts`, `src/lib/db-api-keys.ts`, `src/lib/chat-history.ts`, `src/lib/chat/orchestrator.ts`
- **Approach** (1) src/lib/red-team.ts: add `getConnectedAccount` to the existing `import { getActiveConnectedAccount, getPolicy, getStrategyPrompt } from "./db";` (line 18). Change :192 `const activeAccount = getActiveConnectedAccount(userId);` to `const activeAccount = policy.connectedAccountId ? getConnectedAccount(policy.connectedAccountId, userId) : getActiveConnectedAccount(userId);`. Change :203 `const basePrompt = getStrategyPrompt(userId);` to `const basePrompt = getStrategyPrompt(userId, policy.connectedAccountId);` (getStrategyPrompt already accepts an optional connectedAccountId, db-profiles.ts:602). This automatically fixes deriveExecutionState at :202 too since it consumes the same corrected `activeAccount`. The `policy` variable already resolves to `policyOverride ?? getPolicy(userId)` (:191) so this is a pure account-resolution fix with no signature change. (2) src/lib/learned-context/store.ts:401-403 — change to `const current = getStrategyPrompt(pending.userId, pending.connectedAccountId ?? undefined); const merged = mergeStrategyDirectiveBlock(current, pending.id, pending.value, assertedAt); setStrategyPrompt(merged, pending.userId, pending.connectedAccountId ?? undefined);` — both functions already accept the optional 3rd arg (db-profiles.ts:602, :611); when pending.connectedAccountId is null (a general/user-level directive) this preserves today's exact behavior (falls back to getActiveConnectedAccount via resolveAccount, db-profiles.ts:323-326). (3) Add `connectedAccountId?: string | null;` to ChatTurn (src/lib/types.ts, after `clientTurnId` around :3023). Add a new versioned migration (src/lib/db.ts, version 85, following the v84 pattern at :3134-3151) that ALTERs `chat_turns` adding a nullable `connected_account_id TEXT` column (existing rows get NULL — meaning…
- **Tests that must fail first**
  - New test in test/red-team.test.ts (or a new file): two connected accounts A (active, no short capability) and B (short-capable, has its own custom strategy prompt / redTeamLlmModel); call debateProposal(shortProposal, quote, userId, policyForAccountB) where policyForAccountB.connectedAccountId = B'…
  - test/learned-context-pending.test.ts — add a case: two connected accounts, active account is B; seed a strategy-directive pending row via seedDirectivePending with connectedAccountId set to A (not B); approveAs(...); assert db.getStrategyPrompt(userId, A) contains the AI-LEARNED block and db.getStr…
  - test/chat-history.test.ts — add a case: appendTurn with connectedAccountId 'acct-a' and another with 'acct-b' for the same userId; listTurns(userId, 10, 'acct-a') returns only the acct-a turn (plus any legacy null-account turns); listTurns(userId, 10) (no filter arg) still returns both, unchanged —…
  - test/chat-orchestrator.test.ts — add a case: user chats while account A is active (a turn is recorded), then the active account is switched to B and the user chats again; assert the history array built at orchestrator.ts's listTurns call for the SECOND turn does not include the first (account-A) tu…
- **Risks / what could break** The chat_turns migration is additive/nullable — no backfill needed or possible (pre-migration turns have no recorded account, and treating them as 'always visible' via the `OR connected_account_id IS NULL` filter is a deliberate, conservative continuity choice: a single-account user's history is never affected since virtually all their turns share one account attribution going forward). trimChatTurns (db-api-keys.ts:1970-1978) and MAX_TURNS=100 stay a per-USER cap, not per-account — a heavy multi-account user's older single-account history can still be trimmed by another account's chat volume; this is a known, accepted trade-off, not fixed here (flagging to avoid scope creep — a per-account…

### `web-ios-contract-drift` (effort M)

Confirmed, concrete, high-value bug: GET /api/policy returns the raw TradingPolicy object, where stopLossPct/trailingStopPct/shortStopLossPct live under `riskRules` (src/lib/types.ts:713-731, RiskRules interface); but ios/SocraticTrade/DeskModels.swift's `FullPolicy.init(from:)` (lines 372-386) decodes those three fields from a TOP-LEVEL CodingKeys container that has no such key in the real payload, so `decodeIfPresent` silently returns nil for every account, every time — GuardrailsView.swift:96-98 renders them via `DeskCopy.percentPoints` (DeskModels.swift:683-686), which prints '—' for nil. This is a hand-authored Swift decoder with zero enforcement that it matches the real route shape; MobileModelsTests.swift:409's `fullSnapshotJSON` is itself a hand-typed string literal, not derived f…

- **Files** `ios/SocraticTrade/DeskModels.swift`, `ios/SocraticTradeTests/DeskModelsTests.swift (new or extend existing)`, `test/policy-ios-contract-fixture.test.ts (new)`, `ios/SocraticTradeTests/Fixtures/policy-contract.json (new, generated+checked in)`
- **Approach** Immediate fix (do NOT flatten the API — `riskRules` is a real domain grouping read by dozens of TS consumers; changing the wire shape is far more invasive/risky than fixing the one hand-written Swift decoder that's wrong): in ios/SocraticTrade/DeskModels.swift `FullPolicy`, add a nested `RiskRulesKeys: String, CodingKey { case stopLossPct, trailingStopPct, shortStopLossPct }` and a top-level `CodingKeys` case `riskRules`. In `init(from decoder:)`, replace the three flat `values.decodeIfPresent(Double.self, forKey: .stopLossPct)` etc. lines (374-376) with: decode an optional nested container via `try? values.nestedContainer(keyedBy: RiskRulesKeys.self, forKey: .riskRules)`, then `try riskRulesValues?.decodeIfPresent(Double.self, forKey: .stopLossPct)` (and same for the other two) — falling back to nil (not throwing) when `riskRules` itself is absent, matching every other field's `decodeIfPresent` optionality convention in this struct. Contract-test infrastructure (systemic fix, the cluster's real ask): add a small generator test in test/policy-ios-contract-fixture.test.ts that calls `GET /api/policy`'s underlying `getPolicy(userId)` (or invokes the route handler directly against a seeded temp DB with a known TradingPolicy including a non-default riskRules), writes the resulting JSON to ios/SocraticTradeTests/Fixtures/policy-contract.json, and asserts specific known fields (stopLossPct etc.) are present at `.riskRules.stopLossPct` in the raw JSON (a cheap, direct pin of the wire shape). Add a new XCTest (ios/SocraticTradeTests/DeskModelsTests.swift, new file if one doesn't exist for FullPolicy) that loads that checked-in fixture as a test-bundle resource and decodes it with the REAL `FullPolicy` type, asserting `policy.stopLossPct == <the fixture's known value>` (not jus…
- **Tests that must fail first**
  - ios/SocraticTradeTests/DeskModelsTests.swift — `testFullPolicyDecodesNestedRiskRules()`: decode the checked-in policy-contract.json fixture (or an inline JSON literal with `"riskRules":{"stopLossPct":8,"trailingStopPct":3,"shortStopLossPct":5}`) into `FullPolicy` and assert `policy.stopLossPct == 8…
  - test/policy-ios-contract-fixture.test.ts — assert the generated fixture JSON's `.riskRules.stopLossPct` matches the seeded policy's configured value (pins the SERVER side of the contract so a future accidental flatten/rename is caught on the TS side too).
- **Risks / what could break** Low risk — the Swift change is purely additive/local to one struct's decode, doesn't touch any TS/route code, and every other FullPolicy field's optionality is unchanged. The only thing to verify by hand (no automated Xcode build in this repo's normal gate — CLAUDE.md flags this explicitly) is that `xcodebuild build`/`xcodebuild test` for the SocraticTrade scheme still passes, since this is the one class of change local `tsc`/vitest/`next build` cannot catch. The other 10 members of this cluster (MobileModels.swift's `?? []` blanking-on-rename pattern across many arrays, a separate `benchmarkUnavailable`/`recordedAt` drop on the symbol-desk decode path, per-field XCTest coverage beyond Full…

## E. Method And What Still Is Not Proven

- **Second round:** 8 batches, one per risk family, each told to refute.  Model tier was matched to the task: the money-path, accounting, backend-reliability and security batches ran on the frontier tier (a wrong call there costs real money); the LLM, market-data, client-surface and process batches ran on the mid tier; the five gap lanes and their skeptics and the plan authors ran on the mid tier.  Result: 27 upheld, 11 narrowed, 2 already fixed, 0 refuted outright.
- **Still not proven:** anything marked UNPROVABLE_FROM_CODE, plus every claim about production data volumes.  The clearest example is the fill-ledger cap (`performance-accounting:perf-01`, `backend-reliability:berel-02`): the code truncates to the OLDEST 500 rows per (account, source), but no one has counted the rows in the production DB, so whether it is biting TODAY is unknown.  One `SELECT source, COUNT(*) FROM fill_events GROUP BY source` per account settles it.
- **Not re-verified:** the P2/P3 tail from Part I kept its first-round verdicts.  Given the second round's hit rate on P1s (28% narrowed or already fixed), assume a similar fraction of the P2/P3 tail is overstated, and re-check before acting on any single one.
- **No production writes** were made in either part.  The only code change this review produced is hotfix #2851, which is live.
