{
 "schema": "socratic-trade/full-app-review/work-items@1",
 "generated": "2026-08-19",
 "source_report": "docs/reviews/2026-08-18-full-app-expert-review.md",
 "how_to_use": "Claim work by cluster key (e.g. 'placement-outcome-truth'). Each cluster lists the finding uids it closes; look those up in `findings_index`. Second-round verdicts on P1s are in `p1_verdicts` - respect corrected_severity over the original. Gap-lane findings are in `gap_findings`.",
 "clusters": [
  {
   "key": "placement-outcome-truth",
   "title": "Approve reports success when nothing was placed, and misfiles retryable broker errors as terminal",
   "severity": "P1",
   "surface": "cross-surface",
   "member_uids": [
    "api-contract-web-ios:api-02",
    "trading-money-path:tsx-03",
    "trading-money-path:tsx-15",
    "trading-money-path:tsx-12",
    "trading-money-path:tsx-04",
    "trading-money-path:tsx-01"
   ],
   "root_cause": "executeProposal returns a status object instead of throwing for every non-placement outcome (src/lib/strategy-execution.ts:239-246 declares Promise<{status:string}>; busy at :298, blocked at :498/766/1113/1136/1251/1302, not_placed at :1338/1394, error at :1319-1473) and every consumer treats \"resolved\" as \"placed\": src/lib/mobile-api.ts:1080 stamps finishCommand(command,\"succeeded\",result), the Swift MobileCommand has no result field (ios/SocraticTrade/MobileModels.swift:511-520) so ProposalsView.swift:481 paints a green \"Approved\", and the same resolver files any HTTP 4xx \u2014 429 and 408 included \u2014 as terminal rejected_by_broker at src/lib/strategy-execution.ts:1279.",
   "one_change": "Introduce one PlacementOutcome resolver at src/lib/strategy-execution.ts:1279/:239 that classifies {placed, blocked, busy, retryable, rejected}; return it verbatim with an honest command status at src/lib/mobile-api.ts:1080, decode result.status/reasons in ios/SocraticTrade/MobileModels.swift:511, and render it on both clients the way app/console/components/approval-card.tsx:347-366 already does.",
   "effort": "L",
   "tranche": 1,
   "plan": {
    "cluster_key": "placement-outcome-truth",
    "summary": "executeProposal (src/lib/strategy-execution.ts:232-1490) returns a {status,...} object instead of throwing for every non-placement outcome (busy/blocked/error/not_placed), and mobile-api.ts's runCommand treats any resolved value as success. Confirmed live: mobile-api.ts:965-968 calls executeProposal and returns the result verbatim; executeMobileCommand (mobile-api.ts:1077-1080) always stamps finishCommand(command,'succeeded',result) unless something THROWS. PublicMobileCommand (mobile-api.ts:134-147) already carries `result?: unknown` in the wire payload, but ios/SocraticTrade/MobileModels.swift's MobileCommand struct (lines 511-521) has no `result` field, so MobileStore.swift:214-215 maps status 'succeeded' -> .succeeded and ProposalsView.swift:481 paints 'Approved \u2014 waiting for desk refresh.' (green check) for busy/blocked/error/not_placed alike. Separately, strategy-execution.ts:1279 and the duplicated block at strategy.ts:3999 classify ANY message matching /\\bHTTP 4\\d\\d\\b/i as terminal rejected_by_broker with no status-code narrowing, so a 429 (rate limit) or 408/409 gets permanently retired instead of routed through reconcilePlacementError/not_placed. alpaca.ts's callMcp (307-349) falls back to REST on any MCP failure (timeout/5xx/parse) reusing the same client_order_id (refId), so a genuine duplicate-submission 422 ('client_order_id must be unique') lands in that same over-broad 4xx-terminal bucket and gets recorded as rejected even though the first order may be live at the broker. Also confirmed: app/console/page.tsx:996-997's second Approve call site toasts 'pos'/'Approved: Order status: ${res.status}' for ANY result including busy/blocked, duplicating approval-card.tsx's already-honest finish() mapping (lines 347-366).",
    "files_to_change": [
     "src/lib/mobile-api.ts",
     "src/lib/strategy-execution.ts",
     "src/lib/strategy.ts",
     "src/lib/alpaca.ts",
     "app/console/lib/approve-feedback.ts (new)",
     "app/console/page.tsx",
     "app/console/components/approval-card.tsx",
     "ios/SocraticTrade/MobileModels.swift",
     "ios/SocraticTrade/ActivityView.swift"
    ],
    "approach": "1) mobile-api.ts: add `class ProposalNotPlacedError extends Error { result: unknown; constructor(message: string, result: unknown) { super(message); this.name = 'ProposalNotPlacedError'; this.result = result; } }` near the other command error classes (~line 166-184). Add `const PLACEMENT_SUCCESS_STATUSES = new Set(['placed','filled','paper'])`. In runCommand's 'proposal.approve' case (965-968), after calling executeProposal, `if (!PLACEMENT_SUCCESS_STATUSES.has(result.status)) throw new ProposalNotPlacedError(result.reasons?.join(' ') || `Order was not placed (status: ${result.status}).`, result);` else return result. In errorPayload() (1056-1075) add a branch: `if (error instanceof ProposalNotPlacedError) return { message: error.message, result: error.result };` \u2014 this makes finishCommand(command,'failed',payload.result,payload.message) stamp status='failed' with the real reason as command.error (which ProposalsView.swift's existing `.failed(action, message)` branch and ActivityView's `command.error` label already render honestly \u2014 no further iOS change needed to kill the false-green-check bug). 2) For 'strategy.run_once' (952-956): do NOT broaden the throw condition (skipped_market_closed/skipped_budget are not errors \u2014 throwing would misrepresent them as failures). Instead add `interface MobileCommandResultSummary { status?: string; summary?: string }` decoded on iOS: in MobileModels.swift add `let result: MobileCommandResult?` to `MobileCommand` where `struct MobileCommandResult: Decodable { let status: String?; let summary: String? }` (extra JSON keys are ignored by Decodable by default, so this is safe against the richer server payload). In ActivityView.swift's CommandActivityRow, render `command.result?.summary` under the status pill when present and non-empty, alongside the existing `command.error` line, so a 'succeeded' run_once still shows why nothing ran. 3) Extract a shared classifier: new function in src/lib/broker-side.ts, `export function classifyPlaceOrderError(error: unknown, message: string): 'validation' | 'rejected_terminal' | 'retryable' | 'unclassified'` \u2014 OrderValidationError instance -> 'validation'; message matches /client_order_id.*(must be unique|already exists)/i -> 'retryable' (checked BEFORE the generic 4xx test, addresses tsx-01's MCP-fallback duplicate-submit); message matches /\\bHTTP (400|403|422)\\b/i -> 'rejected_terminal'; message matches /\\bHTTP (401|408|409|425|429)\\b/i -> 'retryable'; else 'unclassified' (falls through to existing reconcilePlacementError/uncertain handling, unchanged). Replace the ad hoc `if (placeError instanceof OrderValidationError || /\\bHTTP 4\\d\\d\\b/i.test(message))` block at strategy-execution.ts:1279 and the duplicated block at strategy.ts:3999 with calls to this classifier: 'validation'/'rejected_terminal' keep today's exact behavior (blocked / rejected_by_broker respectively); 'retryable' now falls into the SAME not_placed/reconcilePlacementError branch already used for OperationLeaseOwnershipError (strategy-execution.ts:1330-1339) instead of the terminal branch. 4) app/console: create app/console/lib/approve-feedback.ts exporting `toastForApproveResult(toast, result, sideLabel, symbol)` extracted verbatim from approval-card.tsx's finish() (320-366); import it from both approval-card.tsx and app/console/page.tsx's DecisionSheet.handleApprove (996-997), replacing the raw `toast.push('pos','Approved', ...)` line.",
    "tests": [
     "test/mobile-api.test.ts \u2014 NEW test 'marks proposal.approve failed when executeProposal reports busy': reuse test/approval-lock.test.ts's setPaperPolicy/makeProposalId pattern, acquireStrategyLock('simulated-run', userId, accountId) before queueMobileCommand({commandType:'proposal.approve', payload:{proposalId}}), run processPendingMobileCommands, then assert `listMobileCommands({userId})[0].status === 'failed'` and `.error` matches /strategy run is in progress/i. FAILS today (status is 'succeeded'); PASSES after the ProposalNotPlacedError throw is added.",
     "test/order-confirmation-status.test.ts \u2014 extend the mocked Alpaca `createOrder` with a module-level `let mockOrderThrowError: (Error & {response?:{status:number}}) | null = null` that createOrder throws when set (instead of returning). NEW test: set `mockOrderThrowError = Object.assign(new Error('Too many requests'), { response: { status: 429, data: {} } })`, call executeProposal, assert `result.status !== 'error'` and `getProposal(proposalId, userId)?.status !== 'rejected_by_broker'` (expect 'not_placed' or 'placing'/uncertain instead). FAILS today (429 matches /\\bHTTP 4\\d\\d\\b/, status becomes 'error'/'rejected_by_broker'); PASSES once classifyPlaceOrderError routes 429 to 'retryable'."
    ],
    "risks": "Existing tests pin the CURRENT 4xx-is-always-terminal behavior and must be re-checked: test/order-confirmation-status.test.ts:192-207 (400/'rejected' synchronous state \u2014 different code path, unaffected) and test/placement-reconcile.test.ts:387-400 ('throw + order PRESENT (declined) -> rejected_by_broker') \u2014 confirm that test's simulated error message does NOT fall into the new 'retryable' bucket (it should stay 'rejected_terminal' if it's a genuine 400/403/422). The mobile-api throw-on-non-success change must not fire for proposal.reject or other command types (scoped narrowly to the 'proposal.approve' case only). tsx-12 (a Retry button/endpoint for not_placed) is NOT included here \u2014 flag as a smaller follow-up S-effort PR once this lands, since not_placed becomes meaningfully more common (429s now land there) and 'safe to retry' with no retry path gets more visible. iOS: adding a field to a Decodable struct is additive/safe; verify with `xcodebuild build` per repo policy since this touches ios/**.",
    "effort": "L"
   }
  },
  {
   "key": "order-provenance-guard",
   "title": "Automation cancel-replaces orders it does not own (bracket take-profit legs, owner GTC sells, manually cancelled stops)",
   "severity": "P1",
   "surface": "backend",
   "member_uids": [
    "trading-money-path:tsx-02",
    "trading-money-path:tsx-14",
    "trading-money-path:tsx-08",
    "ops-observability-admin:sre-12"
   ],
   "root_cause": "No enqueue or pump path checks who placed the order. src/lib/order-replacement.ts:754-757 filters only on side and state===\"held\", so a just-activated bracket take-profit leg (state \"new\", 15 min after entry fill) passes; the pump re-checks only type/held/quantity at :333-364 and never reads EquityOrder.orderClass (src/lib/types.ts:939, mapped at src/lib/alpaca.ts:942); it then cancels and places a MARKET order at :452-477. The protective-stop reconciler (src/lib/order-cancel.ts:198) re-places a stop the owner just cancelled, contradicting the cancel sheet's \"No new order is placed.\"",
   "one_change": "Add an order-provenance check (orderClass/legs + app-placed clientOrderId + a manual-cancel tombstone) at src/lib/order-replacement.ts:754 and src/lib/order-cancel.ts:198 so bracket legs and owner-placed/owner-cancelled orders are never auto-replaced, and drop the per-tick deferral spam plus the misleading alert at order-replacement.ts:760-767.",
   "effort": "M",
   "tranche": 1,
   "plan": {
    "cluster_key": "order-provenance-guard",
    "summary": "Confirmed: no enqueue or pump path checks who placed an order before auto-cancel-replacing it. src/lib/stale-limit-orders.ts's listStaleLimitOrders (23-49) lists every working sell/cover limit/stop_limit order older than staleLimitOrderMinutes with the ONLY exclusion being isWorkingOrderState \u2014 it does not read EquityOrder.orderClass at all. isHeldExitLeg (132-134) \u2014 used to suppress the ALERT, not enqueue \u2014 only checks state==='held', so an ACTIVATED bracket take-profit leg (state flips held->new when its entry fills, orderClass stays 'bracket'/'oco'/'oto' per types.ts:939's comment) is neither excluded from listStaleLimitOrders NOR from the alert. src/lib/order-replacement.ts's autoRemediateStaleExitOrders (732-793) enqueues a cancel_requested row for every item in that list whose side is sell/cover and state!=='held' (line 756) \u2014 no orderClass check, no clientOrderId/provenance check \u2014 then stepReplacementState (300-480) cancels it and places a MARKET order (452-477) for the full remaining quantity. src/lib/broker-side.ts ALREADY has the exact tool needed and unused here: `isBracketOrderClass(orderClass)` (110-112) checks against BRACKET_ORDER_CLASSES=['bracket','oco','oto']. Separately (tsx-08): cancelWorkingOrder (src/lib/order-cancel.ts:121-213) \u2014 the shared console+mobile manual-cancel path \u2014 calls gateway.cancelEquityOrder (line 198) with NO broker_protective_stops bookkeeping; it never calls the existing cancelBrokerProtectiveStop (broker-protective-stops.ts:178) that DOES understand these rows. reconcileBrokerProtectiveStops's own tick (broker-protective-stops.ts:1092-1118) then sees the tracked order's now-canceled state, calls it 'stale_resting_row', deletes the local row (deleteBrokerProtectiveStop), and the placement loop at line 1343-1344 (`if (existing.has(sym)) continue;`) re-arms a fresh stop for the symbol within one tick \u2014 silently undoing the owner's explicit cancel, contradicting the cancel-sheet copy 'No new order is placed' at app/console/orders/cancel-sheet.tsx:87 and ios/SocraticTrade/OrderCancel.swift:125. NOTE: the cluster's root_cause paragraph mis-cites order-cancel.ts:198 itself as 'the protective-stop reconciler' \u2014 that line is just the cancel call; the actual re-place logic lives entirely in broker-protective-stops.ts. NOTE 2: ops-observability-admin:sre-12 (audit-only broker-declined-stop failures with no owner push, broker-protective-stops.ts:1542/1551/1588) does NOT share this cluster's root cause (auto-replace provenance) \u2014 it is a separate notification-gap defect that was mis-bundled by lane tag; recommend handling it as an independent S-effort item, not part of this plan.",
    "files_to_change": [
     "src/lib/order-replacement.ts",
     "src/lib/stale-limit-orders.ts",
     "src/lib/order-cancel.ts",
     "src/lib/broker-protective-stops.ts",
     "app/console/orders/cancel-sheet.tsx",
     "ios/SocraticTrade/OrderCancel.swift"
    ],
    "approach": "1) tsx-02 (AUTO cancel-replace must never touch a bracket leg or a non-app order): in order-replacement.ts's autoRemediateStaleExitOrders loop (~750-758), import `isBracketOrderClass` from './broker-side' and add, right after the existing `if (String(item.order.state...) === 'held') continue;`: `if (isBracketOrderClass(item.order.orderClass)) { out.deferred++; audit('stale_exit_auto_remediation_skipped_bracket_leg', {orderId: item.order.id, symbol, side, orderClass: item.order.orderClass}, userId, input.policy.connectedAccountId); continue; }` and `if (!item.order.clientOrderId) { out.deferred++; audit('stale_exit_auto_remediation_skipped_not_app_placed', {orderId: item.order.id, symbol, side}, userId, input.policy.connectedAccountId); continue; }` (our own placement always sets client_order_id=refId per alpaca.ts:746/674 and robinhood.ts:442 \u2014 absence means the owner placed it directly at the broker). Deliberately do NOT touch listStaleLimitOrders itself or replaceStaleLimitOrderWithMarket (order-replacement.ts:99-256, the owner-initiated manual replace) \u2014 an owner explicitly clicking 'replace with market' on their own resting take-profit leg is a legitimate intentional action, only the AUTOMATED 15-minutes-later path is the bug. 2) tsx-14 (misleading alert + audit spam on bracket legs): in stale-limit-orders.ts's notifyStaleLimitOrders loop (~66-68), broaden the existing suppression from `if (isHeldExitLeg(item.order)) continue;` to `if (isHeldExitLeg(item.order) || isBracketOrderClass(item.order.orderClass)) continue;` (isBracketOrderClass already imported for step 1) so an ACTIVATED bracket leg stops generating the 'cancel/reprice before replacing' push too. Route the per-tick 'stale_exit_auto_remediation_deferred' audit at order-replacement.ts:760-767 through `auditDeduped` (src/lib/audit-dedupe.ts:36, same pattern as broker-protective-stops.ts:76's auditStopSkipped) keyed by orderId so it fires once per streak instead of once per tick. 3) tsx-08 (owner-cancel of a managed stop must not be silently undone): in order-cancel.ts's cancelWorkingOrder, after the successful `await gateway.cancelEquityOrder(...)` at line 198, add: `const managedRow = listBrokerProtectiveStops(policy.accountNumber, userId).find(r => r.brokerOrderId === orderId); if (managedRow) { deleteBrokerProtectiveStop(managedRow.id, userId); setInternalSetting(ownerCancelledStopKey(userId, policy.accountNumber, managedRow.symbol), JSON.stringify({ at: new Date().toISOString(), positionQuantity: lookup.order ? undefined : undefined /* see below */ })); }` \u2014 capture the pre-cancel position quantity from `lookupCancelContext`'s already-fetched positions (thread it through CancelLookup as an optional `positionQuantity`) so the marker can self-invalidate on a real quantity change. Add `ownerCancelledStopKey(userId, accountNumber, symbol)` as a small exported helper (order-cancel.ts or db-settings.ts). In broker-protective-stops.ts's placement loop (~1343-1344), before `if (existing.has(sym)) continue;`, add: `const suppress = getInternalSetting<string>(ownerCancelledStopKey(userId, accountNumber, sym)); if (suppress) { const marker = JSON.parse(suppress) as {positionQuantity?: number}; if (marker.positionQuantity === undefined || Math.abs(marker.positionQuantity - pos.quantity) < POSITION_EPSILON) continue; /* still the same position the owner cancelled for; leave it stop-less */ /* else: quantity changed (partial exit/re-entry) \u2014 clear the marker, fall through to place fresh protection */ }`. Return `{...result, managedStopSuppressed: Boolean(managedRow)}` from cancelWorkingOrder so callers can vary copy. 4) Conditional copy: in app/console/orders/cancel-sheet.tsx and ios/SocraticTrade/OrderCancel.swift, detect a managed stop client-side via `order.clientOrderId?.startsWith('protstop-')` (the broker echoes back exactly this prefix per broker-protective-stops.ts:1506/1559) and swap the 'No new order is placed' sentence for 'This is an app-managed protective stop. It will not be re-placed automatically unless the position changes or you re-enable protection.'",
    "tests": [
     "test/stale-limit-orders.test.ts \u2014 extend the existing 'alerts a bracket exit leg once it activates' fixture (line ~76) with `orderClass: 'bracket'` on the activated-and-stale order and add a NEW assertion `expect(listStaleLimitOrders([...], {staleLimitOrderMinutes:15}, now)).toEqual([])` when orderClass is bracket-family \u2014 mirrors the existing held-leg suppression test. This specific assertion FAILS today (bracket-class activated legs are still listed) and PASSES after notifyStaleLimitOrders's isBracketOrderClass check (note: only the alert-suppression call, not listStaleLimitOrders itself, changes \u2014 write the new assertion against notifyStaleLimitOrders's alerted count, not listStaleLimitOrders's raw listing).",
     "NEW test in a file adjacent to order-replacement.test.ts (e.g. add to that file): construct a stale sell limit order with `orderClass: 'bracket'`, call autoRemediateStaleExitOrders, assert `out.attempted === 0` and no row was inserted into order_replacements for that orderId (query order_replacements directly). FAILS today (the bracket leg is enqueued and cancel-replaced); PASSES after the orderClass/clientOrderId guard.",
     "NEW test: cancel a broker_protective_stops-tracked order via cancelWorkingOrder, then call reconcileBrokerProtectiveStops for the same symbol/position (unchanged quantity) and assert NO new broker order was placed (placed counter stays 0) and the position row remains stop-less. FAILS today (the stop is silently re-placed within the same tick); PASSES after the suppression marker."
    ],
    "risks": "test/order-replacement.test.ts (856+ region) and test/broker-protective-stops.test.ts pin a lot of existing cancel/replace/reconcile behavior with `order()`/fixture helpers that likely omit orderClass and clientOrderId by default (undefined) \u2014 verify the new guards don't accidentally exclude those fixtures' intended-eligible orders (undefined clientOrderId would now be treated as 'not app-placed' and skip auto-remediation; if any existing test relies on auto-remediation firing for a fixture with no clientOrderId set, it will need `clientOrderId: 'some-ref'` added). isBracketOrderClass(undefined) correctly returns false (typeof check), so untagged fixtures are unaffected by that half. The position-quantity-fingerprint marker in broker-protective-stops.ts needs care: getInternalSetting/setInternalSetting are unscoped by connectedAccountId today for other similar markers in this file (mirror the existing hwmKey/sodKey-style key construction exactly) \u2014 do not invent a new keying convention. sre-12 is explicitly OUT of this plan's diff.",
    "effort": "M"
   }
  },
  {
   "key": "quote-value-provenance",
   "title": "Quote pipeline fabricates asOf and picks the wrong field as price, defeating the staleness gate",
   "severity": "P1",
   "surface": "cross-surface",
   "member_uids": [
    "market-data-integrity:mdi-03",
    "market-data-integrity:mdi-04",
    "market-data-integrity:mdi-05",
    "market-data-integrity:mdi-06",
    "market-data-integrity:mdi-07",
    "market-data-integrity:mdi-09",
    "market-data-integrity:mdi-16",
    "market-data-integrity:mdi-17",
    "market-data-integrity:mdi-18"
   ],
   "root_cause": "Values that were not just observed are stamped with the wall clock: src/lib/data-providers.ts:1571-1590 takeScalar writes cascadeFetchedAt = new Date().toISOString() for any field lacking its own asOf, so a 6h-cached nasdaq-quote volume wins recency arbitration at src/lib/evidence-facts.ts:132-155 and overwrites base.volume; src/lib/market.ts:1548 (Yahoo quote-only) and src/lib/quotes-cascade.ts:397 (ROIC profile price) hardcode asOf: new Date(), so policy.ts:404-413's opening-order staleness gate always reads ~0s old; and src/lib/alpaca.ts:551-569 returns the ASK as price, which becomes the displayed price, watchlist price, alert trigger and stop mark.",
   "one_change": "Make asOf non-forgeable \u2014 carry the provider's own observation timestamp (or leave it undefined, which src/lib/quotes-cascade.ts:47-61 already treats as not-fresh) through takeScalar, toQuoteOnlyMarketQuote and the ROIC/CBOE/VWAP tiers, use mid/last instead of ask at alpaca.ts:551, and surface the resulting age in the drilldown, watchlist and scan chips.",
   "effort": "L",
   "tranche": 1,
   "plan": {
    "cluster_key": "quote-value-provenance",
    "summary": "Confirmed the core defect at all three cited anchors. (1) src/lib/data-providers.ts's takeScalar (~1560-1590): `asOf = supplied?.asOf ?? currentRecord?.fieldDates?.[field] ?? ... ?? cascadeFetchedAt` where `cascadeFetchedAt = new Date().toISOString()` (line 1534) \u2014 any provider field lacking its own timestamp (e.g. parseNasdaqQuoteInfo's price/change/volume, cached 6h under generic ttlMs()) gets stamped as observed 'now'. evidence-facts.ts's arbitrateFieldObservation (100-155) is recency-first for volume, so a 6h-stale cached value with a fabricated 'now' timestamp wins over a genuinely fresh Alpaca dailyBar.v. (2) src/lib/market.ts's toQuoteOnlyMarketQuote (1523-1528) hardcodes `asOf: new Date().toISOString()` even though `quote.asOf` (YahooFinanceQuote.asOf, yahoo-finance.ts:76-77 \u2014 derived from the REAL `meta.regularMarketTime`) is already computed and sitting unused on the same object. src/lib/quotes-cascade.ts:397 (ROIC profile price) does the identical `asOf: new Date().toISOString()`. This directly defeats policy.ts's opening-order staleness gate (395-413) and quotes-cascade.ts's isQuoteFresh/quoteAgeSecForStalenessGate (52-88), both of which ALREADY treat a missing/undefined asOf as stale/missing (`if (!quote.asOf) return false` / `missing: true`) \u2014 so simply not fabricating a value is the fix, not new gate logic. (3) src/lib/alpaca.ts's getEquityQuotes (~548-556) sets `price: ask ?? bid ?? 0` \u2014 confirmed verbatim \u2014 which becomes the displayed Price, watchlist price, alert-trigger price, and stop/take-profit mark app-wide (mergeQuoteData, watchlist page, price alerts, currentPricesFromScan), while intradayChangePct stays computed off a different source. The remaining 6 members (mdi-06/07/09/16/17/18) are independently confirmed via this session's read of the register's verify_notes (each cites a specific unread-field or unsafe-parse defect) but were not re-derived from source in this pass; treat those six as lower-confidence-but-plausible until spot-checked, and sequence them after the three P1/P2 anchors.",
    "files_to_change": [
     "src/lib/data-providers.ts",
     "src/lib/market.ts",
     "src/lib/quotes-cascade.ts",
     "src/lib/alpaca.ts",
     "app/console/watchlist/page.tsx",
     "app/console/ui/symbol-drilldown.tsx"
    ],
    "approach": "PRIMARY (do first, highest leverage): (a) market.ts:1523-1528 toQuoteOnlyMarketQuote \u2014 change `asOf: new Date().toISOString()` to `asOf: quote.asOf` (already on YahooFinanceQuote; undefined when Yahoo gave no regularMarketTime, which correctly falls through to isQuoteFresh's existing 'missing -> stale' branch). (b) quotes-cascade.ts:397 ROIC profile block \u2014 same swap: drop the fabricated `new Date().toISOString()`; if ROIC's response has no timestamp field, leave `asOf: undefined` rather than inventing one (checked: `isQuoteFresh` already handles undefined asOf as not-fresh, so downstream `if (isQuoteFresh(q, nowMs, maxAgeMs)) result[symbol] = q;` naturally excludes it \u2014 matches existing intent, just currently defeated by the fabrication). (c) alpaca.ts:548-556 getEquityQuotes \u2014 change `price: ask ?? bid ?? 0` to a real mid: `price: (typeof bid === 'number' && typeof ask === 'number') ? (bid + ask) / 2 : ask ?? bid ?? 0`, keep bid/ask on the BrokerQuote object unchanged (already present) so tooltips can still show ask/bid explicitly; do NOT touch intradayChangePct here (out of scope \u2014 that's computed elsewhere from prevClose, already tracked separately per mdi-05's own note). SECONDARY (data-providers.ts takeScalar, mdi-03 \u2014 larger, needs its own care because 20+ providers funnel through this one function): stop defaulting `fetchedAt`/`asOf` to `cascadeFetchedAt` for FIELDS that came from a cache write with no observation timestamp of their own \u2014 instead, when a provider's raw parse (e.g. parseNasdaqQuoteInfo) does not itself carry a timestamp, store `undefined` for that field's `asOf` (keep `fetchedAt: cascadeFetchedAt` \u2014 that one IS honestly 'when we fetched the cache entry', just not 'when the underlying value was true') and update evidence-facts.ts's `recency()` metric (114-119, already reads `asOf ?? observedAt ?? effectiveAt ?? fetchedAt`) to NOT fall through to fetchedAt for the volume/price-family fields specifically, or give those fields an explicit lowest-recency floor when asOf is absent, so a same-priority-tier candidate with a real asOf always outranks one with none. Give nasdaq-quote's price-family fields (lastSalePrice/percentageChange/volume) a short dedicated TTL cache bucket instead of sharing NEWS_CACHE_TTL_MS (6h) \u2014 new constant e.g. `NASDAQ_QUOTE_TTL_MS = 30_000` alongside the existing ALPACA_SNAPSHOT-style short TTL pattern already used elsewhere in this file. THEN (batch, lower priority \u2014 S effort each, execute per the register's already-verified line targets): mdi-06 pass `grouped.date` through to mergeGroupedBarData (currently only `grouped.bars` reaches it at app/api/scan/route.ts:110) and stamp VWAP's asOf from that date, not fetchedAt; mdi-07 guard symbol-drilldown.tsx:511's `new Date(view.asOf)` with a Date.parse check and stop persisting the Nasdaq sentence string as MarketQuote.asOf in market.ts's toMarketQuote; mdi-09 add a distinct `averageVolume` field alongside `volume` (do not let Robinhood's average-volume preference populate `volume` at data-providers.ts:2143); mdi-16 add `cached`/`source`/oldest-quote-asOf to MarketScanResponse and ios/SocraticTrade/ScanView.swift's subtitle; mdi-17 render `<Ago iso={quote.asOf}/>` plus % change on app/console/watchlist/page.tsx:253 (WatchlistQuote.asOf already exists on the type, just unrendered); mdi-18 convert alpaca.ts:569's `String(bar.time)` to a real ISO via the existing toBusinessDay-style helper and pass `bar.source` through instead of hardcoding 'yahoo-finance-delayed'.",
    "tests": [
     "NEW unit test for market.ts's toQuoteOnlyMarketQuote: feed a YahooFinanceQuote with `asOf: '2026-08-15T14:00:00Z'` and a `now` several hours later; assert the returned MarketQuote.asOf equals the INPUT asOf (not a value close to `now`). FAILS today (asOf is always ~now); PASSES after the fix.",
     "NEW unit test for quotes-cascade's isQuoteFresh/quoteAgeSecForStalenessGate integration: after the ROIC-block fix, assert a ROIC-sourced quote whose upstream gave no price timestamp is excluded from `result[symbol]` (i.e. `isQuoteFresh` returns false for it) rather than always passing. FAILS today (fabricated 'now' always passes); PASSES after removing the fabrication.",
     "NEW unit test for alpaca.ts getEquityQuotes: mock getLatestQuotes returning `{bp: 199, ap: 201}`; assert `quotes[symbol].price === 200` (the mid), not 201 (the ask). FAILS today; PASSES after the mid-price change. Check existing tests asserting `price === ask` for this function first (grep test/*.ts for getEquityQuotes) and update them \u2014 they currently pin the ask-as-price bug."
    ],
    "risks": "policy.ts's staleness gate (395-413) and any other consumer of MarketQuote.asOf/BrokerQuote.asOf that currently relies on it always being a parseable recent timestamp (rather than sometimes undefined) needs auditing \u2014 grep for `new Date(quote.asOf)` / `Date.parse(...asOf)` call sites without a `quote.asOf &&` guard; the market.ts and quotes-cascade.ts fixes intentionally make asOf undefined more often, which is correct per isQuoteFresh's own contract but could newly surface a NaN-date bug in a caller that never expected undefined before. The alpaca.ts mid-price change affects any test asserting the current ask-as-price behavior (search test/*.ts for `getEquityQuotes` assertions on `.price`) \u2014 those pin the bug and must be updated, not left red. data-providers.ts's takeScalar is extremely high-fan-in (used by every enrichment provider); the mdi-03 change should land as its own careful S/M-scoped follow-up after the three P1/P2 anchors are verified in isolation, not bundled into the same PR \u2014 flagging this as a sequencing risk, not a reason to skip it.",
    "effort": "L"
   }
  },
  {
   "key": "market-cache-freshness",
   "title": "Calendar-day freshness math freezes screener, OHLC and enrichment caches during live sessions",
   "severity": "P1",
   "surface": "backend",
   "member_uids": [
    "market-data-integrity:mdi-01",
    "market-data-integrity:mdi-02",
    "market-data-integrity:mdi-08",
    "market-data-integrity:mdi-10",
    "market-data-integrity:mdi-11",
    "market-data-integrity:mdi-12",
    "market-data-integrity:mdi-15"
   ],
   "root_cause": "Freshness is measured in calendar days, not trading sessions. src/lib/market-hours.ts:283-311 isWeekendOrHolidayClosureAhead returns true for the whole of Friday (and the whole day before any holiday), so expiresAtRespectingMarketClose pushes a 5-minute screener TTL to Monday's open \u2014 verified: a Fri 10:00 ET write expires Mon 09:30 ET. src/lib/history.ts:115-126 isBarSeriesFresh accepts a 3-calendar-day-old last bar, so a Monday write suppresses any refetch through Tuesday and Wednesday. The same module has no early-close calendar (market-hours.ts:35), and the universe/history cascades below it mix adjustment bases and a 2026-06-14 static index snapshot.",
   "one_change": "Replace the calendar-day helpers with a session-aware pair \u2014 extend TTL only after the session actually closes (src/lib/market-hours.ts:288/305) and measure bar freshness in elapsed sessions (src/lib/history.ts:115) \u2014 and add the half-day/early-close table the same module needs.",
   "effort": "M",
   "tranche": 1,
   "plan": {
    "cluster_key": "market-cache-freshness",
    "summary": "Verified both anchor functions directly. src/lib/market-hours.ts's isWeekendOrHolidayClosureAhead (288-292) is `if (!isEtCalendarTradingDay(today)) return true; return !isEtCalendarTradingDay(addEtCalendarDays(today, 1));` \u2014 for ANY time on a Friday, today IS a trading day (first branch false) but tomorrow (Saturday) is not, so this returns true for the ENTIRE Friday session, not just after close. expiresAtRespectingMarketClose (305-311) then extends any cache write made at any point on Friday to `nextMarketOpenStrictlyAfterMs` = Monday 09:30 ET, freezing 5-minute screener TTLs, OHLC caches, and 6h enrichment caches through what should be a fully live trading day (and identically the day before any holiday). src/lib/history.ts's isBarSeriesFresh (115-124) computes `ageMs = now - lastBarMs; return ageMs <= maxStalenessDays * 24h` with a default of 3 CALENDAR days; called at history.ts:241 to gate the local-cache short-circuit for fetchDailyOHLC. A Monday-written last bar stays 'fresh' (<=72h) through Tuesday and Wednesday, so a same-day PARTIAL bar persisted as if it were an EOD close (persistEodBarsToCache has no partial/EOD distinction) suppresses refetch for up to two full missed sessions. market-hours.ts additionally has NO early-close/half-day table at all (`const CLOSE = 16*60` in currentMarketSession is a single hardcoded constant with no per-date override, and getMarketHolidays only models FULL closures) \u2014 so on the ~4 NYSE half-days/year (day after Thanksgiving, Christmas Eve, July 3 when a weekday), currentMarketSession keeps reporting 'regular' until 4pm even though the exchange closed at 1pm, and the same TTL-extension logic reads the wrong close boundary. The cluster's other 4 listed members (mdi-08 universe security-type filter, mdi-11 adjustment-basis tagging, mdi-11 corporate-action splicing, mdi-12 Yahoo v7 crumb, mdi-15 static S&P 500 snapshot) do NOT share this cluster's 'calendar-day-vs-trading-session' root cause \u2014 they are independent market-data-integrity defects bundled in by lane tag only; excluded from this plan's diff and listed as separate follow-ups.",
    "files_to_change": [
     "src/lib/market-hours.ts",
     "src/lib/history.ts"
    ],
    "approach": "1) market-hours.ts: replace isWeekendOrHolidayClosureAhead's calendar-day check with a session-boundary check. Add `const MARKET_CLOSE_HOUR_ET = 16; const MARKET_CLOSE_MINUTE_ET = 0;` (mirroring the existing MARKET_OPEN_HOUR_ET/MINUTE_ET at line 263) and an early-close table `const EARLY_CLOSE_DATES: Record<string, {hour:number;minute:number}> = {}` populated per-year the same way getMarketHolidays (113) builds its holiday set (day-after-Thanksgiving = 4th Thursday of Nov + 1 day; Christmas Eve = Dec 24 when a weekday; July 3 when a weekday and not itself a holiday) \u2014 export a `getEarlyCloses(year)` alongside the existing `getMarketHolidays(year)`. Rewrite isWeekendOrHolidayClosureAhead(nowMs) to: compute `todayParts = etDateParts(new Date(nowMs))`; if `!isEtCalendarTradingDay(todayParts)` return true (unchanged, correct); else compute today's actual close instant via `etWallClockToUtcMs(todayParts, closeHourFor(todayParts), closeMinuteFor(todayParts))` where closeHourFor/closeMinuteFor consult EARLY_CLOSE_DATES for today's Y-M-D key, defaulting to MARKET_CLOSE_HOUR_ET/MINUTE_ET; if `nowMs < todayCloseMs` return false (today's session hasn't ended yet \u2014 do NOT extend, a normal-TTL refetch is correct and desired); else (today's session has ended) fall through to the existing `!isEtCalendarTradingDay(addEtCalendarDays(todayParts, 1))` check unchanged. Also apply EARLY_CLOSE_DATES to currentMarketSession's hardcoded `CLOSE = 16*60` (8-60) so half-days correctly read 'closed' 13:00-16:00 ET (mdi-10) \u2014 thread the date through the existing Intl.DateTimeFormat parts already computed there. 2) history.ts: replace isBarSeriesFresh's flat 3-calendar-day window with a trading-session-aware check. New contract: `isBarSeriesFresh(bars, now)` (drop the maxStalenessDays parameter \u2014 a raw day-count no longer means anything) returns true only when the last bar's calendar date is >= the most recently EXPECTED complete trading session's date, computed as: if today (from `now`) is a trading day AND the session has closed (reuse the same close-time logic added to market-hours.ts above), expected date = today; else expected date = the previous trading day (reuse/extend market-hours.ts's existing previousTradingDayStart). Import `isTradingDay`/`previousTradingDayStart` from market-hours.ts rather than duplicating calendar logic in history.ts. Update the call site at history.ts:241 to drop the now-removed `3` argument.",
    "tests": [
     "NEW test in a market-hours test file: `isWeekendOrHolidayClosureAhead` (or the exported `expiresAtRespectingMarketClose` it feeds) called with a Friday 2026-08-21T14:30:00Z (10:00 ET) and a 5-minute baseTtlMs \u2014 assert `expiresAtRespectingMarketClose(...)` returns `nowMs + 5*60_000` (the naive expiry), NOT Monday 09:30 ET. FAILS today (register verified: expires 2026-08-24T13:30Z); PASSES after the close-time gate.",
     "Same function called with a Friday 2026-08-21T21:00:00Z (17:00 ET, after close) \u2014 assert it DOES extend to Monday open (the genuine weekend-closure case must still work).",
     "NEW test for isBarSeriesFresh: build a bars array whose last bar is dated Monday, call with `now` = Wednesday same week (no early close/holiday between) \u2014 assert `isBarSeriesFresh` returns false (stale \u2014 Tuesday's session bar is missing) where the OLD 3-day-window version returned true. FAILS today; PASSES after the session-aware rewrite.",
     "NEW test for getEarlyCloses/currentMarketSession: `currentMarketSession(new Date('2026-11-27T18:30:00Z'))` (13:30 ET, day after Thanksgiving) should return 'closed' or 'post', not 'regular' \u2014 the register's own tsx probe confirms today's code returns 'regular'."
    ],
    "risks": "expiresAtRespectingMarketClose and isBarSeriesFresh are both hot, widely-called cache-freshness primitives (screener, OHLC, enrichment, universe caches per mdi-01's evidence) \u2014 a mis-implemented close-time comparison could flip from 'never extends' (worse: burns provider quota every tick on weekends, the original bug this function was written to fix) to 'always extends' (reintroducing mdi-01) if the early-close/close-hour math has an off-by-one. Test both directions explicitly (Friday morning must NOT extend; Friday evening MUST extend) before landing. isBarSeriesFresh's signature change (dropping maxStalenessDays) is a breaking change to its exported contract \u2014 grep all callers (only history.ts:241 found in this pass, but re-grep before landing) and check test/*.ts files that call it directly with an explicit day count, which will need updating to the new signature.",
    "effort": "M"
   }
  },
  {
   "key": "cash-flow-and-trading-day",
   "title": "Day P&L reads $0.00 and shorts mint phantom deposits: flows are inferred, not read, and the day is not the trading day",
   "severity": "P1",
   "surface": "cross-surface",
   "member_uids": [
    "performance-accounting:perf-02",
    "performance-accounting:perf-03",
    "performance-accounting:perf-08",
    "performance-accounting:perf-14",
    "performance-accounting:perf-16",
    "performance-accounting:perf-22",
    "market-data-integrity:mdi-13",
    "trading-money-path:tsx-17"
   ],
   "root_cause": "app/console/lib/derive.ts:443 calls inferExternalCashFlows([baseline, fakeCurrent], []) with a hardcoded empty fill list, so on any day the strategy trades directionally tradeCash is 0 (src/lib/cash-flows.ts:157-164), the guard at :187-208 sets flow = deltaEquity, and Home prints exactly $0.00 (0.00%). src/lib/cash-flows.ts:164 also gives short/cover the wrong cash sign, minting a 2x-notional phantom deposit per short. Day keys are UTC (cash-flows.ts:33/129-142) while owner timestamps are Central, the risk-breaker HWM is UTC and not flow-adjusted (src/lib/risk-breaker.ts:86) and daily caps roll at NY midnight (src/lib/db-execution.ts:15). The broker's own equity/cash-activity ledger is implemented but unwired (src/lib/alpaca-account-insights.ts:167).",
   "one_change": "Make the broker cash-activity ledger the flow source \u2014 wire src/lib/alpaca-account-insights.ts:167 into the dashboard and benchmark paths \u2014 and keep inference only as a labeled fallback with the short/cover sign, the real fill list and one shared Central trading-day helper reused by risk-breaker.ts:86 and db-execution.ts:15.",
   "effort": "L",
   "tranche": 1,
   "plan": {
    "cluster_key": "cash-flow-and-trading-day",
    "summary": "Confirmed the core defects. deriveDayPnl (app/console/lib/derive.ts:416-454) has NO fills parameter in its signature at all, and its only caller (app/console/page.tsx:62) passes none \u2014 so line 443's `inferExternalCashFlows([baseline, fakeCurrent], [])` hardcodes an empty fill list on every call; whenever the strategy trades directionally that day, tradeCash computes to 0 inside cash-flows.ts's loop (140-165, since sortedFills is always []), the missing-fill guard at cash-flows.ts:187-208 treats the whole equity delta as a transfer or zeroes it out, and Day P&L can print exactly $0.00. Confirmed the short/cover sign bug precisely: cash-flows.ts:164 `tradeCash += f.side === 'sell' || f.side === 'cover' ? f.notional : -f.notional;` \u2014 a SHORT should INCREASE cash (broker credits short-sale proceeds) but falls into the else branch (-notional, wrong sign), and a COVER should DECREASE cash (buying back) but is grouped with 'sell' (+notional, wrong sign) \u2014 BOTH are backwards, and since `flow = deltaCash - tradeCash`, an understated-by-2x tradeCash for a short overstates flow by exactly 2x notional, minting the phantom-deposit effect described. Confirmed `fetchAlpacaAccountActivities` (src/lib/alpaca-account-insights.ts:209-230, wraps GET /v2/account/activities \u2014 Alpaca's own cash-activity/transfer ledger) has ZERO consumers anywhere in src/ or app/ (grep confirmed) \u2014 genuinely unwired as claimed. Confirmed risk-breaker.ts:82 `const day = now.toISOString().slice(0, 10);` is a raw UTC calendar day \u2014 a THIRD, ad hoc day boundary alongside cash-flows.ts/derive.ts's Central-Time display boundary (app/console/lib/format.ts's startOfCentralDay, whose own comment explicitly documents it as a client-safe DUPLICATE of db-execution.ts's `startOfDayInTimeZone`, 'not imported since that module pulls in better-sqlite3') and db-execution.ts's DAILY_RESET_TIME_ZONE='America/New_York' boundary already used for daily notional caps (db-execution.ts:15/18). The UTC boundary in risk-breaker.ts is not a deliberate choice \u2014 it's the only one of the three that matches nothing else in the app.",
    "files_to_change": [
     "src/lib/cash-flows.ts",
     "src/lib/risk-breaker.ts",
     "src/lib/dashboard.ts",
     "app/console/lib/derive.ts",
     "app/console/page.tsx"
    ],
    "approach": "1) SIGN FIX (smallest, highest-confidence, do first): cash-flows.ts:164 \u2014 change `f.side === 'sell' || f.side === 'cover' ? f.notional : -f.notional` to `f.side === 'sell' || f.side === 'short' ? f.notional : -f.notional` (short and sell both increase cash; buy and cover both decrease it \u2014 cover now correctly falls to the else/-notional branch). 2) REAL FILLS instead of []: give deriveDayPnl a `fills: FillEvent[]` parameter (app/console/lib/derive.ts:416-421 signature), pass real same-day fills at line 443 instead of `[]`. Thread this from the caller: app/console/page.tsx:62 needs a fills list scoped to [baseline.timestamp, now] \u2014 add a small server-side query (new function in src/lib/db-fills.ts, e.g. `listFillsSince(accountNumber, source, sinceIso, userId)`, a thin wrapper filtering the account's fills by filledAt >= sinceIso) exposed through the dashboard snapshot (src/lib/dashboard.ts's computeDashboardSnapshot, ~343+) as e.g. `snapshot.recentFillsForDayPnl`, and pass that array into deriveDayPnl at page.tsx:62. 3) PRIMARY SOURCE per the register's own recommendation \u2014 wire the broker's own ledger ahead of inference: in dashboard.ts (or a new small helper module), call `fetchAlpacaAccountActivities(userId, { activityTypes: ['CSD','CSW','ACATC','ACATS','FEE','DIV'] })` (deposit/withdrawal/transfer/fee/dividend activity types) for the account's active period, sum net_amount for transfer-class activities into a `brokerReportedFlow` figure, and have deriveDayPnl prefer `brokerReportedFlow` when present/non-Alpaca-account-agnostic-empty, falling back to the (now-fixed) fills-based inference only when the broker ledger call fails or returns nothing (non-Alpaca brokers). Label which source won on the DayPnl result (`cashFlowAdjusted: 'broker-ledger' | 'inferred' | false`) so the UI can show an honest provenance chip instead of a bare number. 4) DAY BOUNDARY: fix risk-breaker.ts's day-key bug specifically \u2014 change `const day = now.toISOString().slice(0, 10);` (risk-breaker.ts:82) to reuse db-execution.ts's already-exported `startOfDayInTimeZone(now, DAILY_RESET_TIME_ZONE)` (NY time, matching the SAME boundary the daily notional cap already uses, so a fill straddling midnight UTC is graded against the identical 'day' both systems agree on) \u2014 import `{ startOfDayInTimeZone, DAILY_RESET_TIME_ZONE }` from './db-execution' and derive the day key from that Date's ISO date portion instead of the raw `now`. Do NOT change cash-flows.ts/derive.ts's Central-Time display boundary (startOfCentralDay) in this pass \u2014 that is a deliberate, documented, owner-directed DISPLAY convention (distinct concept from the exchange's own trading-session boundary); flag full three-way unification as a separate product decision requiring an explicit owner call, not silently bundled into this correctness fix.",
    "tests": [
     "NEW unit test in test/cash-flows.test.ts (or wherever cash-flows tests live): construct two equity-curve points with a SHORT fill of notional 1000 between them and no external transfer; assert `inferExternalCashFlows(...)` returns an EMPTY flow map (or a flow within materiality threshold of 0), not a ~2000 phantom deposit. FAILS today (short's wrong sign mints a 2x-notional flow); PASSES after the sign fix.",
     "NEW test for deriveDayPnl: pass a real fills array containing one directional buy fill between baseline and now, plus a portfolio.cash delta consistent with ONLY that trade (no external transfer) \u2014 assert `pnl` reflects the mark-to-market move, not $0.00. FAILS today (deriveDayPnl has no fills param, so trade-driven cash movement is always misread as either a transfer or zeroed); PASSES once fills flow through.",
     "NEW test for risk-breaker.ts: call recordAndEvaluateDrawdownBreaker with `now` set to 7:30pm CT (which is after UTC midnight the 'next' calendar day) on two consecutive calls representing the 'same' NY trading day; assert `startOfDayEquity` is NOT reset between them. FAILS today (UTC day-key rolls over mid-evening CT, resetting startOfDayEquity a day early); PASSES after switching to startOfDayInTimeZone(now, 'America/New_York')."
    ],
    "risks": "deriveDayPnl and app/console/page.tsx are on the money-facing Home dashboard's most-viewed tile \u2014 any regression here is immediately visible to the owner. The broker-activities wiring (step 3) is the largest, riskiest piece (new external API call in the dashboard's parallel-fetch chain, needs a timeout/failure fallback so a slow/failed Alpaca activities call never blocks or breaks the dashboard \u2014 mirror the existing `Promise.race`/timeout pattern already used elsewhere in dashboard.ts) and should land as a separate PR after the sign-fix + fills-plumbing land and are verified; do not bundle all four steps into one commit. Existing tests likely pin the CURRENT `[]`-fills / UTC-day behavior \u2014 grep test/derive*.ts, test/cash-flows*.ts, and test/risk-breaker*.ts before changing signatures; any test asserting `flow === 0` purely because fills were empty (rather than because there truly was no transfer) needs its fixture updated to pass real fills. Robinhood/other non-Alpaca brokers have no equivalent activities endpoint wired \u2014 the fallback-to-inference path must stay fully functional for those accounts, not just Alpaca.",
    "effort": "L"
   }
  },
  {
   "key": "realized-pnl-ledger",
   "title": "Realized-P&L ledger reads the oldest fills and mismatches lots, so P&L, win rate, tax and wash-sale locks are wrong",
   "severity": "P2",
   "surface": "backend",
   "member_uids": [
    "performance-accounting:perf-01",
    "backend-reliability:berel-02",
    "performance-accounting:perf-07",
    "performance-accounting:perf-09",
    "performance-accounting:perf-10",
    "performance-accounting:perf-11",
    "performance-accounting:perf-12",
    "performance-accounting:perf-21",
    "performance-accounting:perf-20"
   ],
   "root_cause": "src/lib/db-fills.ts:332 is SELECT ... ORDER BY filled_at ASC LIMIT ? \u2014 the OLDEST N rows \u2014 while the sibling listPortfolioSnapshots (db-fills.ts:221-243) was already fixed to a newest-DESC subquery for this exact bug; all 16 callers pass 500 and src/lib/strategy-tuning.ts:418 passes 30, so the paid tuner's \"recentFills\" are provably the account's first 20 fills. The matcher on top is also wrong: closes with no app lot are silently dropped (src/lib/performance.ts:599), a lot is graded on its first partial exit only (src/lib/outcome-engine.ts:966), partial closes count as round trips (performance.ts:1707), short-side lots are skipped in tax (src/lib/tax.ts:352), a synthetic 20bps haircut is applied to real fills (performance.ts:244), and FillEvent carries no fees.",
   "one_change": "Fix the read to the newest-DESC subquery already used at db-fills.ts:221-243 and make the lot matcher round-trip aware in src/lib/performance.ts:599-1707 (group partials per lot, book unmatched closes into an explicit pre-app bucket, include short lots in tax, drop the synthetic haircut).",
   "effort": "L",
   "tranche": 1,
   "plan": {
    "cluster_key": "realized-pnl-ledger",
    "summary": "Confirmed the primary defect precisely. src/lib/db-fills.ts's listFillEvents (329-337) is `ORDER BY filled_at ASC LIMIT ?` \u2014 with the sibling listPortfolioSnapshots (221-243) already fixed for this exact class of bug via a newest-DESC-subquery-then-ASC pattern (documented inline: 'ASC+LIMIT previously kept the OLDEST slice and dropped recent equity'), listFillEvents was never given the same fix. Confirmed 18 call sites across the codebase pass `limit=500` (dashboard.ts:675-676, experience-memory.ts:263, outcome-engine.ts:964, performance.ts:460-493, robinhood-pnl-crosscheck.ts:60, strategy-tuning.ts:335/409-410/417-418, tax.ts:337, strategy.ts:824-825, app/api/llm-usage/model-stats/route.ts:58) and strategy-tuning.ts:418 passes 30 \u2014 every one of these is asking for 'the most recent N fills' but the SQL returns the OLDEST N once an account/source exceeds the limit. No existing test in test/*.ts exercises the >limit-rows case for listFillEvents (all fixtures use small counts), which is why this slipped through despite the sibling function's fix. Also confirmed the lot-matcher issues layered on top: performance.ts:591-599's FIFO closer breaks (drops) a closing fill with no matching same-side open lot with zero counter/audit (a real gap for pre-app/manual positions per db-fills.ts:405's own comment 'fill_events is not a complete ledger'); outcome-engine.ts:962-970's findRealizedLot does `closedLots.find(...)` (first match only) so a scaled-out position's decision grade is taken from the FIRST partial trim, not the round trip; performance.ts:1707-1710's winRate counts ClosedLots (one per closing fill against a lot) not round trips, so 2 profitable trims + 1 stopped-out remainder reads as 67% win rate on a losing trade; tax.ts:352 `if (lot.side !== 'long' || ...) continue;` drops every closed SHORT from short-term-realized/wash-sale detection; performance.ts:239-268's paper execution-cost model is applied inconsistently (not universally as originally summarized) \u2014 an immediately-known-price paper fill is booked cost-adjusted while a later-reconciled paper fill is overwritten with raw broker truth (strategy-execution.ts:1643-1647), so ONE account's realized P&L mixes shaded and raw prices; FillEvent (types.ts:2742) has no fee/commission/dividend field at all, so all P&L is gross of broker costs.",
    "files_to_change": [
     "src/lib/db-fills.ts",
     "src/lib/performance.ts",
     "src/lib/outcome-engine.ts",
     "src/lib/tax.ts",
     "src/lib/types.ts"
    ],
    "approach": "1) PRIMARY, do first (fixes P&L/tax/tuning/experience-memory/dashboard/LLM-context all at once): rewrite listFillEvents (db-fills.ts:329-337) to the same newest-DESC-subquery pattern already proven at listPortfolioSnapshots (221-243): `SELECT * FROM (SELECT * FROM fill_events WHERE account_number = ? AND source = ? AND user_id = ? ORDER BY filled_at DESC LIMIT ?) newest ORDER BY filled_at ASC` (and the no-source variant identically). No caller signature changes needed \u2014 every one of the 18 call sites keeps working, just correctly. 2) Round-trip-aware grading (perf-09, S-effort once #1 lands): in outcome-engine.ts's findRealizedLot (962-970), instead of `closedLots.find(...)` (first match), collect ALL ClosedLots sharing the same `entryAt`, sum their `pnl`, and only treat the case as terminal once the summed closed quantity reaches the entry fill's quantity (compare against `entryFill.quantity`) \u2014 partial trims should keep the case open, not grade it early. 3) Round-trip win rate (perf-11, M-effort, larger \u2014 group before counting): add a `groupClosedLotsByRoundTrip(lots: ClosedLot[]): ClosedLot[][]` helper (key = entryAt + symbol + side, or an explicit entryRunId if ClosedLot carries one) in performance.ts, and change winRate (1707-1710) and the avg-return aggregator to operate on ONE synthetic aggregated-pnl entry per round trip (sum pnl across the group, mark won/lost by the group's summed pnl) rather than raw ClosedLots; update every UI copy that says 'n=' trades to reflect round trips, not lot-closes. 4) Unmatched-close visibility (perf-12, S-effort): in performance.ts's FIFO loop (~591-599), when `idx === -1` (no matching open lot), instead of a bare `break`, increment a counter (e.g. `unmatchedCloses++`) and push an audit-style entry to a new returned array `unmatchedClosingFills: FillEvent[]`; surface a 'N closing fills had no tracked opening lot (pre-app or manual position)' note on Results wherever the realized-P&L figure renders. 5) Short-side tax parity (perf-07, S-effort): tax.ts:352 and :305 \u2014 change `lot.side !== 'long'` guards to include `side === 'short'` lots in shortTermRealized/wash-sale detection (a closed short is always short-term regardless of holding period \u2014 do not attempt to apply a long-term rate to it). 6) Consistent paper cost model (perf-10, S-effort): performance.ts:239-268 \u2014 either book price = broker truth always and move the modeled haircut into a new `FillEvent.modeledCostBps` field consumed only by the learning loop's net-of-cost stats (keeps Realized P&L == broker-reported), or (simpler, smaller diff) apply the SAME cost-adjustment retroactively when strategy-execution.ts:1643-1647 later overwrites a reconciled fill's price, so every paper fill in an account is consistently either all-shaded or all-raw, never mixed. 7) Fees (perf-20, out of scope for this pass \u2014 flag as a separate M-effort follow-up requiring new broker-activity plumbing shared with the cash-flow-and-trading-day cluster's Alpaca-activities wiring; do not attempt inline here).",
    "tests": [
     "NEW test in a fill-events test file: insert 501 fill_events rows for one account/source with strictly increasing filled_at timestamps, call `listFillEvents(accountNumber, source, 500, userId)`, assert the returned array's LAST element's `filledAt` equals the 501st (newest) inserted fill's filledAt, not the 500th-from-the-start. FAILS today (returns rows 1-500, i.e. the oldest, so the newest fill is absent); PASSES after the DESC-subquery fix. Mirror this pattern from any existing listPortfolioSnapshots regression test if one exists, else model it fresh.",
     "NEW test for outcome-engine's findRealizedLot: build fills for one entry + two partial closing fills (a profitable trim, then a stopped-out remainder) sharing the same entryAt; assert the graded outcome's pnlUsd equals the SUM of both closes (net loss), not just the first trim's profit. FAILS today (find() returns only the first ClosedLot); PASSES after the aggregation fix.",
     "NEW test for tax.ts: construct one closed SHORT lot with a loss; call the short-term-realized aggregator; assert it appears in shortTermRealized. FAILS today (side!=='long' skips it); PASSES after the guard fix."
    ],
    "risks": "The listFillEvents fix alone touches 18 call sites' EFFECTIVE data (not their code) \u2014 any test asserting a SPECIFIC ordering or exact subset of fills from a fixture with MORE than `limit` rows will change output; grep test/*.ts for fixtures inserting >500 (or >30 for strategy-tuning) fills before landing (none found in this pass, but re-check). The round-trip win-rate regrouping (step 3) changes a widely-displayed metric's NUMBER (not just its label) on Results/Home 'By thesis' tables \u2014 needs explicit product sign-off on the grouping key (entryAt+symbol+side vs an explicit run/episode id) since a wrong key could merge two genuinely separate round trips on the same symbol. The paper-cost-model consistency fix (step 6) changes historical-looking realized P&L for the paper book \u2014 confirm with the owner whether 'book broker truth always' (simpler, matches owner's 'an account is an account' framing \u2014 paper is a real broker account) is preferred over 'retroactively re-shade reconciled fills' before choosing a direction; recommend the former as the more owner-ruling-aligned default.",
    "effort": "L"
   }
  },
  {
   "key": "pnl-basis-labels",
   "title": "Performance tiles print numbers without the basis or window they were computed on",
   "severity": "P2",
   "surface": "cross-surface",
   "member_uids": [
    "performance-accounting:perf-04",
    "performance-accounting:perf-05",
    "performance-accounting:perf-06",
    "performance-accounting:perf-15",
    "performance-accounting:perf-18",
    "performance-accounting:perf-17",
    "performance-accounting:perf-19"
   ],
   "root_cause": "src/lib/performance.ts:497 caps the \"Versus the market\" TWR at ~100 snapshots (\u22487 trading days) while app/console/results/page.tsx frames it as account performance; \"Realized/Unrealized P&L\" means lot-ledger on Results, broker cost on Home and broker positions on iOS with no label on any of them (results/page.tsx:957-994); win rate 0% and avg return +0.00% render for accounts with zero closed lots; app/console/lib/derive.ts:804 divides by NET signed cost basis so shorts inflate the percentage; and performance.ts:1664 fabricates a $100 paper equity base that is rendered as money.",
   "one_change": "Attach an explicit basis + window label (and an em-dash empty state instead of 0%) to every performance figure at app/console/results/page.tsx:940-1000, ios/SocraticTrade/ResultsView.swift and app/console/lib/derive.ts:804, sourced from the same struct that computed the number.",
   "effort": "M",
   "tranche": 1,
   "plan": {
    "cluster_key": "pnl-basis-labels",
    "summary": "Confirmed the core shape. app/console/results/page.tsx's BucketCard (940-1000) renders 'Realized P&L' / 'Unrealized P&L' / 'Win rate' / 'Avg return / closed capital' with a basis tooltip ONLY on the 'Avg return' field (title attribute at line ~975) \u2014 Realized and Unrealized carry no basis label at all, and per the register's independently-verified note, the THREE surfaces compute genuinely different numbers for the same nominal metric: Results' Unrealized comes from perf.liveUnrealizedPnl (calculatePnl over app-booked lots, performance.ts:640-660), Home's derive.ts:797-800 deriveMarkToMarket uses broker averageCost x quantity (a different basis), and iOS's ResultsView.swift AccountMetrics.displayedUnrealized prefers broker positions with a ledger fallback (MobileModels.swift:254-270) \u2014 a third basis. Confirmed performance.ts:497-498 `listPortfolioSnapshots(accountNumber, 'live', 100, userId)` caps the 'Versus the market' TWR window to ~100 snapshots (~7 trading sessions at the 60-min default run cadence, 2 snapshots/run) \u2014 but results/page.tsx:237 DOES already show `${startDate} -> ${endDate}` under the 'Your account' stat, so the window is disclosed as dates, just not NAMED as a bounded/accidental rolling window (register's verifier downgraded this to P2 on exactly this basis: 'what is missing is that it is an accidental rolling cap with a moving start, not a chosen window'). Confirmed win rate/avg-return render fabricated non-dash values (0% / +0.00%) for accounts with zero closed lots: results/page.tsx:957's `hasAny` gate is true for any account with equity-curve activity (nearly always true), and performance.ts:1707-1710's winRate/averageReturn both return 0 (not undefined) for an empty lot list; iOS's ResultsView.swift:71-84 has no gate at all. Confirmed derive.ts:804 `unrealizedPct: costBasis > 0 ? (unrealizedPnl / costBasis) * 100 : undefined` uses the NET SIGNED cost basis (shorts subtract from the sum), while the per-position row on the same page (positions.tsx:56-57) divides by `Math.abs(costBasis)` \u2014 inconsistent within one page. Confirmed performance.ts:1658-1666 fabricates `equity: 100 + realized` as a synthetic paper-curve base when paperSnapshots is empty, and results/page.tsx:985 renders it through the money-formatting EquityChart with no synthetic-data caveat. perf-19 (OOS backtest annualization bias, src/lib/backtest.ts) does NOT share this cluster's 'missing UI basis/window label' root cause \u2014 it is an admin-only (/api/admin/backtest-ic) statistical-methodology defect bundled in by lane tag only; excluded from this plan.",
    "files_to_change": [
     "app/console/results/page.tsx",
     "app/console/lib/derive.ts",
     "src/lib/performance.ts",
     "ios/SocraticTrade/ResultsView.swift"
    ],
    "approach": "1) Zero-lot honesty (perf-06, S, do first \u2014 cheapest and most visible): add `closedLotCount: number` to PerformanceSummary (performance.ts, alongside where liveWinRate/liveAvgReturn are computed ~1707-1721), computed as `lots.length` before the winRate/averageReturn calculation. In results/page.tsx's BucketCard, gate the Win rate and Avg return cells on `closedLotCount > 0` (replace the current always-numeric render at ~957/975 with `EM_DASH` when `closedLotCount === 0`, mirroring the pattern the file already uses elsewhere for missing data \u2014 'never fabricate' per format.ts's own header comment). In ios/SocraticTrade/ResultsView.swift (~71-84), add the same `closedLotCount > 0` gate before formatting winRate/avgReturn, falling back to the app's standard em-dash placeholder. 2) Basis labels on Results tiles (perf-05, S): add a `title=` tooltip (matching the existing pattern on the Avg-return cell) to the Realized P&L and Unrealized P&L `<div className='con-card-title'>` headers in BucketCard (940-953): Realized -> 'All-time, app-booked closed lots. Exits of positions this app did not open (pre-app holdings, manual trades) are not included.'; Unrealized -> 'Broker cost basis (average entry price x quantity), not the app's lot ledger.' Pick ONE canonical basis per surface per the register's recommendation: since Home (derive.ts) and iOS already prefer broker-cost-basis, change Results' BucketCard Unrealized to also source from broker cost basis (deriveMarkToMarket-equivalent) rather than perf.liveUnrealizedPnl, reserving the lot-ledger figure for the Tax/lots section specifically labeled 'app-booked lots' \u2014 this is the one actual behavior change in this item; everything else is copy. 3) Name the SPY benchmark window (perf-04, S): in results/page.tsx's benchmark Stat (~234-240), change the `sub` text from the bare date range to `Trailing ~${perf.benchmark.sessionCount ?? 'N'} sessions (${startDate} -> ${endDate})` \u2014 add `sessionCount` to the benchmark payload (performance.ts, derived from the same 100-snapshot-cap computation at line 497) so the label is generated from the real number, not hardcoded. 4) Net vs gross cost basis (perf-15, S): derive.ts:804 \u2014 change `costBasis` used for the PERCENTAGE only (not the dollar unrealizedPnl) to `positions.reduce((sum,p) => sum + Math.abs(p.averageCost * p.quantity), 0)`, matching positions.tsx:56-57's existing `Math.abs(costBasis)` convention; update the label to 'vs gross open cost basis' per the recommendation. 5) Synthetic $100 base (perf-18, S): performance.ts:1658-1666 \u2014 when paperSnapshots is empty, return an EMPTY curve (results/page.tsx's EquityChart already has an honest '<2 points' fallback sentence per the register's verify_note) instead of fabricating `100 + realized`; check deriveDayPnl (derive.ts:424-432) is not separately reading this same synthetic baseline elsewhere (register flagged it could inflate Day P&L to 'the whole account' in this edge case \u2014 verify that path is cut off by returning an empty curve rather than a 2-point synthetic one).",
    "tests": [
     "NEW test: PerformanceSummary computed from zero closed lots \u2014 assert `closedLotCount === 0` and that a snapshot test / render of BucketCard shows EM_DASH for Win rate and Avg return, not '0%'/'+0.00%'. FAILS today (renders fabricated zeros); PASSES after the closedLotCount gate.",
     "NEW test for derive.ts's mark-to-market percentage: build one long position (cost basis +1000) and one short position (cost basis -1000, i.e. net signed sum = 0) with a real unrealized gain on the short; assert `unrealizedPct` is NOT undefined/0 (today's `costBasis > 0` check fails when net signed basis is exactly 0 or negative, hiding a real percentage for short-heavy books). FAILS today; PASSES after switching to the gross (Math.abs-summed) basis.",
     "NEW test for performance.ts's synthetic paper curve: call the curve-builder with paperSnapshots=[] and a non-empty fills list; assert the returned curve is empty (or however the fix represents 'no real equity data'), not `[{equity: 100+realized, ...}, ...]`. FAILS today; PASSES after removing the fabricated base."
    ],
    "risks": "Item 2 (switching Results' Unrealized P&L source from the lot ledger to broker cost basis) is a real behavior/number change, not just a label \u2014 verify no existing test in test/*.ts (grep for 'liveUnrealizedPnl' assertions in results-page-adjacent tests) pins the current lot-ledger-sourced value; if any does, it is pinning the inconsistency this item intentionally removes and should be updated, not left red without review. The zero-lot em-dash gate must not accidentally suppress a genuinely-zero (0%) win rate for an account that DOES have exactly one break-even closed lot \u2014 gate strictly on `closedLotCount > 0`, not on `winRate === 0`. iOS changes need the standard `xcodebuild build` verification per repo policy before claiming done.",
    "effort": "M"
   }
  },
  {
   "key": "identity-fails-open",
   "title": "Identity resolution falls back to the owner instead of failing closed",
   "severity": "P1",
   "surface": "backend",
   "member_uids": [
    "security-auth:sec-01",
    "security-auth:sec-07",
    "security-auth:sec-08"
   ],
   "root_cause": "middleware.ts:385-388 sets trustedEmail = PRIMARY_EMAIL whenever isAuthConfigured() is false, and nothing asserts AUTH_SECRET in production \u2014 instrumentation.ts:47 asserts only ENCRYPTION_KEY \u2014 so a selective env regression makes every anonymous request the owner, with x-authenticated-user-email stamped at middleware.ts:420 and no route outside src/lib/auth/admin.ts checking identity provenance. The same shape repeats at src/lib/request-user.ts:28-34 (missing header \u2192 DEV_USER_ID 'local') and across 228 db functions that default userId=\"local\" (src/lib/db-learning.ts:12); middleware.ts:435's matcher additionally skips any path ending in a static extension.",
   "one_change": "Assert AUTH_SECRET at boot when DB_BOOTSTRAP=live alongside the existing ENCRYPTION_KEY assert (instrumentation.ts:47), gate the middleware.ts:385 local fallback on that same production marker (the dev branch is pinned by ~20 tests, so add rather than remove), and drop the userId=\"local\" defaults so a forgotten argument is a type error.",
   "effort": "M",
   "tranche": 1,
   "plan": {
    "cluster_key": "identity-fails-open",
    "summary": "Confirmed all three cited mechanisms precisely, and confirmed the existing test suite's shape so the fix can be scoped to ADD a gate without breaking pinned dev/test behavior. middleware.ts:165-167 `isAuthConfigured()` returns `Boolean(process.env.AUTH_SECRET) || isFlagOn(process.env.CF_ACCESS_TRUST_EMAIL_HEADER)`. middleware.ts:384-388 (exact lines, confirmed): `if (!trustedEmail && !isAuthConfigured()) { trustedEmail = PRIMARY_EMAIL; identitySource = AUTHENTICATED_IDENTITY_SOURCES.localFallback; }` \u2014 this fallback fires for EVERY request whenever AUTH_SECRET is unset and CF Access trust is off, with NO check for whether this is a real production boot; a selective env regression (AUTH_SECRET dropped from Coolify/Infisical while the app otherwise boots fine) silently makes every anonymous request resolve as the owner (PRIMARY_EMAIL), forwarded via `x-authenticated-user-email` (line 420) to every route handler. instrumentation.ts's register() (9-50+) DOES already fail-fast-in-production for a missing/invalid ENCRYPTION_KEY via `assertEncryptionKeyConfiguredInProduction()` (db-api-keys.ts:103-111, gated on `env.NODE_ENV !== 'production' -> return`) but has NO equivalent assertion for AUTH_SECRET. Confirmed `DB_BOOTSTRAP === 'live'` is this codebase's established, more-precise 'this is really the live production deployment' signal (already used exactly this way at src/lib/r2-usage.ts:553, and set only by scripts/coolify-prod-start.sh:184 for the real Coolify boot) \u2014 a better gate than NODE_ENV alone since NODE_ENV=production can occur in build/CI/other contexts that are not the live server. Confirmed test/middleware-auth.test.ts's Test 4 ('no auth env (authConfigured=false) -> PRIMARY_EMAIL fallback (dev/test behavior)', line 478) and Test 5 (line 490) stub `AUTH_SECRET=''`/`CF_ACCESS_TRUST_EMAIL_HEADER=''` but do NOT stub NODE_ENV or DB_BOOTSTRAP \u2014 so gating the fallback on `DB_BOOTSTRAP === 'live'` leaves these (and the rest of the ~34-test file's dev-fallback-dependent tests) completely unaffected under vitest's default env. Confirmed src/lib/request-user.ts's resolveRequestUserFromEmail (28-31) falls back to `DEV_USER_ID` ('local') whenever the `x-authenticated-user-email` header is empty/missing/has-no-'@' \u2014 middleware ALWAYS explicitly sets this header (even to `''`) on every request it forwards (line 420), so in normal operation this only fires for the two intentional token-carveout branches (admin-token/bearer routes) or the local-fallback path above; the residual risk is a route reached WITHOUT going through middleware at all (a config.matcher gap, or a handler invoked directly in a future refactor) silently resolving as 'local'. The finding's broader recommendation to remove the `userId: string = \"local\"` default from all ~228 db-* functions (making a forgotten argument a compile error) is a real, large, separate mechanical refactor \u2014 NOT attempted in this plan; flagged as an explicit out-of-scope follow-up.",
    "files_to_change": [
     "instrumentation.ts",
     "src/lib/auth-secret-guard.ts (new, small)",
     "middleware.ts",
     "src/lib/request-user.ts"
    ],
    "approach": "1) Boot-time assertion (mirrors assertEncryptionKeyConfiguredInProduction's exact pattern): new small module src/lib/auth-secret-guard.ts exporting `export function assertAuthSecretConfiguredInProduction(env: NodeJS.ProcessEnv = process.env): void { if (env.DB_BOOTSTRAP !== 'live') return; if (env.AUTH_SECRET && env.AUTH_SECRET.trim().length > 0) return; if (isFlagOn(env.CF_ACCESS_TRUST_EMAIL_HEADER) && env.CF_ACCESS_TEAM_DOMAIN?.trim() && env.CF_ACCESS_AUD?.trim()) return; throw new Error('AUTH_SECRET is missing in a live production boot (DB_BOOTSTRAP=live) and Cloudflare Access trust is not fully armed either. Refusing to boot: without a real identity source configured, every request would resolve as the owner via the dev/local fallback. Set AUTH_SECRET (or fully configure CF_ACCESS_TRUST_EMAIL_HEADER + CF_ACCESS_TEAM_DOMAIN + CF_ACCESS_AUD) before starting.'); }` (reuse/import the same `isFlagOn` helper middleware.ts already has, or duplicate the tiny predicate \u2014 check whether it's exported from middleware.ts first since middleware.ts is edge-runtime-only and this guard needs to run in instrumentation.ts's Node runtime). Call it in instrumentation.ts's register() (~47), immediately after the existing `assertEncryptionKeyConfiguredInProduction()` call, same style/position. 2) Middleware fallback gate (the core fix \u2014 one added condition): change middleware.ts:384-388's guard from `if (!trustedEmail && !isAuthConfigured())` to `if (!trustedEmail && !isAuthConfigured() && process.env.DB_BOOTSTRAP !== 'live')`. When DB_BOOTSTRAP==='live' and auth is unconfigured, trustedEmail stays null/empty and execution falls through unchanged to the existing final `else { fail closed: 401 for /api/*, redirect to /login otherwise }` branch (no other code path changes needed \u2014 the fail-closed branch already exists and already does the right thing, it's just unreachable today whenever the dev fallback fires first). 3) request-user.ts defense-in-depth: in resolveRequestUser (34-45), distinguish 'header explicitly present but empty' (the two legitimate token-carveout routes \u2014 must keep resolving to DEV_USER_ID, unchanged) from 'header genuinely absent' (request reached a handler without going through middleware's identity-setting path at all): check `request.headers.has(AUTHENTICATED_EMAIL_HEADER)` \u2014 if DB_BOOTSTRAP==='live' AND the header is entirely absent (not merely empty), throw a new small `UnauthenticatedRequestError` (401) instead of silently resolving to DEV_USER_ID; when the header IS present (even as ''), keep today's exact behavior unchanged. Callers of resolveRequestUser that don't already catch/convert errors to a 401 response need a check \u2014 grep call sites and confirm each API route already has error-boundary handling (most Next.js route handlers here throw straight through to a 500 today for unexpected errors, which is an acceptable fail-closed outcome even before adding a dedicated 401 mapping, but note it as a follow-up polish).",
    "tests": [
     "NEW test in a new/adjacent instrumentation or auth-secret-guard test file: `assertAuthSecretConfiguredInProduction({DB_BOOTSTRAP:'live'})` (no AUTH_SECRET, no CF Access config) throws; `assertAuthSecretConfiguredInProduction({DB_BOOTSTRAP:'live', AUTH_SECRET:'x'})` does not throw; `assertAuthSecretConfiguredInProduction({DB_BOOTSTRAP:'fresh'})` (dev/non-live boot) does not throw regardless of AUTH_SECRET. FAILS today (function doesn't exist / instrumentation.ts never checks this); PASSES once added.",
     "NEW test in test/middleware-auth.test.ts: `vi.stubEnv('DB_BOOTSTRAP','live'); vi.stubEnv('AUTH_SECRET',''); vi.stubEnv('CF_ACCESS_TRUST_EMAIL_HEADER','');` then call middleware() on `/api/dashboard` with no session cookie \u2014 assert `res.status === 401` (or redirect for a page route), NOT 200 with `x-authenticated-user-email` set to PRIMARY_EMAIL. FAILS today (the dev fallback fires unconditionally, returns 200 as the owner); PASSES after the DB_BOOTSTRAP gate. Explicitly re-run the file's existing Test 4/5 (lines 478, 490) UNCHANGED afterward and confirm they still pass (they don't stub DB_BOOTSTRAP, so they should be unaffected).",
     "NEW test for request-user.ts: call `resolveRequestUser(request)` with DB_BOOTSTRAP='live' stubbed and a Request object that has NO `x-authenticated-user-email` header at all (not even empty) \u2014 assert it throws/refuses rather than returning `{userId: 'local'}`. FAILS today; PASSES after the has()-vs-empty distinction is added. Also assert the token-carveout case (header explicitly set to '') still resolves to DEV_USER_ID unchanged."
    ],
    "risks": "This is the highest blast-radius plan in the set \u2014 a wrong DB_BOOTSTRAP check in local/CI/Cursor-Cloud environments (which do NOT set DB_BOOTSTRAP at all) would leave the dev fallback fully intact there (DB_BOOTSTRAP undefined !== 'live'), which is exactly the desired outcome, but double-check the Coolify prod boot script (scripts/coolify-prod-start.sh:184) actually exports DB_BOOTSTRAP into the Next.js server process's env (not just a wrapper shell's env) before relying on it in middleware.ts's edge/node runtime \u2014 verify with `docker exec` or the ops-snapshot script rather than assuming. The ~34-test middleware-auth.test.ts suite and the two request-user.ts-adjacent test files must ALL be re-run green before landing (do not just add new tests \u2014 confirm zero regressions in the existing dev/test-fallback-dependent tests, since this is exactly the class of change the file's own header comment flags as security-critical: 'regression test for the NODE_ENV-edge-inlining IDOR bug'). The 228-call-site `userId=\"local\"` default removal is explicitly NOT part of this plan \u2014 do not attempt it inline; it needs its own dedicated, carefully-tested PR given its size, per the owner's 'sole user today, no compat-shim churn' framing this should be sequenced behind, not ahead of, real product need.",
    "effort": "M"
   }
  },
  {
   "key": "price-alert-evaluation",
   "title": "Price alerts stop evaluating silently \u2014 no active account, one broker, swallowed errors, no age check",
   "severity": "P1",
   "surface": "backend",
   "member_uids": [
    "alerts-01",
    "alerts-02",
    "alerts-05",
    "alerts-06",
    "alerts-09"
   ],
   "root_cause": "src/lib/alerts.ts:67-69 returns [] whenever the console-active account has no accountNumber (zero connected accounts, or the active one was deleted \u2014 deleteConnectedAccount at src/lib/db-api-keys.ts:1376 promotes nobody), even though PriceAlert rows are user-scoped; :73-78 catches any gateway throw in an empty catch with no log or audit, so a Public.com/eToro API error is indistinguishable from \"nothing to check\"; :83 acts on the price with no staleness check although quoteAgeSecForStalenessGate exists; and :17's SYMBOL_RE disagrees with isValidAppSymbol (src/lib/index-universes.ts:311), so malformed symbols sit armed forever. Zero tests cover the evaluation loop.",
   "one_change": "Switch checkPriceAlerts to the user-scoped fetchFreshQuotesCascade(symbols, userId) (src/lib/quotes-cascade.ts:149) instead of gateway.getEquityQuotes on the active account \u2014 this closes the no-account gate and the single-broker error in one edit \u2014 then log the caught error, apply quoteAgeSecForStalenessGate, share isValidAppSymbol, and add the evaluation tests.",
   "effort": "M",
   "tranche": 1,
   "plan": {
    "cluster_key": "price-alert-evaluation",
    "summary": "checkPriceAlerts (src/lib/alerts.ts:63-116) gates on the console-active account's accountNumber, calls only that one broker gateway, swallows any gateway error into an empty result with no log/audit, applies no staleness check, and validates symbols with a looser regex than the rest of the app \u2014 confirmed exactly as described; premise holds.",
    "files_to_change": [
     "src/lib/alerts.ts"
    ],
    "approach": "In src/lib/alerts.ts: (1) delete `getBrokerGateway` import and the `accountNumber`/gateway-based quote fetch at lines 67-78; replace with `const policy = getPolicy(userId);` (still needed for sendNotification) plus `const quotes = await fetchFreshQuotesCascade(symbols, userId)` imported from './quotes-cascade' \u2014 this is USER-scoped (cascades across every connected broker plus Alpaca/Yahoo/ROIC fallbacks per its own doc comment at quotes-cascade.ts:132-148), so it works with zero, one, or many connected accounts and never needs an active account's accountNumber. (2) Wrap the cascade call's catch block to `console.error` and `audit(\"alert.check_error\", { userId, symbols, error }, userId)` before returning `[]`, so a real provider outage is distinguishable from \"nothing armed\" in the audit trail (mirrors the pattern already used at quotes-cascade.ts:236-238 for its own internal levels). (3) Inside the per-alert loop (current lines 80-114), after resolving `quote = quotes[alert.symbol]`, call `quoteAgeSecForStalenessGate(quote, Date.now())` (imported from './quotes-cascade') and `continue` (skip, don't trigger) when `missing` or `ageSec > (policy.maxQuoteAgeSec ?? 120)` \u2014 mirrors the exact isStale formula at src/lib/policy.ts:413. (4) Delete the local `SYMBOL_RE` regex at line 17 and its use in `createAlert` (line 33); import and use `isValidAppSymbol` from './index-universes' instead, so an alert can never be armed on a symbol the rest of the app (watchlist/universe) would already reject.",
    "tests": [
     "New test/price-alerts-evaluation.test.ts (file does not exist today \u2014 zero tests cover this loop): (a) 'triggers with zero connected accounts, using cascade market data alone' \u2014 seed a user with an armed alert and NO connected accounts, mock/stub fetchFreshQuotesCascade (or seed a real Yahoo-fallback-capable quote path per existing quotes-cascade test conventions) to return a hit price, call checkPriceAlerts, assert it is returned triggered \u2014 MUST FAIL today because alerts.ts:69 `if (!accountNumber) return [];` short-circuits before any quote lookup even runs.",
     "(b) 'logs and audits a cascade failure instead of returning silently' \u2014 force fetchFreshQuotesCascade to reject, call checkPriceAlerts, assert an 'alert.check_error' audit row exists \u2014 MUST FAIL today because the empty catch at alerts.ts:76-78 has no audit/log call.",
     "(c) 'does not trigger on a stale quote' \u2014 seed an armed alert whose threshold the quote crosses, but stamp the quote's asOf older than policy.maxQuoteAgeSec, assert the alert stays armed (not triggered) \u2014 MUST FAIL today because current code has no staleness check at all (line 83-86 acts on any numeric price regardless of age).",
     "(d) 'rejects a symbol createAlert would reject elsewhere' \u2014 call createAlert with a symbol isValidAppSymbol rejects but the old SYMBOL_RE accepted (e.g. a leading-dot or double-dot string), assert { error: 'INVALID_SYMBOL' } \u2014 MUST FAIL today since the old regex accepts it."
    ],
    "risks": "fetchFreshQuotesCascade is heavier than a single gateway.getEquityQuotes call (it tries multiple providers in series) \u2014 checkAllUserPriceAlerts (alerts.ts:118-122) already runs per-user sequentially on a scheduler cadence, so latency grows modestly per user with armed alerts; acceptable since alerts already tolerate a several-second cascade elsewhere in the app. Tightening symbol validation via isValidAppSymbol is a behavior change for createAlert: it now allows digits (old regex disallowed them entirely) and rejects leading/double/trailing dots the old regex allowed \u2014 grep test/watchlist-alerts.test.ts and any other createAlert callers (chat-injection.test.ts, chat-orchestrator.test.ts, atlas-golden-eval.test.ts) before landing to confirm no test relies on a symbol shape isValidAppSymbol would now reject; the two existing watchlist-alerts.test.ts cases ('AAPL', '') are unaffected. No persisted-schema change \u2014 PriceAlert rows are untouched.",
    "effort": "M"
   }
  },
  {
   "key": "account-write-guards",
   "title": "Account-scoped write paths have no guards: one click can arm Autopilot or reactivate a draining account",
   "severity": "P1",
   "surface": "cross-surface",
   "member_uids": [
    "pages-01",
    "lifecycle-01",
    "lifecycle-07",
    "lifecycle-11"
   ],
   "root_cause": "importAccountSettings (src/lib/db-profiles.ts:895) strips only identity and user-level fields, so strategyAuthority copies through and a single \"Import settings\" click can flip a live account from Ask-first to Autopilot with none of the TypedConfirm ceremony app/console/guardrails/page.tsx:700 requires; applyProfileToAccount (db-profiles.ts:731) and activateStrategyProfile (:685) do not strip it at all. setActiveConnectedAccount (src/lib/db-api-keys.ts:1323) never reads is_draining, and nothing ever clears the flag that deleteConnectedAccount sets at :1381, so a disconnected account can be re-loaded as the live target while the scheduler drains and purges it.",
   "one_change": "Treat strategyAuthority and is_draining as account-owned state in src/lib/db-profiles.ts:685/731/895 and src/lib/db-api-keys.ts:1323 \u2014 preserve authority the way systemState already is, refuse or clear draining on activate and on re-upsert \u2014 and route any authority change through the existing typed-confirm gate.",
   "effort": "M",
   "tranche": 1,
   "plan": {
    "cluster_key": "account-write-guards",
    "summary": "Verified exactly as described. strategyAuthority is an account-level TradingPolicy field (not in USER_LEVEL_POLICY_FIELDS, src/lib/db-profiles.ts:21-48) so it flows unguarded through copyPolicyConfigToActiveAccount (:382-403, used by createStrategyProfile/updateStrategyProfile/activateStrategyProfile), applyProfileToAccount (:731-764), and importAccountSettings (:862-918) \u2014 all three preserve the target's systemState but none preserves strategyAuthority, so a saved profile or another account's settings can silently arm/disarm Autopilot with zero TypedConfirm ceremony. Separately, setActiveConnectedAccount (src/lib/db-api-keys.ts:1323-1336) never checks is_draining, and deleteConnectedAccount (:1376-1384) never clears it or promotes another account, so a disconnected/draining account can be re-selected as the live target while the scheduler's drain sequence (scheduler.ts:850-881) is still cancelling its orders and about to purge it.",
    "files_to_change": [
     "src/lib/db-profiles.ts",
     "src/lib/db-api-keys.ts"
    ],
    "approach": "In src/lib/db-profiles.ts: (1) copyPolicyConfigToActiveAccount (:382-403) \u2014 change `const currentState = peekPolicy(userId, account.id).systemState;` to capture the whole current policy (`const current = peekPolicy(userId, account.id);`) and spread `strategyAuthority: current.strategyAuthority` alongside the existing `systemState: current.systemState` in the `policy:` object passed to writeAccountStrategyState (this single choke point fixes create/update/activateStrategyProfile in one edit, mirroring the existing systemState-preservation comment at :391-396). (2) applyProfileToAccount (:731-764) \u2014 change `const currentState = getPolicy(userId, connectedAccountId).systemState;` to `const current = getPolicy(userId, connectedAccountId);` and add `strategyAuthority: current.strategyAuthority` to the mergePolicy(...) call at :745-750 alongside the existing `systemState: currentState`. (3) importAccountSettings (:862-918) \u2014 change `const targetSystemState = peekPolicy(userId, targetConnectedAccountId).systemState;` to `const targetPolicy = peekPolicy(...)` and add `strategyAuthority: targetPolicy.strategyAuthority` to the mergePolicy(...) call at :896-901 alongside `systemState: targetSystemState`. No new TypedConfirm ceremony is added \u2014 the fix is that authority simply never travels through these three copy paths, so the ONLY way to change it stays the existing guardrails-page TypedConfirm flow (app/console/guardrails/page.tsx:700-725), consistent with the owner's 'no new blocking ceremony' rule. In src/lib/db-api-keys.ts: setActiveConnectedAccount (:1323-1336) \u2014 inside the existing db.transaction(), change the `exists` SELECT to also fetch `is_draining`, and throw `new Error(\"This account is disconnected and being wound down \u2014 it can no longer be made active.\")` when `is_draining === 1`, before either UPDATE. The existing callers (app/api/connected-accounts/[id]/activate/route.ts:12-13 and src/lib/mobile-api.ts:979) already catch and surface any thrown Error as a 400, so this needs no new route-layer plumbing.",
    "tests": [
     "test/copy-config-preserves-arming.test.ts \u2014 add a case mirroring the existing systemState pair (lines 17-53): active account's strategyAuthority is 'propose'; activate a library profile whose saved policy.strategyAuthority is 'decide'; assert db.getPolicy(u, acct).strategyAuthority stays 'propose' \u2014 MUST FAIL today (currently flips to 'decide').",
     "test/strategy-copy-to-account.test.ts \u2014 add a case mirroring the existing run-state test (lines 46-66): target account's strategyAuthority is 'propose'; applyProfileToAccount a profile whose policy.strategyAuthority is 'decide'; assert the target keeps 'propose' \u2014 MUST FAIL today.",
     "test/settings-import.test.ts \u2014 add a case mirroring 'preserves the target's systemState' (lines 58-75): target's strategyAuthority is 'propose', source's is 'decide'; importAccountSettings(userId, source, target); assert result.strategyAuthority and db.getPolicy(userId, target).strategyAuthority are both 'propose' \u2014 MUST FAIL today.",
     "New assertion (can live in a new test/set-active-connected-account-draining.test.ts or alongside persistence-notification.test.ts's existing 'missing-account' throw test at line 146): upsertConnectedAccount, deleteConnectedAccount(id) to mark it draining, then expect(() => setActiveConnectedAccount(id)).toThrow(/wound down|draining/i) \u2014 MUST FAIL today (currently succeeds and flips is_active=1 on a draining row)."
    ],
    "risks": "None of the grepped existing tests (test/copy-config-preserves-arming.test.ts, test/strategy-copy-to-account.test.ts, test/settings-import.test.ts, and the ~35 files calling setActiveConnectedAccount) construct a scenario where strategyAuthority differs between source/profile and target, or where an account is draining before activation \u2014 so none should need updating, only extending. Double-check src/lib/db-profiles.ts:150 (`isTieredWrite`) and any other reader of USER_LEVEL_POLICY_FIELDS is unaffected since strategyAuthority is not being added to that set, only explicitly re-pinned at three write sites. No schema/migration change.",
    "effort": "M"
   }
  },
  {
   "key": "green-request-schema",
   "title": "Bull strict JSON schema is invalid, so every OpenAI-family Green seat 400s before generation",
   "severity": "P1",
   "surface": "backend",
   "member_uids": [
    "llm-agent-architecture:llm-01",
    "llm-agent-architecture:llm-08"
   ],
   "root_cause": "src/lib/strategy.ts:5870 declares exitPlan inside the Bull item's properties while BULL_PROPOSAL_REQUIRED_KEYS (strategy.ts:6653, used at :5851 for the required array) omits it, under additionalProperties:false \u2014 OpenAI strict mode requires required \u2287 properties, so any native-OpenAI or openai/* routed seat is rejected at the API layer 100% of the time (the fast 400 misdiagnosed as 'terra unservable' in #2831). The same file then trusts provider schema enforcement on json_object transports (strategy.ts:6115) where nothing enforces it.",
   "one_change": "Derive required from properties at src/lib/strategy.ts:5851 (or add exitPlan to BULL_PROPOSAL_REQUIRED_KEYS at :6653), add the invariant test that required \u2287 properties, and add a post-parse validator for the json_object transports.",
   "effort": "S",
   "tranche": 1,
   "plan": {
    "cluster_key": "green-request-schema",
    "summary": "Confirmed exactly. The Bull proposal JSON-schema `properties` object (src/lib/strategy.ts:5852-5871) declares 17 keys including `exitPlan` (:5869-5870), but `required: [...BULL_PROPOSAL_REQUIRED_KEYS]` (:5851) spreads a 16-key constant (:6653-6670) that omits `exitPlan`. Under `additionalProperties:false` + OpenAI/Gemini `strict:true` (wired at src/lib/llm-call.ts:512/515/529), strict mode requires required to be exactly the property key set \u2014 so every native-OpenAI, Gemini, or any openai/*-routed Green seat 400s before generation. DeepSeek and any openAiJsonObject-forced or Gemini-schema-fallback path uses bare `json_object` (llm-call.ts:510/517), which the app does not itself validate on the happy-path JSON.parse (strategy.ts's parse around the extractJsonPayload/JSON.parse branch) \u2014 only the jsonrepair fallback path runs BULL_PROPOSAL_REQUIRED_KEYS.every(key in record) via filterRepairedProposals (:6759).",
    "files_to_change": [
     "src/lib/strategy.ts"
    ],
    "approach": "(1) Add `\"exitPlan\",` to BULL_PROPOSAL_REQUIRED_KEYS (src/lib/strategy.ts:6653-6670, insert between \"bracketTakeProfit\" and \"stopPlan\") \u2014 this is the single source spread into both the schema's `required` array (:5851) and filterRepairedProposals's completeness gate (:6759, :6810), per the function's own doc comment at :6648-6651 that these must never drift; exitPlan stays nullable in the schema (`type: [\"string\",\"null\"]` at :5870) so this only requires the KEY be present, not a non-null value, consistent with autonomyOverride/bracketStopLoss/bracketTakeProfit/stopPlan's existing nullable-but-required treatment. (2) Add a genuine drift-catching invariant test (not just the existing smoke-check at test/strategy-hardening.test.ts:865-871) that captures the ACTUAL outgoing request schema the way test/strategy-candidate-enforcement.test.ts:100-170 already does \u2014 mock fetch, run runStrategyOnce() with an OpenAI-routed policy, read `bullRequest.text.format.schema` (Responses) or `.response_format.json_schema.schema` (chat-completions), and assert `Object.keys(schemaObj.properties.proposals.items.properties).sort()` deep-equals `[...schemaObj.properties.proposals.items.required].sort()`. (3) Add a post-parse completeness check on the happy-path JSON.parse branch (the `try { const parsed = JSON.parse(extractJsonPayload(text))... return { text, proposals: parsed.proposals ?? [], ... }` block a few hundred lines after :6100, immediately following the successful-parse `try`): route `parsed.proposals` through the SAME per-item completeness predicate filterRepairedProposals already applies (key presence + type checks) before returning them, not only on the jsonrepair fallback path \u2014 so a json_object transport (DeepSeek, or a Gemini schema-translation fallback) that returns syntactically-valid but incomplete JSON gets its bad proposals dropped here too, instead of only when jsonrepair's error branch is hit.",
    "tests": [
     "test/strategy-hardening.test.ts:865-871 \u2014 strengthen 'the completeness gate and the structured-output schema share one required-keys source' to `expect(BULL_PROPOSAL_REQUIRED_KEYS).toContain(\"exitPlan\")` \u2014 FAILS before the fix (exitPlan absent).",
     "New test in test/strategy-candidate-enforcement.test.ts (or a new file) per approach step (2): assert schema property keys === schema required keys for the Bull proposals item \u2014 MUST FAIL before the fix (required has 16 keys, properties has 17; a strict-mode OpenAI call built from today's schema literal would 400) and PASS after.",
     "test/strategy-hardening.test.ts's existing filterRepairedProposals tests (e.g. around lines 830-863) \u2014 build a `complete()` fixture that now must also include an `exitPlan` field for the completeness gate to accept it; confirm a repaired proposal missing exitPlan is now correctly DROPPED (previously it would have passed the gate) \u2014 a new assertion here catches drift in the other direction (repair should track the same required set)."
    ],
    "risks": "Any Bull system-prompt or few-shot example that already always emits exitPlan is unaffected. Any LIVE in-flight strategy run using a cached/older request shape is not a concern (this is a code-level constant, not persisted data). The main risk is the jsonrepair completeness gate at filterRepairedProposals now requiring exitPlan too \u2014 verify no existing 'kept' fixture in test/strategy-hardening.test.ts omits exitPlan; if any do, they need an exitPlan field added to their `complete()` helper fixture. Also check LLM_OUTPUT_TOKEN_CAPS.strategyProposal has headroom for one more field per proposal \u2014 negligible since exitPlan can be null.",
    "effort": "S"
   }
  },
  {
   "key": "run-scoped-account",
   "title": "Run-scoped code re-resolves the console-active account instead of the account being traded",
   "severity": "P1",
   "surface": "backend",
   "member_uids": [
    "llm-agent-architecture:llm-02",
    "llm-agent-architecture:llm-05",
    "coach-04"
   ],
   "root_cause": "src/lib/red-team.ts:192 calls getActiveConnectedAccount(userId) and :203 calls getStrategyPrompt(userId) with no account argument, even though debateProposal was handed runPolicy carrying the correct connectedAccountId \u2014 so with two accounts scheduled concurrently the adversarial reviewer can veto on the wrong venue's short capability and reason from another account's custom strategy prompt (deriveExecutionState at :196 has the same bug). src/lib/learned-context/store.ts:401 appends approved directives to the active account's prompt regardless of the pending row's account, and chat turns carry no account at all (src/lib/types.ts:3011) so the 10-turn replay mixes accounts.",
   "one_change": "Thread the owning connectedAccountId explicitly through src/lib/red-team.ts:192/196/203, src/lib/learned-context/store.ts:401 and the chat_turns row plus its replay window, and delete every getActiveConnectedAccount call from run-scoped paths.",
   "effort": "M",
   "tranche": 1,
   "plan": {
    "cluster_key": "run-scoped-account",
    "summary": "Confirmed. debateProposal (src/lib/red-team.ts:177-233) resolves `activeAccount = getActiveConnectedAccount(userId)` (:192) and `basePrompt = getStrategyPrompt(userId)` (:203, no account arg) even though its caller (strategy.ts:2516) already passes the run-scoped `runPolicy` carrying the correct connectedAccountId as `policyOverride`; `deriveExecutionState(policy, activeAccount)` at :202 inherits the same wrong-account activeAccount. src/lib/learned-context/store.ts:401-403 (applyApprovedPending, strategy-directive tier) calls getStrategyPrompt(pending.userId) / setStrategyPrompt(merged, pending.userId) with no account arg despite pending.connectedAccountId being available and used two branches later (:427-428) \u2014 both silently resolve to whatever account is currently 'active' in the console (resolveAccount's getActiveConnectedAccount fallback, db-profiles.ts:323-326), not the account the directive was approved for. src/lib/types.ts's ChatTurn (:3011-3025) and the chat_turns table (db.ts:3970-3980) carry no account column at all, and app/chat/orchestrator.ts:143 (`listTurns(userId, 10)`) and its two appendTurn call sites (:148, :479-485) are unfiltered by the connectedAccountId already resolved at :123 \u2014 so switching the active account mid-conversation replays the WRONG account's prior turns into the new account's context.",
    "files_to_change": [
     "src/lib/red-team.ts",
     "src/lib/learned-context/store.ts",
     "src/lib/types.ts",
     "src/lib/db.ts",
     "src/lib/db-api-keys.ts",
     "src/lib/chat-history.ts",
     "src/lib/chat/orchestrator.ts"
    ],
    "approach": "(1) src/lib/red-team.ts: add `getConnectedAccount` to the existing `import { getActiveConnectedAccount, getPolicy, getStrategyPrompt } from \"./db\";` (line 18). Change :192 `const activeAccount = getActiveConnectedAccount(userId);` to `const activeAccount = policy.connectedAccountId ? getConnectedAccount(policy.connectedAccountId, userId) : getActiveConnectedAccount(userId);`. Change :203 `const basePrompt = getStrategyPrompt(userId);` to `const basePrompt = getStrategyPrompt(userId, policy.connectedAccountId);` (getStrategyPrompt already accepts an optional connectedAccountId, db-profiles.ts:602). This automatically fixes deriveExecutionState at :202 too since it consumes the same corrected `activeAccount`. The `policy` variable already resolves to `policyOverride ?? getPolicy(userId)` (:191) so this is a pure account-resolution fix with no signature change. (2) src/lib/learned-context/store.ts:401-403 \u2014 change to `const current = getStrategyPrompt(pending.userId, pending.connectedAccountId ?? undefined); const merged = mergeStrategyDirectiveBlock(current, pending.id, pending.value, assertedAt); setStrategyPrompt(merged, pending.userId, pending.connectedAccountId ?? undefined);` \u2014 both functions already accept the optional 3rd arg (db-profiles.ts:602, :611); when pending.connectedAccountId is null (a general/user-level directive) this preserves today's exact behavior (falls back to getActiveConnectedAccount via resolveAccount, db-profiles.ts:323-326). (3) Add `connectedAccountId?: string | null;` to ChatTurn (src/lib/types.ts, after `clientTurnId` around :3023). Add a new versioned migration (src/lib/db.ts, version 85, following the v84 pattern at :3134-3151) that ALTERs `chat_turns` adding a nullable `connected_account_id TEXT` column (existing rows get NULL \u2014 meaning pre-migration/unattributed, not 'no account'). In src/lib/db-api-keys.ts: RawChatTurnRow (:1908-1919) gains `connected_account_id: string | null`; mapChatTurn (:1921-1942) maps it to `connectedAccountId: row.connected_account_id ?? null`; insertChatTurn (:1944-1951) writes `turn.connectedAccountId ?? null` as a new bound param; listChatTurns (:1961-1967) gains an optional `connectedAccountId?: string | null` 3rd param and, when it is a defined string, filters `WHERE user_id = ? AND (connected_account_id = ? OR connected_account_id IS NULL)` (falls back to no filter when the param is omitted, so every existing caller that doesn't pass it is byte-identical). (4) src/lib/chat-history.ts: appendTurn's `input` gains `connectedAccountId?: string | null`, stamped onto the constructed ChatTurn (:46-57); listTurns gains a passthrough 3rd param to listChatTurns. (5) src/lib/chat/orchestrator.ts: line 143 `listTurns(userId, 10)` -> `listTurns(userId, 10, connectedAccountId)` (connectedAccountId already resolved at :123); line 148's appendTurn call gains `connectedAccountId`; line 479-485's `persistAssistantTurn` appendTurn call gains `connectedAccountId` too.",
    "tests": [
     "New test in test/red-team.test.ts (or a new file): two connected accounts A (active, no short capability) and B (short-capable, has its own custom strategy prompt / redTeamLlmModel); call debateProposal(shortProposal, quote, userId, policyForAccountB) where policyForAccountB.connectedAccountId = B's id; assert the review does NOT hit the 'cannot short' refusal that would fire if activeAccount resolved to A, and that the system prompt build used B's own getStrategyPrompt content \u2014 MUST FAIL today (resolves account A via getActiveConnectedAccount regardless of policyOverride.connectedAccountId).",
     "test/learned-context-pending.test.ts \u2014 add a case: two connected accounts, active account is B; seed a strategy-directive pending row via seedDirectivePending with connectedAccountId set to A (not B); approveAs(...); assert db.getStrategyPrompt(userId, A) contains the AI-LEARNED block and db.getStrategyPrompt(userId, B) does NOT \u2014 MUST FAIL today (writes land on B, the active account, regardless of pending.connectedAccountId).",
     "test/chat-history.test.ts \u2014 add a case: appendTurn with connectedAccountId 'acct-a' and another with 'acct-b' for the same userId; listTurns(userId, 10, 'acct-a') returns only the acct-a turn (plus any legacy null-account turns); listTurns(userId, 10) (no filter arg) still returns both, unchanged \u2014 the second assertion proves backward compatibility; the first MUST FAIL before this change exists (no filtering capability at all).",
     "test/chat-orchestrator.test.ts \u2014 add a case: user chats while account A is active (a turn is recorded), then the active account is switched to B and the user chats again; assert the history array built at orchestrator.ts's listTurns call for the SECOND turn does not include the first (account-A) turn's text \u2014 MUST FAIL today (10-turn window is unfiltered by account)."
    ],
    "risks": "The chat_turns migration is additive/nullable \u2014 no backfill needed or possible (pre-migration turns have no recorded account, and treating them as 'always visible' via the `OR connected_account_id IS NULL` filter is a deliberate, conservative continuity choice: a single-account user's history is never affected since virtually all their turns share one account attribution going forward). trimChatTurns (db-api-keys.ts:1970-1978) and MAX_TURNS=100 stay a per-USER cap, not per-account \u2014 a heavy multi-account user's older single-account history can still be trimmed by another account's chat volume; this is a known, accepted trade-off, not fixed here (flagging to avoid scope creep \u2014 a per-account cap would need a larger redesign). Grep test/chat-history.test.ts (lines 24-49) and test/chat-orchestrator.test.ts before landing \u2014 both call listTurns/appendTurn without a connectedAccountId and must continue to pass unchanged since the new param is additive-optional. The getConnectedAccount import addition to red-team.ts must not create a cycle \u2014 src/lib/db.ts already re-exports it and red-team.ts already imports several names from './db', so this is a zero-risk addition.",
    "effort": "M"
   }
  },
  {
   "key": "event-loop-pins",
   "title": "RAG mirroring pins the single serving+trading event loop for minutes at a time",
   "severity": "P1",
   "surface": "backend",
   "member_uids": [
    "backend-reliability:berel-01",
    "backend-reliability:berel-04",
    "backend-reliability:berel-06",
    "backend-reliability:berel-07",
    "backend-reliability:berel-10",
    "backend-reliability:berel-11",
    "backend-reliability:berel-12"
   ],
   "root_cause": "src/lib/db-learning.ts:1613-1616 makes idempotency a DELETE with equality on non-rowid FTS5 columns, which FTS5 serves as a full-corpus scan \u2014 measured 4.3ms/row at 10k rows rising to 26.3ms/row at 60k, and ~300ms/chunk at the live corpus per the repo's own receipt in src/lib/rag/fts-mirror-bound.ts:20-23, while the INSERT it was blamed on costs 0.1-0.4ms. src/lib/rag/persist-local-complete.ts:46-54 and src/lib/web-sources/sec-filings.ts:624-631/736-756 then loop that call over a whole filing with no yield, no transaction, and no hasInFlightStrategyWork() gate (scheduler.ts:589-602) \u2014 unlike the bounded SEC worker at sec-ingest-worker.ts:462-475. There is no event-loop lag instrumentation anywhere.",
   "one_change": "Give the FTS mirror a rowid/side-key so idempotency becomes an indexed lookup instead of a scan (src/lib/db-learning.ts:1613 plus the DDL at src/lib/db.ts:2148), and route every mirror caller through the existing planFtsMirrorSlice + yieldEventLoop + strategy gate the SEC worker already uses.",
   "effort": "L",
   "tranche": 1,
   "plan": {
    "cluster_key": "event-loop-pins",
    "summary": "Confirmed with one correction: the adaptive-yield batch helper (insertDocumentChunkFtsBatch, db-learning.ts:1666-1698) and the tick/budget planner (planFtsMirrorSlice, src/lib/rag/fts-mirror-bound.ts) already exist and are already correctly wired into src/lib/rag/sec-ingest-worker.ts:456-476 (with hasInFlightStrategyWork gating at :464) \u2014 that ONE caller is NOT the bug. The real gaps are: (a) the per-row idempotency DELETE inside insertDocumentChunkFts (db-learning.ts:1614-1617) and inside insertDocumentChunkFtsBatch's own runGroup (:1671-1684) is a full-table-scan `WHERE content_hash=? AND symbol=? AND source=? AND accession=?` on an FTS5 virtual table (no B-tree exists on those columns for anything but MATCH), which is the true O(corpus) cost underlying every other symptom; (b) src/lib/rag/persist-local-complete.ts:42-62 (persistLocalComplete, called from src/lib/web-sources/sec-filings.ts:625-632) loops the SINGLE non-batched insertDocumentChunkFts with zero yields and zero hasInFlightStrategyWork gate; (c) src/lib/web-sources/sec-filings.ts:736-756 has a SECOND unbounded per-row insertDocumentChunkFts loop, deliberately synchronous because it must run inside one atomic transaction with the vector-commit proof (comment at :743-744) \u2014 this site structurally cannot yield, so it depends entirely on fix (a) to stay fast.",
    "files_to_change": [
     "src/lib/db.ts",
     "src/lib/db-learning.ts",
     "src/lib/rag/persist-local-complete.ts",
     "src/lib/web-sources/sec-filings.ts",
     "test/pinecone-write-class.test.ts",
     "test/sec-ingest-worker.test.ts"
    ],
    "approach": "(1) src/lib/db.ts: add migration version 85 (after the v84 block ending at :3151, following the exact ALTER-TABLE-guarded pattern used throughout this file) creating `CREATE TABLE IF NOT EXISTS document_chunks_fts_index (content_hash TEXT NOT NULL, symbol TEXT NOT NULL, source TEXT NOT NULL, accession TEXT NOT NULL, fts_rowid INTEGER NOT NULL, PRIMARY KEY (content_hash, symbol, source, accession))` then a one-time backfill `INSERT OR REPLACE INTO document_chunks_fts_index (content_hash, symbol, source, accession, fts_rowid) SELECT content_hash, symbol, source, accession, rowid FROM document_chunks_fts` (a single full pass, run once at migration time \u2014 not a per-request cost). (2) src/lib/db-learning.ts: rewrite insertDocumentChunkFts (:1600-1622) to first `SELECT fts_rowid FROM document_chunks_fts_index WHERE content_hash=? AND symbol=? AND source=? AND accession=?` (an indexed PK lookup), `DELETE FROM document_chunks_fts WHERE rowid = ?` using that fts_rowid when found (FTS5 rowid-delete is fast, unlike a column-value delete), then INSERT the new row, capture `info.lastInsertRowid`, and `INSERT INTO document_chunks_fts_index (...) VALUES (...) ON CONFLICT(content_hash,symbol,source,accession) DO UPDATE SET fts_rowid=excluded.fts_rowid`. Apply the identical lookup+rowid-delete+insert+upsert sequence inside insertDocumentChunkFtsBatch's `runGroup` transaction (:1679-1684), replacing the current `del.run(...)` scan-delete, with the 3 prepared statements hoisted outside the loop like the existing `del`/`ins` statements are today. (3) src/lib/rag/persist-local-complete.ts: change persistLocalComplete (:42-62) to `async`, and replace its `for (const chunk of input.chunks) { insertDocumentChunkFts(...) }` loop with the SAME plan-slice-then-yield-then-check-hasInFlightStrategyWork loop already proven at sec-ingest-worker.ts:456-476 (import planFtsMirrorSlice from '../rag/fts-mirror-bound', insertDocumentChunkFtsBatch + yieldEventLoop from '../db-learning'/'../slow-sync-guard', hasInFlightStrategyWork from '../db-execution'), building `ftsRows` from `input.chunks` first. Update its one caller, src/lib/web-sources/sec-filings.ts:625-632, to `await persistLocalComplete({...})`. (4) src/lib/web-sources/sec-filings.ts:736-756's loop is left structurally as-is (it cannot yield mid-transaction without breaking the 'FTS failures rollback with the vector commit' invariant the comment at :743-744 documents) \u2014 it is fixed entirely by (2), since each per-row DELETE becomes an indexed lookup instead of a scan, so even hundreds of iterations inside one synchronous transaction stay fast regardless of corpus size.",
    "tests": [
     "New assertion in test/sec-ingest-worker.test.ts's 'insertDocumentChunkFtsBatch' describe block (after the existing case at lines 236-261): after inserting rows and re-inserting the SAME (content_hash,symbol,source,accession) key with new text, assert `SELECT COUNT(*) FROM document_chunks_fts_index WHERE content_hash=? AND symbol=? AND source=? AND accession=?` returns exactly 1 row whose fts_rowid matches the CURRENT (post-re-insert) document_chunks_fts row for that key \u2014 MUST FAIL before the fix with 'no such table: document_chunks_fts_index' (the table does not exist yet), PASSES after.",
     "New test proving persistLocalComplete now yields: mock/spy yieldEventLoop (or hasInFlightStrategyWork returning true after the first slice) and call the now-async `await persistLocalComplete({..., chunks: <more than FTS_MIRROR_MAX_CHUNKS_PER_TICK rows>})`; assert yieldEventLoop (or the strategy-gate short-circuit) was invoked at least once and that a second call resumes from where the first left off (same durable-cursor pattern countDocumentChunkFts already provides) \u2014 MUST FAIL before the fix (today's loop is synchronous with no gate and no yield).",
     "test/pinecone-write-class.test.ts:147-158 \u2014 this existing synchronous `it(...)` call to persistLocalComplete MUST be updated to `it(\"...\", async () => { ... await persistLocalComplete({...}); ... })` since the function becomes async; without this update the test would silently race the now-unresolved promise (assertions immediately following would fail non-deterministically) rather than a clean compile/type error \u2014 flagging explicitly so the update lands in the SAME commit as the signature change."
    ],
    "risks": "The document_chunks_fts_index backfill INSERT...SELECT at migration time is itself an O(corpus) full scan \u2014 but it runs exactly ONCE, at deploy/boot, not per-request, so it is in the same cost class as any other migration and acceptable even on a large production corpus (unlike the current per-request cost this whole cluster exists to fix). document-summarizer.ts:466 does its OWN direct bulk `DELETE FROM document_chunks_fts WHERE source=? AND accession=?` (not per-row, not through insertDocumentChunkFts) \u2014 left untouched by this plan; it will leave orphaned document_chunks_fts_index rows for that (source,accession) pair, which is harmless (a stale side-key pointing at a since-deleted fts_rowid just causes a future insertDocumentChunkFts for that exact key to no-op-delete a nonexistent rowid, then correctly insert+upsert) but is worth a one-line comment noting the intentional gap rather than silently forgetting it. document-summarizer.ts:521's own insertDocumentChunkFts loop is left as a direct (non-batched) call site deliberately \u2014 document abstracts are a handful of chunks each, not full filings, so it is not a meaningful event-loop-pin risk and converting it would be scope creep; note this explicitly rather than silently leaving it unmentioned. Verify no other direct `INSERT INTO document_chunks_fts` / `DELETE FROM document_chunks_fts` exists outside the two functions being touched (grepped clean as of this investigation) before landing, since any bypass would desync the new side-key table.",
    "effort": "L"
   }
  },
  {
   "key": "broker-io-deadlines",
   "title": "Broker I/O is unbounded in time and scope, and the protective scheduler lanes latch on it",
   "severity": "P1",
   "surface": "backend",
   "member_uids": [
    "backend-reliability:berel-03",
    "backend-reliability:berel-08",
    "backend-reliability:berel-09",
    "trading-money-path:tsx-05",
    "frontend-performance:dweb-02",
    "backend-reliability:berel-15"
   ],
   "root_cause": "The Alpaca SDK is constructed with no timeout (src/lib/alpaca.ts:247; axios default 0), so getLatestQuotes/createOrder/cancelOrder can hang indefinitely \u2014 and the stop-monitor and stale-exit lanes clear their in-flight key only in .finally (src/lib/scheduler.ts:919/931) with no withDeadline, so one hung socket latches the lane and that account's synthetic stops stop being evaluated forever. The tick itself has no re-entrancy guard (scheduler.ts:411). Separately getEquityOrders asks for status:\"all\", limit 500 with pagination (alpaca.ts:502/508-518) on ~20 call sites including a 15s dashboard poll and two 60s scheduler lanes, and the trade-updates stream has no idle watchdog (src/lib/streams/alpaca-trade-updates-stream.ts:50).",
   "one_change": "Pass a deadline into every Alpaca/Tradier call and wrap each scheduler lane in withDeadline the way src/lib/safety-maintenance.ts:69-78 already does, and scope getEquityOrders (src/lib/alpaca.ts:501) to open orders plus a bounded date window.",
   "effort": "M",
   "tranche": 1,
   "plan": {
    "cluster_key": "broker-io-deadlines",
    "summary": "Partially confirmed, partially superseded by a recent fix (commit d4299bec6, 2026-08-18, this same day per git log) that already added a first-call+retry budget (awaitWithFirstCallRetry/outcomeOrTimeout, src/lib/inflight-deadline.ts) around Alpaca READ calls only: getAccount(s), getPositions, getOrders/getEquityOrders, getEquityQuotes all now bound at 16s+8s. The genuinely still-unbounded gap is the WRITE path: placeEquityOrder/placeOptionOrder's createOrder calls (alpaca.ts:633,737,814) and cancelEquityOrder/cancelBracketSiblingLegs's cancelOrder/sendRequest calls (:862,884,904) go through trackHealth (:256-287) directly with no deadline at all \u2014 trackHealth's own retry logic only fires on a REJECTED promise (isTransientNetworkError), never on one that simply never settles. Separately confirmed: scheduler.ts's stop-monitor (:918-936) and stale-exit (:883-907) in-flight Sets are cleared only in .finally() with no withDeadline wrapper (unlike the near-identical lanes in safety-maintenance.ts:32-78, which already use it); tick() is registered via a bare `setInterval(tick, TICK_MS)` (:411) with no re-entrancy guard; getEquityOrders (:501-546) fetches status:'all' with full pagination and no date bound on ~20 call sites; alpaca-trade-updates-stream.ts has no idle-message watchdog (only onerror/onclose-triggered reconnect).",
    "files_to_change": [
     "src/lib/alpaca.ts",
     "src/lib/scheduler.ts",
     "src/lib/streams/alpaca-trade-updates-stream.ts"
    ],
    "approach": "(1) src/lib/alpaca.ts: import `withDeadline` from './safety-maintenance' (or promote it to a small shared util if that would create a cycle \u2014 safety-maintenance.ts does not import alpaca.ts, so a direct import is safe) and a new exported constant `ALPACA_WRITE_DEADLINE_MS = 20_000` (longer than the 16s read budget, since order placement is not retried in parallel the way reads are). In trackHealth (:256-287), wrap the inner `const result = await fn();` (:262) as `const result = await withDeadline(fn(), ALPACA_WRITE_DEADLINE_MS, \"Alpaca broker call timed out\");` \u2014 this closes the gap for EVERY trackHealth-wrapped call (creates/cancels included) in one place, and is pure defense-in-depth (a strict no-op) for the read call sites that already have their own outer awaitWithFirstCallRetry budget, since that outer race already wins first at 16s < 20s. Do NOT add a parallel-retry for createOrder/cancelOrder (unlike the read paths) \u2014 every createOrder call already carries a deterministic `client_order_id: input.refId` (:640,705,821), so a timeout should surface as a clean rejection and let the EXISTING ambiguous-retry-with-same-refId machinery at the strategy/execution layer (documented on SyntheticTrailingStop.lastAttemptRefId in db-api-keys.ts) decide whether to retry \u2014 firing a second concurrent createOrder from inside trackHealth itself would reintroduce exactly the double-order risk that machinery exists to avoid. (2) src/lib/alpaca.ts, getEquityOrders (:501-546): add a `since` bound to the pagination loop (e.g. stop paginating once a page's oldest `created_at` predates a caller-configurable window, default matching the app's existing stale-order/reconciliation horizons) and change `status: \"all\"` to `status: \"open\"` for the majority of call sites that only need open orders (stop-monitor, stale-exit, pending-fill reconcile) \u2014 audit the ~20 call sites first (grep `getEquityOrders(` across src/lib and app/api) to identify which ones genuinely need historical/all-status orders (e.g. a fills/history report) vs. only open ones, and thread a `{ status?: 'open'|'all', sinceDays?: number }` options param through rather than changing the default silently for every caller. (3) src/lib/scheduler.ts: import `withDeadline, BROKER_TIMEOUT_MS` from './safety-maintenance'. Wrap the stale-exit lane body (the async IIFE at :887-905, specifically the `autoRemediateStaleExitOrders(...)` call inside withAccountMutation) and the stop-monitor lane body (:920-935, the `runSyntheticStopMonitor(...)` call inside withAccountMutation) each in `withDeadline(..., BROKER_TIMEOUT_MS, \"<lane> broker timeout\")`, exactly mirroring safety-maintenance.ts:70-78's existing pattern, so the in-flight Set entry is guaranteed to clear (via the existing `.finally()`) within BROKER_TIMEOUT_MS even if the underlying broker call hangs. Add a module-level `let tickInFlight = false;` guard around tick() (:416+): `if (tickInFlight) return; tickInFlight = true; try { ...existing body... } finally { tickInFlight = false; }` so an overrunning tick (e.g. from the synchronous per-account `await checkBrokerHealth(...)` at :957, which is NOT one of the fire-and-forget `void journalLane(...)` lanes and can itself stall the whole tick) cannot overlap with the next setInterval firing. (4) src/lib/streams/alpaca-trade-updates-stream.ts: add a `lastMessageAt: number` field to StreamState (:23-29), stamp `Date.now()` at the top of `ws.onmessage` (:89+), and add a `setInterval` idle-watchdog (started alongside `connect()`, cleared on close) that force-calls `ws.close()` when `Date.now() - state.lastMessageAt` exceeds a threshold (e.g. 90s \u2014 comfortably above Alpaca's documented heartbeat cadence) \u2014 the existing `ws.onclose` handler (:135+) already drives the backoff-reconnect path, so this only needs to trigger that close, not reimplement reconnect logic.",
    "tests": [
     "New test in test/alpaca-brackets.test.ts (or a new alpaca-broker-deadline.test.ts) using vi.useFakeTimers() per the test/inflight-deadline.test.ts pattern: mock createOrder to `return new Promise(() => undefined)` (never resolves), call placeEquityOrder(...), advance fake time by ALPACA_WRITE_DEADLINE_MS, assert the call rejects with a timeout-shaped error rather than hanging past the test's own timeout \u2014 MUST FAIL/HANG before the fix (trackHealth's await fn() never resolves or rejects today).",
     "Same pattern for cancelEquityOrder mocking cancelOrder as a never-resolving promise \u2014 MUST FAIL/HANG before the fix.",
     "New scheduler test: mock getBrokerGateway's runSyntheticStopMonitor dependency (or the gateway call it makes) to hang; run one tick(); advance fake timers past BROKER_TIMEOUT_MS; assert stopMonitorInFlight no longer contains the account key (i.e. the lane unlatches) \u2014 MUST FAIL before the fix (the Set entry is only ever cleared by .finally(), which never runs on a promise that never settles).",
     "New scheduler re-entrancy test: make tick()'s body await a controllable never-resolving promise (inject via a mocked per-account health check); call startScheduler() (or invoke the tick function directly twice back-to-back before the first resolves); assert the per-account loop body (a spy) was entered only once during the overlap \u2014 MUST FAIL before the fix (setInterval has no guard).",
     "New alpaca-trade-updates-stream test: open the stream with a mock WebSocket, advance fake time past the idle threshold without sending any message, assert ws.close() (or a reconnect) was triggered \u2014 MUST FAIL before the fix (no watchdog exists)."
    ],
    "risks": "trackHealth's deadline must stay ABOVE the read paths' own outer budgets (16s+8s=24s worst case for the retry-extended reads) or it will start firing INSIDE an intentional retry window and break the already-landed 2026-08-18 fix \u2014 size ALPACA_WRITE_DEADLINE_MS carefully and re-run test/inflight-deadline.test.ts's live-max-14s cases after the change to confirm no regression. Narrowing getEquityOrders's default status/window is the highest-blast-radius change in this cluster \u2014 it MUST NOT silently break any caller that relies on seeing filled/canceled/all-status history (grep call sites and audit each one's actual need before changing defaults; prefer an explicit opt-in param over a silent default flip). The scheduler tick re-entrancy guard must not accidentally suppress the FIRST intentional immediate tick at startScheduler() (:409) \u2014 verify with the existing scheduler tests (grep test/*.ts for 'startScheduler'/'tick(' before landing). No persisted-schema changes in this cluster.",
    "effort": "M"
   }
  },
  {
   "key": "ios-state-outcome-truth",
   "title": "iOS shows the previous account's data after sign-out and never surfaces the outcome of a money action",
   "severity": "P1",
   "surface": "ios",
   "member_uids": [
    "ios-engineering:ios-01",
    "ios-engineering:ios-04",
    "ios-engineering:ios-07",
    "ios-engineering:ios-14",
    "ios-engineering:ios-15",
    "ios-hig-ux:ios-01",
    "ios-hig-ux:ios-11",
    "api-contract-web-ios:api-12"
   ],
   "root_cause": "clearLocalSession (ios/SocraticTrade/MobileStore.swift:628-650) nils in-memory state but never removes the UserDefaults blob at :136-144, so a cold launch after sign-out or account deletion re-reads it, sets isAuthenticated from disk, and offline renders the prior account's holdings as current; init also stamps that cached blob lastUpdatedAt = Date() so it shows \"Updated \u00b7 now\". On the write side, store.error has exactly one renderer (SnapshotScaffold, AppComponents.swift:566) which sits at scroll-offset 0 behind the settings sheet and is cleared by load() at MobileStore.swift:292, so a failed guardrail edit is invisible; 401 handling differs per passthrough (:463) and success has no acknowledgement at all.",
   "one_change": "Make one cache/session owner on the Swift side \u2014 clear the UserDefaults snapshot (and move the cookie to the Keychain) inside clearLocalSession, persist the real capture time, and render store.error and success as an .alert/toast anchored to the presenting view instead of a card at scroll-top that load() wipes.",
   "effort": "M",
   "tranche": 1,
   "plan": {
    "cluster_key": "ios-state-outcome-truth",
    "summary": "Confirmed exactly, plus one additional confirmed mechanism. clearLocalSession (ios/SocraticTrade/MobileStore.swift:628-650) nils every in-memory @Published property but never calls UserDefaults.standard.removeObject for the 'cached_mobile_snapshot_data' key (cacheKey, :136); init (:147-154) unconditionally re-reads that blob on cold launch via loadCachedSnapshot(), sets isAuthenticated = (cached != nil) \u2014 true \u2014 and fabricates lastUpdatedAt = Date() rather than the blob's real capture time, so a signed-out/deleted account's stale holdings render as the CURRENT, freshly-loaded state offline. store.error (@Published, single global property) has exactly one renderer \u2014 InlineErrorBanner inside SnapshotScaffold (AppComponents.swift:580-586), which lives on the home view's scroll content at the top \u2014 and is unconditionally cleared by load() on ANY successful reload (MobileStore.swift:292), including background reloads triggered by SSE events (:319-323) unrelated to whatever action set the error; GuardrailEditRows (PolicyTightening.swift:553-627) routes every authority/cap edit through store.submit(), whose only failure surface IS this same global store.error \u2014 invisible while a settings sheet (e.g. GuardrailsView) is presented on top of the home view, and easily wiped by a background poll before the user dismisses the sheet to see it. There is no success acknowledgement path at all for these commands (only per-proposal ProposalActionFeedback exists, and only for proposal.approve/reject).",
    "files_to_change": [
     "ios/SocraticTrade/MobileStore.swift",
     "ios/SocraticTrade/MobileAPIClient.swift",
     "ios/SocraticTrade/PolicyTightening.swift"
    ],
    "approach": "(1) MobileStore.swift, clearLocalSession() (:628-650): add `UserDefaults.standard.removeObject(forKey: Self.cacheKey)` (and a new companion timestamp key, see (2)) alongside the existing in-memory resets, so a cold launch after sign-out/account-deletion has nothing stale to read back. (2) Persist the REAL capture time instead of fabricating it: add `private static let cacheTimestampKey = \"cached_mobile_snapshot_saved_at\"`; in saveCachedSnapshot (:143-145) also write `UserDefaults.standard.set(Date().timeIntervalSince1970, forKey: cacheTimestampKey)` (call this from load()'s success path at :288, same call site); in loadCachedSnapshot (:138-141) also read that companion double back; in init (:147-154) set `lastUpdatedAt = cached == nil ? nil : (real saved timestamp, or nil if the companion key is missing)` instead of `Date()` \u2014 this makes isSnapshotStale(:221-224)'s existing >180s check correctly treat old cached data as stale immediately on cold launch (today it looks artificially fresh for 180s). (3) Add `func hasSessionCookie() -> Bool { HTTPCookieStorage.shared.cookies?.contains(where: ownsCookie) ?? false }` to MobileAPIClient.swift (near ownsCookie, :350-356) \u2014 the session's real authority already lives in the system cookie jar, which clearLocalSession already correctly purges (:633-635); change MobileStore.init's `isAuthenticated = cached != nil` (:151) to `isAuthenticated = previewSnapshot != nil || (cached != nil && client.hasSessionCookie())` so 'we have a cached blob' can no longer stand in for 'this session is actually valid' \u2014 the `previewSnapshot != nil` branch is kept unconditional so ControlCatalogTests.swift's three MobileStore(client:previewSnapshot:) fixtures (which never set up real cookies) are unaffected. (4) Add a view-anchored outcome signal decoupled from the background-load error surface: add `@Published var lastCommandOutcome: CommandOutcome?` to MobileStore (a small new enum: `.success(commandType: String)` / `.failure(commandType: String, message: String)`), set it at both return points of submit() (:436 success, :456 failure) in addition to (not instead of) the existing store.error/proposalNotices bookkeping. In PolicyTightening.swift's GuardrailEditRows (:392+), present `lastCommandOutcome` via `.alert(item:)` anchored to that view itself (visible while the settings sheet is on screen, not hidden behind it), and clear it (`store.lastCommandOutcome = nil`) once shown so it does not resurface after dismiss. Leave store.error / SnapshotScaffold's banner exactly as today for session/load-level errors (401s, network failures) \u2014 this is an ADDITIVE per-action signal, not a replacement, keeping the change scoped.",
    "tests": [
     "New XCTest in ios/SocraticTradeTests (e.g. extend MobileModelsTests.swift or a new MobileStoreSessionTests.swift): seed UserDefaults with a fake cached_mobile_snapshot_data blob for account A, construct MobileStore(client:) (no previewSnapshot, so it reads the real cache), assert snapshot/isAuthenticated reflect A; call clearLocalSession(); construct a FRESH MobileStore(client:) again (simulating cold launch) and assert snapshot == nil and isAuthenticated == false \u2014 MUST FAIL before the fix (the second store re-reads A's stale blob and reports authenticated).",
     "New test: seed the cache blob without the new cacheTimestampKey (simulating data written before this change, or the fresh-write path); after clearLocalSession + a real load() cycle writes both keys; construct a new MobileStore and assert lastUpdatedAt equals the persisted save time (not the construction time) \u2014 assert isSnapshotStale(at: saveTime + 200 seconds) == true \u2014 MUST FAIL before the fix (lastUpdatedAt is always 'now' at init, so staleness can never be detected from a cold-launch cache alone).",
     "New test: construct MobileStore with a cached blob present but NO session cookie in HTTPCookieStorage; assert isAuthenticated == false \u2014 MUST FAIL before the fix (isAuthenticated is derived from cache presence alone). Confirm the existing three ControlCatalogTests.swift call sites (lines 68-71, 84-87, 107-110), which use previewSnapshot:, still assert true / are unaffected.",
     "New test exercising submit()'s two outcome branches directly (mock MobileAPIClient's submit to succeed/fail): assert store.lastCommandOutcome is set to .success/.failure accordingly on each call \u2014 MUST FAIL before the fix (no such property exists)."
    ],
    "risks": "Changing isAuthenticated's derivation could regress the 'offline first paint' UX the cache exists for \u2014 verify SocraticTradeTests/MobileModelsTests.swift:158 and :367's MobileStore(client:) construction sites (no previewSnapshot passed) to see whether they rely on cache-only isAuthenticated=true; if so they will need an explicit cookie stub (there may already be a test-only cookie-seeding helper, or one needs adding as part of this change). This is a Swift/iOS-only change \u2014 CLAUDE.md's iOS build-loop rule applies: `npm run lint`/`tsc`/`npm test`/`npm run build` do NOT compile Swift at all, so `xcodebuild build -project 'ios/Socratic Trade.xcodeproj' -scheme SocraticTrade -destination 'generic/platform=iOS' CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO` (and the `xcodebuild test` variant for the new XCTests) MUST be run before claiming done. No server-side/API contract change is required for this plan (api-contract-web-ios:api-12's concern is addressed client-side by decoupling lastCommandOutcome from store.error, not by changing the mobile snapshot/command response shape) \u2014 confirm during implementation that api-12's actual text doesn't require a server-side field addition; if it does, treat that as a follow-up, not blocking this client-side fix.",
    "effort": "M"
   }
  },
  {
   "key": "home-proposal-rows",
   "title": "Home's latest-run rows use synthetic ids and a private status map: green chips on failures, an Approve button that 404s, no keyboard access",
   "severity": "P1",
   "surface": "desktop-web",
   "member_uids": [
    "desktop-web-ux:dweb-01",
    "desktop-web-ux:dweb-18",
    "web-accessibility:a11y-01"
   ],
   "root_cause": "app/console/page.tsx:79 composes the row id as `${runId}-${symbol}-${status}` while real proposal ids are crypto.randomUUID (src/lib/strategy.ts:3361), so handleApprove always 404s at app/api/proposals/[id]/approve/route.ts:26 \u2014 and page.tsx:1011 offers that button on 'blocked' rows, a routine strategy outcome. page.tsx:1025's local chipTone knows only 'blocked'/'failed'/'pending', so the real statuses 'error' and 'proposed' fall through to green while labels.ts renders the word \"Failed\". The row itself is a click-only div (page.tsx:1029-1030) with no role, tabIndex or key handler, so the details sheet and its Approve flow are unreachable by keyboard.",
   "one_change": "Render those rows from the persisted proposals (real ids) through the shared helpers in app/console/lib/action-verbs.ts / labels.ts, and make the row a keyboard-operable control with the nested SymbolButton excluded from the activation target.",
   "effort": "M",
   "tranche": 1,
   "plan": {
    "cluster_key": "home-proposal-rows",
    "summary": "Home's 'Latest strategy run' / 'Previous Trades' rows are built with synthetic/wrong-id-space ids that make Approve either 404 or hit the wrong DB row, the chip-tone logic doesn't know about the real 'error'/'proposed' statuses so they render green like a fill, the `??` fallback never fires because latest.proposals can be an empty (not missing) array, and the row is a plain onClick div with zero keyboard access. Root cause of the id bug: strategy.ts already mints a real `crypto.randomUUID()` proposalId and inserts it into trade_proposals for every proposal, but never returns that id back on the trace item pushed into `results` (dashboard-types.ts StrategyDecision.proposals[] has no `id` field), so the UI has no real id to approve against for 'Latest strategy run' rows, and 'Previous Trades' rows use a completely different id-space (SocraticDecisionCase.id, not the proposal's trade_proposals id) for the exact same Approve button.",
    "files_to_change": [
     "src/lib/strategy.ts",
     "app/dashboard-types.ts",
     "src/lib/dashboard-feed.ts",
     "app/console/page.tsx"
    ],
    "approach": "1) src/lib/strategy.ts: inside the `for (const proposal of proposals)` loop (starts ~line 3356, `const proposalId = crypto.randomUUID()` declared right after entry), add `id: proposalId` to the object literal at every `results.push({...})` call site in that loop (22 sites: lines 3382, 3431, 3667, 3689, 3746, 3767, 3788, 3822, 3866, 3924, 3976, 4012, 4036, 4074, 4087, 4099, 4112, 4131, 4152, 4199, 4208, 4229 \u2014 verified `proposalId` is in lexical scope at every one, including inside the async mutation-lease closure). 2) app/dashboard-types.ts `StrategyDecision.proposals` (line 49) and src/lib/dashboard-feed.ts `StrategyDecisionLike.proposals` (line 41): add `id?: string` to the array element type (optional \u2014 legacy persisted `strategy_run` audit rows predate this field). `auditBoundedStrategyRunResult` (audit-bounded-run.ts) spreads `...result` untouched except marketScan, so the new field flows through to the persisted audit payload and back out via `latestStrategyRun.proposals[].id` with no other change needed. 3) app/console/page.tsx: in `decisionFromProposal` (line 774) add a 6th param `approvable: boolean = false` and set it on the returned `DecisionRowData` (add `approvable?: boolean` to the type at line 588). In `decisionFromPending` (789) pass `approvable: true` (it already carries a real `pending.id`). In `decisionFromSocratic` (754) explicitly pass/set `approvable: false` \u2014 'Previous Trades' rows must never expose Approve since `decision.id` is a SocraticDecisionCase id, not a trade_proposals id (the exact same 404/wrong-row bug class, currently reachable whenever a previousTrades row's status is 'blocked'). Rewrite the `latestProposals` derivation (lines 77-80): build the trace-derived array into a local const first (`.map` over `latest?.proposals?.slice(0,5)` using `item.id ?? \\`${latest?.runId}-${item.proposal.symbol}-${item.status}\\`` as the row id, and `Boolean(item.id) && item.status === 'proposed'` as `approvable`), then fall back to the pendingProposals-derived array only when that local array's `.length === 0` (not via `??`, which only catches null/undefined and lets an empty array through, hiding older still-pending proposals). Fix `chipTone` (line 1025) to `isExecutedStatus(row.status) || row.status === 'placed' ? 'pos' : isNotPlacedStatus(row.status) || row.status === 'error' ? 'warn' : 'accent'` using the already-imported `isExecutedStatus`/`isNotPlacedStatus` from `./lib/action-verbs` (line 46) instead of the current ad hoc string checks that miss 'error' and 'proposed'. Replace `isPendingOrFailed` (line 1011) and its use at 1071 with `row.approvable === true` so the Approve panel only ever renders for a row with a genuine, currently-'proposed' DB id. Accessibility (dweb-18/a11y-01): leave the outer row `<div>` (1029) with its pointer `onClick` as a mouse convenience, but change the 'View details' text span (1039-1041) into a real `<button type=\"button\" onClick={() => setOpen(true)}>` with `aria-label={\\`View details for ${row.symbol}, ${combinedStatus}\\`}` \u2014 do NOT convert the outer div into a `<button>`/role=button, since it nests `SymbolButton` (ui/symbol-drilldown.tsx:548), itself a real `<button>` with its own `e.stopPropagation()` (line 578); nested interactive buttons are invalid HTML and would double-fire.",
    "tests": [
     "test/e2e-money-path.test.ts \u2014 extend the existing 'runs strategy through to execution' test: after `const aaplProposal = result.proposals.find(...)`, add `expect(aaplProposal?.id).toEqual(expect.any(String))` and, using the already-imported db layer, `expect(getProposal(aaplProposal!.id!, 'local')?.proposal.symbol).toBe('AAPL')` \u2014 FAILS today (`aaplProposal.id` is `undefined`, `getProposal(undefined, ...)` returns null) and PASSES once `id: proposalId` is threaded through every results.push.",
     "test/console-action-rows.test.ts \u2014 add a new describe for a small exported pure helper (extract the chip-tone ternary above into an exported `proposalChipTone(status: string): 'pos'|'warn'|'accent'` in app/console/lib/action-verbs.ts so it's unit-testable outside page.tsx): assert `proposalChipTone('error') === 'warn'` and `proposalChipTone('proposed') === 'accent'` \u2014 both currently resolve to 'pos' via page.tsx's inline ternary, i.e. the existing behavior (uninverted) would fail these two new assertions before the fix.",
     "new test (React Testing Library, colocate as test/console-proposal-row-a11y.test.tsx if one doesn't already exist for page.tsx internals) \u2014 render a latestProposals-derived ProposalRow with status 'blocked' and assert no 'Approve Proposal' button is present (before the fix, `isPendingOrFailed` is true for 'blocked' so the button renders); also assert the 'View details' element has `role='button'`/is a real `<button>` reachable via `Tab` \u2014 fails today since it's a bare `<div onClick>` with the button-hunt failing to find a focusable element by role."
    ],
    "risks": "The 22-site strategy.ts edit is mechanical but high blast-radius by file (the most sensitive module in the repo) \u2014 run the full E2E money-path + strategy-* suite, not just the one new assertion, since any missed/mistyped site would leave that one proposal's trace item without an id (degrading gracefully to `approvable:false`, not a crash, but worth catching in review). Grep `test/*.ts` for `.proposals[0].status` / `.proposals.find` before landing to confirm no existing test snapshot-matches the full proposals[] object shape (an added `id` field would break an exact-equality assertion, not a `.toMatchObject`). The `decisionFromSocratic` `approvable:false` change is a real, if small, behavior change beyond the review's literal 'Home' framing \u2014 flag it in the PR description since it also touches the 'Previous Trades' section on the same page. No persisted-data migration is needed (the new `id` field is additive/optional; old audit rows without it simply keep `approvable:false` until the account's next strategy run).",
    "effort": "M"
   }
  },
  {
   "key": "console-ships-too-much",
   "title": "The console ships server DB internals to the browser and the whole universe on every 15s poll",
   "severity": "P1",
   "surface": "desktop-web",
   "member_uids": [
    "frontend-performance:dweb-01",
    "frontend-performance:dweb-03",
    "frontend-performance:dweb-04",
    "frontend-performance:dweb-05",
    "frontend-performance:dweb-15",
    "api-contract-web-ios:api-04",
    "api-contract-web-ios:api-15"
   ],
   "root_cause": "app/console/settings/brokers.tsx:12 value-imports @/lib/venue-contract, which pulls source-settings \u2192 db-api-keys \u2192 db into the client bundle: SQL schema, migrations and key-store logic reach the browser, and this exact chain produced the 2026-08-18 'process.uptime is not a function' production crash (fixed symptomatically in 01f4e73e2, whose own comment names the chain). No module in the repo imports the server-only guard. On the payload side src/lib/dashboard.ts:1108 serializes raw audit[], the full 500-symbol quotesBySymbol and the entire order history into a 15s poll (app/console/lib/useConsoleData.tsx:23), and post-action refreshes are served from the 10s cache so acked alerts stay on screen.",
   "one_change": "Add server-only to the src/lib/db* modules and split the pure mergeAccountCapabilities helper out of venue-contract so brokers.tsx no longer drags the DB layer, then project the snapshot at src/lib/dashboard.ts:1108 down to the fields the UI actually reads and bust the cache on mutation.",
   "effort": "M",
   "tranche": 1,
   "plan": {
    "cluster_key": "console-ships-too-much",
    "summary": "Two independent problems share one 'the console ships too much to the browser' theme. (1) app/console/settings/brokers.tsx value-imports `mergeAccountCapabilities` from src/lib/venue-contract.ts, and because that module's TOP-LEVEL import is `resolveSourceBool` from source-settings.ts -> db-api-keys.ts/db-settings.ts -> the db.ts barrel (better-sqlite3, SQL schema, migrations, key-store), the WHOLE db layer's JS compiles into the client bundle even though `mergeAccountCapabilities` itself never calls `resolveSourceBool` (only the separate `deriveVenueContract` does, in the same file) \u2014 this is exactly the import chain that produced the 2026-08-18 'process.uptime is not a function' production crash (fixed only symptomatically in 01f4e73e2, which guarded one call site but left the root chain intact). Verified: grepping every '\"use client\"' file in app/ + src/ for value-imports of @/lib/db*, @/lib/source-settings, @/lib/server-knobs, @/lib/venue-contract turns up exactly brokers.tsx (real) and app/console/lib/derive.ts (a `import type` of PositionStopPlan \u2014 erased at compile time, not a real offender). (2) src/lib/dashboard.ts ships an unused raw `audit: clientAudit` array (100 full-payload rows, confirmed zero consumers of `snapshot.audit` anywhere in app/console or ios/ via grep) and an unfiltered `latestScan.quotesBySymbol` covering the whole scanned universe on every 15s poll; app/api/scan/route.ts unconditionally re-audits the full scan even on a cache hit (`scan.cached === true`); and four mutating routes (notifications/ack, orders/cancel, orders/replace-market, plus strategy/run|pause|enable and connected-accounts/*/activate) never call `invalidateDashboardSnapshotCache`, so the client's post-mutation `refresh()` can replay the pre-mutation snapshot for up to the 10s TTL.",
    "files_to_change": [
     "src/lib/venue-contract.ts (split)",
     "src/lib/venue-contract-pure.ts (new)",
     "app/console/settings/brokers.tsx",
     "src/lib/db.ts",
     "src/lib/db-settings.ts",
     "src/lib/db-api-keys.ts",
     "src/lib/dashboard.ts",
     "app/dashboard-types.ts",
     "app/api/scan/route.ts",
     "app/api/notifications/ack/route.ts",
     "src/lib/order-cancel.ts",
     "app/api/orders/replace-market/route.ts",
     "package.json"
    ],
    "approach": "Bundle boundary (P1, dweb-01): `npm install server-only` (Vercel's zero-runtime marker package; not currently a dependency \u2014 `npm ls server-only` returns empty). Add `import \"server-only\";` as the first import in src/lib/db.ts, src/lib/db-settings.ts, src/lib/db-api-keys.ts (server-only's package.json `browser` export throws when a client bundle tries to include it, turning any FUTURE accidental client import into a build-time error instead of a silent 1MB+ leak). Then split venue-contract.ts: create src/lib/venue-contract-pure.ts containing everything that does NOT touch resolveSourceBool \u2014 `knownBrokerLimits` (line 73), `emptyCapabilities`, `mergeAccountCapabilities` (line 195), `brokerDisplayLabel` (line 219), `resolveSessions`, `buildPromptLines`, and the `VenueContract`/capability types \u2014 re-exported from venue-contract.ts for existing server-side importers so nothing else breaks, and change brokers.tsx:12 to `import { mergeAccountCapabilities } from \"@/lib/venue-contract-pure\"` instead. Leave `deriveVenueContract` (the ONLY function that calls `resolveSourceBool`, at line 250) in venue-contract.ts unchanged. Verify with `next build` + `grep -c 'getDb\\|SELECT ' .next/static/chunks/app/console/connections/page-*.js` (should go to 0). Payload/cache (P2, dweb-03/04/05): in src/lib/dashboard.ts drop the `clientAudit` const and the `audit: clientAudit,` line from the returned snapshot object (~line 1108) \u2014 `unifiedFeed`/`auditFeed` (built earlier in the same function from the local `audit` array) are untouched, only the raw top-level field is removed; change `DashboardSnapshot.audit` (app/dashboard-types.ts) from `AuditEvent[]` to `audit?: AuditEvent[]` (additive/optional, matching every other backward-compat field in this type \u2014 test fixtures that still set `audit: []` remain valid). Trim `newestScan.quotesBySymbol` (dashboard.ts ~line 841-845) to the union of `topCandidates` symbols plus every symbol referenced by `positions`/`orders`/`pendingProposals` \u2014 NOT to just held/order symbols, since app/console/ui/symbol-drilldown.tsx and orders/lib.ts both fall back to `quotesBySymbol` by normalized-symbol lookup for arbitrary candidate rows too; keep every candidate's own quote to avoid degrading those drilldowns. app/api/scan/route.ts (~line 116): wrap the unconditional `audit(\"market_scan\", { scan }, ...)` in `if (!scan.cached) { ... }` \u2014 `MarketScan.cached?: boolean` (types.ts:2089) is already stamped by market.ts:729 on every cache hit; confirm in market.ts that `scanMarket`'s cache path actually sets it before relying on it. Cache invalidation: add `invalidateDashboardSnapshotCache(userId);` to app/api/notifications/ack/route.ts (both the `body.all===true` and the specific-`ids` branch, right before each `return NextResponse.json(...)`), and inside `cancelWorkingOrder` in src/lib/order-cancel.ts right after the existing `emitDashboardEvent(...)` call (~line 192) \u2014 this single insertion point also fixes the mobile `order.cancel` command path for free since mobile-api.ts:992 calls the same function. For app/api/orders/replace-market/route.ts, add the same call at the route layer (before `return NextResponse.json(result);` ~line 76) rather than inside `replaceStaleLimitOrderWithMarket` (order-replacement.ts), which has many early-return/throw paths and only one true success path not worth the extra risk of tracing every branch. Note but do not implement in this pass (same mechanical one-liner, lower urgency): strategy/run, strategy/pause, strategy/enable, connected-accounts/*/activate also lack the call.",
    "tests": [
     "new test/venue-contract-pure-boundary.test.ts \u2014 `import { mergeAccountCapabilities } from '../src/lib/venue-contract-pure'` and assert it resolves with no error and produces the same output as an existing test/venue-contract.test.ts case (e.g. 'Tradier is whole-share, no trail, no option orders') \u2014 this doesn't directly prove the bundle boundary (that needs a real `next build` + chunk grep, not a unit test) but pins the pure module's behavior parity with the pre-split function.",
     "test/dashboard-snapshot-cache.test.ts or a new test/dashboard-audit-projection.test.ts \u2014 call `computeDashboardSnapshot`/`getDashboardSnapshot` against a seeded temp DB with at least one `market_scan` audit row present, and assert `snapshot.audit` is `undefined` \u2014 FAILS today (returns the 100-row clientAudit array) and PASSES after the field is dropped.",
     "new test/notifications-ack-cache-invalidation.test.ts \u2014 seed the dashboard snapshot cache via `setCachedDashboardSnapshot`, POST to the ack route's handler, then assert `getCachedDashboardSnapshot(key)` is `undefined` \u2014 fails today (no invalidation call), passes after."
    ],
    "risks": "The server-only guard is the highest-leverage, lowest-risk part of this cluster (a genuinely additive dependency + a compile-time tripwire) \u2014 land it first/separately if splitting the PR. The `quotesBySymbol` trim is the riskiest single edit: verify against app/console/ui/symbol-drilldown.tsx's `resolveQuote` fallback (line 267) and orders/page.tsx:279 with a manual click-through on a symbol that is NOT held/ordered/pending but IS a topCandidate, to confirm its quote still resolves post-trim. Dropping `snapshot.audit` is safe per an exhaustive `grep -rn 'snapshot.audit'` across app/ and ios/ (zero hits) but re-run that grep right before landing in case another agent added a consumer in the interim. The scan-audit cache-skip depends on `scan.cached` being reliably true on every genuine cache hit \u2014 trace `scanMarket`'s cache path (market.ts ~line 320-402) once more before wiring the skip, since a false-negative there would just keep today's (already-accepted) over-auditing, but a false-positive would silently stop auditing a scan that was actually fresh.",
    "effort": "M"
   }
  },
  {
   "key": "merge-gate-blindspots",
   "title": "The required merge gate never compiles or runs the iOS app, and can be skipped entirely for build-imported files",
   "severity": "P1",
   "surface": "cross-surface",
   "member_uids": [
    "qa-test-strategy:qa-01",
    "qa-test-strategy:qa-02",
    "qa-test-strategy:qa-03",
    "qa-test-strategy:qa-11",
    "ios-engineering:ios-09"
   ],
   "root_cause": ".github/workflows/ci.yml:305's required `verify` job depends only on classify + verify-hosted and never on ios-build.yml, which itself only runs `xcodebuild build` (.github/workflows/ios-build.yml:70-78) \u2014 `xcodebuild test` appears nowhere in .github/ or scripts/, so 15 XCTest files covering order-cancel and price-review logic never execute before TestFlight. ci.yml:141's classify regex treats all of docs/** as documentation even though app/api/llm-usage/model-stats/route.ts:9/19 imports docs/benchmarks/*.json at build time, so such an edit auto-merges with tsc/test/build skipped and breaks first in the production deploy. scripts/land.sh:156 runs the same trio and never compiles Swift.",
   "one_change": "Add an `xcodebuild test` job, make the Swift job a required context in the main-protection ruleset next to `verify`, and exclude build-imported paths from the docs-only regex at ci.yml:141 (mirrored in e2e.yml:77 and scripts/land.sh:156).",
   "effort": "M",
   "tranche": 2,
   "plan": null
  },
  {
   "key": "web-ios-contract-drift",
   "title": "Hand-mirrored Swift decoders drift from route payloads with no contract test \u2014 live stop-losses render as em-dashes",
   "severity": "P1",
   "surface": "cross-surface",
   "member_uids": [
    "api-contract-web-ios:api-01",
    "api-contract-web-ios:api-06",
    "api-contract-web-ios:api-13",
    "api-contract-web-ios:api-14",
    "api-contract-web-ios:api-16",
    "qa-test-strategy:qa-04",
    "qa-test-strategy:qa-18",
    "performance-accounting:perf-13",
    "ios-hig-ux:ios-05",
    "api-contract-web-ios:api-11",
    "api-contract-web-ios:api-17"
   ],
   "root_cause": "FullPolicy decodes stopLossPct/trailingStopPct/shortStopLossPct from top-level CodingKeys (ios/SocraticTrade/DeskModels.swift:383-388) while GET /api/policy returns them nested under riskRules (src/lib/types.ts:1188, route at app/api/policy/route.ts:42), so every iPhone Guardrails view shows em-dashes for a configured 8% stop. Nothing ties the two sides together: app/api/mobile/snapshot/route.ts:16-54 is a bare object literal, MobileModels.swift:41-67 decodes every list with `?? []` so a rename blanks a screen silently, and the only Swift decode test is a hand-authored fixture (MobileModelsTests.swift:409). The same class drops recordedAt, benchmarkUnavailable and benchmarkSymbol.",
   "one_change": "Emit a shared fixture from the real routes (snapshot + policy JSON) and decode it in both `npm test` and the XCTest suite, failing on absent or unknown keys \u2014 starting by flattening riskRules in app/api/policy/route.ts:42 or decoding the nested container in DeskModels.swift:372.",
   "effort": "M",
   "tranche": 1,
   "plan": {
    "cluster_key": "web-ios-contract-drift",
    "summary": "Confirmed, concrete, high-value bug: GET /api/policy returns the raw TradingPolicy object, where stopLossPct/trailingStopPct/shortStopLossPct live under `riskRules` (src/lib/types.ts:713-731, RiskRules interface); but ios/SocraticTrade/DeskModels.swift's `FullPolicy.init(from:)` (lines 372-386) decodes those three fields from a TOP-LEVEL CodingKeys container that has no such key in the real payload, so `decodeIfPresent` silently returns nil for every account, every time \u2014 GuardrailsView.swift:96-98 renders them via `DeskCopy.percentPoints` (DeskModels.swift:683-686), which prints '\u2014' for nil. This is a hand-authored Swift decoder with zero enforcement that it matches the real route shape; MobileModelsTests.swift:409's `fullSnapshotJSON` is itself a hand-typed string literal, not derived from the actual route, so nothing catches this class of drift before it ships.",
    "files_to_change": [
     "ios/SocraticTrade/DeskModels.swift",
     "ios/SocraticTradeTests/DeskModelsTests.swift (new or extend existing)",
     "test/policy-ios-contract-fixture.test.ts (new)",
     "ios/SocraticTradeTests/Fixtures/policy-contract.json (new, generated+checked in)"
    ],
    "approach": "Immediate fix (do NOT flatten the API \u2014 `riskRules` is a real domain grouping read by dozens of TS consumers; changing the wire shape is far more invasive/risky than fixing the one hand-written Swift decoder that's wrong): in ios/SocraticTrade/DeskModels.swift `FullPolicy`, add a nested `RiskRulesKeys: String, CodingKey { case stopLossPct, trailingStopPct, shortStopLossPct }` and a top-level `CodingKeys` case `riskRules`. In `init(from decoder:)`, replace the three flat `values.decodeIfPresent(Double.self, forKey: .stopLossPct)` etc. lines (374-376) with: decode an optional nested container via `try? values.nestedContainer(keyedBy: RiskRulesKeys.self, forKey: .riskRules)`, then `try riskRulesValues?.decodeIfPresent(Double.self, forKey: .stopLossPct)` (and same for the other two) \u2014 falling back to nil (not throwing) when `riskRules` itself is absent, matching every other field's `decodeIfPresent` optionality convention in this struct. Contract-test infrastructure (systemic fix, the cluster's real ask): add a small generator test in test/policy-ios-contract-fixture.test.ts that calls `GET /api/policy`'s underlying `getPolicy(userId)` (or invokes the route handler directly against a seeded temp DB with a known TradingPolicy including a non-default riskRules), writes the resulting JSON to ios/SocraticTradeTests/Fixtures/policy-contract.json, and asserts specific known fields (stopLossPct etc.) are present at `.riskRules.stopLossPct` in the raw JSON (a cheap, direct pin of the wire shape). Add a new XCTest (ios/SocraticTradeTests/DeskModelsTests.swift, new file if one doesn't exist for FullPolicy) that loads that checked-in fixture as a test-bundle resource and decodes it with the REAL `FullPolicy` type, asserting `policy.stopLossPct == <the fixture's known value>` (not just 'decode doesn't throw' \u2014 decodeIfPresent never throws on a missing key, so a pure 'no throw' test would have passed even on the broken decoder). CI wiring (document, don't necessarily build in this pass): a `npm run generate:ios-fixtures` step that re-runs the generator and fails CI if the checked-in JSON differs from freshly generated output, catching route-shape drift the moment it happens rather than only in a future manual audit.",
    "tests": [
     "ios/SocraticTradeTests/DeskModelsTests.swift \u2014 `testFullPolicyDecodesNestedRiskRules()`: decode the checked-in policy-contract.json fixture (or an inline JSON literal with `\"riskRules\":{\"stopLossPct\":8,\"trailingStopPct\":3,\"shortStopLossPct\":5}`) into `FullPolicy` and assert `policy.stopLossPct == 8`, `policy.trailingStopPct == 3`, `policy.shortStopLossPct == 5` \u2014 FAILS today (all three decode to nil) and PASSES after the nested-container fix.",
     "test/policy-ios-contract-fixture.test.ts \u2014 assert the generated fixture JSON's `.riskRules.stopLossPct` matches the seeded policy's configured value (pins the SERVER side of the contract so a future accidental flatten/rename is caught on the TS side too)."
    ],
    "risks": "Low risk \u2014 the Swift change is purely additive/local to one struct's decode, doesn't touch any TS/route code, and every other FullPolicy field's optionality is unchanged. The only thing to verify by hand (no automated Xcode build in this repo's normal gate \u2014 CLAUDE.md flags this explicitly) is that `xcodebuild build`/`xcodebuild test` for the SocraticTrade scheme still passes, since this is the one class of change local `tsc`/vitest/`next build` cannot catch. The other 10 members of this cluster (MobileModels.swift's `?? []` blanking-on-rename pattern across many arrays, a separate `benchmarkUnavailable`/`recordedAt` drop on the symbol-desk decode path, per-field XCTest coverage beyond FullPolicy) are real but were not individually re-derived here for time; they are exactly the class of bug the new fixture-based contract test is designed to catch going forward \u2014 recommend adding a fixture+decode-assertion pair for `/api/mobile/snapshot` next, following the same pattern established here, as a fast-follow rather than bundling it into this already-large fix.",
    "effort": "M"
   }
  },
  {
   "key": "per-account-visibility",
   "title": "Multi-account screens show the active account's data labeled as every account's",
   "severity": "P1",
   "surface": "desktop-web",
   "member_uids": [
    "lifecycle-02",
    "lifecycle-12",
    "desktop-web-ux:dweb-12",
    "pages-04",
    "api-contract-web-ios:api-10"
   ],
   "root_cause": "app/console/settings/brokers.tsx:189 computes stateInfo only when isCurrentLoaded, so every other connected account chips a static \"Inactive\" even while it is genuinely running Autopilot (the scheduler runs each account independently, src/lib/scheduler.ts:810); brokers.tsx:191 then filters snapshot.pendingProposals, which src/lib/dashboard.ts:690 already scoped to the active account, so the per-account pending count is structurally always zero. app/console/decisions/page.tsx:28 fetches /api/socratic/decisions with no connectedAccountId although the route supports it, interleaving both accounts' BUY/SELL traces with no per-row label.",
   "one_change": "Feed every per-account row from snapshot.connectedAccountPolicies (already built at src/lib/dashboard.ts:350-368 and already used by chrome.tsx:141) plus a real per-account pending count, and scope /console/decisions by connectedAccountId with a visible account chip per row.",
   "effort": "M",
   "tranche": 1,
   "plan": {
    "cluster_key": "per-account-visibility",
    "summary": "Verified: app/console/settings/brokers.tsx's `renderAccountRow` (line 189) computes `stateInfo` only `isCurrentLoaded && snapshot?.policy ? deriveStateInfo(snapshot.policy) : null` \u2014 every OTHER connected account always renders a static null-state chip even while genuinely running Autopilot, despite the snapshot already carrying a real per-account projection (`snapshot.connectedAccountPolicies`, built read-only via `peekPolicy(userId, account.id)` in src/lib/dashboard.ts:350-368, and ALREADY correctly consumed this exact way by app/console/components/chrome.tsx:140-141's account-switcher). The `pendingCount` at line 191 filters `snapshot.pendingProposals`, which src/lib/dashboard.ts:690 (`listPendingProposals(accountNumber, userId)` where `accountNumber = policy.accountNumber`, the ACTIVE account) already scopes to the active account only \u2014 so the non-active-account filter branch is structurally dead code, always returning 0. Separately, app/console/decisions/page.tsx:28 fetches `/api/socratic/decisions?limit=100` with no `connectedAccountId`, even though the route (app/api/socratic/decisions/route.ts) already supports that param \u2014 interleaving every account's decisions with zero per-row account label in decisions-list.tsx.",
    "files_to_change": [
     "app/console/settings/brokers.tsx",
     "src/lib/dashboard.ts",
     "src/lib/db-proposals.ts",
     "app/dashboard-types.ts",
     "app/console/decisions/page.tsx",
     "app/console/decisions/decisions-list.tsx"
    ],
    "approach": "brokers.tsx: replace the `isCurrentLoaded ? deriveStateInfo(snapshot.policy) : null` line (189) with `const policyForAccount = snapshot.connectedAccountPolicies?.[account.id]; const stateInfo = policyForAccount ? deriveStateInfo(policyForAccount) : null;` \u2014 this is the exact pattern already proven correct in chrome.tsx:140-141, applied uniformly to every account row (active or not). For pendingCount: add a lightweight `countPendingProposals(accountNumber: string, userId: string): number` to src/lib/db-proposals.ts (`SELECT COUNT(*) as n FROM trade_proposals WHERE account_number = ? AND user_id = ? AND status = 'proposed'` \u2014 deliberately a COUNT query, not `listPendingProposals`, to avoid parsing full proposal/decision JSON blobs for every connected account on every snapshot). In src/lib/dashboard.ts, alongside the existing `connectedAccountPolicies` construction (~line 350-368), add a parallel `connectedAccountPendingCounts = Object.fromEntries(connectedAccounts.map((account) => [account.id, account.accountNumber ? countPendingProposals(account.accountNumber, userId) : 0]))` and include it in the returned snapshot object; add `connectedAccountPendingCounts?: Record<string, number>` to `DashboardSnapshot` (app/dashboard-types.ts, additive/optional). In brokers.tsx, replace the whole `pendingCount = isCurrentLoaded ? ... : ...filter(...)` block (lines 191-194) with a single `const pendingCount = snapshot.connectedAccountPendingCounts?.[account.id] ?? 0;` \u2014 correct for every account uniformly, no special-casing. For /console/decisions: convert app/console/decisions/page.tsx from its bare `fetch` to `useConsoleData()` (matching every other console page) so it has `snapshot.connectedAccounts` for an id->label map (`Object.fromEntries(snapshot.connectedAccounts.map(a => [a.id, a.label || a.broker]))`) and `snapshot.policy.connectedAccountId` for the active account; pass the label map as a new optional prop to `DecisionsList` (decisions-list.tsx) which renders a small `Chip`/faint-text account label next to each row's status chip ONLY when `snapshot.connectedAccounts.length > 1` (avoid clutter for the common single-account case), using `decision.connectedAccountId` to look up the label. Keep the current all-accounts-interleaved fetch (don't silently narrow to the active account only \u2014 that would be a functional regression, not requested); the chip alone fixes the misattribution risk the finding raises.",
    "tests": [
     "new test/db-proposals-count.test.ts \u2014 seed two proposed-status trade_proposals rows for account A and zero for account B, assert `countPendingProposals('A', 'local') === 2` and `countPendingProposals('B', 'local') === 0` \u2014 new function, no prior test, straightforward pin.",
     "new test (or extend an existing dashboard.test.ts) \u2014 build a snapshot with two connected accounts where only the INACTIVE one has a proposed proposal, assert `snapshot.connectedAccountPendingCounts[<inactive account id>] === 1` \u2014 FAILS today (that count doesn't exist in the snapshot at all) and PASSES after.",
     "new React test for decisions-list.tsx \u2014 render `DecisionsList` with `decisions` spanning two different `connectedAccountId`s and an `accountLabelById` map with 2 entries, assert both account labels are visible in the rendered output \u2014 fails today (component renders no account text at all)."
    ],
    "risks": "No existing test directly exercises brokers.tsx's `renderAccountRow` (grepped test/*.ts and test/*.tsx for brokers.tsx/renderAccountRow \u2014 zero hits), so this is a clean fix with no pinned-behavior regression risk on that front. `countPendingProposals` is a new, narrowly-scoped read-only query \u2014 no migration concern. The decisions/page.tsx switch from bare fetch to `useConsoleData()` changes its loading/polling behavior (picks up the 15s snapshot poll instead of a one-shot fetch-on-mount) \u2014 verify this doesn't introduce an unwanted extra network waterfall or change the page's existing loading-state UX noticeably; if that's undesirable, an alternative is a second, smaller fetch of just `snapshot.connectedAccounts` via a lighter endpoint instead of the full `useConsoleData()` hook.",
    "effort": "M"
   }
  },
  {
   "key": "coach-tools-and-turns",
   "title": "Coach coerces invalid tool inputs instead of rejecting them, and cannot be cancelled mid-call",
   "severity": "P1",
   "surface": "cross-surface",
   "member_uids": [
    "coach-01",
    "coach-02",
    "coach-07",
    "coach-08",
    "coach-09",
    "coach-10",
    "coach-11",
    "api-contract-web-ios:api-08"
   ],
   "root_cause": "src/lib/chat/tools.ts:92 maps any side that is not exactly \"sell\" \u2014 \"Sell\", \"short\", a typo \u2014 to \"buy\", the opposite economic direction, and :94/96 silently drops a limit price the same way; downstream promote-draft only re-checks that side is buy|sell, which is trivially true after coercion. qty carries no unit and there is no notional field (tools.ts:58), and k has no maximum (tools.ts:155) unlike its clamped siblings. Separately no transport receives args.abortSignal (src/lib/chat/llm.ts:332-346, :688-705), so Cancel and the 120s deadline cannot stop an in-flight provider call, and exhausting the 5-step loop returns only the disclaimer (llm.ts:436-459).",
   "one_change": "Fail closed on out-of-enum tool inputs and clamp/convert numerics server-side in src/lib/chat/tools.ts (mirroring normalizeAlertOp's fail-closed shape), and thread args.abortSignal into every transport in src/lib/chat/llm.ts so cancel and the turn deadline actually abort.",
   "effort": "M",
   "tranche": 1,
   "plan": {
    "cluster_key": "coach-tools-and-turns",
    "summary": "Two independent, both real, money-adjacent bugs plus one correctness gap. (1) src/lib/chat/tools.ts's `draft_order.execute` (line 92) does `input.side === \"sell\" ? \"sell\" : \"buy\"` \u2014 ANY non-exact-'sell' value (case variant, 'short', a typo, missing) silently becomes 'buy', the OPPOSITE economic direction; downstream `chatDraftToProposal` (src/lib/chat/promote-draft.ts:16) only re-validates `draft.side === 'buy' || 'sell'`, which is trivially true after the coercion already happened, so it can never catch this. Line 94's `order_type` has the identical bug shape: any non-exact-'limit' value silently becomes 'market', and because line 96's `limit_usd` ternary keys off that same coerced `order_type`, a legitimate 'limit $150' instruction with a typo'd order_type silently loses the price and becomes a plain market order. `k` (kb_search, line 155) has a `minimum` but no `maximum` and is passed through unclamped. (2) src/lib/chat/llm.ts's `AnthropicLLM`/`OpenAILLM.run()` only check `args.abortSignal?.aborted` BETWEEN steps (lines 395, 537) \u2014 the injectable `Transport`/`OpenAITransport` function types (line 330, 464) never receive the signal, so `defaultTransport`/`defaultOpenAITransport`/`makeOpenAITransport`'s `llmFetch(...)` calls (lines 333, 468, 694) never pass `signal`, meaning a user's explicit Cancel (app/api/chat/route.ts:172's `handle.controller.signal`, aborted via `cancelChatTurn` in turn-registry.ts) has ZERO effect on an in-flight provider call \u2014 confirmed by tracing the whole call chain.",
    "files_to_change": [
     "src/lib/chat/tools.ts",
     "src/lib/chat/llm.ts"
    ],
    "approach": "tools.ts fail-closed side/order_type (mirroring `normalizeAlertOp`'s shape in src/lib/alerts.ts:19-23, the established fail-closed pattern in this codebase): add `function normalizeDraftSide(raw: unknown): 'buy'|'sell'|null { const v = String(raw ?? '').trim().toLowerCase(); return v === 'buy' ? 'buy' : v === 'sell' ? 'sell' : null; }` and a sibling `normalizeOrderType(raw: unknown): 'market'|'limit'|null { const v = String(raw ?? 'market').trim().toLowerCase(); return v === 'market' ? 'market' : v === 'limit' ? 'limit' : null; }`. In `draft_order.execute` (~line 90-97), replace the two coercing ternaries: `const side = normalizeDraftSide(input.side); if (!side) return { error: 'INVALID_SIDE', details: \\`side must be \"buy\" or \"sell\" (got ${JSON.stringify(input.side)})\\` };` and the same shape for `order_type` before computing `limit_usd`. Clamp `k` in kb_search's execute (~line 164): `k: typeof input.k === 'number' ? Math.min(Math.max(Math.trunc(input.k), 1), 20) : 5` and add `maximum: 20` to the schema (line 155). llm.ts abortSignal threading: change `type Transport = (body: any, apiKey: string) => Promise<any>;` (330) and `type OpenAITransport = (body: any, apiKey: string) => Promise<any>;` (464) to accept a third optional `signal?: AbortSignal` param. In `defaultTransport` (331-345) and `defaultOpenAITransport` (466-...) and the closure returned by `makeOpenAITransport` (~689), accept that third param and, when present, compose it with a FRESH internal timeout rather than replacing llm-request.ts's shared 60s `LLM_TIMEOUT_MS` backstop (do NOT touch the shared `llmFetch` function itself \u2014 it's used by many non-chat callers including `fetchLlmWithRetry`'s per-attempt-timeout composition, which is too subtle/high-blast-radius to safely alter in this pass): `const combined = signal ? AbortSignal.any([signal, AbortSignal.timeout(LLM_TIMEOUT_MS)]) : undefined; const res = await llmFetch(url, { ..., ...(combined ? { signal: combined } : {}) });` (when `combined` is undefined, llmFetch's own existing `init.signal ?? AbortSignal.timeout(LLM_TIMEOUT_MS)` fallback applies unchanged). Update the two `this.transport(requestBody, this.apiKey)` call sites (line 424 for AnthropicLLM, line 586 for OpenAILLM) to pass `args.abortSignal` as the third argument. Loop-exhaustion fallback (llm.ts ~436-459): after the `for` loop exits (whether via `break` or exhausting `MAX_STEPS`), if `text` is still empty/whitespace-only and `toolCalls.length > 0`, set a clear fallback string (e.g. summarizing that N tool calls ran but the assistant didn't produce a final summary) instead of returning only `withDisclaimer('')`.",
    "tests": [
     "new test in test suite for tools.ts (check for an existing test/chat-tools.test.ts, else create one) \u2014 `execute({ side: 'Short', qty: 5 }, ctx)` returns `{ error: 'INVALID_SIDE', ... }`, NOT a buy draft \u2014 fails today (produces a `side: 'buy'` draft), passes after.",
     "test/chat-llm.test.ts \u2014 new test 'passes abortSignal as the third argument to transport' (models the existing 'passes the api key in the second argument to transport' test at line ~83): `const controller = new AbortController(); await llm.run({ ...baseArgs, abortSignal: controller.signal }); expect(transport.mock.calls[0][2]).toBe(controller.signal);` \u2014 FAILS today (`this.transport(requestBody, this.apiKey)` only ever passes 2 args, so `calls[0][2]` is `undefined`) and PASSES after threading the signal through."
    ],
    "risks": "The `AbortSignal.any` composition needs Node 20.3+ (repo pins Node 24 via .nvmrc, confirmed fine) \u2014 do NOT touch the shared `llmFetch`/`fetchLlmWithRetry` in llm-request.ts, whose per-attempt-timeout composition (line ~555-559, explicitly commented 'a caller-supplied init.signal is respected as-is') has subtle behavior other non-chat callers (framework-review.ts, learning-review.ts, outcome-engine.ts, post-mortem.ts, proposal-revalidation.ts, strategy-tuning.ts) depend on; scoping the fix to chat/llm.ts's own transport wrappers avoids that landmine entirely. The `order_type`/`side` fail-closed change is a genuine behavior change for any caller that was relying on the old silent-coercion (none should be, since it was always wrong, but re-run the full chat/tools test suite). The loop-exhaustion fallback text is a UX nicety, not safety-critical \u2014 keep its scope small (don't redesign the tool loop) given the cluster's overall effort budget.",
    "effort": "M"
   }
  },
  {
   "key": "approval-path-integrity",
   "title": "Approving is starved by the run lock and the size that gets placed is not the size that was reviewed",
   "severity": "P2",
   "surface": "cross-surface",
   "member_uids": [
    "trading-money-path:tsx-09",
    "trading-money-path:tsx-10",
    "trading-money-path:tsx-11",
    "llm-agent-architecture:llm-06",
    "api-contract-web-ios:api-05",
    "alerts-04"
   ],
   "root_cause": "executeProposal contends for the whole-run strategy lock (src/lib/strategy-execution.ts:297), so an approval during a 15-20 minute autonomous run returns busy \u2014 the console gives up after ~2 min and iOS shows a green check. Mobile approvals also queue FIFO behind strategy.run_once because they are absent from IMMEDIATE_MOBILE_COMMAND_TYPES (src/lib/mobile-api.ts:88), whose claim loop is globally ordered with no per-user partition. On the sizing side, approval-time ratio re-anchoring moves EXIT limits with the market (src/lib/approval-reprice.ts:100), a hard-coded 5% headroom shrinks the owner's own cap (src/lib/policy.ts:567), and Retry Red Team stamps approve-at-half without applying the haircut (src/lib/retry-red-team.ts:47).",
   "one_change": "Give approvals a per-account lease instead of the whole-run lock at src/lib/strategy-execution.ts:297 (and add the non-broker command types to the immediate set at mobile-api.ts:88), and make the reprice, haircut and headroom rules apply to the order that is actually placed rather than to the displayed card.",
   "effort": "L",
   "tranche": 1,
   "plan": {
    "cluster_key": "approval-path-integrity",
    "summary": "Four sub-findings, with one premise correction and one that does NOT hold as stated after reading the code. (1) `executeProposal` (src/lib/strategy-execution.ts:297) calls `acquireStrategyLock(lockOwner, userId, policy.connectedAccountId)` with NO retry \u2014 verified the lock is ALREADY per-account (db-execution.ts:184-186 `strategyLockKey` includes `connectedAccountId` when given, and strategy.ts:460's own run-lock acquisition passes the same `connectedAccountId`), so the review's 'give approvals a per-account lease' framing is already true at the DB layer; the REAL bug is that the lock check is a single instant-fail with zero retry, so an approval that lands mid-run on the SAME account is starved for however long that run holds the lock, when a short bounded wait (matching the existing `LANE_WAITS.approvalPlacement = 30_000` convention in account-mutation.ts, already used elsewhere for 'a human click may briefly wait') would resolve most contention. (2) Mobile approvals: `proposal.approve` is DELIBERATELY excluded from `IMMEDIATE_MOBILE_COMMAND_TYPES` (mobile-api.ts:88) \u2014 it's grouped in `RISK_INCREASING_QUEUED_COMMAND_TYPES` (line 93-97) specifically so a halt can reach into the QUEUE and cancel it via `cancelQueuedRiskIncreasingCommands` before it fires; making it 'immediate' as the finding's one_change literally recommends would REMOVE that panic-button interceptability. The real, verified bug is one level deeper: `claimNextQueuedCommand()` (mobile-api.ts:701-706) claims globally (`WHERE status = 'queued'`, no user_id filter) AND `processPendingMobileCommands` (line 1137) is a strictly serial `while` loop that `await`s each command to FULLY finish before claiming the next \u2014 so ANY queued command (any account, any user) sits untouched for the full duration of whatever command claimed first, confirmed worse than a mis-scoped query. (3) src/lib/approval-reprice.ts's ratio-preserving reprice of exit limits is, after reading the whole module, a deliberately engineered, provenance-aware design \u2014 it already exempts `referencePriceProvenance === 'limit-fallback'` (hand-set prices, which is exactly what a chat-drafted manual sell limit gets stamped as per db-proposals.ts's `ensureReferencePrice`) and only re-anchors proposals whose reference price came from a genuine decision-time quote, preserving their relative aggressiveness rather than blindly chasing the market. This premise does NOT hold as a bug \u2014 recommend VERIFY/discuss with owner, no code change. (4) src/lib/policy.ts:101 `OPENING_ORDER_HEADROOM_PCT = 5` is a hard-coded, non-owner-adjustable module constant used in the headroom gate (~line 567) \u2014 a genuine violation of the 'guardrails are the owner's adjustable preferences' ruling, with the `iraWashSaleHandling` field as the canonical template for how to fix it. (5) src/lib/retry-red-team.ts's `retryProposalRedTeam` calls `stampRedTeamResult(proposal, result)` (finalized-sizing-review.ts:102-110) which only writes verdict METADATA onto `proposal.redTeamVerdict` and never touches `proposal.quantity`/`dollarAmount` \u2014 confirmed via strategy-execution.ts:626's parallel 'final broker-adjusted size' path, which correctly calls `applyRedTeamHalfSize(proposal)` (strategy-risk.ts:50) whenever the verdict is 'approve-at-half'; retry-red-team.ts has NO equivalent call anywhere, so clicking Retry Red Team, getting 'approve-at-half' back, then clicking Approve places the FULL original size \u2014 a real, serious money-path bug.",
    "files_to_change": [
     "src/lib/strategy-execution.ts",
     "src/lib/mobile-api.ts",
     "src/lib/policy.ts",
     "src/lib/types.ts",
     "src/lib/retry-red-team.ts"
    ],
    "approach": "(1) In strategy-execution.ts, wrap the single `acquireStrategyLock(...)` check (line 297) in a small bounded retry loop reusing `LANE_WAITS.approvalPlacement` (30_000ms) from account-mutation.ts: poll every ~500ms-1s up to that budget before returning `{status:'busy',...}`, matching the existing 'human click may briefly wait' philosophy already codified for the broker-mutation lease later in the same function. (2) In mobile-api.ts, do NOT move `proposal.approve` into `IMMEDIATE_MOBILE_COMMAND_TYPES` (would drop it from `cancelQueuedRiskIncreasingCommands`'s halt-interception reach \u2014 a real safety regression). Instead restructure `processPendingMobileCommands` (line 1137) so it can run commands for DIFFERENT `(userId, connectedAccountId)` pairs concurrently instead of one global serial `await` chain: track an in-flight set keyed by `(userId, connectedAccountId)` (not one global boolean), have `claimNextQueuedCommand` (or a new variant) skip a queued row whose account key is already in-flight rather than strictly claiming oldest-first, and dispatch claimed commands via `Promise.allSettled` up to the existing `limit` (currently 3-10). This preserves FIFO ordering WITHIN one account (required for the TOCTOU cap-check correctness the strategy lock protects) while unblocking a different account's queued approve from an unrelated account's long-running command \u2014 the actual real-world complaint. (3) No code change to approval-reprice.ts \u2014 write up the provenance-aware design in the PR description and raise with the owner whether STRATEGY-GENERATED (not chat-drafted) exit limits should also be reprice-exempt, since the current behavior appears intentional (extensively comment-justified, adversarially probed) rather than a defect. (4) In src/lib/policy.ts, change `applyOpeningOrderHeadroom(value: number)` (line 240) to `applyOpeningOrderHeadroom(value: number, headroomPct: number = OPENING_ORDER_HEADROOM_PCT)` and use `headroomPct` in the `Math.floor(value * (100 - headroomPct)) / 100` body \u2014 backward compatible for the existing single-arg call in test/policy.test.ts:259. Add `openingOrderHeadroomPct?: number` to `TuningSettings` (src/lib/types.ts, ~line 317, matching the `/** ... Default N. */` doc-comment convention on sibling fields) and thread `context.policy.tuning?.openingOrderHeadroomPct ?? OPENING_ORDER_HEADROOM_PCT` into the call site at policy.ts:567. Note: exposing this on a Console settings UI (matching `iraWashSaleHandling`'s pattern) is real 'done' but is a fast-follow \u2014 flag it explicitly rather than silently leaving it API-only. (5) In src/lib/retry-red-team.ts, after `stampRedTeamResult(proposal, result)` (~line 44), when `result.verdict === 'approve-at-half'`: call `const haircut = applyRedTeamHalfSize(proposal);` (import from strategy-risk.ts); if `!haircut.applied`, append the note to `proposal.redTeamVerdict.reason` and treat it as effectively rejected (set `proposal.redTeamVerdict.rejected = true`), mirroring strategy-execution.ts's 'not executable' handling; if applied, re-price via `gateway.reviewEquityOrder({ accountNumber: policy.accountNumber, ...proposal })` (gateway obtained the same way `loadApprovalQuoteScan` already needs account context) and pass the fresh `estimatedNotional`/`review` into the existing `updatePendingProposalReprice(proposalId, { proposal, decision, estimatedNotional, review }, userId)` call (that function already accepts both fields) so the persisted row's displayed notional matches the halved size, not the stale full-size one.",
    "tests": [
     "new test in test/retry-red-team.test.ts \u2014 mock `debateProposal` to return `{ verdict: 'approve-at-half', available: true, rejected: false, reason: '...' }` for a proposal with `quantity: 10`, call `retryProposalRedTeam`, then re-fetch the row via `getProposal` and assert `row.proposal.quantity === 5` \u2014 FAILS today (quantity stays 10) and PASSES after wiring `applyRedTeamHalfSize`.",
     "test/policy.test.ts \u2014 extend the existing headroom test: `expect(applyOpeningOrderHeadroom(4.99)).toBe(4.74)` (unchanged, default-arg backward compat) plus a new `expect(applyOpeningOrderHeadroom(4.99, 10)).toBe(4.49)` (10% headroom) \u2014 fails today (function has no second param) and passes after.",
     "new test for strategy-execution.ts's executeProposal \u2014 hold the per-account strategy lock (simulate a concurrent run via `acquireStrategyLock`), call `executeProposal` for a proposal on that SAME account, release the lock ~2s later from a parallel timer, assert `executeProposal` eventually succeeds rather than immediately returning `{status:'busy'}` \u2014 fails today (instant fail, no retry), passes after the bounded-wait retry is added."
    ],
    "risks": "The mobile-worker concurrency restructure ((2) above) is the riskiest single item in this cluster \u2014 it touches the core command dispatcher and must preserve the existing halt-interception (`cancelQueuedRiskIncreasingCommands`) and stale-command sweep (`STALE_MOBILE_COMMAND_THRESHOLD_MS`) semantics exactly; test with the existing test/mobile-api.test.ts and test/stale-mobile-commands.test.ts suites, and consider landing (1)+(4)+(5) (all narrowly-scoped, independently valuable) as a first PR, with (2) as a separate, more carefully reviewed follow-up given its blast radius. (3) is explicitly a no-code-change recommendation \u2014 do not let it get folded into the same PR as an actual fix without an owner decision first. The bounded-wait retry in (1) must stay strictly bounded (30s) \u2014 an unbounded or very long wait would itself become a new UX problem (a hanging HTTP request) that owner rulings explicitly warn against (no new blocking ceremony).",
    "effort": "L"
   }
  },
  {
   "key": "prompt-trust-boundary",
   "title": "Non-owner content can reach the always-trusted strategy prompt with no human and a scanner that is inert in production",
   "severity": "P2",
   "surface": "backend",
   "member_uids": [
    "llm-agent-architecture:llm-04",
    "llm-agent-architecture:llm-13",
    "llm-agent-architecture:llm-18",
    "coach-05",
    "coach-06"
   ],
   "root_cause": "src/lib/learning-review.ts:640-663 applies any 'keep' verdict with no riskTier exclusion, and applyApprovedPending (src/lib/learned-context/store.ts:397-405) merges strategy-directive rows straight into the strategy prompt \u2014 the one region the app never scans (src/lib/prompt-safety.ts:224) \u2014 while learningReviewMode defaults to 'decide' (src/lib/defaults.ts:90). Candidates get there from src/lib/chat/coach-learning.ts:567, which stores 480 chars of arbitrary fetched page text as auto-write 'fact' tier, past a classifier scoped to financial-risk vocabulary; the second-layer semantic gate resolves MockLLM in production (src/lib/learned-context/semantic-gate.ts:124); and app/api/chat-history/route.ts:28 lets the caller write a fabricated 'assistant' turn that is replayed as trusted context.",
   "one_change": "Apply the injection scan at ingest rather than only at prompt assembly, and require a human for anything that lands in the strategy prompt regardless of review mode \u2014 src/lib/chat/coach-learning.ts:567, src/lib/learning-review.ts:660, src/lib/learned-context/store.ts:401 \u2014 plus delete the unused POST writer at app/api/chat-history/route.ts:28.",
   "effort": "M",
   "tranche": 1,
   "plan": {
    "cluster_key": "prompt-trust-boundary",
    "summary": "Confirmed multi-part trust-boundary gap in the learning pipeline that feeds the strategy prompt. src/lib/learning-review.ts's `applyLearningReviewVerdicts` (line 631) applies ANY 'keep' verdict on a `learned_context_pending` row via `applyApprovedPending(pending, nowIso)` (learned-context/store.ts:397-405) with no check of `pending.riskTier` \u2014 and `applyApprovedPending` DIRECTLY calls `setStrategyPrompt(merged, ...)` when `riskTier === 'strategy-directive'` (store.ts:400-404), merging content straight into the one prompt region the app never scans for injected instructions. `learningReviewMode` defaults to `'decide'` (defaults.ts:90) \u2014 fully automated, no human in the loop \u2014 once the (opt-in, off-by-default) learning review feature is enabled at all. The candidate pipeline feeding this: src/lib/chat/coach-learning.ts's URL-lesson path (~line 567) fetches arbitrary web page text and calls `ingestLearned(..., { kind: 'fact', value: lesson.slice(0,800), ... })`; `ingestLearned` (learned-context/store.ts:82) does run the second-layer `classifyWithSemanticGate` classifier internally regardless of the caller's `kind` hint, but that classifier's LLM call resolves via `getLLM(opts.userId)` (chat/llm.ts:741), which fails OPEN to `MockLLM` whenever no real credential resolves for that userId/context (chat/llm.ts:720-723) \u2014 and the classifier's own fail-safe (semantic-gate.ts:139-142) treats any unparseable/mock response as 'fact' (never upgrades to 'risk'), meaning the second-layer safety net is silently INERT (not dangerous, just absent) whenever MockLLM resolves, with zero observability that this happened. Separately, `app/api/chat-history/route.ts`'s POST handler (lines 13-33) lets any caller with a resolvable userId write an arbitrary `role:'assistant'` turn directly into chat history that later gets replayed as trusted prior-assistant context by orchestrator.ts's multi-turn replay \u2014 confirmed genuinely dead/unused: grepped app/console/assistant/chat.tsx, ios/SocraticTrade/MobileAPIClient.swift, ios/SocraticTrade/CoachView.swift and neither web nor iOS ever calls it (both only GET/DELETE); the real, sole writer of turns is orchestrator.ts:148/479 calling `appendTurn` internally from the model's actual output.",
    "files_to_change": [
     "src/lib/learning-review.ts",
     "src/lib/learned-context/store.ts",
     "src/lib/learned-context/semantic-gate.ts",
     "app/api/chat-history/route.ts"
    ],
    "approach": "Require a human for anything landing in the strategy prompt, regardless of `learningReviewMode`: in `applyLearningReviewVerdicts` (learning-review.ts, inside the `verdict.verdict === 'keep'` branch ~line 653), before calling `applyApprovedPending`, check `pending.riskTier === 'strategy-directive'` \u2014 if so, do NOT auto-apply even under `'decide'` mode; instead route it the same way the function already handles 'defer' (leave status pending, attach the reviewer's reasoning via `setPendingLearnedContextReviewNote`, so it surfaces in the human review queue) and audit a new `learning_review_strategy_directive_held_for_human` event so this is visible, not silent. This is a narrow, additive carve-out \u2014 every other riskTier ('fact', 'risk') keeps today's `learningReviewMode`-driven behavior unchanged, so it doesn't reintroduce blocking ceremony on the vast majority of learned-context flow, only on the one tier that becomes literal LLM instructions. Ingest-time scan placement: since `ingestLearned` already runs `classifyWithSemanticGate` on every candidate regardless of caller-supplied `kind` (confirmed), the scan IS already 'at ingest' for the classifier's purposes \u2014 the actual gap is observability, not placement. In semantic-gate.ts's `classifyWithSemanticGate` (~line 121, the `try` block around the LLM call), check `llm instanceof MockLLM` (or add an explicit `resolvedMock: boolean` return from `getLLM`/a cheap `isMockLLM(llm)` type guard) when `opts.llm` was not explicitly injected, and `audit('semantic_gate_mock_llm_fallback', { userId: opts.userId, subject: candidate.subject }, opts.userId)` when it fires in a real (non-test) call \u2014 this makes the previously-silent 'safety net is inert' condition observable in the audit trail without changing the fail-safe behavior itself (still defaults to keyword-tier 'fact', never fails closed/blocks). Delete the unused POST handler entirely from app/api/chat-history/route.ts (lines 13-33), keeping GET and DELETE \u2014 verified zero legitimate callers on web or iOS.",
    "tests": [
     "test/learning-review.test.ts \u2014 new test: build a `learned_context_pending` row with `riskTier: 'strategy-directive'`, a verdict of `'keep'`, call `applyLearningReviewVerdicts`, assert (a) `getStrategyPrompt(userId)` does NOT contain the pending row's value, and (b) the row's status stays `'pending'` with a review note set \u2014 FAILS today (the content lands in the strategy prompt immediately) and PASSES after the carve-out.",
     "test/semantic-gate.test.ts \u2014 new test: call `classifyWithSemanticGate` without an injected `opts.llm` and with no LLM credential resolvable for the test userId (forcing MockLLM), assert an audit row of kind `semantic_gate_mock_llm_fallback` was written \u2014 fails today (no such audit call exists anywhere), passes after.",
     "new/extended test for app/api/chat-history/route.ts \u2014 assert the route module exports no `POST` (or that a POST request 405s) \u2014 fails today (POST succeeds and writes a turn), passes after deletion."
    ],
    "risks": "The strategy-directive carve-out changes real behavior for anyone who has BOTH `learningReviewEnabled: true` AND relies on fully automated 'decide' mode approving strategy-directive-tier lessons today \u2014 per CLAUDE.md this is a sole-user app and the feature is off by default, so blast radius is low, but confirm with the owner this is acceptable before landing (it's a deliberate, scoped exception to their 'decide' mode, not a blanket new ceremony \u2014 every other tier is untouched). The MockLLM audit-only change is pure observability, zero behavior change, essentially risk-free. Deleting the chat-history POST route is safe per the exhaustive grep of every client caller, but re-run that grep immediately before landing in case a very recent commit added a caller.",
    "effort": "M"
   }
  },
  {
   "key": "broker-order-shape",
   "title": "Each broker adapter rewrites the approved order differently and records nothing",
   "severity": "P2",
   "surface": "backend",
   "member_uids": [
    "trading-money-path:tsx-06",
    "trading-money-path:tsx-07",
    "trading-money-path:tsx-13",
    "trading-money-path:tsx-16",
    "trading-money-path:tsx-18",
    "trading-money-path:tsx-19"
   ],
   "root_cause": "There is no shared venue-normalization step: src/lib/alpaca.ts:157 keeps time_in_force=gtc on extended-hours limits that Alpaca rejects with a 422, alpaca.ts:741 never forwards extended_hours on the MCP path, src/lib/robinhood.ts:972 silently converts a fractional or extended-hours limit into a regular-hours MARKET order while the persisted proposal and fill receipt still say 'limit', src/lib/tradier.ts:789 sends the unrounded limit price and always attaches a stop field, getEquityTradability is a stub returning tradable/fractional true for every symbol (alpaca.ts:582), and the hard-gate copy names Robinhood for Alpaca accounts (src/lib/policy.ts:326).",
   "one_change": "Put one venue-normalization step in front of every adapter's place call that resolves TIF, extended-hours, rounding and fractional support per broker and audits any rewrite, and make the tradability probe real so the pre-flight check means something.",
   "effort": "M",
   "tranche": 2,
   "plan": null
  },
  {
   "key": "dead-controls",
   "title": "Shipped controls whose backend does nothing, or something other than the label claims",
   "severity": "P1",
   "surface": "cross-surface",
   "member_uids": [
    "pages-02",
    "pages-03",
    "pages-05",
    "desktop-web-ux:dweb-07",
    "ops-observability-admin:sre-20",
    "admin-03",
    "admin-06"
   ],
   "root_cause": "policy.taxSettings.subtractFromResults is written by app/console/strategy/tax-settings.tsx:213 and read by nothing in the Results render path (app/console/results/page.tsx:940-994 renders P&L unmodified), so \"Show results net of estimated tax\" changes no number in either direction. The Webhook channel's copy promises Discord embeds that only the separate legacy webhookUrl path builds (app/console/settings/delivery.tsx:37 vs src/lib/notify.ts:262 and src/lib/notifications.ts:330-372), and \"Send test\" never probes that legacy field. Presets has no create/rename/delete although /api/profiles implements POST/PUT/DELETE; the admin nav omits learning-ledger revert and the RAG backfills entirely; the R2 card tells the operator to run a curl.",
   "one_change": "For each control, either wire it to the behavior its label claims or remove it \u2014 starting with subtractFromResults in app/console/results/page.tsx:940-994, the legacy webhook probe in /api/notifications/test, and preset CRUD against the existing /api/profiles routes.",
   "effort": "M",
   "tranche": 2,
   "plan": null
  },
  {
   "key": "copy-claims-and-rulings",
   "title": "Shipped copy asserts guardrails the engine does not have and reintroduces the paper/live ceremony the owner ruled out",
   "severity": "P1",
   "surface": "cross-surface",
   "member_uids": [
    "ux-copy-editor:copy-03",
    "ux-copy-editor:copy-11",
    "ux-copy-editor:copy-12",
    "ux-copy-editor:copy-13",
    "ux-copy-editor:copy-16",
    "pages-07",
    "public-site-growth:dpub-03",
    "public-site-growth:dpub-04",
    "public-site-growth:dpub-05",
    "public-site-growth:dpub-12",
    "public-site-growth:dpub-18",
    "public-site-growth:dpub-19",
    "llm-agent-architecture:llm-19",
    "api-contract-web-ios:api-19",
    "coach-03",
    "market-data-integrity:mdi-14",
    "ios-hig-ux:ios-08",
    "ios-hig-ux:ios-12",
    "desktop-web-ux:dweb-19",
    "lifecycle-10"
   ],
   "root_cause": "app/console/macro/indicators.ts:698/706 and again at :753 tell the owner that below-median buys are \"hard-vetoed\" in risk-off/crisis regimes, while src/lib/strategy-risk.ts:187-197 keeps and tags them (its own comment reads 'advisory pre-veto (tag-not-drop)') and socraticOverrideMode defaults to 'execute'; app/how-it-works/page.tsx:94 lists delegation controls that do not exist and app/framework/content.ts:226 states a sizing invariant the override path contradicts. The same class of copy shouts \"(NOT Real Money)\" / red Live pills (app/console/settings/brokers.tsx:626, ios/SocraticTrade/ProposalsView.swift:366) and adds an iOS-only confirm for live activation the web does not have.",
   "one_change": "Run one accuracy pass that rewrites every guardrail claim to the engine's actual advisory behavior and strips the paper/live ceremony, sourcing the guardrail-semantics sentences from a single shared constant so console, iOS and the public pages cannot drift apart again.",
   "effort": "M",
   "tranche": 2,
   "plan": null
  },
  {
   "key": "auth-session-hardening",
   "title": "Sessions are unrevocable 30-day bearer tokens behind a soft perimeter",
   "severity": "P2",
   "surface": "cross-surface",
   "member_uids": [
    "security-auth:sec-02",
    "security-auth:sec-03",
    "security-auth:sec-04",
    "security-auth:sec-05",
    "security-auth:sec-06",
    "security-auth:sec-09",
    "security-auth:sec-10",
    "security-auth:sec-11",
    "security-auth:sec-12",
    "security-auth:sec-13",
    "security-auth:sec-14",
    "ios-engineering:ios-02",
    "ios-engineering:ios-18"
   ],
   "root_cause": "src/lib/auth/session-token.ts:6 issues a stateless 30-day JWT with no server-side record, so sign-out is a cookie delete and there is no revocation, no 'sign out everywhere' and no absolute lifetime; iOS then holds that cookie in non-Keychain storage with no app lock. The handoff around it is loose: app/api/mobile/auth-redirect/route.ts:24 mints a session code for any authenticated GET with a caller-supplied code_challenge, app/api/mobile/auth/apple/route.ts:23 uses no nonce, and app/api/mobile/push/register/route.ts:84 reassigns another user's device token. The origin answers directly at 167.233.254.55:443 with Host: socratictrade.com (middleware.ts:173), so the WAF, bot rules and IP trust are all bypassable, and CSP has been report-only since 2026-07-01.",
   "one_change": "Back sessions with a server-side record (id in the JWT, revocable, absolute lifetime) and bind the mobile handoff to that record with a nonce and a client-bound challenge, then restrict the origin to Cloudflare IPs so the edge rules become load-bearing.",
   "effort": "L",
   "tranche": 2,
   "plan": null
  },
  {
   "key": "ops-liveness-durability",
   "title": "Nothing pages when the desk stops, and the backup/disk story is unverified",
   "severity": "P2",
   "surface": "cross-surface",
   "member_uids": [
    "ops-observability-admin:sre-01",
    "ops-observability-admin:sre-02",
    "ops-observability-admin:sre-03",
    "ops-observability-admin:sre-04",
    "ops-observability-admin:sre-05",
    "ops-observability-admin:sre-07",
    "ops-observability-admin:sre-08",
    "ops-observability-admin:sre-11",
    "ops-observability-admin:sre-14",
    "ops-observability-admin:sre-15",
    "ops-observability-admin:sre-16",
    "backend-reliability:berel-05",
    "backend-reliability:berel-13",
    "backend-reliability:berel-14",
    "backend-reliability:berel-16"
   ],
   "root_cause": "app/api/health/route.ts:88 and :128 set checks.schedulerStale and checks.tradingLiveness.degraded without changing the 200 (ok flips only at :78 and :279), and nothing pushes on them \u2014 src/lib/scheduler.ts has a single sendNotification call, line 372 \u2014 so a wedged scheduler is discoverable only by an external keyword monitor. The Docker HEALTHCHECK gives that same heavy route 5s (Dockerfile:75) while documented stalls exceed it. The R2 'writes paused' banner reads a leftover marker with no endpoint check (src/lib/r2-usage.ts:571 vs scripts/coolify-prod-start.sh:200-212, live replica is B2), nothing rotates litestream-runtime.log (coolify-prod-start.sh:223), no table has retention (src/lib/db-fills.ts:118), and no restore drill has ever run.",
   "one_change": "Make liveness a first-class push plus one operator card \u2014 fire a notification on schedulerStale/tradingLivenessDegraded, split a cheap liveness route for the container probe, gate the R2 banner on isLitestreamReplicaCloudflareR2(), and add log rotation plus table retention.",
   "effort": "L",
   "tranche": 2,
   "plan": null
  },
  {
   "key": "admin-honesty",
   "title": "Operator surfaces present placeholders, unattributed writes and mislabeled scope as fact",
   "severity": "P2",
   "surface": "desktop-web",
   "member_uids": [
    "ops-observability-admin:sre-06",
    "ops-observability-admin:sre-09",
    "ops-observability-admin:sre-13",
    "ops-observability-admin:sre-17",
    "admin-01",
    "admin-02",
    "admin-04",
    "admin-05",
    "admin-07",
    "admin-08"
   ],
   "root_cause": "app/admin/page.tsx:448 renders a bare CPU percentage with no caveat although app/api/admin/server-metrics/route.ts:604-613 documents the scaling as unverified and potentially an 8x under-report (the detail page at server-metrics-client.tsx:175 carries the caveat), and substitutes 0.0% / '0 Running' when a probe fails. app/api/admin/server-knobs/route.ts:80 audits every knob write as user 'local' regardless of which admin flipped it, and the admin chrome never shows the signed-in identity (app/admin/layout.tsx:217 reads only the avatar image). /admin/transcript calls /api/chat-history, which has no requireAdmin and is scoped to the caller's own turns, while the nav calls it 'every chat turn'; no page under app/admin/** has a server-side gate at all.",
   "one_change": "Carry the caveat and the unknown state through to the Overview card instead of substituting zeros, record and display the real admin identity on knob writes, and put one requireAdmin gate on the /admin page tree plus an admin-scoped transcript read.",
   "effort": "M",
   "tranche": 2,
   "plan": null
  },
  {
   "key": "alert-push-delivery",
   "title": "A notification's lifecycle ends before delivery is known, and the tap lands on the wrong screen",
   "severity": "P2",
   "surface": "cross-surface",
   "member_uids": [
    "alerts-03",
    "alerts-07",
    "alerts-08",
    "alerts-10",
    "alerts-11",
    "ops-observability-admin:sre-10",
    "ios-engineering:ios-05",
    "ios-engineering:ios-06",
    "ios-engineering:ios-16",
    "ios-engineering:ios-17",
    "ios-hig-ux:ios-16"
   ],
   "root_cause": "src/lib/alerts.ts:88-89 flips the alert to the one-shot 'triggered' state before sendNotification is awaited, and sendNotification never throws on total delivery failure (src/lib/notifications.ts:265-279 folds every error into a returned status) \u2014 so a revoked token or zero configured channels loses the only chance to notify. The row is tagged with whatever account happened to be active (notifications.ts:216) so it can drop out of the account-filtered Activity feed. The server builds a symbol-specific deep link (src/lib/push-deep-links.ts:99) that neither client reads (app/console/watchlist/page.tsx has no useSearchParams; ios/SocraticTrade/DeepLink.swift:75-77 discards the query), unlike the ?proposal= path at DeepLink.swift:58-73. There is no edit/pause, no badge, no category, and no recovered event (src/lib/db-health.ts:574).",
   "one_change": "Make delivery the state transition \u2014 mark an alert triggered only once a channel accepted it, retrying otherwise \u2014 and implement symbol-aware routing plus badge/category/thread-id on both clients, mirroring the proposal deep-link that already works.",
   "effort": "M",
   "tranche": 2,
   "plan": null
  },
  {
   "key": "visual-tokens-theme-contrast",
   "title": "Tone tokens fail WCAG AA at the sizes they are used, and light-default is not the default in several hosts",
   "severity": "P1",
   "surface": "cross-surface",
   "member_uids": [
    "web-accessibility:a11y-02",
    "web-accessibility:a11y-15",
    "web-accessibility:a11y-17",
    "mobile-web-ux:mweb-25",
    "ios-hig-ux:ios-07",
    "mobile-web-ux:mweb-02",
    "mobile-web-ux:mweb-03",
    "mobile-web-ux:mweb-04",
    "desktop-web-ux:dweb-13",
    "public-site-growth:dpub-15",
    "frontend-performance:dweb-07",
    "frontend-performance:dweb-17",
    "ios-hig-ux:ios-13",
    "ios-hig-ux:ios-18"
   ],
   "root_cause": "app/console/console.css:40-73 defines tone text on soft fills that measures 3.68-4.34:1 (independently recomputed) at 11px/12.5px \u2014 below the 4.5:1 bar since neither size is 'large text' \u2014 including the Stop/Start Agent control; the Approve button's LIVE tag is 9.5px (console.css:578) and input borders are 1.5:1 (console.css:612). iOS uses system Color.green/Color.orange (ios/SocraticTrade/AppComponents.swift:13) and requests Lato weights the bundle does not ship. On the theme side, 'System' is stored by deleting the key so it reads back Light (app/console/lib/useConsoleTheme.ts:37), \u2318K's toggle writes a different hook instance, and <Toaster theme=\"system\"> (app/layout.tsx:80) plus TickerLogo's dark-first default (ui/ticker-logo.tsx:21) render dark inside the light-default app.",
   "one_change": "Darken the tone tokens to at least 4.5:1 at the app's own micro sizes (open PR #2795 already does the console half) and make one theme resolver the single source for the console, the toast host, TickerLogo and iOS so light is the shipped default everywhere.",
   "effort": "M",
   "tranche": 2,
   "plan": null
  },
  {
   "key": "phone-touch-viewport",
   "title": "The phone build inherits desktop control sizes and ignores the visual viewport",
   "severity": "P1",
   "surface": "mobile-web",
   "member_uids": [
    "mobile-web-ux:mweb-01",
    "mobile-web-ux:mweb-05",
    "mobile-web-ux:mweb-06",
    "mobile-web-ux:mweb-07",
    "mobile-web-ux:mweb-08",
    "mobile-web-ux:mweb-09",
    "mobile-web-ux:mweb-10",
    "mobile-web-ux:mweb-12",
    "mobile-web-ux:mweb-13",
    "mobile-web-ux:mweb-16",
    "mobile-web-ux:mweb-18",
    "mobile-web-ux:mweb-19",
    "mobile-web-ux:mweb-21",
    "web-accessibility:a11y-13",
    "ios-hig-ux:ios-14"
   ],
   "root_cause": "app/console/components/chrome.tsx:326 renders the full 'Stop Agent'/'Resume Agent' label in a flex-shrink:0 button (console.css:471-484) alongside a non-shrinking StateChip, avatar and \u2318K trigger, so the only min-w-0 sibling \u2014 the account scope selector, the control naming which brokerage account is loaded \u2014 collapses at 360-390px; the file's own comment computed its ~73px budget against the older short STOP label. The coarse-pointer 44px floor covers only .con-btn (console.css:458), overlays neither lock scroll nor size with dvh (console.css:858, :1220), inputs at 15px/11px trigger iOS auto-zoom (console.css:1251), and sheets ignore browser history.",
   "one_change": "Apply the coarse-pointer 44px floor and a 16px input minimum to every interactive class rather than just .con-btn, truncate or icon-ify the chrome run-state label below the sm breakpoint, and give every overlay scroll-lock plus dvh/visualViewport sizing and a history entry.",
   "effort": "M",
   "tranche": 2,
   "plan": null
  },
  {
   "key": "console-a11y-keyboard",
   "title": "Shared console overlay and menu primitives skip the keyboard and announcement contract",
   "severity": "P2",
   "surface": "desktop-web",
   "member_uids": [
    "web-accessibility:a11y-03",
    "web-accessibility:a11y-05",
    "web-accessibility:a11y-06",
    "web-accessibility:a11y-08",
    "web-accessibility:a11y-09",
    "web-accessibility:a11y-10",
    "web-accessibility:a11y-16",
    "web-accessibility:a11y-20"
   ],
   "root_cause": "The primitives every page composes are non-conformant: app/console/ui/sheet.tsx:116's focus sentry and Escape handler are not stack-aware, so Escape on a drawer or palette above a Sheet closes the Sheet underneath; app/console/components/shell.tsx:180 has no skip link past ~22 focus stops; app/console/ui/toast.tsx:38 announces errors politely and auto-dismisses in 9s with no hover/focus pause, and typed-confirm server refusals are never announced at all; approval cards have no accessible name and repeat a bare 'Reject' (approval-card.tsx:455); the account-scope menu uses role=menu with no arrow-key handling plus an invisible full-screen focusable button (chrome.tsx:235).",
   "one_change": "Fix the shared primitives once \u2014 an overlay stack for focus/Escape in sheet.tsx, assertive non-auto-dismissing error toasts, accessible names on menu/dialog/card roots and a skip link in shell.tsx \u2014 so all 20+ call sites inherit the behavior.",
   "effort": "M",
   "tranche": 2,
   "plan": null
  },
  {
   "key": "signin-return-first-run",
   "title": "Entry paths lose their destination or dead-end: expired sessions, OAuth errors, and iOS first-run",
   "severity": "P1",
   "surface": "cross-surface",
   "member_uids": [
    "public-site-growth:dpub-01",
    "public-site-growth:dpub-02",
    "public-site-growth:dpub-17",
    "lifecycle-03",
    "lifecycle-04",
    "lifecycle-05",
    "lifecycle-06",
    "lifecycle-09",
    "ios-hig-ux:ios-09",
    "ux-copy-editor:copy-14",
    "desktop-web-ux:dweb-16"
   ],
   "root_cause": "middleware.ts:412-419 answers an unauthenticated /api/* with a bare 401 and drops the destination on page redirects, so a push deep link lands on the dashboard after sign-in and app/console/lib/useConsoleData.tsx:139-150 treats that 401 as any transient error \u2014 the tab polls stale data forever and Run once reports a generic 'The run failed'. Every Auth.js error routes to /access-denied (src/lib/auth/auth.ts:197) with no way out, and /login renders for already-authenticated users. On iOS the first-run checklist says to connect a broker 'in Account & Settings' (ios/SocraticTrade/HomeView.swift:87) while that very screen's empty state says to use the website (:844), and readiness ignores the LLM key entirely (src/lib/mobile-api.ts:1315).",
   "one_change": "Carry the destination through sign-in and special-case 401 in the console fetch client (stop polling, route to /login), and make the iOS first-run checklist either open the AdminPortalView cookie-sharing web view or state plainly that connecting happens on the website.",
   "effort": "M",
   "tranche": 2,
   "plan": null
  },
  {
   "key": "llm-catalog-routing-cost",
   "title": "Model choice, receipts and cost each derive from a stale or partial source",
   "severity": "P2",
   "surface": "cross-surface",
   "member_uids": [
    "llm-agent-architecture:llm-03",
    "llm-agent-architecture:llm-07",
    "llm-agent-architecture:llm-09",
    "llm-agent-architecture:llm-10",
    "llm-agent-architecture:llm-11",
    "llm-agent-architecture:llm-12",
    "llm-agent-architecture:llm-14",
    "llm-agent-architecture:llm-15",
    "llm-agent-architecture:llm-16",
    "llm-agent-architecture:llm-17",
    "llm-agent-architecture:llm-20"
   ],
   "root_cause": "src/lib/model-rotation.ts:73 hardcodes a 'dead' OpenRouter list that drops slugs the live catalog still serves; app/console/components/approval-card.tsx:127 asserts 'this run's rotation pick, not a failover' even when an implicit fallback answered; src/lib/chat/llm.ts:655 routes Coach to native providers while the engine and the save-gate assume universal OpenRouter; late post-soft-timeout responses are audited but never metered (src/lib/strategy.ts:6594) and OpenRouter's billed usage.cost is never requested or stored (src/lib/llm-usage.ts:180), so /console/usage is derived from a hand-maintained price table with no embedding/rerank row; and Red Team re-sends the entire Green user content (strategy.ts:6318) that RedTeamReviewContext documents as a subset.",
   "one_change": "Make the live OpenRouter catalog plus the response's own billed cost the source of truth for rotation, receipts and the Usage page, and compact the Red Team payload to the documented RedTeamReviewContext subset.",
   "effort": "M",
   "tranche": 2,
   "plan": null
  },
  {
   "key": "test-coverage-ci-cost",
   "title": "The suite covers shapes but not money paths, reaches the live internet, and serializes 14 minutes into every run",
   "severity": "P2",
   "surface": "backend",
   "member_uids": [
    "qa-test-strategy:qa-05",
    "qa-test-strategy:qa-06",
    "qa-test-strategy:qa-07",
    "qa-test-strategy:qa-08",
    "qa-test-strategy:qa-09",
    "qa-test-strategy:qa-10",
    "qa-test-strategy:qa-12",
    "qa-test-strategy:qa-17",
    "ios-engineering:ios-22",
    "qa-test-strategy:qa-13",
    "qa-test-strategy:qa-15",
    "qa-test-strategy:qa-16"
   ],
   "root_cause": "vitest.config.ts has no setupFiles at all \u2014 only a globalSetup that plumbs TMPDIR \u2014 so nothing prevents a test from reaching SiliconFlow, SEC or Yahoo (STATUS.md still records those 404s), and maxWorkers:1 serializes ~7,000 tests into ~14 minutes of every CI run. 63 of 122 routes have no route-level test including single-proposal approve (app/api/proposals/[id]/approve/route.ts:11), TestBrokerGateway always fills instantly (src/lib/robinhood.ts:920) so only the happy path is reachable, MobileStore.submit/load are untested despite an existing URLProtocol harness, there is no coverage tooling, and the codex-autofix workflow fails on every review event.",
   "one_change": "Add a vitest setup file that fails any un-mocked outbound fetch plus a partial-fill/reject mode on TestBrokerGateway, then write route tests for approve/replace/pause and re-enable parallel workers behind the per-worker temp DB the config already provisions.",
   "effort": "L",
   "tranche": 2,
   "plan": null
  },
  {
   "key": "ios-network-decode",
   "title": "One transient response or one bad row degrades the whole iOS app",
   "severity": "P2",
   "surface": "ios",
   "member_uids": [
    "ios-engineering:ios-03",
    "ios-engineering:ios-08",
    "ios-engineering:ios-12",
    "ios-engineering:ios-13",
    "ios-engineering:ios-21",
    "api-contract-web-ios:api-07",
    "api-contract-web-ios:api-09"
   ],
   "root_cause": "A single failed background reload flips snapshotLoadFailed and the 3-minute staleness gate then disables Approve/Run/Watchlist/Policy (ios/SocraticTrade/MobileStore.swift:223) with no polling fallback; GETs have no 5xx retry and the Cloudflare mapping stops at 523 (MobileAPIClient.swift:373), so deploy-swap 502/503s reach the user raw. SSE reconnects on a fixed 5s loop and every scene-active tears down a healthy stream (MobileStore.swift:343), while every SSE frame triggers a full snapshot reload. Decoding is all-or-nothing: one null required field inside any list element fails the entire snapshot (MobileModels.swift:240) and duplicate command ids trap outright (MobileStore.swift:46).",
   "one_change": "Make decoding element-tolerant \u2014 drop and count bad rows instead of failing the payload \u2014 and add bounded retry with backoff for idempotent GETs and SSE, so a deploy swap or one malformed row degrades a section rather than the app.",
   "effort": "M",
   "tranche": 2,
   "plan": null
  },
  {
   "key": "console-ia-forms-blotters",
   "title": "Console pages disagree about where things live, how they save, and how they are searched",
   "severity": "P2",
   "surface": "desktop-web",
   "member_uids": [
    "desktop-web-ux:dweb-02",
    "desktop-web-ux:dweb-03",
    "desktop-web-ux:dweb-04",
    "desktop-web-ux:dweb-05",
    "desktop-web-ux:dweb-06",
    "desktop-web-ux:dweb-08",
    "desktop-web-ux:dweb-09",
    "desktop-web-ux:dweb-10",
    "desktop-web-ux:dweb-11",
    "desktop-web-ux:dweb-17",
    "pages-08",
    "ux-copy-editor:copy-20",
    "desktop-web-ux:dweb-20",
    "performance-accounting:perf-23",
    "lifecycle-08"
   ],
   "root_cause": "Three different save models coexist across the Configure pages (app/console/guardrails/page.tsx:335), and two numeric fields PATCH on every keystroke against the blur-commit pattern every sibling uses (app/console/settings/page.tsx:874 and strategy/overlays-panel.tsx:170, where a mid-edit empty field saves the fallback 2). /console/decisions is orphaned from the nav (decisions/page.tsx:1), the proposal lifecycle spans five surfaces, a bare 'r' key fires a strategy run with two mounted RunOnce buttons (command-palette.tsx:128), blotters cannot be searched, filtered or exported (activity/page.tsx:216), the Positions blotter lives only on Home without last price or exit action, and Results has no drawdown/profit factor/window.",
   "one_change": "Standardize the page contract \u2014 one save model (local draft plus explicit commit), one hotkey/verb registry, one blotter component with search/filter/export \u2014 and put every route in the nav section it belongs to.",
   "effort": "L",
   "tranche": 2,
   "plan": null
  },
  {
   "key": "ios-parity-gaps",
   "title": "iOS renders a read-only subset of the screens it mirrors",
   "severity": "P2",
   "surface": "ios",
   "member_uids": [
    "ios-hig-ux:ios-02",
    "ios-hig-ux:ios-03",
    "ios-hig-ux:ios-04",
    "ios-hig-ux:ios-06",
    "ios-hig-ux:ios-10",
    "ios-hig-ux:ios-15",
    "ios-hig-ux:ios-19",
    "ios-hig-ux:ios-20",
    "ios-hig-ux:ios-21",
    "ios-hig-ux:ios-22",
    "performance-accounting:perf-24"
   ],
   "root_cause": "Coach replies print raw markdown (ios/SocraticTrade/CoachView.swift:272) and its Suggested Draft cannot be staged although the copy says to approve it in Proposals (:323); the Scan header advertises three side-by-side columns that every row stacks vertically (ScanView.swift:158); the Activity tab is described as everything the agent did but shows only fills and the phone's own commands (MobileControlView.swift:64), so a run_failed push lands on a screen that cannot show the failure; Positions omit per-position P&L (MarketsView.swift:140); Results has no equity curve (ResultsView.swift:57); the symbol sheet offers no actions (SymbolInfoSheet.swift:46); triage is one-at-a-time; and Account & Settings is reachable only from the Home toolbar avatar.",
   "one_change": "Close the parity list against the console screens these mirror \u2014 a shared markdown renderer, the decision-journal feed, per-position P&L and an equity curve \u2014 and surface Account & Settings from More as well as Home.",
   "effort": "L",
   "tranche": 3,
   "plan": null
  },
  {
   "key": "ios-build-compliance",
   "title": "iOS project-level hygiene: no privacy manifest, hardcoded production origin, no strict concurrency",
   "severity": "P2",
   "surface": "ios",
   "member_uids": [
    "ios-engineering:ios-10",
    "ios-engineering:ios-11",
    "ios-engineering:ios-19",
    "ios-engineering:ios-20",
    "api-contract-web-ios:api-18"
   ],
   "root_cause": "ios/project.yml:39 ships no PrivacyInfo.xcprivacy although the app uses UserDefaults, a required-reason API \u2014 ITMS-91053 on upload; the production origin is hardcoded in LoginView.swift:265, AdminPortalView and DeepLink with no debug override, so every simulator run hits production despite MobileAPIClient.productionBaseURL existing to prevent that drift; project.yml:18 builds in Swift 5.9 language mode with no strict-concurrency checking; and MobileStore/PushNotificationCoordinator/TabPreferences are ObservableObject against the repo's own @Observable rule, re-publishing on every SSE line.",
   "one_change": "One ios/project.yml pass \u2014 add the privacy manifest, a build-configuration-driven base URL and strict concurrency \u2014 plus the @Observable migration the repo rule already requires.",
   "effort": "M",
   "tranche": 3,
   "plan": null
  },
  {
   "key": "copy-consistency-rules",
   "title": "Owner copy rules are unenforced on the web: two-space gaps, Title Case, money format, Central time",
   "severity": "P2",
   "surface": "cross-surface",
   "member_uids": [
    "ux-copy-editor:copy-01",
    "ux-copy-editor:copy-02",
    "ux-copy-editor:copy-05",
    "ux-copy-editor:copy-06",
    "ux-copy-editor:copy-07",
    "ux-copy-editor:copy-08",
    "ux-copy-editor:copy-09",
    "ux-copy-editor:copy-10",
    "ux-copy-editor:copy-15",
    "ux-copy-editor:copy-17",
    "ux-copy-editor:copy-18",
    "ux-copy-editor:copy-19",
    "ux-copy-editor:copy-21",
    "ux-copy-editor:copy-22",
    "ios-hig-ux:ios-17",
    "public-site-growth:dpub-06",
    "qa-test-strategy:qa-14",
    "ops-observability-admin:sre-18",
    "desktop-web-ux:dweb-14",
    "desktop-web-ux:dweb-15",
    "pages-06"
   ],
   "root_cause": "SENTENCE_GAP is used in 9 files against roughly 600 web copy strings (app/console/lib/format.ts:54), and the double ASCII spaces typed into JSX collapse to one at render (app/console/components/consent-gate.tsx:107) so source looks compliant and the screen is not; public pages have zero NBSP gaps. Headings and buttons are sentence case against the Title Case rule and against iOS (chrome.tsx:789), one run state carries three names and one stop control four (derive.ts:171), the same guardrail is labeled differently on web and iOS (GuardrailsView.swift:69), compact money renders uppercase suffixes (drilldown-data.ts:674), and app/console/settings/legal.tsx:40 calls toLocaleString() with no timeZone against the Central-time ruling every sibling honors.",
   "one_change": "Add the copy-rule lint the iOS suite already has \u2014 two-space gap, Title Case headings and buttons, lowercase compact money, Central-time timestamps, one term per concept \u2014 and run a single pass to make the strings pass it.",
   "effort": "M",
   "tranche": 3,
   "plan": null
  },
  {
   "key": "phone-layout-density",
   "title": "Phone layouts are the desktop tree at desktop density",
   "severity": "P2",
   "surface": "mobile-web",
   "member_uids": [
    "mobile-web-ux:mweb-11",
    "mobile-web-ux:mweb-14",
    "mobile-web-ux:mweb-15",
    "mobile-web-ux:mweb-17",
    "mobile-web-ux:mweb-20",
    "mobile-web-ux:mweb-22",
    "mobile-web-ux:mweb-23",
    "frontend-performance:dweb-14",
    "frontend-performance:dweb-16"
   ],
   "root_cause": "app/console/console.css:90 defines no phone type-scale bump, so 412 uses of 11px and 185 of 12.5px render at desktop sizes on a 6-inch screen; /console/watchlist keeps a 5-column table whose row actions scroll off (watchlist/page.tsx:227); Home buries portfolio value and day P&L under the readiness hero and strategy bar (page.tsx:217); Scan's phone branch is an unsorted, unfilterable, unvirtualized card wall mounted alongside the hidden desktop tree (scan-table.tsx:505); the equity chart scales to ~70px tall (equity-chart.tsx:11); the Coach composer's Enter always sends with no way to type a newline; and two full-viewport animated gradient orbs keep animating under the opaque console (app/globals.css:155).",
   "one_change": "Give the phone breakpoint its own type scale and card variants, and render one tree per breakpoint instead of mounting both.",
   "effort": "M",
   "tranche": 3,
   "plan": null
  },
  {
   "key": "a11y-structure-naming",
   "title": "Document structure and accessible names are stylistic rather than semantic",
   "severity": "P2",
   "surface": "cross-surface",
   "member_uids": [
    "web-accessibility:a11y-04",
    "web-accessibility:a11y-07",
    "web-accessibility:a11y-11",
    "web-accessibility:a11y-12",
    "web-accessibility:a11y-14",
    "web-accessibility:a11y-18",
    "web-accessibility:a11y-19"
   ],
   "root_cause": "Every console route shares the document title 'Socratic Trade' (app/console/layout.tsx:5, WCAG 2.4.2); 68 heading-styled divs carry con-card-title against 16 real h2/h3 and the collapsible Card demotes its h2 to a span (ui/primitives.tsx:35); none of the 23 console tables has a caption or aria-label and 105 of 106 th elements lack scope (components/positions.tsx:89); the Meter progressbar has no accessible name at any call site (primitives.tsx:201); roughly 147 explanations live only in native title= on non-interactive elements (primitives.tsx:485); readiness state is conveyed by icon and border color alone (readiness-checklist.tsx:27); and the login page has no heading.",
   "one_change": "Fix the primitives and the layout metadata once \u2014 per-route titles, real heading elements inside Card, caption/scope in the table primitive, names on Meter and Tooltip \u2014 so every page inherits the structure.",
   "effort": "M",
   "tranche": 3,
   "plan": null
  },
  {
   "key": "console-first-paint-bundle",
   "title": "First console paint waits on a large bundle and then a single blocking snapshot",
   "severity": "P2",
   "surface": "desktop-web",
   "member_uids": [
    "frontend-performance:dweb-06",
    "frontend-performance:dweb-08",
    "frontend-performance:dweb-09",
    "frontend-performance:dweb-10",
    "frontend-performance:dweb-11",
    "frontend-performance:dweb-12",
    "frontend-performance:dweb-13"
   ],
   "root_cause": "app/console/layout.tsx:22 makes HTML \u2192 ~1.5 MB raw JS \u2192 /api/dashboard a strict waterfall, and routes that fetch their own data still wait behind the full-screen snapshot loader (components/shell.tsx:67). The single console context re-renders every page three times per poll and once per SSE event because fetching and stream health share the context value with snapshot (lib/useConsoleData.tsx:341). Sentry Session Replay (~38 KB gz) ships although replay is env-disabled (instrumentation-client.ts:31), framer motion (~40 KB gz) loads on every page for one tooltip fade (ui/primitives.tsx:7), three heavy client libraries are declared but never imported, and a 1 MB PNG is the primary favicon.",
   "one_change": "Split the context so data and transport health do not re-render together, drop the disabled Replay bundle, the motion dependency and the unused libraries, and let self-fetching routes paint before the snapshot resolves.",
   "effort": "M",
   "tranche": 3,
   "plan": null
  },
  {
   "key": "public-site-funnel-seo",
   "title": "The public site's two halves do not connect and its metadata is stale",
   "severity": "P3",
   "surface": "public-site",
   "member_uids": [
    "public-site-growth:dpub-07",
    "public-site-growth:dpub-08",
    "public-site-growth:dpub-09",
    "public-site-growth:dpub-10",
    "public-site-growth:dpub-11",
    "public-site-growth:dpub-13",
    "public-site-growth:dpub-14",
    "public-site-growth:dpub-16",
    "public-site-growth:dpub-20",
    "mobile-web-ux:mweb-24",
    "ops-observability-admin:sre-19"
   ],
   "root_cause": "An anonymous visitor never reaches the marketing page (app/page.tsx:14), /welcome offers only a mailto 'Request access' with no Sign in (welcome/page.tsx:114) and /login never explains the product, while /framework \u2014 the accurate explainer \u2014 is orphaned and /how-it-works is the only linked one (welcome/page.tsx:137). Metadata is stale or overridden: the sitemap still advertises the deleted /design/socratic-trade route (app/sitemap.ts:15), Cloudflare's managed Content Signals robots prepends Allow: / ahead of the app's Disallow: / (app/robots.ts:47), the root canonical '/' is inherited by every page that does not override it (app/layout.tsx:41), summary_large_image is declared with no image, and the admin sub-route redirect list omits newer pages.",
   "one_change": "Connect the funnel \u2014 Sign in on /welcome, an explainer link on /login, one canonical explainer page \u2014 and correct the metadata: per-page canonical, accurate sitemap entries, an OG image, and a robots policy that matches what is actually served.",
   "effort": "M",
   "tranche": 3,
   "plan": null
  }
 ],
 "p1_verdicts": [
  {
   "uid": "qa-test-strategy:qa-01",
   "verdict": "UPHELD",
   "repro": "1) A regression lands in Swift logic covered by one of the 15 XCTest files under ios/SocraticTradeTests (confirmed present: OrderCancelTests.swift, ProposalPriceReviewTests.swift, PolicyTighteningTests.swift, PushNotificationTests.swift, DeepLinkTests.swift, MobileModelsTests.swift, etc. \u2014 order-cancel and price-review logic on a real trading account). 2) PR opens touching ios/**; .github/workflows/ios-build.yml:70-78 runs only `xcodebuild build ... -destination 'generic/platform=iOS'` \u2014 a compile-only check, verified zero `xcodebuild test` anywhere via `grep -rn 'xcodebuild test' .github/ scripts/` (no hits). 3) The required 'verify' check in ci.yml (needs:[classify, verify-hosted], line 305-306) never references ios-build.yml, so the PR merges on backend checks alone (see qa-02). 4) scripts/ios-ship-testflight.sh, invoked by .github/workflows/ios-ship.yml:105 on a `*/30 * * * *` cron (ios-ship.yml:21), contains no test invocation either (confirmed via grep). 5) The regression reaches TestFlight/the owner's phone with zero automated Swift test having ever run, at any stage.",
   "correction": "None \u2014 evidence holds as stated. File count (15 XCTest files) matches exactly; the claimed line count (2,416) is off from the actual 2,563 but that's immaterial to the finding.",
   "blast_radius": "Money-adjacent: the untested surface includes order-cancel (OrderCancelTests.swift) and price-review (ProposalPriceReviewTests.swift) logic on a real connected brokerage account. No dollar figure is provable from code alone (depends on what regression actually ships), but the exposed logic governs whether an order-cancel action or price-review gate behaves correctly on the phone client of a real-money app, with only a human manually running xcodebuild as a backstop.",
   "confidence": "high"
  },
  {
   "uid": "qa-test-strategy:qa-02",
   "verdict": "UPHELD",
   "repro": "1) An iOS-only PR introduces a non-compiling change (e.g. the documented 2026-08-13 duplicate `@Binding pendingDeepLink` precedent in AGENTS.md). 2) ci.yml's required 'verify' job (line 305: needs:[classify, verify-hosted]; evaluate-gate logic lines 311-350) only ever inspects CLASSIFY_RESULT and HOSTED_RESULT (ubuntu-latest tsc/test/build) \u2014 it never reads ios-build.yml's job result. 3) The repo's merge ruleset requires only 'verify' (+ gitleaks per docs/EFFORT-LOG.md's historical required-contexts entries; no ios/xcodebuild context anywhere) \u2014 confirmed via grep across .github/ for any ruleset reference to ios-build. 4) PR merges green. 5) ios-build.yml itself (a separate, non-required workflow) also runs on push to main and would show red, but nothing blocks on it and ios-ship.yml (the archive/ship workflow) has zero Slack/PagerDuty notification step (confirmed empty grep for 'slack|pagerduty|notify' in ios-ship.yml) \u2014 so the failure is silent until a human checks Actions.",
   "correction": "Narrowing the blast-radius mechanism only, not the verdict: since `xcodebuild archive` (used by ios-ship.yml's TestFlight upload) also requires a successful compile, a genuinely non-compiling merge does not silently ship a broken binary to the phone \u2014 it causes the next scheduled TestFlight ship to fail closed (no new build reaches the phone) rather than shipping a broken one. The real production harm is a silently stalled TestFlight pipeline, not a broken app landing on the device.",
   "blast_radius": "All subsequent iOS fixes queue behind a broken main until a human notices the ios-build.yml red X or the ios-ship.yml archive failure \u2014 there is no alerting. On a project largely driven by autonomous agents working on other lanes, that could be hours to days of TestFlight staleness for the owner's daily-use iOS app.",
   "confidence": "high"
  },
  {
   "uid": "qa-test-strategy:qa-03",
   "verdict": "UPHELD",
   "repro": "1) app/api/llm-usage/model-stats/route.ts:9 and :19 bundle-import docs/benchmarks/2026-07-08-llm-model-benchmark.json and docs/benchmarks/2026-07-10-mistral-rebench.json at build time (confirmed verbatim). 2) A PR edits only one of those files in place (a realistic edit \u2014 they're maintained as dated snapshot data). 3) ci.yml:141's classify regex `grep -Ev '(^|/)[^/]+\\.md$|^docs/'` treats ALL of docs/** as documentation-class, so the diff is classified docs-only (docs_only=true). 4) verify-hosted (ci.yml:207-208, `if: ... docs-only != 'true'`) is skipped entirely \u2014 tsc/test/build never run. 5) The required 'verify' job exits 0 on DOCS_ONLY=true (ci.yml ~333-337) before ever checking HOSTED_RESULT. 6) PR auto-merges; on merge to main, Coolify auto-deploys (per CLAUDE.md's documented auto-deploy-on-merge policy) and runs its own `next build`, which is the first place a JSON-syntax break would be caught \u2014 in production. test/model-stats.test.ts (the only test touching this code) imports src/lib/model-stats.ts functions only, not route.ts (confirmed via grep), so even a full local `npm test` run wouldn't catch a route-level import break.",
   "correction": "None \u2014 evidence holds exactly as stated; e2e.yml:77 carries the identical regex, confirmed verbatim.",
   "blast_radius": "Worst case: a JSON-syntax-breaking edit auto-merges and breaks Coolify's `next build` on the next production deploy \u2014 a production outage on socratictrade.com's LLM-usage/model-stats surface, first caught by the deploy itself, not CI. Lesser case: a syntactically valid but reshaped edit silently changes the cost/latency numbers shown at /console/usage with zero test coverage.",
   "confidence": "high"
  },
  {
   "uid": "qa-test-strategy:qa-04",
   "verdict": "UPHELD",
   "repro": "1) app/api/mobile/snapshot/route.ts:16-54 builds the response as a bare `NextResponse.json({...})` object literal (e.g. `pendingProposals: snapshot.pendingProposals` at line 46, `recentCommands: listMobileCommands(...)` at line 52) with no shared type tying its keys to Swift's CodingKeys. 2) A developer renames one of these top-level keys during a refactor (e.g. of src/lib/dashboard.ts's snapshot shape) \u2014 this compiles cleanly in TS since it's just an object literal. 3) MobileModels.swift's Decodable init (lines 41-67) decodes every array/summary field via `decodeIfPresent(...) ?? []`/`?? .empty` (confirmed verbatim for positions, orders, pendingProposals, connectedAccounts, watchlist, alerts, recentCommands), so a missing/renamed key produces an empty array instead of a decode failure. 4) The only Swift-side decode test, MobileModelsTests.swift (fullSnapshotJSON string literal at line 409, decoded at line 6), is a hand-authored fixture disconnected from the live route \u2014 confirmed no test file anywhere under test/ references 'api/mobile/snapshot' (grep found zero hits; test/mobile-api.test.ts only imports src/lib/mobile-api.ts helpers). 5) tsc/test/build all pass; the phone silently shows an empty proposals/positions/orders list where real ones exist.",
   "correction": "None \u2014 evidence holds exactly as stated.",
   "blast_radius": "Wrong information, not a crash: on a real connected brokerage account, the iOS app could show 'nothing pending' or 'no positions held' when real open proposals or exposure exist, with zero automated signal at any CI stage. No direct order-placement path, but it's a wrong-information bug about real money on the client the owner checks daily.",
   "confidence": "high"
  },
  {
   "uid": "qa-test-strategy:qa-05",
   "verdict": "UPHELD_NARROWED",
   "corrected_severity": "P2",
   "repro": "1) vitest.config.ts has no `setupFiles` entry at all \u2014 only `globalSetup: './test/global-setup.ts'` (confirmed by reading the full file), which is purely per-run TMPDIR/TMP/TEMP plumbing for temp SQLite DBs, not a network guard (confirmed by reading global-setup.ts in full). 2) Any test file that reaches src/lib/vector-db.ts (embeddings/rerank against https://api.siliconflow.cn or https://openrouter.ai, lines 2440/2611) or a market-data fetcher without a per-file mock makes a real outbound HTTP call. 3) STATUS.md:114 (current, from the 2026-08-18 RAG rollout) confirms verbatim: 'Full npm test still hits leftover SiliconFlow + SEC/Yahoo 404s (untouched).' 4) This produces nondeterministic timeouts/429s under CI's ubuntu-latest runner, consistent with maxWorkers being pinned to 1.",
   "correction": "Downgraded P1 -> P2: verified both named live-network sinks are read-only informational calls \u2014 SiliconFlow/OpenRouter embeddings (src/lib/vector-db.ts:2440,2611) and SEC/Yahoo market-data fetchers \u2014 with no order-placement or fund-movement code path reachable from a test process. The underlying claim (no global fetch guard exists; CI hits live third-party hosts) is fully correct, but the worst realistic outcome is CI flakiness and small wasted third-party API quota, not corrupted trading data, a wrong order, or financial loss \u2014 a materially lower blast radius than qa-01 through qa-04, which touch order/proposal/ship-pipeline correctness on a real account.",
   "blast_radius": "CI reliability/cost only: intermittent test failures and unnecessary calls to SiliconFlow/SEC/Yahoo endpoints during every `npm test` run; no money-path or user-facing-data-integrity impact is reachable from this gap.",
   "confidence": "high"
  },
  {
   "uid": "ux-copy-editor:copy-03",
   "verdict": "UPHELD",
   "repro": "1) app/console/macro/page.tsx:185 calls `regimeInfo(board.regime)` and renders `.meaning` directly to the user at :204 (title tooltip) and :232 (visible paragraph) on the live /console/macro page. 2) In a crisis or risk-off VIX regime, indicators.ts:698 and :706 render literal 'hard-vetoed' language (both confirmed verbatim, unchanged \u2014 no commits to this file since #2365). 3) src/lib/strategy-risk.ts:187-197 (Rule 3) tags such a below-median buy proposal with `preVetoReasons` and pushes it into `kept`, not `vetoed` \u2014 the code comment literally reads 'advisory pre-veto (tag-not-drop). Was a hard `continue` drop' (line 187-ish). 4) src/lib/defaults.ts:71 sets `socraticOverrideMode: 'execute'` as the DEFAULT policy value, and src/lib/strategy.ts (resolveSocraticOverride call sites at ~2552-2581, 3124-3164, 3559-3584) shows an `autonomyOverride` thesis on an opening can pass this tag and place the trade under that default. So the described 'hard veto' is, out of the box, an overridable advisory tag.",
   "correction": "None \u2014 evidence holds exactly as stated.",
   "blast_radius": "No dollar figure provable from code (depends on whether the agent actually attaches an override thesis in a given crisis), but the copy directly misdescribes how much protection exists during a market panic on a real account, and contradicts the owner's own documented 'nothing is hard but the account boundary' design principle in user-facing copy.",
   "confidence": "high"
  },
  {
   "uid": "ux-copy-editor:copy-04",
   "verdict": "ALREADY_FIXED",
   "repro": "N/A \u2014 fixed on this branch's own history before the review-docs commit was added.",
   "correction": "Fully fixed by PR #2857, commit 437f0a4e4 ('iOS Guardrails names + fold #2849 Desk subtitle'), which is an ANCESTOR of this branch's tip (git log: 5d453afef docs-review commit is a child of 437f0a4e4). Verified current ios/SocraticTrade/HomeView.swift:94/109 no longer says 'Strategy page' \u2014 the checklist detail now reads `DeskCopy.universeNeedsIndex` = 'Choose at least one base index (e.g. S&P 500) or add extra symbols so the strategy has names to scan.' and the follow-up hint reads `DeskCopy.universeRefreshAfterGuardrails` = 'Add an index or extra symbols on Guardrails, then pull to refresh here.' (DeskModels.swift:668-673). Verified GuardrailsView.swift no longer contains 'Universe edits stay on the Strategy page' (grep for 'Strategy page' across ios/ and app/ returns only two Swift TEST assertions that it's ABSENT: ios/SocraticTradeTests/DeskModelsTests.swift:283 `XCTAssertFalse(DeskCopy.universeNeedsIndex.contains(\"Strategy page\"))` and UserFacingCopyTests.swift:103). The register's verify_note quotes pre-fix text that is no longer present in the current tree; this appears to be a stale finding from a review pass done before #2857 landed, folded into a docs commit made after #2857 was already on main.",
   "blast_radius": "None currently reachable \u2014 the copy now correctly names Guardrails as the destination, matching the real web location at app/console/guardrails/page.tsx:425, and two regression tests pin the fix against recurrence.",
   "confidence": "high"
  },
  {
   "uid": "security-auth:sec-01",
   "verdict": "UPHELD",
   "corrected_severity": "P1",
   "confidence": "high",
   "repro": "Real sequence, each step on current HEAD (5d453afe):\n1. Prod container boots `scripts/coolify-prod-start.sh` with Infisical-injected env. `instrumentation.ts:47-48` calls `assertEncryptionKeyConfiguredInProduction` (src/lib/db-api-keys.ts:103-111) \u2014 that throws only on ENCRYPTION_KEY. I grepped instrumentation.ts and scripts/coolify-prod-start.sh: there is no AUTH_SECRET assert anywhere (the only non-test AUTH_SECRET reads are middleware.ts:166,367, app/api/dashboard/route.ts:10, app/api/auth/robinhood/callback/route.ts:37,71, app/api/mobile/auth/apple/route.ts:36, src/lib/auth/session-token.ts:25).\n2. AUTH_SECRET goes missing selectively (Infisical rename/delete/scope change) while ENCRYPTION_KEY stays \u2192 boot succeeds.\n3. Anonymous request hits the app. `getCfEmail` returns null (CF_ACCESS_TRUST_EMAIL_HEADER off), middleware.ts:354-359 leaves trustedEmail null. The Auth.js branch is skipped entirely because it is wrapped in `if (process.env.AUTH_SECRET)` \u2014 middleware.ts:367.\n4. middleware.ts:165-167 `isAuthConfigured()` \u2192 false, so middleware.ts:385-388 sets `trustedEmail = PRIMARY_EMAIL` (middleware.ts:39, default \"mail@jays.services\") and identitySource = local-fallback.\n5. middleware.ts:393 \u2192 `isEmailAllowed` (middleware.ts:257-258) returns true because PRIMARY_SET contains it. No 401 branch is reached.\n6. middleware.ts:420-421 stamps `x-authenticated-user-email` = the owner's address onto the forwarded request.\n7. `app/api/proposals/[id]/approve/route.ts:17` calls `resolveRequestUserId`, and `src/lib/request-user.ts:34-46` maps that email to the owner's userId with NO provenance requirement \u2014 the only provenance consumer is `src/lib/auth/admin.ts:17-20` via `isVerifiedIdentitySource` (src/lib/auth/strip-identity.ts:21-26), which guards /api/admin/* only.\n8. `executeProposal` (approve/route.ts:28) runs against the owner's connected broker account.\nI searched for a second gate and found none: no route outside admin.ts inspects the identity-source header, and `checkSameOrigin` does not stop a deliberate caller who sets Origin.",
   "correction": "Two overstatements, neither fatal. (a) \"with no error anywhere\" is not exact \u2014 iOS Apple sign-in would 500 and log \"AUTH_SECRET is missing\" (app/api/mobile/auth/apple/route.ts:36-40) and the web OAuth/session path would break, so there IS one visible symptom; the web console is what keeps working silently as the owner. (b) The AGENTS.md \"Infisical keys have been deleted by agents\" precedent is about LLM runtime keys, not AUTH_SECRET, so the likelihood argument is weaker than stated. Also note for the fix: the fallback itself is a deliberate dev contract pinned by ~20 `vi.stubEnv(\"AUTH_SECRET\", \"\")` cases in test/middleware-auth.test.ts, so the guard must be an ADDITIONAL production marker (DB_BOOTSTRAP=live), not removal of the branch. Does not conflict with any owner ruling \u2014 ruling 5 explicitly keeps security isolation, and this is correctness hardening, not paternalism.",
   "blast_radius": "Requires a selective env regression (AUTH_SECRET gone, ENCRYPTION_KEY intact). In that state the origin is directly reachable (per sec-02) and every anonymous request is the owner. Worst realistic money outcome: an unauthenticated caller enumerates a pending proposal id and POSTs /api/proposals/{id}/approve, placing a real broker order on the owner's connected Alpaca account at the owner's own per-trade notional cap, repeatable up to RATE_LIMITS.orders; plus POST /api/proposals/bulk-approve for the whole pending queue in one call, and a settings write flipping strategyAuthority to \"decide\" (autopilot) so the engine keeps trading afterward. Ceiling is the account's buying power, not a single ticket. Read side: all positions, P&L, and account labels. Duration: indefinite and silent on the web surface until someone notices iOS login is broken.",
   "file": "middleware.ts",
   "line": 385
  },
  {
   "uid": "ops-observability-admin:sre-01",
   "verdict": "UPHELD_NARROWED",
   "corrected_severity": "P2",
   "confidence": "high",
   "repro": "What survives, verified:\n1. `app/admin/page.tsx:99-106` fetches exactly six endpoints (connections-health, llm-usage, rag-coverage, server-metrics, chat-history, r2-usage). Neither /api/health nor /api/admin/backup-status is among them.\n2. The one server endpoint it does call for infrastructure, `app/api/admin/server-metrics/route.ts:161,384`, returns `os.uptime()` / Coolify host metadata only \u2014 I grepped it for scheduler/heartbeat/lastTick/releaseSha and found nothing.\n3. The signals exist but land only in probes: `checks.schedulerStale` (app/api/health/route.ts:82-88), `checks.tradingLiveness` (route.ts:128-140), and the same two in app/api/ready/route.ts. A repo-wide grep for consecutiveFailedRuns / lastCompletedRunAt / tradingLiveness / schedulerLastTick over app/ and ios/ returns only those two route files \u2014 no UI consumer anywhere.\n4. `deriveStateInfo` (app/console/lib/derive.ts:128-165) computes the StateChip word purely from policy.systemState, policy.strategyAuthority and market hours; a five-failure streak or a dead tick still renders \"Running\".\nSo the concrete failure is: scheduler heartbeat age, consecutive-failure count, litestream state, release sha and process uptime appear on no page, and /admin \u2014 the page titled Operator Overview \u2014 cannot answer \"is the engine alive\".",
   "correction": "The headline \"No at-a-glance 'is the trader alive' panel on web/admin/iOS\" is wrong for two of the three surfaces, and I cut it. (a) The console home renders run liveness immediately beside the StateChip: app/console/page.tsx:154-172 prints \"Last run {status} \u00b7 {Ago finishedAt}\" in `--con-neg` tone with the persisted failure cause when the run failed (describeLastRun, app/console/lib/last-run.ts:50-60), line 179-186 adds an Activity deep link on failure and \"Next {timeUntil} \u00b7 {cadence}m cadence\". A three-day-old or failed last run is visible on the default page without scrolling. (b) iOS Home's third card is ScheduleOverviewCard (ios/SocraticTrade/HomeView.swift:27 and 659-675): Last Run / Next Run / Cadence, where lastRunAt is the real strategy-run start (src/lib/scheduler.ts:825, getLastStrategyRunStartedAt) \u2014 not intent. What is genuinely missing is the /admin consolidation, the chip tone not degrading, and heartbeat-age / failure-count / litestream state having no UI at all. That is a usability and ops-consolidation gap, not the blindness the P1 claimed.",
   "blast_radius": "No money moves and no order is affected \u2014 this is purely how fast the operator notices a stopped engine. On both clients the owner already sees last-run age and a red failure line on the default screen, so the realistic cost is that diagnosing WHY (dead tick vs. failing runs vs. paused replication) requires opening /api/health or the token-gated ops snapshot instead of one admin card, and that a five-run failure streak reads \"Running \u00b7 Autopilot\" in the chip while the adjacent line reads \"Last run failed\". Worst case is minutes-to-hours of extra diagnosis on an incident day, not an unmanaged position.",
   "file": "app/admin/page.tsx",
   "line": 99
  },
  {
   "uid": "ops-observability-admin:sre-02",
   "verdict": "UPHELD_NARROWED",
   "corrected_severity": "P2",
   "confidence": "high",
   "repro": "The (a)+(b) half is real. Sequence:\n1. The scheduler wedges or every run fails. app/api/health/route.ts:88 sets `checks.schedulerStale = true` at >5 min heartbeat age; route.ts:128-140 sets `checks.tradingLiveness.degraded`.\n2. Neither flips the response code: `ok` starts true at route.ts:67 and is set false at only two sites \u2014 route.ts:78 (DB read failure) and route.ts:279 (hard-stopped dependency). The final line is `Response.json({ ok, checks }, { status: ok ? 200 : 503 })`, so a stalled scheduler answers HTTP 200 and can only be caught by keyword-matching the JSON body.\n3. Nothing pushes it: `grep -c 'sendNotification|notify' src/lib/trading-liveness.ts src/lib/openrouter-credits.ts` returns zero matches in both files, and src/lib/scheduler.ts imports sendNotification at line 25 with exactly one call site, line 372 (autonomy_halted_on_boot).\n4. The in-code external fallback is off by default: the Sentry cron check-in requires `Boolean(process.env.SENTRY_DSN) && process.env.SENTRY_CRONS_ENABLED === \"1\"` (src/lib/scheduler.ts), and docs/ops-observability-security.md:31 confirms it is opt-in.\n5. The in-flight fix is not on this branch: `git merge-base --is-ancestor 61989e895 HEAD` \u2192 NOT_ANCESTOR (that is the #2816 head, \"fix(ops): page health JSON flags, require OPS token\").",
   "correction": "I cut the third of the finding that is (c), and one supporting claim. (c) OpenRouter low balance is an explicit owner decision, not a defect: src/lib/openrouter-credits.ts:1-13 states \"Per owner: no in-app fallback and no in-app alerting \u2014 instead expose the credit balance on the public /api/health probe so an EXTERNAL watchdog (Uptime Robot) can alert when it drops, independently of the app itself.\" The recommendation to fire `budget_alert` when credits.ok flips false directly contradicts that. It is also not a hypothetical layer: docs/EFFORT-LOG.md:493 records \"Uptime Robot monitors created and alert successfully\" as DEPLOYED / PRODUCTION VERIFIED 2026-07-18, and the 2026-08-14 entry references \"the paired credits-low keyword monitor\" firing. Second cut: \"the 180 min default is far slacker than a 15-30 min run cadence\" is wrong at the shipped default \u2014 src/lib/defaults.ts:164 sets runCadenceMinutes: 60, so 180 min is already exactly 3x cadence, which is the formula the finding's own recommendation asks for (`max(60, 3x runCadenceMinutes)`). The claim only holds if the owner has lowered cadence. Finally, combined with the sre-01 narrowing, the owner is not blind in-app: the console home and iOS Home both show last-run age. The surviving defect is narrower \u2014 no PUSH for schedulerStale/tradingLivenessDegraded, and no monitor evidence that those two JSON flags are keyword-watched today.",
   "blast_radius": "No wrong order is ever placed by this; the exposure is the engine going quiet while the owner is away from the app. Worst realistic outcome: an autopilot account stops producing runs and nothing reaches the owner's phone, so for the 3-hour stale threshold and then indefinitely (nothing pages on the flag), there are no new entries, no strategy-driven exits, and no stale-exit remediation. Open positions keep only whatever broker-held bracket legs were attached at entry, so the loss is opportunity cost plus exits that never fire on the app side, not naked risk. Recent precedent on this branch (STATUS.md, 2026-08-18/19: Manual Run once locked, runs sitting llm=0 for ~31 min) is the exact class \u2014 and it was found by the owner clicking, not by a page.",
   "file": "app/api/health/route.ts",
   "line": 128
  },
  {
   "uid": "ops-observability-admin:sre-03",
   "verdict": "UPHELD_NARROWED",
   "corrected_severity": "P2",
   "confidence": "medium",
   "repro": "Both code gaps are real; the second one has a documented live trigger.\nR2 card (the actual false statement):\n1. `app/api/admin/r2-usage/route.ts:29` returns `replicationDisabled: isR2ReplicationDisabled(cfg)`, and that helper (src/lib/r2-usage.ts:571-577) is a bare `existsSync(cfg.disableMarkerPath)` \u2014 no endpoint check. The WRITE site is gated (`isLitestreamReplicaCloudflareR2()` at src/lib/r2-usage.ts:766), proving the authors knew the gate was needed and applied it in only one of the two places.\n2. `scripts/coolify-prod-start.sh:200-212` ignores that marker whenever AWS_S3_ENDPOINT is not Cloudflare R2, so litestream DOES start; litestream.coolify.yml:1-8 confirms the active replica is Backblaze B2 with sync-interval 60s.\n3. `app/admin/page.tsx:497-503` renders, on `r2?.replicationDisabled` alone, \"Socratic Trade litestream\u2192R2 writes are **paused** (free-tier kill-switch)\".\n4. The marker is documented as present and deliberately left: docs/rollouts/2026-08-07-litestream-b2-backup.md says backup was \"paused (R2 free-tier kill-switch marker since ~2026-08-04 / Hetzner cutover)\", the volume-cleanup list for `d83b1aykr03uwr32yhgzaiay-prod-app-data` does not include it, and Next steps item 4 reads \"Clear leftover `.litestream-r2-disabled` marker when convenient (cosmetic once on B2)\". No later rollout (Aug 7-19) records a resume or delete.\nGeneric marker: `scripts/coolify-prod-start.sh:188-192` boots without litestream on `.litestream-disabled`, and that path is referenced nowhere in src/ or app/ \u2014 the only marker path in application code is r2-usage.ts:571's separate R2 marker. With litestream absent the socket read fails, /api/health takes the `reason === \"unavailable\"` branch and emits \"Litestream runtime status is unavailable in DB_BOOTSTRAP=live mode \u2014 replication freshness cannot be confirmed\" (app/api/health/route.ts:459-460) instead of \"deliberately off since <date>\".",
   "correction": "The title \"Backup status can lie\" needs narrowing: it never lies in the dangerous direction. (1) The generic marker does NOT produce a false all-clear \u2014 app/api/health/route.ts:445 sets `checks.storageDegraded = true` and fires `alertStorageWarning(\"litestream_state_unreadable\", ...)`, so the owner is alerted; the defect is that the alert misnames a deliberate owner pause as an unreadable state, which is alert-fatigue and wrong copy, not silence. (2) The R2 card's error is a FALSE ALARM (claims paused while B2 replicates every 60s), i.e. pessimistic, not reassuring. Neither path can make a broken backup look healthy. Also worth noting the degraded flag does not 503 (route.ts:67/78/279 are the only `ok` writes), so this cannot flip the Docker HEALTHCHECK \u2014 I checked that specifically because it would have raised severity. Severity accordingly drops to P2: operator confusion and a stale banner, no data-loss window created by the bug itself.",
   "blast_radius": "No money and no data at risk from the bug itself \u2014 B2 replication is running the whole time the card says it is paused. Realistic harm is decision quality during an incident: the Operator Overview's only backup statement is wrong today (a leftover Aug-4 marker on the prod volume, per the B2 rollout's own Next-steps item), so the owner either wastes time \"resuming\" replication that never stopped, or learns to ignore that banner and misses the day it is true. Second-order: if the owner ever uses the generic `.litestream-disabled` kill switch, /api/health alerts every 12h with 'freshness cannot be confirmed' for as long as it stays set, which trains the same dismissal of the real litestream_replication_stale/stopped alerts. One thing I could not settle from code: whether `/app/data/.litestream-r2-disabled` still exists on the live volume \u2014 `docker exec socratic-app test -f /app/data/.litestream-r2-disabled` (or reading `replicationDisabled` from /api/admin/r2-usage) settles the R2-card half in one command.",
   "file": "scripts/coolify-prod-start.sh",
   "line": 188
  },
  {
   "uid": "backend-reliability:berel-01",
   "verdict": "UPHELD",
   "corrected_severity": "P1",
   "confidence": "high",
   "repro": "1. src/lib/db.ts:2148 declares `CREATE VIRTUAL TABLE document_chunks_fts USING fts5(content_hash, symbol, source, accession, text)` \u2014 no UNINDEXED columns, no side key, no rowid mapping. 2. src/lib/db-learning.ts:1613-1616 (`insertDocumentChunkFts`) issues `DELETE FROM document_chunks_fts WHERE content_hash = ? AND symbol = ? AND source = ? AND accession = ?` before every INSERT; the batch form repeats it at src/lib/db-learning.ts:1671-1674, and the resume cursor `countDocumentChunkFts` repeats it as a COUNT at src/lib/db-learning.ts:1700-1712. 3. FTS5's xBestIndex serves only MATCH and rowid constraints, so those predicates are residual filters over a full scan. I PROVED this rather than assuming it: on a scratch DB with byte-identical DDL, `EXPLAIN QUERY PLAN` returns `SCAN document_chunks_fts VIRTUAL TABLE INDEX 0:`, and measured DELETE cost scales linearly with corpus size \u2014 4.3 ms/row at 10k rows (31 MB), 13.1 ms/row at 30k (91 MB), 26.3 ms/row at 60k (180 MB) \u2014 while the INSERT (i.e. tokenization, the cost the code comments blame at db-learning.ts:1639-1646) was 0.1-0.4 ms/row, 30-260x cheaper. That is the finding's contested causal claim, confirmed: the scan is the cost, not tokenization. 4. Extrapolating the measured slope, the repo's own live receipt of ~300 ms/chunk (src/lib/rag/fts-mirror-bound.ts:20-23, `279522ms / 933 chunks`) lands at a corpus on the order of 700k rows, consistent with the documented 4.2 GB DB. 5. Hot callers that pay it once per chunk: src/lib/rag/persist-local-complete.ts:52 (every ingested filing), src/lib/rag/document-summarizer.ts:521, src/lib/db-learning.ts:1673-1674 via the SEC worker (src/lib/rag/sec-ingest-worker.ts:471), plus the GLOB variants at src/lib/rag/persist-local-complete.ts:65-72 and :103-113. No guard, cache, or short-circuit exists anywhere on that path.",
   "correction": "None material. Two small sharpenings: (a) the finding's secondary complaint that the four metadata columns bloat the term dictionary is true but is NOT the cost driver \u2014 my probe shows insert/tokenization is a rounding error next to the scan, so a fix that only adds UNINDEXED without removing the scan would buy almost nothing; (b) the COUNT at db-learning.ts:1700 is the SEC worker's per-tick resume cursor (sec-ingest-worker.ts:457-461), so the worker pays one extra full corpus scan per tick on top of the per-row ones.",
   "blast_radius": "No direct money loss \u2014 this is an event-loop pin, not an order path. Each mirrored chunk blocks the single Node process for one full scan of the FTS corpus (measured slope; ~300 ms/chunk at the live corpus size per fts-mirror-bound.ts:21). Money-adjacent consequence: while pinned, nothing else on the process runs \u2014 the 60 s scheduler tick's synthetic-stop-monitor and stale-exit lanes (scheduler.ts:919, :884) do not evaluate, so a trailing stop that should fire is delayed by the pin duration, and the dashboard's 16 s+8 s broker budgets abort into a blank portfolio/positions/orders section. Frequency: every chunk of every ingested document, i.e. hundreds to thousands of times per ingest pass, daily.",
   "short_summary": "FTS5 idempotency DELETE/COUNT full-scan the corpus per chunk"
  },
  {
   "uid": "backend-reliability:berel-03",
   "verdict": "UPHELD_NARROWED",
   "corrected_severity": "P1",
   "confidence": "medium",
   "repro": "Sequence, all on current code: 1. The Alpaca SDK issues every request as `axios(req)` with no `timeout` key (node_modules/@alpacahq/alpaca-trade-api/dist/api.js:4-22 and :24-42) and axios' default timeout is 0, and src/lib/alpaca.ts:247 constructs `new Alpaca(options)` without touching it \u2014 so a half-open socket never rejects. 2. `trackHealth` (src/lib/alpaca.ts:256-286) retries only THROWN transient errors; a hang is not a throw, so it never retries or aborts. 3. Of the Alpaca methods, `getLatestQuotes` (src/lib/alpaca.ts:563), `createOrder` (:633, :737, :814), `cancelOrder` (:862, :904), `sendRequest` (:884) and `getOptionPositions` (:840) carry NO deadline at the adapter. 4. The scheduler's synthetic-stop-monitor lane adds its key at scheduler.ts:919 and clears it only in `.finally` at :931 \u2014 no `withDeadline`. The stale-limit-scan lane is the same shape (:884 add, :905 `.finally`). 5. That lane body is `withAccountMutation(... , (ctx) => runSyntheticStopMonitor(...))` (scheduler.ts:922-925); `withAccountMutation` (src/lib/account-mutation.ts:194, 227-243) puts no deadline on `run`, and `journalLane` (src/lib/task-journal.ts:37-52) puts none either. 6. `runSyntheticStopMonitor` calls the unbounded `gateway.getEquityQuotes` at src/lib/synthetic-stops.ts:688 and the unbounded `gateway.placeEquityOrder` at :943. 7. Therefore one hung Alpaca quote or order socket leaves `stopMonitorInFlight` permanently set for that account AND holds the durable account-mutation lease, so on every subsequent 60 s tick the stop monitor is skipped outright and the stale-exit lane's `withAccountMutation` (scheduler.ts:899, waitMs LANE_WAITS.staleExit) returns busy and skips too. 8. Separately, `await checkBrokerHealth(...)` at scheduler.ts:957 is serial per account, and for a Tradier account it reaches `await fetch(url, {method, headers, body})` at src/lib/tradier.ts:339 with no signal \u2014 so the rest of that tick's account loop stalls behind it. Contrast confirmed: safety-maintenance.ts:69-78 wraps the identical monitor in `withDeadline(BROKER_TIMEOUT_MS = 15_000)`, but that function only runs inside a strategy run (src/lib/strategy.ts:615), not every tick.",
   "correction": "Two claims in the finding (and one in the first-round verify note) are wrong and I cut them. (1) The verify note's 'getPositions/getEquityOrders/getEquityQuotes/place paths are unbounded' is false for two of them: `getEquityPositions` (src/lib/alpaca.ts:481-490) and `getEquityOrders` (src/lib/alpaca.ts:511-526) BOTH wrap the SDK call in `awaitWithFirstCallRetry` with a 16 s+8 s budget whose `onFinalTimeout` throws (src/lib/inflight-deadline.ts:19-20, :100-140), as does `readAccount` (:293-305). The genuinely unbounded set is quotes, place, cancel, nested-order GET and option positions. (2) The '~15 min on Linux TCP retransmit' figure applies only to the Alpaca axios/Node-http path; the Tradier `fetch` at tradier.ts:339 runs on undici, whose default headersTimeout/bodyTimeout are 300 s, so that hang is bounded at ~5 minutes, not 15. Narrowed accordingly: this is 'the protective lanes go dark for minutes, and permanently on the Alpaca quote/place/cancel sockets', not 'every broker call can hang 15 minutes'. Severity stays P1 because the permanent case is the money-path one.",
   "blast_radius": "Worst realistic outcome: one hung Alpaca socket inside `runSyntheticStopMonitor` (synthetic-stops.ts:688 quotes, or :943 the exit order itself) latches `stopMonitorInFlight` for that account forever, so the account's synthetic trailing stops stop being evaluated on every subsequent 60 s tick with no error surfaced \u2014 a position whose stop should have exited instead rides the full drawdown until the owner notices manually. Because the same hang holds the account-mutation lease, stale-exit remediation and account-drain for that account also silently skip. Blast is one account per hung socket (the in-flight sets are keyed per user+account), plus, for a Tradier account, up to ~5 minutes of the whole tick's account loop stalled behind the serial `checkBrokerHealth` at scheduler.ts:957. Frequency is not provable from code \u2014 it needs a real half-open socket, which prod has seen as UND_ERR_SOCKET throws (alpaca.ts:267-270) but not, in any receipt I can find, as a silent hang.",
   "short_summary": "Alpaca quote/place/cancel have no timeout; stop-monitor lane latches"
  },
  {
   "uid": "backend-reliability:berel-04",
   "verdict": "UPHELD",
   "corrected_severity": "P1",
   "confidence": "medium",
   "repro": "1. scheduler.ts:589-602 dispatches `journalLane(\"filing-body-ingest\", ..., () => refreshFilingBodies(symbols))` behind only `isFilingIngestDue()` and `checkMonthlyLlmSpendCeiling().ok` \u2014 there is NO `hasInFlightStrategyWork()` gate. I checked every use of that gate: scheduler.ts:637 (ROIC) and sec-ingest-worker.ts:99 and :464 are the only three, exactly as the finding says, so #2848's 'pause ROIC and FTS during a strategy run' does not cover this lane. 2. `refreshFilingBodies` is NOT a queue-feeder for the bounded backfill worker \u2014 grep for SEC_INGEST_WORKER_ENABLED / enqueue in src/lib/web-sources/sec-filings.ts returns nothing; it does the fetch/chunk/store/mirror inline. 3. Per filing, src/lib/web-sources/sec-filings.ts:624-631 chunks the whole document and calls `persistLocalComplete` unconditionally (no flag, no write-class branch above it \u2014 the branch only affects `recordLedger`). 4. src/lib/rag/persist-local-complete.ts:46-54 is a bare `for (const chunk of input.chunks) { insertDocumentChunkFts(...) }` \u2014 synchronous, one auto-commit transaction per chunk, no batching, no `yieldEventLoop`, no slice plan. 5. Each of those calls is the berel-01 full-corpus-scan DELETE (src/lib/db-learning.ts:1613-1616), which the repo's own live receipt puts at ~300 ms/chunk (src/lib/rag/fts-mirror-bound.ts:20-23). A 933-chunk 10-K is therefore ~280 s of unbroken event loop. 6. The full-body write class mirrors the SAME chunks a SECOND time at sec-filings.ts:737-756: `runWithActiveVectorCommitProof(..., timeSync(\"filingFtsMirror\", ..., () => { for (const chunk of localChunks) insertDocumentChunkFts(...) }))` \u2014 one synchronous block, no yield. 7. src/lib/rag/document-summarizer.ts:519-527 has the identical unbounded shape (small: abstract highlights, max 8 source chunks). 8. The contrast is real: sec-ingest-worker.ts:462-475 is the only path that goes through `planFtsMirrorSlice` + `insertDocumentChunkFtsBatch` + `yieldEventLoop` + heartbeat, capped at 6 chunks / 2 s per tick (fts-mirror-bound.ts:37-47).",
   "correction": "None to the mechanism. One honest caveat the finding already flags and I could not close: live exposure depends on `SEC_FILING_RAG_MAX_PER_RUN`, resolved from Infisical/per-user settings at src/lib/web-sources/sec-filings.ts:795-803, where an explicit 0 is authoritative and means paused. Code cannot read it. The most recent written state is `docs/rollouts/2026-08-12-rag-advanced-enable.md:18,38` \u2014 worker on, cap 25, TTL 24h, i.e. armed. Also worth naming: the pin only fires on NEW accessions, since `hasIngestedAccession` short-circuits at sec-filings.ts:525-535 \u2014 so the exposure is 'up to 25 previously-unseen filings per daily pass', not 'every pass re-mirrors the corpus'.",
   "blast_radius": "No direct money loss; this is the loop-pin class that produced the 08-13/08-14/08-18 incidents. Worst realistic outcome: one previously-unseen 10-K on the daily filing-body pass blocks the single Node process for ~4-5 minutes with zero yield (933 chunks x ~300 ms, fts-mirror-bound.ts:21), and up to 25 filings per pass. During that window the site serves edge 503s, the scheduler's 60 s tick does not run at all, so NO account's synthetic-stop monitor or stale-exit replacement is evaluated for the duration \u2014 a stop that should fire is delayed by minutes \u2014 and Manual Run once starves exactly as recorded in STATUS.md. Unlike the SEC worker, there is no lease heartbeat around this loop, so a >60 s stretch also expires whatever lease the caller holds.",
   "short_summary": "filing-body ingest mirrors whole filings into FTS with no yield or gate"
  },
  {
   "uid": "frontend-performance:dweb-02",
   "verdict": "UPHELD_NARROWED",
   "corrected_severity": "P2",
   "confidence": "high",
   "repro": "What is provable, step by step: 1. src/lib/alpaca.ts:501-543 `getEquityOrders` always asks for `status: \"all\"`, `limit: 500` \u2014 MCP arg at :502, REST at :513-518 \u2014 i.e. the full lifetime order history, never scoped to open or to a date window. 2. src/lib/dashboard.ts:485-501 calls it inside the `Promise.all([getPortfolio, getEquityPositions, getEquityOrders])` bundle on every dashboard snapshot build. 3. That build is reached from a 15 s client poll (app/console/lib/useConsoleData.tsx:23 `POLL_MS = 15_000`) through a ~10 s single-flight TTL cache (src/lib/dashboard-snapshot-cache.ts:12, :51-70), so roughly one full-history fetch per 15 s per (user, account) with a console tab open. 4. The scheduler adds two more per 60 s tick per account (src/lib/scheduler.ts:861 drain, :888 stale-limit-scan), and 20 call sites total exist across order-replacement, synthetic-stops, strategy-execution, order-cancel, safety-maintenance and ops-snapshot \u2014 none cached. 5. The whole array is serialized into the snapshot at src/lib/dashboard.ts:1106 and shipped to the browser each poll, where the Orders page renders working orders plus `terminalOrders(snapshot.orders, 20)` (app/console/orders/page.tsx:269) \u2014 at most ~20 terminal rows are ever displayed. 6. It also fans out server-side: src/lib/dashboard.ts:946-948 builds a unique-symbol set over the ENTIRE history and runs `getSymbolLatestPrices` on it, and dashboard.ts:934/1058 feed it into symbol-meta and the unified feed. 7. Amplifier the finding missed: the bundle is wrapped in `awaitWithFirstCallRetry` (dashboard.ts:485, src/lib/inflight-deadline.ts:100-140) which, on a 16 s timeout, re-issues the whole `Promise.all` WITHOUT cancelling the first \u2014 so a slow orders read causes a second concurrent full-history walk.",
   "correction": "I cut three things. (1) The headline '50 pages x 500' is conditional, not the normal case: the walk breaks at src/lib/alpaca.ts:541 `if (page.length < PAGE || added === 0 || !oldest || oldest === until) break;`, so an account with <=500 lifetime order records makes exactly ONE call. Nothing in the review, STATUS.md, or ops-snapshot gives a receipt for this account's order count, so 'N sequential round-trips per snapshot' is unproven. (2) The causal link to the `getPortfolioBundle 8000+7000ms` timeouts is a hypothesis, not evidence \u2014 STATUS.md attributes that window to a broker-wide slow-first-read profile (500/500 ok, avg ~3.1 s, max 14.4 s across ALL alpaca-broker calls) and to `ftsMirrorSlice` pinning the loop 6-12 s, neither of which is order pagination. (3) 'ships all of it to every client each poll' overstates by the cache: it is ~1 fetch per 15 s per (user, account), single-flighted, not per request, and the mobile route already strips it to working orders server-side (app/api/mobile/snapshot/route.ts:45). Severity down to P2: proven wasteful and proven to grow without bound, but no receipt tying it to a live outage and no money path.",
   "blast_radius": "No order can be placed or mis-placed by this \u2014 it is a read. Worst realistic outcome: when the 16 s+8 s bundle budget does fire, dashboard.ts:496 returns `[undefined, [], []]`, so Portfolio, Positions AND Orders all render empty at once on the console and the owner can read 'no resting stop orders' when stops in fact exist \u2014 a wrong-belief risk on a protective-order screen, not a wrong-trade risk. Steady-state cost is one `status:\"all\"` limit-500 Alpaca query per 15 s per open console tab plus 2 per 60 s scheduler tick per account, a JSON payload that grows monotonically with lifetime order count on every poll, and one `getSymbolLatestPrices` DB lookup over every symbol the account has ever traded. Cost grows forever because nothing ever ages history out of the query.",
   "short_summary": "Snapshot fetches full status:\"all\" order history every 15s"
  },
  {
   "uid": "performance-accounting:perf-01",
   "verdict": "UPHELD_NARROWED",
   "corrected_severity": "P2",
   "confidence": "high",
   "repro": "Code defect is certain. (1) src/lib/db-fills.ts:329-337 \u2014 `listFillEvents(accountNumber, source, limit=500, userId)` is `SELECT * FROM fill_events ... ORDER BY filled_at ASC LIMIT ?`, i.e. the OLDEST N rows, while the sibling `listPortfolioSnapshots` (db-fills.ts:221-243) was already fixed to the newest-DESC-subquery pattern with a comment naming this exact bug. (2) src/lib/db.ts:739-766 and 2331-2345 are the only DELETEs on fill_events (migration dedupe + account deletion) \u2014 there is no retention/pruning, so rows only accumulate. (3) Every accounting reader passes 500: dashboard.ts:675-676, performance.ts:460/461/468/492/493, tax.ts:337, strategy.ts:824-825, strategy-tuning.ts:335/409/410/417, outcome-engine.ts:964, experience-memory.ts:263, robinhood-pnl-crosscheck.ts:60, app/api/llm-usage/model-stats/route.ts:58. (4) So on fill #501 for one (account, source): calculatePnl (performance.ts:543) never sees the exit, performance.ts:531 `fills: allFills.slice(-100)` is the last 100 of the OLDEST 500, and tax.ts:152 getWashSaleLockProvenance -> performance.ts:748 getClosedLotsDetailed -> pnlForSource -> the same truncated array, so a loss exit booked after #500 never creates a 30-day lockout. LIVE-TODAY sub-case that needs no 500-row assumption: strategy-tuning.ts:418 asks for `limit 30` (the 30 OLDEST fills) and line 603 sends `fills.slice(0, 20)` to the LLM under the key `recentFills`; performance.ts:1354 sets MIN_CLOSED_LOTS_FOR_WEIGHT_SHIFT = 20, so by the time strategy-tuning.ts:455 lets the tuner produce a factor scorecard at all the account has >=20 closed lots (>=40 fills) and the window is provably already truncated to the oldest 30.",
   "correction": "Three overstatements. (a) 'newest fills silently vanish from every P&L figure' is unconditional in the title but conditional in fact \u2014 it needs >500 rows for one (account, source), and nothing in this tree establishes that: both local DBs (/Users/jay/Code/Socratic.Trade/data/app.db and /Users/jay/apps/trading-monet/data/app.db) have zero fill_events rows, and defaults.ts:137 caps maxDailyOrders at 10, so >=50 trading days of maxed-out activity are needed to reach 500 on one book. It is a latent defect, not a demonstrated live one. (b) It does NOT reach order safety, contrary to the 'cross-surface' framing: the daily order/notional caps read trade_proposals with no LIMIT (db-execution.ts:58-62, 92-96), the PDT day-trade gate uses a date-bounded fill_events query with no LIMIT (db-execution.ts:155), and broker reconciliation uses listPendingBrokerReconciliationFills (db-fills.ts:356-372, no LIMIT). No unintended order can result. (c) perf-01 misses the one part that is provably biting now \u2014 the limit-30 / slice(0,20) 'recentFills' window in strategy-tuning.ts:417-418 and 603, which berel-02 does name.",
   "blast_radius": "Reporting + learning only. Once one book passes 500 fills: Results/iOS Realized P&L, win rate, avg return, the fills list, the Tax open-lot ledger and the thesis/regime/factor scorecards all freeze at the pre-#500 state and never move again, with no on-screen indication. The one concrete money leak is the wash-sale lockout (tax.ts:138-163 <- performance.ts:748): a loss exit booked after row #500 never locks the symbol, so the strategy can rebuy inside the 61-day window and forfeit the deduction \u2014 on this book's typical position size that is a few hundred dollars of deferred loss deduction per occurrence, not a lost trade. No unintended order, no cap bypass, no reconciliation loss. Live today (>=31 fills, which MIN_CLOSED_LOTS_FOR_WEIGHT_SHIFT=20 implies whenever the tuner is functional): each POST /api/strategy/tune paid LLM review is fed the 20 OLDEST fills ever booked under the label 'recentFills', so proposed scan-weight nudges are argued from the account's first trades instead of its last."
  },
  {
   "uid": "backend-reliability:berel-02",
   "verdict": "UPHELD_NARROWED",
   "corrected_severity": "P2",
   "confidence": "high",
   "repro": "Same defect as perf-01, cited correctly. src/lib/db-fills.ts:332 `ORDER BY filled_at ASC LIMIT ?` returns the OLDEST N; I confirmed all 16 non-test call sites pass 500 except strategy-tuning.ts:418 which passes 30 (rg over src/ and app/: robinhood-pnl-crosscheck.ts:60, tax.ts:337, experience-memory.ts:263 and 350 (5000), performance.ts:460/461/468/492/493, dashboard.ts:675/676, outcome-engine.ts:964, strategy-tuning.ts:335/409/410/417/418, strategy.ts:824/825, app/api/llm-usage/model-stats/route.ts:58). The aggravation this finding names is the part I can prove bites on current data: strategy-tuning.ts:415-419 picks `listFillEvents(accountNumber, source, 30, userId)` (30 OLDEST) and strategy-tuning.ts:603 ships `fills.slice(0, 20)` to the model as `recentFills`; strategy-tuning.ts:420-421 + performance.ts:1354 gate the whole weight-shift path behind >=20 closed lots (>=40 fills), so the window is guaranteed stale exactly when the feature is active. The PIT branch is no better \u2014 strategy-tuning.ts:417 does `pitFills(listFillEvents(..., 500, ...)).slice(0, 30)`, oldest-of-oldest. No PR in STATUS.md lines 1-95 touches this; `git log -3 -- src/lib/db-fills.ts` shows no fix on this branch.",
   "correction": "Two claims do not hold up. (a) 'A 15-minute-cadence account reaches 500 fills in weeks' \u2014 the shipped cadence is defaults.ts:165 runCadenceMinutes: 60 with defaults.ts:138 maxProposalsPerRun: 3 and defaults.ts:137 maxDailyOrders: 10, so the ceiling is 10 fills/day and the floor for 500 is ~50 trading days on ONE book; there is no 15-minute cadence in the defaults. Whether any prod (account, source) has actually crossed 500 is not answerable from this tree \u2014 both local DBs have 0 fill_events rows. (b) The blast list is too wide: it does not reach the order path. Daily order/notional caps read trade_proposals unbounded (db-execution.ts:58-62), the PDT gate reads fill_events date-bounded with no LIMIT (db-execution.ts:155), and pending-reconciliation reads have no LIMIT (db-fills.ts:356-372).",
   "blast_radius": "Identical to perf-01 (same query). Proven-live portion: the paid strategy-tuning LLM review argues from the 20 oldest fills labelled 'recentFills' (strategy-tuning.ts:603) and derives `currentRegimeFromLots` from a truncated lot replay (strategy-tuning.ts:334-342), so weight nudges that steer which names get proposed are grounded in the account's first trades. Latent portion (>500 rows on one book): Realized P&L / win rate / avg return / Tax open lots / model-stats / iOS fill history freeze silently, and a post-#500 loss exit stops creating a 30-day wash-sale lockout (tax.ts:152-160), letting the strategy rebuy inside the window and forfeit that loss deduction \u2014 hundreds of dollars of deferred deduction per occurrence at this book's position sizes. No unintended order, no cap or PDT bypass."
  },
  {
   "uid": "performance-accounting:perf-02",
   "verdict": "UPHELD",
   "corrected_severity": "P1",
   "confidence": "high",
   "repro": "Reproduced numerically against the real module (npx tsx importing src/lib/cash-flows.ts). Step by step: (1) app/console/page.tsx:62 calls deriveDayPnl(snapshot.performance, reality.mode, portfolio) on the console Home for every render. (2) app/console/lib/derive.ts:425-431 picks the equity curve and takes `baseline` = the last snapshot point before startOfCentralDay(now) \u2014 in normal operation yesterday's ~15:00 CT (20:00Z) close, since defaults.ts:166 runDuringExtendedHours is false. (3) derive.ts:434-441 builds fakeCurrent from the live portfolio with cash = portfolio.cash and positionsValue = currentEquity - cash; alpaca.ts:453-460 makes totalMarketValue = portfolio_value and cash = account.cash, and performance.ts:176-180 stores snapshot cash/positionsValue as portfolio.cash / equityMarketValue (= equity - cash), so the two points are on the same basis. (4) derive.ts:443 calls inferExternalCashFlows([baseline, fakeCurrent], []) \u2014 the fill list is hardcoded empty. (5) In src/lib/cash-flows.ts:157-164 tradeCash is therefore 0, so line 180 `Math.abs(tradeCash) < 1e-9` sends every trading day into the missing-fill guards; lines 187-208 hit the 'cash and positions moved opposite' branch and line 205 sets flow = deltaEquity whenever |deltaEquity| >= max(0.5% of prior equity, 0.25 * min(|dCash|,|dPos|)). (6) derive.ts:449 then asks isInferredFlowUnverified(flow, baseline.equity, currentEquity), which can never reject it: cash-flows.ts:66-71 bounds at max(5*|deltaEquity|, 2% of start equity) and here |flow| == |deltaEquity| <= 5*|deltaEquity|. (7) derive.ts:452 `pnl = currentEquity - baseline.equity - flow` = exactly 0. Measured: baseline {equity 100000, cash 50000, pos 50000} at 2026-08-18T20:00:00Z -> now {equity 101000, cash 48000, pos 53000} at 2026-08-19T15:00:00Z gives flows {'2026-08-19': 1000}, unverified false, dayPnl 0. Controls that behave correctly in the same probe: no-trade +$1k -> 1000; buy $2k with only +$300 market -> 300 (below the 0.5% floor); and the identical inputs with a fill supplied -> 0 flow, which is why the benchmark path (dashboard.ts:718-720 passes benchmarkFills) is immune. (8) app/console/page.tsx:294-296 renders it as `$0.00 (0.00%)`, and `cashFlowAdjusted` \u2014 the only signal that a flow was subtracted \u2014 is set at derive.ts:465 but never read anywhere in app/console/page.tsx (only the isStaleBaseline caption at lines 305-317), so no caveat is shown.",
   "correction": "Nothing material. One precondition the finding did not state, which I verified holds in normal operation: cash-flows.ts:129-142 collapses points by UTC calendar date, so the bug needs baseline and now on different UTC dates. Yesterday's close at 20:00Z vs today mid-session at >=14:00Z satisfies that; it would only fail if the last pre-today snapshot landed between 00:00Z and 05:00Z (7-11pm CT), which requires extended-hours runs that default off. Also worth noting the failure is exactly $0.00, not merely 'wrong' \u2014 flow is set to deltaEquity itself. No test pins the current behavior: the four deriveDayPnl cases in test/console-live-data-derive.test.ts:353-385 use curve points with no `cash` field, so derive.ts:442's guard skips the flow path entirely and they never exercise it.",
   "blast_radius": "The Home headline. On any day the strategy trades net-directionally (net buys or net sells) and the book moves at least 0.5% of equity and at least 25% of the cash-vs-position swap, Day P&L on /console \u2014 desktop and phone widths \u2014 prints exactly $0.00 (0.00%) with no caveat, instead of the true figure. On a $100k book that hides >=$500 of real intraday P&L, and it hides it on winning and losing days alike, so the owner cannot tell a flat day from a -2% day. Frequency is roughly 'most days the strategy trades and the market moves a normal amount'. No order, position, or persisted-state effect \u2014 this is a display-layer computation only (deriveDayPnl is not consumed by any execution path)."
  },
  {
   "uid": "performance-accounting:perf-03",
   "verdict": "UPHELD",
   "corrected_severity": "P1",
   "confidence": "high",
   "repro": "Reproduced numerically against the real module. (1) src/lib/cash-flows.ts:164 is `tradeCash += f.side === \"sell\" || f.side === \"cover\" ? f.notional : -f.notional`, with the comment at line 157 asserting 'buys/shorts reduce cash'. A short sale credits proceeds to cash and a cover debits it \u2014 which the codebase's own identity confirms: alpaca.ts:457 defines equityMarketValue = account.equity - account.cash, so if cash did not rise on a short, opening one would drop reported equity by the full notional. (2) The app really books those sides: performance.ts:278-289 writes fill.side straight from proposal.side (OrderSide includes short/cover) with notional = Math.abs(...), synthetic-stops.ts:848 books `cover` for short exits, and broker-protective-stops.ts:423-427 maps a broker buy-to-cover to side `cover`. (3) The benchmark path feeds these fills in: dashboard.ts:718-720 passes same-source fills into computeSpyBenchmarkDetailed, which reaches benchmark.ts:373 inferExternalCashFlows(equityCurve, fills). (4) With the sign wrong, flow = deltaCash - tradeCash = +N - (-N) = +2N on a short and -2N on a cover. Measured on a $100k book: $3,000 short with the book +$2.5k -> flow +6000, isInferredFlowUnverified false, benchmark.ts:137-141 divides by prev.equity + flow, so that sub-period reports -3.30% against a true +2.50%. (5) The flow is also displayed: app/console/results/page.tsx:261-265 prints 'neutralized $6,000 net deposits' and lines 285-300 render a 'deposit $6,000' row in the Capital regimes table.",
   "correction": "The finding UNDERSTATES it. It says the sanity bound demotes the phantom flow to 'unverified' on a quiet day and only lets it through 'on a day the book moved >= 2% of the notional'. cash-flows.ts:66-71 bounds at max(5*|deltaEquity|, 2% of start equity), so any short whose notional is between 0.25% and 1% of account equity produces a flow of 2N that is BOTH above the materiality floor (cash-flows.ts:81) and automatically inside the 2%-of-equity leg of the bound \u2014 verified regardless of the market move. Measured: $600 short on a $100k book, flat market -> flow +1200, unverified FALSE, sub-period reports -1.19% against a true 0.00%; the matching cover -> flow -1200, verified, reports +1.22%. The bound protects the LARGE short on a quiet day and lets every small one through. Precondition the finding assumes rather than states: shortSellingEnabled is absent from DEFAULT_POLICY and gated at policy.ts:499, so this needs the owner to have enabled shorting on that account \u2014 supported by real short positions on the live book (docs/rollouts/2026-08-08-review-fixes-e-mobile-punchlist.md:64 names a 'T dust short' and PG carrying a SHORT chip). No test pins the current sign: rg for short/cover in test/benchmark.test.ts and test/cash-flows-deposit-invest.test.ts returns nothing.",
   "blast_radius": "The 'Versus the market' scoreboard and the Capital regimes table on /console/results (and the same numbers on the iOS snapshot). Every app-booked short mints a phantom deposit of 2x its notional and every cover a phantom withdrawal of 2x, each of which is a fabricated transfer row the owner is invited to reconcile against a broker statement that will never show it. A single $600 short on a $100k book (0.6% of equity \u2014 squarely inside the always-verified band) drags that sub-period's reported account return by -1.19pp and its cover adds +1.22pp; benchmark.ts:154-159 chains these geometrically, so a book that shorts routinely accumulates a materially wrong all-time account-vs-SPY figure in both directions, plus a 'neutralized $X net deposits' line that is entirely invented. No order, position, or persisted-state effect \u2014 nothing in the execution path reads accountReturnPct or netExternalFlows (the drawdown breaker at strategy.ts:840-856 uses raw equity)."
  },
  {
   "uid": "trading-money-path:tsx-01",
   "verdict": "UPHELD_NARROWED",
   "corrected_severity": "P3",
   "confidence": "high",
   "repro": "The code shape is exactly as described, but the MCP transport is unreachable from any product surface, so this is latent config-gated code, not a live money path.\n\nCode is real: src/lib/alpaca.ts:307-349 `callMcp` wraps the whole MCP round trip in one try/catch and calls `fallbackFn()` on ANY throw (AbortSignal.timeout(ALPACA_MCP_FETCH_MS=8s, inflight-deadline.ts:21), `Alpaca MCP HTTP <5xx>` at :328, `body.error` at :332, or a JSON parse failure). src/lib/alpaca.ts:766 sets `client_order_id: input.refId` on the MCP args and :640 sets the identical `client_order_id: input.refId` on the REST fallback, so the retry collides. src/lib/alpaca.ts:977 `formatAlpacaOrderError` prefixes `HTTP <status>`, and src/lib/strategy-execution.ts:1279 (`if (placeError instanceof OrderValidationError || /\\bHTTP 4\\d\\d\\b/i.test(message))`) plus src/lib/strategy.ts:3979 short-circuit that 422 to `updateProposalStatus(proposalId, \"rejected_by_broker\", ...)` at strategy-execution.ts:1302, never reaching `reconcilePlacementError` (strategy-execution.ts:1342, defined :1948) whose refId->clientOrderId lookup (:1972-1975) would have adopted the live order.\n\nWhat blocks it today, step by step:\n1. No UI creates an `alpaca-mcp` account. app/console/settings/lib.ts:157-161 `connectAlpacaAccount` hardcodes `{ broker: \"alpaca\" }`; the only other `alpaca-mcp` reference in app/**/*.tsx is a display label (app/console/settings/brokers.tsx:41). iOS has no broker-connect flow at all.\n2. Even a hand-crafted `POST /api/connected-accounts {broker:\"alpaca-mcp\"}` (accepted at app/api/connected-accounts/route.ts:45) must pass `validateBrokerBaseUrl(body.baseUrl, ALPACA_ALLOWED_HOSTS)` at app/api/connected-accounts/route.ts:164-169. ALPACA_ALLOWED_HOSTS is src/lib/egress-guard.ts:31-35 = api.alpaca.markets / paper-api.alpaca.markets / data.alpaca.markets. None of those serves the MCP JSON-RPC endpoint, and src/lib/alpaca.ts:209-210 sets `this.mcpUrl = baseUrl`. POSTing a `tools/call` body at a REST host returns non-2xx, callMcp throws at :328 BEFORE Alpaca ever sees an order, and the REST fallback is then the FIRST submission of that refId \u2014 no duplicate, no 422. In the only configuration reachable without an env change the described failure literally cannot occur.\n3. To get a working MCP transport the operator must additionally set EGRESS_EXTRA_ALLOWED_HOSTS (src/lib/egress-guard.ts:42, 54-56, 76) to a self-hosted MCP gateway host, then create the account by direct API call, then activate it.\n4. STATUS.md ('2026-08-18 CURSOR \u2014 getAccounts 6s timeout blocks Run once after deploy') records the ops snapshot at 21:37:42Z: 'both accounts are Alpaca REST (not MCP)'.\n\nWith all of 1-4 satisfied, the repro is: MCP gateway forwards the POST to Alpaca, then stalls >8s (alpaca.ts:318) -> callMcp:346-348 falls back -> alpaca.ts:640 resubmits refId -> Alpaca 422 'client_order_id must be unique' -> alpaca.ts:977 -> strategy-execution.ts:1279 -> :1302 rejected_by_broker.",
   "correction": "Two overstatements. (1) The title and the STATUS.md headline ('MCP-fallback duplicate orders') say duplicate orders; the code cannot produce a duplicate at Alpaca \u2014 reusing the identical client_order_id is precisely what makes Alpaca refuse the second submission. The harm is misclassification and lost bookkeeping, not a double order. (2) 'no stop plan' overstates it: the orphaned position is still protected, because reconcileBrokerProtectiveStops and the synthetic monitor enumerate positions from `gateway.getEquityPositions` (src/lib/synthetic-stops.ts:248-250) and pass them at :443, so a position the app never booked still gets a broker/synthetic stop armed. Only the per-position LLM stop PLAN (getStopPlans, filtered by live basis at synthetic-stops.ts:272-274) is missing. (3) Severity: P1 assumes a live path. There is none \u2014 no UI, and the built-in host allowlist makes the failure impossible without an EGRESS_EXTRA_ALLOWED_HOSTS change plus a hand-crafted API call. P3.",
   "blast_radius": "Zero today: production runs two Alpaca REST accounts (STATUS.md ops snapshot 2026-08-18 21:37:42Z), and no UI can create an alpaca-mcp account. If the owner ever stands up a self-hosted Alpaca MCP gateway and whitelists it via EGRESS_EXTRA_ALLOWED_HOSTS, each MCP timeout (8s) on an approval can leave one real, possibly filled entry \u2014 at this app's sizing typically a few hundred to a few thousand dollars of notional, e.g. 4 shares of a ~$470 name \u2248 $1.9k \u2014 recorded as 'Rejected by broker': no fill_event row, no lot in the ledger, and that notional never consumed against maxDailyNotional/maxDailyOrders, so the next order can exceed the daily cap by exactly that amount. Frequency is bounded by MCP-gateway flakiness, once per affected approval. The position itself is not left unprotected."
  },
  {
   "uid": "trading-money-path:tsx-02",
   "verdict": "UPHELD",
   "corrected_severity": "P1",
   "confidence": "high",
   "repro": "Every step runs on default settings on current main.\n1. Bracket with a take-profit leg is attached by default: src/lib/defaults.ts:115 `brokerBracketsEnabled: true`; src/lib/defaults.ts:38 `takeProfitPct: 20`; src/lib/strategy.ts:7059 (`bracketsEnabled && brokerSupportsBrackets && canUseWholeShareBracket && !isTradierMarket`) -> :7085 `const takePct = policy.riskRules?.takeProfitPct ?? 0` -> :7144 `next = { ...next, bracketTakeProfit: round2(bracketAnchorPrice * (1 + takePct / 100)) }`.\n2. It reaches Alpaca as a real OCO leg: src/lib/alpaca.ts:613 `isBracket = !!(input.bracketTakeProfit || input.bracketStopLoss)`, :725-727 `orderOptions.order_class = \"bracket\"; orderOptions.take_profit = { limit_price: ... }`. Each leg is independently listed in the plain order list \u2014 stated in this repo's own comment at src/lib/alpaca.ts:873-875 \u2014 and `getEquityOrders` pages `status:\"all\"` (alpaca.ts:501-517), so the leg is returned.\n3. Entry fills; Alpaca flips the TP limit leg held->new and bumps updated_at. The repo encodes exactly this transition as expected behavior: test/stale-limit-orders.test.ts:76-114 ('alerts a bracket exit leg once it activates, measured from activation not creation') asserts an activated leg with state 'new' IS listed.\n4. The scan runs every tick for every broker-backed account with NO systemState gate: src/lib/scheduler.ts:882-901 `if (brokerGateway && !staleExitInFlight.has(key)) { ... journalLane(\"stale-limit-scan\", ...) ... autoRemediateStaleExitOrders(...) }`; brokerGateway is set at scheduler.ts:847 purely from `executionState.submitsBrokerOrders`.\n5. It is listed as stale: src/lib/stale-limit-orders.ts:9 `LIMIT_ORDER_TYPES = new Set([\"limit\", \"stop_limit\"])`, :33-34 type+working-state filters, :40 age measured from `order.updatedAt`, :49-50 `ageMinutes >= thresholdMinutes` with the threshold from src/lib/defaults.ts:159 `staleLimitOrderMinutes: 15`.\n6. It is enqueued for cancel-replace: src/lib/order-replacement.ts:749 `if (input.policy.autoRemediateStaleExits !== false)` (default true, src/lib/defaults.ts:160); :754-757 the ONLY filters are `side !== \"sell\" && side !== \"cover\"` and `state === \"held\"` \u2014 a just-activated leg is 'new', so it passes; :787-791 `INSERT INTO order_replacements ... 'cancel_requested'`.\n7. The pump's re-checks do not save it: src/lib/order-replacement.ts:333-337 re-checks type only against MARKET_REPLACE_TYPES, :339-345 re-checks 'held' only, :348-364 checks only that the broker position backs the quantity \u2014 a bracket TP leg is by construction fully backed. There is no orderClass / legs / clientOrderId-provenance check anywhere (EquityOrder.orderClass exists, src/lib/types.ts:939, mapped at alpaca.ts:942, and is never read here).\n8. Cancel then MARKET: order-replacement.ts:377 `await input.gateway.cancelEquityOrder(...)`, then :452-459 `const marketOrder: EquityOrderInput = { ..., type: \"market\", quantity: remainingQuantity, timeInForce: \"gfd\" }` and :477 `placeEquityOrder({ ...marketOrder, refId: row.replacement_ref_id })`.\nAccount narrowing (see correction): src/lib/order-replacement.ts:748 `liveNeedsHuman = executionState.mode === \"broker/live\" && policy.requireTypedConfirmation !== false`, and src/lib/defaults.ts:70 `requireTypedConfirmation: true`, so a default-configured LIVE account only writes a `stale_exit_auto_remediation_deferred` audit (:760-767). The path EXECUTES on broker/paper (src/lib/execution-mode.ts:68,75) and on any live account whose owner turned typed confirmation off.",
   "correction": "One narrowing and one nuance. Narrowing: on a live account left at the default `requireTypedConfirmation: true` (defaults.ts:70) the remediation is deferred, not executed (order-replacement.ts:748, 758-768) \u2014 so the finding's implied 'the owner's Roth' exposure requires the owner to have turned typed confirmation off, which the product supports as a one-toggle preference. Today the executing surface is the broker/paper account. Nuance: the finding's 'Only the live+typed-confirm defer stands between the owner's Roth and this' is correct as written but understates that the paper account is fully exposed right now. The 'owner-placed GTC sells' half of the claim is equally correct \u2014 nothing in the enqueue or the pump checks whether the app placed the order. I could not check prod audit rows for `stale_exit_auto_remediated`, but the code path needs no unusual state.",
   "blast_radius": "Every bracket-entered long is force-closed at MARKET roughly 15 minutes after its entry fills, wiping out both the 20% take-profit and the 8% stop the same bracket installed (they are one Alpaca OCO group), and it repeats for every bracketed entry the strategy opens. On the paper account that is not cash, but it silently voids the entire take-profit design and corrupts every P&L, win-rate and learning statistic derived from those trades \u2014 the owner reads a strategy that never actually ran. On a live account with typed confirmation turned off, the same sequence liquidates the real position at whatever the market is 15 minutes after entry: at this app's sizing that is a single position of roughly one to a few thousand dollars of notional per occurrence, once per bracketed entry per day, plus the market-vs-limit slippage on each forced exit. Owner-placed GTC limit sells and manual profit trims on the same account are destroyed identically \u2014 cancelled and converted to a full-size market sell."
  },
  {
   "uid": "trading-money-path:tsx-03",
   "verdict": "UPHELD",
   "corrected_severity": "P1",
   "confidence": "high",
   "repro": "1. iOS submits the command: ios/SocraticTrade/ProposalsView.swift:118 and :156 submit `\"proposal.approve\"`; the operation id is :167.\n2. Server runs it without wrapping the outcome: src/lib/mobile-api.ts:965-968 `case \"proposal.approve\": return executeProposal(String(payload.proposalId), command.userId, { liveConfirmation: ... })`.\n3. executeProposal RETURNS non-placement outcomes instead of throwing \u2014 its own signature is `Promise<{ status: string; ... }>` (src/lib/strategy-execution.ts:239-246) and the return sites are src/lib/strategy-execution.ts:298 `{ status: \"busy\" }` (strategy lock held by a running cycle), :498 / :766 / :1113 / :1136 / :1251 / :1302 `blocked`, :379 / :471 / :729 / :829 / :1027 `proposed`, :1338 / :1394 `not_placed`, :1205 / :1319 / :1384 / :1403 / :1417 / :1430 / :1473 `error`.\n4. Any resolved value is stamped succeeded: src/lib/mobile-api.ts:1077-1079 `const result = await runCommand(command); return toPublicMobileCommand(finishCommand(command, \"succeeded\", result));`.\n5. iOS never inspects the result: ios/SocraticTrade/MobileModels.swift:511-520 `struct MobileCommand` has id/commandType/status/error/timestamps and NO `result` field, even though the server ships one (src/lib/mobile-api.ts:292 `result: command.result`). `didFail` is :527 `status == \"failed\" || status == \"cancelled\"`, so it is false here.\n6. Green check: ios/SocraticTrade/MobileStore.swift:214-215 `case \"succeeded\": return .succeeded(action: action)`; ProposalsView.swift:481 `return action == .approve ? \"Approved \u2014 waiting for desk refresh.\" : ...`; :490 `case .succeeded: return \"checkmark.circle.fill\"`; :496 `case .succeeded: return AppPalette.positive`. MobileStore.swift:425-433 only writes a `proposalNotices` failure banner when `command.didFail`.\n7. Web parity confirmed: app/console/components/approval-card.tsx:347-366 `finish()` branches on `result.status` and toasts 'Blocked at approval time' / 'Approval is still busy' with the reasons.\nMost likely trigger in practice: the owner taps Approve on the phone while the scheduled autonomous run holds the strategy lock -> strategy-execution.ts:297-298 returns `busy` immediately -> phone shows a green 'Approved \u2014 waiting for desk refresh.' on a card that is still Pending, and nothing was ever sent to the broker. No test pins this behavior \u2014 test/mobile-api.test.ts:50-64 only exercises payload redaction for proposal.approve.",
   "correction": "'The only truth is buried in Activity' is overstated for most of the listed statuses. The blocked branch fires `sendNotification({ type: \"block\", ... })` (src/lib/strategy-execution.ts:1243-1250, and again at :1294-1301), the 4xx/rejected branch fires `type: \"run_failed\"` (:1305-1315), the lease-lost not_placed branch fires one at :1330-1334, and the `proposed` re-approval branches fire `type: \"pending_approval\"` (:371-378, :1016-1025). So on those paths the owner does get an out-of-band push if notifications are configured. The genuinely silent case \u2014 green check, no notification, no state change \u2014 is `busy` (strategy-execution.ts:297-299, which returns before any notification), which is also the most frequent one. That narrows the 'invisible' claim but not the defect: the phone still affirmatively says 'Approved' when nothing was placed. This finding is also a strict duplicate of api-contract-web-ios:api-02 (same file:line, same root cause), so it should not be counted twice in the backlog.",
   "blast_radius": "No unintended order is ever placed \u2014 the harm is the inverse: an order the owner believes is live never reached the broker. Worst realistic case: the owner approves a protective Risk-Exit proposal from the phone while a scheduled run holds the strategy lock, sees the green 'Approved \u2014 waiting for desk refresh.', stops watching, and the position keeps running unhedged because executeProposal returned `busy` at strategy-execution.ts:298 and no notification was sent. On a losing name that is the full remaining downside of one position (typically one to a few thousand dollars of notional at this app's sizing) instead of the intended exit. Frequency: any phone approval that lands during an in-flight autonomous cycle, i.e. potentially several times a trading day on an active account."
  },
  {
   "uid": "api-contract-web-ios:api-02",
   "verdict": "UPHELD",
   "corrected_severity": "P1",
   "confidence": "high",
   "repro": "Identical to trading-money-path:tsx-03 for the approve path, plus two extra surfaces that I confirmed independently.\nApprove path: src/lib/mobile-api.ts:965-968 returns `executeProposal(...)` verbatim; executeProposal returns `{status:'busy'}` (src/lib/strategy-execution.ts:298), 'blocked' (:498, :766, :1113, :1136, :1251, :1302), 'not_placed' (:1338, :1394), 'error' (:1319, :1403, :1417, :1430, :1473) and 'proposed' (:379, :471, :729, :829, :1027) WITHOUT throwing \u2014 its declared return type is `Promise<{status: string; ...}>` at :239-246. src/lib/mobile-api.ts:1077-1079 stamps any resolved value 'succeeded'. ios/SocraticTrade/MobileModels.swift:511-520 has no `result` field (the server sends one at src/lib/mobile-api.ts:292), :527 `didFail` is false for 'succeeded', so ios/SocraticTrade/MobileStore.swift:214-215 returns .succeeded and ProposalsView.swift:481/490/496 paint the green checkmark 'Approved \u2014 waiting for desk refresh.'. Web handles the same statuses correctly at app/console/components/approval-card.tsx:347-366.\nstrategy.run_once: src/lib/mobile-api.ts:952-955 `const result = await runStrategyOnce(...); if (result.status === \"failed\") throw ...; return result;` \u2014 only 'failed' throws. src/lib/strategy.ts:526 emits `skipped_market_closed`, :637/:645 `skipped_broker_unhealthy`, :676 `skipped`, :698/:757 `skipped_budget`. All of those become mobile status 'succeeded' with the explanatory `summary` living only in `result`, which iOS cannot decode.\norder.cancel: src/lib/mobile-api.ts:998-1005 returns `{ ok: true, status: result.state, orderId, symbol?, dustWarning? }`; the dustWarning likewise rides in `result` and is dropped by the Swift model.\nNo test pins the current behavior: test/mobile-api.test.ts:50-64 covers only payload redaction for proposal.approve.",
   "correction": "Same narrowing as tsx-03: 'Only web /api/proposals/:id/approve surfaces reasons' is too strong for the blocked/error/proposed statuses, which do fire a push (`sendNotification` type 'block' at src/lib/strategy-execution.ts:1243-1250 and :1294-1301, 'run_failed' at :1305-1315, 'pending_approval' at :371-378 and :1016-1025). The status that is both silent and green-checked is `busy` (:297-299), which returns before any notification. Also note this finding and trading-money-path:tsx-03 are the same defect reported by two lanes \u2014 one fix closes both; do not budget them separately. The recommendation to make runCommand throw for non-placement statuses should be weighed against the owner ruling that guardrails are advisory: 'blocked' and 'proposed' are legitimate, non-error outcomes, so the better shape is the finding's second option (decode `result.status`/`reasons` on iOS and label the banner honestly) rather than converting an advisory refusal into a hard 'failed'.",
   "blast_radius": "No wrong order is placed; the failure is a missed intended action reported as a completed one. Worst realistic case: a phone approval of a protective exit returns `busy` because the scheduled run holds the strategy lock (strategy-execution.ts:298), the phone shows a green 'Approved', and the position is left unhedged \u2014 the full remaining downside of one position, typically one to a few thousand dollars of notional at this app's sizing, per occurrence. Secondary, lower-cost cases: a `strategy.run_once` tapped outside market hours or after the daily LLM budget is spent reports success with no proposals and no reason shown (mobile-api.ts:952-955 vs strategy.ts:526/698/757), and an order.cancel that leaves a dust remainder reports success with the dustWarning silently dropped (mobile-api.ts:1004). Frequency: any phone approval during an in-flight autonomous cycle, plus every skipped run_once."
  },
  {
   "uid": "market-data-integrity:mdi-01",
   "verdict": "UPHELD",
   "repro": "market.ts:102 DEFAULT_CACHE_TTL_MS=5min feeds marketCacheTtlMs() -> fetchNasdaqScreener (market.ts:1312-1352) caches screener rows via expiresAtRespectingMarketClose(now, ttlMs) (market.ts:1352). expiresAtRespectingMarketClose (market-hours.ts:305-311) calls isWeekendOrHolidayClosureAhead(nowMs) (market-hours.ts:283-288), which returns true whenever TOMORROW's calendar day isn't a trading day -- true for ALL of Friday, any hour, not just after close. Empirically verified with tsx against the actual repo module: a screener write at Fri 2026-08-21 10:00 ET (naive 5-min expiry 10:05 ET) is forced to expire Mon 2026-08-24 09:30 ET instead. Same function gates history.ts:243/321 (daily OHLC), data-providers.ts:2090/3888/4105 (6h enrichment incl. Massive short-interest, ROIC), and macro.ts's macro cache -- all confirmed wired via grep, not dead code. test/market-hours.test.ts:194-198 only pins a Friday-2pm/6h-TTL case where the naive expiry (8pm) already falls after close, so it never exercises -- and never catches -- the production 5-min screener TTL case where the naive expiry is still deep inside the live session.",
   "correction": "None on the core mechanism -- the bug is real and reproduces exactly as described. Only a severity nuance: execution price itself is NOT corrupted (that comes from a separate live broker/quote-cascade read at order time), so this is a decision-quality staleness bug, not a fabricated-price or duplicate-order bug.",
   "blast_radius": "Every Friday (and every day before a full-close holiday), after the first ~5-minute screener refresh of the day, Market Scan ranking/momentum/breadth and 6h enrichment freeze until Monday's open (or the next trading day for a holiday) -- confirmed live and current: STATUS.md's 2026-08-18 top-of-list explicitly still carries 'Friday cache freeze (mdi-01)' as open, unfixed. A strategy run late Friday can propose/rank a symbol off its 10am state that's no longer true by 2pm. No direct fabricated fill or duplicate order -- worst realistic dollar exposure is one mis-ranked opening proposal at the account's normal per-order notional cap (bounded, not unlimited), recurring weekly.",
   "confidence": "high"
  },
  {
   "uid": "market-data-integrity:mdi-02",
   "verdict": "UPHELD",
   "repro": "history.ts:241 fetchDailyOHLC short-circuits on isBarSeriesFresh(localBars, 3, now) (history.ts:115-126, 3-day/72h window measured from the last bar's DATE-STRING parsed at UTC midnight). Empirically verified with tsx against the real module: with a last bar dated Monday 2026-08-17, isBarSeriesFresh returns true at Tue 10:00 ET AND at Wed 15:00 ET, only flipping false at Wed ~21:00 ET (Thu 00:00 UTC). So a Monday write suppresses ANY live refetch for the entirety of Tuesday and Wednesday -- those two sessions' real closes are never fetched into history_cache_eod (history.ts:161-189, 318/327) until Thursday's first call. Direct real-money-path consumers confirmed by grep: strategy.ts:1274/1286/2338/2396 (technical-signal generation feeding actual trade proposals), benchmark.ts:345 (SPY comparison), alpaca.ts:587 (pre-trade notional review 'last close' fallback). Confirmed the cited #2557b fix (docs/rollouts/2026-08-08-data-integrity-flow-sanity-ledger.md) only added a BENCHMARK_STALE_GRACE_DAYS=5 gate in benchmark.ts on top of whatever fetchDailyOHLC already returned -- a 2-day gap (well under 5 days) sails through that gate silently. No test pins the multi-day-skip scenario; test/flatfile-range.test.ts only pins the raw single-comparison age math, not the Tue/Wed-skipped-entirely behavior.",
   "correction": "None -- matches the finding closely, including the specific #2557b-only-covers->5-day-lag detail.",
   "blast_radius": "Twice a week (Tue+Wed after any Monday close), daily-bar-derived technical indicators feeding strategy.ts's real proposal generation, the SPY benchmark comparison, and the /api/history chart silently run on a 2-session-stale series with no staleness flag anywhere in the chain (the only staleness gate that exists, in benchmark.ts, doesn't trip until 5 days). Not a fabricated price, but a real signal-integrity gap that can steer an actual buy/sell technical signal for up to ~40% of the trading week on a rolling basis.",
   "confidence": "high"
  },
  {
   "uid": "market-data-integrity:mdi-03",
   "verdict": "UPHELD",
   "repro": "data-providers.ts:2522 parseNasdaqQuoteInfo returns bare price/intradayChangePct/volume numbers with NO asOf/fetchedAt/fieldObservations at all. NasdaqQuoteEnrichmentProvider.enrich (data-providers.ts:2588-2624) caches that record under `nasdaq-quote:${symbol}` for ttlMs() = NEWS_CACHE_TTL_MS, default 6h (line 412/475-476) -- the SAME blanket fundamentals TTL the code explicitly documents (line 2448 comment) is wrong for quote-family fields, contrasted with AlpacaSnapshotEnrichmentProvider's dedicated ~30s alpacaSnapshotTtlMs() (line 2450, 488-491) built specifically to fix this class of bug for Alpaca. In the cascade aggregator, takeScalar (data-providers.ts:1571-1590) stamps every field lacking its own asOf with cascadeFetchedAt = new Date().toISOString() computed once at the top of the run (line 1534) -- so a 6h-stale cached nasdaq-quote volume gets stamped as observed 'right now'. evidence-facts.ts:101/107 confirms 'volume' uses mode:'metadata', priorities starting with 'recency'; arbitrateFieldObservation (evidence-facts.ts:132-155) sorts candidates by that fabricated recency FIRST, so nasdaq-quote's fabricated 'now' timestamp -- which by construction is always >= any genuinely real timestamp like Alpaca's dailyBar.t/latestTrade.t (parseAlpacaSnapshot, line ~2495, which DOES stamp a real asOf) -- wins. Critically, the winner's VALUE overwrites base.volume (data-providers.ts:1673-1676), not just metadata/tooltip. Confirmed real-money consumers: policy.ts:585 and strategy-risk.ts:697 compute dollarVol = price*volume feeding the maxOrderPctOfAdv market-impact guardrail that can reject/permit an opening order.",
   "correction": "The finding's secondary clause about price/intradayChangePct 'falling to the hours-old cached value... when Alpaca times out' is technically via a different mechanism (those two fields use REGISTRATION_PRIORITY_FIELDS/registration-order mode, not recency arbitration -- evidence-facts.ts:93-98) -- they fall back to the stale cache simply because it's the only remaining candidate, not because of the fabricated-timestamp trick. The core headline claim (volume arbitration corrupted by a fabricated recency stamp) is fully verified as described.",
   "blast_radius": "On every enrichment cascade run with Alpaca connected (the default broker) -- i.e. routinely, not an edge case -- the ADV/market-impact guardrail on real opening orders is computed against a volume figure that can be up to 6 hours stale while always LOOKING like the freshest candidate available. Worst realistic outcome: the ADV cap under- or over-states true current liquidity, either wrongly blocking a legitimate order or wrongly approving one that represents a larger share of TRUE current volume than the policy intends (real market-impact/slippage risk on the fill) -- bounded by the account's normal per-order notional cap, not unlimited, but a genuine risk-control integrity gap on a live money path.",
   "confidence": "high"
  },
  {
   "uid": "market-data-integrity:mdi-04",
   "verdict": "UPHELD",
   "repro": "market.ts:355-370 customSymbolsMissingFromScreener is a routine path (any watchlist/position symbol the Nasdaq delayed screener didn't return, e.g. OTC/coverage gaps) -> fetchQuoteOnlyMarketQuotes -> toQuoteOnlyMarketQuote (market.ts:1524-1560) hardcodes asOf: new Date().toISOString() (line 1548) and never reads the `quote` parameter's own .asOf field, even though YahooFinanceQuote.asOf is populated from the real regularMarketTime (yahoo-finance.ts:76-85, 185-194) sitting right there unused. This MarketQuote flows into context.marketScan.quotesBySymbol (market.ts:727, quotesBySymbol(mergedRanked)). policy.ts:404-413's opening-order staleness gate reads that quote via quoteAgeSecForStalenessGate (quotes-cascade.ts:73-85), computing ageSec off the fabricated asOf -- always ~0s old regardless of Yahoo's true quote delay -- so isStale is essentially always false, silently defeating the gate's market-to-limit-order conversion. This directly contradicts the adjacent code's own documented contract (policy.ts:401-402 comment: 'Timestamps are read from the run's MarketScan -- never fabricated'; quotes-cascade.ts:47-48: 'Missing/unparseable asOf is NEVER treated as fresh'). A second, independently confirmed instance: quotes-cascade.ts:397 stamps ROIC's company-profile price (not a real-time tick) with the same asOf: new Date().toISOString(), defeating isQuoteFresh (quotes-cascade.ts:52-61) the same way. staleness-gate.test.ts only exercises the gate math against hand-built fixtures with controlled asOf values -- it never calls toQuoteOnlyMarketQuote or the ROIC level, so nothing pins the fabrication as intended.",
   "correction": "None -- the CBOE and mergeGroupedBarData/VWAP sub-claims were not independently re-verified (lower priority, VWAP-only field, not re-checked against current file paths -- cboe.ts wasn't found at that exact path in a quick grep), but the two claims that were verified in full (Yahoo quote-only fallback and ROIC) are each independently sufficient to uphold the finding at its stated severity.",
   "blast_radius": "For any opening BUY/SHORT on a symbol priced only via the quote-only Yahoo fallback or the ROIC last-resort tier (routine for names outside the Nasdaq screener's coverage), a genuinely stale/delayed print can be submitted as a live MARKET order instead of the protective LIMIT-at-intended-entry the gate exists to provide. Worst realistic outcome: the order fills at a materially different price than the stale print implied, for a single proposal's notional -- bounded by the account's normal per-order cap, not unlimited, but a real execution-price protection that is silently inert exactly on the fallback path most likely to carry a stale print in the first place.",
   "confidence": "high"
  },
  {
   "uid": "llm-agent-architecture:llm-01",
   "verdict": "UPHELD",
   "repro": "src/lib/strategy.ts:5870 defines `exitPlan: { type: [\"string\",\"null\"], ... }` inside the Bull proposal item's `properties`, but `BULL_PROPOSAL_REQUIRED_KEYS` (strategy.ts:6653-6669, 16 entries) never lists it, and the item schema sets `additionalProperties:false` (5849) with `required: [...BULL_PROPOSAL_REQUIRED_KEYS]` (5851). Every Bull request built via buildLlmRequestBody (strategy.ts:5880-5885, 5926-5941, run on primary AND every llmFallbackModels entry) reaches llm-call.ts's openAiChatResponseFormat (494-516: strict json_schema for any provider that is not gemini/deepseek) or openAiResponsesTextFormat (521-530: strict always for native OpenAI). OpenAI's strict-mode contract requires every `properties` key to appear in `required` (nullability is expressed via the type-union, which the code already does correctly for every OTHER optional field) -- omitting one key while `additionalProperties:false` is set is rejected at the API layer before generation starts, which explains the fast (881ms) HTTP 400 'Provider returned error' the owner captured for `openai/gpt-5.6-terra` (docs/rollouts/2026-08-18-green-400-failover.md, STATUS.md:122). PR #2831 (581467e1, merged the day before this review) added 400 to the failover-eligible set and demoted terra from first pick (src/lib/model-rotation.ts:79-90, UNSERVABLE_OPENROUTER_FIRST_PICKS) but explicitly did NOT diagnose the 400's root cause ('Zero-Code Findings: ... the 400 is an upstream Provider returned error, not a missing slug' -- rollout doc line 68). No code path retries the same model with a relaxed/non-strict schema on a 400; it only fails over to the next configured model (or fails the run entirely if none remain). test/strategy-hardening.test.ts:868-870 only asserts stopPlan/tradeThesisTag membership and key-uniqueness -- nothing checks required \u2287 properties, so nothing pins the current (buggy) list. `git log --all -S\"BULL_PROPOSAL_REQUIRED_KEYS\"` shows no fix commit past #2740 (which added exitPlan to properties/projection on 2026-08-16 but never touched the required array).",
   "correction": "None on the core mechanism -- verified line-for-line. One nuance the register's evidence slightly overstates: I could not independently confirm the terra 881ms incident IS this bug (vs. some other undiagnosed OpenAI-side rejection) since OpenRouter's error text is generically 'Provider returned error' for many distinct causes (a sibling incident the same week, gpt-5.4-nano, was diagnosed as a completely different cause -- missing max_completion_tokens/require_parameters, already fixed in 6429d984c). What IS independently provable from the code alone, regardless of that specific incident's true cause, is that the schema bug is real and would 400 on its own: any OpenAI-family model (native OpenAI key or OpenRouter openai/* routing) hit with the current Bull strict schema fails 100% of the time.",
   "blast_radius": "Every strategy run whose resolved Green (or Red) endpoint is an OpenAI-family model (gpt-5.6-terra/sol/luna, gpt-5.4-mini/nano, gpt-4o, or any user-configured OpenAI-native key) gets an automatic HTTP 400 and fails over to the next configured model/seat -- extra latency (~1s) and one wasted paid API call per occurrence, silently, forever, until the schema is fixed. Worst realistic case: a user who explicitly configures `llmModel` to an OpenAI model with `llmFallbackModels` empty (the default) gets ZERO trade proposals from every single strategy run -- Green fails closed with no Red review and no order ever gets proposed for that account, indefinitely, with no error surfaced beyond a generic 'Provider returned error' that (per the receipts) is easy to misattribute to model unavailability rather than a request-shape bug -- which is exactly what happened in #2831's own investigation.",
   "confidence": "high"
  },
  {
   "uid": "llm-agent-architecture:llm-02",
   "verdict": "UPHELD",
   "repro": "src/lib/strategy.ts:449 resolves `connectedAccountId = options.connectedAccountId ?? getPolicy(userId).connectedAccountId` -- an explicit per-run target, NOT the console-active account -- and scheduler.ts:169-178 `runScheduledStrategyAndMaybeTune` calls `runStrategyOnce(userId, { connectedAccountId })` for EACH connected account in turn without ever flipping the global 'active' pointer first, so a Paper run and a Roth run against the same user can be in flight (or run sequentially) while the console's is_active=1 account stays whatever the owner last clicked. strategy.ts:490 resolves the run's real account via `getConnectedAccount(connectedAccountId, userId)` and threads it through as `activeAccount`/`input.activeAccount` into `deriveVenueContract(input.policy, input.activeAccount)` (strategy.ts:4953) and `getStrategyPrompt(userId, connectedAccountId)` (strategy.ts:2223) -- both correctly account-scoped for the Bull (Green) path. red-team.ts:191-203, by contrast, calls `getActiveConnectedAccount(userId)` (the global console-active row, db-api-keys.ts:1157) for `deriveVenueContract` and calls `getStrategyPrompt(userId)` with NO second argument, which falls back to the active account inside `resolveAccount` (db-profiles.ts:603, confirmed at 322-325 for the sibling getPolicy path) rather than the run's actual account. `debateProposal` IS called with the run-scoped `runPolicy` (strategy.ts:2055, 2516) which DOES carry the correct `connectedAccountId` (set at db-profiles.ts:522 `policy.connectedAccountId = account.id` when getPolicy is called with an explicit id, as strategy.ts:480 does) -- but red-team.ts never reads `policy.connectedAccountId`, so that correct value is available and simply unused.",
   "correction": "The register's evidence is accurate, but the worst-case is narrower than 'a wrong order gets placed': the SHORT-capability half of the bug has a downstream deterministic backstop -- policy.ts:493-501 re-checks `context.accountCapabilities?.shortSelling` (correctly account-scoped from the actual execution-gating context, not the console-active pointer) before any short order is actually built, so an illegal short cannot silently execute on an account that doesn't support it even if Red Team's early venue check used the wrong account. The STRATEGY-PROMPT-CONTAMINATION half of the bug has no such backstop: `getStrategyPrompt(userId)` at red-team.ts:203 feeds the reviewer another account's custom strategy directives verbatim, and that verdict (approve/approve-at-half/reject) has no downstream re-check against the correct account's own prompt.",
   "blast_radius": "With two-plus accounts connected under one user and scheduled concurrently (the exact live pattern this session's own STATUS.md shows -- Paper `PA33IDTHMFK9` and Roth both scheduled today) the Red Team veto on a risk-adding open can (a) auto-reject a legitimate short on the account under review with a nonsensical 'This <other account's broker> account cannot short' message and zero LLM call, purely because the console-active account differs and lacks short capability -- a missed trading opportunity, not a wrong order; or (b) run the adversarial review using an entirely different account's custom strategy prompt as context, producing an approve/reject/half verdict that does not reflect the account actually being traded -- a real risk-adding position (dollar size = whatever `finalizedSizing` computed for that proposal) can be approved or rejected on the wrong basis, though deterministic execution-time gates (notional caps, short-capability) still apply afterward.",
   "confidence": "high"
  },
  {
   "uid": "llm-agent-architecture:llm-03",
   "verdict": "UPHELD_NARROWED",
   "corrected_severity": "P2",
   "repro": "macro.ts:586-616 `pruneMacro` keeps only vix/fedFundsRate/dgs10Treasury/asOf (MACRO_ALWAYS_KEEP, line 587) plus any field whose value differs from `previous[key]`; every other field is pushed onto `omitted` (name only, no value). strategy.ts:4980-4988 calls this with `previousMacro = getInternalSetting(macroCacheKey)` where `macroCacheKey = \\`last_macro_sent:${input.userId}\\`` (userId-only, not account-scoped) and immediately calls `setInternalSetting(macroCacheKey, macro)` (line 4984) BEFORE the LLM request is built or sent (the request body isn't assembled until ~5880+), so even a failed/errored LLM call still marks the full macro snapshot as 'sent'. `macroeconomicData` (4985-4988) folds the survivors plus `unchangedSinceLastRun: macroOmitted` (bare names) into userContent, which is JSON.stringify'd into the actual Bull prompt and also spread verbatim into `adversaryContext` for Red Team (strategy.ts:6341-6346, `...userContent`). buildBullSystem's prompt instructions (strategy-prompts.ts:185, confirmed present) tell the model to read hyCreditSpread alongside the treasury curve for recession risk -- on any run after the first same-day run where those fields haven't moved, the model receives only the field's NAME, never its number, and each LLM call is stateless (a rotating/failed-over/different model on the next call has literally no memory of the prior run's actual reading), so 'unchanged' carries zero usable information.",
   "correction": "This is real and unaddressed, but it is a prompt-quality/context-completeness defect, not a deterministic execution bug -- it never causes a wrong order, duplicate order, or cross-account leak on its own; it only degrades the INPUT quality of one LLM judgment call among many. The codebase already treats several other missing/partial-context conditions as normal, fail-open, non-blocking behavior by design (upcomingEconomicEvents, marketSignals, etc. at strategy.ts:5000-5010 are explicitly 'failure-tolerant -- never blocks a run'), so this class of defect is consistent with a P2, not the P1 tier reserved elsewhere in this batch for cross-account leaks and total-failure schema bugs. It is also worth noting `pruneMacro` was original, owner-authored design (commit 41ede025a, 2026-06-16, explicit intent: 'delta-only macro' token savings) -- the flaw is a foreseeable-but-unaddressed side effect of that design meeting stateless LLM calls, not a regression from a different feature.",
   "blast_radius": "On any run after the first same-calendar-day run for a user (multiplied further since the cache key is user-scoped and shared across ALL that user's accounts), the Bull AND Red Team prompts omit the actual numeric values of most FRED macro fields (hyCreditSpread, dgs2Treasury, CPI, unemployment, usdIndex, wtiOil, corePCE) even though the system prompt explicitly instructs reading some of them for recession-risk judgment. Worst realistic outcome: a real risk-adding position gets opened/sized by an LLM reasoning about macro risk with an incomplete numeric picture during a deteriorating credit backdrop it cannot actually see the numbers for -- a quality-of-judgment risk on real position sizing, not a mechanical order-placement bug.",
   "confidence": "medium"
  },
  {
   "uid": "llm-agent-architecture:llm-04",
   "verdict": "UPHELD_NARROWED",
   "corrected_severity": "P2",
   "repro": "src/lib/chat/coach-learning.ts:8-9 documents 'Explicit strategy directives queue as strategy-directive pending (human approval required before they touch the strategy prompt).' learning-review.ts's `applyLearningReviewVerdicts` (640-663) calls `applyApprovedPending(pending, nowIso)` on a 'keep' verdict for ANY `learned_context_pending` row with no `riskTier` exclusion (confirmed by grep: no 'strategy-directive' branch/filter anywhere in learning-review.ts). `applyApprovedPending` (learned-context/store.ts:397-405) for `riskTier === 'strategy-directive'` calls `getStrategyPrompt(pending.userId)` + `mergeStrategyDirectiveBlock` + `setStrategyPrompt` -- the identical mutation path the human-approve UI route uses, with no distinguishing tag or reduced trust marker on the resulting text. defaults.ts:89-90 confirms `learningReviewEnabled: false` (feature is OFF until the owner explicitly turns it on) and `learningReviewMode: 'decide'` (once turned on, 'decide' -- the auto-apply mode -- is the sub-default). prompt-safety.ts:224-225 (TRUSTED_OWNER_SOURCES) and strategy.ts:4933-4934 (byte-for-byte preservation) confirm the strategy prompt is the one fully-trusted region with no containment scan applied to whatever lands in it.",
   "correction": "The register's framing treats this purely as a contract violation/bug, but git history shows this is a deliberate, owner-made design choice, not an accidental gap: commit 3be0c041d (2026-07-09, the feature's introduction) originally shipped 'decide' as 'owner opt-in' with the DEFAULT mode being 'annotate' (audit-only, nothing mutated). Commit 6f1aaf87d (2026-07-10, one day later, titled 'decide-default', authored by the owner/MONET seat) explicitly flipped the default to 'decide'. This is exactly the CLAUDE.md-endorsed pattern ('any rule the app enforces gets a user-controlled off-switch... If the owner set it, follow their intent and let them change or override it') -- the owner already has, and has already exercised, full control over this exact toggle. The parent feature is ALSO off by default (`learningReviewEnabled: false`), a double opt-in. What genuinely survives as a defect: (1) the coach-learning.ts comment is now stale/inaccurate given the owner's later default change and should be corrected to describe both modes; (2) the reviewer prompt applies identical scrutiny (the same three tests) to a strategy-directive promotion (which becomes fully-trusted, unscanned prompt text) as to a risk-tier promotion (soft advisory data only) -- no differentiated caution for the higher-trust-tier mutation, which is a real hardening gap worth a targeted fix even though the overall feature-on/mode-decide state is owner-intended.",
   "blast_radius": "Only bites when the owner has explicitly enabled `learningReviewEnabled` (off by default) and left `learningReviewMode` at its 'decide' default (or chosen it deliberately), AND a strategy-directive candidate is sitting in the pending queue with corrupted or subtly-adversarial content (most plausible vector: content ingested from a fetched URL via coach-learning.ts's SSRF-safe fetch, or crafted phrasing in a chat message). If all three hold, the once-daily reviewer LLM -- not a human -- can promote that text verbatim into the ALWAYS-TRUSTED strategy prompt, after which every subsequent Green/Red proposal call for that account treats it as owner intent with no scan or reduced-trust marker, until a human notices and removes it via Strategy Studio. No direct order-placement or cross-account effect -- it is a slow-moving, once-per-day, narrow-precondition trust-boundary risk rather than an active money-path bug.",
   "confidence": "medium"
  },
  {
   "uid": "ios-hig-ux:ios-01",
   "verdict": "UPHELD",
   "repro": "AccountSettingsView is a plain Form (HomeView.swift:754-790, presented as a sheet from HomeView.swift:45) containing GuardrailTighteningSection() (HomeView.swift:771 -> PolicyTightening.swift:375-388 -> GuardrailEditRows() at :392) with NO store.error rendering anywhere in the Form. Real failure paths: (1) PolicyTightening.swift:558-566 requestCap() sets store.error when the bound cap changed while the menu was open; (2) PolicyTightening.swift:598-604 submitCustomAmount() sets store.error on an invalid custom value; (3) MobileStore.swift:668-676 reconcileTrackedCommands() sets store.error when a submitted policy.patch/account.activate later resolves 'failed'. The ONLY renderer of store.error is SnapshotScaffold (AppComponents.swift:566-583), which lives in the underlying TAB, invisible while the sheet covers it. Even on the Guardrails tab itself, GuardrailsView.swift's own InlineErrorBanner (line 18-23) is bound to a separate loadError (full-policy fetch only), not store.error -- so a cap-edit failure there still only surfaces via SnapshotScaffold at scroll-offset 0 while tightenCard (the edit controls) sits last, at the bottom (GuardrailsView.swift:11-27). MobileStore.swift:292 unconditionally clears error=nil on every successful load(), and MobileStore.swift:322 fires scheduleReload()->load() on every SSE event, so a shown error can be wiped within seconds by an unrelated background refresh.",
   "correction": "The finding's own cited evidence line (PolicyTightening.swift:623, typed-phrase mismatch) is unreachable in normal use since the alert's Confirm button stays .disabled until the typed text exactly matches (PolicyTightening.swift:427-431) -- narrow that one example, but the surrounding claim (money-adjacent edits fail silently) is independently confirmed via two other real code paths.",
   "blast_radius": "Owner adjusts a live-account guardrail (raise/lower a notional or %NAV cap, toggle Ask-First<->Autopilot) from Account & Settings; on a stale-cap race or a server-side command failure, they get zero visible feedback and may believe the change applied when it silently did not, running with unintended risk limits until they happen to notice on a later screen.",
   "confidence": "high"
  },
  {
   "uid": "ios-engineering:ios-01",
   "verdict": "UPHELD",
   "repro": "signOut() (MobileStore.swift:623-626) and confirmAccountDeletion() (MobileStore.swift:544-559) both funnel through clearLocalSession() (MobileStore.swift:628-650), which nils snapshot/isAuthenticated in memory but never calls UserDefaults.standard.removeObject(forKey: Self.cacheKey) -- grep across ios/SocraticTrade confirms only MobileStore.swift:139 (read) and :144 (write) touch that key, no remover exists anywhere. On the next cold launch, MobileStore.init (MobileStore.swift:145-153) reads the still-present blob via loadCachedSnapshot() and sets isAuthenticated = cached != nil = true purely from stale disk state, no cookie check. Online: load() computes shouldReportSessionExpiry = hasInitialized || isAuthenticated = true (MobileStore.swift:275), the 401 hits applyAuthAwareError (MobileStore.swift:680-687), and MobileAPIClient.swift:15 shows 'Your session expired.  Sign in again.' for a deliberate sign-out. Offline: load() throws .network (not .unauthorized), so the catch branch (MobileStore.swift:299-304) never calls clearLocalSession() again -- snapshot stays the stale cached blob and isAuthenticated stays true, so SocraticTradeApp.swift:146-147 opens SSE (store.startEvents()) on a dead session and MobileControlView renders the PRIOR account's holdings/orders/proposals as current.",
   "correction": "None -- matches the original claim closely; no fix is present anywhere on the branch (confirmed via full-repo grep for removeObject).",
   "blast_radius": "After sign-out or account deletion, a subsequent offline (or delayed) cold launch shows the PRIOR account's live portfolio/orders/positions as if current -- real risk of a user acting on stale account state, or a deleted account's financial data persisting readable on disk indefinitely. No order is misplaced by this bug alone, but the displayed 'whose money is this' truth is wrong.",
   "confidence": "high"
  },
  {
   "uid": "api-contract-web-ios:api-01",
   "verdict": "UPHELD",
   "repro": "GET /api/policy (app/api/policy/route.ts:42) returns getPolicy(userId) verbatim; TradingPolicy.riskRules: RiskRules (src/lib/types.ts:1188) nests stopLossPct/trailingStopPct/shortStopLossPct -- grepped app/api/policy/route.ts for any spread of riskRules to the top level (none found; only validation reads policy.riskRules.X). FullPolicy.init(from:) (ios/SocraticTrade/DeskModels.swift:372-374) decodes stopLossPct/trailingStopPct/shortStopLossPct from TOP-LEVEL CodingKeys (DeskModels.swift:383-388) -- no nested riskRules container exists anywhere in FullPolicy. GuardrailsView.swift:96-98 calls DeskCopy.percentPoints(policy.stopLossPct) etc.; since the key is never present, decodeIfPresent returns nil and percentPoints(nil) (DeskModels.swift:683-686) returns em-dash.",
   "correction": "None.",
   "blast_radius": "Every iPhone user, every time they open Guardrails, sees Stop Loss / Trailing Stop / Short Stop all showing an em-dash regardless of the actual configured value (e.g. a live 8% stop from DEFAULT_RISK_RULES). Display-only -- does not change actual stop/order behavior (server-side execution reads riskRules directly, not the iOS display) -- but risks the owner concluding a position is unprotected when it is not, or vice versa.",
   "confidence": "high"
  },
  {
   "uid": "api-contract-web-ios:api-03",
   "verdict": "ALREADY_FIXED",
   "repro": "ios/SocraticTrade/MobileAPIClient.swift:132-137 on the reviewed branch's current HEAD reads timeoutInterval = 50 with a comment stating the 35s server budget explicitly ('Client wait is longer so a late seed/200 is not dropped as a dead table'). `git show 1ca5073b7 -- ios/SocraticTrade/MobileAPIClient.swift` shows this was changed from 25->50 (comment from '20s' to '35s') as part of merged PR #2850. `git merge-base --is-ancestor 1ca5073b7 HEAD` confirms this commit is an ancestor of the reviewed branch's HEAD (5d453afef). app/api/scan/route.ts:22 INTERACTIVE_SCAN_BUDGET_MS=35_000 is unchanged, so the client now waits 50s -- 15s of slack beyond the server budget, exceeding even the finding's own recommended 45s fix.",
   "correction": "The finding accurately describes a real defect that existed in the code snapshot it was written against (and STATUS.md's 2026-08-19 entries confirm testers on the currently-SHIPPED TestFlight binary 1.0.68 are still hitting it in the field, since that binary predates PR #2850). But on the branch as currently reviewed, the client-side constant is already fixed. This should be reclassified from a code defect to a deployment/rollout gap (old TF build vs new main).",
   "blast_radius": "None on the reviewed code. On the older shipped TF 1.0.68 binary still in testers' hands, the blast radius remains as described: a legitimately slow (25-35s) scan aborts client-side and shows a generic network error instead of results -- a UX annoyance, not a money-safety issue (no order is placed from a timed-out scan).",
   "confidence": "high"
  },
  {
   "uid": "desktop-web-ux:dweb-01",
   "verdict": "UPHELD",
   "repro": "app/console/page.tsx:77-80 latest?.proposals?.slice(0,5).map(item => decisionFromProposal(`${latest?.runId}-${item.proposal.symbol}-${item.status}`, ...)) ?? snapshot.pendingProposals... -- real strategy.ts status literals (grepped every results.push in src/lib/strategy.ts) are exactly 'blocked' (e.g. line 3382), 'proposed' (3667), 'error' (3924), 'filled'/'placed' (4062/4147) -- never 'pending' or 'failed'. page.tsx:1025 chipTone maps status==='blocked'|'failed'|'not_placed' -> warn, status==='pending' -> accent, ELSE -> 'pos' (green) -- so 'error' and 'proposed' both fall to green, while labels.ts:59-72 maps error -> 'Failed' text, producing a green chip captioned '... Failed'. page.tsx:1011 isPendingOrFailed includes status==='blocked' -- a common real strategy.ts outcome -- so a blocked latest-run row renders the primary 'Approve Proposal' button (page.tsx:1088-1094). handleApprove (page.tsx:987-1006) posts approveProposal(row.id, ...) where row.id is the synthetic composite string from page.tsx:79; the server (app/api/proposals/[id]/approve/route.ts:26 -> src/lib/strategy-execution.ts:262-263 getProposal/throw 'Proposal not found.' -> route.ts:35 maps to 404) only recognizes real persisted ids, which are crypto.randomUUID() (strategy.ts:3361) -- never the synthetic string -- so every tap 404s. page.tsx:1080-1084's live-confirm input auto-calls handleApprove() on exact onChange match with no separate submit button and no Reject control.",
   "correction": "The `??` empty-array-fallback sub-claim is real but minor (only matters when latest.proposals is exactly [] rather than undefined) compared to the false-green-on-error and dead-404-Approve-button issues, which are the substantive teeth of this finding and both independently confirmed.",
   "blast_radius": "(a) A genuinely FAILED order attempt renders with a green success-colored chip captioned e.g. 'Buy Failed' -- real risk of misreading a failed trade as executed. (b) Tapping Approve on any blocked latest-run row always 404s -- a dead control (no wrong order results, but it misrepresents what is actionable and wastes the trader's attention at exactly the moment they are deciding whether to act). (c) The live-confirm input auto-submits on exact text match with no visible Reject/separate-submit step, inconsistent with the app's own dedicated live-order confirmation pattern elsewhere, raising accidental-live-approval risk from paste/autofill/echo scenarios.",
   "confidence": "high"
  },
  {
   "uid": "mobile-web-ux:mweb-01",
   "verdict": "UPHELD",
   "repro": "chrome.tsx:326 renders the full label 'Start Agent'/'Resume Agent'/'Stop Agent' unconditionally (no `hidden sm:inline` truncation) inside a button styled by console.css:471-484 (.con-stop-btn/.con-start-btn: flex-shrink:0, padding 7px 14px, font-size var(--con-fs-sm)=12.5px per console.css:92, font-weight 700, 6px gap) plus a 15px icon (chrome.tsx:340) -- this control never shrinks. StateChip (chrome.tsx:290-316) also carries shrink-0 (chrome.tsx:302) and stacks two lines below sm. shell.tsx:384-419 confirms the row order: ScopeSelector is the ONLY min-w-0 flex-1 element (chrome.tsx:205), followed by StateChip, MutationBusyChip, CommandPaletteTrigger, UserMenu (32px avatar), a phone-only icon-only RunOnceButton (44px coarse-pointer floor, console.css:458-464), and finally RunStateButton -- all fixed-width, all competing against the one flexible sibling. chrome.tsx:199-205's own inline comment computing '~73px leftover at 360px' explicitly assumed a short STOP/Start label (citing PR #1708), not the longer 'Stop Agent'/'Resume Agent' strings that ship today.",
   "correction": "Browser rendering tools are out of scope for this read-only review, so I could not independently re-measure the exact remaining pixel width at 360px/390px -- only the code-level architecture (mismatch between the comment's sizing assumption and the actual non-shrinking button widths) is confirmed from source, same limitation the original verify_note already flagged.",
   "blast_radius": "On common ~360-390px phone viewports, the account-scope selector -- the only control naming which brokerage account (paper/live, broker, last-4) is currently loaded -- is squeezed toward unreadability. This is a legibility/glanceability risk for a multi-account trader, not a routing bug (the correct account is still bound server-side regardless of what the label shows).",
   "confidence": "medium"
  },
  {
   "uid": "web-accessibility:a11y-01",
   "verdict": "UPHELD",
   "repro": "app/console/page.tsx:1030 renders `<div className=\"cursor-pointer ...\" onClick={() => setOpen(true)}>` with 'View details' as inert static text -- no role, tabIndex, or onKeyDown anywhere on the element. The Sheet it opens (page.tsx:1042+) contains the full evidence + Approve flow (page.tsx:1071-1101), so keyboard/switch users cannot reach that flow at all, not just a cosmetic detail view. SymbolButton nested inside the same row (app/console/ui/symbol-drilldown.tsx:548) is confirmed to be a real `<button>`, which independently validates the recommendation that the outer row cannot simply become a `<button>` (would nest interactive elements).",
   "correction": "None.",
   "blast_radius": "Any keyboard-only or switch-control user, on both instances of this row (Latest strategy run + Previous Trades on Home), cannot open Proposal Details or the Approve/Reject-adjacent flow at all -- a complete, silent feature loss for that input modality, not a degraded experience.",
   "confidence": "high"
  },
  {
   "uid": "web-accessibility:a11y-02",
   "verdict": "UPHELD",
   "repro": "console.css:40-50 tokens verified verbatim (--con-pos:#157f4e, --con-neg:#d1332e, --con-warn:#9a6209, --con-info:#0891b2, --con-none:#64748b) with soft backgrounds at console.css:65-73 (color-mix ~10-12% over --con-surface/--con-surface-2, #f4f6fa per console.css:27). I independently reproduced the WCAG relative-luminance contrast math in a standalone script (not copied from the finding) and got IDENTICAL ratios: chip-pos on soft ~4.34:1, con-info on white 3.68:1, chip-neg on soft 4.29:1. Chip text renders at --con-fs-xs=11px/600 (console.css:91, 559-563) and .con-stop-btn/.con-start-btn text at --con-fs-sm=12.5px/700 (console.css:92, 486-498) -- both well below WCAG's large-text threshold (~18.66px bold), so the 4.5:1 AA bar applies and several tokens fail it.",
   "correction": "An open, unmerged PR #2795 ('fix(a11y): console chip AA...', branch cursor/console-a11y-batch-08ac, verified via `gh pr view 2795`: state OPEN, mergedAt null) already darkens these exact tokens and closes issue #2561 -- so the bug is real-and-current on the reviewed code, but a ready fix already exists and needs only review + merge, not new authorship.",
   "blast_radius": "Every light-theme user (the owner's own default per the LIGHT-default ruling) reading a status chip or the Stop/Start Agent button -- i.e. essentially every glance at the console -- is looking at text 7-45% below the WCAG AA contrast floor. Not a trading-logic defect, but a real, constant legibility tax on the money-critical run-state control specifically.",
   "confidence": "high"
  },
  {
   "uid": "frontend-performance:dweb-01",
   "verdict": "UPHELD",
   "repro": "app/console/settings/brokers.tsx:1 'use client'; :12 imports mergeAccountCapabilities (a value import) from @/lib/venue-contract. src/lib/venue-contract.ts:22 imports resolveSourceBool from ./source-settings. src/lib/source-settings.ts:15-16 imports LOCAL_USER from ./db-api-keys and getUserSetting/setUserSetting from ./db-settings. db-settings.ts:3 imports audit from ./db (+ drizzle client/schema); db-api-keys.ts:4-7 imports crypto/fs + getDb/audit from ./db. next.config.mjs:31-58's client-bundle webpack branch ONLY aliases better-sqlite3 and node:* built-ins to false -- it does not exclude any of the actual db.ts/db-settings.ts/db-api-keys.ts/db-execution.ts logic, and a full-tree grep found zero `\"server-only\"` imports anywhere in src/lib/. Direct first-party production proof: commit 01f4e73e2 (PR #2851, an ancestor of this branch's HEAD) fixes a LIVE crash whose own added code comment reads verbatim 'This module is reachable from the browser bundle through the db barrel (app/console/settings/brokers.tsx -> venue-contract -> source-settings -> db-api-keys -> db)' -- the exact chain this finding describes, discovered by the app owners debugging an actual 'Dashboard error: process.uptime is not a function' outage on /console/connections (STATUS.md's 2026-08-18 HOTFIX entry). STATUS.md's own words: 'Root cause (server DB modules in the client bundle...) is a P1 in the MONET full-app review, not fixed here.'",
   "correction": "None -- the mechanism is proven beyond the finding's own static analysis by an actual production incident and the fix commit's own comments. I could not personally reproduce the exact byte counts (1.05MB/285KB gz etc.) since running `next build` is out of scope for this read-only review, but the underlying defect (and that it remains unfixed) is not in question.",
   "blast_radius": "Every /console/connections page load (and, per the finding's architecture, background App-Router prefetch from console-rail links on other pages) ships internal SQL schema strings, query text, encryption-key references, and env-var names to the browser -- an information-disclosure exposure plus real page-weight/load-time cost on phones -- and this exact root cause has ALREADY caused one production outage (the process.uptime crash) as a direct symptom.",
   "confidence": "high"
  }
 ],
 "gap_findings": [
  {
   "id": "pages-01",
   "title": "\"Import from account\" silently flips a live account into Autopilot and imports every guardrail with zero typed confirmation",
   "severity": "P1",
   "kind": "bug",
   "surface": "desktop-web",
   "area": "strategy / account-scoped policy",
   "file": "app/console/strategy/page.tsx",
   "line": 1021,
   "evidence": "doImport() calls importAccountSettings(policy.connectedAccountId, source.id) behind only a plain warning box and an \"Import settings\" button \u2014 no TypedConfirm phrase, no reality.tone==='live' gate. Server side, src/lib/db-profiles.ts:895-901 does `stripIdentityFields(stripUserFields(sourcePolicyRaw))` (ACCOUNT_IDENTITY_POLICY_FIELDS = only connectedAccountId/accountNumber/activeBroker, db-profiles.ts:785) then `mergePolicy({...cleaned, systemState: targetSystemState, ...})` \u2014 `strategyAuthority` is neither an identity field nor a USER_LEVEL_POLICY_FIELD, so it passes through unstripped and overwrites the target's authority.",
   "why": "Every other place in the app that can flip an account from Ask-first to Autopilot (guardrails/page.tsx AutonomyCard) requires typing the literal word \"AUTOPILOT\", and every other place that loosens a guardrail on a live account requires typing CONFIRM (`needsTyped = reality.tone === \"live\" && hasLooser && policy.requireTypedConfirmation !== false`, used identically in policy-form.tsx:335 and strategy/page.tsx:1259 for the AI Review Apply flow). Import bypasses both rituals: one click copies a source account's entire policy \u2014 including strategyAuthority, all riskRules caps, brokerBracketsEnabled, shortSellingEnabled, stop-loss percentages, etc. \u2014 onto a live brokerage account. A user importing from a permissive paper-testing account onto a live one can unintentionally enable autonomous order placement and loosen every cap on real money without ever being asked to confirm, directly contradicting owner rule #1 (\"harden correctness \u2014 never place an unintended order\").",
   "recommendation": "Route importAccountSettings through the same needsTyped/TypedConfirm gate used by PolicySaveBar and AiReviewPanel: diff the source policy against the target's current policy the same way reviewChanges() does, and require the CONFIRM phrase whenever the target is live and any changed field is a loosening (including strategyAuthority propose\u2192decide). At minimum, explicitly preserve the target's own strategyAuthority the same way systemState is already preserved.",
   "effort": "M",
   "verdict": "UPHELD",
   "corrected": null
  },
  {
   "id": "pages-02",
   "title": "\"Show results net of estimated tax\" toggle saves successfully but never changes any number on Results",
   "severity": "P1",
   "kind": "bug",
   "surface": "desktop-web",
   "area": "strategy/guardrails \u2014 tax settings, results",
   "file": "app/console/strategy/tax-settings.tsx",
   "line": 213,
   "evidence": "tax-settings.tsx:210-225 renders a Toggle labeled \"Show results net of estimated tax\" with title \"On: P&L on the Results screen is shown after subtracting estimated taxes at the rates above\", bound to policy.taxSettings.subtractFromResults and auto-saved via savePolicy. Grepping the whole repo, `subtractFromResults` is only ever read/validated in src/lib/mobile-api.ts (input validation) \u2014 results/page.tsx's BucketCard (940-994) and TaxBlock (1048-1160) render `performance.paperRealizedPnl`/`liveRealizedPnl`/etc. and `tax.estimatedTaxLiability` completely unmodified by the flag; no code path subtracts anything from the P&L stats when the toggle is on.",
   "why": "This is a control whose label makes an explicit, verifiable claim about what it does to the money numbers on Results, and the claim is false in both directions \u2014 flipping it on shows no different Realized/Unrealized P&L, and flipping it off doesn't restore anything either, because nothing ever reads it in the display path. A user trying to see their after-tax performance has no way to do so despite the setting existing, saving, and reporting success.",
   "recommendation": "Either wire subtractFromResults into BucketCard/TaxBlock (subtract tax.estimatedShortTermTax/estimatedLongTermTax pro-rata from the realized figures shown, or add a distinct 'net of tax' stat) or remove the toggle and its copy until it's implemented.",
   "effort": "M",
   "verdict": "UPHELD",
   "corrected": null
  },
  {
   "id": "pages-03",
   "title": "Webhook channel's copy promises rich Discord/Slack embeds it never sends, and \"Send test\" never exercises the one webhook field that actually does that",
   "severity": "P1",
   "kind": "bug",
   "surface": "desktop-web",
   "area": "settings \u2014 delivery channels",
   "file": "app/console/settings/delivery.tsx",
   "line": 37,
   "evidence": "delivery.tsx:37 CHANNEL_TITLE.webhook = \"An HTTPS POST with a JSON payload to any URL you control (chat webhooks get rich embeds).\" \u2014 describing the toggle-based \"Webhook\" channel. But that channel's actual sender, CHANNELS.webhook.send in src/lib/notify.ts:262-290, always POSTs plain `{source, kind, title, body, data, sent_at}` JSON with no Discord/Slack special-casing. The Discord rich-embed logic (`isDiscord` + `formatDiscordPayload`) lives only in the SEPARATE, always-on legacy path `sendLegacyWebhook` in src/lib/notifications.ts:330-372, driven by the different field `policy.notificationSettings.webhookUrl` (WebhookUrlRow, delivery.tsx:95-129). The \"Send test\" button (delivery.tsx:268-275, title \"Fires a test alert through every enabled, saved channel\") calls POST /api/notifications/test, which calls `notify()` (src/lib/notify.ts:558) \u2014 this only iterates `prefs.channels` (the toggle-based system) and never touches the legacy webhookUrl field at all.",
   "why": "A user who fills in the legacy \"Webhook URL (optional)\" field expecting the promised rich embeds (which really do exist, just on the other field) gets a plain JSON POST from the toggle-based channel instead if they toggle it on with the same/a different URL \u2014 and if they rely on the legacy field alone (as the always-on mechanism it's documented to be), \"Send test\" gives false confidence: it reports success/failure for every OTHER channel but silently says nothing about whether that legacy webhook \u2014 which is what actually fires for every enabled event \u2014 is even reachable. On a real-money app, a broken alert channel that the UI claims was tested is a real notification-reliability gap (kill_switch, fill, pending_approval alerts could silently never arrive).",
   "recommendation": "Either move the Discord rich-embed logic into the toggle-based CHANNELS.webhook.send so the copy is true, or have the test endpoint also probe policy.notificationSettings.webhookUrl and report it as its own row in the test results.",
   "effort": "S",
   "verdict": "UPHELD",
   "corrected": null
  },
  {
   "id": "pages-04",
   "title": "Decisions index and trace pages merge every connected account's decisions into one list/detail with no account label or filter",
   "severity": "P2",
   "kind": "defect",
   "surface": "desktop-web",
   "area": "decisions",
   "file": "app/console/decisions/page.tsx",
   "line": 28,
   "evidence": "page.tsx:28 calls `fetch(\"/api/socratic/decisions?limit=100\")` with no `connectedAccountId`, even though app/api/socratic/decisions/route.ts explicitly supports that query param (`listSocraticDecisionCases(userId, { limit, connectedAccountId, runId })`). decisions-list.tsx never renders `decision.connectedAccountId` or `decision.accountNumber` (both present on SocraticDecisionCase, types.ts:1816/1819) anywhere in the row. app/console/decisions/[id]/page.tsx never references account either (zero hits for \"account\" in the file).",
   "why": "Every other console page explicitly scopes to the active account and says so (\"Showing the active account only \u2014 switch the account scope (top bar) to see another account's orders\", orders/page.tsx:311-316; Activity's unified feed labels \"Account: {label}\" per row, activity/page.tsx:284-290). The console's own glossary states \"Active account: Exactly one account is active at a time; the entire console... is scoped to it\" (settings/help.tsx:181-185). Decisions breaks that invariant silently: a user with a paper account and a live account sees both accounts' BUY/SELL decision traces interleaved in one reverse-chron list with no visual distinction, which directly undermines the \"every screen states which money it is in words\" money-reality philosophy (help.tsx:27-28) for the one screen that shows full reasoning traces for real trades.",
   "recommendation": "Pass the active account's connectedAccountId to /api/socratic/decisions by default (with an explicit \"all accounts\" toggle if cross-account viewing is intentional), and render an account/environment chip per row and on the trace page, matching Activity's pattern.",
   "effort": "S",
   "duplicate_of": "",
   "verdict": "UPHELD",
   "corrected": null
  },
  {
   "id": "pages-05",
   "title": "Strategy \u2192 Presets has no way to create, rename, or delete a preset \u2014 view/apply only, despite full CRUD existing server-side",
   "severity": "P2",
   "kind": "defect",
   "surface": "desktop-web",
   "area": "strategy \u2014 presets",
   "file": "app/console/strategy/page.tsx",
   "line": 925,
   "evidence": "The Presets card (925-985) only renders `snapshot.profiles` and an \"Apply to this account\" button calling copyProfileToAccount/activateProfile. Grepping app/console for any call to `POST /api/profiles` (create) or `DELETE /api/profiles/[id]` (delete) turns up nothing, yet app/api/profiles/route.ts implements POST createStrategyProfile and app/api/profiles/[id]/route.ts implements PUT updateStrategyProfile and DELETE deleteStrategyProfile \u2014 a complete CRUD surface the UI never calls.",
   "why": "The page's own copy (\"A saved strategy bundle... Applying one COPIES it onto the active account\", help.tsx:193-196) implies presets are something the owner builds and manages, but there is no button anywhere in app/console to save the current account's strategy as a new preset, rename an existing one, or delete one \u2014 the library can only ever contain whatever got seeded some other way. This is a real \"can the owner do the job this section exists for\" gap: the answer today is no.",
   "recommendation": "Add \"Save current strategy as new preset\" (POST /api/profiles with this account's policy/prompt/weights), and rename/delete actions per row, wired to the existing PUT/DELETE routes.",
   "effort": "M",
   "verdict": "UPHELD",
   "corrected": null
  },
  {
   "id": "pages-06",
   "title": "Legal acceptance timestamp renders in the browser's local timezone, not Central Time",
   "severity": "P3",
   "kind": "bug",
   "surface": "desktop-web",
   "area": "settings \u2014 legal",
   "file": "app/console/settings/legal.tsx",
   "line": 40,
   "evidence": "`legal?.accepted && legal.acceptedAt ? \\`Accepted on ${new Date(legal.acceptedAt).toLocaleString()}.\\` : ...` \u2014 no `timeZone` option. Every other timestamp helper in the console routes through app/console/lib/format.ts's CENTRAL_TIME_ZONE-pinned formatters (format.ts:10, \"Owner ruling (2026-08-11): every timestamp shown to the user displays in Central Time\"), which this card bypasses entirely, calling `toLocaleString()` raw.",
   "why": "Directly violates the standing owner ruling that every user-facing timestamp displays in Central Time regardless of the viewer's device timezone \u2014 this is the one console timestamp that instead shows whatever timezone the browser is set to.",
   "recommendation": "Replace with the shared format.ts helper (e.g. fmtExact/formatTimestamp) used everywhere else in the console.",
   "effort": "S",
   "verdict": "UPHELD",
   "corrected": null
  },
  {
   "id": "pages-07",
   "title": "Macro's \"How the strategist uses this label\" disclosure repeats the same inaccurate \"vetoed outright\" claim the regime meaning text already got flagged for",
   "severity": "P2",
   "kind": "defect",
   "surface": "desktop-web",
   "area": "macro",
   "file": "app/console/macro/indicators.ts",
   "line": 753,
   "evidence": "REGIME_USAGE entry \"It gates entries\": \"In Risk-Off or Crisis, buys scoring below the scan median are vetoed outright, and an optional cap limits how large any new position may open in Crisis/Inverted regimes.\" This is the same overstated claim as regimeInfo()'s meaning text at indicators.ts:698/706 (\"hard-vetoed\"), which the register already flags as inaccurate (advisory tag-not-drop in the actual engine, not a hard veto) \u2014 but it is a second, independent copy of the claim on a different UI surface (the collapsible disclosure under the Regime card vs. the chip's own tooltip/meaning text), so a reader who never opens the meaning tooltip but does open \"How the strategist uses this label\" still gets the same wrong mental model.",
   "why": "Same underlying inaccuracy (advisory, agent-decides-and-logs, not a hard block \u2014 per the owner's standing \"guardrails: nothing hard\" ruling) surfacing at a second, unfixed location; fixing only line 698/706 would leave this one still wrong.",
   "recommendation": "Same fix as the registered finding, applied to both locations: change \"vetoed outright\"/\"hard-vetoed\" to advisory language consistent with ADVISORY_NOTE used elsewhere in field-defs.ts.",
   "effort": "S",
   "duplicate_of": "ux-copy-editor:copy-03",
   "verdict": "UPHELD",
   "corrected": null
  },
  {
   "id": "pages-08",
   "title": "Two auto-saving number fields fire a write on every keystroke instead of on blur, breaking the app's own established pattern",
   "severity": "P3",
   "kind": "defect",
   "surface": "desktop-web",
   "area": "settings \u2014 data sources; strategy \u2014 overlays",
   "file": "app/console/settings/page.tsx",
   "line": 874,
   "evidence": "settings/page.tsx:866-880 (DataSourcesCard number row): `onValueChange={(n) => { if (Number.isFinite(n) && n !== Number(row.value)) void saveOne(row.id, n); }}` \u2014 RawNumInput's onValueChange fires on every keystroke (primitives.tsx:310-315 confirms `onChange` calls `onValueChange` per input event), so saveOne (a PATCH) runs mid-typing rather than on blur. overlays-panel.tsx:170-180 \"Max Active\" uses a plain `<TextInput type=\"number\">` whose onChange directly calls `patchTuning({ maxActiveOverlays: ... })` on every keystroke too, with `Number(event.target.value) || 2` silently coercing a momentarily-empty field (normal mid-edit state when replacing a value) to 2 and saving that.",
   "why": "Every other numeric auto-save field in the app (ScanShapeCard settings/page.tsx:593-598, tax-settings.tsx short/long-term rates, llm-budget.tsx, learning-review.tsx, Strategy's own scoring-weights inputs) deliberately holds local text and commits on blur specifically to avoid firing a save per keystroke. These two fields silently deviate: typing a two-digit number issues multiple PATCH requests, and for Max Active a normal \"select-all, retype\" edit gesture can transiently save the fallback value 2 instead of what the user meant to enter.",
   "recommendation": "Switch both to the same local-draft-until-blur pattern already used by every sibling numeric field (RawNumInput + explicit onBlur commit).",
   "effort": "S",
   "verdict": "UPHELD",
   "corrected": null
  },
  {
   "id": "admin-01",
   "title": "Chat Transcript admin page bypasses requireAdmin and does not actually show \"every chat turn\"",
   "severity": "P2",
   "kind": "bug",
   "surface": "desktop-web",
   "area": "Admin / Chat Transcript",
   "file": "app/admin/transcript/transcript-client.tsx",
   "line": 31,
   "evidence": "const res = await fetch(\"/api/chat-history?limit=200\");  -- app/api/chat-history/route.ts:8 is `const userId = resolveRequestUserId(request);` with no requireAdmin call at all; app/admin/layout.tsx:101-103 describes the nav item as \"Every chat turn, with the model that produced each assistant reply.\"",
   "why": "Every other admin data probe (connections-health, llm-usage, rag-coverage, server-metrics, r2-usage, data-catalog, enrichment-coverage, backup-status, server-knobs) is behind the shared requireAdmin() gate and correctly 403s a non-admin session -- app/admin/page.tsx even has dedicated `allForbidden`/`probeErrors` handling to show \"Operator access required\" for exactly that case. `/api/chat-history` has no admin check and is scoped to resolveRequestUserId(request), the CALLER's own account, not \"every\" turn. So (a) any signed-in non-admin user who opens /admin/transcript, or the Overview page's Chat Transcript card (which fetches the same endpoint at app/admin/page.tsx:104), gets a normal 200 response instead of the operator-access notice every sibling widget shows -- confirmed by the code's own comment at app/admin/page.tsx:208-215 that explicitly carves this endpoint out of the all-forbidden check; and (b) if a second admin is ever added via the documented ADMIN_USER_EMAILS allowlist (src/lib/auth/admin.ts:44-50), their \"Chat Transcript\" view would silently show THEIR OWN (likely empty) history rather than the primary trading account's actual coach conversations, defeating the feature's stated audit purpose with no error surfaced anywhere.",
   "recommendation": "Gate an admin-scoped transcript read behind requireAdmin, explicitly reading the primary/local user's turns (or accepting an explicit userId param) instead of reusing the per-caller /api/chat-history endpoint.",
   "effort": "S",
   "verdict": "UPHELD_NARROWED",
   "corrected": "P3"
  },
  {
   "id": "admin-02",
   "title": "RAG purge-legacy is an irreversible corpus-wide vector deletion reachable only by curl, guarded by a confirm string the code itself says is scope-mismatched",
   "severity": "P2",
   "kind": "risk",
   "surface": "backend",
   "area": "Admin / RAG re-embed",
   "file": "app/api/admin/reembed/route.ts",
   "line": 35,
   "evidence": "\"SCOPE WARNING: despite the token's historical name it deletes vectors from EVERY non-current embedding space for the covered docTypes ... not only voyage-finance-2's.\" -- the POST handler at lines 80-88 gates this solely on `body.confirm === \"purge-voyage-vectors\"` plus the shared admin token/email check; there is no admin nav entry or page for this route (grep of app/admin/**/*.tsx for /api/admin/reembed returns nothing).",
   "why": "This action bulk-deletes RAG vectors across every embedding space that isn't the currently-active one for the covered doc types -- broader than its own confirm string implies, per the route's own comment. It is reachable only by hand-crafting an authenticated curl POST with that exact (misleadingly-named) confirm string; there is no UI that shows what would be deleted before it happens, no dry-run visible outside a JSON response, and no button an operator could accidentally NOT click. A stale runbook or copy-pasted curl command using the confirm string's literal (narrower) meaning could delete far more than intended with zero UI guardrail to catch the mismatch.",
   "recommendation": "Surface this as a guarded Admin panel (in the style of Operations' knob resets) that lists the exact embedding spaces/docTypes about to be purged before firing the destructive POST, and/or rename the confirm string to match actual scope.",
   "effort": "M",
   "verdict": "UPHELD_NARROWED",
   "corrected": "P3"
  },
  {
   "id": "admin-03",
   "title": "Several real, cost/state-mutating operator capabilities (learning-ledger revert, tuning dry-run, congress-score-eval, RAG/EarningsCalls backfills) have zero admin UI -- curl-only, JSON body shape read out of code comments",
   "severity": "P2",
   "kind": "opportunity",
   "surface": "backend",
   "area": "Admin / Operator actions",
   "file": "app/admin/layout.tsx",
   "line": 43,
   "evidence": "NAV_ITEMS lists exactly 10 pages, none of which reach learning-ledger, tuning-dry-run, congress-score-eval, congress-share, refresh-websource, sec-ingest, reindex-8k, reindex-10k, or earningscalls. app/api/admin/learning-ledger/route.ts:9 literally calls itself \"panel P0-4\" and app/api/admin/tuning-dry-run/route.ts:9 calls itself \"panel P1-1\"; app/api/admin/congress-score-eval/route.ts:11 calls itself \"item 2\".",
   "why": "learning-ledger's POST reverts a live scoring-weight mutation (with before/after weight vectors) and is admin-gated specifically \"because a revert mutates live policy\" (route.ts:16) -- exactly the kind of action the rest of this admin surface (e.g. Operations' real toggle switches) treats as something an operator should be able to see and act on from a page, not something they curl. The same is true of the RAG/EarningsCalls backfills, which spend real Voyage/bge/EarningsCalls.dev budget. The \"panel P0-4\"/\"panel P1-1\"/\"item 2\" naming in the code strongly implies these were meant to get admin pages that were never wired up.",
   "recommendation": "Wire learning-ledger (list + revert) and tuning-dry-run into the Admin nav first, since both already have complete read+act backend pairs; the naming suggests this was the original plan.",
   "effort": "M",
   "verdict": "UPHELD",
   "corrected": "P2"
  },
  {
   "id": "admin-04",
   "title": "Overview's CPU-load number drops the \"scaling unverified, may read up to 8x low\" caveat that the Server Stats detail page carries for the identical value",
   "severity": "P3",
   "kind": "improvement",
   "surface": "desktop-web",
   "area": "Admin / Overview",
   "file": "app/admin/page.tsx",
   "line": 448,
   "evidence": "<span className=\"con-num font-semibold\">{server?.metrics?.cpu ? `${currentCpu.toFixed(1)}%` : \"Unavailable\"}</span>  -- no caveat text or title anywhere on this card, versus app/admin/server/server-metrics-client.tsx:175-178's CPU_SCALING_CAVEAT (\"...this value may read low\") and the visible caption \"per-core average; scaling unverified\" at lines 848-852 of the same file.",
   "why": "app/api/admin/server-metrics/route.ts:604-613 documents this transform as an explicitly UNVERIFIED 8x risk (\"the only support ... is an uncited comment ... which would make this an 8x under-report on this 8-core cx43 and keep the meter green through a saturation incident\"). The Server Stats detail page was carefully rewritten to disclose that risk inline. The Overview page -- the one literally named to give status \"at a glance\" -- renders the same field from the same API with no caveat at all, so an operator who only checks Overview sees a confident, uncaveated percentage the codebase itself says it does not trust.",
   "recommendation": "Reuse CPU_SCALING_CAVEAT (or a shortened title-only version) on the Overview card, or replace the bare percentage there with the same \"unverified\" language used on the detail page.",
   "effort": "S",
   "verdict": "UPHELD",
   "corrected": "P3"
  },
  {
   "id": "admin-05",
   "title": "Admin chrome never shows which admin identity is signed in, despite the allowlist supporting multiple distinct admins and knob writes already being mis-audited as a fixed user",
   "severity": "P3",
   "kind": "improvement",
   "surface": "desktop-web",
   "area": "Admin / Layout",
   "file": "app/admin/layout.tsx",
   "line": 217,
   "evidence": "fetch(\"/api/auth/session\").then((res) => res.json()).then((s) => setImageUrl(s?.user?.image)) -- only `image` is read and stored; the profile dropdown (lines 262-303) renders \"Profile\" / a theme picker / Back to Console / Sign Out, and no email or name string anywhere in the component.",
   "why": "src/lib/auth/admin.ts:44-50 (ADMIN_USER_EMAILS) is a documented allowlist that supports more than one distinct admin identity, and app/api/admin/server-knobs/route.ts:80 already audits every knob write under a hardcoded userId of \"local\" regardless of who actually flipped it. With no on-screen identity anywhere in the admin chrome, an operator using powerful tools (knob flips today; ledger reverts and backfills if admin-03 is addressed) has no way to visually confirm which session is acting, compounding the existing audit-attribution gap.",
   "recommendation": "Show the resolved session email in the profile trigger or dropdown header -- the fetch already receives it in `s.user.email`, it is just discarded.",
   "effort": "S",
   "verdict": "UPHELD",
   "corrected": "P3"
  },
  {
   "id": "admin-06",
   "title": "The tamper-evident audit hash-chain has no admin viewer, and verifyAuditChain() is never called outside a unit test",
   "severity": "P2",
   "kind": "risk",
   "surface": "backend",
   "area": "Admin / Audit trail",
   "file": "app/api/audit/route.ts",
   "line": 8,
   "evidence": "return NextResponse.json(listAudit(200, resolveRequestUserId(request))); -- no requireAdmin call, capped to 200 rows, no `kind` filter, scoped only to the caller's own userId. Repo-wide `grep -rn \"verifyAuditChain\"` (excluding node_modules) returns only its definition at src/lib/db.ts:3372 and test/audit-chain.test.ts -- no route, cron, or admin page calls it. No app/admin/**/*.tsx file fetches /api/audit either.",
   "why": "src/lib/db.ts's audit() builds a genuinely hash-chained audit_events table (chain_hash/prev_chain_hash, see auditChainMaterial at db.ts:3344) precisely so a knob change, a learning-ledger revert, or a securities import can be proven untampered. That investment is currently invisible in the product: there is no Admin > Audit Log page, no nav item, and the one HTTP route that reads audit_events is unused by any UI and isn't even admin-scoped. If the chain ever silently broke -- a bad migration, a manual DB edit, real tampering -- there is today no automated check and no manual UI path by which the owner would find out; verifyAuditChain only runs inside `npm test`.",
   "recommendation": "Add an admin-gated Admin > Audit Log page (list recent audit_events, filterable by kind, admin identity) that also surfaces verifyAuditChain()'s result; consider running the chain check on a schedule with an alert on mismatch.",
   "effort": "M",
   "verdict": "UPHELD",
   "corrected": "P2"
  },
  {
   "id": "admin-07",
   "title": "Enrichment Coverage report is a single unscoped process-global with no account/run attribution in a multi-account app",
   "severity": "P3",
   "kind": "improvement",
   "surface": "backend",
   "area": "Admin / Enrichment Coverage",
   "file": "src/lib/enrichment-coverage.ts",
   "line": 156,
   "evidence": "let lastCoverageReport: EnrichmentCoverageReport | null = null; -- the EnrichmentCoverageReport interface (lines 123-135) has fields for asOf/symbolCount/fields/sourceWinTotals/providerFailures/contributingSources but no userId, connectedAccountId, or policy identifier of any kind.",
   "why": "app/api/admin/enrichment-coverage/route.ts serves getLastEnrichmentCoverageReport() verbatim -- whichever scan (any connected account, any user) ran most recently across the whole process overwrites the single in-memory report with no attribution. In a multi-connected-account app (the product's own stated model), an operator viewing Admin > Enrichment Coverage cannot tell which account's cascade run produced the numbers on screen, and a scan from one account can silently mask another account's actual coverage state with no indication that happened.",
   "recommendation": "Key the last-report cache by (userId, connectedAccountId) and show which account/run produced the displayed report, or keep a short history instead of a single overwritten snapshot.",
   "effort": "S",
   "verdict": "UPHELD_NARROWED",
   "corrected": "P3"
  },
  {
   "id": "admin-08",
   "title": "A route under /api/admin/ intentionally skips the shared requireAdmin gate with no marker distinguishing it from the ~25 sibling routes that all use it",
   "severity": "P3",
   "kind": "improvement",
   "surface": "backend",
   "area": "Admin / Auth consistency",
   "file": "app/api/admin/securities/import/route.ts",
   "line": 10,
   "evidence": "import { verifySecuritiesImportToken } from \"@/lib/securities-import-auth\"; -- this file has no `requireAdmin` import at all, unlike every other route.ts under app/api/admin/ (confirmed: `grep -n requireAdmin` across all app/api/admin/*/route.ts files returns a hit in every route except this one).",
   "why": "This route is a legitimate, well-documented inter-app (Congress.Trade -> Socratic.Trade) ingest endpoint with its own bearer-token auth, default-closed when unconfigured -- not a defect on its own merits. But it lives at a URL prefix (/api/admin/) that every other route treats as \"gated by the shared operator requireAdmin() check,\" which is exactly the assumption this review started from before reading the file. A future engineer (or agent) grepping for \"is every /api/admin/ route admin-gated\" and pattern-matching on the URL prefix rather than reading each file could reasonably conclude this route is covered when it uses an entirely different auth model.",
   "recommendation": "Move this route out of the /api/admin/ prefix (e.g. /api/integrations/congress-trade/securities-import) so the prefix reliably means \"gated by requireAdmin\" for every route under it, or add a header comment making the divergent auth model impossible to miss on a prefix-based audit.",
   "effort": "S",
   "verdict": "UPHELD",
   "corrected": "P3"
  },
  {
   "id": "coach-01",
   "title": "draft_order silently coerces any non-\"sell\" side value (case-sensitive) to \"buy\", including \"short\"/\"cover\"/typos",
   "severity": "P1",
   "kind": "bug",
   "surface": "cross-surface",
   "area": "coach/draft-order tool",
   "file": "src/lib/chat/tools.ts",
   "line": 92,
   "evidence": "const side = input.side === \"sell\" ? \"sell\" : \"buy\";  // line 92; line 94 has the same pattern: const order_type = input.order_type === \"limit\" ? \"limit\" : \"market\";",
   "why": "This is a strict, case-sensitive equality check with no rejection path. Any side value that is not the exact lowercase string \"sell\" -- \"Sell\", \"SELL\", \"short\", \"cover\", a typo, or an out-of-schema value a less-compliant provider emits -- is silently mapped to \"buy\", the OPPOSITE economic direction from a sell/short. The Coach model catalog includes Gemini, Grok, Mistral, DeepSeek, Moonshot, and any free-typed custom model id (app/console/assistant/chat.tsx custom-model field), several of which are known to be looser about strict JSON-schema enum adherence in tool-calling than Anthropic/OpenAI. The draft_order JSON schema itself only declares side enum [\"buy\",\"sell\"] (tools.ts:64) -- there is no server-side rejection of an out-of-enum value, only silent coercion. Contrast with create_alert's op handling, which normalizes via normalizeAlertOp and fails closed with {error:\"INVALID_OP\"} on anything unrecognized (src/lib/alerts.ts:31-34) -- draft_order does the opposite. The same pattern also silently discards a limit price: if order_type arrives as anything but the exact string \"limit\" (e.g. \"Limit\"), order_type resolves to \"market\" AND limit_usd is forced to null (tools.ts:96), dropping a limit price the user gave. The downstream human-approval ticket (draft-card.tsx) does display the coerced side/order_type, so a careful reviewer can catch it, but the assistant's own natural-language reply is generated independently of the coerced fields and can describe the order differently than the ticket -- exactly the kind of ticket-vs-prose mismatch a rushed approval misses.",
   "recommendation": "Reject the draft (return {error:\"INVALID_SIDE\"}) when input.side is not exactly \"buy\" or \"sell\" (case-insensitive compare, then fail closed on anything else, mirroring create_alert's normalizeAlertOp pattern), and do the same for order_type. If short/cover intent is out of scope for chat, say so explicitly in the draft error rather than silently rewriting the order to a buy.",
   "effort": "S",
   "verdict": "UPHELD",
   "corrected": null
  },
  {
   "id": "coach-02",
   "title": "Neither the chat \"Cancel\" button nor the 120s turn deadline actually aborts an in-flight LLM provider request",
   "severity": "P2",
   "kind": "bug",
   "surface": "cross-surface",
   "area": "coach/cancel and turn-deadline",
   "file": "src/lib/chat/llm.ts",
   "line": 333,
   "evidence": "async function defaultTransport(body, apiKey) { const res = await llmFetch(\"https://api.anthropic.com/v1/messages\", { method: \"POST\", headers: {...}, body: JSON.stringify(body) }); ... }  -- no `signal` is ever passed, so llm-request.ts:402 falls back to `signal: init.signal ?? AbortSignal.timeout(LLM_TIMEOUT_MS)` (LLM_TIMEOUT_MS = 60_000, llm-request.ts:367). makeOpenAITransport (llm.ts:688-705) has the identical gap.",
   "why": "AnthropicLLM.run/OpenAILLM.run only check `args.abortSignal?.aborted` (llm.ts:395, 537) and the deadline budget (via decideStageBudget) at the TOP of each tool-loop step, BEFORE calling `this.transport(...)`. The Transport type itself is `(body, apiKey) => Promise<any>` -- it never receives the AbortSignal, so once a provider HTTP call is in flight there is no way to interrupt it; it can only be interrupted by its own 60s AbortSignal.timeout. For the dominant case (a single-step reply, no tool calls), the abort/deadline check never gets a second chance to fire before the loop naturally exits after that one call completes -- so clicking \"Cancel\" in chat.tsx (line 649-655, which just POSTs /api/chat/cancel and has no client-side AbortController on its own `fetch(\"/api/chat\", ...)` either) has essentially no effect: the user keeps waiting for the original request to resolve normally, and the reply still gets appended to the transcript as if nothing happened. The same missing signal wiring means the advertised CHAT_TURN_DEADLINE_MS (120s default, app/api/chat/route.ts:166) is not a hard ceiling either -- worst case a turn can run up to MAX_STEPS x LLM_TIMEOUT_MS (5 x 60s = 300s) before anything intervenes, since decideStageBudget's step 0 always runs (stage-budget.ts:23-25) regardless of remaining budget and in-flight steps are never preempted.",
   "recommendation": "Thread `args.abortSignal` into the Transport signature and pass it as `signal` on every llmFetch call in defaultTransport/defaultOpenAITransport/makeOpenAITransport (composed with the existing timeout signal via e.g. AbortSignal.any). Also give the client's `/api/chat` fetch its own AbortController tied to the Cancel button so cancel takes effect locally without waiting on the server.",
   "effort": "M",
   "verdict": "UPHELD",
   "corrected": null
  },
  {
   "id": "coach-03",
   "title": "ChatDraft ships permanently-hardcoded placeholder fields (\"Test (local)\" account_label, is_real:false, blocked:false, warnings:[]) that no client renders",
   "severity": "P3",
   "kind": "defect",
   "surface": "cross-surface",
   "area": "coach/draft-order tool response shape",
   "file": "src/lib/chat/tools.ts",
   "line": 105,
   "evidence": "account_label: ctx.deps.accountLabel ?? \"Test (local)\", is_real: false, blocked: false, warnings: [],  // tools.ts:105-108; ctx.deps.accountLabel is itself hardcoded to the literal string \"Test (local)\" in buildProductionDeps (src/lib/chat/orchestrator.ts:770), so account_label is ALWAYS \"Test (local)\" for every production draft, live account or not.",
   "why": "These four fields are set exactly once at draft creation and never mutated anywhere else in the codebase (grep confirms zero other writers). account_label is always the literal string \"Test (local)\" regardless of the actual connected account (which could be a real live broker account) -- a hardcoded test/paper-mode label the owner's rulings explicitly forbid attaching to real data. is_real is always false and blocked/warnings are always empty/false, so they can never reflect an actual policy verdict. Today this is largely inert: draft-card.tsx (web) and CoachDraftCard (iOS) both independently recompute the real account label/scope from live snapshot state rather than reading these fields, so nothing user-facing currently shows \"Test (local)\" for a real account. But the fields are still serialized verbatim in the /api/chat JSON response, are decoded on iOS (DeskModels.swift:120,133,149-151) into a ChatDraft.accountLabel/isReal that a future screen could plausibly surface, and an operator inspecting the raw API/admin transcript would see a real-money draft literally labeled \"Test (local)\" / is_real:false.",
   "recommendation": "Either wire real values through (pass the caller's actual account label/environment into ToolDeps.accountLabel from the active connected account rather than a hardcoded default, and drop is_real/blocked/warnings from the tool's output entirely since policy evaluation genuinely happens later in /api/proposals/from-draft), or remove the dead fields from the ChatDraft type so the wire contract doesn't advertise information nothing computes.",
   "effort": "S",
   "verdict": "UPHELD",
   "corrected": null
  },
  {
   "id": "coach-04",
   "title": "Chat conversation history has no account scoping -- the last-10-turn context replayed into every prompt spans ALL of a user's connected accounts",
   "severity": "P2",
   "kind": "risk",
   "surface": "backend",
   "area": "coach/conversation persistence",
   "file": "src/lib/types.ts",
   "line": 3011,
   "evidence": "export interface ChatTurn { id: string; userId: string; role: ChatTurnRole; text: string; citations: string[]; intent: string | null; redacted: boolean; model?: string | null; clientTurnId?: string | null; createdAt: string; }  -- no connectedAccountId field anywhere on the row; orchestrator.ts:143 does `listTurns(userId, 10)` (userId only) and replays that as `history` to the model on every turn, while every read tool (get_positions/get_portfolio/get_reflection/get_performance_summary/get_quote's broker branch) is scoped to `getPolicy(userId)`'s CURRENTLY ACTIVE account.",
   "why": "A user with multiple connected accounts (e.g. a live account and a paper/practice account) who switches the active account mid-conversation gets a multi-turn LLM context that silently mixes both accounts' answers with no account-boundary marker in the replayed text -- the model has no way to know a prior turn's positions/P&L/portfolio numbers belonged to a different account than the one the current tool calls now resolve against. The client's own Clear-conversation toast concedes this design (\"it is one conversation shared across all your accounts\", chat.tsx:357) but that disclosure only appears at clear time, not on send, and it does not prevent the model from anchoring a follow-up answer (e.g. \"what's my P&L now?\") on stale account-A numbers still sitting in the replayed history text after a switch to account B.",
   "recommendation": "Either scope chat_turns (and the 10-turn replay window) by connectedAccountId so switching accounts starts a fresh context, or inject an explicit account-boundary marker into the replayed history whenever the active account differs from the account each historical turn was answered under (would require persisting connectedAccountId per turn either way).",
   "effort": "M",
   "verdict": "UPHELD",
   "corrected": null
  },
  {
   "id": "coach-05",
   "title": "Dead, unauthenticated POST /api/chat-history lets a user forge arbitrary chat turns -- including fabricated \"assistant\" replies -- into their own persisted transcript",
   "severity": "P2",
   "kind": "risk",
   "surface": "api",
   "area": "coach/chat-history route",
   "file": "app/api/chat-history/route.ts",
   "line": 28,
   "evidence": "export async function POST(request: Request) { const body = ...; const userId = resolveRequestUserId(request, body); if (body.role !== \"user\" && body.role !== \"assistant\") return ...400; ... const turn = appendTurn(userId, { role: body.role, text: body.text, citations: ..., intent: ... }); return NextResponse.json({ turn }, { status: 201 }); }",
   "why": "This handler accepts an arbitrary role (\"user\" OR \"assistant\") and arbitrary text/citations straight from the request body and writes it into the caller's own chat_turns via the same appendTurn used by the real orchestrator -- with no check that this ever went through an actual model call. Grepping the whole app/ios tree shows nothing calls it: chat.tsx only GETs and DELETEs /api/chat-history, and the real assistant turn is always persisted server-side from within makeOrchestrator (orchestrator.ts:479-505). Because this route is live and reachable by any authenticated user for their own account, a user (or a stray script/extension) can insert a fabricated \"assistant\" turn claiming the model said something it never said. src/lib/chat-history.ts's own header comment calls this table the transcript that \"powers chat reload + auditability\" -- a forgeable assistant turn breaks that guarantee, is displayed as real in the admin transcript viewer (app/admin/transcript/transcript-client.tsx), and -- more materially -- gets replayed as trusted prior \"assistant\" context into the SAME user's next real chat turn (orchestrator.ts:143), letting a user self-poison the model's own conversational memory with content it never actually produced.",
   "recommendation": "Remove this POST handler if truly unused, or restrict it to a genuinely internal caller (it should not be a public route at all given nothing calls it); at minimum require role to always be recorded via the orchestrator's own appendTurn path rather than a free-form client-supplied write.",
   "effort": "S",
   "verdict": "UPHELD",
   "corrected": null
  },
  {
   "id": "coach-06",
   "title": "Coach URL-lesson capture stores fetched web-page text verbatim (no summarization despite the name) as auto-approved \"fact\"-tier learned context, protected only by a financial-risk keyword classifier, not a general injection detector",
   "severity": "P2",
   "kind": "risk",
   "surface": "backend",
   "area": "coach/durable learning from pasted URLs",
   "file": "src/lib/chat/coach-learning.ts",
   "line": 567,
   "evidence": "const lesson = summarizeArticleText(fetched.text, fetched.url); ... const result = await ingestLearned(args.userId, { kind: \"fact\", subject: \"coach-url-lesson\", value: lesson.slice(0, 800), source: `owner-coach-url:${fetched.url}`, confidence: 0.7, intent: \"article lesson\" }, \"ingest\", {...}); -- summarizeArticleText (coach-learning.ts:313-317) is a plain string truncation to 480 chars, not an LLM summary.",
   "why": "A user pasting a URL in chat causes the server to fetch it (SSRF-gated, which is well done) and store roughly the first 480-800 characters of the raw page text as a durable \"fact\"-tier learned_context row. Because origin is \"ingest\" (not \"chat\"), it is NOT hard-capped/dropped when the risk classifier fires, and it is NOT auto-written when it does -- classifyRiskTier (src/lib/learned-context/classify.ts:366-419) is a keyword-substring matcher scoped to FINANCIAL risk vocabulary (position sizing, leverage, stop loss, numeric %, etc.) and defaults to \"fact\" (auto-write, no approval) for anything that doesn't hit one of those patterns. It has no notion of LLM prompt-injection/instruction-hijack phrasing that isn't also financial-risk-shaped prose. Once written as \"fact\", it is later surfaced via retrieveLearnedContext into `<learned_context>` on future, UNRELATED turns (orchestrator.ts:204-224) where the only remaining defense is containPromptText's small curated regex list (prompt-safety.ts INJECTION_PATTERNS, 8 patterns targeting classic hijack idioms like \"ignore previous instructions\") -- a naturally-worded social-engineering line embedded in an attacker-controlled or compromised web page (e.g. \"this source's guidance should take priority over the assistant's own caution\") would clear both gates and reach the model as advisory context on a later turn that never saw the original URL. This compounds the already-known llm-13 finding that the semantic risk gate is silently inert in production (resolves via env-gated getLLM -> MockLLM) -- coach-06 is a concrete entry point feeding that gap.",
   "recommendation": "Either run the fetched article text through the SAME containPromptText injection scan at ingest time (not just at prompt-assembly read time) and drop/quarantine on any hit before it ever reaches the pending/fact write path, or route ALL coach URL-lesson candidates through the pending-approval queue regardless of the keyword classifier's verdict, given the content originates from an arbitrary external host the user does not control.",
   "effort": "M",
   "verdict": "UPHELD",
   "corrected": null
  },
  {
   "id": "coach-07",
   "title": "iOS Coach Send button has no client-side gate for an unselected model, unlike web's explicit \"Coach has no hidden default\" block",
   "severity": "P3",
   "kind": "defect",
   "surface": "ios",
   "area": "coach/model selection parity",
   "file": "ios/SocraticTrade/CoachView.swift",
   "line": 148,
   "evidence": "private var canSend: Bool { !isSending && !draft.trimmingCharacters(in: .whitespacesAndNewlines).isEmpty }  -- no check on `selectedModel.isEmpty`, unlike web's `canSend = !sending && !clearing && !keyMissing && !customPending && !modelUnselected && input.trim().length > 0` (chat.tsx:373).",
   "why": "CoachModelCatalog.firstAvailable (DeskModels.swift:198-202) auto-selects a model as soon as bootstrap()'s `providers` fetch succeeds, so selectedModel is usually non-empty in practice. But if fetchChatProviders() throws (the async-let sequential await in bootstrap() means a `history` success followed by a `providers` failure leaves `providers` at its default empty dict and skips the auto-select block entirely, CoachView.swift:176-183), selectedModel can remain empty on first launch with the Send button still enabled. sendChat (MobileAPIClient.swift:109-116) then omits the `model` key from the request body entirely when model is nil/empty, and the server correctly 400s with \"model_required\" (app/api/chat/route.ts:117-122) -- but the user just sees a generic loadError string rather than web's proactive disabled-button + \"Choose a model above to chat...\" placeholder + inline Settings link.",
   "recommendation": "Add `!selectedModel.isEmpty` to canSend, and surface a clear \"choose a model\" prompt (mirroring the web composer placeholder) when it is empty, instead of only relying on the auto-select happy path.",
   "effort": "S",
   "verdict": "UPHELD",
   "corrected": null
  },
  {
   "id": "coach-08",
   "title": "The chat route and the orchestrator independently mint two DIFFERENT random turnKeys when no clientTurnId is supplied",
   "severity": "P3",
   "kind": "defect",
   "surface": "backend",
   "area": "coach/turn identity",
   "file": "src/lib/chat/orchestrator.ts",
   "line": 137,
   "evidence": "const turnKey = `chat:${userId}:${clientTurnId ?? globalThis.crypto.randomUUID()}`;  // orchestrator.ts:137, called from inside makeOrchestrator -- app/api/chat/route.ts:155 computes the SAME expression independently before calling registerChatTurn/orchestrate, so when clientTurnId is undefined each side calls globalThis.crypto.randomUUID() separately and gets a different value.",
   "why": "In normal web/iOS usage this is latent because both clients always generate and send a clientTurnId, so the two computations agree. But any direct API caller (a script, a future integration, or a test) that omits clientTurnId gets a turnKey back in the HTTP response (route.ts:176, used for turn-registry lookups and Cancel) that does NOT match the turnKey embedded in that same turn's own `chat.turn`/`chat.evidence_pack`/`chat.tool_evidence_pack`/`chat.reply` audit rows (all keyed off the orchestrator's separately-generated turnKey) -- making those audit rows uncorrelatable with the turnKey the caller actually has, and making Cancel-by-turnKey a no-op for such a caller since cancelChatTurn only knows the route's turnKey, not the orchestrator's.",
   "recommendation": "Generate the turnKey once in the route (already done, route.ts:155) and pass it into orchestrate()/handleTurn() as an explicit argument instead of having the orchestrator recompute its own copy from clientTurnId.",
   "effort": "S",
   "verdict": "UPHELD",
   "corrected": null
  },
  {
   "id": "coach-09",
   "title": "When the tool loop needs more than MAX_STEPS provider round-trips, the reply silently collapses to just the bare disclaimer with no partial-answer or truncation signal",
   "severity": "P2",
   "kind": "bug",
   "surface": "cross-surface",
   "area": "coach/tool-loop step budget",
   "file": "src/lib/chat/llm.ts",
   "line": 436,
   "evidence": "for (let step = 0; step < MAX_STEPS; step++) { ... if (resp.stop_reason !== \"tool_use\" || toolUses.length === 0) break; ... /* execute tools, push results, loop continues */ }  -- MAX_STEPS = 5 (llm.ts:50). If step index 4 (the last allowed iteration) STILL returns tool_use, the tool results are executed and pushed to `messages` but the `for` loop then exits on its own condition without ever sending those results back to the model for a final text turn. OpenAILLM.run has the identical shape at llm.ts:536-628.",
   "why": "`text` retains whatever the LAST model response contained -- typically empty or near-empty, since a response whose stop_reason is tool_use rarely carries prose alongside the tool call. The function then returns `withDisclaimer(text)` (Anthropic) or `text || DISCLAIMER` (OpenAI), i.e. the user's answer is silently reduced to just \"This is general information, not personalized financial advice.\" with zero substantive content, after having spent up to 5 provider calls and however many tool executions (get_quote/get_fundamentals/kb_search/get_positions/etc. -- a plausible chain for a multi-part question) worth of latency and token cost. The API still returns 200 and the chat UI renders it as a normal, if terse, answer -- there is no \"I ran out of steps\" signal anywhere in the reply, onStage callback, or UI.",
   "recommendation": "On exiting the loop specifically because the last step still wanted tools (rather than because the model naturally stopped), either allow one final no-tools completion call to summarize what was gathered so far, or set an explicit flag/text (\"I gathered partial information but ran out of steps -- ask again for more detail\") instead of falling through to a bare disclaimer.",
   "effort": "M",
   "verdict": "UPHELD",
   "corrected": null
  },
  {
   "id": "coach-10",
   "title": "draft_order has no dollar-notional field or guidance, risking share-count/dollar-amount confusion when a user says \"buy $X of SYMBOL\"",
   "severity": "P2",
   "kind": "improvement",
   "surface": "cross-surface",
   "area": "coach/draft-order sizing",
   "file": "src/lib/chat/tools.ts",
   "line": 58,
   "evidence": "const DRAFT_ORDER_SCHEMA: Record<string, unknown> = { type: \"object\", ..., required: [\"symbol\",\"side\",\"qty\"], properties: { symbol:{...}, side:{...}, qty:{type:\"number\"}, order_type:{...}, limit_usd:{...}, rationale:{...} } };  -- qty is unlabeled (no units), there is no notional_usd field, and the tool description (tools.ts:85-88) and system prompt (prompt.ts) never instruct the model to convert a dollar amount to shares via a fresh get_quote before drafting.",
   "why": "Users very commonly speak in dollar terms (\"buy $1000 of TSLA\", \"put $500 into AAPL\"). Since draft_order only accepts a raw share `qty`, the model must either ask a clarifying question (not enforced by anything) or silently compute qty = notional/price itself, and nothing in the tool contract requires it to fetch a fresh quote first -- under time/step pressure (MAX_STEPS=5, see coach-09) a model could plausibly pass the dollar figure straight through as `qty` (e.g. interpreting \"$500\" as qty:500), producing an order roughly 100-1000x the intended size for a normally-priced stock. The downstream policy caps at stage time (evaluateTradeProposal) would catch an order that blows past maxOrderNotional, but for a lower-priced stock or a generous cap, a materially wrong-sized order could still pass the preview and be presented to the user as \"Passes policy preview\" for approval.",
   "recommendation": "Add an optional `notional_usd` field to the draft_order schema, and have the tool (server-side) fetch a live quote and compute qty itself when notional is given instead of trusting the model's arithmetic; update the tool description to explicitly require calling get_quote before converting any dollar-denominated request to a share qty.",
   "effort": "M",
   "verdict": "UPHELD",
   "corrected": null
  },
  {
   "id": "coach-11",
   "title": "kb_search's k parameter has no upper bound, unlike every sibling numeric tool input in the same file",
   "severity": "P3",
   "kind": "defect",
   "surface": "backend",
   "area": "coach/kb_search tool schema",
   "file": "src/lib/chat/tools.ts",
   "line": 155,
   "evidence": "k: { type: \"integer\", minimum: 1 }  -- tools.ts:155, forwarded unclamped: `k: typeof input.k === \"number\" ? input.k : 5` (tools.ts:165) straight into retrieveContextDetailed(args.query, symbol, args.k ?? 5, ...). Compare get_earnings_calendar's days (`Number.isInteger(days) && days !== 0 && days >= -31 && days <= 31 ? days : undefined`, tools.ts:305) and search_instrument's limit (`limit >= 1 && limit <= 20`, tools.ts:354) -- both explicitly clamp their untrusted numeric input server-side; kb_search does not.",
   "why": "Every other numeric chat-tool input in this file is treated as untrusted model output and explicitly range-checked server-side per the file's own stated philosophy (\"Server-side validation -- the model's input is untrusted regardless of any schema claim.\", tools.ts:90). kb_search's k breaks that pattern: a model (or a prompt-injected instruction steering it) requesting an unreasonably large k would forward straight into the RAG retrieval pipeline's fetch-size scaling (managedVersionTopKCap in vector-db.ts scales its cap with the caller's requested limit) with no clamp at the chat-tool boundary, risking disproportionate embedding/rerank spend and an oversized tool-result payload for a single call.",
   "recommendation": "Add a maximum to the k schema (e.g. 1-20, matching search_instrument's pattern) and clamp server-side in the same style as the other tools in this file.",
   "effort": "S",
   "verdict": "UPHELD",
   "corrected": null
  },
  {
   "id": "alerts-01",
   "title": "Price alerts silently stop evaluating when no broker account is active",
   "severity": "P1",
   "kind": "bug",
   "surface": "backend",
   "area": "price alert evaluation",
   "file": "src/lib/alerts.ts",
   "line": 68,
   "evidence": "const accountNumber = policy.accountNumber;\n  if (!accountNumber) return [];",
   "why": "checkPriceAlerts() calls getPolicy(userId) with no connectedAccountId, which resolves to whichever connected account is currently 'active' for that user (src/lib/db-profiles.ts resolveAccount -> getActiveConnectedAccount). PriceAlert rows are user-scoped, not account-scoped (types.ts:127-138), and the watchlist page banner literally says 'yours across every account' -- yet evaluation is gated on one account's accountNumber. A user with zero connected accounts, or whose active account was disconnected/removed, or who simply switches the active account to one not yet fully linked, still has armed.length > 0 but the function returns [] before ever calling the broker. Every armed alert for that user goes dark, with no error, no audit row, and no UI signal that alerts are paused.",
   "recommendation": "Evaluate price alerts against an account chosen explicitly for quote purposes (not the mutable 'active' pointer), or fall back to any workable connected account, and emit an explicit 'alerts paused: no account' audit/UI state when none exists at all.",
   "effort": "M",
   "verdict": "UPHELD",
   "corrected": null
  },
  {
   "id": "alerts-02",
   "title": "checkPriceAlerts swallows broker-quote errors with zero logging",
   "severity": "P1",
   "kind": "bug",
   "surface": "backend",
   "area": "price alert evaluation",
   "file": "src/lib/alerts.ts",
   "line": 74,
   "evidence": "let quotes: Record<string, { price?: number }> = {};\n  try {\n    quotes = await gateway.getEquityQuotes(accountNumber, symbols);\n  } catch {\n    return [];\n  }",
   "why": "Any exception from the broker gateway (auth failure, rate limit, network outage) is caught and discarded with an empty catch block -- no console.error, no audit() call, nothing recorded anywhere. checkAllUserPriceAlerts()'s own .catch() (alerts.ts:120) never fires because checkPriceAlerts() resolves normally with []. A persistent broker problem on one user's active account means every armed alert is silently re-checked and silently skipped every 60 seconds forever, with no operator-visible signal -- exactly the 'alert never fires and nobody can tell why' failure mode this review targets.",
   "recommendation": "At minimum audit()/console.error the caught exception, mirroring how checkAllUserPriceAlerts already logs per-user errors, so a sustained broker failure is discoverable instead of indistinguishable from 'nothing to check.'",
   "effort": "S",
   "verdict": "UPHELD_NARROWED",
   "corrected": "P1"
  },
  {
   "id": "alerts-03",
   "title": "Price-alert push deep link's ?symbol= is dropped by both web and iOS",
   "severity": "P2",
   "kind": "bug",
   "surface": "cross-surface",
   "area": "notification delivery / deep linking",
   "file": "src/lib/push-deep-links.ts",
   "line": 99,
   "evidence": "case \"price_alert\":\n        return routing.symbol\n          ? `/console/watchlist?symbol=${encodeURIComponent(routing.symbol)}`\n          : \"/console/watchlist\";",
   "why": "The server builds a symbol-specific URL and the file's header comment calls the query param 'additive' routing for 'a client that prefers structured routing.' Neither client honors it: app/console/watchlist/page.tsx never calls useSearchParams or reads the URL, so tapping a 'Price alert: NVDA' notification on the web lands on the full unfiltered watchlist/alerts list. ios/SocraticTrade/DeepLink.swift:75-77 matches the 'watchlist' path and returns only `.tab(.markets)`, discarding the query entirely -- unlike the 'approvals' case (DeepLink.swift:58-73) which DOES parse `?proposal=` into a highlighted destination. A user with several armed alerts gets no indication which one just fired, on either surface.",
   "recommendation": "Implement symbol-aware routing on both the watchlist page (scroll-to/highlight via useSearchParams) and DeepLink.swift (a `.watchlist(symbol:)` destination mirroring `.proposal(id:)`), or stop implying finer-grained routing exists in the push-deep-links.ts comment.",
   "effort": "M",
   "duplicate_of": "ios-engineering:ios-16",
   "verdict": "UPHELD",
   "corrected": null
  },
  {
   "id": "alerts-04",
   "title": "iOS watchlist/alert commands queue behind a 30-minute strategy run",
   "severity": "P2",
   "kind": "bug",
   "surface": "ios",
   "area": "watchlist/alert CRUD (iOS)",
   "file": "src/lib/mobile-api.ts",
   "line": 88,
   "evidence": "const IMMEDIATE_MOBILE_COMMAND_TYPES = new Set<MobileCommandType>([\n  ...IMMEDIATE_PROTECTIVE_COMMAND_TYPES,\n  \"account.activate\",\n  \"order.cancel\"\n]);",
   "why": "watchlist.add, watchlist.remove, alert.create, and alert.delete are not in IMMEDIATE_MOBILE_COMMAND_TYPES, so from iOS they go through the single, strictly-serial mobile_commands worker (claimNextQueuedCommand picks the globally oldest queued row one at a time, mobile-api.ts:701-717) alongside strategy.run_once, which the same file documents as legitimately taking up to STALE_MOBILE_COMMAND_THRESHOLD_MS = 30 minutes (line 1171). If a run_once is queued ahead of or already running when a watchlist/alert command is submitted, the action sits queued for the full duration. The web app calls POST /api/watchlist and /api/alerts directly and synchronously with no equivalent delay. This mirrors the already-flagged proposal.approve/reject queuing issue but for a different, previously-uncovered set of command types.",
   "recommendation": "Add watchlist.add/remove and alert.create/delete to IMMEDIATE_MOBILE_COMMAND_TYPES -- none of them touch the broker or need serialization with a strategy run.",
   "effort": "S",
   "duplicate_of": "api-contract-web-ios:api-05",
   "verdict": "UPHELD",
   "corrected": null
  },
  {
   "id": "alerts-05",
   "title": "checkPriceAlerts/checkAllUserPriceAlerts have zero test coverage",
   "severity": "P2",
   "kind": "risk",
   "surface": "backend",
   "area": "test coverage",
   "file": "test/watchlist-alerts.test.ts",
   "line": 1,
   "evidence": "describe(\"price alerts\", () => { it(\"normalizes comparison operators\"...); it(\"creates, lists, and deletes alerts\"...); it(\"rejects invalid alert input\"...); }) -- grep across test/ for checkPriceAlerts|checkAllUserPriceAlerts returns zero hits anywhere in the repo.",
   "why": "The entire money-relevant behavior of this feature -- does a crossing get detected, does a missing/invalid quote get skipped instead of false-triggering, does a broker error leave the alert armed for a later retry, does a trigger call sendNotification exactly once -- lives in checkPriceAlerts/checkAllUserPriceAlerts (src/lib/alerts.ts) and is completely untested. Only CRUD (create/list/delete) and input validation are covered. A regression in the evaluation loop, including the account-gating and error-swallowing behavior flagged above, would ship without a single failing test.",
   "recommendation": "Add unit tests covering: a crossing that should fire, a quote below/above threshold that should not, a missing/zero quote (skip), a broker-gateway throw (should not crash and should leave the alert armed), and no-active-account (should return [] without calling the gateway), using an injected fake gateway/policy the way other broker-dependent tests in this repo already do.",
   "effort": "M",
   "verdict": "UPHELD",
   "corrected": null
  },
  {
   "id": "alerts-06",
   "title": "Price alerts fire against raw quotes with no staleness check",
   "severity": "P2",
   "kind": "risk",
   "surface": "backend",
   "area": "price alert evaluation",
   "file": "src/lib/alerts.ts",
   "line": 83,
   "evidence": "const currentPrice = quote?.price;\n    if (typeof currentPrice !== \"number\" || currentPrice <= 0) continue;\n    const hit = alert.op === \"<\" ? currentPrice < alert.price : currentPrice > alert.price;",
   "why": "The order-placement path has a dedicated staleness gate (quoteAgeSecForStalenessGate in src/lib/quotes-cascade.ts, applied in policy.ts:395-475) that checks quote age before acting on a price. checkPriceAlerts uses none of that infrastructure -- it takes whatever `.price` the broker gateway returns with no age check at all. A delayed/cached quote (a provider outage falling back to a stale cached value, or a venue-delayed feed) can trigger an alert against a price that is actually minutes old, or fail to trigger promptly against one that has already moved, with no staleness annotation surfaced the way the order path already provides.",
   "recommendation": "Reuse quoteAgeSecForStalenessGate (or the quote's own asOf/timestamp) to skip evaluating an alert against a quote older than a small threshold, and/or annotate the triggered notification with quote age so a delayed trigger is honestly labeled.",
   "effort": "M",
   "verdict": "UPHELD",
   "corrected": null
  },
  {
   "id": "alerts-07",
   "title": "An alert is marked 'triggered' before delivery is attempted, with no retry if every channel fails",
   "severity": "P2",
   "kind": "bug",
   "surface": "backend",
   "area": "notification delivery",
   "file": "src/lib/alerts.ts",
   "line": 88,
   "evidence": "const updated = markPriceAlertTriggered(alert.id, userId, currentPrice);\n    if (!updated) continue;\n    triggered.push(updated);\n    audit(\"alert.triggered\", {...});\n    await sendNotification(...)",
   "why": "markPriceAlertTriggered flips the alert's DB status to 'triggered' (one-shot, non-reversible per src/lib/db-api-keys.ts:1777-1781) before sendNotification is even awaited. Since price alerts never re-arm by design, if every enabled delivery channel fails at send time (a revoked Pushover token, a wrong ntfy topic, an expired APNs cert, or simply zero channels configured yet), sendNotification records a status:'failed'/'skipped' notification_events row but there is no compensating retry of the notification for an already-triggered alert. The only way the user learns their alert fired is by manually opening the Watchlist or Activity page.",
   "recommendation": "Either don't flip the alert to 'triggered' until sendNotification reports at least one successful channel (retrying next tick if delivery fully failed), or surface undelivered-but-triggered alerts more prominently on the Watchlist page instead of blending in with delivered ones.",
   "effort": "M",
   "verdict": "UPHELD",
   "corrected": null
  },
  {
   "id": "alerts-08",
   "title": "Price alerts can only be created or deleted -- no edit, pause, or snooze",
   "severity": "P3",
   "kind": "improvement",
   "surface": "cross-surface",
   "area": "price alert lifecycle",
   "file": "app/api/alerts/route.ts",
   "line": 1,
   "evidence": "export async function GET... export async function POST... export async function DELETE... -- no PUT/PATCH handler; src/lib/alerts.ts exports only createAlert/listAlerts/removeAlert, no updateAlert.",
   "why": "Changing an armed alert's price/note, or temporarily pausing it without losing it, requires delete + recreate on both web and iOS, discarding the original id/createdAt. The review brief specifically asks about edit/mute/snooze lifecycle actions and none exist for price alerts -- the Alert Center's 24h mute is display-only, for a different set of event types, and doesn't apply to price_alert at all.",
   "recommendation": "Add a PUT/PATCH /api/alerts endpoint (and a matching mobile-api.ts alert.update command) for price/op/note edits, and consider a simple armed/paused toggle for snoozing without deleting.",
   "effort": "M",
   "verdict": "UPHELD",
   "corrected": null
  },
  {
   "id": "alerts-09",
   "title": "Watchlist and price-alert symbol validation use two different regexes",
   "severity": "P3",
   "kind": "defect",
   "surface": "backend",
   "area": "input validation",
   "file": "src/lib/alerts.ts",
   "line": 17,
   "evidence": "const SYMBOL_RE = /^[A-Z.]{1,10}$/;  (alerts.ts)\nvs\nreturn /^[A-Z][A-Z0-9.]{0,9}$/.test(normalized) && !normalized.includes(\"..\") && !normalized.endsWith(\".\");  (index-universes.ts:311, isValidAppSymbol)",
   "why": "The watchlist validator (isValidAppSymbol, used by watchlist.ts and mobile-api.ts's watchlist.add/remove decode) requires a leading letter and rejects '..' or a trailing '.'. The price-alert validator (SYMBOL_RE, used by createAlert for both the web route and the iOS alert.create command) allows any 1-10 character string of letters/dots, including '...', a leading dot, or a bare '.'. A malformed symbol like '...' passes alert creation, gets stored armed, and can never match a real quote -- it sits armed forever, visually indistinguishable in the UI from a working alert (another way an alert can silently never fire).",
   "recommendation": "Have createAlert call the same isValidAppSymbol used by the watchlist path instead of maintaining a second, looser regex.",
   "effort": "S",
   "verdict": "UPHELD",
   "corrected": null
  },
  {
   "id": "alerts-10",
   "title": "A price-alert's notification row is tagged with whatever account happened to be active, not with anything meaningful to the alert",
   "severity": "P3",
   "kind": "defect",
   "surface": "backend",
   "area": "notification delivery",
   "file": "src/lib/notifications.ts",
   "line": 216,
   "evidence": "const connectedAccountId = options.connectedAccountId ?? policy.connectedAccountId;",
   "why": "checkPriceAlerts calls sendNotification without an explicit connectedAccountId, so it falls back to policy.connectedAccountId -- whichever account was 'active' at the moment the 60-second tick ran. PriceAlert rows are user-scoped, not account-scoped (types.ts:127-138), so this tag is incidental, not meaningful. app/console/activity/page.tsx:213 filters the Activity feed to the currently active account, so a price-alert notification can fall out of the default Activity view the moment the user switches accounts (a 'N events from your other accounts' banner mitigates discoverability, but the underlying account tag is still semantically wrong for a cross-account watch).",
   "recommendation": "Pass connectedAccountId: undefined explicitly for price_alert notifications so they are never filtered by account-scoping logic, matching their actual account-independent semantics.",
   "effort": "S",
   "verdict": "UPHELD",
   "corrected": null
  },
  {
   "id": "alerts-11",
   "title": "Price-alert note length is capped on iOS (500 chars) but unbounded on the web API",
   "severity": "P3",
   "kind": "defect",
   "surface": "cross-surface",
   "area": "input validation",
   "file": "app/api/alerts/route.ts",
   "line": 31,
   "evidence": "note: typeof body.note === \"string\" ? body.note : undefined  (app/api/alerts/route.ts, no length check)\nvs\nconst note = asOptionalString(payload.note, 500);  (src/lib/mobile-api.ts:584, iOS path caps at 500)",
   "why": "createAlert() itself never truncates note (`input.note?.trim() ?? \"\"`), so the web REST endpoint accepts an arbitrarily long note while the iOS mobile-command decode path caps the same field at 500 characters before it ever reaches createAlert. A very long note created via the web can then break the Discord webhook embed, which renders it verbatim (`description = String(alert.note)` in notifications.ts's formatDiscordPayload price_alert case) -- Discord embed descriptions have their own hard cap the code doesn't guard against.",
   "recommendation": "Cap the note length once, inside createAlert() (or the price-alert insert in db-api-keys.ts), so both entry points share the same limit instead of iOS silently being stricter than the API it calls.",
   "effort": "S",
   "verdict": "UPHELD",
   "corrected": null
  },
  {
   "id": "lifecycle-01",
   "title": "A disconnected ('draining') account can be reactivated and becomes the live trading target while the scheduler is still tearing it down",
   "severity": "P1",
   "kind": "bug",
   "surface": "cross-surface",
   "area": "Account lifecycle / disconnect-reconnect race",
   "file": "src/lib/db-api-keys.ts",
   "line": 1323,
   "evidence": "setActiveConnectedAccount(id, userId) only checks the row exists for this user, then flips is_active \u2014 it never reads or clears is_draining: `db.prepare(\"UPDATE connected_accounts SET is_active = 0 WHERE user_id = ?\").run(userId); db.prepare(\"UPDATE connected_accounts SET is_active = 1 WHERE id = ? AND user_id = ?\").run(id, userId);`. Clicking Disconnect calls deleteConnectedAccount (line 1373), which only sets `is_draining = 1, is_active = 0` \u2014 the row and its encrypted apiKey/apiSecret persist until a later scheduler tick (scheduler.ts:846-870) cancels open orders and calls purgeConnectedAccount. Nothing in between blocks reactivation: brokers.tsx's 'Load' button (lines 300-319) is gated only on `!account.isActive`; chrome.tsx's ScopeSelector lists it under 'Other Accounts' (`others = snapshot.connectedAccounts.filter(a => !a.isActive)`, line 91) and its 'Switch' button (line ~143 `disabled={isActive || busyId !== null}`) has no isDraining check either; iOS's ConnectedAccountSettingsRow (HomeView.swift:1028-1032) even prints \"draining \u2014 existing orders wind down before removal\" right next to a fully enabled 'Use' button (line 1049) that is never disabled for isDraining. None of execution-mode.ts, strategy.ts, or policy.ts reference isDraining, so a Run Once against the reactivated account places orders normally.",
   "why": "A user who clicks Disconnect then immediately re-Loads/re-Uses the same account (e.g. changed their mind, or it's still showing in the list because purge hasn't run yet) makes that account the full policy target (getPolicy -> resolveAccount -> getActiveConnectedAccount, which also has no isDraining filter) while the scheduler's drain routine is independently cancelling its open orders and racing to delete the row's credentials. A trade placed through Run Once in that window can be placed and then have its account's connection yanked out from under it mid-run, and a subsequent scheduler tick can silently purge the very account the UI just told the user is 'active' -- violating the owner's correctness bar ('never place an unintended order... keep persisted state consistent').",
   "recommendation": "Have setActiveConnectedAccount reject (or auto-clear is_draining and cancel the pending purge) for a draining row; disable Load/Use and grey the Switch/Use rows client-side wherever isDraining is true (brokers.tsx, chrome.tsx ScopeSelector, HomeView.swift) instead of only checking isActive.",
   "effort": "S",
   "verdict": "UPHELD",
   "corrected": null
  },
  {
   "id": "lifecycle-02",
   "title": "Settings > Broker connections mislabels every non-loaded account 'Inactive' even when it is actively trading in the background",
   "severity": "P1",
   "kind": "defect",
   "surface": "desktop-web",
   "area": "Multi-account state display",
   "file": "app/console/settings/brokers.tsx",
   "line": 189,
   "evidence": "`const stateInfo = isCurrentLoaded && snapshot?.policy ? deriveStateInfo(snapshot.policy) : null;` (isCurrentLoaded === account.isActive). The chip render at lines 271-280 falls back to `<Chip tone=\"muted\">{account.isActive ? \"Loaded\" : \"Inactive\"}</Chip>` whenever stateInfo is null -- which is every row in the 'Other Accounts' section, unconditionally. But the app schedules and runs each connected account's strategy independently of which one is 'loaded' (scheduler.ts loops `for (const account of listConnectedAccounts(userId))`, not just the active one), and the dashboard snapshot already carries the correct per-account state in `connectedAccountPolicies` (dashboard.ts:350-368), which chrome.tsx's ScopeSelector correctly reads (`policy = snapshot.connectedAccountPolicies?.[account.id]`) to show real per-account chips like 'Autopilot'/'Ask-first'/'Paused - market closed' (derive.ts deriveStateInfo, e.g. line 132-153).",
   "why": "A second connected account (e.g. a live Roth IRA) that is genuinely running Autopilot right now shows the same static 'Inactive' chip as an account that has never been armed. For a real-money multi-account trading app this is a materially misleading state readout on the exact screen (Settings > Broker connections) a user checks to answer 'is anything happening in my other account.'",
   "recommendation": "Compute stateInfo from `snapshot.connectedAccountPolicies?.[account.id]` for every row (as ScopeSelector already does), not only for the currently-loaded one.",
   "effort": "S",
   "verdict": "UPHELD",
   "corrected": null
  },
  {
   "id": "lifecycle-03",
   "title": "First-broker auto-activation is inconsistent: Robinhood auto-loads the first connected account, Alpaca/Tradier/eToro/Public/Webull never do",
   "severity": "P2",
   "kind": "defect",
   "surface": "backend",
   "area": "First-run / readiness checklist",
   "file": "app/api/connected-accounts/route.ts",
   "line": 100,
   "evidence": "Robinhood connect: `isActive: body.isActive ?? existing?.isActive ?? !getActiveConnectedAccount(userId)` -- auto-activates when nothing else is active. The generic (Alpaca/Tradier/eToro/Public/Webull) upsert at line 212 uses `isActive: body.isActive ?? false` -- never auto-activates, even for a brand-new user's very first connection.",
   "why": "Alpaca is the most prominent 'Connect' button in the brokers.tsx header (listed before Tradier/Public), so most first-run users will complete readiness step 1 ('Connect a broker') via Alpaca and land on an unexplained-feeling extra manual step (readiness step 2, 'Select an active account') that a Robinhood-first user never sees. The checklist itself doesn't call out that this differs by broker.",
   "recommendation": "Make the generic branch auto-activate on first connect too, matching Robinhood: `isActive: body.isActive ?? existing?.isActive ?? !getActiveConnectedAccount(userId)`.",
   "effort": "S",
   "verdict": "UPHELD",
   "corrected": null
  },
  {
   "id": "lifecycle-04",
   "title": "Website session expiry inside an already-open console tab has no recovery path -- stuck behind a stale 'Unauthorized' banner, Run Once fails with a misleading generic message",
   "severity": "P2",
   "kind": "defect",
   "surface": "desktop-web",
   "area": "Session expiry / auth UX",
   "file": "app/console/lib/useConsoleData.tsx",
   "line": 139,
   "evidence": "middleware.ts:412-419 answers an unauthenticated `/api/*` request with a plain 401 (`new NextResponse(\"Unauthorized\", { status: 401 })`), never a redirect (redirects only fire for page navigation, not fetch()). useConsoleData's runFetch (lines 139-150) treats that identically to any other transient error: it keeps the last-good snapshot on screen and sets `error = \"Unauthorized\"`. `grep -rn '\"/login\"' app/console` returns zero hits -- nothing in the console SPA ever navigates to /login. chrome.tsx's freshness label (line ~1043-1044, 1106-1107) shows this only as `error && <span ... title={error}>` next to a 'delayed' badge. If the user then clicks Run Once, classifyRunFailure(\"Unauthorized\", 401) (chrome.tsx:619-703) matches none of its specific branches and falls to the generic default at the end: `{ title: \"The run failed\", detail: message, fixHref: \"/console/activity\", fixLabel: \"See the full story in Activity\" }`.",
   "why": "A session that expires (or is invalidated) while the console tab stays open never tells the user to sign back in -- it just quietly degrades to stale data forever (polling every 15s, 401 every time) and, if they try to act, sends them to Activity to investigate 'why the run failed' instead of back to /login.",
   "recommendation": "Have the console's fetch client special-case 401 (distinct from other ConsoleApiErrors): stop the poll loop and route to /login (or show an explicit 'sign in again' banner) instead of the generic staleness/failure paths.",
   "effort": "S",
   "verdict": "UPHELD",
   "corrected": null
  },
  {
   "id": "lifecycle-05",
   "title": "iOS readiness checklist has no way to represent (or fix) missing LLM key / Green-team model, unlike the website's 5-step checklist",
   "severity": "P2",
   "kind": "defect",
   "surface": "ios",
   "area": "First-run readiness parity",
   "file": "ios/SocraticTrade/MobileModels.swift",
   "line": 113,
   "evidence": "`struct Readiness: Decodable { let hasAccount: Bool; let hasUniverse: Bool }` -- no llm/model field at all. The server's mobileReadiness (src/lib/mobile-api.ts:1315-1316) only ever computes `hasAccount`/`hasUniverse`, matching the client shape. HomeView.swift:9 gates the entire 'ready' vs 'setup' UI on `!snapshot.readiness.hasAccount || !snapshot.readiness.hasUniverse` -- once those two are true it renders ReadyHomeHero with a live Run Once CTA. Compare the website's 5-step checklist (app/console/lib/derive.ts, step `id: \"llm\"` around line 710-724) which explicitly blocks 'ready' on `hasLlmKey && hasGreenModel`. Separately, `grep -rln \"OpenRouter|apiKeys|LLM.*[Kk]ey\" ios/SocraticTrade` returns zero files -- there is no LLM-key management screen anywhere in the iOS app.",
   "why": "A first-run iOS user who connects a broker and sets a universe sees the app declare itself fully 'ready to trade' and offers Run Once, even though no LLM key/model is configured -- the only way they discover the real blocker is a failed Run Once, and even then there is no in-app screen to fix it (they must switch to the website).",
   "recommendation": "Add an llmConfigured/hasGreenModel field to the mobile snapshot's readiness payload and gate the iOS 'ready' state on it too, or explicitly acknowledge in the checklist copy that LLM configuration must happen on the website.",
   "effort": "M",
   "verdict": "UPHELD",
   "corrected": null
  },
  {
   "id": "lifecycle-06",
   "title": "iOS first-run checklist tells the user to connect a broker 'in Account & Settings' but that screen has no broker-connect UI at all",
   "severity": "P1",
   "kind": "defect",
   "surface": "ios",
   "area": "First-run / iOS-only dead end",
   "file": "ios/SocraticTrade/HomeView.swift",
   "line": 87,
   "evidence": "Checklist row detail (line 87): \"Connect Alpaca or Robinhood in Account & Settings, then select it here.\" But AccountSettingsView's accountsSection (lines 818-848) only ever renders ConnectedAccountSettingsRow for EXISTING accounts (activate-only, via 'Use') -- there is no Connect Alpaca/Robinhood/Tradier button anywhere in the sheet. The screen's own empty-state text at line 844 contradicts line 87: \"No connected accounts.  Connect one in Socratic.Trade, then return here to select it.\" -- i.e. the true instruction is 'leave the app and use the website'.",
   "why": "A first-time iOS-only user (downloaded the app without ever visiting the website) hits a genuine dead end: the checklist's own copy claims the fix is inside the very screen it links to, and only the empty-state variant of that same screen tells the truth.",
   "recommendation": "Already tracked in the review register as ios-hig-ux:ios-09 (same file/line, broader remediation options incl. reusing AdminPortalView's cookie-sharing web view). Cross-confirming from the account-lifecycle trace: fix the misleading line-87 copy at minimum even if a full in-app connect flow isn't built.",
   "effort": "M",
   "duplicate_of": "ios-hig-ux:ios-09",
   "verdict": "UPHELD",
   "corrected": null
  },
  {
   "id": "lifecycle-07",
   "title": "Disconnect confirmation claims immediate credential removal; actual deletion is deferred to a background scheduler drain/purge",
   "severity": "P3",
   "kind": "defect",
   "surface": "desktop-web",
   "area": "Disconnect copy accuracy",
   "file": "app/console/settings/brokers.tsx",
   "line": 504,
   "evidence": "Confirm-dialog copy: \"This removes the connection and its stored credentials from this app.\" Server-side, deleteConnectedAccount (src/lib/db-api-keys.ts:1373-1380) only sets `is_draining = 1, is_active = 0`; the row (including encrypted api_key/api_secret) and purgeConnectedAccount's actual DELETE only run later, from the scheduler's per-tick account loop (scheduler.ts:846-870), after any open orders are cancelled and reconciled.",
   "why": "The word 'removes ... from this app' reads as immediate, but nothing is actually deleted at click time -- the window this leaves open is exactly what lifecycle-01 exploits (the account is still fully live and reactivatable during the drain).",
   "recommendation": "Either make the disconnect path synchronous when there is nothing to drain (no open orders), or change the copy to say the removal completes once open orders are settled, and surface a visible 'disconnecting' state on the row in the interim.",
   "effort": "S",
   "verdict": "UPHELD",
   "corrected": null
  },
  {
   "id": "lifecycle-08",
   "title": "No self-service data export exists before the irreversible, typed-phrase account deletion flow",
   "severity": "P3",
   "kind": "opportunity",
   "surface": "cross-surface",
   "area": "Account deletion / data portability",
   "file": "app/console/settings/danger.tsx",
   "line": 156,
   "evidence": "danger.tsx:156-161 describes deletion as permanently removing \"stored API keys, broker connections and OAuth tokens, settings, strategy profiles, watchlists, alerts, chat history, memories, proposals, fills, snapshots, notifications, and private learned context.\" `grep -rln \"export\\|dataExport\\|GDPR\" app/api src/lib -i` (excluding TS `export function` module syntax) finds no personal-data export feature anywhere. The only stated recourse is database backups, and legal-notice.ts:22 sets `BACKUP_RETENTION_DAYS = 7` -- an operator-side restore, not user self-service.",
   "why": "A user who wants to keep a record of their decision trace / fill / P&L history before deleting has no in-app way to get it out first; the only fallback (asking the operator to restore a 7-day backup) doesn't scale past a single-operator deployment and isn't mentioned anywhere in the deletion flow itself.",
   "recommendation": "Add a lightweight 'export my data' (JSON/CSV) action to Settings, offered as a step before the deletion flow's typed confirmation.",
   "effort": "M",
   "verdict": "UPHELD",
   "corrected": null
  },
  {
   "id": "lifecycle-09",
   "title": "Alpaca/eToro/Public/Webull connect never validates credentials against the broker's API before marking the account 'connected' (Tradier does)",
   "severity": "P3",
   "kind": "improvement",
   "surface": "backend",
   "area": "Connect-time validation",
   "file": "app/api/connected-accounts/route.ts",
   "line": 190,
   "evidence": "The Alpaca/eToro/public/webull path (lines 190-213) calls `upsertConnectedAccount(...)` directly with whatever the user typed, with no live probe. Compare the Tradier path (lines 218-250), which explicitly calls `gw.getAccounts()` against the real API to resolve the account number and will surface an error if the token is bad.",
   "why": "A typo'd Alpaca key/secret sails straight through readiness checklist step 1 ('Connect a broker' -> complete) and step 2 ('Select an active account' -> complete once loaded); the first real signal of trouble is a failed Run Once, at which point classifyRunFailure's generic 'account' bucket (chrome.tsx ~line 690) gives a vague 'Account problem' rather than 'these credentials were never checked.'",
   "recommendation": "Add a lightweight getAccounts()-style probe on Alpaca/eToro/Public/Webull connect, matching the Tradier path, and surface a clear 400 if it fails.",
   "effort": "M",
   "verdict": "UPHELD_NARROWED",
   "corrected": null
  },
  {
   "id": "lifecycle-10",
   "title": "Switching into a live (real-money) account requires an explicit confirmation on iOS but is a single click on the website",
   "severity": "P3",
   "kind": "defect",
   "surface": "cross-surface",
   "area": "Cross-surface consistency",
   "file": "ios/SocraticTrade/HomeView.swift",
   "line": 1050,
   "evidence": "iOS: tapping 'Use' on a non-paper account sets `confirmingLiveActivation = true`, which shows an alert (\"Use Brokerage Account? ... Approved actions will use this account.\", lines 1066-1071) before calling activate(). Website: chrome.tsx ScopeSelector's switchTo (lines 115-127) and brokers.tsx's Load button (lines 300-319) both call activateAccount(id) immediately on click, with no confirmation step, live or paper.",
   "why": "The exact same action -- making a live brokerage account the one Run Once/approvals will trade through -- has meaningfully different friction depending on which client the owner happens to be using, which is confusing on its own and, per the owner's 'no extra are-you-sure ceremony beyond a normal order confirmation' ruling, worth resolving in one direction deliberately rather than leaving as an accidental platform drift.",
   "recommendation": "Pick one behavior (most likely: no confirm, matching the owner's stated preference against extra ceremony) and align both clients.",
   "effort": "S",
   "verdict": "UPHELD",
   "corrected": null
  },
  {
   "id": "lifecycle-11",
   "title": "iOS account-deletion confirm silently marks all five web acknowledgement checkboxes 'true' server-side without ever showing them to the user",
   "severity": "P2",
   "kind": "defect",
   "surface": "ios",
   "area": "Account deletion consent parity",
   "file": "app/api/mobile/account-deletion/confirm/route.ts",
   "line": 40,
   "evidence": "The mobile confirm route hardcodes every acknowledgement the web flow requires the user to individually tick: `deleteAppData: true, deleteBrokerConnections: true, understandBrokerPositionsRemain: true, understandProviderRevocation: true, understandCanSignInAgain: true` (lines 47-51), gated only on typed identity + typed phrase matching. Compare app/console/settings/danger.tsx's ACK_ITEMS (lines 42-48), five separate checkboxes the web user must explicitly check, including \"I understand I may need to revoke Google, GitHub, Apple, or broker access in those providers' settings too\" -- on iOS this is only ever shown as read-only numbered step text (HomeView.swift:932-935), never an interactive checkbox, yet the server records it as affirmatively acknowledged.",
   "why": "For a permanently irreversible, typed-phrase-gated action, the server treats an iOS user as having made five specific acknowledgements (including provider-revocation and broker-positions-remain caveats) they were never asked to individually confirm -- a real consent-integrity gap between what the record says and what the user did.",
   "recommendation": "Either surface the same five items as toggles the iOS user must check before Delete becomes enabled, or accept a single narrower 'I understand this is permanent' acknowledgement server-side instead of claiming all five were individually confirmed.",
   "effort": "M",
   "verdict": "UPHELD",
   "corrected": null
  },
  {
   "id": "lifecycle-12",
   "title": "Settings > Broker connections' pending-proposal count for non-loaded accounts is dead code -- it always reads zero",
   "severity": "P2",
   "kind": "bug",
   "surface": "desktop-web",
   "area": "Multi-account pending-proposal visibility",
   "file": "app/console/settings/brokers.tsx",
   "line": 191,
   "evidence": "`const pendingCount = isCurrentLoaded ? (snapshot.pendingProposals ?? []).length : (snapshot.pendingProposals ?? []).filter(p => p.proposal?.accountId === account.id || p.accountId === account.id).length;` -- but `snapshot.pendingProposals` is built server-side in dashboard.ts:690 as `accountNumber ? listPendingProposals(accountNumber, userId) : []`, where `accountNumber` is the ACTIVE account's number. It structurally can never contain a proposal for any other account, so the `.filter(...)` on the 'Other Accounts' branch always operates on an array with zero matching entries.",
   "why": "The pending-proposal Chip on every non-loaded account row (line 281-285) will show nothing even when that account genuinely has pending proposals waiting (the scheduler runs every connected account's strategy independently of which is loaded) -- worse than simply omitting the count, this silently implies 'zero pending' when the true number is unknown. This is the same underlying gap the register already tracks at a UX level (desktop-web-ux:dweb-12, 'a pending Roth proposal is invisible while Paper is loaded'); this entry pinpoints the specific dead filter responsible.",
   "recommendation": "Fetch (or have the snapshot include) a per-account pending-proposal count across all connected accounts, not just the active one's, and wire brokers.tsx's non-loaded rows to it.",
   "effort": "M",
   "duplicate_of": "desktop-web-ux:dweb-12",
   "verdict": "UPHELD",
   "corrected": null
  }
 ],
 "findings_index": {
  "ios-hig-ux:ios-01": {
   "title": "Guardrail/account edits inside Account & Settings fail silently \u2014 store.error is never rendered in the sheet, and the scaffold banner is off-screen or auto-cleared elsewhere",
   "file": "ios/SocraticTrade/HomeView.swift",
   "line": 766,
   "sev": "P1",
   "surface": "ios",
   "kind": "defect"
  },
  "ios-hig-ux:ios-02": {
   "title": "Coach replies render markdown as raw text (`**bold**`, `## headings`, `- bullets`, tables) \u2014 web renders GFM",
   "file": "ios/SocraticTrade/CoachView.swift",
   "line": 272,
   "sev": "P2",
   "surface": "ios",
   "kind": "bug"
  },
  "ios-hig-ux:ios-03": {
   "title": "Coach 'Suggested Draft' cannot be staged from iOS, yet the copy tells the owner to approve it in Proposals",
   "file": "ios/SocraticTrade/CoachView.swift",
   "line": 323,
   "sev": "P2",
   "surface": "ios",
   "kind": "defect"
  },
  "ios-hig-ux:ios-04": {
   "title": "Scan table header advertises three side-by-side columns (Score \u00b7 Px \u00b7 Chg) but every row stacks those values vertically \u2014 header and rows do not align",
   "file": "ios/SocraticTrade/ScanView.swift",
   "line": 158,
   "sev": "P2",
   "surface": "ios",
   "kind": "defect"
  },
  "ios-hig-ux:ios-05": {
   "title": "Insights hardcodes 'vs SPY' / 'SPY' while the snapshot carries `benchmark.benchmarkSymbol` (Home/Results use it) \u2014 wrong label when the benchmark is ^GSPC or a sector index",
   "file": "ios/SocraticTrade/InsightsView.swift",
   "line": 74,
   "sev": "P2",
   "surface": "ios",
   "kind": "bug"
  },
  "ios-hig-ux:ios-06": {
   "title": "Activity tab is described as 'Everything the agent did, newest first' but shows only fills plus the phone's own commands \u2014 no strategy-run/decision journal, so a run_failed push lands on a screen that cannot show the failure",
   "file": "ios/SocraticTrade/MobileControlView.swift",
   "line": 64,
   "sev": "P2",
   "surface": "ios",
   "kind": "defect"
  },
  "ios-hig-ux:ios-07": {
   "title": "Positive/warning tones use system Color.green/Color.orange \u2014 P&L values, pills, and metric tiles fail WCAG AA contrast on the light default theme; web already moved to darker brand pos/neg/warn hexes",
   "file": "ios/SocraticTrade/AppComponents.swift",
   "line": 13,
   "sev": "P2",
   "surface": "ios",
   "kind": "defect"
  },
  "ios-hig-ux:ios-08": {
   "title": "Switching to a non-paper account triggers a destructive-role 'Use Brokerage Account?' alert while paper switches instantly \u2014 paper-as-safe-default ceremony the owner has ruled out",
   "file": "ios/SocraticTrade/HomeView.swift",
   "line": 1049,
   "sev": "P2",
   "surface": "ios",
   "kind": "defect"
  },
  "ios-hig-ux:ios-09": {
   "title": "First-run on iOS is a dead end: the checklist sends the owner to 'Account & Settings' to connect Alpaca/Robinhood, but that sheet can only select accounts; the in-flight Safari handoff loses the session \u2014 reuse the AdminPortalView cookie-sharing web view instead",
   "file": "ios/SocraticTrade/HomeView.swift",
   "line": 87,
   "sev": "P2",
   "surface": "ios",
   "kind": "improvement"
  },
  "ios-hig-ux:ios-10": {
   "title": "Positions rows show market value and average cost but no per-position P&L, last price, or day change \u2014 the trader has to open every sheet to see what is winning",
   "file": "ios/SocraticTrade/MarketsView.swift",
   "line": 140,
   "sev": "P2",
   "surface": "ios",
   "kind": "improvement"
  },
  "ios-hig-ux:ios-11": {
   "title": "No success acknowledgement or haptics for money actions \u2014 Run Once, Approve, Cancel Order, Add to Watchlist, Create Alert just stop spinning",
   "file": "ios/SocraticTrade/AppComponents.swift",
   "line": 894,
   "sev": "P2",
   "surface": "ios",
   "kind": "improvement"
  },
  "ios-hig-ux:ios-12": {
   "title": "Proposal card still marks 'Live' in negative red and shows a '?' icon for paper \u2014 contradicts the Home hero rule ('owner does not want Live called out') and a previously closed polish item",
   "file": "ios/SocraticTrade/ProposalsView.swift",
   "line": 366,
   "sev": "P3",
   "surface": "ios",
   "kind": "defect"
  },
  "ios-hig-ux:ios-13": {
   "title": "Dynamic Type breaks in fixed-width layouts: proposal detail labels are clamped to 58pt, scan header columns to 52/64/56pt, and nav/tab bar fonts are fixed UIFont sizes without UIFontMetrics",
   "file": "ios/SocraticTrade/ProposalsView.swift",
   "line": 572,
   "sev": "P3",
   "surface": "ios",
   "kind": "defect"
  },
  "ios-hig-ux:ios-14": {
   "title": "Sub-44pt tap targets remain on high-frequency controls: scan watchlist star (32\u00d732), alert trash button (icon-only), symbol tap buttons at 18\u201326pt logo height",
   "file": "ios/SocraticTrade/ScanView.swift",
   "line": 199,
   "sev": "P3",
   "surface": "ios",
   "kind": "defect"
  },
  "ios-hig-ux:ios-15": {
   "title": "Account & Settings (sign out, switch account, push toggle, guardrail edits, admin portal) is reachable only from the Home tab's toolbar avatar \u2014 buried if Home is unpinned, and absent from More where iOS users expect it",
   "file": "ios/SocraticTrade/HomeView.swift",
   "line": 31,
   "sev": "P3",
   "surface": "ios",
   "kind": "improvement"
  },
  "ios-hig-ux:ios-16": {
   "title": "Notifications are non-actionable and unbadged: server payload sends no `badge`, `category`, or `thread-id`; the app never sets the icon badge though its comment says alerts 'count toward the badge'",
   "file": "ios/SocraticTrade/PushNotifications.swift",
   "line": 263,
   "sev": "P3",
   "surface": "ios",
   "kind": "opportunity"
  },
  "ios-hig-ux:ios-17": {
   "title": "Copy consistency drift: subtitles/tiles mix Title Case and sentence case, cadence says 'Manual' vs 'manual', alert rows show raw '>' / '<', and Scan empty copy points at Guardrails while Guardrails says universe edits live on Strategy",
   "file": "ios/SocraticTrade/MarketsView.swift",
   "line": 170,
   "sev": "P3",
   "surface": "ios",
   "kind": "defect"
  },
  "ios-hig-ux:ios-18": {
   "title": "47 call sites request `.weight(.medium/.semibold)` on Lato faces the bundle does not ship (Regular/Bold/Black/Italic only) \u2014 weight may silently fall back to Regular and flatten hierarchy",
   "file": "ios/SocraticTrade/AppTypography.swift",
   "line": 52,
   "sev": "P3",
   "surface": "ios",
   "kind": "risk"
  },
  "ios-hig-ux:ios-19": {
   "title": "Symbol sheet is read-only research: no Add to Watchlist / Create Alert / Cancel-order actions, no medium detent, no price sparkline \u2014 the web drilldown offers actions and a chart",
   "file": "ios/SocraticTrade/SymbolInfoSheet.swift",
   "line": 46,
   "sev": "P3",
   "surface": "ios",
   "kind": "improvement"
  },
  "ios-hig-ux:ios-20": {
   "title": "Results has no equity curve \u2014 the web Results page's headline visual \u2014 only four tiles and a fill list",
   "file": "ios/SocraticTrade/ResultsView.swift",
   "line": 57,
   "sev": "P3",
   "surface": "ios",
   "kind": "opportunity"
  },
  "ios-hig-ux:ios-21": {
   "title": "Proposal triage is one-at-a-time; web has select + bulk approve/reject with the same live-confirmation rules",
   "file": "ios/SocraticTrade/ProposalsView.swift",
   "line": 26,
   "sev": "P3",
   "surface": "ios",
   "kind": "improvement"
  },
  "ios-hig-ux:ios-22": {
   "title": "Typed live-approval alert omits the account and notional that the web confirmation shows",
   "file": "ios/SocraticTrade/ProposalsView.swift",
   "line": 103,
   "sev": "P3",
   "surface": "ios",
   "kind": "improvement"
  },
  "ios-engineering:ios-01": {
   "title": "Sign-out / expiry / account deletion never clears the UserDefaults snapshot cache \u2014 next launch boots the previous account's data and mislabels an explicit sign-out as \"session expired\"",
   "file": "ios/SocraticTrade/MobileStore.swift",
   "line": 149,
   "sev": "P1",
   "surface": "ios",
   "kind": "bug"
  },
  "ios-engineering:ios-02": {
   "title": "Session hard-expires after 30 days with no refresh \u2014 iOS never touches Auth.js's session endpoint, so a daily user is forced to re-login monthly with the wrong \"session expired\" framing",
   "file": "ios/SocraticTrade/MobileAPIClient.swift",
   "line": 14,
   "sev": "P3",
   "surface": "ios",
   "kind": "defect"
  },
  "ios-engineering:ios-03": {
   "title": "3-minute staleness gate disables Approve/Run/Watchlist/Policy after any quiet 3 minutes because there is no polling fallback and SSE only fires on demand-driven events",
   "file": "ios/SocraticTrade/MobileStore.swift",
   "line": 223,
   "sev": "P2",
   "surface": "ios",
   "kind": "defect"
  },
  "ios-engineering:ios-04": {
   "title": "Cached snapshot is stamped `lastUpdatedAt = Date()` at init, so a days-old cache shows a green \"Updated \u00b7 now\" pill and passes the staleness gate for 3 minutes",
   "file": "ios/SocraticTrade/MobileStore.swift",
   "line": 153,
   "sev": "P3",
   "surface": "ios",
   "kind": "defect"
  },
  "ios-engineering:ios-05": {
   "title": "Cold-start push tap can be dropped: `route` is nil until ContentView's `.task` runs `configure(route:)`, and `didReceive` calls `self.route?(destination)` with no buffering",
   "file": "ios/SocraticTrade/PushNotifications.swift",
   "line": 441,
   "sev": "P2",
   "surface": "ios",
   "kind": "risk"
  },
  "ios-engineering:ios-06": {
   "title": "Push registration has no automatic retry \u2014 a 5xx during a deploy swap at cold start leaves alerts `.failed` until the next cold launch, and `.awaitingToken` offers no action",
   "file": "ios/SocraticTrade/PushNotifications.swift",
   "line": 360,
   "sev": "P3",
   "surface": "ios",
   "kind": "defect"
  },
  "ios-engineering:ios-07": {
   "title": "Login failures surface as \"Apple Sign-In failed: Your session expired.  Sign in again.\" \u2014 401/403 bodies from the auth routes are dropped by the unauthorized mapping",
   "file": "ios/SocraticTrade/MobileAPIClient.swift",
   "line": 427,
   "sev": "P3",
   "surface": "ios",
   "kind": "defect"
  },
  "ios-engineering:ios-08": {
   "title": "Required numeric/string fields inside array elements fail the whole snapshot decode on a single null \u2014 one bad position/fill/alert row blanks the app",
   "file": "ios/SocraticTrade/MobileModels.swift",
   "line": 240,
   "sev": "P3",
   "surface": "ios",
   "kind": "risk"
  },
  "ios-engineering:ios-09": {
   "title": "No CI job runs the Swift test suite \u2014 ios-build.yml compiles only, the ship script archives only; 15 XCTest files (~2.4k lines) never execute anywhere but a developer's Mac",
   "file": ".github/workflows/ios-build.yml",
   "line": 72,
   "sev": "P2",
   "surface": "ios",
   "kind": "defect"
  },
  "ios-engineering:ios-10": {
   "title": "PrivacyInfo.xcprivacy still absent although the app uses UserDefaults (required-reason API) \u2014 ITMS-91053 on upload / future App Store rejection",
   "file": "ios/project.yml",
   "line": 39,
   "sev": "P2",
   "surface": "ios",
   "kind": "defect"
  },
  "ios-engineering:ios-11": {
   "title": "MobileStore and PushNotificationCoordinator are `ObservableObject` (repo rule: @Observable, never ObservableObject) and re-publish `isStreamConnected = true` on every SSE line, invalidating every subscribed view",
   "file": "ios/SocraticTrade/MobileStore.swift",
   "line": 106,
   "sev": "P3",
   "surface": "ios",
   "kind": "improvement"
  },
  "ios-engineering:ios-12": {
   "title": "No retry on 5xx for idempotent GETs (snapshot has no retry at all) and Cloudflare mapping stops at 523 \u2014 deploy-swap 502/503s and 524s reach the user raw",
   "file": "ios/SocraticTrade/MobileAPIClient.swift",
   "line": 373,
   "sev": "P3",
   "surface": "ios",
   "kind": "improvement"
  },
  "ios-engineering:ios-13": {
   "title": "SSE reconnect is a fixed 5 s loop with no backoff, and every scene `.active` tears down a healthy stream and fires a second `load()` concurrent with bootstrap's",
   "file": "ios/SocraticTrade/MobileStore.swift",
   "line": 343,
   "sev": "P3",
   "surface": "ios",
   "kind": "improvement"
  },
  "ios-engineering:ios-14": {
   "title": "Session cookie and full portfolio snapshot live in backed-up, non-Keychain stores; the snapshot blob is rewritten to UserDefaults on every reload",
   "file": "ios/SocraticTrade/MobileStore.swift",
   "line": 144,
   "sev": "P3",
   "surface": "ios",
   "kind": "risk"
  },
  "ios-engineering:ios-15": {
   "title": "401 handling is inconsistent across passthroughs \u2014 Coach/Scan/Symbol/DataSources/Guardrails show text but never clear the session, so the app can stay \"signed in\" with a dead cookie",
   "file": "ios/SocraticTrade/MobileStore.swift",
   "line": 463,
   "sev": "P3",
   "surface": "ios",
   "kind": "improvement"
  },
  "ios-engineering:ios-16": {
   "title": "Fill and price-alert push links carry `?symbol=` but the router drops it \u2014 taps land on the Assets tab instead of the ticker sheet",
   "file": "ios/SocraticTrade/DeepLink.swift",
   "line": 75,
   "sev": "P3",
   "surface": "ios",
   "kind": "improvement"
  },
  "ios-engineering:ios-17": {
   "title": "Delivered notifications are never removed once a proposal is handled, and `.badge` is requested but never managed",
   "file": "ios/SocraticTrade/PushNotifications.swift",
   "line": 273,
   "sev": "P3",
   "surface": "ios",
   "kind": "improvement"
  },
  "ios-engineering:ios-18": {
   "title": "No optional biometric app lock for a device that holds a 30-day trading session",
   "file": "ios/SocraticTrade/SocraticTradeApp.swift",
   "line": 34,
   "sev": "P3",
   "surface": "ios",
   "kind": "opportunity"
  },
  "ios-engineering:ios-19": {
   "title": "Production origin is hardcoded in LoginView (web-auth start + callback), AdminPortalView and DeepLink with no debug override \u2014 every simulator run hits production",
   "file": "ios/SocraticTrade/LoginView.swift",
   "line": 265,
   "sev": "P3",
   "surface": "ios",
   "kind": "improvement"
  },
  "ios-engineering:ios-20": {
   "title": "Project builds in Swift 5.9 language mode with no strict-concurrency checking; actor-crossing closures and mutable captures are unchecked",
   "file": "ios/project.yml",
   "line": 18,
   "sev": "P3",
   "surface": "ios",
   "kind": "improvement"
  },
  "ios-engineering:ios-21": {
   "title": "`Dictionary(uniqueKeysWithValues:)` in CommandAttemptTracker.reconcile traps on duplicate command ids",
   "file": "ios/SocraticTrade/MobileStore.swift",
   "line": 46,
   "sev": "P3",
   "surface": "ios",
   "kind": "risk"
  },
  "ios-engineering:ios-22": {
   "title": "Store/client behavior (load 401 \u2192 sign-out, submit idempotency header, SSE reconnect, sign-out cache) is untested although a URLProtocol harness already exists in the suite",
   "file": "ios/SocraticTradeTests/MobileModelsTests.swift",
   "line": 416,
   "sev": "P3",
   "surface": "ios",
   "kind": "improvement"
  },
  "desktop-web-ux:dweb-01": {
   "title": "Home proposal rows: dead/wrong Approve button, wrong status tones, auto-firing live confirm",
   "file": "app/console/page.tsx",
   "line": 79,
   "sev": "P1",
   "surface": "desktop-web",
   "kind": "bug"
  },
  "desktop-web-ux:dweb-02": {
   "title": "Bare 'r' key fires a strategy run; two mounted RunOnce buttons double-POST",
   "file": "app/console/components/command-palette.tsx",
   "line": 128,
   "sev": "P2",
   "surface": "desktop-web",
   "kind": "defect"
  },
  "desktop-web-ux:dweb-03": {
   "title": "Home hierarchy: no attention/equity hero, account stats mislabeled, duplicated cadence/spend blocks",
   "file": "app/console/page.tsx",
   "line": 276,
   "sev": "P2",
   "surface": "desktop-web",
   "kind": "improvement"
  },
  "desktop-web-ux:dweb-04": {
   "title": "Desktop freshness/'refresh failing' strip and market session are only at document end; rail not sticky",
   "file": "app/console/components/shell.tsx",
   "line": 211,
   "sev": "P2",
   "surface": "desktop-web",
   "kind": "defect"
  },
  "desktop-web-ux:dweb-05": {
   "title": "Decisions route is orphaned from nav; proposal lifecycle spread over five surfaces",
   "file": "app/console/decisions/page.tsx",
   "line": 1,
   "sev": "P2",
   "surface": "desktop-web",
   "kind": "improvement"
  },
  "desktop-web-ux:dweb-06": {
   "title": "Lessons page uses an undefined width token \u2014 page width/padding jump on that tab",
   "file": "app/console/lessons/page.tsx",
   "line": 5,
   "sev": "P2",
   "surface": "desktop-web",
   "kind": "defect"
  },
  "desktop-web-ux:dweb-07": {
   "title": "Home Coach card shows three dead 'quick action' chips and attaches notes to an unnamed decision",
   "file": "app/console/page.tsx",
   "line": 382,
   "sev": "P2",
   "surface": "desktop-web",
   "kind": "defect"
  },
  "desktop-web-ux:dweb-08": {
   "title": "Positions blotter lives only on Home and lacks last price, day change, sort and any exit action",
   "file": "app/console/components/positions.tsx",
   "line": 89,
   "sev": "P2",
   "surface": "desktop-web",
   "kind": "opportunity"
  },
  "desktop-web-ux:dweb-09": {
   "title": "Three different save models across the Configure pages (and mixed within Guardrails)",
   "file": "app/console/guardrails/page.tsx",
   "line": 335,
   "sev": "P2",
   "surface": "desktop-web",
   "kind": "improvement"
  },
  "desktop-web-ux:dweb-10": {
   "title": "Activity journal and Orders history cannot be searched, filtered or exported; hard caps",
   "file": "app/console/activity/page.tsx",
   "line": 216,
   "sev": "P2",
   "surface": "desktop-web",
   "kind": "improvement"
  },
  "desktop-web-ux:dweb-11": {
   "title": "Command palette lacks ticker lookup, STOP/Start, and account switch verbs",
   "file": "app/console/components/command-palette.tsx",
   "line": 160,
   "sev": "P2",
   "surface": "desktop-web",
   "kind": "improvement"
  },
  "desktop-web-ux:dweb-12": {
   "title": "Multi-account desk: no cross-account overview; switching reloads the whole console",
   "file": "app/console/components/chrome.tsx",
   "line": 127,
   "sev": "P2",
   "surface": "desktop-web",
   "kind": "opportunity"
  },
  "desktop-web-ux:dweb-13": {
   "title": "Theme 'System' choice never persists \u2014 reload snaps back to Light",
   "file": "app/console/lib/useConsoleTheme.ts",
   "line": 38,
   "sev": "P3",
   "surface": "desktop-web",
   "kind": "defect"
  },
  "desktop-web-ux:dweb-14": {
   "title": "Title Case rule not applied consistently to card titles, buttons and admin nav",
   "file": "app/console/approvals/page.tsx",
   "line": 366,
   "sev": "P3",
   "surface": "desktop-web",
   "kind": "defect"
  },
  "desktop-web-ux:dweb-15": {
   "title": "Two-space sentence rule effectively unimplemented across the console",
   "file": "app/console/lib/format.ts",
   "line": 54,
   "sev": "P3",
   "surface": "desktop-web",
   "kind": "defect"
  },
  "desktop-web-ux:dweb-16": {
   "title": "'You're set' readiness card returns on every visit \u2014 Dismiss is not persisted",
   "file": "app/console/components/readiness-checklist.tsx",
   "line": 79,
   "sev": "P3",
   "surface": "desktop-web",
   "kind": "defect"
  },
  "desktop-web-ux:dweb-17": {
   "title": "Orders stale-limit banner points to Settings for a field that lives on Guardrails",
   "file": "app/console/orders/page.tsx",
   "line": 322,
   "sev": "P3",
   "surface": "desktop-web",
   "kind": "defect"
  },
  "desktop-web-ux:dweb-18": {
   "title": "Home proposal rows are click-only divs (no keyboard access); sheet Escape stacking in flight",
   "file": "app/console/page.tsx",
   "line": 1029,
   "sev": "P2",
   "surface": "desktop-web",
   "kind": "defect"
  },
  "desktop-web-ux:dweb-19": {
   "title": "Approvals: 'Live' tile/chip wording vs copy canon, plus a reality filter that can never be changed",
   "file": "app/console/approvals/page.tsx",
   "line": 57,
   "sev": "P3",
   "surface": "desktop-web",
   "kind": "improvement"
  },
  "desktop-web-ux:dweb-20": {
   "title": "Watchlist table is thin for a monitor screen (Symbol/Last/Alerts/Added only)",
   "file": "app/console/watchlist/page.tsx",
   "line": 231,
   "sev": "P3",
   "surface": "desktop-web",
   "kind": "opportunity"
  },
  "public-site-growth:dpub-01": {
   "title": "Session-expired redirect drops the destination: deep links (push/email \u2192 /console/approvals?proposal=\u2026) land on the dashboard after sign-in",
   "file": "middleware.ts",
   "line": 417,
   "sev": "P2",
   "surface": "cross-surface",
   "kind": "bug"
  },
  "public-site-growth:dpub-02": {
   "title": "Every Auth.js error (OAuth failure, Apple re-auth without email, config error) is routed to /access-denied, whose only copy is \u201cask the owner to add your email to the allowlist\u201d and which offers no way out",
   "file": "src/lib/auth/auth.ts",
   "line": 197,
   "sev": "P2",
   "surface": "desktop-web",
   "kind": "defect"
  },
  "public-site-growth:dpub-03": {
   "title": "Public explainer copy promises delegation controls that do not exist in the product (\u201cEvidence threshold: minimum source count, contradiction tolerance, freshness window\u201d; \u201cReview mode: notify before/after action, alert only when blocked\u201d)",
   "file": "app/how-it-works/page.tsx",
   "line": 94,
   "sev": "P2",
   "surface": "public-site",
   "kind": "defect"
  },
  "public-site-growth:dpub-04": {
   "title": "/framework states an invariant (\u201cSizing is deterministic and code-side; advisory context cannot reach it\u2026 dissent can shrink risk, never enlarge it\u201d) that the Socratic override sizing path contradicts \u2014 the model can raise notional above the deterministic size",
   "file": "app/framework/content.ts",
   "line": 226,
   "sev": "P2",
   "surface": "public-site",
   "kind": "defect"
  },
  "public-site-growth:dpub-05": {
   "title": "Terms and Conditions were not updated when market-data pooling became a condition of use (#2815): Privacy \u00a78 says \u201cUsing the app requires contributing\u2026\u201d, Terms are silent, effective date still July 10",
   "file": "app/terms-and-conditions/page.tsx",
   "line": 110,
   "sev": "P2",
   "surface": "public-site",
   "kind": "defect"
  },
  "public-site-growth:dpub-06": {
   "title": "Two-space sentence rule is not implemented on any public page: zero NBSP+space gaps, ~40 single-spaced sentence breaks and ~27 raw double ASCII spaces (collapsed by the browser)",
   "file": "app/welcome/page.tsx",
   "line": 230,
   "sev": "P2",
   "surface": "public-site",
   "kind": "defect"
  },
  "public-site-growth:dpub-07": {
   "title": "Anonymous visitors never see the marketing page and the login page never explains the product; /welcome has no \u201cSign in\u201d and /login has no \u201cHow it works / Request access\u201d \u2014 the two halves of the funnel do not connect (prior 2026-08-06 D7 still open)",
   "file": "app/page.tsx",
   "line": 14,
   "sev": "P3",
   "surface": "public-site",
   "kind": "opportunity"
  },
  "public-site-growth:dpub-08": {
   "title": "Sitemap, robots allow-list, and middleware PUBLIC_PREFIXES still advertise the deleted /design/socratic-trade route (404 on prod, listed in the live sitemap.xml)",
   "file": "app/sitemap.ts",
   "line": 15,
   "sev": "P3",
   "surface": "public-site",
   "kind": "defect"
  },
  "public-site-growth:dpub-09": {
   "title": "Production robots.txt is being rewritten by Cloudflare's managed \u201cContent Signals\u201d robots, which prepends `User-agent: * \u2026 Allow: /` ahead of the app's `Disallow: /` \u2014 the app's private-by-default crawl policy is not what is served",
   "file": "app/robots.ts",
   "line": 47,
   "sev": "P3",
   "surface": "public-site",
   "kind": "risk"
  },
  "public-site-growth:dpub-10": {
   "title": "Two overlapping public explainers with divergent narratives: /how-it-works (\u201cDecision framework\u201d, aspirational) is the only one linked; /framework (\u201cTrading framework\u201d, accurate Green/Red/deterministic-sizing story) is orphaned \u2014 no page links to it",
   "file": "app/welcome/page.tsx",
   "line": 137,
   "sev": "P3",
   "surface": "public-site",
   "kind": "improvement"
  },
  "public-site-growth:dpub-11": {
   "title": "Headings, buttons and nav on all public pages use sentence case, contrary to the Title Case rule (\u201cRequest access\u201d, \u201cDecision framework\u201d, \u201cWhat it does\u201d, \u201cCore loop\u201d, \u201c1. Who we are\u201d, page titles \u201cSign in\u201d, \u201cTrading framework\u201d)",
   "file": "app/welcome/page.tsx",
   "line": 117,
   "sev": "P3",
   "surface": "public-site",
   "kind": "defect"
  },
  "public-site-growth:dpub-12": {
   "title": "Landing illustration shows a specific real security as a BUY (\u201cBUY NVDA ~$4,200 \u00b7 82/100 confidence\u201d) beneath a disclosure that says nothing here is a recommendation to buy any security",
   "file": "app/welcome/decision-trace-illustration.tsx",
   "line": 16,
   "sev": "P3",
   "surface": "public-site",
   "kind": "risk"
  },
  "public-site-growth:dpub-13": {
   "title": "Social previews have no image: layout and welcome declare `twitter.card: summary_large_image` and OpenGraph blocks but no `openGraph.images` / opengraph-image route exists",
   "file": "app/layout.tsx",
   "line": 52,
   "sev": "P3",
   "surface": "public-site",
   "kind": "opportunity"
  },
  "public-site-growth:dpub-14": {
   "title": "Root-layout `alternates.canonical: \"/\"` is inherited by every page that does not override it \u2014 /login, /framework, /access-denied and all console pages emit `<link rel=canonical href=\"https://socratictrade.com\">`; /login and /access-denied also inherit index:true once the indexing flag flips",
   "file": "app/layout.tsx",
   "line": 41,
   "sev": "P3",
   "surface": "public-site",
   "kind": "defect"
  },
  "public-site-growth:dpub-15": {
   "title": "Global toast host uses `theme=\"system\"`, so on a dark-mode Mac/iPhone the light-default app shows dark toasts on public and console pages",
   "file": "app/layout.tsx",
   "line": 80,
   "sev": "P3",
   "surface": "cross-surface",
   "kind": "defect"
  },
  "public-site-growth:dpub-16": {
   "title": "Web login value bullets drifted from iOS despite the \u201ckeep in sync\u201d comment, and the third web bullet is device-centric copy on a desktop page",
   "file": "app/login/page.tsx",
   "line": 25,
   "sev": "P3",
   "surface": "desktop-web",
   "kind": "defect"
  },
  "public-site-growth:dpub-17": {
   "title": "/login renders the sign-in form even for an already-authenticated user (public path bypasses identity), instead of bouncing to /console",
   "file": "middleware.ts",
   "line": 306,
   "sev": "P3",
   "surface": "desktop-web",
   "kind": "improvement"
  },
  "public-site-growth:dpub-18": {
   "title": "docs/go-to-market.md contradicts current product philosophy and shipped defaults (paper/local-sim positioning, \u201cTest \u2014 Local Sim\u201d, landing \u201cdefault off \u2192 404\u201d, /strategy as a page)",
   "file": "docs/go-to-market.md",
   "line": 13,
   "sev": "P3",
   "surface": "public-site",
   "kind": "defect"
  },
  "public-site-growth:dpub-19": {
   "title": "Footer legal tagline is inconsistent across public pages (\u201cNot investment advice.  You set authority.\u201d vs \u201cNot investment advice. Trading involves risk of loss.\u201d) and privacy \u00a75 omits identity providers and error monitoring",
   "file": "app/framework/page.tsx",
   "line": 62,
   "sev": "P3",
   "surface": "public-site",
   "kind": "improvement"
  },
  "public-site-growth:dpub-20": {
   "title": "Hero headline leans on internal jargon (\u201cautonomy desk for market decisions\u201d) \u2014 clear to the owner, opaque to a first-time reader; the plain-English value is buried in the sub-head",
   "file": "app/welcome/page.tsx",
   "line": 126,
   "sev": "P3",
   "surface": "public-site",
   "kind": "improvement"
  },
  "mobile-web-ux:mweb-01": {
   "title": "Phone chrome bar over-budget since run-state label became \"Stop Agent\"/\"Resume Agent\" \u2014 account scope squeezed to ~0-15px at 360-390px",
   "file": "app/console/components/chrome.tsx",
   "line": 326,
   "sev": "P1",
   "surface": "mobile-web",
   "kind": "defect"
  },
  "mobile-web-ux:mweb-02": {
   "title": "iOS Safari status/toolbar tint follows OS scheme, not the console's actual (default LIGHT) theme",
   "file": "app/console/layout.tsx",
   "line": 15,
   "sev": "P2",
   "surface": "mobile-web",
   "kind": "defect"
  },
  "mobile-web-ux:mweb-03": {
   "title": "Choosing \"System\" theme never survives a reload \u2014 it is stored by deleting the key, and a missing key reads back as Light",
   "file": "app/console/lib/useConsoleTheme.ts",
   "line": 37,
   "sev": "P2",
   "surface": "cross-surface",
   "kind": "bug"
  },
  "mobile-web-ux:mweb-04": {
   "title": "\u2318K \"Toggle theme\" writes localStorage but does not change the visible theme (separate hook instance from the shell)",
   "file": "app/console/components/command-palette.tsx",
   "line": 68,
   "sev": "P2",
   "surface": "cross-surface",
   "kind": "bug"
  },
  "mobile-web-ux:mweb-05": {
   "title": "iOS Safari auto-zooms on focus: \u2318K search input is 15px and Coach model/reasoning pickers + custom-model field force 11px inline",
   "file": "app/console/console.css",
   "line": 1251,
   "sev": "P2",
   "surface": "mobile-web",
   "kind": "defect"
  },
  "mobile-web-ux:mweb-06": {
   "title": "Three different hard-coded bottom offsets vs the real tab-bar height \u2014 save bar and toasts collide with the tab bar when the home-indicator inset appears",
   "file": "app/console/components/policy-form.tsx",
   "line": 358,
   "sev": "P2",
   "surface": "mobile-web",
   "kind": "defect"
  },
  "mobile-web-ux:mweb-07": {
   "title": "Bottom sheets / More sheet / palette do not lock or contain scroll \u2014 page behind scrolls on iOS while a modal is open",
   "file": "app/console/console.css",
   "line": 858,
   "sev": "P2",
   "surface": "mobile-web",
   "kind": "defect"
  },
  "mobile-web-ux:mweb-08": {
   "title": "Full-screen overlays (symbol drawer, sheets, More sheet) ignore browser history \u2014 Android back / iOS edge-swipe leaves the console instead of closing the overlay",
   "file": "app/console/ui/symbol-drawer.tsx",
   "line": 63,
   "sev": "P2",
   "surface": "mobile-web",
   "kind": "risk"
  },
  "mobile-web-ux:mweb-09": {
   "title": "Bottom-anchored sheets with typed-confirm inputs vs the on-screen keyboard \u2014 no interactiveWidget or visualViewport handling",
   "file": "app/console/layout.tsx",
   "line": 9,
   "sev": "P2",
   "surface": "mobile-web",
   "kind": "defect"
  },
  "mobile-web-ux:mweb-10": {
   "title": "Toggle switch is a 36x20 target with no coarse-pointer bump and no clickable label",
   "file": "app/console/console.css",
   "line": 671,
   "sev": "P2",
   "surface": "mobile-web",
   "kind": "defect"
  },
  "mobile-web-ux:mweb-11": {
   "title": "No phone type-scale bump: 412 uses of 11px and 185 of 12.5px text render at desktop sizes on a 6-inch screen",
   "file": "app/console/console.css",
   "line": 90,
   "sev": "P2",
   "surface": "mobile-web",
   "kind": "improvement"
  },
  "mobile-web-ux:mweb-12": {
   "title": "Phone chrome-bar controls are 32px (avatar, \u2318K trigger, run-state chip) \u2014 known follow-up still open",
   "file": "app/console/components/chrome.tsx",
   "line": 900,
   "sev": "P2",
   "surface": "mobile-web",
   "kind": "improvement"
  },
  "mobile-web-ux:mweb-13": {
   "title": "Overlay close/pin/row targets under 44px: sheet X is 18px, drawer X 32px, More-sheet pins 36px and rows ~35px, scan star 28px, expand toggle text-only",
   "file": "app/console/ui/sheet.tsx",
   "line": 157,
   "sev": "P2",
   "surface": "mobile-web",
   "kind": "improvement"
  },
  "mobile-web-ux:mweb-14": {
   "title": "Watchlist has no phone card variant \u2014 a 5-column table with row actions in the last column scrolls its Alert/Delete buttons off-screen",
   "file": "app/console/watchlist/page.tsx",
   "line": 227,
   "sev": "P2",
   "surface": "mobile-web",
   "kind": "improvement"
  },
  "mobile-web-ux:mweb-15": {
   "title": "Home on a phone buries portfolio value / day P&L under the readiness hero, strategy bar and \"Latest strategy run\" list",
   "file": "app/console/page.tsx",
   "line": 217,
   "sev": "P2",
   "surface": "mobile-web",
   "kind": "improvement"
  },
  "mobile-web-ux:mweb-16": {
   "title": "Sticky top chrome eats ~110px on phones and the healthy freshness line hides the as-of clock in a hover-only title",
   "file": "app/console/components/chrome.tsx",
   "line": 1022,
   "sev": "P3",
   "surface": "mobile-web",
   "kind": "improvement"
  },
  "mobile-web-ux:mweb-17": {
   "title": "Scan on phones is an unsorted, unfilterable, unvirtualized card wall \u2014 sort/columns/search live only in the lg table header",
   "file": "app/console/scan/scan-table.tsx",
   "line": 505,
   "sev": "P3",
   "surface": "mobile-web",
   "kind": "improvement"
  },
  "mobile-web-ux:mweb-18": {
   "title": "Consent gate card has no max-height/scroll \u2014 Accept & Continue can be unreachable on short/landscape phones or with large text",
   "file": "app/console/components/consent-gate.tsx",
   "line": 87,
   "sev": "P3",
   "surface": "mobile-web",
   "kind": "defect"
  },
  "mobile-web-ux:mweb-19": {
   "title": "Tap-opened tooltips are viewport-unaware (w-max max-w-xs, centered) \u2014 right-edge chips overflow the screen and cause horizontal scroll leakage",
   "file": "app/console/ui/primitives.tsx",
   "line": 504,
   "sev": "P3",
   "surface": "mobile-web",
   "kind": "improvement"
  },
  "mobile-web-ux:mweb-20": {
   "title": "Two full-viewport animated gradient orbs keep animating under the opaque console \u2014 GPU/battery cost on phones for invisible pixels",
   "file": "app/globals.css",
   "line": 155,
   "sev": "P3",
   "surface": "mobile-web",
   "kind": "improvement"
  },
  "mobile-web-ux:mweb-21": {
   "title": "Command palette sizes with vh (not dvh/visualViewport) \u2014 on phones the auto-focused search opens the keyboard over most of the result list",
   "file": "app/console/console.css",
   "line": 1220,
   "sev": "P3",
   "surface": "mobile-web",
   "kind": "improvement"
  },
  "mobile-web-ux:mweb-22": {
   "title": "Equity chart is a fixed 640x140 viewBox that scales to ~70px tall on a phone, with no touch inspection",
   "file": "app/console/components/equity-chart.tsx",
   "line": 11,
   "sev": "P3",
   "surface": "mobile-web",
   "kind": "improvement"
  },
  "mobile-web-ux:mweb-23": {
   "title": "Coach composer: Enter always sends \u2014 on a phone keyboard there is no Shift, so a newline is impossible; no enterKeyHint",
   "file": "app/console/assistant/chat.tsx",
   "line": 612,
   "sev": "P3",
   "surface": "mobile-web",
   "kind": "improvement"
  },
  "mobile-web-ux:mweb-24": {
   "title": "/welcome offers no Sign in link \u2014 a returning owner on a phone has only a mailto \"Request access\"",
   "file": "app/welcome/page.tsx",
   "line": 114,
   "sev": "P3",
   "surface": "public-site",
   "kind": "improvement"
  },
  "mobile-web-ux:mweb-25": {
   "title": "LIVE tag inside the Approve button is 9.5px \u2014 money-critical label below the sanctioned micro tier; prior P2 still open",
   "file": "app/console/console.css",
   "line": 578,
   "sev": "P3",
   "surface": "mobile-web",
   "kind": "improvement"
  },
  "web-accessibility:a11y-01": {
   "title": "Home 'Recent decisions' rows are click-only <div>s \u2014 details + Approve flow unreachable by keyboard",
   "file": "app/console/page.tsx",
   "line": 1029,
   "sev": "P1",
   "surface": "desktop-web",
   "kind": "bug"
  },
  "web-accessibility:a11y-02": {
   "title": "Light theme: tone-colored text on soft fills and the Stop/Start button fail WCAG AA (3.0\u20134.3:1); --con-info fails on every surface",
   "file": "app/console/console.css",
   "line": 559,
   "sev": "P1",
   "surface": "cross-surface",
   "kind": "defect"
  },
  "web-accessibility:a11y-03": {
   "title": "Sheet focus sentry and Escape are not stack-aware \u2014 Escape on a drawer/palette above a Sheet also closes the Sheet",
   "file": "app/console/ui/sheet.tsx",
   "line": 116,
   "sev": "P2",
   "surface": "cross-surface",
   "kind": "defect"
  },
  "web-accessibility:a11y-04": {
   "title": "Every console route has the same document title 'Socratic Trade' (WCAG 2.4.2)",
   "file": "app/console/layout.tsx",
   "line": 5,
   "sev": "P2",
   "surface": "cross-surface",
   "kind": "defect"
  },
  "web-accessibility:a11y-05": {
   "title": "No skip link \u2014 ~22 focus stops (chrome bar + 15-item rail) precede <main> on every console page",
   "file": "app/console/components/shell.tsx",
   "line": 180,
   "sev": "P2",
   "surface": "desktop-web",
   "kind": "improvement"
  },
  "web-accessibility:a11y-06": {
   "title": "Error toasts are polite + auto-dismiss in 9s with no hover/focus pause; typed-confirm server refusals are never announced",
   "file": "app/console/ui/toast.tsx",
   "line": 38,
   "sev": "P2",
   "surface": "cross-surface",
   "kind": "defect"
  },
  "web-accessibility:a11y-07": {
   "title": "~147 explanations live only in native title= on non-interactive elements; Tooltip triggers on Chip/Stat/Ago are not focusable nor aria-described",
   "file": "app/console/ui/primitives.tsx",
   "line": 485,
   "sev": "P2",
   "surface": "mobile-web",
   "kind": "defect"
  },
  "web-accessibility:a11y-08": {
   "title": "Approval cards have no heading or accessible name; 'Reject' is a bare repeated name across N cards",
   "file": "app/console/components/approval-card.tsx",
   "line": 455,
   "sev": "P2",
   "surface": "cross-surface",
   "kind": "defect"
  },
  "web-accessibility:a11y-09": {
   "title": "Account-scope menu uses role=menu without arrow keys/focus move and puts an invisible full-screen focusable button in the tab order; UserMenu/theme buttons lack state",
   "file": "app/console/components/chrome.tsx",
   "line": 235,
   "sev": "P2",
   "surface": "cross-surface",
   "kind": "defect"
  },
  "web-accessibility:a11y-10": {
   "title": "Scan 'Columns' popover: no aria-expanded/controls, no Escape, focus not moved in",
   "file": "app/console/scan/scan-table.tsx",
   "line": 356,
   "sev": "P2",
   "surface": "desktop-web",
   "kind": "defect"
  },
  "web-accessibility:a11y-11": {
   "title": "None of the 23 console tables has a caption/aria-label; 105/106 <th> lack scope",
   "file": "app/console/components/positions.tsx",
   "line": 89,
   "sev": "P3",
   "surface": "desktop-web",
   "kind": "improvement"
  },
  "web-accessibility:a11y-12": {
   "title": "Meter progressbar has no accessible name at any of its call sites",
   "file": "app/console/ui/primitives.tsx",
   "line": 201,
   "sev": "P3",
   "surface": "cross-surface",
   "kind": "defect"
  },
  "web-accessibility:a11y-13": {
   "title": "Coarse-pointer 44px floor covers only .con-btn; sheet/drawer close X (18px), toast \u00d7, scope/state/avatar triggers and Watch stars are excluded",
   "file": "app/console/console.css",
   "line": 458,
   "sev": "P3",
   "surface": "mobile-web",
   "kind": "improvement"
  },
  "web-accessibility:a11y-14": {
   "title": "Readiness checklist conveys complete/next state by icon and border color only",
   "file": "app/console/components/readiness-checklist.tsx",
   "line": 27,
   "sev": "P3",
   "surface": "cross-surface",
   "kind": "defect"
  },
  "web-accessibility:a11y-15": {
   "title": "Text-input boundaries are 1.5:1 against their background (WCAG 1.4.11 non-text contrast)",
   "file": "app/console/console.css",
   "line": 612,
   "sev": "P3",
   "surface": "cross-surface",
   "kind": "defect"
  },
  "web-accessibility:a11y-16": {
   "title": "Mobile 'More' tab has no aria-expanded/aria-controls; approvals badge count is an unlabeled number inside the link name",
   "file": "app/console/components/nav.tsx",
   "line": 442,
   "sev": "P3",
   "surface": "mobile-web",
   "kind": "defect"
  },
  "web-accessibility:a11y-17": {
   "title": "Interactive text below the app's own 10px micro-tier floor: BROKER tag inside Approve at 9.5px, tab-bar labels at 10px",
   "file": "app/console/console.css",
   "line": 578,
   "sev": "P3",
   "surface": "mobile-web",
   "kind": "defect"
  },
  "web-accessibility:a11y-18": {
   "title": "Heading structure gaps: 68 heading-styled <div class=\"con-card-title\"> vs 16 real h2/h3; collapsible Card demotes its h2 to <span> and nests a Link inside <summary>",
   "file": "app/console/ui/primitives.tsx",
   "line": 35,
   "sev": "P3",
   "surface": "cross-surface",
   "kind": "improvement"
  },
  "web-accessibility:a11y-19": {
   "title": "Login page has no heading and its Apple-only warning is 3.2:1 amber",
   "file": "app/login/page.tsx",
   "line": 99,
   "sev": "P3",
   "surface": "public-site",
   "kind": "defect"
  },
  "web-accessibility:a11y-20": {
   "title": "Live updates (new proposals via SSE, freshness going 'delayed') are silent to assistive tech",
   "file": "app/console/components/chrome.tsx",
   "line": 1043,
   "sev": "P3",
   "surface": "cross-surface",
   "kind": "opportunity"
  },
  "api-contract-web-ios:api-01": {
   "title": "iOS Guardrails stop-loss rows always show \"\u2014\": FullPolicy reads stopLossPct at top level, server nests it under riskRules",
   "file": "ios/SocraticTrade/DeskModels.swift",
   "line": 323,
   "sev": "P1",
   "surface": "cross-surface",
   "kind": "bug"
  },
  "api-contract-web-ios:api-02": {
   "title": "proposal.approve command reports \"succeeded\" (green check on iOS) when the broker order was blocked / not placed / errored",
   "file": "src/lib/mobile-api.ts",
   "line": 965,
   "sev": "P1",
   "surface": "cross-surface",
   "kind": "bug"
  },
  "api-contract-web-ios:api-03": {
   "title": "iOS Scan times out at 25s while the server scan budget is now 35s (#2830 today) \u2014 slow scans surface as \"Check your connection\"",
   "file": "ios/SocraticTrade/MobileAPIClient.swift",
   "line": 137,
   "sev": "P1",
   "surface": "cross-surface",
   "kind": "defect"
  },
  "api-contract-web-ios:api-04": {
   "title": "strategy.run_once stores the full StrategyResult (marketScan.quotesBySymbol, topCandidates, llmSteps) in mobile_commands.result and ships it in every /api/mobile/snapshot recentCommands[]",
   "file": "src/lib/mobile-api.ts",
   "line": 955,
   "sev": "P2",
   "surface": "cross-surface",
   "kind": "defect"
  },
  "api-contract-web-ios:api-05": {
   "title": "proposal.approve / proposal.reject are queued FIFO behind a running strategy.run_once (up to 30 min) with no ETA, while the web approve path is synchronous",
   "file": "src/lib/mobile-api.ts",
   "line": 88,
   "sev": "P2",
   "surface": "cross-surface",
   "kind": "improvement"
  },
  "api-contract-web-ios:api-06": {
   "title": "No generated contract fixtures between server routes and Swift decoders \u2014 hand-mirrored enums/constants/JSON drift silently",
   "file": "ios/SocraticTradeTests/MobileModelsTests.swift",
   "line": 6,
   "sev": "P2",
   "surface": "cross-surface",
   "kind": "risk"
  },
  "api-contract-web-ios:api-07": {
   "title": "Snapshot decode is all-or-nothing: one malformed element in any list blanks the whole iOS app",
   "file": "ios/SocraticTrade/MobileModels.swift",
   "line": 58,
   "sev": "P2",
   "surface": "ios",
   "kind": "risk"
  },
  "api-contract-web-ios:api-08": {
   "title": "iOS Coach request timeout (90s) is shorter than the server chat deadline (120s default); retry then hits 409 with no human message",
   "file": "ios/SocraticTrade/MobileAPIClient.swift",
   "line": 117,
   "sev": "P3",
   "surface": "cross-surface",
   "kind": "defect"
  },
  "api-contract-web-ios:api-09": {
   "title": "Every SSE payload frame (ready, dashboard.chat-turn stages, shared market-data/dirty broadcasts) triggers a full snapshot reload on iOS",
   "file": "ios/SocraticTrade/MobileAPIClient.swift",
   "line": 310,
   "sev": "P3",
   "surface": "cross-surface",
   "kind": "improvement"
  },
  "api-contract-web-ios:api-10": {
   "title": "Mobile command backlog count and worker queue are global across users (not scoped to the requester)",
   "file": "src/lib/mobile-api.ts",
   "line": 1158,
   "sev": "P3",
   "surface": "api",
   "kind": "risk"
  },
  "api-contract-web-ios:api-11": {
   "title": "zod is a dependency but unused by all 122 routes; four different error envelopes plus plain-text 401/403 from middleware",
   "file": "middleware.ts",
   "line": 416,
   "sev": "P2",
   "surface": "api",
   "kind": "improvement"
  },
  "api-contract-web-ios:api-12": {
   "title": "iOS maps every 401/403 to \"Your session expired. Sign in again.\" and wipes cookies, discarding the server's real reason",
   "file": "ios/SocraticTrade/MobileAPIClient.swift",
   "line": 427,
   "sev": "P3",
   "surface": "cross-surface",
   "kind": "defect"
  },
  "api-contract-web-ios:api-13": {
   "title": "No client-version negotiation: catalog.version exists but GETs carry no build header and the snapshot has no minimum-build/upgrade signal",
   "file": "src/lib/mobile-api.ts",
   "line": 1290,
   "sev": "P3",
   "surface": "cross-surface",
   "kind": "improvement"
  },
  "api-contract-web-ios:api-14": {
   "title": "docs/mobile-api-and-clients.md is stale and omits most of the routes iOS actually depends on; /api/mobile/bootstrap has no caller",
   "file": "docs/mobile-api-and-clients.md",
   "line": 34,
   "sev": "P3",
   "surface": "api",
   "kind": "defect"
  },
  "api-contract-web-ios:api-15": {
   "title": "/api/scan returns the entire MarketScan (quotesBySymbol for the whole universe, full MarketQuote per candidate) to a phone that decodes 9 fields",
   "file": "app/api/scan/route.ts",
   "line": 118,
   "sev": "P3",
   "surface": "cross-surface",
   "kind": "opportunity"
  },
  "api-contract-web-ios:api-16": {
   "title": "iOS Symbol sheet cannot age-tag other-account positions: server sends recordedAt, Swift PeerAccount drops it",
   "file": "ios/SocraticTrade/MobileModels.swift",
   "line": 557,
   "sev": "P3",
   "surface": "cross-surface",
   "kind": "improvement"
  },
  "api-contract-web-ios:api-17": {
   "title": "POST /api/mobile/commands maps any non-validation throw (DB, JSON) to HTTP 400 with the raw message; worker kick swallows failures",
   "file": "app/api/mobile/commands/route.ts",
   "line": 65,
   "sev": "P3",
   "surface": "api",
   "kind": "defect"
  },
  "api-contract-web-ios:api-18": {
   "title": "Login/handoff URLs hardcode https://socratictrade.com in LoginView despite MobileAPIClient.productionBaseURL existing to prevent that drift",
   "file": "ios/SocraticTrade/LoginView.swift",
   "line": 265,
   "sev": "P3",
   "surface": "ios",
   "kind": "defect"
  },
  "api-contract-web-ios:api-19": {
   "title": "With no provider key, iOS Coach silently selects the offline \"Mock\" model and chats with MockLLM instead of pointing at Connections",
   "file": "ios/SocraticTrade/DeskModels.swift",
   "line": 198,
   "sev": "P3",
   "surface": "cross-surface",
   "kind": "defect"
  },
  "frontend-performance:dweb-01": {
   "title": "/console/connections client bundle ships the server DB layer (SQL schema, migrations, key-store logic): 1.05 MB gz vs ~476 KB for every other console page",
   "file": "app/console/settings/brokers.tsx",
   "line": 12,
   "sev": "P1",
   "surface": "desktop-web",
   "kind": "defect"
  },
  "frontend-performance:dweb-02": {
   "title": "getEquityOrders pulls the ENTIRE Alpaca order history (up to 50 pages x 500) on every dashboard snapshot and 20 other call sites, then ships all of it to every client each poll",
   "file": "src/lib/alpaca.ts",
   "line": 491,
   "sev": "P1",
   "surface": "cross-surface",
   "kind": "defect"
  },
  "frontend-performance:dweb-03": {
   "title": "Dashboard snapshot is a 'load the universe' payload polled every 15 s: unused raw `audit[]` (incl. unbounded market_scan payloads), full 500-symbol `latestScan.quotesBySymbol`, full order history, duplicated profiles",
   "file": "src/lib/dashboard.ts",
   "line": 1108,
   "sev": "P2",
   "surface": "cross-surface",
   "kind": "improvement"
  },
  "frontend-performance:dweb-04": {
   "title": "`market_scan` audit still stores the full scan on every /console/scan mount (even cache-served scans) \u2014 the 2026-08-02 audit hygiene only bounded strategy_run",
   "file": "app/api/scan/route.ts",
   "line": 116,
   "sev": "P2",
   "surface": "backend",
   "kind": "defect"
  },
  "frontend-performance:dweb-05": {
   "title": "After acknowledge/cancel/run-once, `await refresh()` is served from the 10 s snapshot cache, so the acked alert / cancelled order stays on screen",
   "file": "app/console/components/alert-center.tsx",
   "line": 298,
   "sev": "P2",
   "surface": "cross-surface",
   "kind": "bug"
  },
  "frontend-performance:dweb-06": {
   "title": "Single console context re-renders every page 3x per poll and once per SSE event because `fetching` and `stream` health live in the same context value as `snapshot`",
   "file": "app/console/lib/useConsoleData.tsx",
   "line": 341,
   "sev": "P2",
   "surface": "desktop-web",
   "kind": "improvement"
  },
  "frontend-performance:dweb-07": {
   "title": "TickerLogo starts as theme=dark and flips to light after mount, so every logo in the light-default console requests the dark variant first (double fetch + wrong-variant flash), plus a MutationObserver/matchMedia listener per instance",
   "file": "app/console/ui/ticker-logo.tsx",
   "line": 21,
   "sev": "P2",
   "surface": "cross-surface",
   "kind": "bug"
  },
  "frontend-performance:dweb-08": {
   "title": "/console/watchlist and /console/lessons fetch their own data but still wait behind the full-screen snapshot loader (up to ~24 s worst case)",
   "file": "app/console/components/shell.tsx",
   "line": 67,
   "sev": "P2",
   "surface": "cross-surface",
   "kind": "defect"
  },
  "frontend-performance:dweb-09": {
   "title": "Sentry Session Replay bundle (~38 KB gz) ships on every page although replay is env-disabled; Sentry core is ~160 KB gz of every console load",
   "file": "instrumentation-client.ts",
   "line": 31,
   "sev": "P2",
   "surface": "cross-surface",
   "kind": "improvement"
  },
  "frontend-performance:dweb-10": {
   "title": "`motion` (framer) shared chunk (~40 KB gz / 124 KB raw) is loaded on every console page for one tooltip fade",
   "file": "app/console/ui/primitives.tsx",
   "line": 7,
   "sev": "P2",
   "surface": "cross-surface",
   "kind": "improvement"
  },
  "frontend-performance:dweb-11": {
   "title": "HTML -> JS -> /api/dashboard is a strict waterfall: the (up to ~24 s) snapshot request cannot start until ~1.5 MB raw JS is parsed and hydrated",
   "file": "app/console/layout.tsx",
   "line": 22,
   "sev": "P2",
   "surface": "cross-surface",
   "kind": "opportunity"
  },
  "frontend-performance:dweb-12": {
   "title": "Three heavy client libraries are declared but never imported (@xyflow/react, lightweight-charts, react-resizable-panels)",
   "file": "package.json",
   "line": 45,
   "sev": "P3",
   "surface": "desktop-web",
   "kind": "improvement"
  },
  "frontend-performance:dweb-13": {
   "title": "1 MB 1024x1024 PNG is declared as the primary favicon (no `sizes`) on every page and in the manifest",
   "file": "app/layout.tsx",
   "line": 25,
   "sev": "P3",
   "surface": "cross-surface",
   "kind": "defect"
  },
  "frontend-performance:dweb-14": {
   "title": "Intro canvas forces layout every animation frame (getBoundingClientRect + querySelectorAll per frame, up to END + 45 s) exactly while the first snapshot is parsing; header logo runs a 60 fps rAF loop for a 1 Hz redraw",
   "file": "app/console/components/intro-canvas.tsx",
   "line": 427,
   "sev": "P3",
   "surface": "mobile-web",
   "kind": "improvement"
  },
  "frontend-performance:dweb-15": {
   "title": "Symbol drawer endpoints (/api/history, /api/quote, /api/symbol-desk) send no Cache-Control, so every drawer open re-fetches EOD history that the server already caches",
   "file": "app/console/ui/symbol-drilldown.tsx",
   "line": 69,
   "sev": "P3",
   "surface": "cross-surface",
   "kind": "improvement"
  },
  "frontend-performance:dweb-16": {
   "title": "Desktop and mobile trees are both mounted (hidden by CSS) in scan-table, positions and orders, doubling row mounts; the phone card list is not virtualized",
   "file": "app/console/scan/scan-table.tsx",
   "line": 505,
   "sev": "P3",
   "surface": "mobile-web",
   "kind": "improvement"
  },
  "frontend-performance:dweb-17": {
   "title": "Root `<Toaster theme=\"system\">` renders sonner toasts in OS-dark while the console defaults to light",
   "file": "app/layout.tsx",
   "line": 80,
   "sev": "P3",
   "surface": "cross-surface",
   "kind": "defect"
  },
  "trading-money-path:tsx-01": {
   "title": "Alpaca MCP placement falls back to REST after any MCP error and re-submits the same client_order_id; the duplicate 422 is then filed as rejected_by_broker while the first order lives at the broker",
   "file": "src/lib/alpaca.ts",
   "line": 346,
   "sev": "P1",
   "surface": "backend",
   "kind": "bug"
  },
  "trading-money-path:tsx-02": {
   "title": "Auto stale-exit remediation cancel-replaces ANY working sell/cover limit or stop_limit older than 15 min with a MARKET order \u2014 including activated bracket take-profit legs, bracket stop_limit legs and owner-placed GTC sells",
   "file": "src/lib/order-replacement.ts",
   "line": 755,
   "sev": "P1",
   "surface": "backend",
   "kind": "bug"
  },
  "trading-money-path:tsx-03": {
   "title": "iOS shows 'Approved \u2014 waiting for desk refresh' (green check) for approvals that returned busy/blocked/rejected_by_broker/not_placed/error",
   "file": "src/lib/mobile-api.ts",
   "line": 966,
   "sev": "P1",
   "surface": "ios",
   "kind": "bug"
  },
  "trading-money-path:tsx-04": {
   "title": "Any HTTP 4xx from the broker \u2014 including 429 rate-limit and 408 \u2014 is filed as a definitive terminal rejected_by_broker without a refId check",
   "file": "src/lib/strategy-execution.ts",
   "line": 1279,
   "sev": "P2",
   "surface": "backend",
   "kind": "defect"
  },
  "trading-money-path:tsx-05": {
   "title": "Alpaca getEquityOrders walks the ENTIRE order history (up to 50\u00d7500 pages) on every call, and 3+ scheduler lanes call it every 60s per account",
   "file": "src/lib/alpaca.ts",
   "line": 500,
   "sev": "P2",
   "surface": "backend",
   "kind": "risk"
  },
  "trading-money-path:tsx-06": {
   "title": "Alpaca getEquityTradability is a stub (tradable:true, fractional:true for every symbol) \u2014 shortable/fractionable/easy_to_borrow are never checked pre-flight",
   "file": "src/lib/alpaca.ts",
   "line": 582,
   "sev": "P2",
   "surface": "backend",
   "kind": "defect"
  },
  "trading-money-path:tsx-07": {
   "title": "Alpaca extended-hours LIMIT entries keep time_in_force=gtc; Alpaca requires DAY for extended_hours=true, so a whole-share ext-hours entry the owner approved 422s",
   "file": "src/lib/alpaca.ts",
   "line": 157,
   "sev": "P3",
   "surface": "backend",
   "kind": "bug"
  },
  "trading-money-path:tsx-08": {
   "title": "Manually cancelling an app-managed protective stop from the console/iOS is silently undone next tick \u2014 the reconciler deletes the row and places a fresh stop; the cancel sheet promises 'No new order is placed'",
   "file": "src/lib/order-cancel.ts",
   "line": 198,
   "sev": "P2",
   "surface": "cross-surface",
   "kind": "defect"
  },
  "trading-money-path:tsx-09": {
   "title": "Approval-time ratio re-anchoring moves EXIT limits with the market \u2014 a held take-profit sell at $110 (ref $100) is placed at $99 if the quote fell to $90; entries chase upward inside the drift cap",
   "file": "src/lib/approval-reprice.ts",
   "line": 100,
   "sev": "P2",
   "surface": "backend",
   "kind": "risk"
  },
  "trading-money-path:tsx-10": {
   "title": "Approve is starved for the entire duration of an autonomous run (which can be 15-20 min per STATUS.md) because executeProposal contends for the whole-run strategy lock; console gives up after ~2 min, iOS shows 'Approved' (tsx-03)",
   "file": "src/lib/strategy-execution.ts",
   "line": 297,
   "sev": "P2",
   "surface": "cross-surface",
   "kind": "improvement"
  },
  "trading-money-path:tsx-11": {
   "title": "Hard-coded 5% opening-order headroom gate shrinks the owner's own per-order cap with no policy toggle \u2014 a human-approved $490 order against a $500 maxOrderNotional is blocked at approval",
   "file": "src/lib/policy.ts",
   "line": 567,
   "sev": "P2",
   "surface": "backend",
   "kind": "defect"
  },
  "trading-money-path:tsx-12": {
   "title": "'Not placed \u2014 safe to retry' has no retry path: not_placed is terminal and executeProposal refuses anything but status=proposed",
   "file": "src/lib/dashboard-ui.ts",
   "line": 326,
   "sev": "P2",
   "surface": "cross-surface",
   "kind": "improvement"
  },
  "trading-money-path:tsx-13": {
   "title": "Robinhood adapter silently converts a fractional/dollar BUY LIMIT (or ext-hours entry) into a regular-hours MARKET order with no audit; the persisted proposal and fill receipt still say 'limit'",
   "file": "src/lib/robinhood.ts",
   "line": 972,
   "sev": "P3",
   "surface": "backend",
   "kind": "defect"
  },
  "trading-money-path:tsx-14": {
   "title": "Stale-exit remediation on live spams 'stale_exit_auto_remediation_deferred' every 60s per resting sell limit and pushes a misleading 'limit order still working \u2014 cancel/reprice before replacing with market' alert for bracket take-profit legs",
   "file": "src/lib/order-replacement.ts",
   "line": 763,
   "sev": "P2",
   "surface": "cross-surface",
   "kind": "defect"
  },
  "trading-money-path:tsx-15": {
   "title": "Console decision-sheet Approve toasts positive 'Approved \u00b7 Order status: busy/blocked/error' for non-placed outcomes",
   "file": "app/console/page.tsx",
   "line": 997,
   "sev": "P3",
   "surface": "desktop-web",
   "kind": "defect"
  },
  "trading-money-path:tsx-16": {
   "title": "Policy hard-gate copy is broker-specific: 'No Robinhood account is selected.' fires for Alpaca/Tradier accounts",
   "file": "src/lib/policy.ts",
   "line": 326,
   "sev": "P3",
   "surface": "cross-surface",
   "kind": "defect"
  },
  "trading-money-path:tsx-17": {
   "title": "Daily notional/order-count budget resets at America/New_York midnight while every owner-facing timestamp is Central \u2014 the 'Today' budget chip rolls over at 11pm CT",
   "file": "src/lib/db-execution.ts",
   "line": 15,
   "sev": "P3",
   "surface": "cross-surface",
   "kind": "improvement"
  },
  "trading-money-path:tsx-18": {
   "title": "Tradier single-leg path sends the raw (unrounded) limit price and always attaches `stop` even on limit/market types, unlike its own bracket path",
   "file": "src/lib/tradier.ts",
   "line": 789,
   "sev": "P3",
   "surface": "backend",
   "kind": "defect"
  },
  "trading-money-path:tsx-19": {
   "title": "Alpaca MCP placement path never forwards extended_hours, so an approved ext-hours limit entry over alpaca-mcp is silently placed as a regular-hours order",
   "file": "src/lib/alpaca.ts",
   "line": 741,
   "sev": "P3",
   "surface": "backend",
   "kind": "defect"
  },
  "performance-accounting:perf-01": {
   "title": "Fill ledger keeps the OLDEST 500 fills per source; newest fills silently vanish from every P&L figure",
   "file": "src/lib/db-fills.ts",
   "line": 332,
   "sev": "P1",
   "surface": "cross-surface",
   "kind": "bug"
  },
  "performance-accounting:perf-02": {
   "title": "Home Day P&L is zeroed on ordinary trading days: flow inference runs with NO fills and reads market P&L as a deposit/withdrawal",
   "file": "app/console/lib/derive.ts",
   "line": 443,
   "sev": "P1",
   "surface": "desktop-web",
   "kind": "bug"
  },
  "performance-accounting:perf-03": {
   "title": "Short/cover cash sign is inverted in flow inference \u2192 phantom deposits/withdrawals on every short trade",
   "file": "src/lib/cash-flows.ts",
   "line": 164,
   "sev": "P1",
   "surface": "cross-surface",
   "kind": "bug"
  },
  "performance-accounting:perf-04": {
   "title": "'Versus the market' TWR silently covers only the last ~100 snapshots (\u22487 trading days) but is framed as account performance",
   "file": "src/lib/performance.ts",
   "line": 497,
   "sev": "P2",
   "surface": "cross-surface",
   "kind": "defect"
  },
  "performance-accounting:perf-05": {
   "title": "'Unrealized P&L' / 'Realized P&L' mean different things on Results (lot ledger), Home (broker cost) and iOS (broker positions) \u2014 none labels its basis",
   "file": "app/console/results/page.tsx",
   "line": 962,
   "sev": "P2",
   "surface": "cross-surface",
   "kind": "defect"
  },
  "performance-accounting:perf-06": {
   "title": "Win rate '0%' and Avg return '+0.00%' render for accounts with zero closed lots (web + iOS)",
   "file": "app/console/results/page.tsx",
   "line": 957,
   "sev": "P2",
   "surface": "cross-surface",
   "kind": "defect"
  },
  "performance-accounting:perf-07": {
   "title": "Tax 'Short-term realized' silently excludes every covered short (short-side lots skipped)",
   "file": "src/lib/tax.ts",
   "line": 352,
   "sev": "P2",
   "surface": "desktop-web",
   "kind": "bug"
  },
  "performance-accounting:perf-08": {
   "title": "Drawdown breaker HWM is not flow-adjusted and its 'day' is UTC \u2014 withdrawals/paper resets read as drawdown, daily-loss window resets at 7pm CT",
   "file": "src/lib/risk-breaker.ts",
   "line": 86,
   "sev": "P2",
   "surface": "backend",
   "kind": "defect"
  },
  "performance-accounting:perf-09": {
   "title": "Decision outcome grading uses only the FIRST partial exit of a lot (won/lost/pnlUsd wrong for scaled-out positions)",
   "file": "src/lib/outcome-engine.ts",
   "line": 966,
   "sev": "P2",
   "surface": "backend",
   "kind": "bug"
  },
  "performance-accounting:perf-10": {
   "title": "Synthetic 20 bps 'paper' execution-cost haircut is applied to REAL Alpaca-paper fill prices, but only when the price is known at booking \u2014 same account mixes shaded and raw fills",
   "file": "src/lib/performance.ts",
   "line": 244,
   "sev": "P2",
   "surface": "backend",
   "kind": "defect"
  },
  "performance-accounting:perf-11": {
   "title": "Win rate and 'n' count partial-close lots, not round trips \u2014 trims systematically inflate wins and sample counts",
   "file": "src/lib/performance.ts",
   "line": 1707,
   "sev": "P2",
   "surface": "cross-surface",
   "kind": "defect"
  },
  "performance-accounting:perf-12": {
   "title": "Closing fills with no matching app lot are silently dropped \u2014 exits of pre-app/manual positions never realize P&L, and nothing surfaces the count",
   "file": "src/lib/performance.ts",
   "line": 599,
   "sev": "P2",
   "surface": "cross-surface",
   "kind": "risk"
  },
  "performance-accounting:perf-13": {
   "title": "iOS drops benchmarkUnavailable / unverifiedFlows / subPeriods and always claims 'cash flows neutralized'",
   "file": "ios/SocraticTrade/MobileModels.swift",
   "line": 458,
   "sev": "P2",
   "surface": "ios",
   "kind": "defect"
  },
  "performance-accounting:perf-14": {
   "title": "Broker-authoritative equity history and cash-activity ledger are already implemented but unwired \u2014 inference could be replaced by facts",
   "file": "src/lib/alpaca-account-insights.ts",
   "line": 167,
   "sev": "P2",
   "surface": "backend",
   "kind": "opportunity"
  },
  "performance-accounting:perf-15": {
   "title": "Home mark-to-market % uses NET signed cost basis \u2014 shorts shrink the denominator and inflate the % (rows use |basis|)",
   "file": "app/console/lib/derive.ts",
   "line": 804,
   "sev": "P3",
   "surface": "desktop-web",
   "kind": "defect"
  },
  "performance-accounting:perf-16": {
   "title": "Capital-regime and flow dates are UTC calendar days; Day P&L uses Central \u2014 snapshots after 7pm CT land on the next day's row",
   "file": "src/lib/cash-flows.ts",
   "line": 40,
   "sev": "P3",
   "surface": "desktop-web",
   "kind": "defect"
  },
  "performance-accounting:perf-17": {
   "title": "SPY benchmark is total-return or price-return depending on which history provider answered; intraday tip vs EOD close misaligned",
   "file": "src/lib/history.ts",
   "line": 518,
   "sev": "P3",
   "surface": "backend",
   "kind": "defect"
  },
  "performance-accounting:perf-18": {
   "title": "Synthetic paper curve fabricates a $100 equity base rendered as money",
   "file": "src/lib/performance.ts",
   "line": 1664,
   "sev": "P3",
   "surface": "desktop-web",
   "kind": "defect"
  },
  "performance-accounting:perf-19": {
   "title": "OOS backtest annualizes compounded overlapping-horizon returns by calendar days (overstates annualized return; Sharpe scaling inconsistent)",
   "file": "src/lib/backtest.ts",
   "line": 1207,
   "sev": "P3",
   "surface": "api",
   "kind": "defect"
  },
  "performance-accounting:perf-20": {
   "title": "FillEvent carries no fee/commission or dividend fields \u2014 all P&L is gross of costs and cash income",
   "file": "src/lib/types.ts",
   "line": 2742,
   "sev": "P3",
   "surface": "backend",
   "kind": "improvement"
  },
  "performance-accounting:perf-21": {
   "title": "Wash-sale estimate disallows the whole lot loss regardless of replacement quantity and never carries basis forward",
   "file": "src/lib/tax.ts",
   "line": 312,
   "sev": "P3",
   "surface": "desktop-web",
   "kind": "defect"
  },
  "performance-accounting:perf-22": {
   "title": "TWR flow convention assumes the transfer arrived at the START of the sub-period though it is detected at the END snapshot",
   "file": "src/lib/benchmark.ts",
   "line": 137,
   "sev": "P3",
   "surface": "backend",
   "kind": "improvement"
  },
  "performance-accounting:perf-23": {
   "title": "Results has no drawdown, profit factor, expectancy, or selectable window; downside deviation is computed but never shown",
   "file": "app/console/results/page.tsx",
   "line": 230,
   "sev": "P3",
   "surface": "desktop-web",
   "kind": "opportunity"
  },
  "performance-accounting:perf-24": {
   "title": "iOS Home shows Equity + Open P&L only \u2014 no Day P&L or equity sparkline (web Home has both)",
   "file": "ios/SocraticTrade/HomeView.swift",
   "line": 207,
   "sev": "P3",
   "surface": "ios",
   "kind": "opportunity"
  },
  "backend-reliability:berel-01": {
   "title": "FTS5 idempotency DELETE/COUNT use equality on non-rowid columns = full-table scan per chunk; this, not tokenization, is the ~165-300ms/row that pins the loop",
   "file": "src/lib/db-learning.ts",
   "line": 1671,
   "sev": "P1",
   "surface": "backend",
   "kind": "bug"
  },
  "backend-reliability:berel-02": {
   "title": "listFillEvents returns the OLDEST 500 fills (ORDER BY filled_at ASC LIMIT 500) so P&L, open lots, tax, model-stats and dashboard silently freeze once an account passes 500 fills",
   "file": "src/lib/db-fills.ts",
   "line": 332,
   "sev": "P1",
   "surface": "cross-surface",
   "kind": "bug"
  },
  "backend-reliability:berel-03": {
   "title": "Broker calls have no HTTP timeout (Alpaca SDK axios default 0; Tradier fetch has no signal) and the scheduler's stop-monitor / stale-exit lanes are guarded by in-flight sets that only release when the hung promise settles",
   "file": "src/lib/scheduler.ts",
   "line": 914,
   "sev": "P1",
   "surface": "backend",
   "kind": "risk"
  },
  "backend-reliability:berel-04": {
   "title": "filing-body-ingest, persistLocalComplete and document-summarizer still mirror whole documents into FTS synchronously with no yield and no in-flight-strategy gate \u2014 the same pin class the SEC worker was bounded against",
   "file": "src/lib/web-sources/sec-filings.ts",
   "line": 736,
   "sev": "P1",
   "surface": "backend",
   "kind": "risk"
  },
  "backend-reliability:berel-05": {
   "title": "Docker HEALTHCHECK (5 s timeout) probes the heavy /api/health \u2014 measured 4.2 s under loop load \u2014 so a modest pin flips the container unhealthy and a Coolify restart re-halts autonomy",
   "file": "Dockerfile",
   "line": 75,
   "sev": "P3",
   "surface": "backend",
   "kind": "risk"
  },
  "backend-reliability:berel-06": {
   "title": "No event-loop lag instrumentation anywhere \u2014 every loop-pin incident was reconstructed from broker latency and Uptime Robot; retries under a pin (awaitWithFirstCallRetry) just double broker load",
   "file": "src/lib/inflight-deadline.ts",
   "line": 128,
   "sev": "P2",
   "surface": "backend",
   "kind": "improvement"
  },
  "backend-reliability:berel-07": {
   "title": "All CPU-heavy RAG work (HTML extraction, chunking, FTS5 writes, JSON.parse of MB artifacts) runs on the single serving+trading event loop; no worker_threads or sidecar exists",
   "file": "src/lib/rag/sec-ingest-worker.ts",
   "line": 463,
   "sev": "P2",
   "surface": "backend",
   "kind": "opportunity"
  },
  "backend-reliability:berel-08": {
   "title": "Scheduler tick has no re-entrancy guard: setInterval fires every 60 s while a tick can await serial per-account broker health calls and then all strategy runs, so ticks overlap and unguarded per-tick lanes stack under exactly the slow-broker conditions of 08-18",
   "file": "src/lib/scheduler.ts",
   "line": 411,
   "sev": "P2",
   "surface": "backend",
   "kind": "defect"
  },
  "backend-reliability:berel-09": {
   "title": "Alpaca getEquityOrders walks the ENTIRE order history (status:all, up to 50\u00d7500) on every call, and it is called every 60 s by the stale-limit lane and again by pending-fill reconcile and the dashboard",
   "file": "src/lib/alpaca.ts",
   "line": 491,
   "sev": "P2",
   "surface": "backend",
   "kind": "improvement"
  },
  "backend-reliability:berel-10": {
   "title": "Each embed_queued resume slice re-reads the raw filing, JSON.parses sections and chunks artifacts and runs two full-scan FTS COUNTs before writing 6 chunks \u2014 fixed sync overhead repeated ~150 times per large filing",
   "file": "src/lib/rag/sec-ingest-worker.ts",
   "line": 353,
   "sev": "P2",
   "surface": "backend",
   "kind": "defect"
  },
  "backend-reliability:berel-11": {
   "title": "busy_timeout = 60000 on a synchronous single-connection means any external writer contention (rolling-deploy sibling container, litestream checkpoint) freezes the whole process \u2014 HTTP, health probe and scheduler \u2014 for up to a minute",
   "file": "src/lib/db.ts",
   "line": 99,
   "sev": "P3",
   "surface": "backend",
   "kind": "risk"
  },
  "backend-reliability:berel-12": {
   "title": "hasInFlightStrategyWork / markStaleRunningRuns scan strategy_runs by status with no status-leading index; the check runs every 5 s in the SEC worker, per FTS slice and per ROIC period",
   "file": "src/lib/db-execution.ts",
   "line": 311,
   "sev": "P3",
   "surface": "backend",
   "kind": "defect"
  },
  "backend-reliability:berel-13": {
   "title": "litestream-runtime.log is appended forever with tee -a on the persistent volume; nothing rotates or truncates it",
   "file": "scripts/coolify-prod-start.sh",
   "line": 223,
   "sev": "P3",
   "surface": "backend",
   "kind": "risk"
  },
  "backend-reliability:berel-14": {
   "title": "Several always-growing tables have no retention: llm_usage, notification_events, portfolio_snapshots (positions JSON per run), sec_ingest_task_attempts (one row per FTS slice), strategy_runs, history_cache_eod, processed_webhooks",
   "file": "src/lib/db-fills.ts",
   "line": 118,
   "sev": "P3",
   "surface": "backend",
   "kind": "risk"
  },
  "backend-reliability:berel-15": {
   "title": "Alpaca trade-updates stream captures credentials once at start and has no idle/ping watchdog, so a rotated key or a half-open socket silently stops streamed fills until redeploy",
   "file": "src/lib/streams/alpaca-trade-updates-stream.ts",
   "line": 50,
   "sev": "P3",
   "surface": "backend",
   "kind": "defect"
  },
  "backend-reliability:berel-16": {
   "title": "/api/health flags that matter for a trader (schedulerStale, tradingLivenessDegraded, storage/litestream degraded) are informational JSON only; the public probe stays 200 and only openrouterCredits is keyword-monitored",
   "file": "app/api/health/route.ts",
   "line": 88,
   "sev": "P3",
   "surface": "backend",
   "kind": "improvement"
  },
  "security-auth:sec-01": {
   "title": "Production boots with no AUTH_SECRET guard: a missing secret turns every anonymous request into the owner",
   "file": "middleware.ts",
   "line": 385,
   "sev": "P1",
   "surface": "backend",
   "kind": "risk"
  },
  "security-auth:sec-02": {
   "title": "Origin is directly reachable at 167.233.254.55:443 with Host: socratictrade.com \u2014 every Cloudflare layer (WAF, bot/UA rules, upload cap, cf-connecting-ip trust) is bypassable",
   "file": "middleware.ts",
   "line": 173,
   "sev": "P2",
   "surface": "backend",
   "kind": "risk"
  },
  "security-auth:sec-03": {
   "title": "next-auth 5.0.0-beta.31 / @auth/core 0.41.2 carries three published advisories (one critical) with a fix one beta ahead",
   "file": "package.json",
   "line": 56,
   "sev": "P2",
   "surface": "backend",
   "kind": "risk"
  },
  "security-auth:sec-04": {
   "title": "Sessions cannot be revoked: 30-day stateless JWT, sign-out is cookie-delete only, no 'sign out everywhere', no absolute lifetime",
   "file": "src/lib/auth/session-token.ts",
   "line": 6,
   "sev": "P2",
   "surface": "cross-surface",
   "kind": "risk"
  },
  "security-auth:sec-05": {
   "title": "Mobile web-auth handoff mints a session code for ANY authenticated GET with an arbitrary code_challenge \u2014 a scheme-squatting app plus one tapped link steals a 30-day session",
   "file": "app/api/mobile/auth-redirect/route.ts",
   "line": 24,
   "sev": "P2",
   "surface": "ios",
   "kind": "risk"
  },
  "security-auth:sec-06": {
   "title": "Unauthenticated /api/mobile/auth/exchange buffers an unbounded JSON body (and /api/mobile/auth/apple has no rate limit) on the single process that also runs trading",
   "file": "app/api/mobile/auth/exchange/route.ts",
   "line": 8,
   "sev": "P2",
   "surface": "backend",
   "kind": "defect"
  },
  "security-auth:sec-07": {
   "title": "Middleware matcher skips any path ending in .png/.svg/... including /api/* \u2014 identity-header stripping, CSRF, and auth do not run there",
   "file": "middleware.ts",
   "line": 435,
   "sev": "P3",
   "surface": "backend",
   "kind": "risk"
  },
  "security-auth:sec-08": {
   "title": "228 lib/db functions default userId to \"local\" (the owner's account) \u2014 a forgotten argument silently reads or writes the owner's data",
   "file": "src/lib/db-learning.ts",
   "line": 12,
   "sev": "P3",
   "surface": "backend",
   "kind": "risk"
  },
  "security-auth:sec-09": {
   "title": "Native Sign in with Apple does not use a nonce, so a captured identity token is replayable for its lifetime",
   "file": "app/api/mobile/auth/apple/route.ts",
   "line": 23,
   "sev": "P3",
   "surface": "ios",
   "kind": "risk"
  },
  "security-auth:sec-10": {
   "title": "CSP has been report-only since 2026-07-01 with unsafe-inline/unsafe-eval; nothing is enforced and reports only go to console.log",
   "file": "middleware.ts",
   "line": 126,
   "sev": "P3",
   "surface": "desktop-web",
   "kind": "improvement"
  },
  "security-auth:sec-11": {
   "title": "/api/ops/* falls back to ADMIN_REINDEX_TOKEN when OPS_DIAGNOSTIC_TOKEN is unset \u2014 one token spans two trust levels",
   "file": "src/lib/ops-auth.ts",
   "line": 21,
   "sev": "P3",
   "surface": "api",
   "kind": "risk"
  },
  "security-auth:sec-12": {
   "title": "x-admin-token path has no rate limit and no failure audit; middleware lets any bearer/x-admin-token request straight to admin handlers",
   "file": "src/lib/auth/admin.ts",
   "line": 88,
   "sev": "P3",
   "surface": "api",
   "kind": "risk"
  },
  "security-auth:sec-13": {
   "title": "POST /api/mobile/push/register reassigns a device token owned by another user to the caller",
   "file": "app/api/mobile/push/register/route.ts",
   "line": 84,
   "sev": "P3",
   "surface": "ios",
   "kind": "risk"
  },
  "security-auth:sec-14": {
   "title": "No local app lock on iOS: the 30-day session cookie is available to anyone holding the unlocked phone",
   "file": "ios/SocraticTrade/MobileStore.swift",
   "line": 633,
   "sev": "P3",
   "surface": "ios",
   "kind": "opportunity"
  },
  "llm-agent-architecture:llm-01": {
   "title": "Bull strict JSON schema lists `exitPlan` in properties but not in `required` \u2014 OpenAI-family Green seats 400 since 2026-08-16 (misdiagnosed as 'terra unservable')",
   "file": "src/lib/strategy.ts",
   "line": 5847,
   "sev": "P1",
   "surface": "backend",
   "kind": "bug"
  },
  "llm-agent-architecture:llm-02": {
   "title": "Red Team reviewer resolves the ACTIVE console account, not the account under review (venue/short capability, execution mode, strategy prompt)",
   "file": "src/lib/red-team.ts",
   "line": 192,
   "sev": "P1",
   "surface": "backend",
   "kind": "bug"
  },
  "llm-agent-architecture:llm-03": {
   "title": "Delta-only macro pruning strips unchanged FRED fields from every run after the first \u2014 the stateless model never sees values the prompt tells it to use",
   "file": "src/lib/strategy.ts",
   "line": 4960,
   "sev": "P1",
   "surface": "backend",
   "kind": "defect"
  },
  "llm-agent-architecture:llm-04": {
   "title": "Daily LLM learning review auto-approves strategy-directive pending rows into the strategy prompt as TRUSTED owner text (contract says human approval required)",
   "file": "src/lib/learning-review.ts",
   "line": 660,
   "sev": "P1",
   "surface": "backend",
   "kind": "risk"
  },
  "llm-agent-architecture:llm-05": {
   "title": "Approved coach directives are appended to the ACTIVE account's strategy prompt (and user/profile prompt) regardless of the pending row's account",
   "file": "src/lib/learned-context/store.ts",
   "line": 401,
   "sev": "P2",
   "surface": "backend",
   "kind": "bug"
  },
  "llm-agent-architecture:llm-06": {
   "title": "'Retry Red Team' stamps approve-at-half / reject verdicts without applying the haircut or hold \u2014 approving the card places FULL size",
   "file": "src/lib/retry-red-team.ts",
   "line": 47,
   "sev": "P2",
   "surface": "cross-surface",
   "kind": "bug"
  },
  "llm-agent-architecture:llm-07": {
   "title": "Every Red Team review re-sends the ENTIRE Green user content (all candidates, RAG, learned, reflection, scorecards) \u2014 RedTeamReviewContext documents a subset the code ignores",
   "file": "src/lib/strategy.ts",
   "line": 6318,
   "sev": "P2",
   "surface": "backend",
   "kind": "improvement"
  },
  "llm-agent-architecture:llm-08": {
   "title": "Strict-parse path trusts provider schema enforcement; json_object transports (DeepSeek, Gemini fallback) can deliver off-playbook tags / non-venue types / null enums that sanitizeProposals silently normalizes",
   "file": "src/lib/strategy.ts",
   "line": 6115,
   "sev": "P2",
   "surface": "backend",
   "kind": "defect"
  },
  "llm-agent-architecture:llm-09": {
   "title": "Late (post-soft-timeout) LLM responses are audited but never metered; Red Team late responses are not captured at all \u2014 the Usage page and budget ledger miss the most expensive calls",
   "file": "src/lib/strategy.ts",
   "line": 6594,
   "sev": "P2",
   "surface": "desktop-web",
   "kind": "defect"
  },
  "llm-agent-architecture:llm-10": {
   "title": "Hardcoded 'dead' OpenRouter model list drops claude-fable-5 and kimi-latest from rotation although the live catalog serves both slugs today",
   "file": "src/lib/model-rotation.ts",
   "line": 73,
   "sev": "P2",
   "surface": "backend",
   "kind": "bug"
  },
  "llm-agent-architecture:llm-11": {
   "title": "Under rotation the approval card asserts 'this run's rotation pick, not a failover' even when an implicit fallback (the cheapest seats) actually served the proposal",
   "file": "app/console/components/approval-card.tsx",
   "line": 127,
   "sev": "P2",
   "surface": "cross-surface",
   "kind": "defect"
  },
  "llm-agent-architecture:llm-12": {
   "title": "Coach/Assistant routes models to NATIVE providers while the engine, rotation and the policy save-gate assume universal OpenRouter \u2014 an OpenRouter-only owner sees 'no key' for every Coach model and llama routes to OpenAI",
   "file": "src/lib/chat/llm.ts",
   "line": 655,
   "sev": "P2",
   "surface": "desktop-web",
   "kind": "defect"
  },
  "llm-agent-architecture:llm-13": {
   "title": "Learned-context semantic gate resolves its LLM via env CHAT_LLM (getLLM) \u2014 MockLLM in production, so the 'on by default' second-layer risk classifier is silently inert",
   "file": "src/lib/learned-context/semantic-gate.ts",
   "line": 124,
   "sev": "P2",
   "surface": "backend",
   "kind": "defect"
  },
  "llm-agent-architecture:llm-14": {
   "title": "Strategy page Red Team hint describes the retired two-pass design ('deeper adversarial debate on high-conviction \u2026 ideas'); fallback hint hides the implicit rotation fallback",
   "file": "app/console/strategy/page.tsx",
   "line": 722,
   "sev": "P2",
   "surface": "desktop-web",
   "kind": "defect"
  },
  "llm-agent-architecture:llm-15": {
   "title": "/console/usage still shows no embedding/rerank spend \u2014 RAG cost is only on the admin RAG-coverage page",
   "file": "app/console/usage/page.tsx",
   "line": 10,
   "sev": "P2",
   "surface": "desktop-web",
   "kind": "improvement"
  },
  "llm-agent-architecture:llm-16": {
   "title": "OpenRouter's billed `usage.cost` is never requested or stored; all cost figures come from a hand-maintained price table",
   "file": "src/lib/llm-usage.ts",
   "line": 180,
   "sev": "P2",
   "surface": "backend",
   "kind": "improvement"
  },
  "llm-agent-architecture:llm-17": {
   "title": "No Settings nudge when Green and Red are the same model (the owner-approved replacement for the dropped hard independence rule)",
   "file": "app/console/strategy/page.tsx",
   "line": 716,
   "sev": "P3",
   "surface": "desktop-web",
   "kind": "improvement"
  },
  "llm-agent-architecture:llm-18": {
   "title": "Pending-proposal revalidation feeds raw rationale (persisted model + Red text) to a withdraw/reaffirm reviewer with no containment and no data-not-command clause",
   "file": "src/lib/proposal-revalidation.ts",
   "line": 203,
   "sev": "P3",
   "surface": "backend",
   "kind": "risk"
  },
  "llm-agent-architecture:llm-19": {
   "title": "'Mock - deterministic, no key' is a product-facing model in the Coach picker (a test/simulation mode in the console)",
   "file": "app/ui/llm-model-catalog.ts",
   "line": 124,
   "sev": "P3",
   "surface": "desktop-web",
   "kind": "defect"
  },
  "llm-agent-architecture:llm-20": {
   "title": "Prompt versions bump weekly (2.8\u21922.12 in 10 days) with no behavioral eval \u2014 the 'offline strategy eval' scores fixtures, not model output; no recorded-payload replay per version",
   "file": "src/lib/strategy-prompts.ts",
   "line": 20,
   "sev": "P3",
   "surface": "backend",
   "kind": "opportunity"
  },
  "market-data-integrity:mdi-01": {
   "title": "Weekend TTL extension fires during Friday's live session: screener/OHLC/enrichment caches freeze until Monday",
   "file": "src/lib/market-hours.ts",
   "line": 288,
   "sev": "P1",
   "surface": "backend",
   "kind": "bug"
  },
  "market-data-integrity:mdi-02": {
   "title": "Daily-bar cache served as 'fresh' for 3 calendar days: series lags up to two sessions and today's partial bar is persisted as an EOD close",
   "file": "src/lib/history.ts",
   "line": 246,
   "sev": "P1",
   "surface": "cross-surface",
   "kind": "bug"
  },
  "market-data-integrity:mdi-03": {
   "title": "Cascade stamps cache-served provider values with a 'now' fetchedAt/asOf; nasdaq-quote price/change/volume are cached 6h and win volume arbitration on fabricated recency",
   "file": "src/lib/data-providers.ts",
   "line": 1571,
   "sev": "P1",
   "surface": "cross-surface",
   "kind": "bug"
  },
  "market-data-integrity:mdi-04": {
   "title": "asOf fabricated from the wall clock (Yahoo quote-only fallback, ROIC profile price, CBOE, grouped VWAP) defeats the quote-staleness gate",
   "file": "src/lib/market.ts",
   "line": 1520,
   "sev": "P1",
   "surface": "backend",
   "kind": "bug"
  },
  "market-data-integrity:mdi-05": {
   "title": "Alpaca broker quote 'price' is the ASK; it becomes the displayed Price, watchlist price, alert trigger and stop/take-profit mark, while Chg% stays the screener's",
   "file": "src/lib/alpaca.ts",
   "line": 551,
   "sev": "P2",
   "surface": "cross-surface",
   "kind": "defect"
  },
  "market-data-integrity:mdi-06": {
   "title": "Interactive Scan VWAP is yesterday's grouped-daily VWAP stamped with fetch time and described as today's session",
   "file": "src/lib/market.ts",
   "line": 1241,
   "sev": "P2",
   "surface": "desktop-web",
   "kind": "defect"
  },
  "market-data-integrity:mdi-07": {
   "title": "Nasdaq screener asOf is a sentence ('Last price as of Aug 18, 2026'); it is stored as MarketQuote.asOf and renders 'Invalid Date' in the drilldown",
   "file": "app/console/ui/symbol-drilldown.tsx",
   "line": 511,
   "sev": "P2",
   "surface": "desktop-web",
   "kind": "defect"
  },
  "market-data-integrity:mdi-08": {
   "title": "Dynamic universes (NYSE/Nasdaq Composite, FT Wilshire 5000) include preferreds, baby bonds and units \u2014 ~20% of the NYSE screener",
   "file": "src/lib/market.ts",
   "line": 1360,
   "sev": "P2",
   "surface": "backend",
   "kind": "risk"
  },
  "market-data-integrity:mdi-09": {
   "title": "'volume' mixes session, 10-day average and IEX-only volume across providers, feeding liquidity score, dollar-volume floor and outlier detection",
   "file": "src/lib/data-providers.ts",
   "line": 2143,
   "sev": "P2",
   "surface": "cross-surface",
   "kind": "defect"
  },
  "market-data-integrity:mdi-10": {
   "title": "No early-close (1:00 PM ET) calendar: sessions read 'regular' until 4 PM on half-days",
   "file": "src/lib/market-hours.ts",
   "line": 35,
   "sev": "P2",
   "surface": "backend",
   "kind": "risk"
  },
  "market-data-integrity:mdi-11": {
   "title": "History cascade merges split-only, split+dividend and IEX-only bars into one series/cache without tagging the adjustment basis",
   "file": "src/lib/history.ts",
   "line": 611,
   "sev": "P2",
   "surface": "cross-surface",
   "kind": "defect"
  },
  "market-data-integrity:mdi-12": {
   "title": "Yahoo v7 batch quote runs without the crumb session the repo says v7 now requires; failure is silent and the fallback fans out unbounded per-symbol chart calls",
   "file": "src/lib/yahoo-finance.ts",
   "line": 147,
   "sev": "P2",
   "surface": "backend",
   "kind": "risk"
  },
  "market-data-integrity:mdi-13": {
   "title": "Snapshot/benchmark day keys use the UTC date: evening (post-market) snapshots align to the NEXT session's SPY close",
   "file": "src/lib/cash-flows.ts",
   "line": 33,
   "sev": "P2",
   "surface": "backend",
   "kind": "defect"
  },
  "market-data-integrity:mdi-14": {
   "title": "Dead mock enrichment tier still exported (hash-generated PE/EPS/dividends/headlines) despite AGENTS.md saying it was removed",
   "file": "src/lib/data-providers.ts",
   "line": 1255,
   "sev": "P3",
   "surface": "backend",
   "kind": "risk"
  },
  "market-data-integrity:mdi-15": {
   "title": "Index membership is a static Wikipedia snapshot from 2026-06-14; the live S&P 500 constituents module has no consumer",
   "file": "src/lib/sp500.ts",
   "line": 1,
   "sev": "P3",
   "surface": "backend",
   "kind": "improvement"
  },
  "market-data-integrity:mdi-16": {
   "title": "Delayed/cached provenance not surfaced: iOS scan says 'as of <generatedAt>', web chip says 'fresh', dashboard tile shows no age",
   "file": "ios/SocraticTrade/ScanView.swift",
   "line": 59,
   "sev": "P3",
   "surface": "cross-surface",
   "kind": "improvement"
  },
  "market-data-integrity:mdi-17": {
   "title": "Watchlist shows a bare price: no age, no change, raw provider id, and the price is the broker ask",
   "file": "app/console/watchlist/page.tsx",
   "line": 253,
   "sev": "P3",
   "surface": "desktop-web",
   "kind": "improvement"
  },
  "market-data-integrity:mdi-18": {
   "title": "Alpaca after-hours close fallback stamps asOf as String(bar.time) and labels any history tier as 'yahoo-finance-delayed'",
   "file": "src/lib/alpaca.ts",
   "line": 569,
   "sev": "P3",
   "surface": "backend",
   "kind": "defect"
  },
  "ux-copy-editor:copy-01": {
   "title": "Two-space sentence rule is unenforced across ~600 web copy strings; SENTENCE_GAP used in only 9 files",
   "file": "app/console/page.tsx",
   "line": 265,
   "sev": "P2",
   "surface": "desktop-web",
   "kind": "defect"
  },
  "ux-copy-editor:copy-02": {
   "title": "Double ASCII spaces typed into JSX text collapse to one space at render \u2014 looks compliant in source, isn't on screen",
   "file": "app/console/components/consent-gate.tsx",
   "line": 107,
   "sev": "P2",
   "surface": "cross-surface",
   "kind": "defect"
  },
  "ux-copy-editor:copy-03": {
   "title": "Macro page says below-median buys are \"hard-vetoed\" in risk-off/crisis regimes; the code is advisory tag-not-drop",
   "file": "app/console/macro/indicators.ts",
   "line": 698,
   "sev": "P1",
   "surface": "desktop-web",
   "kind": "bug"
  },
  "ux-copy-editor:copy-04": {
   "title": "iOS tells the user to edit the universe \"on the Strategy page\" \u2014 the app has no Strategy screen and on the web the universe editor lives under Guardrails",
   "file": "ios/SocraticTrade/HomeView.swift",
   "line": 94,
   "sev": "P1",
   "surface": "ios",
   "kind": "bug"
  },
  "ux-copy-editor:copy-05": {
   "title": "\"Approvals\" vs \"Proposals\": nav/H1/iOS tab say Proposals, but a dozen copy strings send users to \"Approvals\"",
   "file": "app/console/assistant/draft-card.tsx",
   "line": 343,
   "sev": "P2",
   "surface": "cross-surface",
   "kind": "defect"
  },
  "ux-copy-editor:copy-06": {
   "title": "Coach page still calls itself \"the assistant\" in six user-facing strings",
   "file": "app/console/assistant/chat.tsx",
   "line": 385,
   "sev": "P3",
   "surface": "desktop-web",
   "kind": "defect"
  },
  "ux-copy-editor:copy-07": {
   "title": "One run state, three names (Exit-only / Close-only / Close Only) and one stop control, four names (Stop Agent / STOP everything / system stopped / strategy stopped) across web and iOS",
   "file": "app/console/lib/derive.ts",
   "line": 171,
   "sev": "P2",
   "surface": "cross-surface",
   "kind": "defect"
  },
  "ux-copy-editor:copy-08": {
   "title": "Web console headings and buttons are sentence case (\"Run once\", \"Commit changes\", \"Win rate\") against the Title Case rule and against iOS (\"Run Once\", \"Win Rate\")",
   "file": "app/console/components/chrome.tsx",
   "line": 789,
   "sev": "P2",
   "surface": "desktop-web",
   "kind": "defect"
  },
  "ux-copy-editor:copy-09": {
   "title": "iOS mixes Title Case and sentence case inside the same tile rows, empty states, insight titles, and section subtitles; \"Ask-First\" vs \"Ask-first\"",
   "file": "ios/SocraticTrade/ActivityView.swift",
   "line": 28,
   "sev": "P3",
   "surface": "ios",
   "kind": "defect"
  },
  "ux-copy-editor:copy-10": {
   "title": "\"Green team\"/\"Red team\" lowercase in 18 places vs \"Green Team\"/\"Red Team\" product names in 103",
   "file": "app/console/settings/help.tsx",
   "line": 103,
   "sev": "P3",
   "surface": "cross-surface",
   "kind": "defect"
  },
  "ux-copy-editor:copy-11": {
   "title": "ALL-CAPS \"(NOT Real Money)\" / \"(LIVE \u2014 Real Money)\" account labels and shouting PAPER chips contradict `Alpaca (paper)` and the no-ceremony ruling",
   "file": "app/console/settings/brokers.tsx",
   "line": 626,
   "sev": "P2",
   "surface": "cross-surface",
   "kind": "defect"
  },
  "ux-copy-editor:copy-12": {
   "title": "iOS proposal cards show a red \"Live\" pill on every live proposal \u2014 the rule and the iOS Home hero both say no Live callouts",
   "file": "ios/SocraticTrade/ProposalsView.swift",
   "line": 366,
   "sev": "P2",
   "surface": "ios",
   "kind": "defect"
  },
  "ux-copy-editor:copy-13": {
   "title": "Scolding + inaccurate breaker copy: \"a hard limit did its job. Review what fired before loosening anything.\" while breakers are advisory by default",
   "file": "app/console/components/chrome.tsx",
   "line": 662,
   "sev": "P2",
   "surface": "desktop-web",
   "kind": "defect"
  },
  "ux-copy-editor:copy-14": {
   "title": "Setup copy says \"Connect Alpaca or Robinhood\" while Connections offers Alpaca, Robinhood, Tradier, Public and eToro",
   "file": "app/console/lib/derive.ts",
   "line": 534,
   "sev": "P2",
   "surface": "cross-surface",
   "kind": "defect"
  },
  "ux-copy-editor:copy-15": {
   "title": "Web compact money renders uppercase suffixes (\"$3.4T\", \"$850B\", \"$1K\u2013$15K\") via Intl compact; rule and iOS use lowercase k/m/b",
   "file": "app/console/ui/drilldown-data.ts",
   "line": 674,
   "sev": "P3",
   "surface": "desktop-web",
   "kind": "defect"
  },
  "ux-copy-editor:copy-16": {
   "title": "\"Mock (offline)\" model is offered as the first group in the Coach model picker (web + iOS) and the server silently answers with a mock when no key resolves",
   "file": "app/ui/llm-model-catalog.ts",
   "line": 124,
   "sev": "P2",
   "surface": "cross-surface",
   "kind": "defect"
  },
  "ux-copy-editor:copy-17": {
   "title": "Public pages, Terms and Privacy use sentence-case section headings and have zero NBSP/SENTENCE_GAP (all single-space sentences)",
   "file": "app/welcome/page.tsx",
   "line": 146,
   "sev": "P3",
   "surface": "public-site",
   "kind": "defect"
  },
  "ux-copy-editor:copy-18": {
   "title": "First-run/empty and loading copy leaks engineering vocabulary (\"snapshot\", \"persisted decision trace\", \"dashboard API\", \"autonomy desk\")",
   "file": "app/console/page.tsx",
   "line": 443,
   "sev": "P3",
   "surface": "desktop-web",
   "kind": "improvement"
  },
  "ux-copy-editor:copy-19": {
   "title": "Same guardrail fields carry different labels on web vs iOS (Max per order / Max Order, Max spend per day / Daily Cap, Run every / Cadence, Base indices / Indices)",
   "file": "ios/SocraticTrade/GuardrailsView.swift",
   "line": 69,
   "sev": "P3",
   "surface": "cross-surface",
   "kind": "improvement"
  },
  "ux-copy-editor:copy-20": {
   "title": "Settings jump-list labels do not match the card titles they scroll to (Display\u2192Appearance, Boot\u2192After a restart, AI budget\u2192Daily AI Budget\u2026)",
   "file": "app/console/settings/page.tsx",
   "line": 63,
   "sev": "P3",
   "surface": "desktop-web",
   "kind": "improvement"
  },
  "ux-copy-editor:copy-21": {
   "title": "\"P/L\" on Orders and proposal cards vs \"P&L\" everywhere else (web and iOS)",
   "file": "app/console/orders/page.tsx",
   "line": 376,
   "sev": "P3",
   "surface": "cross-surface",
   "kind": "defect"
  },
  "ux-copy-editor:copy-22": {
   "title": "Microcopy nits: release-note voice (\"Bulk approve now supports LIVE proposals\"), brand spelled two ways, one browser-timezone timestamp",
   "file": "app/console/approvals/page.tsx",
   "line": 466,
   "sev": "P3",
   "surface": "desktop-web",
   "kind": "defect"
  },
  "qa-test-strategy:qa-01": {
   "title": "iOS unit tests never execute in CI or before TestFlight ship",
   "file": ".github/workflows/ios-build.yml",
   "line": 73,
   "sev": "P1",
   "surface": "ios",
   "kind": "risk"
  },
  "qa-test-strategy:qa-02": {
   "title": "Swift build result is not part of the required merge gate",
   "file": ".github/workflows/ci.yml",
   "line": 305,
   "sev": "P1",
   "surface": "ios",
   "kind": "risk"
  },
  "qa-test-strategy:qa-03": {
   "title": "Docs-only CI fast path skips the build for build-time-imported docs/benchmarks JSON",
   "file": ".github/workflows/ci.yml",
   "line": 141,
   "sev": "P1",
   "surface": "backend",
   "kind": "risk"
  },
  "qa-test-strategy:qa-04": {
   "title": "No API-to-iOS snapshot contract test; a server rename silently blanks phone lists",
   "file": "ios/SocraticTrade/MobileModels.swift",
   "line": 60,
   "sev": "P1",
   "surface": "cross-surface",
   "kind": "risk"
  },
  "qa-test-strategy:qa-05": {
   "title": "No global network guard in vitest; suite still reaches SEC/Yahoo/SiliconFlow",
   "file": "vitest.config.ts",
   "line": 31,
   "sev": "P1",
   "surface": "backend",
   "kind": "defect"
  },
  "qa-test-strategy:qa-06": {
   "title": "Browser coverage is one 18-line desktop smoke on `/`, not run on PRs, no phone width",
   "file": "test/e2e/dashboard-smoke.spec.ts",
   "line": 3,
   "sev": "P2",
   "surface": "cross-surface",
   "kind": "defect"
  },
  "qa-test-strategy:qa-07": {
   "title": "Single-proposal approve, replace-market, pause/enable routes have no route-level tests (63/122 routes untested)",
   "file": "app/api/proposals/[id]/approve/route.ts",
   "line": 11,
   "sev": "P2",
   "surface": "api",
   "kind": "risk"
  },
  "qa-test-strategy:qa-08": {
   "title": "TestBrokerGateway always fills instantly, so integration tests can only see the happy path",
   "file": "src/lib/robinhood.ts",
   "line": 920,
   "sev": "P2",
   "surface": "backend",
   "kind": "risk"
  },
  "qa-test-strategy:qa-09": {
   "title": "MobileStore.submit/load are untested despite an existing URLProtocol harness",
   "file": "ios/SocraticTrade/MobileStore.swift",
   "line": 358,
   "sev": "P2",
   "surface": "ios",
   "kind": "improvement"
  },
  "qa-test-strategy:qa-10": {
   "title": "maxWorkers:1 serializes ~7,000 tests: `npm test` is ~14 min of every CI run",
   "file": "vitest.config.ts",
   "line": 31,
   "sev": "P2",
   "surface": "backend",
   "kind": "improvement"
  },
  "qa-test-strategy:qa-11": {
   "title": "land.sh skips lint and never compiles Swift for ios/** diffs",
   "file": "scripts/land.sh",
   "line": 156,
   "sev": "P2",
   "surface": "cross-surface",
   "kind": "defect"
  },
  "qa-test-strategy:qa-12": {
   "title": "ESLint warn backlog has no ratchet: 49 console warnings (26 set-state-in-effect), 346 in src/api/admin",
   "file": "eslint.config.mjs",
   "line": 52,
   "sev": "P2",
   "surface": "desktop-web",
   "kind": "improvement"
  },
  "qa-test-strategy:qa-13": {
   "title": "No coverage tooling, so thin spots in policy/performance are invisible",
   "file": "package.json",
   "line": 86,
   "sev": "P3",
   "surface": "backend",
   "kind": "improvement"
  },
  "qa-test-strategy:qa-14": {
   "title": "Owner two-space sentence rule has no automated guard on the web (iOS has one)",
   "file": "app/console/scan/page.tsx",
   "line": 111,
   "sev": "P3",
   "surface": "desktop-web",
   "kind": "opportunity"
  },
  "qa-test-strategy:qa-15": {
   "title": "Flat 614-file test dir with PR/agent-named files and two live-network scratch scripts",
   "file": "test/alpaca-ping.ts",
   "line": 4,
   "sev": "P3",
   "surface": "backend",
   "kind": "improvement"
  },
  "qa-test-strategy:qa-16": {
   "title": "codex-autofix workflow fails on every review event, training everyone to ignore red checks",
   "file": ".github/workflows/codex-autofix.yml",
   "line": 21,
   "sev": "P3",
   "surface": "backend",
   "kind": "defect"
  },
  "qa-test-strategy:qa-17": {
   "title": "No invariant test that every API route resolves the request user or is explicitly public",
   "file": "test/route-ownership.test.ts",
   "line": 18,
   "sev": "P2",
   "surface": "api",
   "kind": "improvement"
  },
  "qa-test-strategy:qa-18": {
   "title": "Console and iOS run-state derivations are tested from separate hand-typed tables",
   "file": "ios/SocraticTradeTests/RunStateDerivationTests.swift",
   "line": 4,
   "sev": "P3",
   "surface": "cross-surface",
   "kind": "opportunity"
  },
  "ops-observability-admin:sre-01": {
   "title": "No at-a-glance 'is the trader alive' panel on web/admin/iOS; run-state chip is intent, not liveness",
   "file": "app/admin/page.tsx",
   "line": 99,
   "sev": "P1",
   "surface": "cross-surface",
   "kind": "defect"
  },
  "ops-observability-admin:sre-02": {
   "title": "schedulerStale, tradingLivenessDegraded and OpenRouter low-balance never trigger an in-app push; only external keyword monitors can page",
   "file": "app/api/health/route.ts",
   "line": 128,
   "sev": "P1",
   "surface": "backend",
   "kind": "risk"
  },
  "ops-observability-admin:sre-03": {
   "title": "Backup status can lie: litestream kill-switch markers are invisible to /api/health and Backup Status, and the R2 'writes paused' card ignores the B2 cutover",
   "file": "scripts/coolify-prod-start.sh",
   "line": 188,
   "sev": "P1",
   "surface": "desktop-web",
   "kind": "bug"
  },
  "ops-observability-admin:sre-04": {
   "title": "Docker HEALTHCHECK gives the heavy /api/health only 5s; documented 6\u201313s event-loop stalls can flip the container unhealthy and fail deploy swaps",
   "file": "Dockerfile",
   "line": 75,
   "sev": "P2",
   "surface": "backend",
   "kind": "risk"
  },
  "ops-observability-admin:sre-05": {
   "title": "/api/health cannot represent a broker outage when Alpaca runs on user-lane keys (the production case)",
   "file": "app/api/health/route.ts",
   "line": 235,
   "sev": "P2",
   "surface": "api",
   "kind": "defect"
  },
  "ops-observability-admin:sre-06": {
   "title": "Admin and console have no route-level error boundaries; a render crash drops to the public-site-styled root boundary and loses the admin shell",
   "file": "app/error.tsx",
   "line": 14,
   "sev": "P2",
   "surface": "desktop-web",
   "kind": "defect"
  },
  "ops-observability-admin:sre-07": {
   "title": "Deploy drift is still only detectable by manually running verify-deploy-sha.sh",
   "file": "scripts/verify-deploy-sha.sh",
   "line": 6,
   "sev": "P2",
   "surface": "backend",
   "kind": "risk"
  },
  "ops-observability-admin:sre-08": {
   "title": "All app + litestream stdout is appended forever to a log file on the persistent data volume with no rotation",
   "file": "scripts/coolify-prod-start.sh",
   "line": 223,
   "sev": "P2",
   "surface": "backend",
   "kind": "risk"
  },
  "ops-observability-admin:sre-09": {
   "title": "The LLM/OpenRouter chat lane has no Connections health lane; Admin Connections lists openai/anthropic/gemini/xai user lanes that never receive traffic under universal routing",
   "file": "app/api/admin/connections-health/route.ts",
   "line": 26,
   "sev": "P2",
   "surface": "desktop-web",
   "kind": "defect"
  },
  "ops-observability-admin:sre-10": {
   "title": "Provider/storage alerts re-fire on fixed cooldowns with no 'recovered' event and no 'since/nth reminder', and storage alerting runs only as a side effect of GET /api/health",
   "file": "src/lib/db-health.ts",
   "line": 574,
   "sev": "P2",
   "surface": "cross-surface",
   "kind": "improvement"
  },
  "ops-observability-admin:sre-11": {
   "title": "An overdue next run is never flagged: web says 'Next run now' forever, iOS says 'Next Run 12 minutes ago'",
   "file": "app/console/lib/format.ts",
   "line": 123,
   "sev": "P2",
   "surface": "cross-surface",
   "kind": "defect"
  },
  "ops-observability-admin:sre-12": {
   "title": "Broker-held protective stop declined/failed is audit-only; no owner push",
   "file": "src/lib/broker-protective-stops.ts",
   "line": 1542,
   "sev": "P2",
   "surface": "backend",
   "kind": "improvement"
  },
  "ops-observability-admin:sre-13": {
   "title": "Admin overview fabricates 'CPU load 0.0%' and '0 Running' when the series/probe is empty or failed",
   "file": "app/admin/page.tsx",
   "line": 448,
   "sev": "P2",
   "surface": "desktop-web",
   "kind": "bug"
  },
  "ops-observability-admin:sre-14": {
   "title": "Three backup layers, zero restore drills \u2014 nothing ever proves the B2 replica or R2 cold snapshot restores",
   "file": "src/lib/r2-cold-snapshot.ts",
   "line": 1,
   "sev": "P2",
   "surface": "backend",
   "kind": "risk"
  },
  "ops-observability-admin:sre-15": {
   "title": "Sentry events carry no release; Langfuse release reads a Vercel var \u2014 neither uses the app's own release identity",
   "file": "sentry.server.config.ts",
   "line": 5,
   "sev": "P3",
   "surface": "backend",
   "kind": "improvement"
  },
  "ops-observability-admin:sre-16": {
   "title": "Ops runbook docs are stale (PM2 litestream\u2192R2, self-hosted gitleaks, 'restart pm2 trading') and there is no current 'what pages me and why' page",
   "file": "docs/ops-observability-security.md",
   "line": 52,
   "sev": "P3",
   "surface": "backend",
   "kind": "defect"
  },
  "ops-observability-admin:sre-17": {
   "title": "Server-knob writes are audited as user 'local' regardless of which admin flipped them",
   "file": "app/api/admin/server-knobs/route.ts",
   "line": 80,
   "sev": "P3",
   "surface": "api",
   "kind": "defect"
  },
  "ops-observability-admin:sre-18": {
   "title": "Copy nits on ops surfaces: 'Data catalog' nav label, iOS 'Next run' vs 'Next Run', unlabeled CT timestamps on overview",
   "file": "app/admin/layout.tsx",
   "line": 77,
   "sev": "P3",
   "surface": "cross-surface",
   "kind": "defect"
  },
  "ops-observability-admin:sre-19": {
   "title": "admin.socratictrade.com sub-route redirect list omits the newer admin pages",
   "file": "middleware.ts",
   "line": 276,
   "sev": "P3",
   "surface": "public-site",
   "kind": "defect"
  },
  "ops-observability-admin:sre-20": {
   "title": "Overview R2 card tells the owner to run a curl (POST /api/admin/r2-usage/resume) instead of offering the action",
   "file": "app/admin/page.tsx",
   "line": 501,
   "sev": "P3",
   "surface": "desktop-web",
   "kind": "improvement"
  }
 }
}