<!--
Provenance: produced 2026-07-16 by a 13-agent expert-panel workflow (Claude Code cloud
session), owner-directed: "have market/trading experts and LLM prompt / ML / RAG / coding
experts determine if there is a better way to request/obtain stop strategy and parameters
in trade proposals ... and just debate how to enable the system to most intelligently
propose trades (longs/shorts/options) with exit strategies planned, modify exit strategies
when needed, and ensure proper execution of exit strategies."
Structure: 4 code-mapping readers -> 4 domain-expert position papers (trader-risk,
quant-ml, llm-prompt, exec-systems) -> 4 cross-critique debate rounds -> 1 synthesis whose
load-bearing claims were re-verified against the working tree (main @ 32362e9).
File:line citations reference that commit and will drift.
Related docs: docs/phase-7-strategy.md, docs/capability-trading-roadmap.md,
docs/event-driven-llm-triggering.md, docs/rollouts/2026-07-16-exit-strategy-expert-panel.md
-->

# Exit-Strategy Intelligence: Expert Panel Findings

**Panel:** trader-risk, quant-ml, llm-prompt, exec-systems — four subsystem maps, four position papers, one adversarial debate round. **Synthesis basis:** all load-bearing claims below were re-verified against the working tree during synthesis (including the debate round's one major factual correction — see Gap 2). Effort scale: **S** ≈ ≤1 day, **M** ≈ 2–5 days, **L** ≈ 1–2+ weeks of single-agent work, each including tests.

---

## Verdict in one paragraph

Socratic.Trade's exit *plumbing* is genuinely excellent — idempotent synthetic fires with CAS claims and portable refIds (`synthetic-stops.ts:643-677`, `db-api-keys.ts:900-979`), lot-keyed stop-plan integrity (`db-api-keys.ts:1542-1584`), bracket teardown queues (`broker-protective-stops.ts:994-1043`) — but its exit *policy* is fire-and-forget: a stop plan is written once at fill, can never be revised without adding size (`sanitizeProposals` drops `stopPlan` on sell/cover, `strategy.ts:5195-5197`), is invisible to the LLM that re-decides exits every hour (`userContent` at `strategy.ts:4249-4364` carries no protection state), and is enforced through lanes with three silent tail holes: a bad-tick filter that permanently disarms trailing stops after a >10% gap (`synthetic-stops.ts:30,114,528`), fixed/ATR plans that have **no tick-cadence enforcement at all** (excluded from the synthetic monitor at `synthetic-stops.ts:406,440` — hourly proactive checks only), and long-only broker-held protection (`broker-protective-stops.ts:492-499`) on the eve of live shorts (owner decision 2026-07-10). The panel converged on a three-phase plan: **A)** make today's promises true (gap fix, halted-mode protection, prompt visibility, honest disclosures, options visibility); **B)** persist a parameterized Exit Contract and extend broker-resident lanes to ATR plans and shorts; **C)** add the hold-and-retune revision verb, structured invalidation/time exits, clamped numeric elicitation, and the counterfactual ledger that makes any of it measurable. Every new behavior ships as an owner-adjustable preference with a default and an off-switch — the `iraWashSaleHandling` template — never a cage.

---

## How exits work today

**At proposal time**, the Bull strategist emits per-trade `bracketStopLoss`/`bracketTakeProfit` prices and a `stopPlan` style (`default|fixed|atr|trailing|none` + rationale) on openings (`strategy.ts:4463-4479, 4541-4542`). It cannot set numeric parameters (ATR multiple, trail %, fixed %) — those come from account `riskRules` (`types.ts:586-612`); there is no invalidation field on a normal opening (only on `autonomyOverride`, `strategy.ts:4450`) and no time-based exit field anywhere (`holdingDays` is outcome-only, `types.ts:2139`). Nothing is ever rejected: `sanitizeProposals` (`strategy.ts:5150-5216`) drops/clamps, and `enrichOpeningProposal` (`strategy.ts:5254-5429`) backfills defaults, attaches broker brackets where supported (Alpaca/Tradier, ≥1 whole share), and — for explicit fixed/atr plans — **discards the LLM's own stop price and reprices from the account-pinned distance** so all enforcement layers agree (`strategy.ts:5343-5348`, PR #1371).

**After fill**, the surviving plan is persisted to `position_stop_plans` keyed by lot basis (`performance.ts:300-361`; `db.ts:2435-2446`) and enforced by a stack: broker-native brackets/trails (Alpaca `alpaca.ts:472-567`, Tradier OTOCO `tradier.ts:690-748`), broker-held GTC stops re-placed each tick by a reconciler (`broker-protective-stops.ts:210-992`), a ~60s synthetic trailing-stop monitor (`synthetic-stops.ts:147-754`, invoked `scheduler.ts:691-696`), hourly proactive full-exit proposals on stop breach (`generateProactiveRiskProposals`, `strategy.ts:5431-5551`), and deterministic take-profit trim bands (`strategy.ts:5600-5653`). Exits are structurally privileged in policy — sell/cover pass through halted/close_only/liquidating states and skip opening gates (`policy.ts:315-330`). The LLM re-reviews the full held book every cadence run (default 60 min) with rich context (`strategy.ts:4285-4363`) and may sell/trim any position (`strategy.ts:3940`).

**The four most important gaps:**

1. **Write-once, decided blind.** The stop plan is frozen at entry for the position's life — the LLM can only change it by scaling in (`strategy.ts:5195-5197`), the owner cannot change it per-position at all (no route/UI writes `position_stop_plans`; display-only, `app/console/components/positions.tsx:48`) — and the LLM re-decides exits every run without seeing the plan, the resting protection, or the distance-to-stop (verified: zero `stopPlan` references in `strategy-prompts.ts`; no protection field in `userContent`).
2. **Enforcement tail holes — with a corrected exposure map.** The debate round found that three of four papers mis-stated who the gap-deadlock bug hits. Verified truth: fixed/atr plans are *purged from and never registered in* the synthetic monitor (`synthetic-stops.ts:406, 440`) — their only intraday enforcement is the hourly proactive check plus any surviving entry bracket. So (a) an **atr-plan position on Tradier opened with a market entry has effectively no protection but an hourly check** (Tradier market entries silently drop the bracket, `tradier.ts:683-692`; Tradier has no broker-held stop lane, `broker-protective-stops.ts:97`); (b) the bad-tick deadlock (`synthetic-stops.ts:30,114,528` — a >10% gap makes every subsequent real print a "bad tick" forever, and a bad tick never updates `lastPrice`) disarms **trailing-plan and account-trail positions, including shorts** (`synthetic-stops.ts:443-444`), exactly when the stop matters most; (c) an unattended restart reverts `active`→`halted` (`scheduler.ts:274-318`, unless per-user `autoResumeOnBoot`) and `halted` skips the monitor entirely (`scheduler.ts:683-696` — "the only no-order state"), leaving only broker-held protection.
3. **Shorts are going live with the thinnest protection tier.** All broker-held stop lanes filter `quantity > 0` (`broker-protective-stops.ts:492-499`); a short whose entry bracket was stripped (`stopPlan:"trailing"/"none"` strip both legs, `strategy.ts:5293-5296`) is protected only by the app-uptime synthetic lane — which carries gap #2 — against an unbounded-loss instrument.
4. **Elicitation contradicts execution, and some positions are invisible.** The prompt demands setup-derived, ATR-scaled stops ("Do NOT default every trade to the same fixed percentage," `strategy-prompts.ts:149`) while the candidate evidence contains **no ATR field** (verified: `compactCandidateForPrompt`, `strategy.ts:5012-5052`) and fixed/atr plans reprice the LLM's stop away anyway (`strategy.ts:5343-5348`); the prompt also instructs shorts to emit `shortStopLossPct` (`strategy-prompts.ts:105`) — a policy field the schema cannot express. Meanwhile OCC option positions are deliberately filtered out of the book (`tradier.ts:499-506`), so an assigned or manually-opened contract is invisible to every exit layer.

---

## Consensus recommendations (post-debate, ranked)

### 1. Close the synthetic-lane tail holes: gap-deadlock fix + protective mode in `halted` — **S-M**
*Unanimous (all four papers, all four critiques). The only outright bug on the board.*
**What/why:** (a) Replace single-print rejection in `evaluateStop` with confirmation-based acceptance: persist `suspect_price`/`suspect_count` on the synthetic-stop row (state must survive tick restarts — in-memory is not enough); N=2–3 consecutive out-of-band prints agreeing within ~1.5–2% accept the new level, update `lastPrice`, and evaluate the stop; add a session-boundary reset at the regular-hours open. **Debate amendment (trader):** during extended hours, acceptance must be corroborated by a real (non-synthetic) bid/ask — the quote plumbing already distinguishes these (`synthetic-stops.ts:516-520`) — so two thin 4 a.m. prints can't fire a marketable-limit into a chasm. Note the DB `lastPrice` fallback (`:495-506`) cannot corroborate — it replays the stale anchor itself (exec correction). (b) Run the monitor in `halted` in protective-only mode behind an owner preference (`protectWhileHalted`, default on): the suppression hook already exists (`running=false` → "exit suppressed" audit, `synthetic-stops.ts:532-534`); change `protectiveState` at `scheduler.ts:683-696` to include halted when the toggle is set. This decouples "resume trading" from "resume protection" — today `autoResumeOnBoot` (`scheduler.ts:277-298`) is the only lever and it re-arms *everything*.
**Sketch:** `synthetic-stops.ts` (`evaluateStop`, `:109-130`; upsert at `:528`), two columns in `db.ts` migrate + `db-api-keys.ts` upsert, `scheduler.ts:683-696`, a Guardrails toggle in `field-defs.ts`. Unit test replaying a −25% gap sequence and an extended-hours thin-print sequence. Because the scheduler comment calls halted "the only no-order state" deliberately, write the design note per CLAUDE.md before changing it.

### 2. Give fixed/atr plans a tick-cadence lane: static-trigger rows in the existing synthetic framework — **M**
*The debate's corrected version of "tick-cadence breach checks." Exec-systems' mechanism; serves trader-risk's goal; no dissent after correction.*
**What/why:** Fixed/atr positions currently have no sub-hourly enforcement (`synthetic-stops.ts:406,440`). Do **not** generate proposals from the tick loop (rejected — see What NOT to Do); instead generalize `SyntheticTrailingStop` to carry a static trigger price and register fixed/atr plans as fixed-level rows. They inherit, for free, the CAS claim (`db-api-keys.ts:974-979`), refId dedup and fire-generation re-arm (`synthetic-stops.ts:258-322`), side-aware coverage math (`broker-side.ts:100-186`), the bad-tick filter (as fixed by Rec 1), and protective-exit routing — one lane, one idempotency scheme, which is exactly the discipline the 2026-07-08 MU incident bought.
**Sketch:** add a `kind: "trailing" | "fixed_level"` (or nullable `staticTriggerPrice`) to the synthetic row; registration path (`synthetic-stops.ts:414-490`) registers fixed/atr plans instead of `continue`-ing at `:440`, priced from the same `effectiveStopPct` precedence the proactive layer uses (`strategy.ts:5474-5489`) so both layers agree today, upgraded to the persisted contract distance after Rec 3; purge logic at `:404-412` updated so plan exclusion applies only to *trailing* rows. Keep `generateProactiveRiskProposals` as the run-cadence backstop.

### 3. Persist the parameterized Exit Contract in `position_stop_plans` — **M**
*Exec-systems R1 + quant-ml #4; endorsed by every critique as the keystone migration.*
**What/why:** The plan row stores only style + rationale, which is *why* atr plans are excluded from broker-held lanes ("the reconciler only knows the flat %," `broker-protective-stops.ts:283-292`) and why the enricher must discard LLM stop prices (`strategy.ts:5343-5348`). Add nullable columns: `resolved_stop_pct`, `stop_price`, `entry_atr_pct`, `trail_percent` (exists), `take_profit_price`, `max_holding_until`, `invalidation`. Write them at fill commit (`performance.ts:300-361`; recovery paths `strategy-execution.ts:170-211, 1268-1305`) from the values `enrichOpeningProposal` already computed (`strategy.ts:5316-5330`). All enforcement layers — reconciler (`kindForSymbol`/`desiredBrokerStopKind`, `broker-protective-stops.ts:121-125, 290-303`), proactive generator, static synthetic rows, bracket enricher — read the persisted value with account policy as fallback when null. One number, all layers: this is what makes ATR plans broker-holdable, short-lane distances correct, and per-trade numerics (Rec 7) safe at all.
**Sketch:** `db.ts` migrate + `db-api-keys.ts:1481-1709`; extend the lot-integrity filter (`:1542-1584`) to the new columns. **Two debate-mandated details:** (a) Guardrails account-level edits apply to *new lots by default* with an explicit "apply to existing positions" action — either default is defensible, silent repricing of existing protection is not; (b) also add the corporate-action re-key: a split adjusts broker `avgCost`, and the ±$0.005 basis filter (verified, `db-api-keys.ts:1576`) then silently drops the plan — detect proportional basis/qty changes, re-key the row, and notify on any plan the filter drops. This is the money path — frontier-tier adversarial review per the fleet rule.

### 4. Short-side broker-held stop lane, landed **before** live shorts flow — **M**
*Unanimous. Sequencing discipline, not paternalism — the owner already decided to enable live shorts (`docs/capability-trading-roadmap.md:71-74`).*
**What/why:** Shorts have unbounded loss and today the thinnest tier: entry bracket legs (strippable) + the synthetic lane (gap-deadlocked until Rec 1, frozen in downtime/halted). Generalize `liveLongs` → `livePositions` (`broker-protective-stops.ts:492-499`), place GTC **buy-stops above** market for `quantity < 0` (native buy-side trailing on Alpaca REST; downward-ratcheting fixed-stop emulation on MCP/Robinhood-style lanes), mirror `canArmTrailingNow`'s inequality (`:918-933`), map exit side via existing `toBrokerSide`/Tradier `buy_to_cover` (`broker-side.ts:4-14`, `tradier.ts:134-135`). Coverage math is already side-aware (`liveExitOrderCoverage(..., "short")`, `broker-side.ts:100-104`), so the double-cover guards carry over. Fix the stale comment at `strategy-risk.ts:102-104`. Secondary: reserve margin against short notional in the sizer (`marginRequirementPct` is declared-but-dead, roadmap:55-59). Gate behind `shortSellingEnabled` + a `brokerStopsForShorts` preference (default on). Verify on paper before live shorts; this is new placement surface on the money path and needs the full `pending_cancel` idempotency treatment the long lane has (`:512-559`).

### 5. Put protection state and the ATR into the prompt; fix the prompt bugs — **S**
*Unanimous, cheapest item, improves every exit decision on day one; prerequisite for the revision verb.*
**What/why:** Add a per-held-position block to `userContent` (`strategy.ts:4249-4364`): plan style + rationale, effective stop price/distance (% and R), enforcement lane (`bracket | broker_stop | native_trail | synthetic | NONE`), trail HWM, resting exit orders, trim-band state, holding days, MAE/MFE-to-date, `earningsDate` for held names (candidates already get `earnIn`, `strategy.ts:5042`; held positions get nothing), and explicit `UNPROTECTED` flags. Everything is already computed in-run: `stopPlanBySymbol`/`stopPlanRationaleBySymbol` (`strategy.ts:789-807`), broker orders fetched for the duplicate-exit guard (`strategy.ts:2775-2806`), synthetic rows in SQLite. Also: serialize per-candidate/per-held `atrStopPct` into evidence (hoist the ATR precompute that currently runs post-LLM at `strategy.ts:1474-1493`; bars are cached) so the flagship instruction at `strategy-prompts.ts:149` becomes followable; fix `strategy-prompts.ts:105` to reference `bracketStopLoss`/`stopPlan` instead of the inexpressible `shortStopLossPct`; one interpreting paragraph in `buildBullSystem`; bump `agentic-strategy` to 2.3.0 (`strategy-prompts.ts:19`). ~40–80 tokens per held name — trivial at book size.

### 6. The hold-and-retune verb: `exitRevisions[]` sibling array + a per-position owner editor — **M–L**
*Unanimous on the capability; shape and gates settled in debate (see Contested Points 1, 4).*
**What/why:** The single largest process defect: the LLM must add risk to adjust risk, and the owner has no per-position lever at all. Add a top-level `exitRevisions[]` array beside `proposals` in the structured-output schema (`strategy.ts:4496-4548`): `{symbol, stopPlan?, stopPrice?/trailPercent? (post-Rec 3/7), takeProfitPrice?, maxHoldingDays?, invalidation?, rationale (required)}`. **The apply path is a DB write, never an inline broker call:** commit via `recordStopPlan` at the live basis (reuse `filterStopPlansByLiveBasis` semantics — not fill-gated, there is no fill) and let the existing convergence machinery do the physical work — teardown queue (`db-api-keys.ts:1620-1711`), reconciler cancel-then-replace (`broker-protective-stops.ts:647-871`), synthetic purge/re-register. The reconcile loop is the only component that has earned the right to cancel protection (`canArmTrailingNow` shared cancel/place gate, `broker-protective-stops.ts:486-490`) — adopt this as a repo rule. Gates (all owner preferences): tighten = auto-eligible (risk-reducing, same privilege exits get at `policy.ts:315-319`); widen/disarm = propose by default, **re-enters the heat math** (a widening revision increases dollars-at-stop; today heat is entry-time-only, `strategy-risk.ts:660-673`) with the approval card showing incremental $-at-stop and offering the paired trim ("widen 8%→12% keeps $ risk flat if you trim ~1/3"); owner can set everything to auto. Churn control: price-space hysteresis (ignore moves <X%) primary, stricter for family-crossing revisions (fixed/atr↔trailing costs a full teardown round-trip with an unprotected interval until the next tick); keep the `"none"`-requires-rationale rule (`strategy.ts:5210`). Amendments to one bracket leg must replace-or-reteardown *both* legs (OCO siblings die together) and the card must say which. Log every revision (from→to, who, why) to an audit table, and log pre/post-revision plans for counterfactual replay (Rec 9) so revision skill is measurable. Same write exposed as an API route + edit control on `positions.tsx`.

### 7. Complete the exit contract at proposal time: `invalidation`, `maxHoldingDays`, `stopBasis` + clamped numerics — **M** (strictly after Rec 3)
*All four papers wanted pieces of this; debate settled the shape (Contested Points 1, 3).*
**What/why:** (a) `invalidation: string|null` required on every opening (today only on `autonomyOverride`, `strategy.ts:4450`) — the falsifiable kill-condition, persisted on the contract, echoed in the Rec 5 block so every re-review becomes hypothesis-testing rather than fresh vibes; clamp per the existing 1000-char pattern (`strategy.ts:5179`). (b) `maxHoldingDays: number|null` — the first time-based exit in the system; on expiry, `generateProactiveRiskProposals` emits a "time stop reached: exit or re-underwrite" **review proposal**, never a silent market order (the exit type owners most want to override); add `"time_stop"` to the `riskExit` union (`types.ts:2142`). (c) `stopBasis: {method: support_level|atr_multiple|thesis_price|account_default, anchor, atrMultiple}` — elicit the *derivation*, recompute server-side from live ATR at fill, clamp to owner-configured bands in `riskRules` (e.g. `atrMultipleRange: [1.5, 4]`), execute live in-band, and persist the resolved distance to the Rec 3 columns so no layer diverges. Sizing coupling: the per-trade resolved distance must flow into `applyDeterministicSizing`'s own-risk basis (today flat %, `strategy-risk.ts:665-673`) or R-based sizing is fiction. Few-shot examples of good stop reasoning and evaluable invalidations in `buildBullSystem` (models write vague conditions unless shown concrete ones).

### 8. Exit observability: the `exit_events` timeline + truthful lane disclosure — **S-M**
*Exec-systems R7, endorsed by all three critiques ("attribution substrate," "political precondition for revision autonomy").*
**What/why:** Eight enforcement layers with precedence means "why is NVDA still open after −9%?" is currently answered by an engineer reading four tables. Build a per-position read model from existing writes: plan committed/revised (who/why), brackets placed/torn down (including the 10-attempt teardown abandonment, `broker-protective-stops.ts:1034-1041` — today an invisible state holding real orphaned orders), synthetic register/ratchet/suppress/bad-tick-skip, cancel-replace cycles and `pending_cancel` stalls, duplicate-exit blocks (`strategy.ts:2775-2806`), which layer closed the lot, and **trigger-price-vs-fill slippage per fire** (quant addition — lane comparisons are biased without it). Surface as an expandable timeline on the position card. Bundle the honesty fixes: Tradier market-entry bracket fallthrough gets the same disclosure note the Robinhood path has (`strategy.ts:5380-5389` is the template) or an owner toggle converting the entry to a marketable limit to preserve OTOCO; fix the false comment at `tradier.ts:683-685` (the synthetic monitor is *not* the fallback lane for fixed/atr plans); one Guardrails line that broker stops trigger RTH-only (gaps fill at the open), and one that under `propose` authority the proactive layers produce approval cards, not protection.

### 9. Counterfactual exit ledger + statistical discipline + analog metadata — **M**
*Quant-ml #3+#8 as amended in debate; the only mechanism by which Recs 6–7 ever become measurably right.*
**What/why:** At lot close, replay the holding period against a menu of exit policies and persist per-lot deltas (`exit_counterfactuals`), aggregated per (style × regime × horizon). **Debate-mandated scope cuts:** v1 covers **fixed / time / no-stop policies only** — trailing counterfactuals on daily OHLC are unknowable (intra-bar HWM-vs-touch ordering) and would launder bias as data; the replay must model **which lane would have fired and when** (synthetic = next-tick market, proactive = up to 59 min late, broker = broker-speed) or it systematically overrates app-resident protection; gap fills book at `min(stop, open)`; when stop and TP share a bar, book the stop. Discipline applies to *every* learning surface (scorecards, tuner): no cell shown below n=10 (prompt) / n=30 (tuner suggestions), empirical-Bayes shrinkage toward parent pools, n reported beside every number, outcomes stamped with `promptVersion` and event covariates (`throughEarnings`, regime-at-exit). Cheapest first step (quant's find): stamp stop style + resolved distance into the episodic-analog documents (`experience-memory.ts` already retrieves `{returnPct, holdingDays, riskExit, mae, mfe}`) — ~20 lines, immediate value. Tuner evidence upgrades route through the existing manual-approval lane only; the riskRules auto-apply wall (`strategy-tuning.ts:1331-1334`) stays.

### 10. Make invisible instruments visible: options/unmanaged positions, read-only — **S**
*Unanimous, 4/4.* Stop silently dropping OCC positions (`tradier.ts:499-506`); add a read-only `getOptionPositions`/unmanaged-positions surface (Tradier already fetches the rows; Robinhood via existing MCP calls, `robinhood.ts:1080-1115`), render under a clearly labeled "unmanaged — no automated exit protection" section in `positions.tsx`, one prose line in the prompt's portfolio context, and notifications on first appearance (assignment), expiry ≤3 sessions, and ITM-at-expiry. No order placement, no `BrokerGateway` change; respect the ×100-multiplier discipline in display (roadmap:133-138). The realistic hazard is assignment producing a position no layer even shows anyone.

### 11. Held-name event triggers + earnings producer — **S-M interim / M end-state**
*Unanimous goal; shape phased per Contested Point 2.* Flip the built-but-inert trigger engine (`TRIGGER_ENGINE`, verified default-off at `triggers.ts:87-88`) into a per-account `triggerConfig` preference, scoped initially to **held-symbol events** (8-K producer `sec8k.ts:11-30`, regime flips `regime-watch.ts:126-129` already wired). The daily LLM budget already exists and is enforced inside `runStrategyOnce` (`src/lib/llm-budget.ts`; `triggers.ts:464-465`), so full-run-per-event is a safe interim. Add an earnings-calendar producer (FMP/Finnhub carry calendars in the existing cascade) firing a review the session before a held name reports. End-state: the scoped review mode (Contested Point 2).

---

## Contested points — rulings

**1. Who owns the stop number?** (trader: clamp and execute LLM numerics now; quant: shadow-log, grant authority at n≥30 positive delta; llm-prompt: elicit the derivation, recompute server-side; exec: nothing until the distance is persisted.)
**Ruling: derivation + clamps + live execution, strictly after the Exit Contract migration; no shadow-authority gate.** The shadow gate dies three deaths: at this account's trade rate n≥30 per comparison is a year during which the prompt/model rotates (the sample scores a generator that no longer exists); the comparison is confounded because stop width should drive position size, so replaying LLM widths against rule-sized positions measures the wrong thing; and the prompt either admits the numbers don't execute (models under-invest in dead fields) or lies. Owner-set clamp bands already bound the worst case to the band edge. But trader's original "honor the numbers in the enricher" is equally rejected as written — it recreates the exact layer-divergence bug PR #1371 fixed (verified comment at `strategy.ts:5343-5348`: every layer prices off the same distance). The safe order is: persist (Rec 3) → elicit `stopBasis` → recompute in-band → write the resolved distance where all layers read it → log everything to the ledger regardless.

**2. Event response: full `runStrategyOnce` vs a scoped single-position review?** (trader/quant: full runs, caps exist; llm-prompt: scoped call for cost and action-space containment.)
**Ruling: phase it — full runs now, scoped review as the end state, built as a *mode* of `runStrategyOnce`, never a parallel pipeline.** The budget objection to full runs is weaker than claimed (llm-budget is real and enforced), so turn the engine on without waiting. But llm-prompt's action-space point stands: an 8-K-triggered full run re-opens the entire entry space at moments of market stress, and full-run cost is why the engine has sat dark since it was built. The scoped mode must share the per-account run lock and the duplicate-exit guard (exec's non-negotiable — overlapping cadence and triggered runs on one symbol double-exit otherwise), share sanitize→policy→placement, include a three-line portfolio summary (trader: a single-position reviewer that can't see concentration or shared factor exposure is dangerous), include the entry-time thesis/regime snapshot, and forbid entries.

**3. Invalidation: prose string vs typed trigger array with a deterministic price watcher?**
**Ruling: string + `maxHoldingDays` in v1; typed `event`/`regime_flip` kinds in v2 wired to the trigger engine; price-kind watchers never.** Trader and quant convincingly killed the price watcher: a price-expressible invalidation *is a stop*, and a second price-trigger machine beside the stop system — different debounce, different idempotency history, same instrument — is precisely the MU-incident failure class. But llm-prompt's counterpunch also lands: purely prose invalidations re-import sycophancy at review time ("guidance was *soft*, not *cut*"). Resolution: time is evaluated deterministically (it can't race a price); event/regime kinds are matched by the already-wired producers so a fired trigger arrives in the prompt as a **fact** ("TRIGGER FIRED: …"), not a question; anything price-shaped is folded into `stopPlan`/`stopBasis`.

**4. Are exit revisions risk-neutral?** (exec: yes, exempt from opening gates; trader: widening is risk-additive in dollars-at-stop.)
**Ruling: asymmetric, trader is right.** Tighten = risk-reducing, auto-eligible, exit-privileged. Widen/disarm = risk-adding: it increases book heat that nothing currently re-checks (heat is computed at entry sizing only, verified `strategy-risk.ts:660-673`), so widening re-enters the heat math, surfaces incremental $-at-stop and new R on the card, defaults to propose, and offers the paired trim to hold $-risk constant. Per the owner philosophy, both directions are settable to auto — the default is a preference, the accounting is not optional.

**5. Persisted contract vs live re-derivation; what does a Guardrails edit mean?**
**Ruling: the persisted row is the contract of record; the synthetic trail keeps ratcheting live; account-level Guardrails edits apply to new lots by default with an explicit "apply to existing positions" action.** Exec's "all layers read persisted only" silently changes today's semantics (an owner who tightens the account stop expects the book to tighten); quant/trader's amendment — make the scope choice explicit, never silent in either direction — is the correct owner-facing shape. Log persisted-vs-live-recomputed distance per tick into `exit_events` so the stale-contract question gets answered empirically.

**6. Mandatory per-position review verdicts every run?** (llm-prompt: yes, with `wouldEnterToday`; trader: rubber-stamping + action bias; quant: hourly self-reports are autocorrelated non-signal.)
**Ruling: keep the probe, drop the cadence.** Require review verdicts (`hold|trim|exit|amend`, `thesisIntact`, `wouldEnterToday`) only on **trigger events and threshold crossings** (fired invalidation, new MAE low, +2R reached, earnings within 5 sessions), with batch "hold, unchanged" otherwise. Persist as telemetry; score verdict *changes* against outcomes; never feed verdict levels to the tuner. The `wouldEnterToday`-no-but-hold-anyway pattern is the disposition-effect signature worth catching — hourly forced verdicts would bury it in noise.

**7. Default for protection-while-halted?** (papers: default on; exec caveat: `halted` may mean "hands off, I'm trading this manually," and the scheduler comment marks the current behavior deliberate.)
**Ruling: ship the toggle default-ON for protective exits, but present it as an explicit semantic choice at enable time** ("halted = no new risk" vs "halted = fully disarmed"), with the note that live exit-order coverage already suppresses double-firing against the owner's own working orders (`synthetic-stops.ts:445-475`). Firing an owner-configured stop is executing the owner's standing instruction, not paternalism. Document the semantics change with a design note per the repo's paper-trail rule.

---

## Longs / shorts / options coverage

**Longs — works today, best-covered class.** Entry brackets (Alpaca/Tradier), native trailing (Alpaca REST), broker-held GTC lanes with ratchet (`broker-protective-stops.ts`), synthetic trailing monitor, hourly proactive exits, deterministic trim bands. Remaining holes are the tail items above: fixed/atr tick coverage (Rec 2), gap deadlock for trailing rows (Rec 1), halted blackout (Rec 1), corporate-action plan drops (Rec 3), Tradier's no-broker-lane + market-entry bracket fallthrough (Recs 3/8). Build order: Rec 1 → 2 → 3.

**Shorts — ~92% built, off-by-default, about to go live; protection tier is inverted.** Proposal/policy/accounting are real: double-gated sides (`strategy-risk.ts:91-94`, `policy.ts:433-459` — a short must declare a stop), notional/exposure caps, FIFO cover P&L (`performance.ts:484-520`), directional brackets (`strategy.ts:5361-5368`), synthetic low-watermark trail firing covers (`synthetic-stops.ts:443-444, 589`). Missing: **any broker-held lane** (`broker-protective-stops.ts:492-499`) and margin modeling. Build order, strictly before live short flow: Rec 1 (the synthetic lane is currently shorts' only downtime-adjacent net, and it gap-deadlocks) → Rec 3 (persisted distances) → Rec 4 (buy-stop lane, paper-verified) → margin reserve in the sizer → then enable. This is sequencing the owner's own decision correctly, not second-guessing it.

**Options — invisible today; visibility first, trading later.** No order surface exists anywhere (roadmap:48-54) and positions are deliberately filtered from the book (`tradier.ts:499-506`), so an assigned or manually-opened contract has no stop, no P&L, no prompt presence, no UI row. Build order: Rec 10 (read-only book + assignment/expiry/ITM alerts + prompt disclosure) now — it is a correctness/honesty matter, not a feature; option *exit machinery* (multi-leg closes, ×100 multiplier audit, expiry sweeps) waits for a deliberate net-new-subsystem effort. Note that every option carries a built-in time invalidation, so the `maxHoldingDays` machinery from Rec 7 is the natural substrate when that day comes.

---

## Proposed phased roadmap

Each phase is independently shippable and valuable; items within a phase are parallelizable agent lanes (board reservations per repo protocol). Phase A requires no output-schema changes and no `position_stop_plans` migration.

### Phase A — Make today's promises true (all S/S-M; ~1 week of parallel lanes)
1. **A1 — Gap-deadlock fix** (Rec 1a): `evaluateStop` confirmation logic + `suspect_price`/`suspect_count` columns + extended-hours real-quote corroboration + session reset. Tests: replay −25% gap; thin-print pre-market sequence; single-spike immunity preserved. Files: `src/lib/synthetic-stops.ts:109-130,528`, `db.ts` migrate, `db-api-keys.ts` upsert (`:900-926` CASE-guard style).
2. **A2 — `protectWhileHalted`** (Rec 1b): scheduler gate (`scheduler.ts:683-696`), Guardrails toggle (`field-defs.ts`), design note for the halted-semantics change. Test: monitor fires exits in halted with toggle on; never registers looser trails.
3. **A3 — Prompt visibility bundle** (Rec 5): protection-state block in `userContent` (`strategy.ts:4249-4364`, sourced from `stopPlanBySymbol` at `:789-807` + broker orders + synthetic rows), ATR into candidate/held evidence (hoist precompute from `strategy.ts:1474-1493`; extend `compactCandidateForPrompt`, `:5012+`), held-position `earningsDate`, `shortStopLossPct` prompt fix (`strategy-prompts.ts:105`), interpreting paragraph, version bump to 2.3.0.
4. **A4 — Honesty notes** (Rec 8 subset): Tradier market-entry bracket disclosure matching `strategy.ts:5380-5389`; fix the false comment at `tradier.ts:683-685`; Guardrails lines for RTH-only broker stops and the propose-authority blind spot (`stop-flow.tsx`).
5. **A5 — Options/unmanaged visibility** (Rec 10): un-filter + typed read-only surface (`tradier.ts:499-506`), `positions.tsx` unmanaged section, assignment/expiry notifications via `db-notifications`.

### Phase B — The Exit Contract and the lanes (M items; the money-path phase, frontier-tier review)
1. **B1 — Contract migration** (Rec 3): columns + fill-commit writes + lot-filter extension + corporate-action re-key/alert.
2. **B2 — All layers read persisted-with-fallback** + Guardrails new-lots-default with explicit retroactive action.
3. **B3 — Static synthetic rows for fixed/atr** (Rec 2), priced from B1.
4. **B4 — Broker-held ATR stops + short buy-stop lane** (Rec 4); paper-verify; then (and only then) live shorts flow.
5. **B5 — `exit_events` timeline** (Rec 8) including fire slippage; surfaces B1–B4's behavior to the owner.
6. **B6 — Eval harness** (precondition for Phase C schema work): schema-conformance fixtures per model in the rotation, hold-bias canary (synthetic dead-thesis fixtures; measure exit/revise rate across prompt versions), `promptVersion` stamping on outcome rows.

### Phase C — Intelligent revision and measurement (M/L items; gated on B6)
1. **C1 — `exitRevisions[]` verb + owner per-position editor** (Rec 6): asymmetric autonomy, heat-recheck + paired trim, hysteresis, audit + pre/post CF logging, Bear/reviewer visibility of revisions.
2. **C2 — `stopBasis` + clamped numerics + sizing coupling** (Rec 7c), executing live in-band.
3. **C3 — `invalidation` + `maxHoldingDays`** (Rec 7a/b) with `"time_stop"` outcome label; expiries emit review proposals.
4. **C4 — Counterfactual ledger + shrinkage discipline + analog stop-style stamping** (Rec 9); tuner evidence upgraded, manual approval unchanged.
5. **C5 — Trigger engine on for held names + earnings producer** (Rec 11, full-run interim), then the scoped review mode of `runStrategyOnce` (shared lock + guards, portfolio context, no entries); event-kind invalidations v2; threshold-triggered review verdicts.
6. **C6 — Broker-native order replacement fast-path** (Alpaca `PATCH /v2/orders/{id}`, Tradier `PUT`): shrinks every ratchet/reprice `pending_cancel` window to ~zero and makes C1 repricing near-atomic; cancel-recreate remains the Robinhood path.

---

## What NOT to do

- **No second price-trigger machine.** Price-kind invalidation watchers running in the tick loop beside the stop system were killed in debate: two lanes reacting to the same price with different idempotency state is the MU-incident (~280 duplicate sells) failure class. Price-shaped invalidations are stops — encode them in `stopPlan`/`stopBasis`.
- **Do not honor LLM numeric stop params in `enrichOpeningProposal` before the contract is persisted.** It recreates the layer-divergence bug PR #1371 fixed (`strategy.ts:5343-5348`); the discard-and-reprice stays until Rec 3 lands.
- **No shadow-mode authority gate for LLM numerics.** Small-n never arrives, the sizing confound makes the comparison meaningless, and the prompt-honesty dilemma poisons the sample. Clamps + derivation + ledger instead.
- **No proposal generation from the tick loop.** It bypasses the run lock and duplicate-exit guard, spams approval cards under `propose`, and races the synthetic lane with different idempotency keys. Static synthetic rows (Rec 2) achieve the goal inside the proven framework.
- **Do not overload `OrderSide` with `"hold"` or a revision value.** `OrderSide` is a named cross-file consistency trap (CLAUDE.md); the sibling `exitRevisions[]` array is the only shape with zero blast radius on the order state machine.
- **No inline cancel-replace of protection from any handler.** Only the reconcile loop cancels resting protection (`broker-protective-stops.ts:486-490` pattern). Adopt as a repo rule.
- **No trailing-stop cells in the daily-OHLC counterfactual ledger.** Intra-bar HWM-vs-touch ordering is unknowable; a daily-bar trail counterfactual is bias dressed as data. Fixed/time/no-stop only until intraday bars exist.
- **No conditional-on-winners excursion rules in the tuner** ("winners' MAE was small → tighten"): survivorship-biased and self-reinforcing toward the whipsaw death spiral. Width evidence comes from full-population counterfactual replay, min-n gated and shrunk.
- **No mandatory hourly per-position verdicts.** Autocorrelated rubber-stamp telemetry plus induced action bias; event/threshold-triggered reviews only.
- **Do not auto-apply riskRules tuner changes.** The write-scope wall (`strategy-tuning.ts:1331-1334`) stays; the panel's work makes the *evidence* for manual approval good.
- **No per-position exit-only LLM call per tick,** and no LLM raw stop prices executing unclamped — cost-prohibitive and trust-inverted respectively; cadence + events + deterministic lanes is the right decomposition.
- **No take-profit "plans" symmetric to stop plans yet.** The deterministic banded trim ratchet (`strategy.ts:5600-5653`) is the best-designed exit component in the system; counterfactual-score it before adding LLM degrees of freedom.
- **No paternalistic hard blocks anywhere in this program.** Every gate above — halted protection, widen-requires-approval, time-stop behavior, trigger scopes, clamp bands — ships as an owner preference with a disclosed default and an off-switch, per the repo's binding product philosophy.
