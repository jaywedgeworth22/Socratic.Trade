# Socratic.Trade Desktop Website (Console) Review — 2026-08-21

Reviewer: DEEPSEEK review subagent (read-only). Repo: `/Users/jay/apps/trading-deepseek` (clean checkout, branch `deepseek/lane`, base `41a7a438d`). Scope: console site at widths ≥1024px, app shell, middleware, live-site probes. Live production sha observed: `e0a4959a7` (health/`release`), ~4 commits behind origin/main.

Board items referenced (not re-filed): 2056ceab (tone-token contrast), 620ef423 (entry paths), fa8dc319 (dead controls — partially fixed), 5d9f6340 (console ships too much — fixed), a3ccc8a9 (home proposal rows — fixed), 30a5e1ba (guardrail copy — fixed), 2d0f31ae (pages disagree), 8badaa3f (copy rules unenforced), 031dbbec (public site halves), 62fb3d7d (overlay/menu keyboard), dbf4b43c (P&L basis labels), 923df16c (account write guards).

## Summary

The console is an unusually disciplined codebase: a single coalesced snapshot data layer with honest staleness labeling, server-authoritative money actions with typed-confirmation rituals, verified-wired controls from the dead-controls cluster, and an accessible-by-design shell (skip link, focus-trap stack, sr-only status). No P0 (money-path/user-blocking) defects were found. Two P1s: a legacy-pending-proposal approve dead-end (client resolves "live" from the row's stamped executionMode while the server uses the current account, so typed confirmation can never be entered — card also mislabels the account "NO ACCOUNT"), and Guardrails' Discard button not discarding the Universe group's draft (discarded edits resurface in a later commit). The rest are P2/P3 polish, a11y, copy-rule, and security-posture gaps (report-only CSP, no per-page tab titles, year-long CDN cache on legal pages).

## Strengths

- **Data honesty**: single 15s poll + SSE with deadline/abort/coalescing (useConsoleData.tsx); failures never blank screens; "delayed/fresh/aging" labels derived, not claimed; Day P&L labels stale baselines instead of hiding them; no "mock/fallback" labels on real data anywhere in scope.
- **Money-path rigor**: typed confirmations server-authoritative (409 + server `expectedText`), approve retries bounded (~3 min) with busy handling, placement outcomes reported as placed/blocked/failed, Discard/Review-and-save prompt with dirty guard wired through nav AND the account selector, policy PUTs serialized client-side to avoid read-merge-write races.
- **Verified wiring**: the three dead-controls fixes (net-of-tax toggle, webhook Send test, preset CRUD) are genuinely wired end-to-end; guardrails "Import from account" no longer arms Autopilot (moved to Strategy with run-state-preserving copy path).
- **Accessibility**: skip link + `tabIndex={-1}` main, stack-aware focus trap with focus restore, sr-only loading/status announcements, roving-tabindex scan tabs, command palette combobox pattern, light-default theme per owner rule.
- **Contrast**: all tone chips pass AA at their used sizes in both themes (verified with the repo's own contrast engine); the #2561 a11y batch is confirmed landed.
- **Freshness of data flows**: watchlist optimistic star reverts on failure; scan keeps last-good on error with labeled notices; per-page polls are visibility-gated and sane (15s snapshot, 30s watchlist, 60s/120s lessons).

## Findings

1. **P1** | **Legacy pending proposals (no stamped executionMode) on a live account can never be approved with typed confirmation — and the card says "NO ACCOUNT"** | `app/console/components/approval-card.tsx:248,258,383-393`; `app/console/lib/derive.ts:52-77`; `src/lib/strategy-execution.ts:257-284` | The card computes reality solely from `pending.executionMode`; a row with NULL mode (legacy/compact rows, see `src/lib/db-proposals.ts:336`) renders tone "none" → chip "NO ACCOUNT · nothing connected yet", and `willPromptTyped` is false, so approve() skips the typed sheet. The server, however, resolves the gate mode from the current account (`executionState.mode`) and 409s LIVE_CONFIRMATION_REQUIRED on every approve; the catch only toasts, so the phrase can never be typed. Same dead-end on bulk approve (`approvals/page.tsx:217-221,196-204`). | Mirror the server's `row.executionMode ?? currentMode` resolution client-side (or stamp execution_mode on pending rows), and in the 409 catch render the server's `expectedText` with a typing input instead of a toast. | M
2. **P1** | **Guardrails "Discard" does not discard Universe-group edits — they resurface in a later commit** | `app/console/guardrails/page.tsx:199-232`; `app/console/components/policy-form.tsx:364-366` | Universe fields (indices, extra symbols, blocklist, order types, sell-to-fund) live in the page's own `universeDraft`, while the save bar's Discard only calls `draft.clear()` on the separate `usePolicyDraft`. After Discard, `extraPatch` is still rebuilt from the dirty `universeDraft`, so the save bar stays ("N uncommitted changes") and the next commit silently persists universe edits the user believed they threw away — on the page that governs what gets scanned/traded. A universe-only edit can never be discarded at all. | Lift a single `discardAll()` that also clears `universeDraft` (and resets inputs to policy values), or move universe fields into the shared draft. | S
3. **P2** | **Production CSP is report-only with `unsafe-inline` + `unsafe-eval` — no effective XSS defense-in-depth** | `middleware.ts:149-164,183-187` (live header observed) | The live site emits `Content-Security-Policy-Report-Only` (CSP_ENABLED on, CSP_REPORT_ONLY not off) with `script-src 'self' 'unsafe-inline' 'unsafe-eval'`. Report-only means violations are never blocked; the permissive script-src means even an enforced policy would allow inline script and eval. For an OAuth app holding session cookies this leaves XSS as a viable session-theft vector with no CSP backstop. | Enforce a tightened policy (hash-based script-src for the Next bootstrap, drop unsafe-eval) once report-only telemetry shows no breakage; at minimum ship an enforcing `default-src 'self'`-based policy. | M
4. **P2** | **Every console screen shares the tab title "Socratic Trade"** | `app/console/layout.tsx:5-7`; `app/console/assistant/page.tsx:5-7` (only metadata exporters, neither sets `title`); root `app/layout.tsx:10-13` | No console page exports a title and no `document.title` is set anywhere in `app/`. On the desktop (the primary multi-tab environment), Home/Scan/Orders/Guardrails/… are indistinguishable in the tab strip — the h1s change but the tab title never does. | Add per-page `metadata.title` (or a shared helper producing e.g. "Orders · Socratic Trade") for each console destination; h1===rail-label canon already exists via `destinationLabel`. | S
5. **P2** | **Evidence sheet can attach the latest run's model/red-team rows to an unrelated older decision** | `app/console/page.tsx:866,1036` | `ProposalRow` calls `deriveEvidenceRows(snapshot, latest, decision)` without the row's own proposal; inside, when a persisted decision has no inline evidence/ragAttributions, `p` falls back to `latest?.proposals?.[0]?.proposal` (page.tsx:866) — the newest run's first proposal, possibly a different symbol. The sheet then renders "Model attribution"/"Adversarial review" rows sourced from that unrelated proposal while the thesis narrative above belongs to the viewed row. | Pass the row's proposal into `deriveEvidenceRows` and only fall back to latest-run data when both are absent (and label the fallback as such). | S
6. **P2** | **Coach "quick action" chips are inert — look like buttons, do nothing** | `app/console/page.tsx:392-396` | "Refocus mandate", "Critique thesis", "Promote lesson" render as three `Chip`s with no onClick/href/handler, directly under copy inviting coaching ("Tell it what it overweighted…"). They read as one-tap actions; clicking does nothing. | Wire them to prefill/route the coach note (placeholder + focus) or restyle as plain legend so they don't look clickable. | S
7. **P2** | **Comparison-account "Realized P&L" is gross while the active bucket is net-of-tax under the same toggle** | `app/console/results/page.tsx:228-232` vs `163,177` | With `taxSettings.subtractFromResults` on, the active bucket gets `netOfTax` + "(net of est. tax)" suffix, but the comparison `BucketCard` (via `bucketFor(...)`) passes no netOfTax and shows raw realized P&L. Two adjacent "Realized P&L" values sit on different tax bases; only a missing suffix distinguishes them. | Pass `netOfTax`/`netRealized` to the comparison card too (using the active account's rates, labeled) or annotate the tile "gross — no tax adjustment"; add an inline basis line (deepens dbf4b43c with a new tile). | S
8. **P2** | **"Send test" races the delivery auto-save queue — can test stale, unsaved prefs** | `app/console/settings/delivery.tsx:243-260,170-180` | Channel toggles persist via queued `persist()` and targets on blur; `sendTest()` fires `POST /api/notifications/test` immediately without flushing those writes. A user who just toggled a channel on (or edited the webhook URL) then clicks Send test can get "skipped — channel is off" or a test delivered to the OLD target. This is a confirmation ritual for alert delivery, so a false result is actively misleading. | Flush/await the pending auto-save chain before firing the test, or pass the current optimistic prefs to the test endpoint. | S
9. **P2** | **Account-scope menu breaks the keyboard contract: focusable invisible backdrop, no arrow nav, no focus move** | `app/console/components/chrome.tsx:236-241,243-275` | The `role="menu"` (aria-haspopup="menu") implements no APG menu pattern: ArrowUp/Down do nothing, focus never moves into the menu on open, and the click-away backdrop is a real `<button aria-label="Close account menu">` (opacity-0, viewport-covering) sitting between the trigger and the menu in DOM order — Tab from the trigger lands on the invisible button, and Enter closes the menu. Menu items also contain a descriptive `<p>` and a divider (247-250, 264) which surface as odd menu entries. | Make the backdrop a non-focusable `<div>` (UserMenu already does this at chrome.tsx:912); implement focus-into-menu on open, Arrow/Home/End movement, Escape with focus return; move the explanatory `<p>` out of `role="menu"`. | M
10. **P2** | **Tooltip descriptions never reach screen readers for interactive triggers** | `app/console/ui/primitives.tsx:501-522` (Btn at 96-99) | `aria-describedby={tooltipId}` and the sr-only copy sit on the wrapper `<span>`, which only gets `tabIndex={0}` when the child is non-interactive. For button/input children, focus lands on the inner element which carries no aria-describedby, so Run-once's "Blocked: …" reason, the scan sort instructions ("Click to sort by X (currently ascending)"), and the Columns trigger's description are never announced. | For single interactive children, clone the child and add `aria-describedby` (and merge `aria-label` for icon buttons) onto the actual focusable element. | M
11. **P2** | **Read notification items fade below AA: 4.24:1 title, 2.79:1 body** | `app/console/components/notification-inbox.tsx:118` (`read && "opacity-60"`) | The whole `<li>` drops to 60% opacity; composited over `--con-surface-2` (#f4f6fa), the 13.5px semibold title (#17202e) is 4.24:1 and the muted detail (#46556b) is 2.79:1 — both under the 4.5:1 AA floor. Dark theme passes (6.31:1). New instance distinct from board 2056ceab (tone tokens). | Raise to `opacity-70` (5.86:1) or use a dedicated read-state token instead of blanket opacity. | S
12. **P2** | **Post-OAuth redirect lands on Home, losing the deep link that sent the user to /login** | `app/login/page.tsx:31-34,44` (all providers `redirectTo: "/"`) | The middleware 307s every gated path to `/login` with no `?next=`/callbackUrl, and every OAuth provider redirects back to `/`. A user who followed a shared deep link (e.g. `/console/orders` or `?proposal=…`) to the login wall is returned to Home after signing in, not to their destination. Deepens board 620ef423 (entry-path dead-ends) with a concrete mechanism. | Carry the original path through login (callbackUrl param, validated to same-origin `/console*`), and have the login server action redirect to it after OAuth. | M
13. **P2** | **Bulk live-approve dead-ends when the client's live-set differs from the server's (null-executionMode rows)** | `app/console/approvals/page.tsx:518-521,217-221,196-204` vs `app/api/proposals/bulk-approve/route.ts:24-26` | Same root cause as finding 1: client counts "live" by stamped executionMode; server counts `row.executionMode ?? currentMode`. When the current account is live and a selected proposal lacks a stamp, the client skips the typed sheet, the server 409s with its own phrase, and the catch only toasts — no input exists anywhere to type it, so the batch can never go through. | Compute the live set with the same `?? currentMode` fallback, and/or on 409 render the server's reasons + expectedText with an input in the sheet. | S/M
14. **P2** | **Legal/welcome pages cached at the CDN for one year — edits can serve stale for a year** | live headers on `/privacy-policy`, `/terms-and-conditions`, `/welcome` (`cache-control: s-maxage=31536000`, `x-nextjs-prerender: 1`) | Fully static prerenders are served with a 1-year shared-cache TTL. A docs-only deploy that changes the privacy policy or terms does not necessarily invalidate the CDN copy, so users (and the consent gate linking to them) can keep seeing the old text for up to a year. | Set an explicit `revalidate`/`Cache-Control` for legal pages (e.g. `s-maxage=3600` + SWR), or purge the CDN cache on deploy for these paths. | S
15. **P3** | **Switching accounts discards the unsaved guardrails draft (consent prompt, but one-way)** | `app/console/guardrails/page.tsx:193` (`key={snapshot.policy.connectedAccountId}`); `app/console/lib/useDirtyGuard.tsx:186-198` | The page remounts on account switch, resetting `draft` + `universeDraft`. The ScopeSelector does route through the 3-option dirty prompt (chrome.tsx:152), so the user is warned — but the only way to switch is to explicitly discard everything, including typed-confirm-gated loosening; there is no "save draft aside / migrate onto the new account" path. | Offer "switch and keep a copy" (migrate draft onto the new account's policy baseline) or block switching while dirty with a clearer option set. | M
16. **P3** | **"Approved" toast overclaims when approve stays `busy` after retry exhaustion** | `app/console/page.tsx:1021`; `app/console/lib/api.ts:343-347` | `approveProposal` retries ~15 times (~3 min) then returns the final `busy` result; `handleApprove` toasts title "Approved" with body "Order status: busy". The server never confirmed placement. | Title the toast from `res.status` (e.g. "Order still submitting — busy"), keep "Approved" only for terminal statuses. | S
17. **P3** | **Typed live-confirmation input has no retry path after a failed approval** | `app/console/page.tsx:1104-1113` | Once `liveConfirm` is set, the approve button is disabled and the only trigger is the input's `onChange` on exact match; if the attempt then fails with a non-409 error, the input still holds the matching text, onChange won't re-fire, and the user must clear and retype the whole phrase to retry. | Trigger on form submit/Enter plus a Retry button, or reset the input when the attempt fails. | S
18. **P3** | **Data Sources numeric rows PATCH on every keystroke** | `app/console/settings/page.tsx:866-880`; `app/console/ui/primitives.tsx:325-330` | `RawNumInput.onValueChange` fires per keystroke and the "only when changed" guard compares against the pre-update `row.value`, so typing "500" issues up to three `PATCH /api/settings/source-features` calls (5, 50, 500) — while every other numeric field on the page commits on blur. Intermediate values also become server-visible momentarily. | Commit numeric source rows on blur, matching the rest of the page. | S
19. **P3** | **Overlays "Max Active" is not clamped to its declared max=6** | `app/console/strategy/overlays-panel.tsx:170-180` | The input declares `min={1} max={6}` but the onChange clamps only the floor (`Math.max(1, …)`); typing 99 persists `maxActiveOverlays: 99` (no server-side clamp found in the write path), and the "Would Fire Now" preview then honors it. | Add `Math.min(6, …)` client-side and validate server-side. | S
20. **P3** | **Decisions index error state has no retry — a transient failure strands the page** | `app/console/decisions/page.tsx:39-48,71-75` | The page fetches `/api/socratic/decisions` once; on failure it renders a message with no Retry control and no auto-retry, while every other list surface (watchlist, api-keys, connections) offers one. The dashboard's sequential broker chain can take ~24s, so a transient miss is realistic. | Add a Retry button to the error card. | S
21. **P3** | **UserMenu theme switcher exposes no selected state to assistive tech** | `app/console/components/chrome.tsx:931-951` | The three Light/Dark/System buttons carry aria-labels but no `aria-pressed`/`aria-current`; the active choice is communicated only by background/border/font-weight. The `Segmented` primitive already does `aria-pressed` correctly. | Add `aria-pressed={active}` to each button. | S
22. **P3** | **Notification inbox panel never moves focus in; Tab escapes behind the scrim** | `app/console/components/notification-inbox.tsx:86-99` | On open, focus stays on the trigger; the panel is not aria-modal and nothing contains focus, so after the last control Tab walks into page content behind the z-40 scrim (focus ring invisible). Escape and focus-return are correct. | Move focus to the panel/first item on open and contain Tab (popover menu pattern). | S
23. **P3** | **Copy: bulk-action buttons are lowercase; Title Case per fleet rules** | `app/console/approvals/page.tsx:364,389,404` | "select visible", "approve selected (N)", "reject selected (N)" are all-lowercase button labels (with ASCII "…"), violating the owner's button Title Case rule. New instances for board 8badaa3f. | "Select Visible", "Approve Selected (3)", "Reject Selected (3)", and use the ellipsis character. | S
24. **P3** | **Copy: ~16 user-facing strings join sentences with a single space** | `app/console/page.tsx:276-277,321,390,502,1103,1296-1299`; `scan/page.tsx:69,111,133,214,222-224`; `guardrails/page.tsx:325-327,349-350,601-604,638`; `settings/page.tsx:384`; `results/page.tsx:258`; `strategy/page.tsx:830-831`; `orders/cancel-sheet.tsx:87`; `settings/brokers.tsx:893` | Single-space joins in user-visible prose, e.g. page.tsx:321 "…at {time}. No snapshot was persisted…", scan/page.tsx:69 "{message} The last good scan stays on screen." `SENTENCE_GAP` (format.ts:54) exists but isn't used. New instances for board 8badaa3f. | Replace with two literal spaces or `{SENTENCE_GAP}`. | M
25. **P3** | **Copy: card titles mix Title Case and sentence case on the same page** | `app/console/page.tsx:266,287,404,430,480,550`; `scan/page.tsx:373`; `guardrails/page.tsx:314` | "Autonomous actions", "Outcome learning loop", "Mark to market", "Risk utilization", "Market scan candidates" are sentence case beside "Previous Trades", "Market Analysis", "Proposal Details" (Title Case); guardrails "Options And Event Contracts" capitalizes "And". | Pick Title Case (owner rule) for all card titles; lowercase conjunctions. | M
26. **P3** | **"AI review pending" button label reads as a passive status, not an action** | `app/console/page.tsx:1292` | The button triggers a bulk LLM review of pending framework proposals, but its resting label is indistinguishable from a status line; only the tooltip explains it's an action. | Imperative label ("Run AI review" / "Review all pending") with the count in caption text. | S
27. **P3** | **Autopilot arming doesn't run the LLM-key/readiness preflight that Run once requires** | `app/console/guardrails/page.tsx:578-597` | `setAuthority("decide")` writes strategyAuthority straight through savePolicy; the run-time enforcement fires later, so a user can arm Autopilot on an account with no LLM key and only discover it when the run can't propose. The same card does show the run block, so this is informational, not a safety hole. | Run `deriveRunBlock(snapshot)` as a preflight before arming, or surface the block inline in the typed ritual. | S
28. **P3** | **Preset safety copy overclaims on the no-account activate path** | `app/console/strategy/page.tsx:930-931` vs `app/console/lib/api.ts:474-477`; `src/lib/db-profiles.ts:753-756,383-404` | "A preset can never start or stop the strategy; your run state is always preserved" is true for the copy-to-account path (applyProfileToAccount preserves systemState), but the no-account `activateProfile` path writes the preset's stored policy — whose doc comment says "including the preset's stored systemState" — into the user-level base policy. A preset saved while Running can carry that state forward. | Either strip systemState in activateStrategyProfile (like the copy path) or scope the copy to the copy path. | S/M
29. **P3** | **Bulk reject hides which proposals failed** | `app/console/approvals/page.tsx:241-247` | The reject loop swallows per-proposal errors (`catch { failed += 1 }`) and the toast reports only "M failed", while the approve path names failing symbols and reasons. | Capture symbol + reason per failure like the approve path. | S
30. **P3** | **Dashboard "Opening orders" renders "0 of 0" when no order cap is configured** | `app/console/page.tsx:536` vs `app/console/guardrails/page.tsx:168` | `limit: String(risk.dailyOrders.limit ?? 0)` displays a zero-looking cap ("used 0 of 0"), while the notional row in the same card correctly renders "no cap" and Guardrails renders "no cap". New instance of board 2d0f31ae (pages disagree). | Render "no cap" when the limit is undefined. | S
31. **P3** | **Copy: uppercase "PAPER" and inconsistent paper-case between broker sheets** | `app/console/settings/brokers.tsx:615,636` ("Connected as Alpaca PAPER Account (NOT Real Money)") vs `781` ("Tradier Sandbox (Paper — NOT Real Money)") | Fleet rule: paper is lowercase ("Alpaca (paper)"), no "NOT Real Money" ceremony. Both strings are off-rule and inconsistent with each other. | Normalize to sentence case in both sheets and the toast. | S
32. **P4** | **Scan header shows the scan age twice when stale** | `app/console/scan/page.tsx:105-126` | When `!isFresh`, the freshness chip renders `<Ago>` ("1h ago") and the adjacent span renders "scanned <Ago>" ("scanned 1h ago") — the same age twice in one row. | Drop the chip's Ago when the "scanned…" line is shown. | S
33. **P4** | **"Regime" value capitalizes a sentence-case value ("Unknown")** | `app/console/page.tsx:590` | `value={macroBoard.regime || "Unknown"}` renders a capitalized fallback where values are sentence case, and never admits "unknown" as a state. | Render the raw label and use lowercase "unknown" fallback. | S
34. **P4** | **/favicon.ico 404s (browsers that request it by default get a 404)** | live probe; `public/` has no favicon.ico | The site declares `<link rel="icon" href="/icon.png">` (fine for modern browsers), but the legacy `/favicon.ico` path returns 404. | Add a `public/favicon.ico` (or redirect). | S
35. **P4** | **`X-Powered-By: Next.js` exposed** | live headers | Minor info disclosure of the framework/version surface. | Strip via headers config. | S
36. **P4** | **Sitemap omits the root URL; public pages never link to the console** | `app/sitemap.ts`; `app/welcome/page.tsx:31` (primary CTA is a mailto), `app/how-it-works/page.tsx:121` (links back to /welcome) | The sitemap lists /welcome, /how-it-works, /design, /legal — no root URL (which 307s to /login anyway). The marketing half links to each other and a mailto but never to the app itself. Deepens board 031dbbec ("public site's two halves do not connect"). | Add an app entry link ("Open the desk") on welcome/how-it-works; include `/` in the sitemap only if indexing is enabled. | S
37. **P4** | **robots.txt: the app's `User-Agent: * Disallow: /` group is dead after Cloudflare's managed `*` Allow group** | live robots.txt; `app/robots.ts` | Cloudflare prepends a managed block whose first `User-agent: *` group allows `/`; the app's later `*` disallow group never wins for generic bots (specific AI-bot disallows still apply). Since indexing is off and the app is noindex, impact is low. | Reorder so the app's `*` disallow precedes Cloudflare's, or rely on the meta noindex + specific bot rules. | S
38. **P4** | **Stale comment in global-error.tsx: "Dark is the app default"** | `app/global-error.tsx` | The comment contradicts the actual default (light, per useConsoleTheme/themeInitScript and the owner rule). | Fix the comment. | S
39. **P4** | **Dead import in approvals page** | `app/console/approvals/page.tsx:11` (`fetchPendingLearnedContext`) | Imported but never referenced. | Remove it. | S
40. **P4** | **Client duplicates the server's live-batch phrase generator** | `app/console/approvals/page.tsx:518-521` vs `src/lib/strategy.ts:331-337` | Hand-copied twin of server logic; today they match and a mismatch is recoverable via the 409, but this is the class of copy that drifts silently. | Share the generator or add a unit test asserting both agree. | S
41. **P4** | **Tooltip popup animation not gated on prefers-reduced-motion** | `app/console/ui/primitives.tsx:523-539` | Framer-Motion JS transforms aren't affected by the CSS reduced-motion block; no MotionConfig reducedMotion="user" anywhere. CSS-transition surfaces handle it correctly. | Wrap in `<MotionConfig reducedMotion="user">`. | S
42. **P4** | **`.con-live-tag` (BROKER) renders at 9.5px — below the design system's 10px micro floor** | `app/console/console.css:636` | The token comment sanctions `--con-fs-2xs` (10px) only for captions/timestamps, never interactive text; the BROKER tag inside money-critical buttons is 9.5px/800. Contrast passes (6.01:1/7.08:1). | Bump to `--con-fs-2xs` and use the token. | S
43. **P4** | **Enter/Space do nothing on focusable tooltip-trigger spans** | `app/console/ui/primitives.tsx:509-517` | Non-interactive triggers become `tabIndex={0}` spans whose "click to toggle" affordance is mouse-only; Enter/Space don't toggle. | Add onKeyDown handling (or document focus-open as the keyboard path). | S
44. **P4** | **Command palette "No matches" `<li>` sits inside `role="listbox"` without an option role** | `app/console/components/command-palette.tsx:282-284` | Listbox children are expected to be options; the empty message can be announced as a phantom option. | Give it `role="option"` (aria-disabled) or render outside the listbox. | S
45. **P4** | **Intro splash intercepts Space/Enter/Escape at window level without target checks** | `app/console/components/intro-canvas.tsx:389-391` | A keystroke meant for any element focused behind the overlay dismisses it; the wrapper is aria-hidden so the affordance is invisible to SR users (reduced-motion and session skip paths do cover them). | Check `e.target` isn't an input/button before skipping. | S

## Quick wins (top 5 — smallest, highest value)

1. **Fix the two P1s**: (a) resolve pending-card reality as `executionMode ?? currentMode` + render a typing input on 409 (approval-card.tsx / approvals/page.tsx); (b) make Guardrails Discard clear `universeDraft` (policy-form.tsx:364-366 + guardrails/page.tsx). Both are small, surgical, and unblock money-path flows.
2. **Per-page tab titles** — one metadata export per console destination (or a shared map); ~15 one-liners, immediate desktop UX win.
3. **Notification read-state contrast** — `opacity-60` → `opacity-70` (notification-inbox.tsx:118); one-word fix, passes AA.
4. **Title Case bulk buttons + single-space copy joins** (approvals/page.tsx:364,389,404 and the ~16 strings) — mechanical, follows the owner's binding copy rules.
5. **Favicon.ico + strip X-Powered-By** — two config lines, cleans browser console 404 noise and a minor info leak.

## Verification notes

Commands run (repo `/Users/jay/apps/trading-deepseek`, live `https://socratictrade.com`):

- Context: `board list --app socratic-trade --status open` (242 rows; cross-referenced 15 items); `git log --oneline -20` (base `41a7a438d`); read `STATUS.md`, `PLAN.md`, `AGENTS.md`, `docs/EFFORT-LOG.md` head, `docs/audits/2026-08-17-{architecture-backend,brokers-data-cascade,web-ios-parity}.md`, `docs/rollouts/2026-08-20-wire-dead-controls.md`, `docs/rollouts/2026-08-17-console-a11y-batch.md`.
- Live probes: `curl -sI https://socratictrade.com/` and `/console` (both 307 → /login); `/welcome`, `/login`, `/privacy-policy` (200); `/api/health` (200, release sha `e0a4959a7`); `/api/live` (401 — live middleware predates #2817's PUBLIC_PREFIXES entry; deploy lag, not a defect); `/robots.txt`, `/sitemap.xml`, `/manifest.webmanifest`, `/icon.png` (200), `/favicon.ico` (404); header inspection of CSP (report-only + unsafe-inline/eval), HSTS, X-Frame-Options, Referrer-Policy, Permissions-Policy, X-Powered-By; `/x.png` control probe confirmed middleware header provenance.
- Code reads: `app/layout.tsx`, `app/page.tsx`, `middleware.ts`, `next.config.mjs`, `app/manifest.ts`, `app/robots.ts`, `app/sitemap.ts`, `app/login/page.tsx`, `app/error.tsx`, `app/global-error.tsx`, `app/console/{layout,page}.tsx`, `components/{shell,chrome,nav,policy-form,approval-card,notification-inbox,consent-gate}.tsx`, `ui/{primitives,sheet,focus-trap,use-overlay,toast}.tsx`, `lib/{api,useConsoleData,derive,format,contrast,useAutoSave,useDirtyGuard,useConsoleTheme}.ts`, `scan/{page,scan-table,use-live-scan,columns}.tsx`, `guardrails/page.tsx`, `approvals/page.tsx`, `settings/{page,delivery,brokers}.tsx`, `strategy/{page,overlays-panel}.tsx`, `results/page.tsx`, `orders/page.tsx`, `decisions/page.tsx`, `usage/page.tsx`, `src/lib/strategy-execution.ts`, `src/lib/db-profiles.ts`, `app/api/policy/route.ts`.
- Contrast math: python re-implementation of `contrast.ts` (sRGB gamma → relative luminance → ratio) for light/dark tokens, chip soft-fills, notification read-opacity composites, accent token.
- Verification of subagent P1s/P2s done first-hand: approve dead-end (approval-card.tsx:248,258,383-393 + strategy-execution.ts:257-284), Guardrails Discard (policy-form.tsx:364-366 + guardrails/page.tsx:199-232), coach chips (page.tsx:392-396), evidence fallback (page.tsx:866,1036), comparison bucket (results/page.tsx:228-232), RawNumInput keystroke persist (settings/page.tsx:866-880), overlays clamp (overlays-panel.tsx:170-180), read-opacity contrast (computed 4.24:1/2.79:1), "0 of 0" (page.tsx:536), tooltip aria-describedby placement (primitives.tsx:501-522).
- Read-only: no repo files modified; scratch notes only in /tmp.
