# DeepSeek review — operator/deploy scripts in `scripts/` (Socratic.Trade)

Repo: /Users/jay/apps/trading-deepseek (origin/main @ 41a7a438d, clean except `docs/EFFORT-LOG.md` locally modified — a doc, not scripts).
READ-ONLY review.  No repo file was modified.

## Summary

The production boot chain and the deploy-latch machinery are fundamentally sound: the exit-code contract is honored in the container (bash supervisor translates spontaneous clean exits to 40, both node wrappers translate signal deaths to 128+N with no `process.kill(process.pid)` re-raise, and the exec chain invokes `node_modules/.bin/next` directly with no `npm run`), secret handling is clean across every reviewed script (no bare `infisical secrets`, no `set -x`/`curl -v`, no cat of `~/.secrets/global-api-keys`, tokens only ever in 0600 curl config files or headers), and the ASCII `$VAR`-adjacency trap is absent (zero matches on the fatal pattern; the flagged non-ASCII bytes are all non-adjacent, so nothing breaks today).  The concrete problems are: (1) `rth-deploy-drain.sh` exits 0 when it could not nudge Coolify, so a failed after-close drain is a green CI job with no alert (the silent-freeze class this machinery exists to catch), and the gh-redeliver path cannot work with GITHUB_TOKEN, so the drain depends entirely on an unverifiable `COOLIFY_DEPLOY_WEBHOOK_URL` secret; (2) the deploy-freshness watchdog has no RTH-latch awareness and will page/alert on every deliberately latched weekday-RTH merge at the 1h mark; (3) `runner-availability.sh` still publishes `VERIFY_RUNNER_STATE` for the retired Mac self-hosted runner / pm2 `trading` lane, and `scripts/ops/fleet-watchdog.service` still references the deleted `fleet-site-watchdog.sh`; (4) the Mac-side litestream restore/drill scripts + `litestream.yml` still describe the dead R2 replica (`region: auto` is R2-only), while the live replica is B2.  Twelve findings total (1×P1, 2×P2, 5×P3, 4×P4).

## Findings

**1. P1** | **rth-deploy-drain.sh exits 0 when it cannot nudge — a failed drain is silently green** | `scripts/rth-deploy-drain.sh:136-139` (`if [ "$nudge_ok" -eq 0 ]; then log "could not nudge Coolify..."; exit 0; fi`), wired by `.github/workflows/rth-deploy-latch.yml:66-71` (drain job runs the script; a 0 exit = job success).  What's wrong: the scheduled after-close recovery job reports SUCCESS even when no nudge happened.  In practice the gh-redeliver path (`rth-deploy-drain.sh:105-125`) almost always fails with GITHUB_TOKEN (it lacks `admin:repo_hooks`; the script itself logs "GITHUB_TOKEN often cannot write hooks"), so the only working path is the `COOLIFY_DEPLOY_WEBHOOK_URL` secret (`:127-134`) — if that secret is unset, the drain can never nudge and always exits 0.  Alerting is then blind: the freshness watchdog's Slack de-dupe (`alert-deploy-freshness.sh:110-117`) and Sentry's [workflow, branch] fingerprint (`sentry-ci-report.yml`, `scripts/sentry-ci-report.py`) group the earlier staleness into one alert, so after the first page nothing new fires; prod can stay behind for days with every automation green — the 2026-08-06 silent-freeze class.  Suggested fix: `exit 1` (fail the job → Sentry pages) when `nudge_ok=0`, and fail loudly at startup if neither a gh hook nor `COOLIFY_DEPLOY_WEBHOOK_URL` is available.  Effort S.

**2. P2** | **Deploy-freshness watchdog pages STALE on deliberately RTH-latched merges** | `scripts/alert-deploy-freshness.sh:202-217` (page when oldest undeployed ≥ 3600s; no RTH awareness), `.github/workflows/deploy-freshness.yml` (cron every 20 min; job fails → Sentry error + failed cron check-in), vs `.github/workflows/rth-deploy-latch.yml` + `Dockerfile` (`RUN tsx scripts/assert-rth-deploy-latch.ts` before `npm ci`).  What's wrong: the 2026-08-18 latch intentionally refuses weekday-RTH image builds, so a 10:00 ET merge is legitimately undeployed until the 16:00 close; the 2026-08-06 watchdog cannot distinguish "latch refused the build" from "webhook silently died" and will exit 3 + page/alert at the 1h mark for every weekday-RTH merge (then the 21:20 UTC drain "resolves" it).  False pages in exactly the class the watchdog exists for train agents/humans to ignore real ones.  Suggested fix: have the workflow/script treat a latched weekday-RTH gap as in-flight (e.g., compute `isMarketOpen` via the latch's own `market-calendar` module and pass a flag, or skip the stale verdict inside the RTH window on weekdays).  Effort M.

**3. P2** | **`runner-availability.sh` still operates the RETIRED Mac self-hosted runner + pm2 `trading` lane** | `scripts/runner-availability.sh:3` ("hybrid CI runner routing"), `:18-19` (requires "GitHub Actions runner process alive" + "pm2 app 'trading' online"), `:30-32` ("Run under pm2 on the production Mac"), `:34,55-56` (pm2 jlist, `Runner.Listener`), `:161-173` (pm2 status check).  What's wrong: the script's entire purpose is publishing `VERIFY_RUNNER_STATE` so `ci.yml` routes the verify gate to a self-hosted Mac runner — but ci.yml already removed that apparatus (`.github/workflows/ci.yml:11-22` explicitly says the router is dead and verify runs on ubuntu-latest), and AGENTS.md is binding: Mac runner `trading-live-mac` deleted 2026-07-21, "DO NOT EVER START, RE-REGISTER, OR REFERENCE ... `trading-live` RUNNER LABELS AGAIN", fleet CI GitHub-hosted only, Mac `trading` pm2 lane retired rollback-only.  The script is dead code whose instructions would violate the rule if followed, and it has no `MAC-LOCAL-PROCESSES.md` row either.  Suggested fix: delete with a rollout note (or mark ROLLBACK-ONLY and strip the runner/pm2 references).  Effort S.

**4. P3** | **Mac-side restore/drill scripts still target the DEAD R2 replica; `litestream.yml` pins R2-only `region: auto`** | `scripts/litestream-restore.sh:2` ("restore ... from the Litestream R2 replica"), `:9` ("for Cloudflare R2"); `scripts/litestream-restore-drill.sh:2,10,20` ("from the R2 replica", "AWS_REGION (default: auto, for Cloudflare R2)"); `litestream.yml:19-25` (`region: auto`, `${AWS_S3_ENDPOINT}`).  What's wrong: `litestream.coolify.yml` (the live in-container config) documents B2 `jays-socratic-trade-eu` @ `s3.eu-central-003.backblazeb2.com` as the LIVE replica, R2 `socratic-trade-bucket` as dead/prunable, and calls out the footgun that BOTH replicas use the identical object path `trading-live/app.db` (a wrong-endpoint delete destroys the active backup).  The Mac-side rollback/drill scripts still claim R2 is the source, and `region: auto` is invalid for B2 (B2 wants the cluster id, e.g. `eu-central-003` — see the note in `litestream.coolify.yml`).  If the Mac `.env.local` is still R2-era, the rollback/drill restores from a dead bucket (or nothing); if B2-era, `region: auto` breaks.  The footgun warning exists only in `litestream.coolify.yml`, not in the Mac scripts.  Suggested fix: update the comments + region, and echo endpoint+bucket before any restore/delete with a guard refusing the R2 bucket for writes.  Effort S.

**5. P3** | **`run-litestream.sh` loads `LITESTREAM_*` vars but `litestream.yml` consumes `AWS_*` vars** | `scripts/run-litestream.sh:13` (`eval "$(grep -E '^LITESTREAM_' .../trading-live/.env.local)"`) vs `litestream.yml:19-25` (`${AWS_S3_BUCKET_NAME}`, `${AWS_S3_ENDPOINT}`, `${AWS_ACCESS_KEY_ID}`, `${AWS_SECRET_ACCESS_KEY}`) and `scripts/litestream-restore.sh:19` (`grep -E '^AWS_'`).  What's wrong: if `.env.local` stores AWS_* (as litestream-restore.sh assumes), run-litestream.sh loads nothing and the Mac rollback replicator starts with no credentials; the two Mac-side helpers disagree on which var family the env file holds.  Also neither script has a `MAC-LOCAL-PROCESSES.md` row (binding rule: "any helper script other agents are expected to run" must be registered).  Suggested fix: align the var family (or load both), and add the row to `/Users/jay/apps/MAC-LOCAL-PROCESSES.md`.  Effort S.

**6. P3** | **ASCII-only rule violations across 7 scripts + githooks (none `$VAR`-adjacent — latent, but rule-breaking)** | `scripts/coolify-prod-start.sh:197`, `scripts/effort-orphan-report.sh:55`, `scripts/fetch-prod-ops-snapshot.sh:10`, `scripts/infisical-secrets-safe.sh:2,50,67`, `scripts/land.sh:2,11,28,35,56,68,70-72,94,96,103,105,111,115,136,150,152,154,177,180,185,196,228,268` (incl. U+26A0 ⚠️ at :228), `scripts/run-litestream.sh:4-5`, `scripts/githooks/pre-commit:13` (🚨 in echo), `scripts/githooks/pre-push:11,29,39-41,48,76`.  What's wrong: em dashes / arrows / box-drawing / emoji in files AGENTS.md mandates "pure ASCII" (the fatal bash-3.2.57 pattern is a non-ASCII byte directly adjacent to `$VAR`).  Hexdump verification on the closest cases (`infisical-secrets-safe.sh:50` — `$a` + space + em dash; `land.sh:103` — `${CURRENT_HOOKS:-unset}'` + space + em dash) confirms none is adjacent, so nothing fails today — but these are exactly the files an agent will edit next, and one edit can create the adjacency.  Suggested fix: replace with ASCII (`---`, `->`), and add the `grep -P '[^\x00-\x7F]' scripts/*.sh` check to `land.sh`'s verify gate or the `verify` CI job.  Effort S.

**7. P3** | **`infisical-prod-cutover.sh` operates the retired Mac pm2 production lane** | `scripts/infisical-prod-cutover.sh:97` (`command -v pm2`), `:273-275` (`pm2 delete` / `pm2 start npm -- ... -- run start:secrets`), `:286` ("Check 'pm2 logs $APP'").  What's wrong: AGENTS.md retires the Mac pm2 `trading` lane (rollback-only) and the boot path is Coolify + `infisical-run.mjs`; this historical cutover tool still drives the old lane and re-uses the exact `npm run` exec-chain class the contract retired (on the Mac, not the container, so no contract violation today).  Verified: its secret values (`infisical-prod-cutover.sh:216-232`) go into a chmod-600 file via `umask 177` + `> "$ENV_FILE"`, never stdout — no leak.  Suggested fix: mark the header ROLLBACK-ONLY / historical, or delete.  Effort S.

**8. P3** | **`scripts/ops/fleet-watchdog.service` references the deleted `fleet-site-watchdog.sh`** | `scripts/ops/fleet-watchdog.service` (`ExecStart=/usr/local/bin/fleet-site-watchdog.sh`).  What's wrong: AGENTS.md states `scripts/ops/fleet-site-watchdog.sh` was deleted 2026-07-31 (it monitored the dead boxes), but the systemd unit that launches it is still committed; if installed on the current box it would restart-fail every 15s, and as repo content it contradicts the documented deletion.  Suggested fix: delete the unit (with a rollout note) or update `ExecStart` to the current watchdog (deploy-freshness is GitHub-side, so deletion is the likely answer).  Effort S.

**9. P4** | **Stale comments referencing retired self-hosted runners (`socratic-ci` / `ci-cpx32`)** | `.github/workflows/security.yml:25`, `.github/workflows/sentry-ci-report.yml:80-83`, `.github/workflows/e2e.yml:8,146`, `.github/workflows/ci.yml:11-22`, `scripts/sync-effort-issues.py:329`.  What's wrong: comments only — `runs-on` verified `ubuntu-latest` in every case (security.yml:26, sentry-ci-report.yml:84-85, e2e.yml:29,102, ci.yml:216,333), so no functional impact; they mislead future readers about fleet CI topology.  Suggested fix: clean up comments.  Effort S.

**10. P4** | **`fetch-prod-ops-snapshot.sh:4` stale "trading-live" comment** | `scripts/fetch-prod-ops-snapshot.sh:4` ("The same token must be set on trading-live ...").  What's wrong: per AGENTS.md, `OPS_DIAGNOSTIC_TOKEN` lives in Coolify prod env (Infisical) and the Mac `trading-live` lane is retired rollback-only.  Comment-only staleness.  Suggested fix: update the comment.  Effort S.

**11. P4** | **`land.sh:171-172` hints `pm2 restart trading-<name>` for previews** | `scripts/land.sh:171-172` ("if your PM2 preview starts erroring ... restart it: pm2 restart trading-$(basename ...)").  What's wrong: previews are retired (AGENTS.md); the correct fix is restarting `npm run dev` in the worktree.  AGENTS.md tolerates leftover pm2 apps, so this is cosmetic, but the hint resurrects retired vocabulary.  Suggested fix: reword the hint.  Effort S.

**12. P4** | **`sync-provider-knobs.sh` uses `usage.jays.services` (UM API) — verify it still resolves** | `scripts/sync-provider-knobs.sh:11,41,51` (`USAGE_API="${USAGE_API:-https://usage.jays.services/api/subscriptions}"`).  What's wrong: not on the retired preview list (trading-beta/claude/codex/cursor/monet/antigravity), but the apex + `*.jays.services` wildcard records were deleted 2026-07-09 and the fleet moved to Hetzner; a per-app record must still exist for this default to work.  Verify-only — if UM is still on-box with its own DNS record this is fine.  Suggested fix: confirm DNS/route or pin the current endpoint.  Effort S.

## Verified clean (checked, no finding)

- Exit-code contract in the container: `scripts/coolify-prod-start.sh:136-163` supervisor (trap → forward TERM/INT → re-wait → translate spontaneous clean exit to **40**; `:152-156`); app invoked as `node_modules/.bin/next start` directly at `:167,191,206,223` — no `npm run` anywhere in the chain (`Dockerfile` `CMD ["bash","scripts/coolify-prod-start.sh"]` → `infisical-run.mjs` → `infisical-app-child.mjs` → next).  `scripts/infisical-run.mjs:245-259` and `scripts/infisical-app-child.mjs:64-75` translate signal deaths to `128+N` and explicitly never re-raise (`process.kill(process.pid` grep across `scripts/` + `src/` = 0 hits).  `Dockerfile` HEALTHCHECK = `GET /api/live` (not `/api/health`), matching AGENTS.md.
- Latch wiring: `Dockerfile` runs `tsx scripts/assert-rth-deploy-latch.ts` BEFORE `npm ci` (exit 2 = RTH refuse, exit 3 = image-noop, both fail the build → last healthy container stays); `scripts/assert-rth-deploy-latch.ts` comment "never call it from coolify-prod-start.sh" honored; the drain re-evaluates the latch and skips image-noop diffs (`rth-deploy-drain.sh:36-52,89-100`).
- `scripts/verify-deploy-sha.sh` correctly asserts containment: `git merge-base --is-ancestor "$EXPECTED_SHA" "$live_full"` at `:185` (matches AGENTS.md "live sha CONTAINS your commit"); exit codes 0-5 well-differentiated; null release sha never passes (`:152-159`).
- `scripts/alert-deploy-freshness.sh` correctly pages on the OLDEST undeployed commit ≥ 1h (`:188-217`), not tip age; Slack de-dupe + Sentry grouping work as documented (the flaw is only the RTH interaction in Finding 2).
- Secrets: no bare `infisical secrets` (only string literals), no `set -x/-v`, no `curl -v`, no `cat` of `~/.secrets/global-api-keys` (handoff-trap grep pattern absent).  `infisical-secrets-safe.sh` prints names/lengths only, redirects `set` output to `/dev/null`, and refuses the LLM runtime key names (`:28,66-68`).  `slack-sync.sh:261-267` writes the token to a 0600 temp curl config, never argv/stdout; `auth.test` response never contains the token (`:340-341`).  `sync-provider-knobs.sh:86-99` same 0600-config pattern and drops the token var; remote Infisical read returns grep-filtered keys only.  `fetch-prod-ops-snapshot.sh:30-33` sends the token only in the `x-ops-token` header.  `ios-ship-testflight.sh` is a pin-guarded wrapper (`ios-fleet-pin.sh --check` at `:13`) that prints nothing; the actual `~/.secrets/appstore-connect.env` consumer is the untracked `/Users/jay/apps/ios-fleet/ship-testflight.sh` (outside this repo; the pin check is the repo's guard).  `infisical-bootstrap-env.mjs:508-515` prints only "ready/not configured" flags.
- Mac-local registry: `slack-sync`/`cursor-slack-sync` ARE on `/Users/jay/apps/MAC-LOCAL-PROCESSES.md` (lines 78,139); `run-litestream.sh`, `litestream-restore.sh`, `runner-availability.sh` are NOT (Finding 5/3 note this).

## Verification notes (exact commands run)

All run from `/Users/jay/apps/trading-deepseek` (origin/main @ `41a7a438d`; note: `git status` shows `docs/EFFORT-LOG.md` locally modified — a doc, not scripts).

- `ls -la scripts/; ls scripts/ops/; git log -1 --oneline; git status --short` — inventory + checkout state.
- ASCII: `perl -e 'for my $f (@ARGV) { open my $fh, "<", $f or next; my $ln=0; while (<$fh>) { $ln++; print "$f:$ln:$_" if /[^\x00-\x7F]/ } }' scripts/*.sh` (BSD grep lacks `-P`; perl equivalent) — 7 files flagged; plus the same scan over `scripts/githooks/*`, `scripts/claude-hooks/*`, `scripts/ops/*`, and every other `.sh` in the repo (`.codex/*.sh`, root) — only githooks flagged.  Adjacency pattern `/\$\{?\w+\}?[^\x00-\x7F]/` = **zero matches**.  Hexdump spot-checks: `sed -n '50p' scripts/infisical-secrets-safe.sh | hexdump -C`, `sed -n '103p;228p' scripts/land.sh | hexdump -C` — all em dashes/emoji separated from `$VAR` by ASCII space/quote.
- Secrets: `grep -rn 'infisical secrets' scripts/`, `grep -rnE 'cat .*\.secrets|source .*\.secrets'`, `grep -rnE 'set -[a-zA-Z]*[xv]|curl .*-v\b'`, `grep -rnE 'echo .*\$?(TOKEN|SECRET|KEY|PASS|API)' scripts/*.sh`, `grep -rnE 'cat [^|]*\$?(TOKEN|SECRET|KEY)|Authorization: Bearer \$\{?[A-Z]' scripts/*.sh`.
- Stale infra: `grep -rnE '141\.148\.182\.224|135\.181\.192\.190|77\.42\.35\.209|m1os7ijf31bg3fanil152e4b|<CI_COOLIFY_SERVER_UUID>|trading-live-mac|socratic-trade-preview|ci-cpx32|fleet-ci-|oracle-ci|socratic-ci' scripts/ .github/ Dockerfile`; `grep -rnE 'trading-beta\.jays|claude\.jays|codex\.jays|cursor\.jays|monet\.jays|antigravity\.jays|\.jays\.services' scripts/ .github/ Dockerfile`; `grep -rn 'pm2' scripts/`.
- Exit-code contract: `grep -rn 'process\.kill(process\.pid' scripts/ src/` (0 hits); read `Dockerfile`, `scripts/coolify-prod-start.sh`, `scripts/infisical-run.mjs`, `scripts/infisical-app-child.mjs` in full.
- Deploy machinery: read `Dockerfile`, `.github/workflows/rth-deploy-latch.yml`, `deploy-freshness.yml`, `sentry-ci-report.yml`, `ci.yml` (lines 200-335), `scripts/assert-rth-deploy-latch.ts`, `src/lib/rth-deploy-latch.ts`, `scripts/rth-deploy-drain.sh`, `scripts/verify-deploy-sha.sh`, `scripts/alert-deploy-freshness.sh`.
- R2/B2: `grep -rnE 'r2\.cloudflarestorage|socratic-trade-bucket|jays-socratic-trade-eu|backblaze|AWS_S3_ENDPOINT|B2_' scripts/`; read `litestream.coolify.yml`, `litestream.yml`, `scripts/run-litestream.sh`, `scripts/litestream-restore.sh`, `scripts/litestream-restore-drill.sh`.
- Mac processes: `grep -nE 'run-litestream|litestream-restore|runner-availability|ios-fleet-pin|isolate-shared-box|slack-sync|infisical' /Users/jay/apps/MAC-LOCAL-PROCESSES.md`.
- Read in full: `land.sh`, `slack-sync.sh`, `infisical-secrets-safe.sh`, `ios-ship-testflight.sh`, `ios-fleet-pin.sh`, `fetch-prod-ops-snapshot.sh`, `cloud-setup.sh`, `isolate-shared-box-batch.sh`, `runner-availability.sh`, `infisical-prod-cutover.sh` (lines 97-290), `sync-provider-knobs.sh` (lines 1-140), `setup-slack-sync.sh` (grep), `scripts/ops/fleet-watchdog.service`.

No script was executed; no network/production-mutating command was run.  `~/.secrets/*` files were never read.
