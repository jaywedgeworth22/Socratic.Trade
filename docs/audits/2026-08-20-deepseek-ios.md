# Socratic.Trade iOS App — Top-to-Bottom Review

Reviewer: DeepSeek fleet review subagent · Read-only (no repo modifications) · Base: clean checkout at origin/main `41a7a438d` (branch `deepseek/lane`)
Date: 2026-08-21 · Board reference: `board list --app socratic-trade --status open` (242 open; iOS items referenced by id below)

## Summary

The iOS app is in unusually good shape for a fleet-built native client: the model/view split is clean, every hand-mirrored route I sampled (chat, quote, symbol-desk, llm-budget, source-features, mobile commands, account deletion, push register, ack) matched its TS producer exactly, copy discipline (two-space sentences, Title Case commands vs sentence-case state words, always-inline nav titles, honest no-data rendering) is enforced by tests, and the push deep-link contract is guarded by a genuine cross-language test that the web side parses. The three known review items are partially resolved: 3b343933 (sign-out) is fixed and tested; the nested stop-loss half of 89249c60 is fixed, but the no-contract-test program remains open; 64f21332 (read-only subset) is roughly a third closed (Coach markdown, Scan header, Activity run-failure visibility). The biggest remaining risks are process-level, not code-level: the required `verify` merge gate never compiles or tests Swift, and there is no privacy manifest despite heavy UserDefaults use (an App Store release blocker the day Apple enforces it). Both `xcodebuild build` and the test-target compile pass clean on this checkout.

## Strengths

- **Architecture discipline**: `@MainActor` stores, `NavigationStack` + value-based links everywhere (never `NavigationView`), `.preferredColorScheme(.light)` forced at the app root (SocraticTradeApp.swift:41), light default honored on every screen including the login sheet and launch background.
- **API contracts verified by hand**: `FullPolicy` now decodes nested `riskRules` stop percents with top-level fallback (DeskModels.swift:368-374); `MobileSnapshot` matches `app/api/mobile/snapshot/route.ts` field-for-field (including `latestScan`, `catalog`, `needsAppConsent`); chat/quote/symbol-desk/llm-budget/source-features/commands/deletion/push payloads all line up with their routes; `OrderCancellation` and `PolicyTightening` payloads match `src/lib/mobile-api.ts` validation exactly (orderId+accountNumber stale-view guard, `expectedCurrent`).
- **Money-path hardening**: typed confirmations for live approves (`APPROVE LIVE <SYMBOL>`, matches `src/lib/strategy.ts:332`), Autopilot/CONFIRM phrases on loosening policy edits, `CommandAttemptTracker` idempotency keys, `proposalActionFeedback` settling on real placement outcomes (`filled/placed/paper`), placement `not_placed`/`busy`/`blocked` honest copy.
- **Push is production-grade**: APNs environment resolved from the embedded provisioning profile (never `#if DEBUG`), token withdrawn BEFORE cookies on sign-out, token reassignment on new sign-in, foreground banner suppression only while the SSE stream is actually up, and the cross-language `PushDeepLinkContractTests` table that `test/apns-deep-link-contract.test.ts` parses — the right pattern for the rest of the contract surface.
- **Copy**: two-space sentences verified across every Swift string literal (scan found only `Avg. Return` abbreviations); UserFacingCopyTests forbids wire enums/route names/Apple jargon; no fabricated data labels (`AccountMetrics` gates win-rate on closed-lot count, realized on fill history, unrealized on broker positions).
- **Resilience choices that are right**: Scan keeps last-good names on a 503 (`keepingLastGood`), client scan timeout (50s) outlasts the server budget (35s), SSE reconnects with 5s backoff, snapshot cache survives cold launch with the real saved-at timestamp, and account-switch commands stay available when the snapshot is stale.
- **Testing**: 15 test files / ~190+ tests; unit coverage of decode edge cases, markdown security (no remote images, no raw HTML), push payload routing, deep-link normalization, policy-tightening math, and the run-state word derivation.

## Findings

**P1** | **The required merge gate never compiles or tests the iOS app, and iOS tests never run in CI** | `.github/workflows/ci.yml` (no xcodebuild/ios steps; only `bash -n` on ship scripts at 279-293), `.github/workflows/ios-build.yml:40-92` (build-only, no test target, separate workflow), ruleset `main-protection` requires only `verify` (`gh api repos/jaywedgeworth22/Socratic.Trade/rulesets/17945518` → required_status_checks: [verify]) | A Swift compile break or a red Swift test target can merge to main (this already happened twice: the 08-13 duplicate `@Binding` merge that compiled nowhere, and the 08-20 STATUS noting 5 test-target build errors sat on clean main because `xcodebuild test` was failing at BUILD). The only iOS CI is a non-required, app-target-only build that runs solely on `ios/**` changes. | Add `xcodebuild test -destination 'platform=iOS Simulator,...'` to the `verify` workflow (or make `ios-build.yml` required and add the test target to it). This is the standing `qa-01`/`qa-02` from the 08-19 review and the STATUS.md "Next" item. | S

**P1** | **No privacy manifest though UserDefaults (a required-reason API) is used — App Store release blocker** | `ios/project.yml` (no `PrivacyInfo.xcprivacy` source; verified none exists in `ios/`), UserDefaults used at MobileStore.swift:150-163, TabPreferences (MobileControlView.swift:94), CoachView.swift:10, SocraticTradeApp.swift:14 | UserDefaults is on Apple's required-reason API list (CA92.1). App Store submission without a `PrivacyInfo.xcprivacy` declaring `CA92.1` trips ITMS-91053. TestFlight uploads have not tripped it (68 builds shipped), so enforcement may be deferred — but the first full App Store release or Apple tightening will block on it. Board item 410bda84 (`ios-build-compliance`) covers this plus the next two findings. | Add a `PrivacyInfo.xcprivacy` (reason `CA92.1` for UserDefaults; file-timestamp/disk-space reasons only if those APIs are added) and list it in `project.yml` sources so `xcodegen generate` keeps it. | S

**P1** | **A single failed background reload flips the staleness gate and disables Approve/Run/Watchlist/Policy, with no polling fallback when SSE is down** | MobileStore.swift:265-268 (`isSnapshotStale` returns `snapshotLoadFailed || … > 180`), MobileStore.swift:309 (`guard let snapshot, !isSnapshotStale(at: now) else { return false }`), MobileStore.swift:350 (`snapshotLoadFailed = true` on any error) | One transient error (deploy-swap 502, flaky carrier) sets `snapshotLoadFailed`; every non-protective command — including `proposal.approve` — is then refused until the NEXT successful load. With SSE down there is no periodic reload (only pull-to-refresh and scene-active), so the desk can sit gated indefinitely. Board item ce75f8d0 (`ios-network-decode`). | Separate transient load failure from age-based staleness: either don't set `snapshotLoadFailed` for a single failed reload (only for consecutive failures), or gate on `now - lastUpdatedAt` alone and let `load()`'s own success clear it; add a 60s background reload when `isStreamConnected == false && isAuthenticated`. | M

**P1** | **Crash vector: `Dictionary(uniqueKeysWithValues:)` traps on duplicate command ids** | MobileStore.swift:46 | `Dictionary(uniqueKeysWithValues: commands.map { ($0.id, $0) })` has a precondition that aborts on duplicate keys. `recentCommands` is server data (`listMobileCommands`), so a db quirk or double-inserted row hard-crashes the app on every `reconcileTrackedCommands` until the row is gone. | Build with uniquing: `Dictionary(commands.map { ($0.id, $0) }, uniquingKeysWith: { first, _ in first })`. | S

**P2** | **Hand-mirrored Swift decoders still have no contract test — 89249c60 only partially addressed** | DeskModels.swift:368-374 (nested riskRules — FIXED), MobileModels.swift:64-74 (every list decoded with `?? []`, masking renames), `Fixtures/policy-contract.json` + MobileModelsTests.swift:409 (hand-authored fixtures only) | The specific stop-loss decode is fixed and tested, but the systemic issue stands: nothing ties `app/api/mobile/snapshot/route.ts` / `app/api/policy/route.ts` payloads to the Swift decoders except static fixtures. A field rename on the server blanks a screen silently (`?? []`), and the fixture program was explicitly deferred ("Broader web-ios-contract-drift fixture program remains out of scope"). | Replicate the push-deep-link pattern: a JSON file generated from the TS route types (or checked-in snapshot of each route's response) that Swift tests decode AND a TS test that asserts the fixture matches the live route. Cover at least `MobileSnapshot`, `FullPolicy`, `MarketScanResponse`, `SymbolDeskInfo`. | M

**P2** | **All-or-nothing snapshot decoding: one bad row blanks the whole app** | MobileModels.swift:64-74 (`try values.decodeIfPresent([Position].self, …) ?? []` with non-optional `Position.symbol/quantity/marketValue` at 255-264; same for orders/commands/notifications) | Any single malformed element inside a list throws the entire `MobileSnapshot` decode, so one bad order row or notification row → `MobileAPIError.decoding` → `snapshotLoadFailed` → the whole app shows the error/stale state instead of the 99% that decoded. The web side already has row-level resilience (`marketScanQuotesFromAudit` kept valid rows); iOS does not. Board item ce75f8d0. | Decode lists element-by-element with a `try?` per element and drop bad rows (or make the non-optional fields optional/lossy). At minimum do this for `recentCommands`, `orders`, `notifications`, `positions`. | M

**P2** | **Tone tokens fail WCAG AA at the sizes they are used** | AppComponents.swift:13-15 (`positive = Color.green`, `warning = Color.orange`, `negative = Color.red`) used as text foreground at 11-12pt in `StatusPill` (AppComponents.swift:571-572) and MetricTile values | System green (#34C759 ≈ 2.4:1) / orange / red on white fail the 4.5:1 AA bar for small text; the pills render colored text on a 12%-opacity tint of the same hue, which is worse. Board item 2056ceab (`visual-tokens-theme-contrast`) — the iOS half is unaddressed. | Define dark-toned semantic tokens for light theme (e.g. green ≈ #1a7f37, orange ≈ #9a6700, red ≈ #b3261e) with light-theme variants for dark backgrounds; assert contrast in a test. | S

**P2** | **iOS still renders a read-only subset of the screens it mirrors — 64f21332 roughly a third closed** | FIXED: Coach markdown (CoachMarkdown.swift), Scan column header (ScanView.swift:131-133), run_failed visibility (Activity inbox + PushNotificationTests contract row), Coach draft copy no longer promises staging (CoachView.swift:332-338). STILL OPEN: per-position P&L (MarketsView.swift:156-162 shows only marketValue + avg cost; web positions show P&L), no equity curve / thesis scorecards on Results (ResultsView.swift — deliberate, documented in 2026-08-20-ios-web-parity.md), Coach Suggested Draft has no stage action (dead-end card), Guardrails Universe index checkboxes (web-only; deferred), Account & Settings reachable only from the Home toolbar avatar (HomeView.swift:33-41; More has no settings row), and whole console pages absent: Strategy, Macro, Decisions, Lessons, Usage, Connections | Each remaining gap is documented as a deliberate deferral, but the aggregate is still "a read-only subset": the owner cannot edit the universe indices, cannot stage a Coach draft, cannot see per-position P&L, and must go to the web for Connections/Strategy/Macro/Decisions/Lessons. | Close in priority order: (1) per-position P&L (data already in `Position.marketValue/averageCost/quantity`), (2) a Settings row in More (two-line change), (3) universe index editor on Guardrails (server `policy.patch` already accepts `includedIndices`), (4) Coach draft → `/api/proposals/from-draft` staging as its own reviewed change. | L (split into S/M items)

**P2** | **Stores violate the repo's own `@Observable` rule and re-publish globally** | MobileStore.swift:115 (`final class MobileStore: ObservableObject`), PushNotifications.swift:238, MobileControlView.swift:88 (TabPreferences) — vs `ios/CLAUDE.md` rule "`@Observable` + `@MainActor` on stores. Never `ObservableObject`" | Every `@Published` write (snapshot load sets ~6 published props) invalidates the whole object for every `@EnvironmentObject` consumer, and the board item notes re-publishing on every SSE line. Works, but contradicts the project rule and costs re-render work. Also: Swift 5.9 language mode with no strict-concurrency checking (project.yml:17-19). Board item 410bda84. | Migrate stores to `@Observable` + `@Environment` (mechanical for `MobileStore`; `PushNotificationCoordinator`/`TabPreferences` similarly), and enable `SWIFT_STRICT_CONCURRENCY = complete` in `project.yml` to surface latent races. | M

**P3** | **Version record drift: project.yml says 1.0.8 while TestFlight is at 1.0.68** | project.yml:23-27 (`MARKETING_VERSION: "1.0.8"`, `CURRENT_PROJECT_VERSION: "202608132022"`) vs fleet train (`/Users/jay/apps/ios-fleet/README.md:89-97`: plain patch increment 1.0.0→1.0.1→…; 08-19 rollouts: "TestFlight 1.0.68") | The ship script passes MARKETING_VERSION on the xcodebuild command line, so builds are correct, but the repo's own "record of what shipped" is 60 versions stale — exactly the drift the file's comment (project.yml:20-30) warns about, and `--sync-project-version` is a documented silent no-op because `xcodegen generate` rewrites the pbxproj from this file. Anyone answering "what version is the repo at?" gets 1.0.8. | Make the fleet ship script also write `project.yml` (the comment's own suggested fix), or sync it in `ios-fleet-pin.sh`; at minimum update both version fields on each ship. | S

**P3** | **Transient error handling: no 5xx retry on GETs; 502/503 reach the user raw** | MobileAPIClient.swift:383-402 (`get` retries only `MobileAPIError.network`; a 502/503 is `serverError` and surfaces immediately), 448-453 (only 521-523 mapped in error copy) | During a deploy swap the app's non-snapshot GETs (`/api/policy`, `/api/scan` handled, `/api/settings/*`, `/api/chat-history`) fail on the first 5xx instead of retrying like the network-error path; the user sees "Something went wrong.  Try again." and must tap Retry. The snapshot path at least keeps last-good. | Extend the `get` retry to `serverError(statusCode:)` in the 502/503/504 range with the existing backoff; keep 4xx as terminal. | S

**P3** | **Fixed-size UIKit chrome ignores Dynamic Type** | AppTypography.swift:70-78 (nav title 17pt bold, tab item 10pt, segmented control 13pt via `uiFont` with fixed sizes — no `relativeTo`) | SwiftUI content scales (all `.app*` styles use `Font.custom(…, relativeTo:)`), but the nav bar, tab bar, and segmented controls are set through appearance proxies with fixed point sizes, so a user with a large Dynamic Type setting gets content that scales while chrome stays tiny (tab labels at 10pt). | Use `UIFontMetrics.default.scaledFont(for:)` for the appearance-proxy fonts (or `Font.custom(_:size:relativeTo:)` equivalents), so chrome tracks the content size category. | S

**P3** | **Sub-44pt hit targets in a few places** | ActivityView.swift:105 (notification filter capsule `.frame(minHeight: 36)`), MobileControlView.swift:397-406 (More pin button = bare `Image` with no min frame/hit padding) | The 44pt minimum is honored on CommandButtons, order cancel, watchlist rows, and scan rows, but the filter segmented control (36pt) and the pin icon button (~20pt icon hit area) fall below it — a real miss for thumb use and VoiceOver touch navigation. | Bump the filter capsules to `minHeight: 44` and give the pin button `.frame(minWidth: 44, minHeight: 44)` (or `.contentShape(Rectangle())` with padding). | S

**P3** | **Every SSE event triggers a full snapshot reload, and scene-active double-loads at launch** | MobileStore.swift:366-371 (`onEvent` → `scheduleReload` → `load()` per payload frame), SocraticTradeApp.swift:107-113 + 145-156 (bootstrap `load()` plus scenePhase `.active` `load()` fire concurrently at first launch; generation guard makes it safe but wasteful) | Each real SSE event (command status change, alert) re-fetches the entire snapshot (~30 commands + positions + orders + notifications + compact scan). Coalesced but sequential (`reloadPending` drains one-by-one), so a burst of events means N full fetches. Board item ce75f8d0 flagged the same. | Debounce SSE-triggered reloads (e.g. reload at most once per 10-15s, or only when the event's command id is not already reflected in `recentCommands`), and skip the scene-active reload when the stream is connected and `lastUpdatedAt` is fresh. | M

**P3** | **`TimelineView(.periodic 30s)` re-evaluates the whole screen content on every tick** | AppComponents.swift:697-706 | Every screen built on `SnapshotScaffold` re-evaluates its entire subtree every 30 seconds even when nothing changed; combined with `@EnvironmentObject` re-renders this is wasted work on scroll-heavy screens (Markets, Scan). | Replace the 30s tick with observation of `store.lastUpdatedAt`/`isStreamConnected` for the staleness banner, or scope the `TimelineView` to just the banner row. | S

**P4** | **"Open and Recent Broker Orders" subtitle overstates the payload** | MarketsView.swift:187 vs `app/api/mobile/snapshot/route.ts` (`orders: snapshot.orders.filter(o => isWorkingOrderState(o.state))`) | The section title promises recent (closed) orders, but the snapshot only ever contains working orders, so a filled order vanishes immediately and "Recent" is never true. | Either ship closed orders in the snapshot (trimmed) or rename the subtitle to "Working Broker Orders". | S

**P4** | **Known markdown gap: blockquote markers still print in Coach** | CoachMarkdown.swift:37-97 (block parser has heading/bullet/ordered/code/table/paragraph; a `> quote` line falls through to paragraph and the `>` renders) — documented in 2026-08-20-ios-web-parity.md | Minor cosmetic gap in the new renderer; harmless but visible when the model replies with blockquotes. | Add a `.quote` block case that strips the leading `>` marker (and renders in the secondary style). | S

**P4** | **No app-icon badge set or cleared** | PushNotifications.swift (no `applicationIconBadgeNumber` anywhere; server `buildApnsPayload` at src/lib/apns.ts:287-294 sends no `badge` field) | The in-app Activity tab badge shows unread counts, but the home-screen icon shows nothing; alerts are only discoverable by opening the app. Not a bug, but a UX gap vs. the badge count the app already tracks. | Have the server include `badge` in the APNs payload, or have the app set `applicationIconBadgeNumber` from `unreadNotificationCount` on each snapshot and clear it when Activity is opened. | S

**P4** | **Hardcoded production origin with no debug/staging override** | MobileAPIClient.swift:61 (`productionBaseURL`), LoginView.swift:265-273 (hardcoded `https://socratictrade.com/api/...` auth URLs), AdminPortalView.swift:39-40 (`portalURL`), DeepLink.swift:47 | Everything targets production with no debug/staging switch, so every local/simulator run exercises the live trading backend (safe for read-only screens, risky during development of money paths). Board item 410bda84 (`ios-build-compliance`). | Route LoginView/AdminPortal/DeepLink through `MobileAPIClient.productionBaseURL` (or a `#if DEBUG` override point), single source for the origin. | S

## Quick wins (top 5)

1. **Add `xcodebuild test` to the required CI gate** (P1) — the highest-leverage change; a red Swift target can merge today.
2. **Ship a `PrivacyInfo.xcprivacy`** declaring CA92.1 for UserDefaults (P1) — one small file, unblocks App Store release.
3. **Fix the `Dictionary(uniqueKeysWithValues:)` crash** (P1) — one line (`uniquingKeysWith:`).
4. **Stop gating Approve/Run on a single failed reload** (P1) — decouple `snapshotLoadFailed` from the command gate, or add a 60s polling fallback when SSE is down.
5. **Contrast-safe tone tokens + 44pt hit targets** (P2/P3) — dark-toned green/orange/red and two minHeight fixes; both are small, testable, and close the AA gap.

## Verification notes

Commands run (all read-only, no repo modification, no simctl):
- `git status` / `git log -3` — clean checkout at `41a7a438d` on `deepseek/lane`.
- `board list --app socratic-trade --status open` (and JSON) — 242 open; verified current state of 64f21332, 3b343933, 89249c60, 410bda84, ce75f8d0, 2056ceab; `board show` returns 404 on the mac-collab API, so item text was read via `board list --json`.
- `gh api repos/jaywedgeworth22/Socratic.Trade/rulesets/17945518` — main-protection ruleset requires exactly `verify`; ios-build.yml is not required.
- Static contract comparison of ~12 Swift decoders vs their `app/api/**` producers (chat, chat-history, quote, symbol-desk, llm-budget, source-features, notifications/ack, mobile snapshot/commands/consent/push/account-deletion, policy).
- Swift string-literal scan for single-space sentence gaps — only `Avg. Return` abbreviations flagged (two-space rule holds).
- **`xcodebuild build -project 'ios/Socratic Trade.xcodeproj' -scheme SocraticTrade -destination 'generic/platform=iOS' CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO` → BUILD SUCCEEDED, exit 0** (only benign "No AppIntents.framework dependency" warning; target arm64-apple-ios18.0, Swift 5.9).
- **`xcodebuild build-for-testing …` (same destination/flags) → TEST BUILD SUCCEEDED, exit 0** — the Swift test target (15 files, ~190 tests) compiles on this checkout; tests were NOT executed (no simulator use per instructions).
